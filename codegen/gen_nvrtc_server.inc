// Generated by codegen.py. Do not edit by hand.

int handle_nvrtcVersion(conn_t *conn) {
  int major{};
  major = {};
  int minor{};
  minor = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(int *, int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcVersion");
  return_value = fn == nullptr ? function_not_found() : fn(&major, &minor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &major, sizeof(int)) < 0 ||
      rpc_write(conn, &minor, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvrtcGetNumSupportedArchs(conn_t *conn) {
  int numArchs{};
  numArchs = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetNumSupportedArchs");
  return_value = fn == nullptr ? function_not_found() : fn(&numArchs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &numArchs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvrtcDestroyProgram(conn_t *conn) {
  nvrtcProgram prog{};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcDestroyProgram");
  return_value = fn == nullptr ? function_not_found() : fn(&prog);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvrtcGetPTXSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t ptxSizeRet{};
  ptxSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetPTXSize");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &ptxSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ptxSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvrtcGetCUBINSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t cubinSizeRet{};
  cubinSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetCUBINSize");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &cubinSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &cubinSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDA_VERSION >= 12000
int handle_nvrtcGetLTOIRSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t LTOIRSizeRet{};
  LTOIRSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetLTOIRSize");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &LTOIRSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &LTOIRSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 12000
int handle_nvrtcGetOptiXIRSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t optixirSizeRet{};
  optixirSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetOptiXIRSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(prog, &optixirSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &optixirSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION < 13000
int handle_nvrtcGetNVVMSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t nvvmSizeRet{};
  nvvmSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetNVVMSize");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &nvvmSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nvvmSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvrtcGetProgramLogSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t logSizeRet{};
  logSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetProgramLogSize");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &logSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &logSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvrtcAddNameExpression(conn_t *conn) {
  nvrtcProgram prog;
  const char *name_expression = nullptr;
  std::size_t name_expression_len;
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_read(conn, &name_expression_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  name_expression = (const char *)malloc(name_expression_len);
  if ((name_expression_len != 0 && name_expression == nullptr) ||
      rpc_read(conn, (void *)name_expression, name_expression_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcAddNameExpression");
  return_value =
      fn == nullptr ? function_not_found() : fn(prog, name_expression);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)name_expression);
  return 0;
ERROR_0:
  free((void *)name_expression);
  return -1;
}

#if CUDA_VERSION >= 12080
int handle_nvrtcGetPCHHeapSize(conn_t *conn) {
  size_t ret{};
  ret = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(size_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetPCHHeapSize");
  return_value = fn == nullptr ? function_not_found() : fn(&ret);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ret, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 12080
int handle_nvrtcSetPCHHeapSize(conn_t *conn) {
  size_t size;
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcSetPCHHeapSize");
  return_value = fn == nullptr ? function_not_found() : fn(size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 12080
int handle_nvrtcGetPCHCreateStatus(conn_t *conn) {
  nvrtcProgram prog;
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetPCHCreateStatus");
  return_value = fn == nullptr ? function_not_found() : fn(prog);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 12080
int handle_nvrtcGetPCHHeapSizeRequired(conn_t *conn) {
  nvrtcProgram prog;
  size_t size{};
  size = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetPCHHeapSizeRequired");
  return_value = fn == nullptr ? function_not_found() : fn(prog, &size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13020
int handle_nvrtcGetTileIRSize(conn_t *conn) {
  nvrtcProgram prog;
  size_t TileIRSizeRet{};
  TileIRSizeRet = {};
  int request_id;
  nvrtcResult return_value;
  using fn_t = nvrtcResult (*)(nvrtcProgram, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prog, sizeof(nvrtcProgram)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvrtc_symbol<fn_t>("nvrtcGetTileIRSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(prog, &TileIRSizeRet);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &TileIRSizeRet, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvrtcResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif
