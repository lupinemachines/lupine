// Generated by codegen.py. Do not edit by hand.

int handle_cusolverMgCreate(conn_t *conn) {
  cusolverMgHandle_t handle{};
  handle = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverMgDestroy(conn_t *conn) {
  cusolverMgHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverMgDeviceSelect(conn_t *conn) {
  cusolverMgHandle_t handle;
  int nbDevices;
  int *deviceId = nullptr;
  size_t deviceId_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &nbDevices, sizeof(int)) < 0 || nbDevices < 0 || false)
    goto ERROR_0;
  deviceId_size = nbDevices * sizeof(int);
  deviceId = (int *)malloc(deviceId_size);
  if (deviceId_size != 0 && deviceId == nullptr)
    goto ERROR_0;
  if ((deviceId_size != 0 && rpc_read(conn, deviceId, deviceId_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgDeviceSelect");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, nbDevices,
                          (nbDevices * sizeof(int) == 0 ? nullptr : deviceId));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceId);
  return 0;
ERROR_0:
  free((void *)deviceId);
  return -1;
}

int handle_cusolverMgCreateDeviceGrid(conn_t *conn) {
  cudaLibMgGrid_t grid{};
  grid = {};
  int32_t numRowDevices;
  int32_t numColDevices;
  int32_t *deviceId = nullptr;
  size_t deviceId_size;
  uint64_t deviceId_count = 0;
  cusolverMgGridMapping_t mapping;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cudaLibMgGrid_t *, int32_t, int32_t,
                                    const int32_t *, cusolverMgGridMapping_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numRowDevices, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &numColDevices, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &deviceId_count, sizeof(deviceId_count)) < 0 || false)
    goto ERROR_0;
  deviceId_size = deviceId_count * sizeof(const int32_t);
  deviceId = (int32_t *)malloc(deviceId_size);
  if (deviceId_size != 0 && deviceId == nullptr)
    goto ERROR_0;
  if ((deviceId_size != 0 && rpc_read(conn, deviceId, deviceId_size) < 0) ||
      rpc_read(conn, &mapping, sizeof(cusolverMgGridMapping_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgCreateDeviceGrid");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&grid, numRowDevices, numColDevices,
               (deviceId_count * sizeof(const int32_t) == 0 ? nullptr
                                                            : deviceId),
               mapping);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &grid, sizeof(cudaLibMgGrid_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceId);
  return 0;
ERROR_0:
  free((void *)deviceId);
  return -1;
}

int handle_cusolverMgDestroyGrid(conn_t *conn) {
  cudaLibMgGrid_t grid;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cudaLibMgGrid_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &grid, sizeof(cudaLibMgGrid_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgDestroyGrid");
  return_value = fn == nullptr ? function_not_found() : fn(grid);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverMgCreateMatrixDesc(conn_t *conn) {
  cudaLibMgMatrixDesc_t desc{};
  desc = {};
  int64_t numRows;
  int64_t numCols;
  int64_t rowBlockSize;
  int64_t colBlockSize;
  cudaDataType dataType;
  cudaLibMgGrid_t grid;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cudaLibMgMatrixDesc_t *, int64_t, int64_t, int64_t,
                           int64_t, cudaDataType, const cudaLibMgGrid_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numRows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &numCols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &rowBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &colBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &grid, sizeof(const cudaLibMgGrid_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgCreateMatrixDesc");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&desc, numRows, numCols, rowBlockSize,
                                    colBlockSize, dataType, grid);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &desc, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverMgDestroyMatrixDesc(conn_t *conn) {
  cudaLibMgMatrixDesc_t desc;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cudaLibMgMatrixDesc_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(cudaLibMgMatrixDesc_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgDestroyMatrixDesc");
  return_value = fn == nullptr ? function_not_found() : fn(desc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverMgSyevd_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  void *W = nullptr;
  size_t W_size;
  uint64_t W_count = 0;
  cudaDataType dataTypeW;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, void **, int, int,
                                    cudaLibMgMatrixDesc_t, void *, cudaDataType,
                                    cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &W_count, sizeof(W_count)) < 0 || false)
    goto ERROR_0;
  W_size = W_count;
  W = (void *)malloc(W_size);
  if (W_size != 0 && W == nullptr)
    goto ERROR_0;
  if ((W_size != 0 && rpc_read(conn, W, W_size) < 0) ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgSyevd_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, (W_count == 0 ? nullptr : W), dataTypeW,
               computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)W);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)W);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgSyevd(conn_t *conn) {
  cusolverMgHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  void *W = nullptr;
  size_t W_size;
  uint64_t W_count = 0;
  cudaDataType dataTypeW;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, void **, int, int,
                                    cudaLibMgMatrixDesc_t, void *, cudaDataType,
                                    cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &W_count, sizeof(W_count)) < 0 || false)
    goto ERROR_0;
  W_size = W_count;
  W = (void *)malloc(W_size);
  if (W_size != 0 && W == nullptr)
    goto ERROR_0;
  if ((W_size != 0 && rpc_read(conn, W, W_size) < 0) ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgSyevd");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, (W_count == 0 ? nullptr : W), dataTypeW,
               computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, W, W_count) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)W);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)W);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgGetrf_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  int M;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  int **array_d_IPIV = nullptr;
  size_t array_d_IPIV_size;
  uint64_t array_d_IPIV_count = 0;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, int, int, void **, int,
                                    int, cudaLibMgMatrixDesc_t, int **,
                                    cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &M, sizeof(int)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_IPIV_count, sizeof(array_d_IPIV_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_IPIV_size = array_d_IPIV_count * sizeof(int *);
  array_d_IPIV = (int **)malloc(array_d_IPIV_size);
  if (array_d_IPIV_size != 0 && array_d_IPIV == nullptr)
    goto ERROR_0;
  if ((array_d_IPIV_size != 0 &&
       rpc_read(conn, array_d_IPIV, array_d_IPIV_size) < 0) ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgGetrf_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, M, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_IPIV_count * sizeof(int *) == 0 ? nullptr
                                                        : array_d_IPIV),
               computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgGetrf(conn_t *conn) {
  cusolverMgHandle_t handle;
  int M;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  int **array_d_IPIV = nullptr;
  size_t array_d_IPIV_size;
  uint64_t array_d_IPIV_count = 0;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, int, int, void **, int,
                                    int, cudaLibMgMatrixDesc_t, int **,
                                    cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &M, sizeof(int)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_IPIV_count, sizeof(array_d_IPIV_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_IPIV_size = array_d_IPIV_count * sizeof(int *);
  array_d_IPIV = (int **)malloc(array_d_IPIV_size);
  if (array_d_IPIV_size != 0 && array_d_IPIV == nullptr)
    goto ERROR_0;
  if ((array_d_IPIV_size != 0 &&
       rpc_read(conn, array_d_IPIV, array_d_IPIV_size) < 0) ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgGetrf");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, M, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_IPIV_count * sizeof(int *) == 0 ? nullptr
                                                        : array_d_IPIV),
               computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgGetrs_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasOperation_t TRANS;
  int N;
  int NRHS;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  int **array_d_IPIV = nullptr;
  size_t array_d_IPIV_size;
  uint64_t array_d_IPIV_count = 0;
  void **array_d_B = nullptr;
  size_t array_d_B_size;
  uint64_t array_d_B_count = 0;
  int IB;
  int JB;
  cudaLibMgMatrixDesc_t descrB;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverMgHandle_t, cublasOperation_t, int, int, void **, int, int,
      cudaLibMgMatrixDesc_t, int **, void **, int, int, cudaLibMgMatrixDesc_t,
      cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &TRANS, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &NRHS, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_IPIV_count, sizeof(array_d_IPIV_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_IPIV_size = array_d_IPIV_count * sizeof(int *);
  array_d_IPIV = (int **)malloc(array_d_IPIV_size);
  if (array_d_IPIV_size != 0 && array_d_IPIV == nullptr)
    goto ERROR_0;
  if ((array_d_IPIV_size != 0 &&
       rpc_read(conn, array_d_IPIV, array_d_IPIV_size) < 0) ||
      rpc_read(conn, &array_d_B_count, sizeof(array_d_B_count)) < 0 || false)
    goto ERROR_0;
  array_d_B_size = array_d_B_count * sizeof(void *);
  array_d_B = (void **)malloc(array_d_B_size);
  if (array_d_B_size != 0 && array_d_B == nullptr)
    goto ERROR_0;
  if ((array_d_B_size != 0 && rpc_read(conn, array_d_B, array_d_B_size) < 0) ||
      rpc_read(conn, &IB, sizeof(int)) < 0 ||
      rpc_read(conn, &JB, sizeof(int)) < 0 ||
      rpc_read(conn, &descrB, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgGetrs_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, TRANS, N, NRHS,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_IPIV_count * sizeof(int *) == 0 ? nullptr
                                                        : array_d_IPIV),
               (array_d_B_count * sizeof(void *) == 0 ? nullptr : array_d_B),
               IB, JB, descrB, computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_B);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_B);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgGetrs(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasOperation_t TRANS;
  int N;
  int NRHS;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  int **array_d_IPIV = nullptr;
  size_t array_d_IPIV_size;
  uint64_t array_d_IPIV_count = 0;
  void **array_d_B = nullptr;
  size_t array_d_B_size;
  uint64_t array_d_B_count = 0;
  int IB;
  int JB;
  cudaLibMgMatrixDesc_t descrB;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverMgHandle_t, cublasOperation_t, int, int, void **, int, int,
      cudaLibMgMatrixDesc_t, int **, void **, int, int, cudaLibMgMatrixDesc_t,
      cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &TRANS, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &NRHS, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_IPIV_count, sizeof(array_d_IPIV_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_IPIV_size = array_d_IPIV_count * sizeof(int *);
  array_d_IPIV = (int **)malloc(array_d_IPIV_size);
  if (array_d_IPIV_size != 0 && array_d_IPIV == nullptr)
    goto ERROR_0;
  if ((array_d_IPIV_size != 0 &&
       rpc_read(conn, array_d_IPIV, array_d_IPIV_size) < 0) ||
      rpc_read(conn, &array_d_B_count, sizeof(array_d_B_count)) < 0 || false)
    goto ERROR_0;
  array_d_B_size = array_d_B_count * sizeof(void *);
  array_d_B = (void **)malloc(array_d_B_size);
  if (array_d_B_size != 0 && array_d_B == nullptr)
    goto ERROR_0;
  if ((array_d_B_size != 0 && rpc_read(conn, array_d_B, array_d_B_size) < 0) ||
      rpc_read(conn, &IB, sizeof(int)) < 0 ||
      rpc_read(conn, &JB, sizeof(int)) < 0 ||
      rpc_read(conn, &descrB, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgGetrs");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, TRANS, N, NRHS,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_IPIV_count * sizeof(int *) == 0 ? nullptr
                                                        : array_d_IPIV),
               (array_d_B_count * sizeof(void *) == 0 ? nullptr : array_d_B),
               IB, JB, descrB, computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)array_d_B);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)array_d_B);
  free((void *)array_d_IPIV);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotrf_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cublasFillMode_t, int,
                                    void **, int, int, cudaLibMgMatrixDesc_t,
                                    cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotrf_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotrf(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int h_info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cublasFillMode_t, int,
                                    void **, int, int, cudaLibMgMatrixDesc_t,
                                    cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &h_info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotrf");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &h_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &h_info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotrs_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  void **array_d_B = nullptr;
  size_t array_d_B_size;
  uint64_t array_d_B_count = 0;
  int IB;
  int JB;
  cudaLibMgMatrixDesc_t descrB;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverMgHandle_t, cublasFillMode_t, int, int, void **, int, int,
      cudaLibMgMatrixDesc_t, void **, int, int, cudaLibMgMatrixDesc_t,
      cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_B_count, sizeof(array_d_B_count)) < 0 || false)
    goto ERROR_0;
  array_d_B_size = array_d_B_count * sizeof(void *);
  array_d_B = (void **)malloc(array_d_B_size);
  if (array_d_B_size != 0 && array_d_B == nullptr)
    goto ERROR_0;
  if ((array_d_B_size != 0 && rpc_read(conn, array_d_B, array_d_B_size) < 0) ||
      rpc_read(conn, &IB, sizeof(int)) < 0 ||
      rpc_read(conn, &JB, sizeof(int)) < 0 ||
      rpc_read(conn, &descrB, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotrs_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, nrhs,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_B_count * sizeof(void *) == 0 ? nullptr : array_d_B),
               IB, JB, descrB, computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_B);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_B);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotrs(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  void **array_d_B = nullptr;
  size_t array_d_B_size;
  uint64_t array_d_B_count = 0;
  int IB;
  int JB;
  cudaLibMgMatrixDesc_t descrB;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int h_info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverMgHandle_t, cublasFillMode_t, int, int, void **, int, int,
      cudaLibMgMatrixDesc_t, void **, int, int, cudaLibMgMatrixDesc_t,
      cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &array_d_B_count, sizeof(array_d_B_count)) < 0 || false)
    goto ERROR_0;
  array_d_B_size = array_d_B_count * sizeof(void *);
  array_d_B = (void **)malloc(array_d_B_size);
  if (array_d_B_size != 0 && array_d_B == nullptr)
    goto ERROR_0;
  if ((array_d_B_size != 0 && rpc_read(conn, array_d_B, array_d_B_size) < 0) ||
      rpc_read(conn, &IB, sizeof(int)) < 0 ||
      rpc_read(conn, &JB, sizeof(int)) < 0 ||
      rpc_read(conn, &descrB, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &h_info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotrs");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, nrhs,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA,
               (array_d_B_count * sizeof(void *) == 0 ? nullptr : array_d_B),
               IB, JB, descrB, computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &h_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &h_info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)array_d_B);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)array_d_B);
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotri_bufferSize(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  cudaDataType computeType;
  int64_t lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cublasFillMode_t, int,
                                    void **, int, int, cudaLibMgMatrixDesc_t,
                                    cudaDataType, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotri_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, computeType, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_A);
  return -1;
}

int handle_cusolverMgPotri(conn_t *conn) {
  cusolverMgHandle_t handle;
  cublasFillMode_t uplo;
  int N;
  void **array_d_A = nullptr;
  size_t array_d_A_size;
  uint64_t array_d_A_count = 0;
  int IA;
  int JA;
  cudaLibMgMatrixDesc_t descrA;
  cudaDataType computeType;
  void **array_d_work = nullptr;
  size_t array_d_work_size;
  uint64_t array_d_work_count = 0;
  int64_t lwork;
  int h_info{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverMgHandle_t, cublasFillMode_t, int,
                                    void **, int, int, cudaLibMgMatrixDesc_t,
                                    cudaDataType, void **, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverMgHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int)) < 0 ||
      rpc_read(conn, &array_d_A_count, sizeof(array_d_A_count)) < 0 || false)
    goto ERROR_0;
  array_d_A_size = array_d_A_count * sizeof(void *);
  array_d_A = (void **)malloc(array_d_A_size);
  if (array_d_A_size != 0 && array_d_A == nullptr)
    goto ERROR_0;
  if ((array_d_A_size != 0 && rpc_read(conn, array_d_A, array_d_A_size) < 0) ||
      rpc_read(conn, &IA, sizeof(int)) < 0 ||
      rpc_read(conn, &JA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(cudaLibMgMatrixDesc_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &array_d_work_count, sizeof(array_d_work_count)) < 0 ||
      false)
    goto ERROR_0;
  array_d_work_size = array_d_work_count * sizeof(void *);
  array_d_work = (void **)malloc(array_d_work_size);
  if (array_d_work_size != 0 && array_d_work == nullptr)
    goto ERROR_0;
  if ((array_d_work_size != 0 &&
       rpc_read(conn, array_d_work, array_d_work_size) < 0) ||
      rpc_read(conn, &lwork, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &h_info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolvermg_symbol<fn_t>("cusolverMgPotri");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, N,
               (array_d_A_count * sizeof(void *) == 0 ? nullptr : array_d_A),
               IA, JA, descrA, computeType,
               (array_d_work_count * sizeof(void *) == 0 ? nullptr
                                                         : array_d_work),
               lwork, &h_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &h_info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)array_d_work);
  free((void *)array_d_A);
  return 0;
ERROR_0:
  free((void *)array_d_work);
  free((void *)array_d_A);
  return -1;
}
