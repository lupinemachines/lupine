// Generated by codegen.py. Do not edit by hand.

#define LUPINE_SYMBOL_NAME_(name) #name
#define LUPINE_SYMBOL_NAME(name) LUPINE_SYMBOL_NAME_(name)

int handle_cufftCreate(conn_t *conn) {
  cufftHandle handle;
  handle = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftCreate));
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftDestroy(conn_t *conn) {
  cufftHandle plan;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftDestroy));
  return_value = fn == nullptr ? function_not_found() : fn(plan);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftEstimate1d(conn_t *conn) {
  int nx;
  cufftType type;
  int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(int, cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftEstimate1d));
  return_value =
      fn == nullptr ? function_not_found() : fn(nx, type, batch, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftEstimate2d(conn_t *conn) {
  int nx;
  int ny;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftEstimate2d));
  return_value =
      fn == nullptr ? function_not_found() : fn(nx, ny, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftEstimate3d(conn_t *conn) {
  int nx;
  int ny;
  int nz;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(int, int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &nz, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftEstimate3d));
  return_value =
      fn == nullptr ? function_not_found() : fn(nx, ny, nz, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftEstimateMany(conn_t *conn) {
  int rank;
  int *n = nullptr;
  size_t n_size;
  int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  int istride;
  int idist;
  int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  int ostride;
  int odist;
  cufftType type;
  int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(int, int *, int *, int, int, int *, int, int,
                               cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(int);
  n = (int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(int);
  if (!inembed_null) {
    inembed = (int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(int)) < 0 ||
      rpc_read(conn, &idist, sizeof(int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(int);
  if (!onembed_null) {
    onembed = (int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(int)) < 0 ||
      rpc_read(conn, &odist, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftEstimateMany));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(rank, (rank * sizeof(int) == 0 ? nullptr : n),
                          (inembed_null ? nullptr : inembed), istride, idist,
                          (onembed_null ? nullptr : onembed), ostride, odist,
                          type, batch, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftExecC2C(conn_t *conn) {
  cufftHandle plan;
  cufftComplex *idata;
  cufftComplex *odata;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cufftComplex *, cufftComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftComplex *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftComplex *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecC2C));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, idata, odata, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftExecC2R(conn_t *conn) {
  cufftHandle plan;
  cufftComplex *idata;
  cufftReal *odata;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftComplex *, cufftReal *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftComplex *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftReal *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecC2R));
  return_value = fn == nullptr ? function_not_found() : fn(plan, idata, odata);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftExecD2Z(conn_t *conn) {
  cufftHandle plan;
  cufftDoubleReal *idata;
  cufftDoubleComplex *odata;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cufftDoubleReal *, cufftDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftDoubleReal *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecD2Z));
  return_value = fn == nullptr ? function_not_found() : fn(plan, idata, odata);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftExecR2C(conn_t *conn) {
  cufftHandle plan;
  cufftReal *idata;
  cufftComplex *odata;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftReal *, cufftComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftReal *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecR2C));
  return_value = fn == nullptr ? function_not_found() : fn(plan, idata, odata);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftExecZ2D(conn_t *conn) {
  cufftHandle plan;
  cufftDoubleComplex *idata;
  cufftDoubleReal *odata;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cufftDoubleComplex *, cufftDoubleReal *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftDoubleComplex *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftDoubleReal *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecZ2D));
  return_value = fn == nullptr ? function_not_found() : fn(plan, idata, odata);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftExecZ2Z(conn_t *conn) {
  cufftHandle plan;
  cufftDoubleComplex *idata;
  cufftDoubleComplex *odata;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftDoubleComplex *,
                               cufftDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &idata, sizeof(cufftDoubleComplex *)) < 0 ||
      rpc_read(conn, &odata, sizeof(cufftDoubleComplex *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftExecZ2Z));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, idata, odata, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUFFT_VERSION >= 11200
int handle_cufftGetPlanPropertyInt64(conn_t *conn) {
  cufftHandle plan;
  cufftProperty property;
  long long int returnPtrValue;
  returnPtrValue = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftProperty, long long int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &property, sizeof(cufftProperty)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetPlanPropertyInt64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(plan, property, &returnPtrValue);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnPtrValue, sizeof(long long int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cufftGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value;
  value = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetProperty));
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftGetSize(conn_t *conn) {
  cufftHandle handle;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSize));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftGetSize1d(conn_t *conn) {
  cufftHandle handle;
  int nx;
  cufftType type;
  int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSize1d));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nx, type, batch, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftGetSize2d(conn_t *conn) {
  cufftHandle handle;
  int nx;
  int ny;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSize2d));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nx, ny, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftGetSize3d(conn_t *conn) {
  cufftHandle handle;
  int nx;
  int ny;
  int nz;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &nz, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSize3d));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nx, ny, nz, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftGetSizeMany(conn_t *conn) {
  cufftHandle handle;
  int rank;
  int *n = nullptr;
  size_t n_size;
  int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  int istride;
  int idist;
  int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  int ostride;
  int odist;
  cufftType type;
  int batch;
  size_t workArea;
  workArea = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int *, int *, int, int, int *,
                               int, int, cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(int);
  n = (int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(int);
  if (!inembed_null) {
    inembed = (int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(int)) < 0 ||
      rpc_read(conn, &idist, sizeof(int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(int);
  if (!onembed_null) {
    onembed = (int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(int)) < 0 ||
      rpc_read(conn, &odist, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSizeMany));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, rank, (rank * sizeof(int) == 0 ? nullptr : n),
                          (inembed_null ? nullptr : inembed), istride, idist,
                          (onembed_null ? nullptr : onembed), ostride, odist,
                          type, batch, &workArea);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workArea, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftGetSizeMany64(conn_t *conn) {
  cufftHandle plan;
  int rank;
  long long int *n = nullptr;
  size_t n_size;
  long long int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  long long int istride;
  long long int idist;
  long long int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  long long int ostride;
  long long int odist;
  cufftType type;
  long long int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, long long int *,
                               long long int *, long long int, long long int,
                               long long int *, long long int, long long int,
                               cufftType, long long int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(long long int);
  n = (long long int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(long long int);
  if (!inembed_null) {
    inembed = (long long int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &idist, sizeof(long long int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(long long int);
  if (!onembed_null) {
    onembed = (long long int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &odist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(long long int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetSizeMany64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(plan, rank, (rank * sizeof(long long int) == 0 ? nullptr : n),
               (inembed_null ? nullptr : inembed), istride, idist,
               (onembed_null ? nullptr : onembed), ostride, odist, type, batch,
               &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftGetVersion(conn_t *conn) {
  int version;
  version = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftGetVersion));
  return_value = fn == nullptr ? function_not_found() : fn(&version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftMakePlan1d(conn_t *conn) {
  cufftHandle plan;
  int nx;
  cufftType type;
  int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftMakePlan1d));
  return_value = fn == nullptr ? function_not_found()
                               : fn(plan, nx, type, batch, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftMakePlan2d(conn_t *conn) {
  cufftHandle plan;
  int nx;
  int ny;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftMakePlan2d));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, nx, ny, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftMakePlan3d(conn_t *conn) {
  cufftHandle plan;
  int nx;
  int ny;
  int nz;
  cufftType type;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int, int, cufftType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &nz, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftMakePlan3d));
  return_value = fn == nullptr ? function_not_found()
                               : fn(plan, nx, ny, nz, type, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftMakePlanMany(conn_t *conn) {
  cufftHandle plan;
  int rank;
  int *n = nullptr;
  size_t n_size;
  int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  int istride;
  int idist;
  int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  int ostride;
  int odist;
  cufftType type;
  int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int *, int *, int, int, int *,
                               int, int, cufftType, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(int);
  n = (int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(int);
  if (!inembed_null) {
    inembed = (int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(int)) < 0 ||
      rpc_read(conn, &idist, sizeof(int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(int);
  if (!onembed_null) {
    onembed = (int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(int)) < 0 ||
      rpc_read(conn, &odist, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftMakePlanMany));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(plan, rank, (rank * sizeof(int) == 0 ? nullptr : n),
                          (inembed_null ? nullptr : inembed), istride, idist,
                          (onembed_null ? nullptr : onembed), ostride, odist,
                          type, batch, &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftMakePlanMany64(conn_t *conn) {
  cufftHandle plan;
  int rank;
  long long int *n = nullptr;
  size_t n_size;
  long long int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  long long int istride;
  long long int idist;
  long long int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  long long int ostride;
  long long int odist;
  cufftType type;
  long long int batch;
  size_t workSize;
  workSize = {};
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, long long int *,
                               long long int *, long long int, long long int,
                               long long int *, long long int, long long int,
                               cufftType, long long int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(long long int);
  n = (long long int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(long long int);
  if (!inembed_null) {
    inembed = (long long int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &idist, sizeof(long long int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(long long int);
  if (!onembed_null) {
    onembed = (long long int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &odist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(long long int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftMakePlanMany64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(plan, rank, (rank * sizeof(long long int) == 0 ? nullptr : n),
               (inembed_null ? nullptr : inembed), istride, idist,
               (onembed_null ? nullptr : onembed), ostride, odist, type, batch,
               &workSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftPlan1d(conn_t *conn) {
  cufftHandle plan;
  plan = {};
  int nx;
  cufftType type;
  int batch;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle *, int, cufftType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftPlan1d));
  return_value =
      fn == nullptr ? function_not_found() : fn(&plan, nx, type, batch);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftPlan2d(conn_t *conn) {
  cufftHandle plan;
  plan = {};
  int nx;
  int ny;
  cufftType type;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle *, int, int, cufftType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftPlan2d));
  return_value = fn == nullptr ? function_not_found() : fn(&plan, nx, ny, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftPlan3d(conn_t *conn) {
  cufftHandle plan;
  plan = {};
  int nx;
  int ny;
  int nz;
  cufftType type;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle *, int, int, int, cufftType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nx, sizeof(int)) < 0 ||
      rpc_read(conn, &ny, sizeof(int)) < 0 ||
      rpc_read(conn, &nz, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftPlan3d));
  return_value =
      fn == nullptr ? function_not_found() : fn(&plan, nx, ny, nz, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftPlanMany(conn_t *conn) {
  cufftHandle plan;
  plan = {};
  int rank;
  int *n = nullptr;
  size_t n_size;
  int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  int istride;
  int idist;
  int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  int ostride;
  int odist;
  cufftType type;
  int batch;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle *, int, int *, int *, int, int,
                               int *, int, int, cufftType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(int);
  n = (int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(int);
  if (!inembed_null) {
    inembed = (int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(int)) < 0 ||
      rpc_read(conn, &idist, sizeof(int)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(int);
  if (!onembed_null) {
    onembed = (int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(int)) < 0 ||
      rpc_read(conn, &odist, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(cufftType)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftPlanMany));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&plan, rank, (rank * sizeof(int) == 0 ? nullptr : n),
               (inembed_null ? nullptr : inembed), istride, idist,
               (onembed_null ? nullptr : onembed), ostride, odist, type, batch);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

#if CUFFT_VERSION >= 11200
int handle_cufftResetPlanProperty(conn_t *conn) {
  cufftHandle plan;
  cufftProperty property;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftProperty);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &property, sizeof(cufftProperty)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftResetPlanProperty));
  return_value = fn == nullptr ? function_not_found() : fn(plan, property);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cufftSetAutoAllocation(conn_t *conn) {
  cufftHandle plan;
  int autoAllocate;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &autoAllocate, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftSetAutoAllocation));
  return_value = fn == nullptr ? function_not_found() : fn(plan, autoAllocate);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUFFT_VERSION >= 11200
int handle_cufftSetPlanPropertyInt64(conn_t *conn) {
  cufftHandle plan;
  cufftProperty property;
  long long int inputValueInt;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftProperty, const long long int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &property, sizeof(cufftProperty)) < 0 ||
      rpc_read(conn, &inputValueInt, sizeof(const long long int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftSetPlanPropertyInt64));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, property, inputValueInt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cufftSetStream(conn_t *conn) {
  cufftHandle plan;
  cudaStream_t stream;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftSetStream));
  return_value = fn == nullptr ? function_not_found() : fn(plan, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftSetWorkArea(conn_t *conn) {
  cufftHandle plan;
  void *workArea;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &workArea, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftSetWorkArea));
  return_value = fn == nullptr ? function_not_found() : fn(plan, workArea);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtClearCallback(conn_t *conn) {
  cufftHandle plan;
  cufftXtCallbackType cbType;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftXtCallbackType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &cbType, sizeof(cufftXtCallbackType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtClearCallback));
  return_value = fn == nullptr ? function_not_found() : fn(plan, cbType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExec(conn_t *conn) {
  cufftHandle plan;
  void *input;
  void *output;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, void *, void *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(void *)) < 0 ||
      rpc_read(conn, &output, sizeof(void *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExec));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, input, output, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptor(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptor));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, input, output, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorC2C(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorC2C));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, input, output, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorC2R(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorC2R));
  return_value = fn == nullptr ? function_not_found() : fn(plan, input, output);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorD2Z(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorD2Z));
  return_value = fn == nullptr ? function_not_found() : fn(plan, input, output);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorR2C(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorR2C));
  return_value = fn == nullptr ? function_not_found() : fn(plan, input, output);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorZ2D(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorZ2D));
  return_value = fn == nullptr ? function_not_found() : fn(plan, input, output);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtExecDescriptorZ2Z(conn_t *conn) {
  cufftHandle plan;
  cudaLibXtDesc *input;
  cudaLibXtDesc *output;
  int direction;
  int request_id;
  cufftResult return_value;
  using fn_t =
      cufftResult (*)(cufftHandle, cudaLibXtDesc *, cudaLibXtDesc *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &input, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &output, sizeof(cudaLibXtDesc *)) < 0 ||
      rpc_read(conn, &direction, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtExecDescriptorZ2Z));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, input, output, direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtGetSizeMany(conn_t *conn) {
  cufftHandle plan;
  int rank;
  long long int *n = nullptr;
  size_t n_size;
  long long int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  long long int istride;
  long long int idist;
  cudaDataType inputtype;
  long long int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  long long int ostride;
  long long int odist;
  cudaDataType outputtype;
  long long int batch;
  size_t workSize;
  workSize = {};
  cudaDataType executiontype;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(
      cufftHandle, int, long long int *, long long int *, long long int,
      long long int, cudaDataType, long long int *, long long int,
      long long int, cudaDataType, long long int, size_t *, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(long long int);
  n = (long long int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(long long int);
  if (!inembed_null) {
    inembed = (long long int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &idist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &inputtype, sizeof(cudaDataType)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(long long int);
  if (!onembed_null) {
    onembed = (long long int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &odist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &outputtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &batch, sizeof(long long int)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtGetSizeMany));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(plan, rank, (rank * sizeof(long long int) == 0 ? nullptr : n),
               (inembed_null ? nullptr : inembed), istride, idist, inputtype,
               (onembed_null ? nullptr : onembed), ostride, odist, outputtype,
               batch, &workSize, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftXtMakePlanMany(conn_t *conn) {
  cufftHandle plan;
  int rank;
  long long int *n = nullptr;
  size_t n_size;
  long long int *inembed = nullptr;
  size_t inembed_size;
  uint8_t inembed_null = 0;
  long long int istride;
  long long int idist;
  cudaDataType inputtype;
  long long int *onembed = nullptr;
  size_t onembed_size;
  uint8_t onembed_null = 0;
  long long int ostride;
  long long int odist;
  cudaDataType outputtype;
  long long int batch;
  size_t workSize;
  workSize = {};
  cudaDataType executiontype;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(
      cufftHandle, int, long long int *, long long int *, long long int,
      long long int, cudaDataType, long long int *, long long int,
      long long int, cudaDataType, long long int, size_t *, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 || rank < 0 || false)
    goto ERROR_0;
  n_size = rank * sizeof(long long int);
  n = (long long int *)malloc(n_size);
  if (n_size != 0 && n == nullptr)
    goto ERROR_0;
  if ((n_size != 0 && rpc_read(conn, n, n_size) < 0) || rank < 0 ||
      rpc_read(conn, &inembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  inembed_size = rank * sizeof(long long int);
  if (!inembed_null) {
    inembed = (long long int *)malloc(inembed_size);
    if (inembed_size != 0 && inembed == nullptr)
      goto ERROR_0;
  }
  if ((!inembed_null && inembed_size != 0 &&
       rpc_read(conn, inembed, inembed_size) < 0) ||
      rpc_read(conn, &istride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &idist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &inputtype, sizeof(cudaDataType)) < 0 || rank < 0 ||
      rpc_read(conn, &onembed_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  onembed_size = rank * sizeof(long long int);
  if (!onembed_null) {
    onembed = (long long int *)malloc(onembed_size);
    if (onembed_size != 0 && onembed == nullptr)
      goto ERROR_0;
  }
  if ((!onembed_null && onembed_size != 0 &&
       rpc_read(conn, onembed, onembed_size) < 0) ||
      rpc_read(conn, &ostride, sizeof(long long int)) < 0 ||
      rpc_read(conn, &odist, sizeof(long long int)) < 0 ||
      rpc_read(conn, &outputtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &batch, sizeof(long long int)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtMakePlanMany));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(plan, rank, (rank * sizeof(long long int) == 0 ? nullptr : n),
               (inembed_null ? nullptr : inembed), istride, idist, inputtype,
               (onembed_null ? nullptr : onembed), ostride, odist, outputtype,
               batch, &workSize, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return 0;
ERROR_0:
  free((void *)onembed);
  free((void *)inembed);
  free((void *)n);
  return -1;
}

int handle_cufftXtQueryPlan(conn_t *conn) {
  cufftHandle plan;
  void *queryStruct = nullptr;
  cufftXtQueryType queryType;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, void *, cufftXtQueryType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 || false)
    goto ERROR_0;
  queryStruct = (void *)malloc(96);
  if ((96 != 0 && queryStruct == nullptr) ||
      rpc_read(conn, &queryType, sizeof(cufftXtQueryType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtQueryPlan));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, queryStruct, queryType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, queryStruct, 96) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)queryStruct);
  return 0;
ERROR_0:
  free((void *)queryStruct);
  return -1;
}

int handle_cufftXtSetCallbackSharedSize(conn_t *conn) {
  cufftHandle plan;
  cufftXtCallbackType cbType;
  size_t sharedSize;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftXtCallbackType, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &cbType, sizeof(cufftXtCallbackType)) < 0 ||
      rpc_read(conn, &sharedSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtSetCallbackSharedSize));
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, cbType, sharedSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cufftXtSetGPUs(conn_t *conn) {
  cufftHandle handle;
  int nGPUs;
  int *whichGPUs = nullptr;
  size_t whichGPUs_size;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &nGPUs, sizeof(int)) < 0 || nGPUs < 0 || false)
    goto ERROR_0;
  whichGPUs_size = nGPUs * sizeof(int);
  whichGPUs = (int *)malloc(whichGPUs_size);
  if (whichGPUs_size != 0 && whichGPUs == nullptr)
    goto ERROR_0;
  if ((whichGPUs_size != 0 && rpc_read(conn, whichGPUs, whichGPUs_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtSetGPUs));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nGPUs, (nGPUs * sizeof(int) == 0 ? nullptr : whichGPUs));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)whichGPUs);
  return 0;
ERROR_0:
  free((void *)whichGPUs);
  return -1;
}

int handle_cufftXtSetWorkAreaPolicy(conn_t *conn) {
  cufftHandle plan;
  cufftXtWorkAreaPolicy policy;
  size_t *workSize_null_check;
  size_t workSize;
  int request_id;
  cufftResult return_value;
  using fn_t = cufftResult (*)(cufftHandle, cufftXtWorkAreaPolicy, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cufftHandle)) < 0 ||
      rpc_read(conn, &policy, sizeof(cufftXtWorkAreaPolicy)) < 0 ||
      rpc_read(conn, &workSize_null_check, sizeof(size_t *)) < 0 ||
      (workSize_null_check && rpc_read(conn, &workSize, sizeof(size_t)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cufft_symbol<fn_t>(LUPINE_SYMBOL_NAME(cufftXtSetWorkAreaPolicy));
  return_value = fn == nullptr ? function_not_found()
                               : fn(plan, policy,
                                    workSize_null_check ? &workSize : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cufftResult)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
