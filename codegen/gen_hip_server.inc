// Generated by codegen.py. Do not edit by hand.

#define LUPINE_SYMBOL_NAME_(name) #name
#define LUPINE_SYMBOL_NAME(name) LUPINE_SYMBOL_NAME_(name)

int handle_hipInit(conn_t *conn) {
  unsigned int flags;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipInit));
  return_value = fn == nullptr ? function_not_found() : fn(flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipGetDeviceCount(conn_t *conn) {
  int count;
  count = {};
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipGetDeviceCount));
  return_value = fn == nullptr ? function_not_found() : fn(&count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipDeviceGet(conn_t *conn) {
  int device;
  device = {};
  int ordinal;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ordinal, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipDeviceGet));
  return_value = fn == nullptr ? function_not_found() : fn(&device, ordinal);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipGetLastError(conn_t *conn) {
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipGetLastError));
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipExtGetLastError(conn_t *conn) {
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipExtGetLastError));
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipPeekAtLastError(conn_t *conn) {
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipPeekAtLastError));
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipGetDevicePropertiesR0600(conn_t *conn) {
  hipDeviceProp_tR0600 prop;
  std::memset(&prop, 0, sizeof(prop));
  int deviceId;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(hipDeviceProp_tR0600 *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &deviceId, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipGetDevicePropertiesR0600));
  return_value = fn == nullptr ? function_not_found() : fn(&prop, deviceId);

  prop.hdpMemFlushCntl = nullptr;
  prop.hdpRegFlushCntl = nullptr;

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &prop, sizeof(hipDeviceProp_tR0600)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipDeviceGetName(conn_t *conn) {
  int len;
  char *name = nullptr;
  int deviceId;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(char *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &len, sizeof(int)) < 0 || len < 0 || false)
    goto ERROR_0;
  name = (char *)malloc(len * sizeof(char));
  if ((len * sizeof(char) != 0 && name == nullptr) ||
      rpc_read(conn, &deviceId, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipDeviceGetName));
  return_value = fn == nullptr ? function_not_found()
                               : fn((len * sizeof(char) == 0 ? nullptr : name),
                                    len, deviceId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, name, len * sizeof(char)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)name);
  return 0;
ERROR_0:
  free((void *)name);
  return -1;
}

int handle_hipDeviceTotalMem(conn_t *conn) {
  size_t bytes;
  bytes = {};
  int deviceId;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(size_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &deviceId, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipDeviceTotalMem));
  return_value = fn == nullptr ? function_not_found() : fn(&bytes, deviceId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipDeviceGetAttribute(conn_t *conn) {
  int pi;
  pi = {};
  hipDeviceAttribute_t attr;
  int deviceId;
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(int *, hipDeviceAttribute_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attr, sizeof(hipDeviceAttribute_t)) < 0 ||
      rpc_read(conn, &deviceId, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipDeviceGetAttribute));
  return_value = fn == nullptr ? function_not_found() : fn(&pi, attr, deviceId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pi, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipDriverGetVersion(conn_t *conn) {
  int driverVersion;
  driverVersion = {};
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipDriverGetVersion));
  return_value = fn == nullptr ? function_not_found() : fn(&driverVersion);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &driverVersion, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_hipRuntimeGetVersion(conn_t *conn) {
  int runtimeVersion;
  runtimeVersion = {};
  int request_id;
  hipError_t return_value;
  using fn_t = hipError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = hip_symbol<fn_t>(LUPINE_SYMBOL_NAME(hipRuntimeGetVersion));
  return_value = fn == nullptr ? function_not_found() : fn(&runtimeVersion);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &runtimeVersion, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(hipError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
