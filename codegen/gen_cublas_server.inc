// Generated by codegen.py. Do not edit by hand.

int handle_cublasCreate_v2(conn_t *conn) {
  cublasHandle_t handle;
  handle = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCreate_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDestroy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDestroy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetVersion_v2(conn_t *conn) {
  cublasHandle_t handle;
  int version;
  version = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetVersion_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value;
  value = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetProperty");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetWorkspace_v2(conn_t *conn) {
  cublasHandle_t handle;
  void *workspace;
  size_t workspaceSizeInBytes;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetWorkspace_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, workspace, workspaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetStream_v2(conn_t *conn) {
  cublasHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetStream_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetStream_v2(conn_t *conn) {
  cublasHandle_t handle;
  cudaStream_t streamId;
  streamId = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetStream_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetPointerMode_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasPointerMode_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasPointerMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetPointerMode_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasPointerMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetPointerMode_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasPointerMode_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasPointerMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasPointerMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetPointerMode_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetAtomicsMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasAtomicsMode_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasAtomicsMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetAtomicsMode");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasAtomicsMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetAtomicsMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasAtomicsMode_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasAtomicsMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasAtomicsMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetAtomicsMode");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetMathMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasMath_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasMath_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetMathMode");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasMath_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetMathMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasMath_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasMath_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasMath_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetMathMode");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetSmCountTarget(conn_t *conn) {
  cublasHandle_t handle;
  int smCountTarget;
  smCountTarget = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetSmCountTarget");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &smCountTarget);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &smCountTarget, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetSmCountTarget(conn_t *conn) {
  cublasHandle_t handle;
  int smCountTarget;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &smCountTarget, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetSmCountTarget");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, smCountTarget);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDA_VERSION >= 13030
int handle_cublasGetEmulationStrategy(conn_t *conn) {
  cublasHandle_t handle;
  cublasEmulationStrategy_t emulationStrategy;
  emulationStrategy = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t,
                                              cublasEmulationStrategy_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetEmulationStrategy");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &emulationStrategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &emulationStrategy, sizeof(cublasEmulationStrategy_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetEmulationStrategy(conn_t *conn) {
  cublasHandle_t handle;
  cublasEmulationStrategy_t emulationStrategy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasEmulationStrategy_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &emulationStrategy, sizeof(cublasEmulationStrategy_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetEmulationStrategy");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, emulationStrategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasGetEmulationSpecialValuesSupport(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationSpecialValuesSupport mask;
  mask = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cudaEmulationSpecialValuesSupport *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetEmulationSpecialValuesSupport");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetEmulationSpecialValuesSupport(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationSpecialValuesSupport mask;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cudaEmulationSpecialValuesSupport);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetEmulationSpecialValuesSupport");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasGetFixedPointEmulationMantissaControl(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationMantissaControl mantissaControl;
  mantissaControl = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t,
                                              cudaEmulationMantissaControl *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetFixedPointEmulationMantissaControl");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &mantissaControl);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaControl, sizeof(cudaEmulationMantissaControl)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetFixedPointEmulationMantissaControl(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationMantissaControl mantissaControl;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t,
                                              cudaEmulationMantissaControl);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaControl, sizeof(cudaEmulationMantissaControl)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetFixedPointEmulationMantissaControl");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, mantissaControl);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasGetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cublasHandle_t handle;
  int maxMantissaBitCount;
  maxMantissaBitCount = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetFixedPointEmulationMaxMantissaBitCount");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &maxMantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &maxMantissaBitCount, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cublasHandle_t handle;
  int maxMantissaBitCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &maxMantissaBitCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetFixedPointEmulationMaxMantissaBitCount");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, maxMantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasGetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cublasHandle_t handle;
  int mantissaBitOffset;
  mantissaBitOffset = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetFixedPointEmulationMantissaBitOffset");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitOffset, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cublasHandle_t handle;
  int mantissaBitOffset;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitOffset, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetFixedPointEmulationMantissaBitOffset");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasGetFixedPointEmulationMantissaBitCountPointer(conn_t *conn) {
  cublasHandle_t handle;
  int *mantissaBitCount;
  mantissaBitCount = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      "cublasGetFixedPointEmulationMantissaBitCountPointer");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitCount, sizeof(int *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDA_VERSION >= 13030
int handle_cublasSetFixedPointEmulationMantissaBitCountPointer(conn_t *conn) {
  cublasHandle_t handle;
  int *mantissaBitCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitCount, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      "cublasSetFixedPointEmulationMantissaBitCountPointer");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasLoggerConfigure(conn_t *conn) {
  int logIsOn;
  int logToStdOut;
  int logToStdErr;
  const char *logFileName;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(int, int, int, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &logIsOn, sizeof(int)) < 0 ||
      rpc_read(conn, &logToStdOut, sizeof(int)) < 0 ||
      rpc_read(conn, &logToStdErr, sizeof(int)) < 0 ||
      rpc_read(conn, &logFileName, sizeof(const char *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasLoggerConfigure");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(logIsOn, logToStdOut, logToStdErr, logFileName);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetLoggerCallback(conn_t *conn) {
  cublasLogCallback userCallback;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasLogCallback);
  fn_t fn = nullptr;
  if (rpc_read(conn, &userCallback, sizeof(cublasLogCallback)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSetLoggerCallback");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(userCallback);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetLoggerCallback(conn_t *conn) {
  cublasLogCallback userCallback;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasLogCallback *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &userCallback, sizeof(cublasLogCallback)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGetLoggerCallback");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(&userCallback);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &userCallback, sizeof(cublasLogCallback)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSnrm2_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSnrm2_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDnrm2_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDnrm2_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const cuComplex *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScnrm2_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScnrm2_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDznrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDznrm2_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDznrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDznrm2_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSdot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const float *, int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSdot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSdot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, const float *,
                                     int64_t, const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSdot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDdot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const double *, int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDdot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDdot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, const double *, int64_t,
      double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDdot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdotu_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const cuComplex *,
                                     int, const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdotu_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdotu_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, const cuComplex *,
      int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdotu_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdotc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const cuComplex *,
                                     int, const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdotc_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdotc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, const cuComplex *,
      int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdotc_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdotu_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdotu_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdotu_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdotu_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdotc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdotc_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdotc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdotc_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, y, incy, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *alpha;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *alpha;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *alpha;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *alpha;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *alpha;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *alpha;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *alpha;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *alpha;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *alpha;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *alpha;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const cuDoubleComplex *,
                                              cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *alpha;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdscal_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *alpha;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdscal_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const float *, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSaxpy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, const float *,
                                     const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSaxpy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const double *, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDaxpy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const double *, const double *,
                                              int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDaxpy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const cuComplex *,
                                     const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCaxpy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, const cuComplex *, int64_t,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCaxpy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, const cuDoubleComplex *,
      int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZaxpy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, const cuDoubleComplex *,
      int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZaxpy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, alpha, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCopyEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const void *,
                                              cudaDataType, int, void *,
                                              cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCopyEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCopyEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const void *, cudaDataType, int64_t, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCopyEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScopy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScopy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDcopy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDcopy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCcopy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, const cuComplex *,
                                     int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCcopy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const cuDoubleComplex *, int,
                                              cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZcopy_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const cuDoubleComplex *, int64_t,
                                              cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZcopy_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float x;
  int incx;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, float *, int,
                                              float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSswap_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float x;
  int64_t incx;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, float *,
                                              int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSswap_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double x;
  int incx;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, double *,
                                              int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDswap_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double x;
  int64_t incx;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, double *,
                                              int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDswap_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex x;
  int incx;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, cuComplex *,
                                              int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCswap_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex x;
  int64_t incx;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCswap_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex x;
  int incx;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZswap_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex x;
  int64_t incx;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, cuDoubleComplex *,
                                     int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZswap_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSwapEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, void *, cudaDataType,
                                     int, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSwapEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSwapEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, void *,
                                              cudaDataType, int64_t, void *,
                                              cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSwapEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIsamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIsamax_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIsamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const float *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIsamax_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIdamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIdamax_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIdamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIdamax_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIcamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIcamax_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIcamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIcamax_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIzamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIzamax_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIzamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIzamax_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIamaxEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const void *,
                                              cudaDataType, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIamaxEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIamaxEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const void *, cudaDataType, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIamaxEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIsamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIsamin_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIsamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const float *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIsamin_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIdamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIdamin_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIdamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIdamin_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIcamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIcamin_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIcamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIcamin_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIzamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIzamin_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIzamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIzamin_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIaminEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  int result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const void *,
                                              cudaDataType, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIaminEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasIaminEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  int64_t result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const void *, cudaDataType, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasIaminEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, xType, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSasum_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t,
                                              const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSasum_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDasum_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDasum_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int,
                                              const cuComplex *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScasum_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasScasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  float result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuComplex *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasScasum_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDzasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDzasum_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDzasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  double result;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDzasum_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, x, incx, &result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &result, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float x;
  int incx;
  float y;
  int incy;
  const float *c;
  const float *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, float *, int, float *,
                                     int, const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float x;
  int64_t incx;
  float y;
  int64_t incy;
  const float *c;
  const float *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, float *,
                                              int64_t, float *, int64_t,
                                              const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double x;
  int incx;
  double y;
  int incy;
  const double *c;
  const double *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, double *,
                                              int, double *, int,
                                              const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double x;
  int64_t incx;
  double y;
  int64_t incy;
  const double *c;
  const double *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, double *,
                                              int64_t, double *, int64_t,
                                              const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex x;
  int incx;
  cuComplex y;
  int incy;
  const float *c;
  const cuComplex *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, cuComplex *,
                                              int, cuComplex *, int,
                                              const float *, const cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex x;
  int64_t incx;
  cuComplex y;
  int64_t incy;
  const float *c;
  const cuComplex *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, cuComplex *, int64_t, cuComplex *, int64_t,
      const float *, const cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex x;
  int incx;
  cuComplex y;
  int incy;
  const float *c;
  const float *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, cuComplex *,
                                              int, cuComplex *, int,
                                              const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex x;
  int64_t incx;
  cuComplex y;
  int64_t incy;
  const float *c;
  const float *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, cuComplex *, int64_t, cuComplex *, int64_t,
      const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const float *)) < 0 ||
      rpc_read(conn, &s, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex x;
  int incx;
  cuDoubleComplex y;
  int incy;
  const double *c;
  const cuDoubleComplex *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, cuDoubleComplex *, int, cuDoubleComplex *, int,
      const double *, const cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex x;
  int64_t incx;
  cuDoubleComplex y;
  int64_t incy;
  const double *c;
  const cuDoubleComplex *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, cuDoubleComplex *, int64_t, cuDoubleComplex *,
      int64_t, const double *, const cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex x;
  int incx;
  cuDoubleComplex y;
  int incy;
  const double *c;
  const double *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, cuDoubleComplex *, int, cuDoubleComplex *, int,
      const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdrot_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex x;
  int64_t incx;
  cuDoubleComplex y;
  int64_t incy;
  const double *c;
  const double *s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, cuDoubleComplex *, int64_t, cuDoubleComplex *,
      int64_t, const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c, sizeof(const double *)) < 0 ||
      rpc_read(conn, &s, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdrot_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, c, s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  float a;
  float b;
  float c;
  float s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, float *, float *,
                                              float *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a, sizeof(float)) < 0 ||
      rpc_read(conn, &b, sizeof(float)) < 0 ||
      rpc_read(conn, &c, sizeof(float)) < 0 ||
      rpc_read(conn, &s, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrotg_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &a, &b, &c, &s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &a, sizeof(float)) < 0 ||
      rpc_write(conn, &b, sizeof(float)) < 0 ||
      rpc_write(conn, &c, sizeof(float)) < 0 ||
      rpc_write(conn, &s, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  double a;
  double b;
  double c;
  double s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, double *,
                                              double *, double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a, sizeof(double)) < 0 ||
      rpc_read(conn, &b, sizeof(double)) < 0 ||
      rpc_read(conn, &c, sizeof(double)) < 0 ||
      rpc_read(conn, &s, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrotg_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &a, &b, &c, &s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &a, sizeof(double)) < 0 ||
      rpc_write(conn, &b, sizeof(double)) < 0 ||
      rpc_write(conn, &c, sizeof(double)) < 0 ||
      rpc_write(conn, &s, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  cuComplex a;
  cuComplex b;
  float c;
  cuComplex s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cuComplex *, cuComplex *, float *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &b, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &c, sizeof(float)) < 0 ||
      rpc_read(conn, &s, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCrotg_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &a, &b, &c, &s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &a, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &b, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &c, sizeof(float)) < 0 ||
      rpc_write(conn, &s, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  cuDoubleComplex a;
  cuDoubleComplex b;
  double c;
  cuDoubleComplex s;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cuDoubleComplex *,
                                              cuDoubleComplex *, double *,
                                              cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &b, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &c, sizeof(double)) < 0 ||
      rpc_read(conn, &s, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZrotg_v2");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED : fn(handle, &a, &b, &c, &s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &a, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &b, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &c, sizeof(double)) < 0 ||
      rpc_write(conn, &s, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrotm_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float x;
  int incx;
  float y;
  int incy;
  const float *param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, float *, int,
                                              float *, int, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &param, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrotm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrotm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float x;
  int64_t incx;
  float y;
  int64_t incy;
  const float *param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, float *, int64_t,
                                     float *, int64_t, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &param, sizeof(const float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrotm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrotm_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double x;
  int incx;
  double y;
  int incy;
  const double *param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, double *, int, double *, int, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &param, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrotm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrotm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double x;
  int64_t incx;
  double y;
  int64_t incy;
  const double *param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int64_t, double *, int64_t,
                                     double *, int64_t, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &param, sizeof(const double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrotm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, n, &x, incx, &y, incy, param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrotmg_v2(conn_t *conn) {
  cublasHandle_t handle;
  float d1;
  float d2;
  float x1;
  const float *y1;
  float param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, float *, float *,
                                              float *, const float *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &d1, sizeof(float)) < 0 ||
      rpc_read(conn, &d2, sizeof(float)) < 0 ||
      rpc_read(conn, &x1, sizeof(float)) < 0 ||
      rpc_read(conn, &y1, sizeof(const float *)) < 0 ||
      rpc_read(conn, &param, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSrotmg_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &d1, &d2, &x1, y1, &param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &d1, sizeof(float)) < 0 ||
      rpc_write(conn, &d2, sizeof(float)) < 0 ||
      rpc_write(conn, &x1, sizeof(float)) < 0 ||
      rpc_write(conn, &param, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrotmg_v2(conn_t *conn) {
  cublasHandle_t handle;
  double d1;
  double d2;
  double x1;
  const double *y1;
  double param;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, double *, double *, double *, const double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &d1, sizeof(double)) < 0 ||
      rpc_read(conn, &d2, sizeof(double)) < 0 ||
      rpc_read(conn, &x1, sizeof(double)) < 0 ||
      rpc_read(conn, &y1, sizeof(const double *)) < 0 ||
      rpc_read(conn, &param, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDrotmg_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, &d1, &d2, &x1, y1, &param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &d1, sizeof(double)) < 0 ||
      rpc_write(conn, &d2, sizeof(double)) < 0 ||
      rpc_write(conn, &x1, sizeof(double)) < 0 ||
      rpc_write(conn, &param, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  const float *x;
  int incx;
  const float *beta;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *, const float *,
      int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  const float *beta;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const float *, int64_t, const float *, int64_t, const float *, float *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  const double *x;
  int incx;
  const double *beta;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  const double *beta;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const double *,
      const double *, int64_t, const double *, int64_t, const double *,
      double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, const cuComplex *,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  const float *alpha;
  const float *A;
  int lda;
  const float *x;
  int incx;
  const float *beta;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, int, const float *,
      const float *, int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  const float *beta;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  const double *alpha;
  const double *A;
  int lda;
  const double *x;
  int incx;
  const double *beta;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  const double *beta;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, const cuComplex *,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, trans, m, n, kl, ku, alpha, A, lda,
                                    x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *A;
  int lda;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *A;
  int64_t lda;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *A;
  int lda;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *A;
  int64_t lda;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const float *A;
  int lda;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStbmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const float *A;
  int64_t lda;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStbmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const double *A;
  int lda;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtbmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const double *A;
  int64_t lda;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtbmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtbmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuComplex *A;
  int64_t lda;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtbmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtbmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtbmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *AP;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStpmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *AP;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStpmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *AP;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtpmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *AP;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtpmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *AP;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtpmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *AP;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtpmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *AP;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtpmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *AP;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtpmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *A;
  int lda;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *A;
  int64_t lda;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *A;
  int lda;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *A;
  int64_t lda;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *AP;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStpsv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *AP;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStpsv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *AP;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtpsv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *AP;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtpsv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *AP;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtpsv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *AP;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtpsv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *AP;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtpsv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *AP;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtpsv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, diag, n, AP, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const float *A;
  int lda;
  float x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStbsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const float *A;
  int64_t lda;
  float x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStbsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const double *A;
  int lda;
  double x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtbsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const double *A;
  int64_t lda;
  double x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtbsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  cuComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtbsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuComplex *A;
  int64_t lda;
  cuComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtbsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtbsv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtbsv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, trans, diag, n, k, A, lda, &x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &x, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  const float *x;
  int incx;
  const float *beta;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const float *, int,
      const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsymv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  const float *beta;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      int64_t, const float *, int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsymv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  const double *x;
  int incx;
  const double *beta;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsymv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  const double *beta;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsymv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, const cuComplex *,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsymv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsymv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsymv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsymv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, const cuComplex *,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhemv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhemv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  const float *alpha;
  const float *A;
  int lda;
  const float *x;
  int incx;
  const float *beta;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, int, const float *, const float *,
      int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  const float *beta;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, int64_t, const float *,
      const float *, int64_t, const float *, int64_t, const float *, float *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  const double *alpha;
  const double *A;
  int lda;
  const double *x;
  int incx;
  const double *beta;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  const double *beta;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, int64_t, const double *,
      const double *, int64_t, const double *, int64_t, const double *,
      double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, const cuComplex *,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhbmv_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhbmv_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, k, alpha, A, lda, x, incx,
                                    beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *AP;
  const float *x;
  int incx;
  const float *beta;
  float y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const float *,
      const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *AP;
  const float *x;
  int64_t incx;
  const float *beta;
  float y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      const float *, int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *AP;
  const double *x;
  int incx;
  const double *beta;
  double y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *AP;
  const double *x;
  int64_t incx;
  const double *beta;
  double y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      const double *, int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *AP;
  const cuComplex *x;
  int incx;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, const cuComplex *, int, const cuComplex *, cuComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *AP;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *AP;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpmv_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *AP;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpmv_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, AP, x, incx, beta, &y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSger_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, int,
                                              const float *, const float *, int,
                                              const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSger_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSger_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const float *, const float *, int64_t,
      const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSger_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDger_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, const double *, const double *, int,
      const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDger_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDger_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const double *, const double *, int64_t,
      const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDger_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgeru_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, const cuComplex *, const cuComplex *, int,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgeru_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgeru_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgeru_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgerc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, const cuComplex *, const cuComplex *, int,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgerc_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgerc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgerc_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgeru_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgeru_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgeru_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgeru_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgerc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgerc_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgerc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgerc_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, m, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  float A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const float *, const float *,
                                              int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  float A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  double A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  double A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const cuComplex *x;
  int incx;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const cuComplex *,
      int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const cuComplex *x;
  int64_t incx;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *,
      const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  float AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const float *, const float *,
                                              int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  float AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int64_t, const float *,
                                              const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  double AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const double *,
                                              const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  double AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const cuComplex *x;
  int incx;
  cuComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const cuComplex *,
      int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const cuComplex *x;
  int64_t incx;
  cuComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *,
      const cuComplex *, int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *,
      const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpr_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpr_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n, alpha, x, incx, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const float *, int,
      const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      int, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, cuComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, cuComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *alpha;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const float *, const float *,
                                              int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSspr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const float *alpha;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      int64_t, const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSspr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *alpha;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDspr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const double *alpha;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDspr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *alpha;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuComplex *,
      const cuComplex *, int, const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChpr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, const cuComplex *, int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChpr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpr2_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhpr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhpr2_v2_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, uplo, n, alpha, x, incx, y, incy, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemvBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const float **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const float **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const float *const[], int, const float *const[], int, const float *,
      float *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const float *const);
  Aarray = (const float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const float *const);
  xarray = (const float **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Aarray),
               lda,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  int64_t batchCount;
  const float **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const float **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const float *const[], int64_t, const float *const[], int64_t,
      const float *, float *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const float *const);
  Aarray = (const float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const float *const);
  xarray = (const float **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Aarray),
               lda,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const double *alpha;
  int batchCount;
  const double **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const double **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const double *beta;
  double **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const double *,
      const double *const[], int, const double *const[], int, const double *,
      double *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const double *const);
  Aarray = (const double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const double *const);
  xarray = (const double **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(double *const);
  yarray = (double **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(double *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const double *alpha;
  int64_t batchCount;
  const double **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const double **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const double *beta;
  double **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const double *,
      const double *const[], int64_t, const double *const[], int64_t,
      const double *, double *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const double *const);
  Aarray = (const double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const double *const);
  xarray = (const double **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(double *const);
  yarray = (double **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(double *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuComplex *alpha;
  int batchCount;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const cuComplex **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const cuComplex *beta;
  cuComplex **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *,
      const cuComplex *const[], int, const cuComplex *const[], int,
      const cuComplex *, cuComplex *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const cuComplex *const);
  xarray = (const cuComplex **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(cuComplex *const);
  yarray = (cuComplex **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : xarray),
               incx, beta,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  int64_t batchCount;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const cuComplex **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const cuComplex *beta;
  cuComplex **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *const[], int64_t, const cuComplex *const[], int64_t,
      const cuComplex *, cuComplex *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const cuComplex *const);
  xarray = (const cuComplex **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(cuComplex *const);
  yarray = (cuComplex **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : xarray),
               incx, beta,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  int batchCount;
  const cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const cuDoubleComplex **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *const[], int, const cuDoubleComplex *const[], int,
      const cuDoubleComplex *, cuDoubleComplex *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Aarray = (const cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  xarray = (const cuDoubleComplex **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(cuDoubleComplex *const);
  yarray = (cuDoubleComplex **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Aarray),
               lda,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : xarray),
               incx, beta,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                 : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  int64_t batchCount;
  const cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const cuDoubleComplex **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const cuDoubleComplex *beta;
  cuDoubleComplex **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *const[], int64_t,
      const cuDoubleComplex *const[], int64_t, const cuDoubleComplex *,
      cuDoubleComplex *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Aarray = (const cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  xarray = (const cuDoubleComplex **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(cuDoubleComplex *const);
  yarray = (cuDoubleComplex **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Aarray),
               lda,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : xarray),
               incx, beta,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                 : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHSHgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  int batchCount;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const __half **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const float *beta;
  __half **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *const[], int, const __half *const[], int, const float *,
      __half *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __half *const);
  xarray = (const __half **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(__half *const);
  yarray = (__half **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSHgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(__half *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHSHgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  int64_t batchCount;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const __half **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const float *beta;
  __half **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *const[], int64_t, const __half *const[], int64_t,
      const float *, __half *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __half *const);
  xarray = (const __half **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(__half *const);
  yarray = (__half **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSHgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(__half *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHSSgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  int batchCount;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const __half **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *const[], int, const __half *const[], int, const float *,
      float *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __half *const);
  xarray = (const __half **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSSgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHSSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  int64_t batchCount;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const __half **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *const[], int64_t, const __half *const[], int64_t,
      const float *, float *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __half *const);
  xarray = (const __half **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSSgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasTSTgemvBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const __nv_bfloat16 **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const __nv_bfloat16 **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const float *beta;
  __nv_bfloat16 **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *const[], int, const __nv_bfloat16 *const[], int,
      const float *, __nv_bfloat16 *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  Aarray = (const __nv_bfloat16 **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  xarray = (const __nv_bfloat16 **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(__nv_bfloat16 *const);
  yarray = (__nv_bfloat16 **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSTgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : Aarray),
               lda,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : xarray),
               incx, beta,
               (batchCount * sizeof(__nv_bfloat16 *const) == 0 ? nullptr
                                                               : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasTSTgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  int64_t batchCount;
  const __nv_bfloat16 **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const __nv_bfloat16 **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const float *beta;
  __nv_bfloat16 **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *const[], int64_t, const __nv_bfloat16 *const[],
      int64_t, const float *, __nv_bfloat16 *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  Aarray = (const __nv_bfloat16 **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  xarray = (const __nv_bfloat16 **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(__nv_bfloat16 *const);
  yarray = (__nv_bfloat16 **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSTgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : Aarray),
               lda,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : xarray),
               incx, beta,
               (batchCount * sizeof(__nv_bfloat16 *const) == 0 ? nullptr
                                                               : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasTSSgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  int batchCount;
  const __nv_bfloat16 **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const __nv_bfloat16 **xarray = nullptr;
  size_t xarray_size;
  int incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *const[], int, const __nv_bfloat16 *const[], int,
      const float *, float *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  Aarray = (const __nv_bfloat16 **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  xarray = (const __nv_bfloat16 **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSSgemvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : Aarray),
               lda,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasTSSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  int64_t batchCount;
  const __nv_bfloat16 **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const __nv_bfloat16 **xarray = nullptr;
  size_t xarray_size;
  int64_t incx;
  const float *beta;
  float **yarray = nullptr;
  size_t yarray_size;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *const[], int64_t, const __nv_bfloat16 *const[],
      int64_t, const float *, float *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  Aarray = (const __nv_bfloat16 **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  xarray_size = batchCount * sizeof(const __nv_bfloat16 *const);
  xarray = (const __nv_bfloat16 **)malloc(xarray_size);
  if (xarray_size != 0 && xarray == nullptr)
    goto ERROR_0;
  if ((xarray_size != 0 && rpc_read(conn, xarray, xarray_size) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  yarray_size = batchCount * sizeof(float *const);
  yarray = (float **)malloc(yarray_size);
  if (yarray_size != 0 && yarray == nullptr)
    goto ERROR_0;
  if ((yarray_size != 0 && rpc_read(conn, yarray, yarray_size) < 0) ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSSgemvBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, alpha,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : Aarray),
               lda,
               (batchCount * sizeof(const __nv_bfloat16 *const) == 0 ? nullptr
                                                                     : xarray),
               incx, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : yarray),
               incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)yarray);
  free((void *)xarray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  long long int strideA;
  const float *x;
  int incx;
  long long int stridex;
  const float *beta;
  float y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *, const float *,
      int, long long int, const float *, int, long long int, const float *,
      float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  long long int strideA;
  const float *x;
  int64_t incx;
  long long int stridex;
  const float *beta;
  float y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const float *, int64_t, long long int, const float *, int64_t,
      long long int, const float *, float *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  long long int strideA;
  const double *x;
  int incx;
  long long int stridex;
  const double *beta;
  double y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const double *,
      const double *, int, long long int, const double *, int, long long int,
      const double *, double *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  long long int strideA;
  const double *x;
  int64_t incx;
  long long int stridex;
  const double *beta;
  double y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const double *,
      const double *, int64_t, long long int, const double *, int64_t,
      long long int, const double *, double *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &y, sizeof(double)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *x;
  int incx;
  long long int stridex;
  const cuComplex *beta;
  cuComplex y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *,
      const cuComplex *, int, long long int, const cuComplex *, int,
      long long int, const cuComplex *, cuComplex *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *x;
  int64_t incx;
  long long int stridex;
  const cuComplex *beta;
  cuComplex y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *, int64_t, long long int, const cuComplex *, int64_t,
      long long int, const cuComplex *, cuComplex *, int64_t, long long int,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  long long int strideA;
  const cuDoubleComplex *x;
  int incx;
  long long int stridex;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, long long int, const cuDoubleComplex *, int,
      long long int, const cuDoubleComplex *, cuDoubleComplex *, int,
      long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  long long int strideA;
  const cuDoubleComplex *x;
  int64_t incx;
  long long int stridex;
  const cuDoubleComplex *beta;
  cuDoubleComplex y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t, long long int,
      const cuDoubleComplex *, int64_t, long long int, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHSHgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *x;
  int incx;
  long long int stridex;
  const float *beta;
  __half y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *, int, long long int, const __half *, int, long long int,
      const float *, __half *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(__half)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSHgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHSHgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *x;
  int64_t incx;
  long long int stridex;
  const float *beta;
  __half y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *, int64_t, long long int, const __half *, int64_t,
      long long int, const float *, __half *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(__half)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSHgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHSSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *x;
  int incx;
  long long int stridex;
  const float *beta;
  float y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *, int, long long int, const __half *, int, long long int,
      const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSSgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHSSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *x;
  int64_t incx;
  long long int stridex;
  const float *beta;
  float y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *, int64_t, long long int, const __half *, int64_t,
      long long int, const float *, float *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHSSgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasTSTgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const __nv_bfloat16 *A;
  int lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int incx;
  long long int stridex;
  const float *beta;
  __nv_bfloat16 y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *, int, long long int, const __nv_bfloat16 *, int,
      long long int, const float *, __nv_bfloat16 *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(__nv_bfloat16)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSTgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(__nv_bfloat16)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasTSTgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const __nv_bfloat16 *A;
  int64_t lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int64_t incx;
  long long int stridex;
  const float *beta;
  __nv_bfloat16 y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *, int64_t, long long int, const __nv_bfloat16 *,
      int64_t, long long int, const float *, __nv_bfloat16 *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(__nv_bfloat16)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSTgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(__nv_bfloat16)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasTSSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  const float *alpha;
  const __nv_bfloat16 *A;
  int lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int incx;
  long long int stridex;
  const float *beta;
  float y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *, int, long long int, const __nv_bfloat16 *, int,
      long long int, const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSSgemvStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasTSSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  const float *alpha;
  const __nv_bfloat16 *A;
  int64_t lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int64_t incx;
  long long int stridex;
  const float *beta;
  float y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *, int64_t, long long int, const __nv_bfloat16 *,
      int64_t, long long int, const float *, float *, int64_t, long long int,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &y, sizeof(float)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasTSSgemvStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, trans, m, n, alpha, A, lda, strideA, x, incx,
                          stridex, beta, &y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &y, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  float *alpha_null_check;
  float alpha;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  float *beta_null_check;
  float beta;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const float *, int, const float *, int, const float *,
      float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const float *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const float)) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemm_v2");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, B, ldb,
                          beta_null_check ? &beta : nullptr, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  const float *beta;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const double *alpha;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *beta;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const double *, const double *, int, const double *, int, const double *,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  const double *beta;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3m(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3m");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3m_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3m_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const cuComplex *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mEx");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, const cuComplex *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mEx_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemm3m(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemm3m");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemm3m_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemm3m_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHgemm(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const __half *alpha;
  const __half *A;
  int lda;
  const __half *B;
  int ldb;
  const __half *beta;
  __half C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const __half *, const __half *, int, const __half *, int, const __half *,
      __half *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &C, sizeof(__half)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemm");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHgemm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const __half *alpha;
  const __half *A;
  int64_t lda;
  const __half *B;
  int64_t ldb;
  const __half *beta;
  __half C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *, int64_t, const __half *, int64_t,
      const __half *, __half *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &C, sizeof(__half)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemm_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, transa, transb, m, n, k, alpha, A,
                                    lda, B, ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemmEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const float *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmEx");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const void *, cudaDataType, int64_t, const void *,
      cudaDataType, int64_t, const float *, void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmEx_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemmEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const cuComplex *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmEx");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, const cuComplex *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmEx_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k, alpha, A, Atype, lda,
                          B, Btype, ldb, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const float *A;
  int lda;
  const float *beta;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyrk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *beta;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyrk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const double *alpha;
  const double *A;
  int lda;
  const double *beta;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const double *, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyrk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *beta;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, double *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyrk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, cuComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyrk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyrk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrkEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const void *, cudaDataType, int, const cuComplex *,
      void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrkEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrkEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const void *, cudaDataType, int64_t, const cuComplex *,
      void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrkEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrk3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const void *, cudaDataType, int, const cuComplex *,
      void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrk3mEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrk3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const cuComplex *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const void *, cudaDataType, int64_t, const cuComplex *,
      void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrk3mEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const cuComplex *A;
  int lda;
  const float *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const cuComplex *, int, const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const cuComplex *A;
  int64_t lda;
  const float *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const cuComplex *, int64_t, const float *, cuComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZherk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const double *alpha;
  const cuDoubleComplex *A;
  int lda;
  const double *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const double *, const cuDoubleComplex *, int, const double *,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZherk_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZherk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const double *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const double *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const cuDoubleComplex *, int64_t, const double *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZherk_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda,
                                    beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherkEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const void *, cudaDataType, int, const float *, void *,
      cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherkEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherkEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const void *, cudaDataType, int64_t, const float *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherkEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherk3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int lda;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const void *, cudaDataType, int, const float *, void *,
      cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherk3mEx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherk3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const float *beta;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const void *, cudaDataType, int64_t, const float *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherk3mEx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, Atype,
                                    lda, beta, C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  const float *beta;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const float *, int, const float *, int, const float *,
      float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  const float *beta;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyr2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const double *alpha;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *beta;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const double *, const double *, int, const double *, int, const double *,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  const double *beta;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyr2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyr2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyr2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const float *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCher2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const float *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCher2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const double *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher2k_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZher2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const double *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const double *, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZher2k_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const float *alpha;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  const float *beta;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const float *, int, const float *, int, const float *,
      float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyrkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  const float *beta;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsyrkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const double *alpha;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *beta;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const double *, const double *, int, const double *, int, const double *,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyrkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  const double *beta;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsyrkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsyrkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyrkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsyrkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const float *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCherkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const float *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCherkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZherkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const double *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZherkx");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZherkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const double *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const double *, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZherkx_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, trans, n, k, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  const float *beta;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const float *, const float *, int, const float *, int, const float *,
      float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsymm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  const float *beta;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSsymm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *beta;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const double *, const double *, int, const double *, int, const double *,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsymm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  const double *beta;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDsymm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsymm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCsymm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsymm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZsymm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const cuComplex *beta;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChemm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  const cuComplex *beta;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasChemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhemm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  const cuDoubleComplex *beta;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZhemm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, m, n, alpha, A, lda, B,
                                    ldb, beta, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  float B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const float *, const float *, int, float *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  float B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *, int64_t,
      float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(float)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  double B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const double *, const double *, int, double *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  double B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *,
      int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(double)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  cuComplex B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuComplex *, const cuComplex *, int,
      cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  cuComplex B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, &B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &B, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const float *alpha;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const float *, const float *, int,
      const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrmm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const float *alpha;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *, int64_t,
      const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrmm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const double *alpha;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const double *, const double *, int,
      const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrmm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const double *alpha;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrmm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const cuComplex *alpha;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuComplex *, const cuComplex *, int,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrmm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const cuComplex *alpha;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrmm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrmm_v2");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrmm_v2_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, side, uplo, trans, diag, m, n,
                                    alpha, A, lda, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHgemmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const __half *alpha;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const __half **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  const __half *beta;
  __half **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const __half *, const __half *const[], int, const __half *const[], int,
      const __half *, __half *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const __half *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const __half *const);
  Barray = (const __half **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const __half *)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(__half *const);
  Carray = (__half **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Barray),
               ldb, beta,
               (batchCount * sizeof(__half *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHgemmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const __half *alpha;
  const __half **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const __half **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  const __half *beta;
  __half **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *const[], int64_t,
      const __half *const[], int64_t, const __half *, __half *const[], int64_t,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const __half *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const __half *const);
  Aarray = (const __half **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const __half *const);
  Barray = (const __half **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const __half *)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(__half *const);
  Carray = (__half **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k, alpha,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const __half *const) == 0 ? nullptr
                                                              : Barray),
               ldb, beta,
               (batchCount * sizeof(__half *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgemmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  const float *alpha;
  const float **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const float **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  const float *beta;
  float **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const float *const[], int, const float *const[], int,
      const float *, float *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const float *const);
  Aarray = (const float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const float *const);
  Barray = (const float **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(float *const);
  Carray = (float **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k, alpha,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Aarray),
               lda,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Barray),
               ldb, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : Carray), ldc,
               batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgemmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  const float *alpha;
  const float **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const float **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  const float *beta;
  float **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const float *const[], int64_t,
      const float *const[], int64_t, const float *, float *const[], int64_t,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const float *const);
  Aarray = (const float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const float *const);
  Barray = (const float **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta, sizeof(const float *)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(float *const);
  Carray = (float **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k, alpha,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Aarray),
               lda,
               (batchCount * sizeof(const float *const) == 0 ? nullptr
                                                             : Barray),
               ldb, beta,
               (batchCount * sizeof(float *const) == 0 ? nullptr : Carray), ldc,
               batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgemmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  double *alpha_null_check;
  double alpha;
  const double **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const double **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  double *beta_null_check;
  double beta;
  double **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const double *, const double *const[], int, const double *const[], int,
      const double *, double *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const double *const);
  Aarray = (const double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const double *const);
  Barray = (const double **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(double *const);
  Carray = (double **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(double *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgemmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  double *alpha_null_check;
  double alpha;
  const double **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const double **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  double *beta_null_check;
  double beta;
  double **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *const[], int64_t,
      const double *const[], int64_t, const double *, double *const[], int64_t,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const double *const);
  Aarray = (const double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const double *const);
  Barray = (const double **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(double *const);
  Carray = (double **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Aarray),
               lda,
               (batchCount * sizeof(const double *const) == 0 ? nullptr
                                                              : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(double *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const cuComplex **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *const[], int,
      const cuComplex *const[], int, const cuComplex *, cuComplex *const[], int,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuComplex *const);
  Barray = (const cuComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuComplex *const);
  Carray = (cuComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const cuComplex **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *const[], int64_t,
      const cuComplex *const[], int64_t, const cuComplex *, cuComplex *const[],
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuComplex *const);
  Barray = (const cuComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuComplex *const);
  Carray = (cuComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemm3mBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const cuComplex **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *const[], int,
      const cuComplex *const[], int, const cuComplex *, cuComplex *const[], int,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuComplex *const);
  Barray = (const cuComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuComplex *const);
  Carray = (cuComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgemm3mBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const cuComplex **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *const[], int64_t,
      const cuComplex *const[], int64_t, const cuComplex *, cuComplex *const[],
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuComplex *const);
  Barray = (const cuComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuComplex *const);
  Carray = (cuComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Aarray),
               lda,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                 : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgemmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const cuDoubleComplex **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  cuDoubleComplex **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *const[], int,
      const cuDoubleComplex *const[], int, const cuDoubleComplex *,
      cuDoubleComplex *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Aarray = (const cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Barray = (const cuDoubleComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuDoubleComplex *const);
  Carray = (cuDoubleComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Aarray),
               lda,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                 : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgemmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int64_t lda;
  const cuDoubleComplex **Barray = nullptr;
  size_t Barray_size;
  int64_t ldb;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  cuDoubleComplex **Carray = nullptr;
  size_t Carray_size;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *const[], int64_t,
      const cuDoubleComplex *const[], int64_t, const cuDoubleComplex *,
      cuDoubleComplex *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Aarray = (const cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const cuDoubleComplex *const);
  Barray = (const cuDoubleComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(cuDoubleComplex *const);
  Carray = (cuDoubleComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Aarray),
               lda,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0
                    ? nullptr
                    : Barray),
               ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                 : Carray),
               ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasHgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  __half *alpha_null_check;
  __half alpha;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *B;
  int ldb;
  long long int strideB;
  __half *beta_null_check;
  __half beta;
  __half C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const __half *, const __half *, int, long long int, const __half *, int,
      long long int, const __half *, __half *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const __half *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const __half)) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const __half *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const __half)) < 0) ||
      rpc_read(conn, &C, sizeof(__half)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemmStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  __half *alpha_null_check;
  __half alpha;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *B;
  int64_t ldb;
  long long int strideB;
  __half *beta_null_check;
  __half beta;
  __half C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *, int64_t, long long int,
      const __half *, int64_t, long long int, const __half *, __half *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const __half *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const __half)) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const __half *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const __half)) < 0) ||
      rpc_read(conn, &C, sizeof(__half)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasHgemmStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(__half)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  float *alpha_null_check;
  float alpha;
  const float *A;
  int lda;
  long long int strideA;
  const float *B;
  int ldb;
  long long int strideB;
  float *beta_null_check;
  float beta;
  float C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const float *, int, long long int, const float *, int,
      long long int, const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const float *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const float)) < 0) ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  float *alpha_null_check;
  float alpha;
  const float *A;
  int64_t lda;
  long long int strideA;
  const float *B;
  int64_t ldb;
  long long int strideB;
  float *beta_null_check;
  float beta;
  float C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const float *, int64_t, long long int,
      const float *, int64_t, long long int, const float *, float *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const float *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const float)) < 0) ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgemmStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  double *alpha_null_check;
  double alpha;
  const double *A;
  int lda;
  long long int strideA;
  const double *B;
  int ldb;
  long long int strideB;
  double *beta_null_check;
  double beta;
  double C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const double *, const double *, int, long long int, const double *, int,
      long long int, const double *, double *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemmStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  double *alpha_null_check;
  double alpha;
  const double *A;
  int64_t lda;
  long long int strideA;
  const double *B;
  int64_t ldb;
  long long int strideB;
  double *beta_null_check;
  double beta;
  double C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *, int64_t, long long int,
      const double *, int64_t, long long int, const double *, double *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgemmStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *B;
  int ldb;
  long long int strideB;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, long long int,
      const cuComplex *, int, long long int, const cuComplex *, cuComplex *,
      int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *B;
  int64_t ldb;
  long long int strideB;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, long long int,
      const cuComplex *, int64_t, long long int, const cuComplex *, cuComplex *,
      int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemmStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3mStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *B;
  int ldb;
  long long int strideB;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, long long int,
      const cuComplex *, int, long long int, const cuComplex *, cuComplex *,
      int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3mStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *B;
  int64_t ldb;
  long long int strideB;
  cuComplex *beta_null_check;
  cuComplex beta;
  cuComplex C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, long long int,
      const cuComplex *, int64_t, long long int, const cuComplex *, cuComplex *,
      int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgemm3mStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex *A;
  int lda;
  long long int strideA;
  const cuDoubleComplex *B;
  int ldb;
  long long int strideB;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  cuDoubleComplex C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int, long long int,
      const cuDoubleComplex *, int, long long int, const cuDoubleComplex *,
      cuDoubleComplex *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemmStridedBatched");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  long long int strideA;
  const cuDoubleComplex *B;
  int64_t ldb;
  long long int strideB;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  cuDoubleComplex C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      long long int, const cuDoubleComplex *, int64_t, long long int,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t, long long int,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgemmStridedBatched_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n, k,
                          alpha_null_check ? &alpha : nullptr, A, lda, strideA,
                          B, ldb, strideB, beta_null_check ? &beta : nullptr,
                          &C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGemmBatchedEx_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  void *alpha_null_check;
  void *alpha;
  const void **Aarray = nullptr;
  size_t Aarray_size;
  cudaDataType Atype;
  int64_t lda;
  const void **Barray = nullptr;
  size_t Barray_size;
  cudaDataType Btype;
  int64_t ldb;
  void *beta_null_check;
  void *beta;
  void **Carray = nullptr;
  size_t Carray_size;
  cudaDataType Ctype;
  int64_t ldc;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const void *, const void *const[], cudaDataType, int64_t,
      const void *const[], cudaDataType, int64_t, const void *, void *const[],
      cudaDataType, int64_t, int64_t, cublasComputeType_t, cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const void *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const void *)) < 0) ||
      false)
    goto ERROR_0;
  Aarray_size = batchCount * sizeof(const void *const);
  Aarray = (const void **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchCount * sizeof(const void *const);
  Barray = (const void **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const void *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const void *)) < 0) ||
      false)
    goto ERROR_0;
  Carray_size = batchCount * sizeof(void *const);
  Carray = (void **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasGemmBatchedEx_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, transa, transb, m, n, k,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const void *const) == 0 ? nullptr : Aarray),
               Atype, lda,
               (batchCount * sizeof(const void *const) == 0 ? nullptr : Barray),
               Btype, ldb, beta_null_check ? &beta : nullptr,
               (batchCount * sizeof(void *const) == 0 ? nullptr : Carray),
               Ctype, ldc, batchCount, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  float *alpha_null_check;
  float alpha;
  const float *A;
  int lda;
  float *beta_null_check;
  float beta;
  const float *B;
  int ldb;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int,
      const float *, const float *, int, const float *, const float *, int,
      float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const float *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const float)) < 0) ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgeam");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  float *alpha_null_check;
  float alpha;
  const float *A;
  int64_t lda;
  float *beta_null_check;
  float beta;
  const float *B;
  int64_t ldb;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, const float *,
      int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const float *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const float)) < 0) ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgeam_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  double *alpha_null_check;
  double alpha;
  const double *A;
  int lda;
  double *beta_null_check;
  double beta;
  const double *B;
  int ldb;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int,
      const double *, const double *, int, const double *, const double *, int,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgeam");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  double *alpha_null_check;
  double alpha;
  const double *A;
  int64_t lda;
  double *beta_null_check;
  double beta;
  const double *B;
  int64_t ldb;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, const double *,
      int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const double *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const double)) < 0) ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgeam_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int lda;
  cuComplex *beta_null_check;
  cuComplex beta;
  const cuComplex *B;
  int ldb;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgeam");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex *A;
  int64_t lda;
  cuComplex *beta_null_check;
  cuComplex beta;
  const cuComplex *B;
  int64_t ldb;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuComplex *)) < 0 ||
      (beta_null_check && rpc_read(conn, &beta, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgeam_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  const cuDoubleComplex *B;
  int ldb;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int, cuDoubleComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgeam");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *beta_null_check;
  cuDoubleComplex beta;
  const cuDoubleComplex *B;
  int64_t ldb;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (beta_null_check &&
       rpc_read(conn, &beta, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgeam_64");
  return_value = fn == nullptr
                     ? CUBLAS_STATUS_NOT_SUPPORTED
                     : fn(handle, transa, transb, m, n,
                          alpha_null_check ? &alpha : nullptr, A, lda,
                          beta_null_check ? &beta : nullptr, B, ldb, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrsmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  float *alpha_null_check;
  float alpha;
  const float **A = nullptr;
  size_t A_size;
  int lda;
  float **B = nullptr;
  size_t B_size;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const float *, const float *const[], int,
      float *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const float *const);
  A = (const float **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(float *const);
  B = (float **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const float *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(float *const) == 0 ? nullptr : B), ldb,
               batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasStrsmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  float *alpha_null_check;
  float alpha;
  const float **A = nullptr;
  size_t A_size;
  int64_t lda;
  float **B = nullptr;
  size_t B_size;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *const[],
      int64_t, float *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const float *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const float)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const float *const);
  A = (const float **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(float *const);
  B = (float **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrsmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const float *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(float *const) == 0 ? nullptr : B), ldb,
               batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasDtrsmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  double *alpha_null_check;
  double alpha;
  const double **A = nullptr;
  size_t A_size;
  int lda;
  double **B = nullptr;
  size_t B_size;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const double *, const double *const[], int,
      double *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const double *const);
  A = (const double **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(double *const);
  B = (double **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const double *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(double *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasDtrsmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  double *alpha_null_check;
  double alpha;
  const double **A = nullptr;
  size_t A_size;
  int64_t lda;
  double **B = nullptr;
  size_t B_size;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *const[],
      int64_t, double *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const double *)) < 0 ||
      (alpha_null_check && rpc_read(conn, &alpha, sizeof(const double)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const double *const);
  A = (const double **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(double *const);
  B = (double **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrsmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const double *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(double *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasCtrsmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **A = nullptr;
  size_t A_size;
  int lda;
  cuComplex **B = nullptr;
  size_t B_size;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuComplex *, const cuComplex *const[],
      int, cuComplex *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const cuComplex *const);
  A = (const cuComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(cuComplex *const);
  B = (cuComplex **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasCtrsmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  cuComplex *alpha_null_check;
  cuComplex alpha;
  const cuComplex **A = nullptr;
  size_t A_size;
  int64_t lda;
  cuComplex **B = nullptr;
  size_t B_size;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *const[], int64_t, cuComplex *const[], int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuComplex)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const cuComplex *const);
  A = (const cuComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(cuComplex *const);
  B = (cuComplex **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrsmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuComplex *const) == 0 ? nullptr : A),
               lda, (batchCount * sizeof(cuComplex *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasZtrsmBatched(conn_t *conn) {
  int batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex **A = nullptr;
  size_t A_size;
  int lda;
  cuDoubleComplex **B = nullptr;
  size_t B_size;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *const[], int, cuDoubleComplex *const[], int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const cuDoubleComplex *const);
  A = (const cuDoubleComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(cuDoubleComplex *const);
  B = (cuDoubleComplex **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsmBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0 ? nullptr
                                                                       : A),
               lda,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasZtrsmBatched_64(conn_t *conn) {
  int64_t batchCount;
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  cuDoubleComplex *alpha_null_check;
  cuDoubleComplex alpha;
  const cuDoubleComplex **A = nullptr;
  size_t A_size;
  int64_t lda;
  cuDoubleComplex **B = nullptr;
  size_t B_size;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *const[], int64_t, cuDoubleComplex *const[],
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (alpha_null_check &&
       rpc_read(conn, &alpha, sizeof(const cuDoubleComplex)) < 0) ||
      false)
    goto ERROR_0;
  A_size = batchCount * sizeof(const cuDoubleComplex *const);
  A = (const cuDoubleComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  B_size = batchCount * sizeof(cuDoubleComplex *const);
  B = (cuDoubleComplex **)malloc(B_size);
  if (B_size != 0 && B == nullptr)
    goto ERROR_0;
  if ((B_size != 0 && rpc_read(conn, B, B_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrsmBatched_64");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_null_check ? &alpha : nullptr,
               (batchCount * sizeof(const cuDoubleComplex *const) == 0 ? nullptr
                                                                       : A),
               lda,
               (batchCount * sizeof(cuDoubleComplex *const) == 0 ? nullptr : B),
               ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)B);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)B);
  free((void *)A);
  return -1;
}

int handle_cublasSdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const float *A;
  int lda;
  float *x_null_check;
  float x;
  int incx;
  float C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasSideMode_t,
                                              int, int, const float *, int,
                                              const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const float *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const float)) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSdgmm");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const float *A;
  int64_t lda;
  float *x_null_check;
  float x;
  int64_t incx;
  float C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int64_t, int64_t, const float *,
      int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const float *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const float)) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSdgmm_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const double *A;
  int lda;
  double *x_null_check;
  double x;
  int incx;
  double C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int, int, const double *, int,
      const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const double *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const double)) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDdgmm");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const double *A;
  int64_t lda;
  double *x_null_check;
  double x;
  int64_t incx;
  double C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int64_t, int64_t, const double *,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const double *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const double)) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDdgmm_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex *x_null_check;
  cuComplex x;
  int incx;
  cuComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int, int, const cuComplex *, int,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const cuComplex *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdgmm");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  cuComplex *x_null_check;
  cuComplex x;
  int64_t incx;
  cuComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int64_t, int64_t, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const cuComplex *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCdgmm_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *x_null_check;
  cuDoubleComplex x;
  int incx;
  cuDoubleComplex C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdgmm");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *x_null_check;
  cuDoubleComplex x;
  int64_t incx;
  cuDoubleComplex C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasSideMode_t, int64_t, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (x_null_check && rpc_read(conn, &x, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZdgmm_64");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, mode, m, n, A, lda,
                                    x_null_check ? &x : nullptr, incx, &C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSmatinvBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const float **A = nullptr;
  size_t A_size;
  int lda;
  float **Ainv = nullptr;
  size_t Ainv_size;
  int lda_inv;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const float *const[],
                                     int, float *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const float *const);
  A = (const float **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Ainv_size = batchSize * sizeof(float *const);
  Ainv = (float **)malloc(Ainv_size);
  if (Ainv_size != 0 && Ainv == nullptr)
    goto ERROR_0;
  if ((Ainv_size != 0 && rpc_read(conn, Ainv, Ainv_size) < 0) ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSmatinvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const float *const) == 0 ? nullptr : A), lda,
               (batchSize * sizeof(float *const) == 0 ? nullptr : Ainv),
               lda_inv, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Ainv);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)Ainv);
  free((void *)A);
  return -1;
}

int handle_cublasDmatinvBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const double **A = nullptr;
  size_t A_size;
  int lda;
  double **Ainv = nullptr;
  size_t Ainv_size;
  int lda_inv;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, const double *const[],
                                     int, double *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const double *const);
  A = (const double **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Ainv_size = batchSize * sizeof(double *const);
  Ainv = (double **)malloc(Ainv_size);
  if (Ainv_size != 0 && Ainv == nullptr)
    goto ERROR_0;
  if ((Ainv_size != 0 && rpc_read(conn, Ainv, Ainv_size) < 0) ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDmatinvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const double *const) == 0 ? nullptr : A),
               lda, (batchSize * sizeof(double *const) == 0 ? nullptr : Ainv),
               lda_inv, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Ainv);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)Ainv);
  free((void *)A);
  return -1;
}

int handle_cublasCmatinvBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const cuComplex **A = nullptr;
  size_t A_size;
  int lda;
  cuComplex **Ainv = nullptr;
  size_t Ainv_size;
  int lda_inv;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuComplex *const[], int, cuComplex *const[],
      int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const cuComplex *const);
  A = (const cuComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Ainv_size = batchSize * sizeof(cuComplex *const);
  Ainv = (cuComplex **)malloc(Ainv_size);
  if (Ainv_size != 0 && Ainv == nullptr)
    goto ERROR_0;
  if ((Ainv_size != 0 && rpc_read(conn, Ainv, Ainv_size) < 0) ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCmatinvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const cuComplex *const) == 0 ? nullptr : A),
               lda,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : Ainv),
               lda_inv, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Ainv);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)Ainv);
  free((void *)A);
  return -1;
}

int handle_cublasZmatinvBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex **A = nullptr;
  size_t A_size;
  int lda;
  cuDoubleComplex **Ainv = nullptr;
  size_t Ainv_size;
  int lda_inv;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *const[], int,
      cuDoubleComplex *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const cuDoubleComplex *const);
  A = (const cuDoubleComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Ainv_size = batchSize * sizeof(cuDoubleComplex *const);
  Ainv = (cuDoubleComplex **)malloc(Ainv_size);
  if (Ainv_size != 0 && Ainv == nullptr)
    goto ERROR_0;
  if ((Ainv_size != 0 && rpc_read(conn, Ainv, Ainv_size) < 0) ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZmatinvBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const cuDoubleComplex *const) == 0 ? nullptr
                                                                      : A),
               lda,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : Ainv),
               lda_inv, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Ainv);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)Ainv);
  free((void *)A);
  return -1;
}

int handle_cublasSgeqrfBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int m;
  int n;
  float **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  float **TauArray = nullptr;
  size_t TauArray_size;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, int, float *const[],
                                     int, float *const[], int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(float *const);
  Aarray = (float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  TauArray_size = batchSize * sizeof(float *const);
  TauArray = (float **)malloc(TauArray_size);
  if (TauArray_size != 0 && TauArray == nullptr)
    goto ERROR_0;
  if ((TauArray_size != 0 && rpc_read(conn, TauArray, TauArray_size) < 0) ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgeqrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, m, n,
               (batchSize * sizeof(float *const) == 0 ? nullptr : Aarray), lda,
               (batchSize * sizeof(float *const) == 0 ? nullptr : TauArray),
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)TauArray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)TauArray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgeqrfBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int m;
  int n;
  double **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  double **TauArray = nullptr;
  size_t TauArray_size;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, int, double *const[],
                                     int, double *const[], int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(double *const);
  Aarray = (double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  TauArray_size = batchSize * sizeof(double *const);
  TauArray = (double **)malloc(TauArray_size);
  if (TauArray_size != 0 && TauArray == nullptr)
    goto ERROR_0;
  if ((TauArray_size != 0 && rpc_read(conn, TauArray, TauArray_size) < 0) ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgeqrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, m, n,
               (batchSize * sizeof(double *const) == 0 ? nullptr : Aarray), lda,
               (batchSize * sizeof(double *const) == 0 ? nullptr : TauArray),
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)TauArray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)TauArray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgeqrfBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int m;
  int n;
  cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  cuComplex **TauArray = nullptr;
  size_t TauArray_size;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, int, int,
                                              cuComplex *const[], int,
                                              cuComplex *const[], int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(cuComplex *const);
  Aarray = (cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  TauArray_size = batchSize * sizeof(cuComplex *const);
  TauArray = (cuComplex **)malloc(TauArray_size);
  if (TauArray_size != 0 && TauArray == nullptr)
    goto ERROR_0;
  if ((TauArray_size != 0 && rpc_read(conn, TauArray, TauArray_size) < 0) ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgeqrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, m, n,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : Aarray),
               lda,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : TauArray),
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)TauArray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)TauArray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgeqrfBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int m;
  int n;
  cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  cuDoubleComplex **TauArray = nullptr;
  size_t TauArray_size;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, int, cuDoubleComplex *const[], int,
      cuDoubleComplex *const[], int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(cuDoubleComplex *const);
  Aarray = (cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  TauArray_size = batchSize * sizeof(cuDoubleComplex *const);
  TauArray = (cuDoubleComplex **)malloc(TauArray_size);
  if (TauArray_size != 0 && TauArray == nullptr)
    goto ERROR_0;
  if ((TauArray_size != 0 && rpc_read(conn, TauArray, TauArray_size) < 0) ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgeqrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, m, n,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : Aarray),
               lda,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : TauArray),
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)TauArray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)TauArray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasSgelsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  float **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  float **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int info;
  int devInfoArray;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, float *const[], int,
      float *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(float *const);
  Aarray = (float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchSize * sizeof(float *const);
  Carray = (float **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgelsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, nrhs,
               (batchSize * sizeof(float *const) == 0 ? nullptr : Aarray), lda,
               (batchSize * sizeof(float *const) == 0 ? nullptr : Carray), ldc,
               &info, &devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &devInfoArray, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgelsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  double **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  double **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int info;
  int devInfoArray;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, double *const[], int,
      double *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(double *const);
  Aarray = (double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchSize * sizeof(double *const);
  Carray = (double **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgelsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, nrhs,
               (batchSize * sizeof(double *const) == 0 ? nullptr : Aarray), lda,
               (batchSize * sizeof(double *const) == 0 ? nullptr : Carray), ldc,
               &info, &devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &devInfoArray, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgelsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  cuComplex **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int info;
  int devInfoArray;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int, cuComplex *const[], int,
      cuComplex *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(cuComplex *const);
  Aarray = (cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchSize * sizeof(cuComplex *const);
  Carray = (cuComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgelsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, nrhs,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : Aarray),
               lda,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : Carray),
               ldc, &info, &devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &devInfoArray, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgelsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  cuDoubleComplex **Carray = nullptr;
  size_t Carray_size;
  int ldc;
  int info;
  int devInfoArray;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, int,
      cuDoubleComplex *const[], int, cuDoubleComplex *const[], int, int *,
      int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(cuDoubleComplex *const);
  Aarray = (cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Carray_size = batchSize * sizeof(cuDoubleComplex *const);
  Carray = (cuDoubleComplex **)malloc(Carray_size);
  if (Carray_size != 0 && Carray == nullptr)
    goto ERROR_0;
  if ((Carray_size != 0 && rpc_read(conn, Carray, Carray_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgelsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, m, n, nrhs,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : Aarray),
               lda,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : Carray),
               ldc, &info, &devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &devInfoArray, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Carray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Carray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasStpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *AP_null_check;
  float AP;
  float A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP_null_check, sizeof(const float *)) < 0 ||
      (AP_null_check && rpc_read(conn, &AP, sizeof(const float)) < 0) ||
      rpc_read(conn, &A, sizeof(float)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStpttr");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    AP_null_check ? &AP : nullptr, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *AP_null_check;
  double AP;
  double A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP_null_check, sizeof(const double *)) < 0 ||
      (AP_null_check && rpc_read(conn, &AP, sizeof(const double)) < 0) ||
      rpc_read(conn, &A, sizeof(double)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtpttr");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    AP_null_check ? &AP : nullptr, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *AP_null_check;
  cuComplex AP;
  cuComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t, int,
                                     const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP_null_check, sizeof(const cuComplex *)) < 0 ||
      (AP_null_check && rpc_read(conn, &AP, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtpttr");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    AP_null_check ? &AP : nullptr, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *AP_null_check;
  cuDoubleComplex AP;
  cuDoubleComplex A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const cuDoubleComplex *,
                                              cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (AP_null_check &&
       rpc_read(conn, &AP, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtpttr");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    AP_null_check ? &AP : nullptr, &A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &A, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A_null_check;
  float A;
  int lda;
  float AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A_null_check, sizeof(const float *)) < 0 ||
      (A_null_check && rpc_read(conn, &A, sizeof(const float)) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasStrttp");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    A_null_check ? &A : nullptr, lda, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A_null_check;
  double A;
  int lda;
  double AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasFillMode_t, int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A_null_check, sizeof(const double *)) < 0 ||
      (A_null_check && rpc_read(conn, &A, sizeof(const double)) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDtrttp");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    A_null_check ? &A : nullptr, lda, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A_null_check;
  cuComplex A;
  int lda;
  cuComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t, int,
                                     const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A_null_check, sizeof(const cuComplex *)) < 0 ||
      (A_null_check && rpc_read(conn, &A, sizeof(const cuComplex)) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCtrttp");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    A_null_check ? &A : nullptr, lda, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A_null_check;
  cuDoubleComplex A;
  int lda;
  cuDoubleComplex AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(cublasHandle_t, cublasFillMode_t,
                                              int, const cuDoubleComplex *, int,
                                              cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A_null_check, sizeof(const cuDoubleComplex *)) < 0 ||
      (A_null_check && rpc_read(conn, &A, sizeof(const cuDoubleComplex)) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZtrttp");
  return_value = fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                               : fn(handle, uplo, n,
                                    A_null_check ? &A : nullptr, lda, &AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &AP, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  int batchSize;
  float **A = nullptr;
  size_t A_size;
  int lda;
  int P;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, float *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(float *const);
  A = (float **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgetrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n, (batchSize * sizeof(float *const) == 0 ? nullptr : A),
               lda, &P, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &P, sizeof(int)) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)A);
  return -1;
}

int handle_cublasDgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  int batchSize;
  double **A = nullptr;
  size_t A_size;
  int lda;
  int P;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, double *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(double *const);
  A = (double **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgetrfBatched");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                    : fn(handle, n,
                         (batchSize * sizeof(double *const) == 0 ? nullptr : A),
                         lda, &P, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &P, sizeof(int)) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)A);
  return -1;
}

int handle_cublasCgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  int batchSize;
  cuComplex **A = nullptr;
  size_t A_size;
  int lda;
  int P;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, cuComplex *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(cuComplex *const);
  A = (cuComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgetrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : A), lda,
               &P, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &P, sizeof(int)) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)A);
  return -1;
}

int handle_cublasZgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  int batchSize;
  cuDoubleComplex **A = nullptr;
  size_t A_size;
  int lda;
  int P;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, cuDoubleComplex *const[], int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(cuDoubleComplex *const);
  A = (cuDoubleComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgetrfBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr : A),
               lda, &P, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &P, sizeof(int)) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)A);
  return -1;
}

int handle_cublasSgetriBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const float **A = nullptr;
  size_t A_size;
  int lda;
  int *P_null_check;
  int P;
  float **C = nullptr;
  size_t C_size;
  int ldc;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const float *const[], int, const int *,
      float *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const float *const);
  A = (const float **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P_null_check, sizeof(const int *)) < 0 ||
      (P_null_check && rpc_read(conn, &P, sizeof(const int)) < 0) || false)
    goto ERROR_0;
  C_size = batchSize * sizeof(float *const);
  C = (float **)malloc(C_size);
  if (C_size != 0 && C == nullptr)
    goto ERROR_0;
  if ((C_size != 0 && rpc_read(conn, C, C_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgetriBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const float *const) == 0 ? nullptr : A), lda,
               P_null_check ? &P : nullptr,
               (batchSize * sizeof(float *const) == 0 ? nullptr : C), ldc,
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)C);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)C);
  free((void *)A);
  return -1;
}

int handle_cublasDgetriBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const double **A = nullptr;
  size_t A_size;
  int lda;
  int *P_null_check;
  int P;
  double **C = nullptr;
  size_t C_size;
  int ldc;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const double *const[], int, const int *,
      double *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const double *const);
  A = (const double **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P_null_check, sizeof(const int *)) < 0 ||
      (P_null_check && rpc_read(conn, &P, sizeof(const int)) < 0) || false)
    goto ERROR_0;
  C_size = batchSize * sizeof(double *const);
  C = (double **)malloc(C_size);
  if (C_size != 0 && C == nullptr)
    goto ERROR_0;
  if ((C_size != 0 && rpc_read(conn, C, C_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgetriBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const double *const) == 0 ? nullptr : A),
               lda, P_null_check ? &P : nullptr,
               (batchSize * sizeof(double *const) == 0 ? nullptr : C), ldc,
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)C);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)C);
  free((void *)A);
  return -1;
}

int handle_cublasCgetriBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const cuComplex **A = nullptr;
  size_t A_size;
  int lda;
  int *P_null_check;
  int P;
  cuComplex **C = nullptr;
  size_t C_size;
  int ldc;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuComplex *const[], int, const int *,
      cuComplex *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const cuComplex *const);
  A = (const cuComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P_null_check, sizeof(const int *)) < 0 ||
      (P_null_check && rpc_read(conn, &P, sizeof(const int)) < 0) || false)
    goto ERROR_0;
  C_size = batchSize * sizeof(cuComplex *const);
  C = (cuComplex **)malloc(C_size);
  if (C_size != 0 && C == nullptr)
    goto ERROR_0;
  if ((C_size != 0 && rpc_read(conn, C, C_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgetriBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const cuComplex *const) == 0 ? nullptr : A),
               lda, P_null_check ? &P : nullptr,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : C), ldc,
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)C);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)C);
  free((void *)A);
  return -1;
}

int handle_cublasZgetriBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex **A = nullptr;
  size_t A_size;
  int lda;
  int *P_null_check;
  int P;
  cuDoubleComplex **C = nullptr;
  size_t C_size;
  int ldc;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, int, const cuDoubleComplex *const[], int, const int *,
      cuDoubleComplex *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || false)
    goto ERROR_0;
  A_size = batchSize * sizeof(const cuDoubleComplex *const);
  A = (const cuDoubleComplex **)malloc(A_size);
  if (A_size != 0 && A == nullptr)
    goto ERROR_0;
  if ((A_size != 0 && rpc_read(conn, A, A_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P_null_check, sizeof(const int *)) < 0 ||
      (P_null_check && rpc_read(conn, &P, sizeof(const int)) < 0) || false)
    goto ERROR_0;
  C_size = batchSize * sizeof(cuDoubleComplex *const);
  C = (cuDoubleComplex **)malloc(C_size);
  if (C_size != 0 && C == nullptr)
    goto ERROR_0;
  if ((C_size != 0 && rpc_read(conn, C, C_size) < 0) ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgetriBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, n,
               (batchSize * sizeof(const cuDoubleComplex *const) == 0 ? nullptr
                                                                      : A),
               lda, P_null_check ? &P : nullptr,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr : C),
               ldc, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)C);
  free((void *)A);
  return 0;
ERROR_0:
  free((void *)C);
  free((void *)A);
  return -1;
}

int handle_cublasSgetrsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const float **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  int *devIpiv_null_check;
  int devIpiv;
  float **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const float *const[], int,
      const int *, float *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(const float *const);
  Aarray = (const float **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv_null_check, sizeof(const int *)) < 0 ||
      (devIpiv_null_check && rpc_read(conn, &devIpiv, sizeof(const int)) < 0) ||
      false)
    goto ERROR_0;
  Barray_size = batchSize * sizeof(float *const);
  Barray = (float **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasSgetrsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, n, nrhs,
               (batchSize * sizeof(const float *const) == 0 ? nullptr : Aarray),
               lda, devIpiv_null_check ? &devIpiv : nullptr,
               (batchSize * sizeof(float *const) == 0 ? nullptr : Barray), ldb,
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasDgetrsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const double **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  int *devIpiv_null_check;
  int devIpiv;
  double **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const double *const[], int,
      const int *, double *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(const double *const);
  Aarray = (const double **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv_null_check, sizeof(const int *)) < 0 ||
      (devIpiv_null_check && rpc_read(conn, &devIpiv, sizeof(const int)) < 0) ||
      false)
    goto ERROR_0;
  Barray_size = batchSize * sizeof(double *const);
  Barray = (double **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasDgetrsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, n, nrhs,
               (batchSize * sizeof(const double *const) == 0 ? nullptr
                                                             : Aarray),
               lda, devIpiv_null_check ? &devIpiv : nullptr,
               (batchSize * sizeof(double *const) == 0 ? nullptr : Barray), ldb,
               &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasCgetrsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  const int *devIpiv;
  cuComplex **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *const[],
      int, const int *, cuComplex *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(const cuComplex *const);
  Aarray = (const cuComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 || false)
    goto ERROR_0;
  Barray_size = batchSize * sizeof(cuComplex *const);
  Barray = (cuComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasCgetrsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, n, nrhs,
               (batchSize * sizeof(const cuComplex *const) == 0 ? nullptr
                                                                : Aarray),
               lda, devIpiv,
               (batchSize * sizeof(cuComplex *const) == 0 ? nullptr : Barray),
               ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasZgetrsBatched(conn_t *conn) {
  int batchSize;
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuDoubleComplex **Aarray = nullptr;
  size_t Aarray_size;
  int lda;
  int *devIpiv_null_check;
  int devIpiv;
  cuDoubleComplex **Barray = nullptr;
  size_t Barray_size;
  int ldb;
  int info;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, int, int,
      const cuDoubleComplex *const[], int, const int *,
      cuDoubleComplex *const[], int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 || false)
    goto ERROR_0;
  Aarray_size = batchSize * sizeof(const cuDoubleComplex *const);
  Aarray = (const cuDoubleComplex **)malloc(Aarray_size);
  if (Aarray_size != 0 && Aarray == nullptr)
    goto ERROR_0;
  if ((Aarray_size != 0 && rpc_read(conn, Aarray, Aarray_size) < 0) ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv_null_check, sizeof(const int *)) < 0 ||
      (devIpiv_null_check && rpc_read(conn, &devIpiv, sizeof(const int)) < 0) ||
      false)
    goto ERROR_0;
  Barray_size = batchSize * sizeof(cuDoubleComplex *const);
  Barray = (cuDoubleComplex **)malloc(Barray_size);
  if (Barray_size != 0 && Barray == nullptr)
    goto ERROR_0;
  if ((Barray_size != 0 && rpc_read(conn, Barray, Barray_size) < 0) ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasZgetrsBatched");
  return_value =
      fn == nullptr
          ? CUBLAS_STATUS_NOT_SUPPORTED
          : fn(handle, trans, n, nrhs,
               (batchSize * sizeof(const cuDoubleComplex *const) == 0 ? nullptr
                                                                      : Aarray),
               lda, devIpiv_null_check ? &devIpiv : nullptr,
               (batchSize * sizeof(cuDoubleComplex *const) == 0 ? nullptr
                                                                : Barray),
               ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)Barray);
  free((void *)Aarray);
  return 0;
ERROR_0:
  free((void *)Barray);
  free((void *)Aarray);
  return -1;
}

int handle_cublasUint8gemmBias(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  cublasOperation_t transc;
  int m;
  int n;
  int k;
  const unsigned char *A;
  int A_bias;
  int lda;
  const unsigned char *B;
  int B_bias;
  int ldb;
  unsigned char C;
  int C_bias;
  int ldc;
  int C_mult;
  int C_shift;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t(CUBLASWINAPI *)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, cublasOperation_t,
      int, int, int, const unsigned char *, int, int, const unsigned char *,
      int, int, unsigned char *, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transc, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const unsigned char *)) < 0 ||
      rpc_read(conn, &A_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const unsigned char *)) < 0 ||
      rpc_read(conn, &B_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(unsigned char)) < 0 ||
      rpc_read(conn, &C_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &C_mult, sizeof(int)) < 0 ||
      rpc_read(conn, &C_shift, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>("cublasUint8gemmBias");
  return_value =
      fn == nullptr ? CUBLAS_STATUS_NOT_SUPPORTED
                    : fn(handle, transa, transb, transc, m, n, k, A, A_bias,
                         lda, B, B_bias, ldb, &C, C_bias, ldc, C_mult, C_shift);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &C, sizeof(unsigned char)) < 0 ||
      rpc_write(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
