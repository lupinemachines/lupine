// Generated by codegen.py. Do not edit by hand.

#define LUPINE_SYMBOL_NAME_(name) #name
#define LUPINE_SYMBOL_NAME(name) LUPINE_SYMBOL_NAME_(name)

// A pointer-mode scalar's value lands in the handler's inline slot,
// or on the heap when it is wider.
static unsigned char *lupine_scalar_slot(std::vector<unsigned char> &heap,
                                         unsigned char *slot, size_t size,
                                         uint32_t width) {
  if (width <= size) {
    return slot;
  }
  heap.resize(width);
  return heap.data();
}

int handle_cublasAsumEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const void *, cudaDataType, int,
                         void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasAsumEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasAsumEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const void *, cudaDataType,
                         int64_t, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasAsumEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasAxpyEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphaType;
  const void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, const void *, cudaDataType, int,
                                  void *, cudaDataType, int, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphaType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasAxpyEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphaType, x, xType, incx, y, yType, incy, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasAxpyEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphaType;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const void *, cudaDataType, const void *,
      cudaDataType, int64_t, void *, cudaDataType, int64_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphaType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasAxpyEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphaType, x, xType, incx, y, yType, incy, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCaxpy_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                         const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCaxpy_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCcopy_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCcopy_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int, int,
                                  const cuComplex *, int, const cuComplex *,
                                  int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdgmm));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, int64_t, int64_t, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdgmm_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCdotc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int,
                                  const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdotc_v2));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n, x, incx, y, incy,
                         result_width != 0
                             ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                   result_heap, result_value,
                                   sizeof(result_value), result_width))
                             : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCdotc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *, int64_t,
                         const cuComplex *, int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdotc_v2_64));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n, x, incx, y, incy,
                         result_width != 0
                             ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                   result_heap, result_value,
                                   sizeof(result_value), result_width))
                             : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCdotu_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int,
                                  const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdotu_v2));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n, x, incx, y, incy,
                         result_width != 0
                             ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                   result_heap, result_value,
                                   sizeof(result_value), result_width))
                             : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCdotu_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *, int64_t,
                         const cuComplex *, int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCdotu_v2_64));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n, x, incx, y, incy,
                         result_width != 0
                             ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                   result_heap, result_value,
                                   sizeof(result_value), result_width))
                             : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  int, int, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  const cuComplex *B;
  int ldb;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgeam));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  const cuComplex *B;
  int64_t ldb;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgeam_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgelsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  cuComplex *const *Aarray;
  int lda;
  cuComplex *const *Carray;
  int ldc;
  int info;
  info = {};
  int *devInfoArray;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  int, cuComplex *const *, int,
                                  cuComplex *const *, int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Carray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgelsBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, trans, m, n, nrhs, Aarray, lda, Carray, ldc,
                          &info, devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3m(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3m));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgemm3mBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int lda;
  const cuComplex *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *const *, int,
      const cuComplex *const *, int, const cuComplex *, cuComplex *const *, int,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemm3mBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int64_t lda;
  const cuComplex *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *const *, int64_t,
      const cuComplex *const *, int64_t, const cuComplex *, cuComplex *const *,
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemm3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const cuComplex *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemm3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, const cuComplex *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemm3mStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, long long int,
      const cuComplex *, int, long long int, const cuComplex *, cuComplex *,
      int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemm3mStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, long long int,
      const cuComplex *, int64_t, long long int, const cuComplex *, cuComplex *,
      int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3mStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemm3m_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm3m_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int lda;
  const cuComplex *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *const *, int,
      const cuComplex *const *, int, const cuComplex *, cuComplex *const *, int,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int64_t lda;
  const cuComplex *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *const *, int64_t,
      const cuComplex *const *, int64_t, const cuComplex *, cuComplex *const *,
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemmEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const cuComplex *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, const cuComplex *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, long long int,
      const cuComplex *, int, long long int, const cuComplex *, cuComplex *,
      int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, long long int,
      const cuComplex *, int64_t, long long int, const cuComplex *, cuComplex *,
      int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemmStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuComplex *, const cuComplex *, int64_t, const cuComplex *,
      int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int lda;
  const cuComplex *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *,
      const cuComplex *const *, int, const cuComplex *const *, int,
      const cuComplex *, cuComplex *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *Aarray;
  int64_t lda;
  const cuComplex *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *const *, int64_t, const cuComplex *const *, int64_t,
      const cuComplex *, cuComplex *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  long long int strideA;
  const cuComplex *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const cuComplex *,
      const cuComplex *, int, long long int, const cuComplex *, int,
      long long int, const cuComplex *, cuComplex *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  long long int strideA;
  const cuComplex *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, const cuComplex *, const cuComplex *,
                                  int64_t, long long int, const cuComplex *,
                                  int64_t, long long int, const cuComplex *,
                                  cuComplex *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, const cuComplex *, const cuComplex *,
                                  int64_t, const cuComplex *, int64_t,
                                  const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgeqrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  cuComplex *const *Aarray;
  int lda;
  cuComplex *const *TauArray;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, cuComplex *const *,
                                  int, cuComplex *const *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TauArray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgeqrfBatched));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n, Aarray, lda, TauArray, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgerc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgerc_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgerc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgerc_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgeru_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgeru_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCgeru_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgeru_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex *const *A;
  int lda;
  int *P;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuComplex *const *, int,
                                  int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgetrfBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, P, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgetriBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *const *A;
  int lda;
  const int *P;
  cuComplex *const *C;
  int ldc;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *const *, int,
                         const int *, cuComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(const int *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgetriBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, P, C, ldc, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCgetrsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuComplex *const *Aarray;
  int lda;
  const int *devIpiv;
  cuComplex *const *Barray;
  int ldb;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const cuComplex *const *, int, const int *,
                                  cuComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &Barray, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCgetrsBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, Aarray, lda,
                                    devIpiv, Barray, ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasChbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  int64_t, const cuComplex *, const cuComplex *,
                                  int64_t, const cuComplex *, int64_t,
                                  const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasChemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t,
                                  cublasFillMode_t, int, int, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasChemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const cuComplex *, const cuComplex *, int64_t,
                                  const cuComplex *, int64_t, const cuComplex *,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCher2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCher2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                         const cuComplex *, const cuComplex *, int64_t,
                         const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCher2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCher2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCher_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *x;
  int incx;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const float *,
                         const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCher_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const cuComplex *, int64_t,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCher_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCherk3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const float *, const void *, cudaDataType,
                         int, const float *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherk3mEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCherk3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const void *, cudaDataType, int64_t, const float *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherk3mEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCherkEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const float *, const void *, cudaDataType,
                         int, const float *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherkEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCherkEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const void *, cudaDataType, int64_t, const float *, void *,
      cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherkEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCherk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const cuComplex *, int, const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCherk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int64_t, int64_t, const float *, const cuComplex *,
                         int64_t, const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCherkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCherkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const float *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCherkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasChpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *AP;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *AP;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const cuComplex *, const cuComplex *,
                                  const cuComplex *, int64_t, const cuComplex *,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasChpr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChpr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const cuComplex *, const cuComplex *, int64_t,
                                  const cuComplex *, int64_t, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasChpr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *x;
  int incx;
  cuComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const float *,
                         const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasChpr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  cuComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const cuComplex *, int64_t,
                                  cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasChpr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCmatinvBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *const *A;
  int lda;
  cuComplex *const *Ainv;
  int lda_inv;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *const *,
                                  int, cuComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Ainv, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCmatinvBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, Ainv, lda_inv, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCopyEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, int, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCopyEx));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCopyEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const void *, cudaDataType,
                         int64_t, void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCopyEx_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCreate_v2(conn_t *conn) {
  cublasHandle_t handle;
  handle = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCreate_v2));
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex *x;
  int incx;
  cuComplex *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const cuComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, cuComplex *, int, cuComplex *,
                         int, const float *, const cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex *x;
  int64_t incx;
  cuComplex *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const cuComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, cuComplex *, int64_t,
                                  cuComplex *, int64_t, const float *,
                                  const cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t a_width;
  alignas(16) unsigned char a_value[64];
  std::vector<unsigned char> a_heap;
  cuComplex *a = nullptr;
  uint32_t b_width;
  alignas(16) unsigned char b_value[64];
  std::vector<unsigned char> b_heap;
  cuComplex *b = nullptr;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  cuComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cuComplex *, cuComplex *,
                                  float *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a_width, sizeof(a_width)) < 0 ||
      (a_width == 0 && rpc_read(conn, &a, sizeof(a)) < 0) ||
      (a_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                a_width) < 0) ||
      rpc_read(conn, &b_width, sizeof(b_width)) < 0 ||
      (b_width == 0 && rpc_read(conn, &b, sizeof(b)) < 0) ||
      (b_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                b_width) < 0) ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCrotg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               a_width != 0 ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                  a_heap, a_value, sizeof(a_value), a_width))
                            : a,
               b_width != 0 ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                  b_heap, b_value, sizeof(b_value), b_width))
                            : b,
               c_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (a_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                 a_width) < 0) ||
      (b_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                 b_width) < 0) ||
      (c_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                 c_width) < 0) ||
      (s_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                 s_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex *x;
  int incx;
  cuComplex *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const float *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, cuComplex *, int, cuComplex *,
                         int, const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex *x;
  int64_t incx;
  cuComplex *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const float *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, cuComplex *, int64_t,
                         cuComplex *, int64_t, const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuComplex *x;
  int incx;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuComplex *, int,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCswap_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuComplex *x;
  int64_t incx;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, cuComplex *, int64_t,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCswap_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t,
                                  cublasFillMode_t, int, int, const cuComplex *,
                                  const cuComplex *, int, const cuComplex *,
                                  int, const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsymm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsymm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, const cuComplex *,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsymv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const cuComplex *, const cuComplex *, int64_t,
                                  const cuComplex *, int64_t, const cuComplex *,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsymv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  const cuComplex *y;
  int incy;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  const cuComplex *y;
  int64_t incy;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                         const cuComplex *, const cuComplex *, int64_t,
                         const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int incx;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, const cuComplex *, int,
                                  cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *x;
  int64_t incx;
  cuComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const cuComplex *, const cuComplex *, int64_t,
                                  cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyrk3mEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const void *, cudaDataType, int, const cuComplex *,
      void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrk3mEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyrk3mEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const void *, cudaDataType, int64_t, const cuComplex *,
      void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrk3mEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyrkEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const void *, cudaDataType, int, const cuComplex *,
      void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrkEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyrkEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const void *, cudaDataType, int64_t, const cuComplex *,
      void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrkEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const cuComplex *, const cuComplex *, int,
                         const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int64_t, int64_t, const cuComplex *, const cuComplex *,
                         int64_t, const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuComplex *, const cuComplex *, int, const cuComplex *, int,
      const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuComplex *, const cuComplex *, int64_t, const cuComplex *, int64_t,
      const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCsyrkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtbmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuComplex *A;
  int64_t lda;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtbmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtbsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuComplex *A;
  int64_t lda;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtbsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *AP;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtpmv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *AP;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtpmv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *AP;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtpsv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *AP;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const cuComplex *, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtpsv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *AP;
  cuComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtpttr));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, AP, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasCtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  cuComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuComplex *, const cuComplex *, int,
      const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrmm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  const cuComplex *B;
  int64_t ldb;
  cuComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrmm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtrsmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *A;
  int lda;
  cuComplex *const *B;
  int ldb;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuComplex *, const cuComplex *const *,
      int, cuComplex *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtrsmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *const *A;
  int64_t lda;
  cuComplex *const *B;
  int64_t ldb;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *,
      const cuComplex *const *, int64_t, cuComplex *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  cuComplex *B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t,
                                  cublasFillMode_t, cublasOperation_t,
                                  cublasDiagType_t, int, int, const cuComplex *,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int64_t lda;
  cuComplex *B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuComplex *, const cuComplex *,
      int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const cuComplex *, int, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasCtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuComplex *A;
  int64_t lda;
  cuComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuComplex *, int64_t, cuComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasCtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  cuComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const cuComplex *, int, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasCtrttp));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, A, lda, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDasum_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDasum_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const double *,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDaxpy_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDaxpy_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const double *, int,
                                  double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDcopy_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDcopy_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const double *A;
  int lda;
  const double *x;
  int incx;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int, int,
                                  const double *, int, const double *, int,
                                  double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDdgmm));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int64_t,
                                  int64_t, const double *, int64_t,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDdgmm_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDdot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  const double *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const double *, int,
                                  const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDdot_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx, y, incy,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDdot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDdot_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx, y, incy,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDestroy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDestroy_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, int64_t, int64_t, const double *,
                                  const double *, int64_t, const double *,
                                  int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  const double *B;
  int ldb;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, const double *, const double *, int,
                         const double *, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgeam));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  const double *B;
  int64_t ldb;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, const double *,
      int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgeam_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgelsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  double *const *Aarray;
  int lda;
  double *const *Carray;
  int ldc;
  int info;
  info = {};
  int *devInfoArray;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  int, double *const *, int, double *const *,
                                  int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Carray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgelsBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, trans, m, n, nrhs, Aarray, lda, Carray, ldc,
                          &info, devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgemmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *Aarray;
  int lda;
  const double *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const double *, const double *const *, int, const double *const *, int,
      const double *, double *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *Aarray;
  int64_t lda;
  const double *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *const *, int64_t,
      const double *const *, int64_t, const double *, double *const *, int64_t,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasDgemmGroupedBatched(conn_t *conn) {
  cublasHandle_t handle;
  int group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int *m_array = nullptr;
  size_t m_array_size;
  int *n_array = nullptr;
  size_t n_array_size;
  int *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const double *alpha_array = nullptr;
  const double *const *Aarray;
  int *lda_array = nullptr;
  size_t lda_array_size;
  const double *const *Barray;
  int *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const double *beta_array = nullptr;
  double *const *Carray;
  int *ldc_array = nullptr;
  size_t ldc_array_size;
  int *group_size = nullptr;
  size_t group_size_size;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int *, const int *, const int *, const double *,
      const double *const *, const int *, const double *const *, const int *,
      const double *, double *const *, const int *, int, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int)) < 0 || group_count < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int);
  m_array = (int *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int);
  n_array = (int *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int);
  k_array = (int *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      group_count < 0 || false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int);
  lda_array = (int *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const double *const *)) < 0 ||
      group_count < 0 || false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int);
  ldb_array = (int *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(double *const *)) < 0 || group_count < 0 ||
      false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int);
  ldc_array = (int *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int);
  group_size = (int *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmGroupedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray,
               (group_count * sizeof(const int) == 0 ? nullptr : lda_array),
               Barray,
               (group_count * sizeof(const int) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray,
               (group_count * sizeof(const int) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int) == 0 ? nullptr : group_size));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasDgemmGroupedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int64_t *m_array = nullptr;
  size_t m_array_size;
  int64_t *n_array = nullptr;
  size_t n_array_size;
  int64_t *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const double *alpha_array = nullptr;
  const double *const *Aarray;
  int64_t *lda_array = nullptr;
  size_t lda_array_size;
  const double *const *Barray;
  int64_t *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const double *beta_array = nullptr;
  double *const *Carray;
  int64_t *ldc_array = nullptr;
  size_t ldc_array_size;
  int64_t *group_size = nullptr;
  size_t group_size_size;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int64_t *, const int64_t *, const int64_t *, const double *,
      const double *const *, const int64_t *, const double *const *,
      const int64_t *, const double *, double *const *, const int64_t *,
      int64_t, const int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int64_t);
  m_array = (int64_t *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int64_t);
  n_array = (int64_t *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int64_t);
  k_array = (int64_t *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 || false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int64_t);
  lda_array = (int64_t *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const double *const *)) < 0 || false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int64_t);
  ldb_array = (int64_t *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(double *const *)) < 0 || false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int64_t);
  ldc_array = (int64_t *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int64_t);
  group_size = (int64_t *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmGroupedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : lda_array),
               Barray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int64_t) == 0 ? nullptr
                                                         : group_size));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

int handle_cublasDgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  long long int strideA;
  const double *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const double *, const double *, int, long long int, const double *, int,
      long long int, const double *, double *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  long long int strideA;
  const double *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *, int64_t, long long int,
      const double *, int64_t, long long int, const double *, double *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemmStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, int, const double *, const double *, int,
                         const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *Aarray;
  int lda;
  const double *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const double *, const double *const *, int,
                                  const double *const *, int, const double *,
                                  double *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *Aarray;
  int64_t lda;
  const double *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const double *,
      const double *const *, int64_t, const double *const *, int64_t,
      const double *, double *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  long long int strideA;
  const double *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const double *,
      const double *, int, long long int, const double *, int, long long int,
      const double *, double *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  long long int strideA;
  const double *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const double *,
      const double *, int64_t, long long int, const double *, int64_t,
      long long int, const double *, double *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, const double *, const double *,
                                  int64_t, const double *, int64_t,
                                  const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgeqrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  double *const *Aarray;
  int lda;
  double *const *TauArray;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, double *const *,
                                  int, double *const *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TauArray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgeqrfBatched));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n, Aarray, lda, TauArray, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDger_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, const double *,
                                  const double *, int, const double *, int,
                                  double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDger_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDger_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, int64_t,
                                  const double *, const double *, int64_t,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDger_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double *const *A;
  int lda;
  int *P;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, double *const *, int,
                                  int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgetrfBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, P, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgetriBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *const *A;
  int lda;
  const int *P;
  double *const *C;
  int ldc;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *const *, int,
                         const int *, double *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(const int *)) < 0 ||
      rpc_read(conn, &C, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgetriBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, P, C, ldc, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDgetrsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const double *const *Aarray;
  int lda;
  const int *devIpiv;
  double *const *Barray;
  int ldb;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const double *const *, int, const int *,
                                  double *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &Barray, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDgetrsBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, Aarray, lda,
                                    devIpiv, Barray, ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDmatinvBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *const *A;
  int lda;
  double *const *Ainv;
  int lda_inv;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const double *const *,
                                  int, double *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Ainv, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDmatinvBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, Ainv, lda_inv, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDnrm2_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDnrm2_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDotEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  const void *y;
  cudaDataType yType;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, int, const void *, cudaDataType,
                                  int, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDotEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDotEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  const void *y;
  cudaDataType yType;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDotEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDotcEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  const void *y;
  cudaDataType yType;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, int, const void *, cudaDataType,
                                  int, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDotcEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDotcEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  const void *y;
  cudaDataType yType;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const void *, cudaDataType, int64_t,
      const void *, cudaDataType, int64_t, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDotcEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double *x;
  int incx;
  double *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const double *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, double *, int, double *,
                                  int, const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double *x;
  int64_t incx;
  double *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const double *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, double *, int64_t, double *,
                         int64_t, const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t a_width;
  alignas(16) unsigned char a_value[64];
  std::vector<unsigned char> a_heap;
  double *a = nullptr;
  uint32_t b_width;
  alignas(16) unsigned char b_value[64];
  std::vector<unsigned char> b_heap;
  double *b = nullptr;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  double *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, double *, double *, double *,
                                  double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a_width, sizeof(a_width)) < 0 ||
      (a_width == 0 && rpc_read(conn, &a, sizeof(a)) < 0) ||
      (a_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                a_width) < 0) ||
      rpc_read(conn, &b_width, sizeof(b_width)) < 0 ||
      (b_width == 0 && rpc_read(conn, &b, sizeof(b)) < 0) ||
      (b_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                b_width) < 0) ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrotg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               a_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                  a_heap, a_value, sizeof(a_value), a_width))
                            : a,
               b_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                  b_heap, b_value, sizeof(b_value), b_width))
                            : b,
               c_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (a_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                 a_width) < 0) ||
      (b_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                 b_width) < 0) ||
      (c_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                 c_width) < 0) ||
      (s_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                 s_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDrotm_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double *x;
  int incx;
  double *y;
  int incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const double *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, double *, int, double *,
                                  int, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrotm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               param_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDrotm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double *x;
  int64_t incx;
  double *y;
  int64_t incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const double *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, double *, int64_t,
                                  double *, int64_t, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrotm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               param_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDrotmg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t d1_width;
  alignas(16) unsigned char d1_value[64];
  std::vector<unsigned char> d1_heap;
  double *d1 = nullptr;
  uint32_t d2_width;
  alignas(16) unsigned char d2_value[64];
  std::vector<unsigned char> d2_heap;
  double *d2 = nullptr;
  uint32_t x1_width;
  alignas(16) unsigned char x1_value[64];
  std::vector<unsigned char> x1_heap;
  double *x1 = nullptr;
  uint32_t y1_width;
  alignas(16) unsigned char y1_value[64];
  std::vector<unsigned char> y1_heap;
  const double *y1 = nullptr;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  double *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, double *, double *, double *,
                                  const double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &d1_width, sizeof(d1_width)) < 0 ||
      (d1_width == 0 && rpc_read(conn, &d1, sizeof(d1)) < 0) ||
      (d1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d1_heap, d1_value,
                                                    sizeof(d1_value), d1_width),
                                 d1_width) < 0) ||
      rpc_read(conn, &d2_width, sizeof(d2_width)) < 0 ||
      (d2_width == 0 && rpc_read(conn, &d2, sizeof(d2)) < 0) ||
      (d2_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d2_heap, d2_value,
                                                    sizeof(d2_value), d2_width),
                                 d2_width) < 0) ||
      rpc_read(conn, &x1_width, sizeof(x1_width)) < 0 ||
      (x1_width == 0 && rpc_read(conn, &x1, sizeof(x1)) < 0) ||
      (x1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(x1_heap, x1_value,
                                                    sizeof(x1_value), x1_width),
                                 x1_width) < 0) ||
      rpc_read(conn, &y1_width, sizeof(y1_width)) < 0 ||
      (y1_width == 0 && rpc_read(conn, &y1, sizeof(y1)) < 0) ||
      (y1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(y1_heap, y1_value,
                                                    sizeof(y1_value), y1_width),
                                 y1_width) < 0) ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDrotmg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               d1_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         d1_heap, d1_value, sizeof(d1_value), d1_width))
                   : d1,
               d2_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         d2_heap, d2_value, sizeof(d2_value), d2_width))
                   : d2,
               x1_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         x1_heap, x1_value, sizeof(x1_value), x1_width))
                   : x1,
               y1_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         y1_heap, y1_value, sizeof(y1_value), y1_width))
                   : y1,
               param_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                      param_heap, param_value,
                                      sizeof(param_value), param_width))
                                : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (d1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d1_heap, d1_value, sizeof(d1_value), d1_width),
           d1_width) < 0) ||
      (d2_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d2_heap, d2_value, sizeof(d2_value), d2_width),
           d2_width) < 0) ||
      (x1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(x1_heap, x1_value, sizeof(x1_value), x1_width),
           x1_width) < 0) ||
      (param_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(param_heap, param_value,
                                    sizeof(param_value), param_width),
                 param_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDsbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, int, const double *,
      const double *, int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  int64_t, const double *, const double *,
                                  int64_t, const double *, int64_t,
                                  const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDspmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *AP;
  const double *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDspmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *AP;
  const double *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      const double *, int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDspr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const double *,
                         const double *, int, const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDspr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const double *, const double *, int64_t,
                                  const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDspr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  double *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const double *,
                         const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDspr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  double *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                         const double *, const double *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDspr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  double *x;
  int incx;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDswap_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  double *x;
  int64_t incx;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, double *, int64_t,
                                  double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDswap_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         int, int, const double *, const double *, int,
                         const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsymm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsymm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const double *, const double *,
      int, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsymv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsymv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  const double *y;
  int incy;
  double *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const double *, const double *, int,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  const double *y;
  int64_t incy;
  double *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const double *, const double *, int64_t,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const double *, const double *, int,
                         const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int incx;
  double *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const double *,
                         const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *x;
  int64_t incx;
  double *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const double *, const double *, int64_t,
                                  double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const double *, const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyrk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int64_t, int64_t, const double *, const double *,
                         int64_t, const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyrk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const double *, const double *, int,
                         const double *, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyrkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const double *, int64_t, const double *, int64_t,
      const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDsyrkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const double *A;
  int lda;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtbmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const double *A;
  int64_t lda;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtbmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const double *A;
  int lda;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtbsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const double *A;
  int64_t lda;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtbsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *AP;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         cublasDiagType_t, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtpmv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *AP;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtpmv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *AP;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         cublasDiagType_t, int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtpsv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *AP;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const double *, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtpsv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *AP;
  double *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const double *)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtpttr));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, AP, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  double *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const double *, const double *, int,
      const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrmm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  const double *B;
  int64_t ldb;
  double *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *,
      int64_t, const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrmm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *A;
  int lda;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *A;
  int64_t lda;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtrsmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *A;
  int lda;
  double *const *B;
  int ldb;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const double *, const double *const *, int,
      double *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtrsmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *const *A;
  int64_t lda;
  double *const *B;
  int64_t ldb;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *const *,
      int64_t, double *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(double *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  double *B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         cublasOperation_t, cublasDiagType_t, int, int,
                         const double *, const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int64_t lda;
  double *B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const double *, const double *,
      int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const double *A;
  int lda;
  double *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const double *A;
  int64_t lda;
  double *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const double *, int64_t, double *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  double *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const double *, int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDtrttp));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, A, lda, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasDzasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDzasum_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDzasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t,
                                  const cuDoubleComplex *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDzasum_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasDznrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  int, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDznrm2_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasDznrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  double *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t,
                                  const cuDoubleComplex *, int64_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasDznrm2_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasGemmBatchedEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *const *Aarray;
  cudaDataType Atype;
  int lda;
  const void *const *Barray;
  cudaDataType Btype;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *const *Carray;
  cudaDataType Ctype;
  int ldc;
  int batchCount;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const void *, const void *const *, cudaDataType, int, const void *const *,
      cudaDataType, int, const void *, void *const *, cudaDataType, int, int,
      cublasComputeType_t, cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(void *const *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmBatchedEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, Atype, lda, Barray, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, Ctype, ldc, batchCount, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasGemmBatchedEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *const *Aarray;
  cudaDataType Atype;
  int64_t lda;
  const void *const *Barray;
  cudaDataType Btype;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *const *Carray;
  cudaDataType Ctype;
  int64_t ldc;
  int64_t batchCount;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const void *, const void *const *, cudaDataType, int64_t,
      const void *const *, cudaDataType, int64_t, const void *, void *const *,
      cudaDataType, int64_t, int64_t, cublasComputeType_t, cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(void *const *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmBatchedEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, Atype, lda, Barray, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, Ctype, ldc, batchCount, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasGemmEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const void *, const void *, cudaDataType, int, const void *, cudaDataType,
      int, const void *, void *, cudaDataType, int, cublasComputeType_t,
      cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasGemmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const void *, const void *, cudaDataType, int64_t, const void *,
      cudaDataType, int64_t, const void *, void *, cudaDataType, int64_t,
      cublasComputeType_t, cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasGemmGroupedBatchedEx(conn_t *conn) {
  cublasHandle_t handle;
  int group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int *m_array = nullptr;
  size_t m_array_size;
  int *n_array = nullptr;
  size_t n_array_size;
  int *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const void *alpha_array = nullptr;
  const void *const *Aarray;
  cudaDataType_t Atype;
  int *lda_array = nullptr;
  size_t lda_array_size;
  const void *const *Barray;
  cudaDataType_t Btype;
  int *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const void *beta_array = nullptr;
  void *const *Carray;
  cudaDataType_t Ctype;
  int *ldc_array = nullptr;
  size_t ldc_array_size;
  int *group_size = nullptr;
  size_t group_size_size;
  cublasComputeType_t computeType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int *, const int *, const int *, const void *, const void *const *,
      cudaDataType_t, const int *, const void *const *, cudaDataType_t,
      const int *, const void *, void *const *, cudaDataType_t, const int *,
      int, const int *, cublasComputeType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int)) < 0 || group_count < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int);
  m_array = (int *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int);
  n_array = (int *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int);
  k_array = (int *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType_t)) < 0 || group_count < 0 ||
      false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int);
  lda_array = (int *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType_t)) < 0 || group_count < 0 ||
      false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int);
  ldb_array = (int *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(void *const *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType_t)) < 0 || group_count < 0 ||
      false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int);
  ldc_array = (int *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int);
  group_size = (int *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmGroupedBatchedEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray, Atype,
               (group_count * sizeof(const int) == 0 ? nullptr : lda_array),
               Barray, Btype,
               (group_count * sizeof(const int) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray, Ctype,
               (group_count * sizeof(const int) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int) == 0 ? nullptr : group_size),
               computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasGemmGroupedBatchedEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int64_t *m_array = nullptr;
  size_t m_array_size;
  int64_t *n_array = nullptr;
  size_t n_array_size;
  int64_t *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const void *alpha_array = nullptr;
  const void *const *Aarray;
  cudaDataType_t Atype;
  int64_t *lda_array = nullptr;
  size_t lda_array_size;
  const void *const *Barray;
  cudaDataType_t Btype;
  int64_t *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const void *beta_array = nullptr;
  void *const *Carray;
  cudaDataType_t Ctype;
  int64_t *ldc_array = nullptr;
  size_t ldc_array_size;
  int64_t *group_size = nullptr;
  size_t group_size_size;
  cublasComputeType_t computeType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int64_t *, const int64_t *, const int64_t *, const void *,
      const void *const *, cudaDataType_t, const int64_t *, const void *const *,
      cudaDataType_t, const int64_t *, const void *, void *const *,
      cudaDataType_t, const int64_t *, int64_t, const int64_t *,
      cublasComputeType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int64_t);
  m_array = (int64_t *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int64_t);
  n_array = (int64_t *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int64_t);
  k_array = (int64_t *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType_t)) < 0 || false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int64_t);
  lda_array = (int64_t *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const void *const *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType_t)) < 0 || false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int64_t);
  ldb_array = (int64_t *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(void *const *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType_t)) < 0 || false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int64_t);
  ldc_array = (int64_t *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int64_t);
  group_size = (int64_t *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmGroupedBatchedEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray, Atype,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : lda_array),
               Barray, Btype,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray, Ctype,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int64_t) == 0 ? nullptr
                                                         : group_size),
               computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

int handle_cublasGemmStridedBatchedEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  long long int strideA;
  const void *B;
  cudaDataType Btype;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  long long int strideC;
  int batchCount;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const void *, const void *, cudaDataType, int, long long int,
      const void *, cudaDataType, int, long long int, const void *, void *,
      cudaDataType, int, long long int, int, cublasComputeType_t,
      cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmStridedBatchedEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, strideA, B, Btype, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc, strideC, batchCount, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasGemmStridedBatchedEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  long long int strideA;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  cublasComputeType_t computeType;
  cublasGemmAlgo_t algo;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const void *, const void *, cudaDataType, int64_t, long long int,
      const void *, cudaDataType, int64_t, long long int, const void *, void *,
      cudaDataType, int64_t, long long int, int64_t, cublasComputeType_t,
      cublasGemmAlgo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cublasComputeType_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cublasGemmAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGemmStridedBatchedEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, strideA, B, Btype, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc, strideC, batchCount, computeType, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasGetAtomicsMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasAtomicsMode_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasAtomicsMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetAtomicsMode));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasAtomicsMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetCudartVersion(conn_t *conn) {
  int request_id;
  size_t return_value = {};
  cublasStatus_t rpc_status = CUBLAS_STATUS_SUCCESS;
  using fn_t = size_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetCudartVersion));
  if (fn == nullptr)
    rpc_status = function_not_found();
  else
    return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(size_t)) < 0 ||
      rpc_write(conn, &rpc_status, sizeof(rpc_status)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 130100
int handle_cublasGetEmulationSpecialValuesSupport(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationSpecialValuesSupport mask;
  mask = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cudaEmulationSpecialValuesSupport *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasGetEmulationSpecialValuesSupport));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120900
int handle_cublasGetEmulationStrategy(conn_t *conn) {
  cublasHandle_t handle;
  cublasEmulationStrategy_t emulationStrategy;
  emulationStrategy = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasEmulationStrategy_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetEmulationStrategy));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &emulationStrategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &emulationStrategy, sizeof(cublasEmulationStrategy_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasGetFixedPointEmulationMantissaBitCountPointer(conn_t *conn) {
  cublasHandle_t handle;
  int *mantissaBitCount;
  mantissaBitCount = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasGetFixedPointEmulationMantissaBitCountPointer));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitCount, sizeof(int *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasGetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cublasHandle_t handle;
  int mantissaBitOffset;
  mantissaBitOffset = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasGetFixedPointEmulationMantissaBitOffset));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitOffset, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasGetFixedPointEmulationMantissaControl(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationMantissaControl mantissaControl;
  mantissaControl = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cudaEmulationMantissaControl *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasGetFixedPointEmulationMantissaControl));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &mantissaControl);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaControl, sizeof(cudaEmulationMantissaControl)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasGetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cublasHandle_t handle;
  int maxMantissaBitCount;
  maxMantissaBitCount = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasGetFixedPointEmulationMaxMantissaBitCount));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &maxMantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &maxMantissaBitCount, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasGetMathMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasMath_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasMath_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetMathMode));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasMath_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetPointerMode_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasPointerMode_t mode;
  mode = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasPointerMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetPointerMode_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cublasPointerMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value;
  value = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetProperty));
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetSmCountTarget(conn_t *conn) {
  cublasHandle_t handle;
  int smCountTarget;
  smCountTarget = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetSmCountTarget));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &smCountTarget);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &smCountTarget, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetStream_v2(conn_t *conn) {
  cublasHandle_t handle;
  cudaStream_t streamId;
  streamId = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetStream_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasGetVersion_v2(conn_t *conn) {
  cublasHandle_t handle;
  int version;
  version = {};
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasGetVersion_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle, &version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHSHgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *const *Aarray;
  int lda;
  const __half *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __half *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const float *, const __half *const *, int,
                                  const __half *const *, int, const float *,
                                  __half *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(__half *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSHgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHSHgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *const *Aarray;
  int64_t lda;
  const __half *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __half *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *const *, int64_t, const __half *const *, int64_t,
      const float *, __half *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(__half *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSHgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasHSHgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __half *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *, int, long long int, const __half *, int, long long int,
      const float *, __half *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(__half *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSHgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHSHgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __half *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *, int64_t, long long int, const __half *, int64_t,
      long long int, const float *, __half *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(__half *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSHgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasHSSgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *const *Aarray;
  int lda;
  const __half *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const float *, const __half *const *, int,
                                  const __half *const *, int, const float *,
                                  float *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSSgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHSSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *const *Aarray;
  int64_t lda;
  const __half *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *const *, int64_t, const __half *const *, int64_t,
      const float *, float *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSSgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasHSSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __half *, int, long long int, const __half *, int, long long int,
      const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSSgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHSSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __half *, int64_t, long long int, const __half *, int64_t,
      long long int, const float *, float *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHSSgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasHgemm(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *A;
  int lda;
  const __half *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, int, const __half *, const __half *, int,
                         const __half *, int, const __half *, __half *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(__half *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemm));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasHgemmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *const *Aarray;
  int lda;
  const __half *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const __half *, const __half *const *, int, const __half *const *, int,
      const __half *, __half *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(__half *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHgemmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *const *Aarray;
  int64_t lda;
  const __half *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *const *, int64_t,
      const __half *const *, int64_t, const __half *, __half *const *, int64_t,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const __half *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(__half *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasHgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *A;
  int lda;
  long long int strideA;
  const __half *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const __half *, const __half *, int, long long int, const __half *, int,
      long long int, const __half *, __half *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(__half *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemmStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasHgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *A;
  int64_t lda;
  long long int strideA;
  const __half *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *, int64_t, long long int,
      const __half *, int64_t, long long int, const __half *, __half *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(__half *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemmStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120000
int handle_cublasHgemm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const __half *alpha = nullptr;
  const __half *A;
  int64_t lda;
  const __half *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const __half *beta = nullptr;
  __half *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const __half *, const __half *, int64_t, const __half *, int64_t,
      const __half *, __half *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(__half *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasHgemm_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const __half *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIamaxEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIamaxEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIamaxEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const void *,
                                  cudaDataType, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIamaxEx_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, xType, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIaminEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const void *,
                                  cudaDataType, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIaminEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIaminEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const void *,
                                  cudaDataType, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIaminEx_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, xType, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIcamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIcamax_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIcamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIcamax_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIcamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIcamin_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIcamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIcamin_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIdamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIdamax_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIdamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIdamax_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIdamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const double *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIdamin_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIdamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const double *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIdamin_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIsamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIsamax_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIsamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIsamax_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIsamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIsamin_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIsamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIsamin_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIzamax_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIzamax_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIzamax_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t,
                                  const cuDoubleComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIzamax_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasIzamin_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIzamin_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasIzamin_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  int64_t *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t,
                                  const cuDoubleComplex *, int64_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasIzamin_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, x, incx,
                          result_width != 0
                              ? reinterpret_cast<int64_t *>(lupine_scalar_slot(
                                    result_heap, result_value,
                                    sizeof(result_value), result_width))
                              : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasNrm2Ex(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const void *x;
  cudaDataType xType;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const void *, cudaDataType, int,
                         void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasNrm2Ex));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasNrm2Ex_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const void *x;
  cudaDataType xType;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType resultType;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const void *, cudaDataType,
                         int64_t, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &resultType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasNrm2Ex_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               resultType, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasRotEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const void *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const void *s = nullptr;
  cudaDataType csType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, void *, cudaDataType,
                                  int, void *, cudaDataType, int, const void *,
                                  const void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      rpc_read(conn, &csType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               c_width != 0 ? reinterpret_cast<const void *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<const void *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s,
               csType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasRotEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const void *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const void *s = nullptr;
  cudaDataType csType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, void *, cudaDataType, int64_t,
                         void *, cudaDataType, int64_t, const void *,
                         const void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      rpc_read(conn, &csType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               c_width != 0 ? reinterpret_cast<const void *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<const void *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s,
               csType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasRotgEx(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t a_width;
  alignas(16) unsigned char a_value[64];
  std::vector<unsigned char> a_heap;
  void *a = nullptr;
  uint32_t b_width;
  alignas(16) unsigned char b_value[64];
  std::vector<unsigned char> b_heap;
  void *b = nullptr;
  cudaDataType abType;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  void *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  void *s = nullptr;
  cudaDataType csType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, void *, void *, cudaDataType,
                                  void *, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a_width, sizeof(a_width)) < 0 ||
      (a_width == 0 && rpc_read(conn, &a, sizeof(a)) < 0) ||
      (a_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                a_width) < 0) ||
      rpc_read(conn, &b_width, sizeof(b_width)) < 0 ||
      (b_width == 0 && rpc_read(conn, &b, sizeof(b)) < 0) ||
      (b_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                b_width) < 0) ||
      rpc_read(conn, &abType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      rpc_read(conn, &csType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotgEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               a_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                  a_heap, a_value, sizeof(a_value), a_width))
                            : a,
               b_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                  b_heap, b_value, sizeof(b_value), b_width))
                            : b,
               abType,
               c_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s,
               csType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (a_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                 a_width) < 0) ||
      (b_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                 b_width) < 0) ||
      (c_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                 c_width) < 0) ||
      (s_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                 s_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasRotmEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const void *param = nullptr;
  cudaDataType paramType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, void *, cudaDataType,
                                  int, void *, cudaDataType, int, const void *,
                                  cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      rpc_read(conn, &paramType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotmEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               param_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param,
               paramType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasRotmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const void *param = nullptr;
  cudaDataType paramType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, void *, cudaDataType,
                                  int64_t, void *, cudaDataType, int64_t,
                                  const void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      rpc_read(conn, &paramType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotmEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, xType, incx, y, yType, incy,
               param_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param,
               paramType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasRotmgEx(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t d1_width;
  alignas(16) unsigned char d1_value[64];
  std::vector<unsigned char> d1_heap;
  void *d1 = nullptr;
  cudaDataType d1Type;
  uint32_t d2_width;
  alignas(16) unsigned char d2_value[64];
  std::vector<unsigned char> d2_heap;
  void *d2 = nullptr;
  cudaDataType d2Type;
  uint32_t x1_width;
  alignas(16) unsigned char x1_value[64];
  std::vector<unsigned char> x1_heap;
  void *x1 = nullptr;
  cudaDataType x1Type;
  uint32_t y1_width;
  alignas(16) unsigned char y1_value[64];
  std::vector<unsigned char> y1_heap;
  const void *y1 = nullptr;
  cudaDataType y1Type;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  void *param = nullptr;
  cudaDataType paramType;
  cudaDataType executiontype;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, void *, cudaDataType, void *,
                         cudaDataType, void *, cudaDataType, const void *,
                         cudaDataType, void *, cudaDataType, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &d1_width, sizeof(d1_width)) < 0 ||
      (d1_width == 0 && rpc_read(conn, &d1, sizeof(d1)) < 0) ||
      (d1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d1_heap, d1_value,
                                                    sizeof(d1_value), d1_width),
                                 d1_width) < 0) ||
      rpc_read(conn, &d1Type, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d2_width, sizeof(d2_width)) < 0 ||
      (d2_width == 0 && rpc_read(conn, &d2, sizeof(d2)) < 0) ||
      (d2_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d2_heap, d2_value,
                                                    sizeof(d2_value), d2_width),
                                 d2_width) < 0) ||
      rpc_read(conn, &d2Type, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &x1_width, sizeof(x1_width)) < 0 ||
      (x1_width == 0 && rpc_read(conn, &x1, sizeof(x1)) < 0) ||
      (x1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(x1_heap, x1_value,
                                                    sizeof(x1_value), x1_width),
                                 x1_width) < 0) ||
      rpc_read(conn, &x1Type, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &y1_width, sizeof(y1_width)) < 0 ||
      (y1_width == 0 && rpc_read(conn, &y1, sizeof(y1)) < 0) ||
      (y1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(y1_heap, y1_value,
                                                    sizeof(y1_value), y1_width),
                                 y1_width) < 0) ||
      rpc_read(conn, &y1Type, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      rpc_read(conn, &paramType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasRotmgEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               d1_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         d1_heap, d1_value, sizeof(d1_value), d1_width))
                   : d1,
               d1Type,
               d2_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         d2_heap, d2_value, sizeof(d2_value), d2_width))
                   : d2,
               d2Type,
               x1_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         x1_heap, x1_value, sizeof(x1_value), x1_width))
                   : x1,
               x1Type,
               y1_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         y1_heap, y1_value, sizeof(y1_value), y1_width))
                   : y1,
               y1Type,
               param_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                      param_heap, param_value,
                                      sizeof(param_value), param_width))
                                : param,
               paramType, executiontype);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (d1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d1_heap, d1_value, sizeof(d1_value), d1_width),
           d1_width) < 0) ||
      (d2_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d2_heap, d2_value, sizeof(d2_value), d2_width),
           d2_width) < 0) ||
      (x1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(x1_heap, x1_value, sizeof(x1_value), x1_width),
           x1_width) < 0) ||
      (param_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(param_heap, param_value,
                                    sizeof(param_value), param_width),
                 param_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSasum_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSasum_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const float *,
                                  const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSaxpy_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSaxpy_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasScalEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphaType;
  void *x;
  cudaDataType xType;
  int incx;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const void *, cudaDataType,
                         void *, cudaDataType, int, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphaType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScalEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphaType, x, xType, incx, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasScalEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphaType;
  void *x;
  cudaDataType xType;
  int64_t incx;
  cudaDataType executionType;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const void *, cudaDataType,
                         void *, cudaDataType, int64_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphaType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &executionType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScalEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphaType, x, xType, incx, executionType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasScasum_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScasum_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasScasum_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScasum_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasScnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuComplex *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuComplex *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScnrm2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasScnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuComplex *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const cuComplex *,
                                  int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScnrm2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasScopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScopy_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasScopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasScopy_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const float *A;
  int lda;
  const float *x;
  int incx;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int, int,
                         const float *, int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSdgmm));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int64_t,
                                  int64_t, const float *, int64_t,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSdgmm_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSdot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  const float *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const float *, int,
                                  const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSdot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSdot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSdot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSetAtomicsMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasAtomicsMode_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasAtomicsMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasAtomicsMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetAtomicsMode));
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 130100
int handle_cublasSetEmulationSpecialValuesSupport(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationSpecialValuesSupport mask;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cudaEmulationSpecialValuesSupport);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasSetEmulationSpecialValuesSupport));
  return_value = fn == nullptr ? function_not_found() : fn(handle, mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120900
int handle_cublasSetEmulationStrategy(conn_t *conn) {
  cublasHandle_t handle;
  cublasEmulationStrategy_t emulationStrategy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasEmulationStrategy_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &emulationStrategy, sizeof(cublasEmulationStrategy_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetEmulationStrategy));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, emulationStrategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasSetFixedPointEmulationMantissaBitCountPointer(conn_t *conn) {
  cublasHandle_t handle;
  int *mantissaBitCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitCount, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasSetFixedPointEmulationMantissaBitCountPointer));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasSetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cublasHandle_t handle;
  int mantissaBitOffset;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitOffset, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasSetFixedPointEmulationMantissaBitOffset));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasSetFixedPointEmulationMantissaControl(conn_t *conn) {
  cublasHandle_t handle;
  cudaEmulationMantissaControl mantissaControl;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cudaEmulationMantissaControl);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mantissaControl, sizeof(cudaEmulationMantissaControl)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasSetFixedPointEmulationMantissaControl));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, mantissaControl);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 130100
int handle_cublasSetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cublasHandle_t handle;
  int maxMantissaBitCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &maxMantissaBitCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(
      LUPINE_SYMBOL_NAME(cublasSetFixedPointEmulationMaxMantissaBitCount));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, maxMantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSetMathMode(conn_t *conn) {
  cublasHandle_t handle;
  cublasMath_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasMath_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasMath_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetMathMode));
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetPointerMode_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasPointerMode_t mode;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasPointerMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasPointerMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetPointerMode_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetSmCountTarget(conn_t *conn) {
  cublasHandle_t handle;
  int smCountTarget;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &smCountTarget, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetSmCountTarget));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, smCountTarget);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetStream_v2(conn_t *conn) {
  cublasHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetStream_v2));
  return_value = fn == nullptr ? function_not_found() : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSetWorkspace_v2(conn_t *conn) {
  cublasHandle_t handle;
  void *workspace;
  size_t workspaceSizeInBytes;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSetWorkspace_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, workspace, workspaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, int, int, const float *,
      const float *, int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, int64_t, int64_t, const float *,
                                  const float *, int64_t, const float *,
                                  int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  const float *B;
  int ldb;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, const float *, const float *, int,
                         const float *, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgeam));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  const float *B;
  int64_t ldb;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, const float *,
      int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgeam_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgelsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  float *const *Aarray;
  int lda;
  float *const *Carray;
  int ldc;
  int info;
  info = {};
  int *devInfoArray;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  int, float *const *, int, float *const *, int,
                                  int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Carray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgelsBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, trans, m, n, nrhs, Aarray, lda, Carray, ldc,
                          &info, devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgemmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *Aarray;
  int lda;
  const float *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const float *const *, int, const float *const *, int,
      const float *, float *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *Aarray;
  int64_t lda;
  const float *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t,
                                  cublasOperation_t, int64_t, int64_t, int64_t,
                                  const float *, const float *const *, int64_t,
                                  const float *const *, int64_t, const float *,
                                  float *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgemmEx(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int lda;
  const void *B;
  cudaDataType Btype;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const void *, cudaDataType, int, const void *,
      cudaDataType, int, const float *, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmEx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemmEx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const void *A;
  cudaDataType Atype;
  int64_t lda;
  const void *B;
  cudaDataType Btype;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  void *C;
  cudaDataType Ctype;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const void *, cudaDataType, int64_t, const void *,
      cudaDataType, int64_t, const float *, void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Atype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &Btype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(void *)) < 0 ||
      rpc_read(conn, &Ctype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmEx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, Atype, lda, B, Btype, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, Ctype, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasSgemmGroupedBatched(conn_t *conn) {
  cublasHandle_t handle;
  int group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int *m_array = nullptr;
  size_t m_array_size;
  int *n_array = nullptr;
  size_t n_array_size;
  int *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const float *alpha_array = nullptr;
  const float *const *Aarray;
  int *lda_array = nullptr;
  size_t lda_array_size;
  const float *const *Barray;
  int *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const float *beta_array = nullptr;
  float *const *Carray;
  int *ldc_array = nullptr;
  size_t ldc_array_size;
  int *group_size = nullptr;
  size_t group_size_size;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int *, const int *, const int *, const float *,
      const float *const *, const int *, const float *const *, const int *,
      const float *, float *const *, const int *, int, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int)) < 0 || group_count < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int);
  m_array = (int *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int);
  n_array = (int *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int);
  k_array = (int *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      group_count < 0 || false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int);
  lda_array = (int *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const float *const *)) < 0 ||
      group_count < 0 || false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int);
  ldb_array = (int *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(float *const *)) < 0 || group_count < 0 ||
      false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int);
  ldc_array = (int *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      group_count < 0 || false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int);
  group_size = (int *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmGroupedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray,
               (group_count * sizeof(const int) == 0 ? nullptr : lda_array),
               Barray,
               (group_count * sizeof(const int) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray,
               (group_count * sizeof(const int) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int) == 0 ? nullptr : group_size));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

#if CUBLAS_VERSION >= 120500
int handle_cublasSgemmGroupedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t group_count;
  cublasOperation_t *transa_array = nullptr;
  size_t transa_array_size;
  cublasOperation_t *transb_array = nullptr;
  size_t transb_array_size;
  int64_t *m_array = nullptr;
  size_t m_array_size;
  int64_t *n_array = nullptr;
  size_t n_array_size;
  int64_t *k_array = nullptr;
  size_t k_array_size;
  uint32_t alpha_array_width;
  alignas(16) unsigned char alpha_array_value[64];
  std::vector<unsigned char> alpha_array_heap;
  const float *alpha_array = nullptr;
  const float *const *Aarray;
  int64_t *lda_array = nullptr;
  size_t lda_array_size;
  const float *const *Barray;
  int64_t *ldb_array = nullptr;
  size_t ldb_array_size;
  uint32_t beta_array_width;
  alignas(16) unsigned char beta_array_value[64];
  std::vector<unsigned char> beta_array_heap;
  const float *beta_array = nullptr;
  float *const *Carray;
  int64_t *ldc_array = nullptr;
  size_t ldc_array_size;
  int64_t *group_size = nullptr;
  size_t group_size_size;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, const cublasOperation_t *, const cublasOperation_t *,
      const int64_t *, const int64_t *, const int64_t *, const float *,
      const float *const *, const int64_t *, const float *const *,
      const int64_t *, const float *, float *const *, const int64_t *, int64_t,
      const int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &group_count, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;
  transa_array_size = group_count * sizeof(const cublasOperation_t);
  transa_array = (cublasOperation_t *)malloc(transa_array_size);
  if (transa_array_size != 0 && transa_array == nullptr)
    goto ERROR_0;
  if ((transa_array_size != 0 &&
       rpc_read(conn, transa_array, transa_array_size) < 0) ||
      false)
    goto ERROR_0;
  transb_array_size = group_count * sizeof(const cublasOperation_t);
  transb_array = (cublasOperation_t *)malloc(transb_array_size);
  if (transb_array_size != 0 && transb_array == nullptr)
    goto ERROR_0;
  if ((transb_array_size != 0 &&
       rpc_read(conn, transb_array, transb_array_size) < 0) ||
      false)
    goto ERROR_0;
  m_array_size = group_count * sizeof(const int64_t);
  m_array = (int64_t *)malloc(m_array_size);
  if (m_array_size != 0 && m_array == nullptr)
    goto ERROR_0;
  if ((m_array_size != 0 && rpc_read(conn, m_array, m_array_size) < 0) || false)
    goto ERROR_0;
  n_array_size = group_count * sizeof(const int64_t);
  n_array = (int64_t *)malloc(n_array_size);
  if (n_array_size != 0 && n_array == nullptr)
    goto ERROR_0;
  if ((n_array_size != 0 && rpc_read(conn, n_array, n_array_size) < 0) || false)
    goto ERROR_0;
  k_array_size = group_count * sizeof(const int64_t);
  k_array = (int64_t *)malloc(k_array_size);
  if (k_array_size != 0 && k_array == nullptr)
    goto ERROR_0;
  if ((k_array_size != 0 && rpc_read(conn, k_array, k_array_size) < 0) ||
      rpc_read(conn, &alpha_array_width, sizeof(alpha_array_width)) < 0 ||
      (alpha_array_width == 0 &&
       rpc_read(conn, &alpha_array, sizeof(alpha_array)) < 0) ||
      (alpha_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_array_heap, alpha_array_value,
                                   sizeof(alpha_array_value),
                                   alpha_array_width),
                alpha_array_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 || false)
    goto ERROR_0;
  lda_array_size = group_count * sizeof(const int64_t);
  lda_array = (int64_t *)malloc(lda_array_size);
  if (lda_array_size != 0 && lda_array == nullptr)
    goto ERROR_0;
  if ((lda_array_size != 0 && rpc_read(conn, lda_array, lda_array_size) < 0) ||
      rpc_read(conn, &Barray, sizeof(const float *const *)) < 0 || false)
    goto ERROR_0;
  ldb_array_size = group_count * sizeof(const int64_t);
  ldb_array = (int64_t *)malloc(ldb_array_size);
  if (ldb_array_size != 0 && ldb_array == nullptr)
    goto ERROR_0;
  if ((ldb_array_size != 0 && rpc_read(conn, ldb_array, ldb_array_size) < 0) ||
      rpc_read(conn, &beta_array_width, sizeof(beta_array_width)) < 0 ||
      (beta_array_width == 0 &&
       rpc_read(conn, &beta_array, sizeof(beta_array)) < 0) ||
      (beta_array_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_array_heap, beta_array_value,
                                   sizeof(beta_array_value), beta_array_width),
                beta_array_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(float *const *)) < 0 || false)
    goto ERROR_0;
  ldc_array_size = group_count * sizeof(const int64_t);
  ldc_array = (int64_t *)malloc(ldc_array_size);
  if (ldc_array_size != 0 && ldc_array == nullptr)
    goto ERROR_0;
  if ((ldc_array_size != 0 && rpc_read(conn, ldc_array, ldc_array_size) < 0) ||
      false)
    goto ERROR_0;
  group_size_size = group_count * sizeof(const int64_t);
  group_size = (int64_t *)malloc(group_size_size);
  if (group_size_size != 0 && group_size == nullptr)
    goto ERROR_0;
  if ((group_size_size != 0 &&
       rpc_read(conn, group_size, group_size_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmGroupedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transa_array),
               (group_count * sizeof(const cublasOperation_t) == 0
                    ? nullptr
                    : transb_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : m_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : n_array),
               (group_count * sizeof(const int64_t) == 0 ? nullptr : k_array),
               alpha_array_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         alpha_array_heap, alpha_array_value,
                         sizeof(alpha_array_value), alpha_array_width))
                   : alpha_array,
               Aarray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : lda_array),
               Barray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldb_array),
               beta_array_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_array_heap, beta_array_value,
                         sizeof(beta_array_value), beta_array_width))
                   : beta_array,
               Carray,
               (group_count * sizeof(const int64_t) == 0 ? nullptr : ldc_array),
               group_count,
               (group_count * sizeof(const int64_t) == 0 ? nullptr
                                                         : group_size));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return 0;
ERROR_0:
  free((void *)group_size);
  free((void *)ldc_array);
  free((void *)ldb_array);
  free((void *)lda_array);
  free((void *)k_array);
  free((void *)n_array);
  free((void *)m_array);
  free((void *)transb_array);
  free((void *)transa_array);
  return -1;
}

#endif

int handle_cublasSgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  long long int strideA;
  const float *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const float *, const float *, int, long long int, const float *, int,
      long long int, const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  long long int strideA;
  const float *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const float *, int64_t, long long int,
      const float *, int64_t, long long int, const float *, float *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemmStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, int, const float *, const float *, int,
                         const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *Aarray;
  int lda;
  const float *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const float *, const float *const *, int,
                                  const float *const *, int, const float *,
                                  float *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *Aarray;
  int64_t lda;
  const float *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const float *const *, int64_t, const float *const *, int64_t,
      const float *, float *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  long long int strideA;
  const float *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *, const float *,
      int, long long int, const float *, int, long long int, const float *,
      float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  long long int strideA;
  const float *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const float *, int64_t, long long int, const float *, int64_t,
      long long int, const float *, float *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *, const float *,
      int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t, int64_t,
                         const float *, const float *, int64_t, const float *,
                         int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgeqrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  float *const *Aarray;
  int lda;
  float *const *TauArray;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, int, float *const *, int,
                                  float *const *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TauArray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgeqrfBatched));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n, Aarray, lda, TauArray, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSger_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, int, const float *, const float *,
                         int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSger_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSger_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, int64_t,
                                  const float *, const float *, int64_t,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSger_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float *const *A;
  int lda;
  int *P;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, float *const *, int,
                                  int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgetrfBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, P, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgetriBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *const *A;
  int lda;
  const int *P;
  float *const *C;
  int ldc;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *const *, int,
                         const int *, float *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(const int *)) < 0 ||
      rpc_read(conn, &C, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgetriBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, P, C, ldc, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSgetrsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const float *const *Aarray;
  int lda;
  const int *devIpiv;
  float *const *Barray;
  int ldb;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const float *const *, int, const int *,
                                  float *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &Barray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSgetrsBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, Aarray, lda,
                                    devIpiv, Barray, ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSmatinvBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *const *A;
  int lda;
  float *const *Ainv;
  int lda_inv;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const float *const *,
                                  int, float *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Ainv, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSmatinvBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, Ainv, lda_inv, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSnrm2_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const float *x;
  int incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSnrm2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSnrm2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const float *x;
  int64_t incx;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  float *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSnrm2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx,
               result_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float *x;
  int incx;
  float *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const float *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, float *, int, float *,
                                  int, const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float *x;
  int64_t incx;
  float *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const float *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, float *, int64_t, float *,
                         int64_t, const float *, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t a_width;
  alignas(16) unsigned char a_value[64];
  std::vector<unsigned char> a_heap;
  float *a = nullptr;
  uint32_t b_width;
  alignas(16) unsigned char b_value[64];
  std::vector<unsigned char> b_heap;
  float *b = nullptr;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  float *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, float *, float *, float *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a_width, sizeof(a_width)) < 0 ||
      (a_width == 0 && rpc_read(conn, &a, sizeof(a)) < 0) ||
      (a_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                a_width) < 0) ||
      rpc_read(conn, &b_width, sizeof(b_width)) < 0 ||
      (b_width == 0 && rpc_read(conn, &b, sizeof(b)) < 0) ||
      (b_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                b_width) < 0) ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrotg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               a_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                  a_heap, a_value, sizeof(a_value), a_width))
                            : a,
               b_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                  b_heap, b_value, sizeof(b_value), b_width))
                            : b,
               c_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                  s_heap, s_value, sizeof(s_value), s_width))
                            : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (a_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                 a_width) < 0) ||
      (b_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                 b_width) < 0) ||
      (c_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                 c_width) < 0) ||
      (s_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                 s_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSrotm_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float *x;
  int incx;
  float *y;
  int incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const float *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, float *, int, float *,
                                  int, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrotm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               param_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSrotm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float *x;
  int64_t incx;
  float *y;
  int64_t incy;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  const float *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, float *, int64_t,
                                  float *, int64_t, const float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) ||
      (param_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(param_heap, param_value, sizeof(param_value),
                                   param_width),
                param_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrotm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               param_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(param_heap, param_value,
                                            sizeof(param_value), param_width))
                   : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSrotmg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t d1_width;
  alignas(16) unsigned char d1_value[64];
  std::vector<unsigned char> d1_heap;
  float *d1 = nullptr;
  uint32_t d2_width;
  alignas(16) unsigned char d2_value[64];
  std::vector<unsigned char> d2_heap;
  float *d2 = nullptr;
  uint32_t x1_width;
  alignas(16) unsigned char x1_value[64];
  std::vector<unsigned char> x1_heap;
  float *x1 = nullptr;
  uint32_t y1_width;
  alignas(16) unsigned char y1_value[64];
  std::vector<unsigned char> y1_heap;
  const float *y1 = nullptr;
  uint32_t param_width;
  alignas(16) unsigned char param_value[64];
  std::vector<unsigned char> param_heap;
  float *param = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, float *, float *, float *,
                                  const float *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &d1_width, sizeof(d1_width)) < 0 ||
      (d1_width == 0 && rpc_read(conn, &d1, sizeof(d1)) < 0) ||
      (d1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d1_heap, d1_value,
                                                    sizeof(d1_value), d1_width),
                                 d1_width) < 0) ||
      rpc_read(conn, &d2_width, sizeof(d2_width)) < 0 ||
      (d2_width == 0 && rpc_read(conn, &d2, sizeof(d2)) < 0) ||
      (d2_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(d2_heap, d2_value,
                                                    sizeof(d2_value), d2_width),
                                 d2_width) < 0) ||
      rpc_read(conn, &x1_width, sizeof(x1_width)) < 0 ||
      (x1_width == 0 && rpc_read(conn, &x1, sizeof(x1)) < 0) ||
      (x1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(x1_heap, x1_value,
                                                    sizeof(x1_value), x1_width),
                                 x1_width) < 0) ||
      rpc_read(conn, &y1_width, sizeof(y1_width)) < 0 ||
      (y1_width == 0 && rpc_read(conn, &y1, sizeof(y1)) < 0) ||
      (y1_width != 0 && rpc_read(conn,
                                 lupine_scalar_slot(y1_heap, y1_value,
                                                    sizeof(y1_value), y1_width),
                                 y1_width) < 0) ||
      rpc_read(conn, &param_width, sizeof(param_width)) < 0 ||
      (param_width == 0 && rpc_read(conn, &param, sizeof(param)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSrotmg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               d1_width != 0
                   ? reinterpret_cast<float *>(lupine_scalar_slot(
                         d1_heap, d1_value, sizeof(d1_value), d1_width))
                   : d1,
               d2_width != 0
                   ? reinterpret_cast<float *>(lupine_scalar_slot(
                         d2_heap, d2_value, sizeof(d2_value), d2_width))
                   : d2,
               x1_width != 0
                   ? reinterpret_cast<float *>(lupine_scalar_slot(
                         x1_heap, x1_value, sizeof(x1_value), x1_width))
                   : x1,
               y1_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         y1_heap, y1_value, sizeof(y1_value), y1_width))
                   : y1,
               param_width != 0 ? reinterpret_cast<float *>(lupine_scalar_slot(
                                      param_heap, param_value,
                                      sizeof(param_value), param_width))
                                : param);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (d1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d1_heap, d1_value, sizeof(d1_value), d1_width),
           d1_width) < 0) ||
      (d2_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(d2_heap, d2_value, sizeof(d2_value), d2_width),
           d2_width) < 0) ||
      (x1_width != 0 &&
       rpc_write(
           conn,
           lupine_scalar_slot(x1_heap, x1_value, sizeof(x1_value), x1_width),
           x1_width) < 0) ||
      (param_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(param_heap, param_value,
                                    sizeof(param_value), param_width),
                 param_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSsbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, int, const float *, const float *,
      int, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t, int64_t,
                         const float *, const float *, int64_t, const float *,
                         int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const float *,
                                  float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSspmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *AP;
  const float *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const float *, const float *, const float *,
                                  int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSspmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *AP;
  const float *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const float *, const float *,
                                  int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSspr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const float *,
                         const float *, int, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSspr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const float *, int64_t,
                                  const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSspr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  float *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const float *, const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSspr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  float *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                         const float *, const float *, int64_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSspr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  float *x;
  int incx;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSswap_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  float *x;
  int64_t incx;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, float *, int64_t,
                                  float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSswap_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         int, int, const float *, const float *, int,
                         const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsymm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsymm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const float *, const float *, int,
      const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsymv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const float *, const float *,
      int64_t, const float *, int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsymv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  const float *y;
  int incy;
  float *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const float *,
                         const float *, int, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  const float *y;
  int64_t incy;
  float *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const float *, int64_t,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const float *, const float *, int,
                         const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int incx;
  float *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const float *,
                         const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *x;
  int64_t incx;
  float *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const float *, const float *, int64_t,
                                  float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const float *, const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyrk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyrk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasSsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const float *, const float *, int,
                         const float *, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyrkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const float *, const float *, int64_t, const float *, int64_t,
      const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSsyrkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const float *A;
  int lda;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStbmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const float *A;
  int64_t lda;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStbmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const float *A;
  int lda;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int, int,
                                  const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStbsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const float *A;
  int64_t lda;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, int64_t, const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStbsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *AP;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         cublasDiagType_t, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStpmv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *AP;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStpmv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *AP;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         cublasDiagType_t, int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStpsv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *AP;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const float *, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStpsv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *AP;
  float *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const float *)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStpttr));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, AP, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasStrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  float *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const float *, const float *, int,
      const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrmm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  const float *B;
  int64_t ldb;
  float *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *, int64_t,
      const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrmm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *A;
  int lda;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *A;
  int64_t lda;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStrsmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *A;
  int lda;
  float *const *B;
  int ldb;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const float *, const float *const *, int,
      float *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStrsmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *const *A;
  int64_t lda;
  float *const *B;
  int64_t ldb;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *const *,
      int64_t, float *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  float *B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         cublasOperation_t, cublasDiagType_t, int, int,
                         const float *, const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int64_t lda;
  float *B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const float *, const float *, int64_t,
      float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const float *A;
  int lda;
  float *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int,
                                  const float *, int, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasStrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const float *A;
  int64_t lda;
  float *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  const float *, int64_t, float *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasStrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  float *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                                  const float *, int, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasStrttp));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, A, lda, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasSwapEx(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  void *x;
  cudaDataType xType;
  int incx;
  void *y;
  cudaDataType yType;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, void *, cudaDataType,
                                  int, void *, cudaDataType, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSwapEx));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasSwapEx_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  void *x;
  cudaDataType xType;
  int64_t incx;
  void *y;
  cudaDataType yType;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, void *, cudaDataType,
                                  int64_t, void *, cudaDataType, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(void *)) < 0 ||
      rpc_read(conn, &xType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasSwapEx_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, x, xType, incx, y, yType, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasTSSgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *const *Aarray;
  int lda;
  const __nv_bfloat16 *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                                  const float *, const __nv_bfloat16 *const *,
                                  int, const __nv_bfloat16 *const *, int,
                                  const float *, float *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSSgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasTSSgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *const *Aarray;
  int64_t lda;
  const __nv_bfloat16 *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *const *, int64_t, const __nv_bfloat16 *const *,
      int64_t, const float *, float *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(float *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSSgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasTSSgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *A;
  int lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *, int, long long int, const __nv_bfloat16 *, int,
      long long int, const float *, float *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSSgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasTSSgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *A;
  int64_t lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t,
                                  int64_t, const float *, const __nv_bfloat16 *,
                                  int64_t, long long int, const __nv_bfloat16 *,
                                  int64_t, long long int, const float *,
                                  float *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSSgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasTSTgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *const *Aarray;
  int lda;
  const __nv_bfloat16 *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __nv_bfloat16 *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *const *, int, const __nv_bfloat16 *const *, int,
      const float *, __nv_bfloat16 *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(__nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSTgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasTSTgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *const *Aarray;
  int64_t lda;
  const __nv_bfloat16 *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __nv_bfloat16 *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *const *, int64_t, const __nv_bfloat16 *const *,
      int64_t, const float *, __nv_bfloat16 *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const __nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(__nv_bfloat16 *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSTgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasTSTgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *A;
  int lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __nv_bfloat16 *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const float *,
      const __nv_bfloat16 *, int, long long int, const __nv_bfloat16 *, int,
      long long int, const float *, __nv_bfloat16 *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(__nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSTgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasTSTgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const __nv_bfloat16 *A;
  int64_t lda;
  long long int strideA;
  const __nv_bfloat16 *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  __nv_bfloat16 *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, const float *,
      const __nv_bfloat16 *, int64_t, long long int, const __nv_bfloat16 *,
      int64_t, long long int, const float *, __nv_bfloat16 *, int64_t,
      long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const __nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(__nv_bfloat16 *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasTSTgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasUint8gemmBias(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  cublasOperation_t transc;
  int m;
  int n;
  int k;
  const unsigned char *A;
  int A_bias;
  int lda;
  const unsigned char *B;
  int B_bias;
  int ldb;
  unsigned char *C;
  int C_bias;
  int ldc;
  int C_mult;
  int C_shift;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, cublasOperation_t,
      int, int, int, const unsigned char *, int, int, const unsigned char *,
      int, int, unsigned char *, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transc, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const unsigned char *)) < 0 ||
      rpc_read(conn, &A_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const unsigned char *)) < 0 ||
      rpc_read(conn, &B_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(unsigned char *)) < 0 ||
      rpc_read(conn, &C_bias, sizeof(int)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &C_mult, sizeof(int)) < 0 ||
      rpc_read(conn, &C_shift, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasUint8gemmBias));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, transa, transb, transc, m, n, k, A, A_bias,
                          lda, B, B_bias, ldb, C, C_bias, ldc, C_mult, C_shift);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZaxpy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZaxpy_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZaxpy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, const cuDoubleComplex *,
      int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZaxpy_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZcopy_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZcopy_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZcopy_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const cuDoubleComplex *,
                         int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZcopy_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZdgmm(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int m;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, int, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdgmm));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZdgmm_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t mode;
  int64_t m;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, int64_t,
                                  int64_t, const cuDoubleComplex *, int64_t,
                                  const cuDoubleComplex *, int64_t,
                                  cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdgmm_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, m, n, A, lda, x, incx, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZdotc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuDoubleComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdotc_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(
                         lupine_scalar_slot(result_heap, result_value,
                                            sizeof(result_value), result_width))
                   : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZdotc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuDoubleComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdotc_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(
                         lupine_scalar_slot(result_heap, result_value,
                                            sizeof(result_value), result_width))
                   : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZdotu_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuDoubleComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdotu_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(
                         lupine_scalar_slot(result_heap, result_value,
                                            sizeof(result_value), result_width))
                   : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZdotu_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  cuDoubleComplex *result = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdotu_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               result_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(
                         lupine_scalar_slot(result_heap, result_value,
                                            sizeof(result_value), result_width))
                   : result);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZdrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const double *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuDoubleComplex *, int,
                                  cuDoubleComplex *, int, const double *,
                                  const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZdrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const double *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, cuDoubleComplex *,
                                  int64_t, cuDoubleComplex *, int64_t,
                                  const double *, const double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZdscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const double *,
                                  cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZdscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, const double *,
                                  cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZdscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int kl;
  int ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int, int, int,
                         const cuDoubleComplex *, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kl, sizeof(int)) < 0 ||
      rpc_read(conn, &ku, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  int64_t kl;
  int64_t ku;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &kl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ku, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n, kl, ku,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgeam(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  const cuDoubleComplex *B;
  int ldb;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgeam));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgeam_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  const cuDoubleComplex *B;
  int64_t ldb;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgeam_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgelsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  int nrhs;
  cuDoubleComplex *const *Aarray;
  int lda;
  cuDoubleComplex *const *Carray;
  int ldc;
  int info;
  info = {};
  int *devInfoArray;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int, int,
                         cuDoubleComplex *const *, int,
                         cuDoubleComplex *const *, int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Carray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgelsBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, trans, m, n, nrhs, Aarray, lda, Carray, ldc,
                          &info, devInfoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgemm3m(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemm3m));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemm3m_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemm3m_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *Aarray;
  int lda;
  const cuDoubleComplex *const *Barray;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *const *Carray;
  int ldc;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *const *, int,
      const cuDoubleComplex *const *, int, const cuDoubleComplex *,
      cuDoubleComplex *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *Aarray;
  int64_t lda;
  const cuDoubleComplex *const *Barray;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *const *Carray;
  int64_t ldc;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *const *, int64_t,
      const cuDoubleComplex *const *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &Barray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &Carray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, Barray, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               Carray, ldc, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemmStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  long long int strideA;
  const cuDoubleComplex *B;
  int ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  long long int strideC;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int, long long int,
      const cuDoubleComplex *, int, long long int, const cuDoubleComplex *,
      cuDoubleComplex *, int, long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemmStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemmStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  long long int strideA;
  const cuDoubleComplex *B;
  int64_t ldb;
  long long int strideB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  long long int strideC;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      long long int, const cuDoubleComplex *, int64_t, long long int,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t, long long int,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideB, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideC, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemmStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, B, ldb, strideB,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc, strideC, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, cublasOperation_t,
                         int, int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t transa;
  cublasOperation_t transb;
  int64_t m;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, cublasOperation_t, int64_t, int64_t,
      int64_t, const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &transa, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &transb, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transa, transb, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemvBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *Aarray;
  int lda;
  const cuDoubleComplex *const *xarray;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *const *yarray;
  int incy;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *const *, int, const cuDoubleComplex *const *, int,
      const cuDoubleComplex *, cuDoubleComplex *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemvBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemvBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *Aarray;
  int64_t lda;
  const cuDoubleComplex *const *xarray;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *const *yarray;
  int64_t incy;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *const *, int64_t,
      const cuDoubleComplex *const *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *const *, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &Aarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &xarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yarray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemvBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               Aarray, lda, xarray, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               yarray, incy, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemvStridedBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  long long int strideA;
  const cuDoubleComplex *x;
  int incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  long long int stridey;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, long long int, const cuDoubleComplex *, int,
      long long int, const cuDoubleComplex *, cuDoubleComplex *, int,
      long long int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemvStridedBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemvStridedBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  long long int strideA;
  const cuDoubleComplex *x;
  int64_t incx;
  long long int stridex;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  long long int stridey;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t, long long int,
      const cuDoubleComplex *, int64_t, long long int, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t, long long int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridex, sizeof(long long int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &stridey, sizeof(long long int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemvStridedBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, strideA, x, incx, stridex,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy, stridey, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int64_t, int64_t,
                         const cuDoubleComplex *, const cuDoubleComplex *,
                         int64_t, const cuDoubleComplex *, int64_t,
                         const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, trans, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgeqrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *const *Aarray;
  int lda;
  cuDoubleComplex *const *TauArray;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, int, cuDoubleComplex *const *,
                         int, cuDoubleComplex *const *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TauArray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgeqrfBatched));
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n, Aarray, lda, TauArray, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgerc_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgerc_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgerc_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgerc_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgeru_v2(conn_t *conn) {
  cublasHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgeru_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZgeru_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgeru_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZgetrfBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex *const *A;
  int lda;
  int *P;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuDoubleComplex *const *,
                                  int, int *, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgetrfBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, P, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgetriBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *const *A;
  int lda;
  const int *P;
  cuDoubleComplex *const *C;
  int ldc;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, int, const cuDoubleComplex *const *, int, const int *,
      cuDoubleComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &P, sizeof(const int *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgetriBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, P, C, ldc, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZgetrsBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuDoubleComplex *const *Aarray;
  int lda;
  const int *devIpiv;
  cuDoubleComplex *const *Barray;
  int ldb;
  int info;
  info = {};
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasOperation_t, int, int,
                         const cuDoubleComplex *const *, int, const int *,
                         cuDoubleComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &Barray, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZgetrsBatched));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, Aarray, lda,
                                    devIpiv, Barray, ldb, &info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZhbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhbmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t, int64_t,
                         const cuDoubleComplex *, const cuDoubleComplex *,
                         int64_t, const cuDoubleComplex *, int64_t,
                         const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhbmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZhemm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhemm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhemm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhemm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZhemv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhemv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhemv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhemv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZher2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                         const cuDoubleComplex *, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZher2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZher2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZher2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const double *, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZher_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const double *,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZher_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const double *, const cuDoubleComplex *,
                                  int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZher_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZherk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const double *, const cuDoubleComplex *, int,
                         const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZherk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZherk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const double *, const cuDoubleComplex *, int64_t, const double *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZherk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZherkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZherkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZherkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const double *, cuDoubleComplex *,
      int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZherkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZhpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *AP;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpmv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *AP;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpmv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               AP, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZhpr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                         const cuDoubleComplex *, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhpr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZhpr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int, const double *,
                         const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZhpr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int64_t,
                                  const double *, const cuDoubleComplex *,
                                  int64_t, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZhpr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZmatinvBatched(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  const cuDoubleComplex *const *A;
  int lda;
  cuDoubleComplex *const *Ainv;
  int lda_inv;
  int *info;
  int batchSize;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *const *,
                         int, cuDoubleComplex *const *, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Ainv, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda_inv, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZmatinvBatched));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, A, lda, Ainv, lda_inv, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZrot_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *y;
  int incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const cuDoubleComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuDoubleComplex *, int,
                                  cuDoubleComplex *, int, const double *,
                                  const cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZrot_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0 ? reinterpret_cast<const cuDoubleComplex *>(
                                  lupine_scalar_slot(s_heap, s_value,
                                                     sizeof(s_value), s_width))
                            : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZrot_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *y;
  int64_t incy;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const cuDoubleComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, cuDoubleComplex *,
                                  int64_t, cuDoubleComplex *, int64_t,
                                  const double *, const cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZrot_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, x, incx, y, incy,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0 ? reinterpret_cast<const cuDoubleComplex *>(
                                  lupine_scalar_slot(s_heap, s_value,
                                                     sizeof(s_value), s_width))
                            : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZrotg_v2(conn_t *conn) {
  cublasHandle_t handle;
  uint32_t a_width;
  alignas(16) unsigned char a_value[64];
  std::vector<unsigned char> a_heap;
  cuDoubleComplex *a = nullptr;
  uint32_t b_width;
  alignas(16) unsigned char b_value[64];
  std::vector<unsigned char> b_heap;
  cuDoubleComplex *b = nullptr;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  cuDoubleComplex *s = nullptr;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cuDoubleComplex *, cuDoubleComplex *,
                         double *, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &a_width, sizeof(a_width)) < 0 ||
      (a_width == 0 && rpc_read(conn, &a, sizeof(a)) < 0) ||
      (a_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                a_width) < 0) ||
      rpc_read(conn, &b_width, sizeof(b_width)) < 0 ||
      (b_width == 0 && rpc_read(conn, &b, sizeof(b)) < 0) ||
      (b_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                b_width) < 0) ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZrotg_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               a_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(lupine_scalar_slot(
                         a_heap, a_value, sizeof(a_value), a_width))
                   : a,
               b_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(lupine_scalar_slot(
                         b_heap, b_value, sizeof(b_value), b_width))
                   : b,
               c_width != 0 ? reinterpret_cast<double *>(lupine_scalar_slot(
                                  c_heap, c_value, sizeof(c_value), c_width))
                            : c,
               s_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (a_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(a_heap, a_value, sizeof(a_value), a_width),
                 a_width) < 0) ||
      (b_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(b_heap, b_value, sizeof(b_value), b_width),
                 b_width) < 0) ||
      (c_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                 c_width) < 0) ||
      (s_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                 s_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZscal_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, const cuDoubleComplex *,
                                  cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZscal_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZscal_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, int64_t, const cuDoubleComplex *,
                         cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZscal_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZswap_v2(conn_t *conn) {
  cublasHandle_t handle;
  int n;
  cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int, cuDoubleComplex *, int,
                                  cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZswap_v2));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZswap_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  int64_t n;
  cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, int64_t, cuDoubleComplex *,
                                  int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZswap_v2_64));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, x, incx, y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsymm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsymm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsymm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsymm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsymv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *x;
  int incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsymv_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsymv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *x;
  int64_t incx;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int64_t incy;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsymv_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, x, incx,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, incy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsyr2_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  const cuDoubleComplex *y;
  int incy;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                         const cuDoubleComplex *, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr2_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsyr2_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  const cuDoubleComplex *y;
  int64_t incy;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incy, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr2_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, y, incy, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsyr2k_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr2k_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsyr2k_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr2k_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsyr_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int incx;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsyr_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *x;
  int64_t incx;
  cuDoubleComplex *A;
  int64_t lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyr_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               x, incx, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsyrk_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int, int,
      const cuDoubleComplex *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyrk_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsyrk_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyrk_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZsyrkx(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, cublasOperation_t,
                         int, int, const cuDoubleComplex *,
                         const cuDoubleComplex *, int, const cuDoubleComplex *,
                         int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyrkx));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZsyrkx_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int64_t n;
  int64_t k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, int64_t, int64_t,
      const cuDoubleComplex *, const cuDoubleComplex *, int64_t,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZsyrkx_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, trans, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtbmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtbmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtbmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  int64_t, const cuDoubleComplex *, int64_t,
                                  cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtbmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtbsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtbsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtbsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  int64_t k;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t,
                                  cublasOperation_t, cublasDiagType_t, int64_t,
                                  int64_t, const cuDoubleComplex *, int64_t,
                                  cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtbsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, k, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtpmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *AP;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtpmv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtpmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *AP;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtpmv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtpsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *AP;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtpsv_v2));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtpsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *AP;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtpsv_v2_64));
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, trans, diag, n, AP, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtpttr(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *AP;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                         const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtpttr));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, AP, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cublasZtrmm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasSideMode_t, cublasFillMode_t,
                         cublasOperation_t, cublasDiagType_t, int, int,
                         const cuDoubleComplex *, const cuDoubleComplex *, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrmm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtrmm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  const cuDoubleComplex *B;
  int64_t ldb;
  cuDoubleComplex *C;
  int64_t ldc;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, const cuDoubleComplex *, int64_t,
      cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrmm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtrmv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrmv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtrmv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrmv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtrsmBatched(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *A;
  int lda;
  cuDoubleComplex *const *B;
  int ldb;
  int batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *const *, int, cuDoubleComplex *const *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsmBatched));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtrsmBatched_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *const *A;
  int64_t lda;
  cuDoubleComplex *const *B;
  int64_t ldb;
  int64_t batchCount;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *const *, int64_t, cuDoubleComplex *const *,
      int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *const *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsmBatched_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtrsm_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *B;
  int ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsm_v2));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtrsm_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t m;
  int64_t n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *B;
  int64_t ldb;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      cublasDiagType_t, int64_t, int64_t, const cuDoubleComplex *,
      const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsm_v2_64));
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, side, uplo, trans, diag, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, B, ldb);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtrsv_v2(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *x;
  int incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int, const cuDoubleComplex *, int, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsv_v2));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUBLAS_VERSION >= 120000
int handle_cublasZtrsv_v2_64(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  cublasDiagType_t diag;
  int64_t n;
  const cuDoubleComplex *A;
  int64_t lda;
  cuDoubleComplex *x;
  int64_t incx;
  int request_id;
  cublasStatus_t return_value;
  using fn_t = cublasStatus_t (*)(
      cublasHandle_t, cublasFillMode_t, cublasOperation_t, cublasDiagType_t,
      int64_t, const cuDoubleComplex *, int64_t, cuDoubleComplex *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrsv_v2_64));
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, trans, diag, n, A, lda, x, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cublasZtrttp(conn_t *conn) {
  cublasHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *AP;
  int request_id;
  cublasStatus_t return_value;
  using fn_t =
      cublasStatus_t (*)(cublasHandle_t, cublasFillMode_t, int,
                         const cuDoubleComplex *, int, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cublasHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &AP, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cublas_symbol<fn_t>(LUPINE_SYMBOL_NAME(cublasZtrttp));
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, uplo, n, A, lda, AP);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cublasStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
