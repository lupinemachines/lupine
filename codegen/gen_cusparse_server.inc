// Generated by codegen.py. Do not edit by hand.

// A pointer-mode scalar's value lands in the handler's inline slot,
// or on the heap when it is wider.
static unsigned char *lupine_scalar_slot(std::vector<unsigned char> &heap,
                                         unsigned char *slot, size_t size,
                                         uint32_t width) {
  if (width <= size) {
    return slot;
  }
  heap.resize(width);
  return heap.data();
}

int handle_cusparseCreate(conn_t *conn) {
  cusparseHandle_t handle{};
  handle = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroy(conn_t *conn) {
  cusparseHandle_t handle;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetVersion(conn_t *conn) {
  cusparseHandle_t handle;
  int version{};
  version = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  value = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetStream(conn_t *conn) {
  cusparseHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetStream(conn_t *conn) {
  cusparseHandle_t handle;
  cudaStream_t streamId{};
  streamId = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetPointerMode(conn_t *conn) {
  cusparseHandle_t handle;
  cusparsePointerMode_t mode{};
  mode = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparsePointerMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetPointerMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cusparsePointerMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetPointerMode(conn_t *conn) {
  cusparseHandle_t handle;
  cusparsePointerMode_t mode;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparsePointerMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cusparsePointerMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetPointerMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLoggerOpenFile(conn_t *conn) {
  const char *logFile = nullptr;
  std::size_t logFile_len;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &logFile_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  logFile = (const char *)malloc(logFile_len);
  if ((logFile_len != 0 && logFile == nullptr) ||
      rpc_read(conn, (void *)logFile, logFile_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseLoggerOpenFile");
  return_value = fn == nullptr ? function_not_found() : fn(logFile);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)logFile);
  return 0;
ERROR_0:
  free((void *)logFile);
  return -1;
}

int handle_cusparseLoggerSetLevel(conn_t *conn) {
  int level;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &level, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseLoggerSetLevel");
  return_value = fn == nullptr ? function_not_found() : fn(level);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLoggerSetMask(conn_t *conn) {
  int mask;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mask, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseLoggerSetMask");
  return_value = fn == nullptr ? function_not_found() : fn(mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLoggerForceDisable(conn_t *conn) {
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseLoggerForceDisable");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateMatDescr(conn_t *conn) {
  cusparseMatDescr_t descrA{};
  descrA = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateMatDescr");
  return_value = fn == nullptr ? function_not_found() : fn(&descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyMatDescr(conn_t *conn) {
  cusparseMatDescr_t descrA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyMatDescr");
  return_value = fn == nullptr ? function_not_found() : fn(descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetMatType(conn_t *conn) {
  cusparseMatDescr_t descrA;
  cusparseMatrixType_t type;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t, cusparseMatrixType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &type, sizeof(cusparseMatrixType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetMatType");
  return_value = fn == nullptr ? function_not_found() : fn(descrA, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetMatType(conn_t *conn) {
  cusparseMatDescr_t descrA;
  int request_id;
  cusparseMatrixType_t return_value;
  using fn_t = cusparseMatrixType_t (*)(const cusparseMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetMatType");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseMatrixType_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetMatFillMode(conn_t *conn) {
  cusparseMatDescr_t descrA;
  cusparseFillMode_t fillMode;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t, cusparseFillMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &fillMode, sizeof(cusparseFillMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetMatFillMode");
  return_value = fn == nullptr ? function_not_found() : fn(descrA, fillMode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetMatFillMode(conn_t *conn) {
  cusparseMatDescr_t descrA;
  int request_id;
  cusparseFillMode_t return_value;
  using fn_t = cusparseFillMode_t (*)(const cusparseMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetMatFillMode");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseFillMode_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetMatDiagType(conn_t *conn) {
  cusparseMatDescr_t descrA;
  cusparseDiagType_t diagType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t, cusparseDiagType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &diagType, sizeof(cusparseDiagType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetMatDiagType");
  return_value = fn == nullptr ? function_not_found() : fn(descrA, diagType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetMatDiagType(conn_t *conn) {
  cusparseMatDescr_t descrA;
  int request_id;
  cusparseDiagType_t return_value;
  using fn_t = cusparseDiagType_t (*)(const cusparseMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetMatDiagType");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseDiagType_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSetMatIndexBase(conn_t *conn) {
  cusparseMatDescr_t descrA;
  cusparseIndexBase_t base;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseMatDescr_t, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &base, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSetMatIndexBase");
  return_value = fn == nullptr ? function_not_found() : fn(descrA, base);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGetMatIndexBase(conn_t *conn) {
  cusparseMatDescr_t descrA;
  int request_id;
  cusparseIndexBase_t return_value;
  using fn_t = cusparseIndexBase_t (*)(const cusparseMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGetMatIndexBase");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(descrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateCsric02Info(conn_t *conn) {
  csric02Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csric02Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsric02Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyCsric02Info(conn_t *conn) {
  csric02Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csric02Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsric02Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateBsric02Info(conn_t *conn) {
  bsric02Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsric02Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBsric02Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyBsric02Info(conn_t *conn) {
  bsric02Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsric02Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyBsric02Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateCsrilu02Info(conn_t *conn) {
  csrilu02Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrilu02Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsrilu02Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyCsrilu02Info(conn_t *conn) {
  csrilu02Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrilu02Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsrilu02Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateBsrilu02Info(conn_t *conn) {
  bsrilu02Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrilu02Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBsrilu02Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyBsrilu02Info(conn_t *conn) {
  bsrilu02Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrilu02Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyBsrilu02Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateBsrsv2Info(conn_t *conn) {
  bsrsv2Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrsv2Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBsrsv2Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyBsrsv2Info(conn_t *conn) {
  bsrsv2Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrsv2Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyBsrsv2Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateBsrsm2Info(conn_t *conn) {
  bsrsm2Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrsm2Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBsrsm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyBsrsm2Info(conn_t *conn) {
  bsrsm2Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(bsrsm2Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyBsrsm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateCsru2csrInfo(conn_t *conn) {
  csru2csrInfo_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csru2csrInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsru2csrInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyCsru2csrInfo(conn_t *conn) {
  csru2csrInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csru2csrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsru2csrInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateColorInfo(conn_t *conn) {
  cusparseColorInfo_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseColorInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateColorInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(cusparseColorInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyColorInfo(conn_t *conn) {
  cusparseColorInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseColorInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(cusparseColorInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyColorInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreatePruneInfo(conn_t *conn) {
  pruneInfo_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(pruneInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreatePruneInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroyPruneInfo(conn_t *conn) {
  pruneInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(pruneInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyPruneInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgemvi(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  int nnz;
  const float *xVal;
  const int *xInd;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, int,
                                    int, const float *, const float *, int, int,
                                    const float *, const int *, const float *,
                                    float *, cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgemvi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, nnz, xVal, xInd,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgemvi_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  int pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, int,
                                    int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgemvi_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, n, nnz, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgemvi(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  int nnz;
  const double *xVal;
  const int *xInd;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const double *,
      const double *, int, int, const double *, const int *, const double *,
      double *, cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgemvi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, nnz, xVal, xInd,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgemvi_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  int pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, int,
                                    int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgemvi_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, n, nnz, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgemvi(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  int nnz;
  const cuComplex *xVal;
  const int *xInd;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cuComplex *,
      const cuComplex *, int, int, const cuComplex *, const int *,
      const cuComplex *, cuComplex *, cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgemvi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, nnz, xVal, xInd,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgemvi_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  int pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, int,
                                    int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgemvi_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, n, nnz, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgemvi(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  int nnz;
  const cuDoubleComplex *xVal;
  const int *xInd;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, int, const cuDoubleComplex *, const int *,
      const cuDoubleComplex *, cuDoubleComplex *, cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgemvi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, nnz, xVal, xInd,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgemvi_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  int pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, int,
                                    int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgemvi_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, n, nnz, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const float *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, int, const float *, const float *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const double *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      const double *, const cusparseMatDescr_t, const double *, const int *,
      const int *, int, const double *, const double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const cuComplex *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, int, const cuComplex *, const cuComplex *,
      cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const cuDoubleComplex *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int,
      const cuDoubleComplex *, const cuDoubleComplex *, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrxmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int sizeOfMask;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedMaskPtrA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedEndPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const float *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      int, const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, const int *, const int *, int, const float *, const float *,
      float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &sizeOfMask, sizeof(int)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedMaskPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedEndPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrxmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, sizeOfMask, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedMaskPtrA, bsrSortedRowPtrA,
               bsrSortedEndPtrA, bsrSortedColIndA, blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrxmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int sizeOfMask;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedMaskPtrA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedEndPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const double *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      int, const double *, const cusparseMatDescr_t, const double *,
      const int *, const int *, const int *, const int *, int, const double *,
      const double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &sizeOfMask, sizeof(int)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedMaskPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedEndPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrxmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, sizeOfMask, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedMaskPtrA, bsrSortedRowPtrA,
               bsrSortedEndPtrA, bsrSortedColIndA, blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrxmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int sizeOfMask;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedMaskPtrA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedEndPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const cuComplex *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      int, const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, const int *, const int *, int,
      const cuComplex *, const cuComplex *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &sizeOfMask, sizeof(int)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedMaskPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedEndPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrxmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, sizeOfMask, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedMaskPtrA, bsrSortedRowPtrA,
               bsrSortedEndPtrA, bsrSortedColIndA, blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrxmv(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int sizeOfMask;
  int mb;
  int nb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedMaskPtrA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedEndPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  const cuDoubleComplex *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *y;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int, int,
      int, const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, const int *,
      const int *, int, const cuDoubleComplex *, const cuDoubleComplex *,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &sizeOfMask, sizeof(int)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedMaskPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedEndPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrxmv");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, sizeOfMask, mb, nb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedMaskPtrA, bsrSortedRowPtrA,
               bsrSortedEndPtrA, bsrSortedColIndA, blockDim, x,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXbsrsv2_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  bsrsv2Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXbsrsv2_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, float *, const int *, const int *, int,
      bsrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsv2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, double *, const int *, const int *, int,
      bsrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsv2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, cuComplex *, const int *, const int *, int,
      bsrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsv2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, cuDoubleComplex *, const int *, const int *,
      int, bsrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsv2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, const float *, const int *, const int *, int,
      bsrsv2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsv2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, const double *, const int *, const int *, int,
      bsrsv2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsv2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, const cuComplex *, const int *, const int *,
      int, bsrsv2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsv2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, int, bsrsv2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsv2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, mb, nnzb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, info,
                          policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  const float *f;
  float *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, int, bsrsv2Info_t, const float *, float *,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, info, f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  const double *f;
  double *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const double *, const cusparseMatDescr_t, const double *, const int *,
      const int *, int, bsrsv2Info_t, const double *, double *,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, info, f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  const cuComplex *f;
  cuComplex *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, int, bsrsv2Info_t, const cuComplex *,
      cuComplex *, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, info, f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  int mb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  bsrsv2Info_t info;
  const cuDoubleComplex *f;
  cuDoubleComplex *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t, int, int,
      const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, bsrsv2Info_t,
      const cuDoubleComplex *, cuDoubleComplex *, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, mb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockDim, info, f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrmm(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int mb;
  int n;
  int kb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockSize;
  const float *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, int, const float *,
      const cusparseMatDescr_t, const float *, const int *, const int *,
      const int, const float *, const int, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(const int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(const int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrmm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transB, mb, n, kb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockSize, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrmm(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int mb;
  int n;
  int kb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockSize;
  const double *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, int, const double *,
      const cusparseMatDescr_t, const double *, const int *, const int *,
      const int, const double *, const int, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(const int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(const int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrmm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transB, mb, n, kb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockSize, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrmm(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int mb;
  int n;
  int kb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockSize;
  const cuComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, int, const cuComplex *,
      const cusparseMatDescr_t, const cuComplex *, const int *, const int *,
      const int, const cuComplex *, const int, const cuComplex *, cuComplex *,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(const int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(const int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrmm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transB, mb, n, kb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockSize, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrmm(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int mb;
  int n;
  int kb;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockSize;
  const cuDoubleComplex *B;
  int ldb;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, const int, const cuDoubleComplex *, const int,
      const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &kb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(const int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(const int)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrmm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transB, mb, n, kb, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedValA, bsrSortedRowPtrA, bsrSortedColIndA,
               blockSize, B, ldb,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXbsrsm2_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  bsrsm2Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXbsrsm2_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsm2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t, float *,
      const int *, const int *, int, bsrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsm2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsm2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t, double *,
      const int *, const int *, int, bsrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsm2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsm2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t, cuComplex *,
      const int *, const int *, int, bsrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsm2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsm2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsm2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, bsrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsm2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, bsrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsm2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int, bsrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsm2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, bsrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsm2_analysis");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, transA, transXY, mb, n, nnzb, descrA,
                          bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
                          blockSize, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  const float *B;
  int ldb;
  float *X;
  int ldx;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const float *,
      const cusparseMatDescr_t, const float *, const int *, const int *, int,
      bsrsm2Info_t, const float *, int, float *, int, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &X, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldx, sizeof(int)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transXY, mb, n, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
               blockSize, info, B, ldb, X, ldx, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  const double *B;
  int ldb;
  double *X;
  int ldx;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const double *,
      const cusparseMatDescr_t, const double *, const int *, const int *, int,
      bsrsm2Info_t, const double *, int, double *, int, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &X, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldx, sizeof(int)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transXY, mb, n, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
               blockSize, info, B, ldb, X, ldx, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  const cuComplex *B;
  int ldb;
  cuComplex *X;
  int ldx;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cuComplex *,
      const cusparseMatDescr_t, const cuComplex *, const int *, const int *,
      int, bsrsm2Info_t, const cuComplex *, int, cuComplex *, int,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &X, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldx, sizeof(int)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transXY, mb, n, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
               blockSize, info, B, ldb, X, ldx, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  cusparseOperation_t transA;
  cusparseOperation_t transXY;
  int mb;
  int n;
  int nnzb;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockSize;
  bsrsm2Info_t info;
  const cuDoubleComplex *B;
  int ldb;
  cuDoubleComplex *X;
  int ldx;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, cusparseOperation_t,
      cusparseOperation_t, int, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, int, bsrsm2Info_t, const cuDoubleComplex *, int,
      cuDoubleComplex *, int, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transXY, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrsm2Info_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &X, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldx, sizeof(int)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, transA, transXY, mb, n, nnzb,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, bsrSortedVal, bsrSortedRowPtr, bsrSortedColInd,
               blockSize, info, B, ldb, X, ldx, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  csrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  float *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrilu02Info_t, int,
                                    double *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<float *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  csrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  double *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrilu02Info_t, int,
                                    double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  csrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  cuComplex *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrilu02Info_t, int,
                                    double *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  csrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  cuDoubleComplex *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrilu02Info_t, int,
                                    double *, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsrilu02_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  csrilu02Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrilu02_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, float *,
      const int *, const int *, csrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrilu02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, double *,
      const int *, const int *, csrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrilu02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuComplex *,
      const int *, const int *, csrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrilu02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuDoubleComplex *,
      const int *, const int *, csrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrilu02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrilu02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrilu02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrilu02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, const int *, const int *,
                           csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrilu02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, float *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrilu02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, double *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrilu02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuComplex *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrilu02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuDoubleComplex *,
      const int *, const int *, csrilu02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrilu02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  bsrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  float *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrilu02Info_t, int,
                                    double *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<float *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  bsrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  double *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrilu02Info_t, int,
                                    double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  bsrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  cuComplex *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrilu02Info_t, int,
                                    double *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<cuComplex *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrilu02_numericBoost(conn_t *conn) {
  cusparseHandle_t handle;
  bsrilu02Info_t info;
  int enable_boost;
  uint32_t tol_width;
  alignas(16) unsigned char tol_value[64];
  std::vector<unsigned char> tol_heap;
  double *tol = nullptr;
  uint32_t boost_val_width;
  alignas(16) unsigned char boost_val_value[64];
  std::vector<unsigned char> boost_val_heap;
  cuDoubleComplex *boost_val = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrilu02Info_t, int,
                                    double *, cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &enable_boost, sizeof(int)) < 0 ||
      rpc_read(conn, &tol_width, sizeof(tol_width)) < 0 ||
      (tol_width == 0 && rpc_read(conn, &tol, sizeof(tol)) < 0) ||
      (tol_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(tol_heap, tol_value, sizeof(tol_value),
                                   tol_width),
                tol_width) < 0) ||
      rpc_read(conn, &boost_val_width, sizeof(boost_val_width)) < 0 ||
      (boost_val_width == 0 &&
       rpc_read(conn, &boost_val, sizeof(boost_val)) < 0) ||
      (boost_val_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(boost_val_heap, boost_val_value,
                                   sizeof(boost_val_value), boost_val_width),
                boost_val_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrilu02_numericBoost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, info, enable_boost,
               tol_width != 0
                   ? reinterpret_cast<double *>(lupine_scalar_slot(
                         tol_heap, tol_value, sizeof(tol_value), tol_width))
                   : tol,
               boost_val_width != 0
                   ? reinterpret_cast<cuDoubleComplex *>(lupine_scalar_slot(
                         boost_val_heap, boost_val_value,
                         sizeof(boost_val_value), boost_val_width))
                   : boost_val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXbsrilu02_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  bsrilu02Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXbsrilu02_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, int, bsrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrilu02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, int, bsrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrilu02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, int, bsrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrilu02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrilu02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsrilu02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrilu02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrilu02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrilu02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrilu02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrilu02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrilu02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsrilu02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsrilu02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsrilu02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsrilu02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsrilu02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsrilu02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsrilu02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsrilu02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsric02_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  csric02Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsric02_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, float *,
      const int *, const int *, csric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsric02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, double *,
      const int *, const int *, csric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsric02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuComplex *,
      const int *, const int *, csric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsric02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuDoubleComplex *,
      const int *, const int *, csric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsric02_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsric02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsric02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsric02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, const int *, const int *,
                           csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsric02_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsric02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, float *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsric02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsric02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, double *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsric02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsric02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuComplex *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsric02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsric02(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrSortedValA_valM;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, cuDoubleComplex *,
      const int *, const int *, csric02Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA_valM, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsric02");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrSortedValA_valM,
                                    csrSortedRowPtrA, csrSortedColIndA, info,
                                    policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXbsric02_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  bsric02Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, bsric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXbsric02_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, int, bsric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsric02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, int, bsric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsric02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, int, bsric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsric02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsric02_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsric02Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsric02_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pInputBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pInputBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsric02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pInputBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pInputBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pInputBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsric02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pInputBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pInputBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pInputBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsric02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pInputBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsric02_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pInputBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pInputBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsric02_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pInputBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsric02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsric02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsric02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsric02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsric02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsric02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsric02(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nnzb;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int blockDim;
  bsric02Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, int, bsric02Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(bsric02Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsric02");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nnzb, descrA, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, blockDim, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *dl;
  const float *d;
  const float *du;
  const float *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                                    const float *, const float *, const float *,
                                    int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *dl;
  const double *d;
  const double *du;
  const double *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    const double *, const double *,
                                    const double *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  const cuComplex *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, const cuComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  const cuDoubleComplex *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cuDoubleComplex *,
                           const cuDoubleComplex *, const cuDoubleComplex *,
                           const cuDoubleComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *dl;
  const float *d;
  const float *du;
  float *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                           const float *, const float *, float *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *dl;
  const double *d;
  const double *du;
  double *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    const double *, const double *, double *,
                                    int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  cuComplex *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, cuComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  cuDoubleComplex *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cuDoubleComplex *,
                           const cuDoubleComplex *, const cuDoubleComplex *,
                           cuDoubleComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2_nopivot_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *dl;
  const float *d;
  const float *du;
  const float *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                                    const float *, const float *, const float *,
                                    int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2_nopivot_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2_nopivot_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *dl;
  const double *d;
  const double *du;
  const double *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    const double *, const double *,
                                    const double *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2_nopivot_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2_nopivot_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  const cuComplex *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, const cuComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2_nopivot_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2_nopivot_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  const cuDoubleComplex *B;
  int ldb;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cuDoubleComplex *,
                           const cuDoubleComplex *, const cuDoubleComplex *,
                           const cuDoubleComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2_nopivot_bufferSizeExt");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, dl, d, du, B, ldb, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2_nopivot(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *dl;
  const float *d;
  const float *du;
  float *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                           const float *, const float *, float *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2_nopivot");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2_nopivot(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *dl;
  const double *d;
  const double *du;
  double *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    const double *, const double *, double *,
                                    int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2_nopivot");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2_nopivot(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  cuComplex *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, cuComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2_nopivot");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2_nopivot(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  cuDoubleComplex *B;
  int ldb;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cuDoubleComplex *,
                           const cuDoubleComplex *, const cuDoubleComplex *,
                           cuDoubleComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2_nopivot");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, dl, d, du, B, ldb, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2StridedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const float *dl;
  const float *d;
  const float *du;
  const float *x;
  int batchCount;
  int batchStride;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const float *, const float *,
                           const float *, const float *, int, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2StridedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2StridedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const double *dl;
  const double *d;
  const double *du;
  const double *x;
  int batchCount;
  int batchStride;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const double *,
                                    const double *, const double *,
                                    const double *, int, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2StridedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2StridedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  const cuComplex *x;
  int batchCount;
  int batchStride;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const cuComplex *,
                                    const cuComplex *, const cuComplex *,
                                    const cuComplex *, int, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2StridedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2StridedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  const cuDoubleComplex *x;
  int batchCount;
  int batchStride;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, const cuDoubleComplex *, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2StridedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsv2StridedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const float *dl;
  const float *d;
  const float *du;
  float *x;
  int batchCount;
  int batchStride;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const float *, const float *,
                           const float *, float *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsv2StridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsv2StridedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const double *dl;
  const double *d;
  const double *du;
  double *x;
  int batchCount;
  int batchStride;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const double *,
                                    const double *, const double *, double *,
                                    int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsv2StridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsv2StridedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  cuComplex *x;
  int batchCount;
  int batchStride;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const cuComplex *,
                                    const cuComplex *, const cuComplex *,
                                    cuComplex *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsv2StridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsv2StridedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  cuDoubleComplex *x;
  int batchCount;
  int batchStride;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, const cuDoubleComplex *, const cuDoubleComplex *,
      const cuDoubleComplex *, cuDoubleComplex *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsv2StridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, dl, d, du, x, batchCount,
                                    batchStride, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const float *dl;
  const float *d;
  const float *du;
  const float *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                                    const float *, const float *, const float *,
                                    int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, dl, d, du, x, batchCount,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const double *dl;
  const double *d;
  const double *du;
  const double *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    const double *, const double *,
                                    const double *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, dl, d, du, x, batchCount,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  const cuComplex *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, const cuComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, dl, d, du, x, batchCount,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  const cuDoubleComplex *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cuDoubleComplex *,
                           const cuDoubleComplex *, const cuDoubleComplex *,
                           const cuDoubleComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, dl, d, du, x, batchCount,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgtsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  float *dl;
  float *d;
  float *du;
  float *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, float *,
                                    float *, float *, float *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(float *)) < 0 ||
      rpc_read(conn, &d, sizeof(float *)) < 0 ||
      rpc_read(conn, &du, sizeof(float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgtsvInterleavedBatch");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, algo, m, dl, d, du, x, batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgtsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  double *dl;
  double *d;
  double *du;
  double *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, double *,
                                    double *, double *, double *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(double *)) < 0 ||
      rpc_read(conn, &d, sizeof(double *)) < 0 ||
      rpc_read(conn, &du, sizeof(double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgtsvInterleavedBatch");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, algo, m, dl, d, du, x, batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgtsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  cuComplex *dl;
  cuComplex *d;
  cuComplex *du;
  cuComplex *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, cuComplex *, cuComplex *,
                           cuComplex *, cuComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgtsvInterleavedBatch");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, algo, m, dl, d, du, x, batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgtsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  cuDoubleComplex *dl;
  cuDoubleComplex *d;
  cuDoubleComplex *du;
  cuDoubleComplex *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, cuDoubleComplex *, cuDoubleComplex *,
      cuDoubleComplex *, cuDoubleComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &dl, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgtsvInterleavedBatch");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, algo, m, dl, d, du, x, batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgpsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const float *ds;
  const float *dl;
  const float *d;
  const float *du;
  const float *dw;
  const float *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const float *, const float *, const float *,
      const float *, const float *, const float *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(const float *)) < 0 ||
      rpc_read(conn, &dl, sizeof(const float *)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &du, sizeof(const float *)) < 0 ||
      rpc_read(conn, &dw, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(const float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgpsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgpsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const double *ds;
  const double *dl;
  const double *d;
  const double *du;
  const double *dw;
  const double *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                           const double *, const double *, const double *,
                           const double *, const double *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(const double *)) < 0 ||
      rpc_read(conn, &dl, sizeof(const double *)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &du, sizeof(const double *)) < 0 ||
      rpc_read(conn, &dw, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(const double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgpsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgpsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const cuComplex *ds;
  const cuComplex *dl;
  const cuComplex *d;
  const cuComplex *du;
  const cuComplex *dw;
  const cuComplex *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cuComplex *,
      const cuComplex *, const cuComplex *, const cuComplex *,
      const cuComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &dw, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgpsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgpsvInterleavedBatch_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  const cuDoubleComplex *ds;
  const cuDoubleComplex *dl;
  const cuDoubleComplex *d;
  const cuDoubleComplex *du;
  const cuDoubleComplex *dw;
  const cuDoubleComplex *x;
  int batchCount;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, const cuDoubleComplex *,
      const cuDoubleComplex *, const cuDoubleComplex *, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &dl, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &dw, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgpsvInterleavedBatch_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgpsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  float *ds;
  float *dl;
  float *d;
  float *du;
  float *dw;
  float *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, float *, float *,
                           float *, float *, float *, float *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(float *)) < 0 ||
      rpc_read(conn, &dl, sizeof(float *)) < 0 ||
      rpc_read(conn, &d, sizeof(float *)) < 0 ||
      rpc_read(conn, &du, sizeof(float *)) < 0 ||
      rpc_read(conn, &dw, sizeof(float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgpsvInterleavedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgpsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  double *ds;
  double *dl;
  double *d;
  double *du;
  double *dw;
  double *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, double *, double *,
                           double *, double *, double *, double *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(double *)) < 0 ||
      rpc_read(conn, &dl, sizeof(double *)) < 0 ||
      rpc_read(conn, &d, sizeof(double *)) < 0 ||
      rpc_read(conn, &du, sizeof(double *)) < 0 ||
      rpc_read(conn, &dw, sizeof(double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgpsvInterleavedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgpsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  cuComplex *ds;
  cuComplex *dl;
  cuComplex *d;
  cuComplex *du;
  cuComplex *dw;
  cuComplex *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, cuComplex *,
                                    cuComplex *, cuComplex *, cuComplex *,
                                    cuComplex *, cuComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &dl, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &dw, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgpsvInterleavedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgpsvInterleavedBatch(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  int m;
  cuDoubleComplex *ds;
  cuDoubleComplex *dl;
  cuDoubleComplex *d;
  cuDoubleComplex *du;
  cuDoubleComplex *dw;
  cuDoubleComplex *x;
  int batchCount;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, cuDoubleComplex *, cuDoubleComplex *,
      cuDoubleComplex *, cuDoubleComplex *, cuDoubleComplex *,
      cuDoubleComplex *, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &ds, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &dl, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &d, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &du, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &dw, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgpsvInterleavedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, m, ds, dl, d, du, dw, x,
                                    batchCount, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrgeam2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const float *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  const float *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const float *, const cusparseMatDescr_t, int,
      const float *, const int *, const int *, const float *,
      const cusparseMatDescr_t, int, const float *, const int *, const int *,
      const cusparseMatDescr_t, const float *, const int *, const int *,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrgeam2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrgeam2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const double *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  const double *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const double *, const cusparseMatDescr_t, int,
      const double *, const int *, const int *, const double *,
      const cusparseMatDescr_t, int, const double *, const int *, const int *,
      const cusparseMatDescr_t, const double *, const int *, const int *,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrgeam2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrgeam2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  const cuComplex *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cusparseMatDescr_t,
      int, const cuComplex *, const int *, const int *, const cuComplex *,
      const cusparseMatDescr_t, int, const cuComplex *, const int *,
      const int *, const cusparseMatDescr_t, const cuComplex *, const int *,
      const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrgeam2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrgeam2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuDoubleComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  const cuDoubleComplex *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, int, const cuDoubleComplex *, const int *,
      const int *, const cuDoubleComplex *, const cusparseMatDescr_t, int,
      const cuDoubleComplex *, const int *, const int *,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrgeam2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsrgeam2Nnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *workspace;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, int, const int *,
      const int *, const cusparseMatDescr_t, int, const int *, const int *,
      const cusparseMatDescr_t, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrgeam2Nnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, descrA, nnzA, csrSortedRowPtrA, csrSortedColIndA,
               descrB, nnzB, csrSortedRowPtrB, csrSortedColIndB, descrC,
               csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               workspace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrgeam2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const float *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const float *, const cusparseMatDescr_t, int,
      const float *, const int *, const int *, const float *,
      const cusparseMatDescr_t, int, const float *, const int *, const int *,
      const cusparseMatDescr_t, float *, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrgeam2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrgeam2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const double *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const double *, const cusparseMatDescr_t, int,
      const double *, const int *, const int *, const double *,
      const cusparseMatDescr_t, int, const double *, const int *, const int *,
      const cusparseMatDescr_t, double *, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrgeam2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrgeam2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  cuComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuComplex *, const cusparseMatDescr_t,
      int, const cuComplex *, const int *, const int *, const cuComplex *,
      const cusparseMatDescr_t, int, const cuComplex *, const int *,
      const int *, const cusparseMatDescr_t, cuComplex *, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrgeam2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrgeam2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuDoubleComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, int, const cuDoubleComplex *, const int *,
      const int *, const cuDoubleComplex *, const cusparseMatDescr_t, int,
      const cuDoubleComplex *, const int *, const int *,
      const cusparseMatDescr_t, cuDoubleComplex *, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrgeam2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsrcolor(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t fractionToColor_width;
  alignas(16) unsigned char fractionToColor_value[64];
  std::vector<unsigned char> fractionToColor_heap;
  const float *fractionToColor = nullptr;
  uint32_t ncolors_width;
  alignas(16) unsigned char ncolors_value[64];
  std::vector<unsigned char> ncolors_heap;
  int *ncolors = nullptr;
  int *coloring;
  int *reordering;
  cusparseColorInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, int *, int *, int *,
      const cusparseColorInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &fractionToColor_width, sizeof(fractionToColor_width)) <
          0 ||
      (fractionToColor_width == 0 &&
       rpc_read(conn, &fractionToColor, sizeof(fractionToColor)) < 0) ||
      (fractionToColor_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(fractionToColor_heap, fractionToColor_value,
                                   sizeof(fractionToColor_value),
                                   fractionToColor_width),
                fractionToColor_width) < 0) ||
      rpc_read(conn, &ncolors_width, sizeof(ncolors_width)) < 0 ||
      (ncolors_width == 0 && rpc_read(conn, &ncolors, sizeof(ncolors)) < 0) ||
      rpc_read(conn, &coloring, sizeof(int *)) < 0 ||
      rpc_read(conn, &reordering, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const cusparseColorInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrcolor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               fractionToColor_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         fractionToColor_heap, fractionToColor_value,
                         sizeof(fractionToColor_value), fractionToColor_width))
                   : fractionToColor,
               ncolors_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                        ncolors_heap, ncolors_value,
                                        sizeof(ncolors_value), ncolors_width))
                                  : ncolors,
               coloring, reordering, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (ncolors_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(ncolors_heap, ncolors_value,
                                    sizeof(ncolors_value), ncolors_width),
                 ncolors_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsrcolor(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t fractionToColor_width;
  alignas(16) unsigned char fractionToColor_value[64];
  std::vector<unsigned char> fractionToColor_heap;
  const double *fractionToColor = nullptr;
  uint32_t ncolors_width;
  alignas(16) unsigned char ncolors_value[64];
  std::vector<unsigned char> ncolors_heap;
  int *ncolors = nullptr;
  int *coloring;
  int *reordering;
  cusparseColorInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, int *, int *, int *,
      const cusparseColorInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &fractionToColor_width, sizeof(fractionToColor_width)) <
          0 ||
      (fractionToColor_width == 0 &&
       rpc_read(conn, &fractionToColor, sizeof(fractionToColor)) < 0) ||
      (fractionToColor_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(fractionToColor_heap, fractionToColor_value,
                                   sizeof(fractionToColor_value),
                                   fractionToColor_width),
                fractionToColor_width) < 0) ||
      rpc_read(conn, &ncolors_width, sizeof(ncolors_width)) < 0 ||
      (ncolors_width == 0 && rpc_read(conn, &ncolors, sizeof(ncolors)) < 0) ||
      rpc_read(conn, &coloring, sizeof(int *)) < 0 ||
      rpc_read(conn, &reordering, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const cusparseColorInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrcolor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               fractionToColor_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         fractionToColor_heap, fractionToColor_value,
                         sizeof(fractionToColor_value), fractionToColor_width))
                   : fractionToColor,
               ncolors_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                        ncolors_heap, ncolors_value,
                                        sizeof(ncolors_value), ncolors_width))
                                  : ncolors,
               coloring, reordering, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (ncolors_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(ncolors_heap, ncolors_value,
                                    sizeof(ncolors_value), ncolors_width),
                 ncolors_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsrcolor(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t fractionToColor_width;
  alignas(16) unsigned char fractionToColor_value[64];
  std::vector<unsigned char> fractionToColor_heap;
  const float *fractionToColor = nullptr;
  uint32_t ncolors_width;
  alignas(16) unsigned char ncolors_value[64];
  std::vector<unsigned char> ncolors_heap;
  int *ncolors = nullptr;
  int *coloring;
  int *reordering;
  cusparseColorInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, const float *, int *, int *, int *,
      const cusparseColorInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &fractionToColor_width, sizeof(fractionToColor_width)) <
          0 ||
      (fractionToColor_width == 0 &&
       rpc_read(conn, &fractionToColor, sizeof(fractionToColor)) < 0) ||
      (fractionToColor_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(fractionToColor_heap, fractionToColor_value,
                                   sizeof(fractionToColor_value),
                                   fractionToColor_width),
                fractionToColor_width) < 0) ||
      rpc_read(conn, &ncolors_width, sizeof(ncolors_width)) < 0 ||
      (ncolors_width == 0 && rpc_read(conn, &ncolors, sizeof(ncolors)) < 0) ||
      rpc_read(conn, &coloring, sizeof(int *)) < 0 ||
      rpc_read(conn, &reordering, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const cusparseColorInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrcolor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               fractionToColor_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         fractionToColor_heap, fractionToColor_value,
                         sizeof(fractionToColor_value), fractionToColor_width))
                   : fractionToColor,
               ncolors_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                        ncolors_heap, ncolors_value,
                                        sizeof(ncolors_value), ncolors_width))
                                  : ncolors,
               coloring, reordering, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (ncolors_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(ncolors_heap, ncolors_value,
                                    sizeof(ncolors_value), ncolors_width),
                 ncolors_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsrcolor(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t fractionToColor_width;
  alignas(16) unsigned char fractionToColor_value[64];
  std::vector<unsigned char> fractionToColor_heap;
  const double *fractionToColor = nullptr;
  uint32_t ncolors_width;
  alignas(16) unsigned char ncolors_value[64];
  std::vector<unsigned char> ncolors_heap;
  int *ncolors = nullptr;
  int *coloring;
  int *reordering;
  cusparseColorInfo_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, const double *, int *,
      int *, int *, const cusparseColorInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &fractionToColor_width, sizeof(fractionToColor_width)) <
          0 ||
      (fractionToColor_width == 0 &&
       rpc_read(conn, &fractionToColor, sizeof(fractionToColor)) < 0) ||
      (fractionToColor_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(fractionToColor_heap, fractionToColor_value,
                                   sizeof(fractionToColor_value),
                                   fractionToColor_width),
                fractionToColor_width) < 0) ||
      rpc_read(conn, &ncolors_width, sizeof(ncolors_width)) < 0 ||
      (ncolors_width == 0 && rpc_read(conn, &ncolors, sizeof(ncolors)) < 0) ||
      rpc_read(conn, &coloring, sizeof(int *)) < 0 ||
      rpc_read(conn, &reordering, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const cusparseColorInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrcolor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               fractionToColor_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         fractionToColor_heap, fractionToColor_value,
                         sizeof(fractionToColor_value), fractionToColor_width))
                   : fractionToColor,
               ncolors_width != 0 ? reinterpret_cast<int *>(lupine_scalar_slot(
                                        ncolors_heap, ncolors_value,
                                        sizeof(ncolors_value), ncolors_width))
                                  : ncolors,
               coloring, reordering, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (ncolors_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(ncolors_heap, ncolors_value,
                                    sizeof(ncolors_value), ncolors_width),
                 ncolors_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSnnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *A;
  int lda;
  int *nnzPerRowCol;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int,
                                    int, const cusparseMatDescr_t,
                                    const float *, int, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRowCol, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSnnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, A, lda, nnzPerRowCol,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *A;
  int lda;
  int *nnzPerRowCol;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int,
                                    int, const cusparseMatDescr_t,
                                    const double *, int, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRowCol, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, A, lda, nnzPerRowCol,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCnnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *A;
  int lda;
  int *nnzPerRowCol;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int,
                                    int, const cusparseMatDescr_t,
                                    const cuComplex *, int, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRowCol, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCnnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, A, lda, nnzPerRowCol,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZnnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *A;
  int lda;
  int *nnzPerRowCol;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int,
                                    int, const cusparseMatDescr_t,
                                    const cuDoubleComplex *, int, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRowCol, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZnnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, A, lda, nnzPerRowCol,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSnnz_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  cusparseMatDescr_t descr;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  int *nnzPerRow;
  uint32_t nnzC_width;
  alignas(16) unsigned char nnzC_value[64];
  std::vector<unsigned char> nnzC_heap;
  int *nnzC = nullptr;
  float tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cusparseMatDescr_t,
                           const float *, const int *, int *, int *, float);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &descr, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzC_width, sizeof(nnzC_width)) < 0 ||
      (nnzC_width == 0 && rpc_read(conn, &nnzC, sizeof(nnzC)) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSnnz_compress");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, descr, csrSortedValA, csrSortedRowPtrA, nnzPerRow,
               nnzC_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzC_heap, nnzC_value, sizeof(nnzC_value), nnzC_width))
                   : nnzC,
               tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzC_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzC_heap, nnzC_value, sizeof(nnzC_value),
                                    nnzC_width),
                 nnzC_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnnz_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  cusparseMatDescr_t descr;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  int *nnzPerRow;
  uint32_t nnzC_width;
  alignas(16) unsigned char nnzC_value[64];
  std::vector<unsigned char> nnzC_heap;
  int *nnzC = nullptr;
  double tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cusparseMatDescr_t,
                           const double *, const int *, int *, int *, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &descr, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzC_width, sizeof(nnzC_width)) < 0 ||
      (nnzC_width == 0 && rpc_read(conn, &nnzC, sizeof(nnzC)) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnnz_compress");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, descr, csrSortedValA, csrSortedRowPtrA, nnzPerRow,
               nnzC_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzC_heap, nnzC_value, sizeof(nnzC_value), nnzC_width))
                   : nnzC,
               tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzC_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzC_heap, nnzC_value, sizeof(nnzC_value),
                                    nnzC_width),
                 nnzC_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCnnz_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  cusparseMatDescr_t descr;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  int *nnzPerRow;
  uint32_t nnzC_width;
  alignas(16) unsigned char nnzC_value[64];
  std::vector<unsigned char> nnzC_heap;
  int *nnzC = nullptr;
  cuComplex tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, int *, int *, cuComplex);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &descr, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzC_width, sizeof(nnzC_width)) < 0 ||
      (nnzC_width == 0 && rpc_read(conn, &nnzC, sizeof(nnzC)) < 0) ||
      rpc_read(conn, &tol, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCnnz_compress");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, descr, csrSortedValA, csrSortedRowPtrA, nnzPerRow,
               nnzC_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzC_heap, nnzC_value, sizeof(nnzC_value), nnzC_width))
                   : nnzC,
               tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzC_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzC_heap, nnzC_value, sizeof(nnzC_value),
                                    nnzC_width),
                 nnzC_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZnnz_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  cusparseMatDescr_t descr;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  int *nnzPerRow;
  uint32_t nnzC_width;
  alignas(16) unsigned char nnzC_value[64];
  std::vector<unsigned char> nnzC_heap;
  int *nnzC = nullptr;
  cuDoubleComplex tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, const cusparseMatDescr_t, const cuDoubleComplex *,
      const int *, int *, int *, cuDoubleComplex);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &descr, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzC_width, sizeof(nnzC_width)) < 0 ||
      (nnzC_width == 0 && rpc_read(conn, &nnzC, sizeof(nnzC)) < 0) ||
      rpc_read(conn, &tol, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZnnz_compress");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, descr, csrSortedValA, csrSortedRowPtrA, nnzPerRow,
               nnzC_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzC_heap, nnzC_value, sizeof(nnzC_value), nnzC_width))
                   : nnzC,
               tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzC_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzC_heap, nnzC_value, sizeof(nnzC_value),
                                    nnzC_width),
                 nnzC_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsr2csr_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedColIndA;
  const int *csrSortedRowPtrA;
  int nnzA;
  const int *nnzPerRow;
  float *csrSortedValC;
  int *csrSortedColIndC;
  int *csrSortedRowPtrC;
  float tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, int, const int *, float *, int *, int *, float);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2csr_compress");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, csrSortedValA, csrSortedColIndA,
                          csrSortedRowPtrA, nnzA, nnzPerRow, csrSortedValC,
                          csrSortedColIndC, csrSortedRowPtrC, tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsr2csr_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedColIndA;
  const int *csrSortedRowPtrA;
  int nnzA;
  const int *nnzPerRow;
  double *csrSortedValC;
  int *csrSortedColIndC;
  int *csrSortedRowPtrC;
  double tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const double *, const int *, const int *, int,
                           const int *, double *, int *, int *, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2csr_compress");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, csrSortedValA, csrSortedColIndA,
                          csrSortedRowPtrA, nnzA, nnzPerRow, csrSortedValC,
                          csrSortedColIndC, csrSortedRowPtrC, tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsr2csr_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedColIndA;
  const int *csrSortedRowPtrA;
  int nnzA;
  const int *nnzPerRow;
  cuComplex *csrSortedValC;
  int *csrSortedColIndC;
  int *csrSortedRowPtrC;
  cuComplex tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuComplex *, const int *, const int *, int,
                           const int *, cuComplex *, int *, int *, cuComplex);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &tol, sizeof(cuComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2csr_compress");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, csrSortedValA, csrSortedColIndA,
                          csrSortedRowPtrA, nnzA, nnzPerRow, csrSortedValC,
                          csrSortedColIndC, csrSortedRowPtrC, tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsr2csr_compress(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedColIndA;
  const int *csrSortedRowPtrA;
  int nnzA;
  const int *nnzPerRow;
  cuDoubleComplex *csrSortedValC;
  int *csrSortedColIndC;
  int *csrSortedRowPtrC;
  cuDoubleComplex tol;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, const int *,
      cuDoubleComplex *, int *, int *, cuDoubleComplex);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &tol, sizeof(cuDoubleComplex)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2csr_compress");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, csrSortedValA, csrSortedColIndA,
                          csrSortedRowPtrA, nnzA, nnzPerRow, csrSortedValC,
                          csrSortedColIndC, csrSortedRowPtrC, tol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcoo2csr(conn_t *conn) {
  cusparseHandle_t handle;
  const int *cooRowInd;
  int nnz;
  int m;
  int *csrSortedRowPtr;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, const int *, int, int,
                                    int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &cooRowInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcoo2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, cooRowInd, nnz, m, csrSortedRowPtr, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsr2coo(conn_t *conn) {
  cusparseHandle_t handle;
  const int *csrSortedRowPtr;
  int nnz;
  int m;
  int *cooRowInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, const int *, int, int,
                                    int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &cooRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsr2coo");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, csrSortedRowPtr, nnz, m, cooRowInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsr2bsrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  int *bsrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const int *, const int *, int, const cusparseMatDescr_t, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsr2bsrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedRowPtrA, csrSortedColIndA,
               blockDim, descrC, bsrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsr2bsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  float *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, const cusparseMatDescr_t,
      float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2bsr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, m, n, descrA, csrSortedValA,
                          csrSortedRowPtrA, csrSortedColIndA, blockDim, descrC,
                          bsrSortedValC, bsrSortedRowPtrC, bsrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsr2bsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  double *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, const cusparseMatDescr_t,
      double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2bsr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, m, n, descrA, csrSortedValA,
                          csrSortedRowPtrA, csrSortedColIndA, blockDim, descrC,
                          bsrSortedValC, bsrSortedRowPtrC, bsrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsr2bsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  cuComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int,
      const cusparseMatDescr_t, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2bsr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, m, n, descrA, csrSortedValA,
                          csrSortedRowPtrA, csrSortedColIndA, blockDim, descrC,
                          bsrSortedValC, bsrSortedRowPtrC, bsrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsr2bsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int,
      const cusparseMatDescr_t, cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2bsr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, m, n, descrA, csrSortedValA,
                          csrSortedRowPtrA, csrSortedColIndA, blockDim, descrC,
                          bsrSortedValC, bsrSortedRowPtrC, bsrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSbsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, const cusparseMatDescr_t,
      float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSbsr2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, mb, nb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, descrC,
                          csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDbsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, const cusparseMatDescr_t,
      double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDbsr2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, mb, nb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, descrC,
                          csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCbsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  cuComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int,
      const cusparseMatDescr_t, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCbsr2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, mb, nb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, descrC,
                          csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZbsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int blockDim;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int,
      const cusparseMatDescr_t, cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZbsr2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, dirA, mb, nb, descrA, bsrSortedValA,
                          bsrSortedRowPtrA, bsrSortedColIndA, blockDim, descrC,
                          csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgebsr2gebsc_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, const float *,
                           const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgebsr2gebsc_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgebsr2gebsc_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, const double *,
                           const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgebsr2gebsc_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgebsr2gebsc_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, const cuComplex *,
                           const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgebsr2gebsc_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgebsr2gebsc_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cuDoubleComplex *, const int *,
                                    const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgebsr2gebsc_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
               bsrSortedColInd, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgebsr2gebsc(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const float *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  float *bscVal;
  int *bscRowInd;
  int *bscColPtr;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const float *, const int *, const int *,
      int, int, float *, int *, int *, cusparseAction_t, cusparseIndexBase_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &bscVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &bscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &bscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgebsr2gebsc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
                          bsrSortedColInd, rowBlockDim, colBlockDim, bscVal,
                          bscRowInd, bscColPtr, copyValues, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgebsr2gebsc(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const double *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  double *bscVal;
  int *bscRowInd;
  int *bscColPtr;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const double *, const int *, const int *,
      int, int, double *, int *, int *, cusparseAction_t, cusparseIndexBase_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &bscVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &bscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &bscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgebsr2gebsc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
                          bsrSortedColInd, rowBlockDim, colBlockDim, bscVal,
                          bscRowInd, bscColPtr, copyValues, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgebsr2gebsc(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const cuComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  cuComplex *bscVal;
  int *bscRowInd;
  int *bscColPtr;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuComplex *, const int *,
      const int *, int, int, cuComplex *, int *, int *, cusparseAction_t,
      cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &bscVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &bscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgebsr2gebsc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
                          bsrSortedColInd, rowBlockDim, colBlockDim, bscVal,
                          bscRowInd, bscColPtr, copyValues, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgebsr2gebsc(conn_t *conn) {
  cusparseHandle_t handle;
  int mb;
  int nb;
  int nnzb;
  const cuDoubleComplex *bsrSortedVal;
  const int *bsrSortedRowPtr;
  const int *bsrSortedColInd;
  int rowBlockDim;
  int colBlockDim;
  cuDoubleComplex *bscVal;
  int *bscRowInd;
  int *bscColPtr;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuDoubleComplex *, const int *,
      const int *, int, int, cuDoubleComplex *, int *, int *, cusparseAction_t,
      cusparseIndexBase_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &bsrSortedVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &bscVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &bscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgebsr2gebsc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mb, nb, nnzb, bsrSortedVal, bsrSortedRowPtr,
                          bsrSortedColInd, rowBlockDim, colBlockDim, bscVal,
                          bscRowInd, bscColPtr, copyValues, idxBase, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgebsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, int,
      const cusparseMatDescr_t, float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgebsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, descrA, bsrSortedValA, bsrSortedRowPtrA,
               bsrSortedColIndA, rowBlockDim, colBlockDim, descrC,
               csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgebsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, int,
      const cusparseMatDescr_t, double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgebsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, descrA, bsrSortedValA, bsrSortedRowPtrA,
               bsrSortedColIndA, rowBlockDim, colBlockDim, descrC,
               csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgebsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  cusparseMatDescr_t descrC;
  cuComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int, int,
      const cusparseMatDescr_t, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgebsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, descrA, bsrSortedValA, bsrSortedRowPtrA,
               bsrSortedColIndA, rowBlockDim, colBlockDim, descrC,
               csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgebsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *csrSortedValC;
  int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, int,
      const cusparseMatDescr_t, cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgebsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, descrA, bsrSortedValA, bsrSortedRowPtrA,
               bsrSortedColIndA, rowBlockDim, colBlockDim, descrC,
               csrSortedValC, csrSortedRowPtrC, csrSortedColIndC);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  int rowBlockDim;
  int colBlockDim;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, rowBlockDim, colBlockDim, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsr2gebsrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrC;
  int *bsrSortedRowPtrC;
  int rowBlockDim;
  int colBlockDim;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int,
                                    int, const cusparseMatDescr_t, const int *,
                                    const int *, const cusparseMatDescr_t,
                                    int *, int, int, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsr2gebsrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedRowPtrA, csrSortedColIndA,
               descrC, bsrSortedRowPtrC, rowBlockDim, colBlockDim,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrC;
  float *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDim;
  int colBlockDim;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, const cusparseMatDescr_t,
      float *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2gebsr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, descrC, bsrSortedValC, bsrSortedRowPtrC,
               bsrSortedColIndC, rowBlockDim, colBlockDim, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrC;
  double *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDim;
  int colBlockDim;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, const cusparseMatDescr_t,
      double *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2gebsr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, descrC, bsrSortedValC, bsrSortedRowPtrC,
               bsrSortedColIndC, rowBlockDim, colBlockDim, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrC;
  cuComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDim;
  int colBlockDim;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, const cusparseMatDescr_t,
      cuComplex *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2gebsr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, descrC, bsrSortedValC, bsrSortedRowPtrC,
               bsrSortedColIndC, rowBlockDim, colBlockDim, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDim;
  int colBlockDim;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int, int,
                           const cusparseMatDescr_t, const cuDoubleComplex *,
                           const int *, const int *, const cusparseMatDescr_t,
                           cuDoubleComplex *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2gebsr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, m, n, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, descrC, bsrSortedValC, bsrSortedRowPtrC,
               bsrSortedColIndC, rowBlockDim, colBlockDim, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgebsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  int rowBlockDimC;
  int colBlockDimC;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int, int, int,
                           const cusparseMatDescr_t, const float *, const int *,
                           const int *, int, int, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgebsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
               bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA, colBlockDimA,
               rowBlockDimC, colBlockDimC, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgebsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  int rowBlockDimC;
  int colBlockDimC;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int, int, int,
                           const cusparseMatDescr_t, const double *,
                           const int *, const int *, int, int, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgebsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
               bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA, colBlockDimA,
               rowBlockDimC, colBlockDimC, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgebsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  int rowBlockDimC;
  int colBlockDimC;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int, int, int,
                           const cusparseMatDescr_t, const cuComplex *,
                           const int *, const int *, int, int, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgebsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
               bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA, colBlockDimA,
               rowBlockDimC, colBlockDimC, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgebsr2gebsr_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  int rowBlockDimC;
  int colBlockDimC;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseDirection_t, int, int, int,
                           const cusparseMatDescr_t, const cuDoubleComplex *,
                           const int *, const int *, int, int, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgebsr2gebsr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
               bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA, colBlockDimA,
               rowBlockDimC, colBlockDimC, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXgebsr2gebsrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  cusparseMatDescr_t descrC;
  int *bsrSortedRowPtrC;
  int rowBlockDimC;
  int colBlockDimC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, int,
      const cusparseMatDescr_t, const int *, const int *, int, int,
      const cusparseMatDescr_t, int *, int, int, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXgebsr2gebsrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedRowPtrA,
               bsrSortedColIndA, rowBlockDimA, colBlockDimA, descrC,
               bsrSortedRowPtrC, rowBlockDimC, colBlockDimC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSgebsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const float *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  cusparseMatDescr_t descrC;
  float *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDimC;
  int colBlockDimC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, int,
      const cusparseMatDescr_t, const float *, const int *, const int *, int,
      int, const cusparseMatDescr_t, float *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgebsr2gebsr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
                         bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA,
                         colBlockDimA, descrC, bsrSortedValC, bsrSortedRowPtrC,
                         bsrSortedColIndC, rowBlockDimC, colBlockDimC, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDgebsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const double *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  cusparseMatDescr_t descrC;
  double *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDimC;
  int colBlockDimC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, int,
      const cusparseMatDescr_t, const double *, const int *, const int *, int,
      int, const cusparseMatDescr_t, double *, int *, int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgebsr2gebsr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
                         bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA,
                         colBlockDimA, descrC, bsrSortedValC, bsrSortedRowPtrC,
                         bsrSortedColIndC, rowBlockDimC, colBlockDimC, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCgebsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  cusparseMatDescr_t descrC;
  cuComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDimC;
  int colBlockDimC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, int,
      const cusparseMatDescr_t, const cuComplex *, const int *, const int *,
      int, int, const cusparseMatDescr_t, cuComplex *, int *, int *, int, int,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgebsr2gebsr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
                         bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA,
                         colBlockDimA, descrC, bsrSortedValC, bsrSortedRowPtrC,
                         bsrSortedColIndC, rowBlockDimC, colBlockDimC, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZgebsr2gebsr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseDirection_t dirA;
  int mb;
  int nb;
  int nnzb;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *bsrSortedValA;
  const int *bsrSortedRowPtrA;
  const int *bsrSortedColIndA;
  int rowBlockDimA;
  int colBlockDimA;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *bsrSortedValC;
  int *bsrSortedRowPtrC;
  int *bsrSortedColIndC;
  int rowBlockDimC;
  int colBlockDimC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseDirection_t, int, int, int,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, int, int, const cusparseMatDescr_t, cuDoubleComplex *, int *,
      int *, int, int, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &dirA, sizeof(cusparseDirection_t)) < 0 ||
      rpc_read(conn, &mb, sizeof(int)) < 0 ||
      rpc_read(conn, &nb, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzb, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &rowBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &bsrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &bsrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &bsrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &rowBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &colBlockDimC, sizeof(int)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgebsr2gebsr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, dirA, mb, nb, nnzb, descrA, bsrSortedValA,
                         bsrSortedRowPtrA, bsrSortedColIndA, rowBlockDimA,
                         colBlockDimA, descrC, bsrSortedValC, bsrSortedRowPtrC,
                         bsrSortedColIndC, rowBlockDimC, colBlockDimC, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateIdentityPermutation(conn_t *conn) {
  cusparseHandle_t handle;
  int n;
  int *p;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &p, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateIdentityPermutation");
  return_value = fn == nullptr ? function_not_found() : fn(handle, n, p);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcoosort_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  const int *cooRowsA;
  const int *cooColsA;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &cooRowsA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cooColsA, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcoosort_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, cooRowsA, cooColsA,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcoosortByRow(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  int *cooRowsA;
  int *cooColsA;
  int *P;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int, int *,
                                    int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &cooRowsA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cooColsA, sizeof(int *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcoosortByRow");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, cooRowsA, cooColsA, P, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcoosortByColumn(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  int *cooRowsA;
  int *cooColsA;
  int *P;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int, int *,
                                    int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &cooRowsA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cooColsA, sizeof(int *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcoosortByColumn");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, cooRowsA, cooColsA, P, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsrsort_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  const int *csrRowPtrA;
  const int *csrColIndA;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrsort_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrRowPtrA, csrColIndA,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcsrsort(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const int *csrRowPtrA;
  int *csrColIndA;
  int *P;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrsort");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrRowPtrA,
                                    csrColIndA, P, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcscsort_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  const int *cscColPtrA;
  const int *cscRowIndA;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &cscColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndA, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcscsort_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, cscColPtrA, cscRowIndA,
                                    &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseXcscsort(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const int *cscColPtrA;
  int *cscRowIndA;
  int *P;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &cscColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcscsort");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, cscColPtrA,
                                    cscRowIndA, P, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsru2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  float *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, float *,
                           const int *, int *, csru2csrInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsru2csr_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrVal, csrRowPtr,
                                    csrColInd, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsru2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  double *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, double *,
                           const int *, int *, csru2csrInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsru2csr_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrVal, csrRowPtr,
                                    csrColInd, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsru2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cuComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, cuComplex *,
                           const int *, int *, csru2csrInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsru2csr_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrVal, csrRowPtr,
                                    csrColInd, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsru2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, int, cuDoubleComplex *,
                           const int *, int *, csru2csrInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsru2csr_bufferSizeExt");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrVal, csrRowPtr,
                                    csrColInd, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsru2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, float *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsru2csr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsru2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, double *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsru2csr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsru2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, cuComplex *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsru2csr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsru2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, cuDoubleComplex *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsru2csr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScsr2csru(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, float *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2csru");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDcsr2csru(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, double *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2csru");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCcsr2csru(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, cuComplex *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2csru");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseZcsr2csru(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  int *csrColInd;
  csru2csrInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int,
                                    const cusparseMatDescr_t, cuDoubleComplex *,
                                    const int *, int *, csru2csrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csru2csrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2csru");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const __half *, int,
                           const __half *, const cusparseMatDescr_t,
                           const __half *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneDense2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const float *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *, int,
                           const float *, const cusparseMatDescr_t,
                           const float *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneDense2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const double *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *, int,
                           const double *, const cusparseMatDescr_t,
                           const double *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneDense2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const __half *, int, const __half *,
      const cusparseMatDescr_t, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneDense2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const float *, int, const float *,
      const cusparseMatDescr_t, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneDense2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const double *, int, const double *,
      const cusparseMatDescr_t, int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneDense2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const __half *, int, const __half *,
      const cusparseMatDescr_t, __half *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(__half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneDense2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const float *, int, const float *,
      const cusparseMatDescr_t, float *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneDense2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const double *, int, const double *,
      const cusparseMatDescr_t, double *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneDense2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, const __half *, const cusparseMatDescr_t,
      const __half *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const float *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, const cusparseMatDescr_t,
      const float *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csr_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  const double *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, const cusparseMatDescr_t,
      const double *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csr_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, const __half *, const cusparseMatDescr_t, int *,
      int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, const cusparseMatDescr_t, int *,
      int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csrNnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, const cusparseMatDescr_t, int *,
      int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csrNnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const __half *threshold = nullptr;
  cusparseMatDescr_t descrC;
  __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, const __half *, const cusparseMatDescr_t,
      __half *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(__half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const __half *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const float *threshold = nullptr;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, const cusparseMatDescr_t,
      float *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  uint32_t threshold_width;
  alignas(16) unsigned char threshold_value[64];
  std::vector<unsigned char> threshold_heap;
  const double *threshold = nullptr;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, const cusparseMatDescr_t,
      double *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &threshold_width, sizeof(threshold_width)) < 0 ||
      (threshold_width == 0 &&
       rpc_read(conn, &threshold, sizeof(threshold)) < 0) ||
      (threshold_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(threshold_heap, threshold_value,
                                   sizeof(threshold_value), threshold_width),
                threshold_width) < 0) ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csr");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA,
               threshold_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         threshold_heap, threshold_value,
                         sizeof(threshold_value), threshold_width))
                   : threshold,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  const __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const __half *, int,
                           float, const cusparseMatDescr_t, const __half *,
                           const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>(
      "cusparseHpruneDense2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  const float *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *, int,
                           float, const cusparseMatDescr_t, const float *,
                           const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>(
      "cusparseSpruneDense2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  const double *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *, int,
                           float, const cusparseMatDescr_t, const double *,
                           const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>(
      "cusparseDpruneDense2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const __half *,
                                    int, float, const cusparseMatDescr_t, int *,
                                    int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneDense2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *,
                                    int, float, const cusparseMatDescr_t, int *,
                                    int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneDense2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *,
                                    int, float, const cusparseMatDescr_t, int *,
                                    int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneDense2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, A, lda, percentage, descrC, csrRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneDense2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const __half *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const __half *, int,
                           float, const cusparseMatDescr_t, __half *,
                           const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(__half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneDense2csrByPercentage");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, percentage, descrC,
                                    csrSortedValC, csrSortedRowPtrC,
                                    csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneDense2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const float *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const float *, int,
                           float, const cusparseMatDescr_t, float *,
                           const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneDense2csrByPercentage");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, percentage, descrC,
                                    csrSortedValC, csrSortedRowPtrC,
                                    csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneDense2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  const double *A;
  int lda;
  float percentage;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const double *, int,
                           float, const cusparseMatDescr_t, double *,
                           const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneDense2csrByPercentage");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, percentage, descrC,
                                    csrSortedValC, csrSortedRowPtrC,
                                    csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  const __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, float, const cusparseMatDescr_t, const __half *,
      const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  const float *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, float, const cusparseMatDescr_t, const float *,
      const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csrByPercentage_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  const double *csrSortedValC;
  const int *csrSortedRowPtrC;
  const int *csrSortedColIndC;
  pruneInfo_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, float, const cusparseMatDescr_t, const double *,
      const int *, const int *, pruneInfo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csrByPercentage_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, float, const cusparseMatDescr_t, int *, int *,
      pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, float, const cusparseMatDescr_t, int *, int *,
      pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csrNnzByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, float, const cusparseMatDescr_t, int *, int *,
      pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csrNnzByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseHpruneCsr2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const __half *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  __half *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const __half *,
      const int *, const int *, float, const cusparseMatDescr_t, __half *,
      const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const __half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(__half *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseHpruneCsr2csrByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpruneCsr2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, float, const cusparseMatDescr_t, float *,
      const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpruneCsr2csrByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDpruneCsr2csrByPercentage(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float percentage;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  pruneInfo_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, float, const cusparseMatDescr_t, double *,
      const int *, int *, pruneInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &percentage, sizeof(float)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(pruneInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDpruneCsr2csrByPercentage");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, percentage, descrC, csrSortedValC,
               csrSortedRowPtrC, csrSortedColIndC, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCsr2cscEx2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  const void *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  void *cscVal;
  int *cscColPtr;
  int *cscRowInd;
  cudaDataType valType;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  cusparseCsr2CscAlg_t alg;
  void *buffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const void *, const int *, const int *,
      void *, int *, int *, cudaDataType, cusparseAction_t, cusparseIndexBase_t,
      cusparseCsr2CscAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscVal, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &valType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseCsr2CscAlg_t)) < 0 ||
      rpc_read(conn, &buffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsr2cscEx2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, csrVal, csrRowPtr,
                                    csrColInd, cscVal, cscColPtr, cscRowInd,
                                    valType, copyValues, idxBase, alg, buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCsr2cscEx2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int nnz;
  const void *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  void *cscVal;
  int *cscColPtr;
  int *cscRowInd;
  cudaDataType valType;
  cusparseAction_t copyValues;
  cusparseIndexBase_t idxBase;
  cusparseCsr2CscAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const void *, const int *, const int *,
      void *, int *, int *, cudaDataType, cusparseAction_t, cusparseIndexBase_t,
      cusparseCsr2CscAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscVal, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscColPtr, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscRowInd, sizeof(int *)) < 0 ||
      rpc_read(conn, &valType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &copyValues, sizeof(cusparseAction_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseCsr2CscAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsr2cscEx2_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, csrVal, csrRowPtr, csrColInd,
                          cscVal, cscColPtr, cscRowInd, valType, copyValues,
                          idxBase, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateSpVec(conn_t *conn) {
  cusparseSpVecDescr_t spVecDescr{};
  spVecDescr = {};
  int64_t size;
  int64_t nnz;
  void *indices;
  void *values;
  cusparseIndexType_t idxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpVecDescr_t *, int64_t, int64_t,
                                    void *, void *, cusparseIndexType_t,
                                    cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &indices, sizeof(void *)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 ||
      rpc_read(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateSpVec");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spVecDescr, size, nnz, indices, values,
                                    idxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spVecDescr, sizeof(cusparseSpVecDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstSpVec(conn_t *conn) {
  cusparseConstSpVecDescr_t spVecDescr{};
  spVecDescr = {};
  int64_t size;
  int64_t nnz;
  const void *indices;
  const void *values;
  cusparseIndexType_t idxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpVecDescr_t *, int64_t, int64_t, const void *, const void *,
      cusparseIndexType_t, cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &indices, sizeof(const void *)) < 0 ||
      rpc_read(conn, &values, sizeof(const void *)) < 0 ||
      rpc_read(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstSpVec");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spVecDescr, size, nnz, indices, values,
                                    idxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spVecDescr, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDestroySpVec(conn_t *conn) {
  cusparseConstSpVecDescr_t spVecDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroySpVec");
  return_value = fn == nullptr ? function_not_found() : fn(spVecDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpVecGet(conn_t *conn) {
  cusparseSpVecDescr_t spVecDescr;
  int64_t size{};
  size = {};
  int64_t nnz{};
  nnz = {};
  void *indices{};
  indices = {};
  void *values{};
  values = {};
  cusparseIndexType_t idxType{};
  idxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpVecDescr_t, int64_t *, int64_t *,
                                    void **, void **, cusparseIndexType_t *,
                                    cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseSpVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVecGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spVecDescr, &size, &nnz, &indices, &values,
                                    &idxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &indices, sizeof(void *)) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstSpVecGet(conn_t *conn) {
  cusparseConstSpVecDescr_t spVecDescr;
  int64_t size{};
  size = {};
  int64_t nnz{};
  nnz = {};
  const void *indices{};
  indices = {};
  const void *values{};
  values = {};
  cusparseIndexType_t idxType{};
  idxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseConstSpVecDescr_t, int64_t *, int64_t *,
                           const void **, const void **, cusparseIndexType_t *,
                           cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstSpVecGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spVecDescr, &size, &nnz, &indices, &values,
                                    &idxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &indices, sizeof(const void *)) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpVecGetIndexBase(conn_t *conn) {
  cusparseConstSpVecDescr_t spVecDescr;
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseConstSpVecDescr_t, cusparseIndexBase_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVecGetIndexBase");
  return_value =
      fn == nullptr ? function_not_found() : fn(spVecDescr, &idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpVecGetValues(conn_t *conn) {
  cusparseSpVecDescr_t spVecDescr;
  void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpVecDescr_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseSpVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVecGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spVecDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstSpVecGetValues(conn_t *conn) {
  cusparseConstSpVecDescr_t spVecDescr;
  const void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpVecDescr_t, const void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstSpVecGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spVecDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpVecSetValues(conn_t *conn) {
  cusparseSpVecDescr_t spVecDescr;
  void *values;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpVecDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spVecDescr, sizeof(cusparseSpVecDescr_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVecSetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spVecDescr, values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateDnVec(conn_t *conn) {
  cusparseDnVecDescr_t dnVecDescr{};
  dnVecDescr = {};
  int64_t size;
  void *values;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnVecDescr_t *, int64_t, void *,
                                    cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateDnVec");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&dnVecDescr, size, values, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dnVecDescr, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstDnVec(conn_t *conn) {
  cusparseConstDnVecDescr_t dnVecDescr{};
  dnVecDescr = {};
  int64_t size;
  const void *values;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnVecDescr_t *, int64_t,
                                    const void *, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &values, sizeof(const void *)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstDnVec");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&dnVecDescr, size, values, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dnVecDescr, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDestroyDnVec(conn_t *conn) {
  cusparseConstDnVecDescr_t dnVecDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyDnVec");
  return_value = fn == nullptr ? function_not_found() : fn(dnVecDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnVecGet(conn_t *conn) {
  cusparseDnVecDescr_t dnVecDescr;
  int64_t size{};
  size = {};
  void *values{};
  values = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnVecDescr_t, int64_t *, void **,
                                    cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnVecGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dnVecDescr, &size, &values, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstDnVecGet(conn_t *conn) {
  cusparseConstDnVecDescr_t dnVecDescr;
  int64_t size{};
  size = {};
  const void *values{};
  values = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnVecDescr_t, int64_t *,
                                    const void **, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstDnVecGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dnVecDescr, &size, &values, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDnVecGetValues(conn_t *conn) {
  cusparseDnVecDescr_t dnVecDescr;
  void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnVecDescr_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnVecGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnVecDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstDnVecGetValues(conn_t *conn) {
  cusparseConstDnVecDescr_t dnVecDescr;
  const void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnVecDescr_t, const void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstDnVecGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnVecDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDnVecSetValues(conn_t *conn) {
  cusparseDnVecDescr_t dnVecDescr;
  void *values;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnVecDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnVecDescr, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnVecSetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnVecDescr, values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDestroySpMat(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroySpMat");
  return_value = fn == nullptr ? function_not_found() : fn(spMatDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMatGetFormat(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  cusparseFormat_t format{};
  format = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseConstSpMatDescr_t, cusparseFormat_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetFormat");
  return_value = fn == nullptr ? function_not_found() : fn(spMatDescr, &format);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &format, sizeof(cusparseFormat_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMatGetIndexBase(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseConstSpMatDescr_t, cusparseIndexBase_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetIndexBase");
  return_value =
      fn == nullptr ? function_not_found() : fn(spMatDescr, &idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMatGetValues(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spMatDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstSpMatGetValues(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  const void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t, const void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstSpMatGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spMatDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpMatSetValues(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  void *values;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatSetValues");
  return_value = fn == nullptr ? function_not_found() : fn(spMatDescr, values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMatGetSize(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t, int64_t *,
                                    int64_t *, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(spMatDescr, &rows, &cols, &nnz);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMatGetStridedBatch(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int batchCount{};
  batchCount = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetStridedBatch");
  return_value =
      fn == nullptr ? function_not_found() : fn(spMatDescr, &batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCooSetStridedBatch(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int batchCount;
  int64_t batchStride;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t, int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCooSetStridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, batchCount, batchStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCsrSetStridedBatch(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int batchCount;
  int64_t offsetsBatchStride;
  int64_t columnsValuesBatchStride;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseSpMatDescr_t, int, int64_t, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &offsetsBatchStride, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &columnsValuesBatchStride, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsrSetStridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, batchCount, offsetsBatchStride,
                                    columnsValuesBatchStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12100
int handle_cusparseBsrSetStridedBatch(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int batchCount;
  int64_t offsetsBatchStride;
  int64_t columnsBatchStride;
  int64_t ValuesBatchStride;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t, int, int64_t, int64_t,
                                    int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &offsetsBatchStride, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &columnsBatchStride, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ValuesBatchStride, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseBsrSetStridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, batchCount, offsetsBatchStride,
                                    columnsBatchStride, ValuesBatchStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpMatGetAttribute(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  cusparseSpMatAttribute_t attribute;
  size_t dataSize;
  void *data = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t,
                                    cusparseSpMatAttribute_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &attribute, sizeof(cusparseSpMatAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data = (void *)calloc(dataSize, 1);
  if ((dataSize != 0 && data == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatGetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, attribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, data, dataSize) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseSpMatSetAttribute(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  cusparseSpMatAttribute_t attribute;
  size_t dataSize;
  void *data = nullptr;
  size_t data_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t,
                                    cusparseSpMatAttribute_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &attribute, sizeof(cusparseSpMatAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = dataSize;
  data = (void *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatSetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, attribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseCreateCsr(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  void *csrRowOffsets;
  void *csrColInd;
  void *csrValues;
  cusparseIndexType_t csrRowOffsetsType;
  cusparseIndexType_t csrColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t *, int64_t, int64_t,
                                    int64_t, void *, void *, void *,
                                    cusparseIndexType_t, cusparseIndexType_t,
                                    cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &csrRowOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &csrValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &csrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &csrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, rows, cols, nnz, csrRowOffsets,
                                    csrColInd, csrValues, csrRowOffsetsType,
                                    csrColIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstCsr(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  const void *csrRowOffsets;
  const void *csrColInd;
  const void *csrValues;
  cusparseIndexType_t csrRowOffsetsType;
  cusparseIndexType_t csrColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t *, int64_t, int64_t, int64_t, const void *,
      const void *, const void *, cusparseIndexType_t, cusparseIndexType_t,
      cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &csrRowOffsets, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrValues, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &csrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstCsr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, rows, cols, nnz, csrRowOffsets,
                                    csrColInd, csrValues, csrRowOffsetsType,
                                    csrColIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCreateCsc(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  void *cscColOffsets;
  void *cscRowInd;
  void *cscValues;
  cusparseIndexType_t cscColOffsetsType;
  cusparseIndexType_t cscRowIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t *, int64_t, int64_t,
                                    int64_t, void *, void *, void *,
                                    cusparseIndexType_t, cusparseIndexType_t,
                                    cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cscColOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscRowInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscColOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &cscRowIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsc");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, rows, cols, nnz, cscColOffsets,
                                    cscRowInd, cscValues, cscColOffsetsType,
                                    cscRowIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstCsc(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  const void *cscColOffsets;
  const void *cscRowInd;
  const void *cscValues;
  cusparseIndexType_t cscColOffsetsType;
  cusparseIndexType_t cscRowIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t *, int64_t, int64_t, int64_t, const void *,
      const void *, const void *, cusparseIndexType_t, cusparseIndexType_t,
      cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cscColOffsets, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cscRowInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cscValues, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cscColOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &cscRowIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstCsc");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, rows, cols, nnz, cscColOffsets,
                                    cscRowInd, cscValues, cscColOffsetsType,
                                    cscRowIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCsrGet(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  void *csrRowOffsets{};
  csrRowOffsets = {};
  void *csrColInd{};
  csrColInd = {};
  void *csrValues{};
  csrValues = {};
  cusparseIndexType_t csrRowOffsetsType{};
  csrRowOffsetsType = {};
  cusparseIndexType_t csrColIndType{};
  csrColIndType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t, int64_t *, int64_t *, int64_t *, void **, void **,
      void **, cusparseIndexType_t *, cusparseIndexType_t *,
      cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsrGet");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, &rows, &cols, &nnz, &csrRowOffsets,
                          &csrColInd, &csrValues, &csrRowOffsetsType,
                          &csrColIndType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &csrRowOffsets, sizeof(void *)) < 0 ||
      rpc_write(conn, &csrColInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &csrValues, sizeof(void *)) < 0 ||
      rpc_write(conn, &csrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &csrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstCsrGet(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  const void *csrRowOffsets{};
  csrRowOffsets = {};
  const void *csrColInd{};
  csrColInd = {};
  const void *csrValues{};
  csrValues = {};
  cusparseIndexType_t csrRowOffsetsType{};
  csrRowOffsetsType = {};
  cusparseIndexType_t csrColIndType{};
  csrColIndType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t, int64_t *, int64_t *, int64_t *, const void **,
      const void **, const void **, cusparseIndexType_t *,
      cusparseIndexType_t *, cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstCsrGet");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, &rows, &cols, &nnz, &csrRowOffsets,
                          &csrColInd, &csrValues, &csrRowOffsetsType,
                          &csrColIndType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &csrRowOffsets, sizeof(const void *)) < 0 ||
      rpc_write(conn, &csrColInd, sizeof(const void *)) < 0 ||
      rpc_write(conn, &csrValues, sizeof(const void *)) < 0 ||
      rpc_write(conn, &csrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &csrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCscGet(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  void *cscColOffsets{};
  cscColOffsets = {};
  void *cscRowInd{};
  cscRowInd = {};
  void *cscValues{};
  cscValues = {};
  cusparseIndexType_t cscColOffsetsType{};
  cscColOffsetsType = {};
  cusparseIndexType_t cscRowIndType{};
  cscRowIndType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t, int64_t *, int64_t *, int64_t *, void **, void **,
      void **, cusparseIndexType_t *, cusparseIndexType_t *,
      cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCscGet");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, &rows, &cols, &nnz, &cscColOffsets,
                          &cscRowInd, &cscValues, &cscColOffsetsType,
                          &cscRowIndType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cscColOffsets, sizeof(void *)) < 0 ||
      rpc_write(conn, &cscRowInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &cscValues, sizeof(void *)) < 0 ||
      rpc_write(conn, &cscColOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &cscRowIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstCscGet(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  const void *cscColOffsets{};
  cscColOffsets = {};
  const void *cscRowInd{};
  cscRowInd = {};
  const void *cscValues{};
  cscValues = {};
  cusparseIndexType_t cscColOffsetsType{};
  cscColOffsetsType = {};
  cusparseIndexType_t cscRowIndType{};
  cscRowIndType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t, int64_t *, int64_t *, int64_t *, const void **,
      const void **, const void **, cusparseIndexType_t *,
      cusparseIndexType_t *, cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstCscGet");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, &rows, &cols, &nnz, &cscColOffsets,
                          &cscRowInd, &cscValues, &cscColOffsetsType,
                          &cscRowIndType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cscColOffsets, sizeof(const void *)) < 0 ||
      rpc_write(conn, &cscRowInd, sizeof(const void *)) < 0 ||
      rpc_write(conn, &cscValues, sizeof(const void *)) < 0 ||
      rpc_write(conn, &cscColOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &cscRowIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCsrSetPointers(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  void *csrRowOffsets;
  void *csrColInd;
  void *csrValues;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseSpMatDescr_t, void *, void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &csrValues, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsrSetPointers");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, csrRowOffsets, csrColInd, csrValues);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCscSetPointers(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  void *cscColOffsets;
  void *cscRowInd;
  void *cscValues;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseSpMatDescr_t, void *, void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &cscColOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscRowInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &cscValues, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCscSetPointers");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(spMatDescr, cscColOffsets, cscRowInd, cscValues);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12100
int handle_cusparseCreateBsr(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t brows;
  int64_t bcols;
  int64_t bnnz;
  int64_t rowBlockSize;
  int64_t colBlockSize;
  void *bsrRowOffsets;
  void *bsrColInd;
  void *bsrValues;
  cusparseIndexType_t bsrRowOffsetsType;
  cusparseIndexType_t bsrColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  cusparseOrder_t order;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t *, int64_t, int64_t, int64_t, int64_t, int64_t,
      void *, void *, void *, cusparseIndexType_t, cusparseIndexType_t,
      cusparseIndexBase_t, cudaDataType, cusparseOrder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &brows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bcols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bnnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &rowBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &colBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bsrRowOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &bsrColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &bsrValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &bsrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &bsrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBsr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, brows, bcols, bnnz,
                                    rowBlockSize, colBlockSize, bsrRowOffsets,
                                    bsrColInd, bsrValues, bsrRowOffsetsType,
                                    bsrColIndType, idxBase, valueType, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12100
int handle_cusparseCreateConstBsr(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t brows;
  int64_t bcols;
  int64_t bnnz;
  int64_t rowBlockDim;
  int64_t colBlockDim;
  const void *bsrRowOffsets;
  const void *bsrColInd;
  const void *bsrValues;
  cusparseIndexType_t bsrRowOffsetsType;
  cusparseIndexType_t bsrColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  cusparseOrder_t order;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t *, int64_t, int64_t, int64_t, int64_t, int64_t,
      const void *, const void *, const void *, cusparseIndexType_t,
      cusparseIndexType_t, cusparseIndexBase_t, cudaDataType, cusparseOrder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &brows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bcols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bnnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &rowBlockDim, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &colBlockDim, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bsrRowOffsets, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bsrColInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bsrValues, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bsrRowOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &bsrColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstBsr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, brows, bcols, bnnz,
                                    rowBlockDim, colBlockDim, bsrRowOffsets,
                                    bsrColInd, bsrValues, bsrRowOffsetsType,
                                    bsrColIndType, idxBase, valueType, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCreateCoo(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  void *cooRowInd;
  void *cooColInd;
  void *cooValues;
  cusparseIndexType_t cooIdxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t *, int64_t, int64_t, int64_t, void *, void *, void *,
      cusparseIndexType_t, cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cooRowInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCoo");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&spMatDescr, rows, cols, nnz, cooRowInd, cooColInd,
                          cooValues, cooIdxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstCoo(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  const void *cooRowInd;
  const void *cooColInd;
  const void *cooValues;
  cusparseIndexType_t cooIdxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t *, int64_t, int64_t, int64_t, const void *,
      const void *, const void *, cusparseIndexType_t, cusparseIndexBase_t,
      cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cooRowInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cooColInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cooValues, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cooIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstCoo");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&spMatDescr, rows, cols, nnz, cooRowInd, cooColInd,
                          cooValues, cooIdxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCooGet(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  void *cooRowInd{};
  cooRowInd = {};
  void *cooColInd{};
  cooColInd = {};
  void *cooValues{};
  cooValues = {};
  cusparseIndexType_t idxType{};
  idxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t, int64_t *, int64_t *, int64_t *, void **, void **,
      void **, cusparseIndexType_t *, cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCooGet");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(spMatDescr, &rows, &cols, &nnz, &cooRowInd, &cooColInd,
                         &cooValues, &idxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cooRowInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &cooColInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &cooValues, sizeof(void *)) < 0 ||
      rpc_write(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstCooGet(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  const void *cooRowInd{};
  cooRowInd = {};
  const void *cooColInd{};
  cooColInd = {};
  const void *cooValues{};
  cooValues = {};
  cusparseIndexType_t idxType{};
  idxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t, int64_t *, int64_t *, int64_t *, const void **,
      const void **, const void **, cusparseIndexType_t *,
      cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstCooGet");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(spMatDescr, &rows, &cols, &nnz, &cooRowInd, &cooColInd,
                         &cooValues, &idxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cooRowInd, sizeof(const void *)) < 0 ||
      rpc_write(conn, &cooColInd, sizeof(const void *)) < 0 ||
      rpc_write(conn, &cooValues, sizeof(const void *)) < 0 ||
      rpc_write(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCooSetPointers(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  void *cooRows;
  void *cooColumns;
  void *cooValues;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseSpMatDescr_t, void *, void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &cooRows, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooColumns, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooValues, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCooSetPointers");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, cooRows, cooColumns, cooValues);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseCreateBlockedEll(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t ellBlockSize;
  int64_t ellCols;
  void *ellColInd;
  void *ellValue;
  cusparseIndexType_t ellIdxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t *, int64_t, int64_t, int64_t, int64_t, void *,
      void *, cusparseIndexType_t, cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellCols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &ellValue, sizeof(void *)) < 0 ||
      rpc_read(conn, &ellIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateBlockedEll");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&spMatDescr, rows, cols, ellBlockSize, ellCols,
                          ellColInd, ellValue, ellIdxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstBlockedEll(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t ellBlockSize;
  int64_t ellCols;
  const void *ellColInd;
  const void *ellValue;
  cusparseIndexType_t ellIdxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstSpMatDescr_t *, int64_t,
                                    int64_t, int64_t, int64_t, const void *,
                                    const void *, cusparseIndexType_t,
                                    cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellBlockSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellCols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ellColInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ellValue, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ellIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstBlockedEll");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&spMatDescr, rows, cols, ellBlockSize, ellCols,
                          ellColInd, ellValue, ellIdxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseBlockedEllGet(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t ellBlockSize{};
  ellBlockSize = {};
  int64_t ellCols{};
  ellCols = {};
  void *ellColInd{};
  ellColInd = {};
  void *ellValue{};
  ellValue = {};
  cusparseIndexType_t ellIdxType{};
  ellIdxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t, int64_t *, int64_t *, int64_t *, int64_t *, void **,
      void **, cusparseIndexType_t *, cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseBlockedEllGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, &rows, &cols, &ellBlockSize,
                                    &ellCols, &ellColInd, &ellValue,
                                    &ellIdxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellBlockSize, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellCols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellColInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &ellValue, sizeof(void *)) < 0 ||
      rpc_write(conn, &ellIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstBlockedEllGet(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t ellBlockSize{};
  ellBlockSize = {};
  int64_t ellCols{};
  ellCols = {};
  const void *ellColInd{};
  ellColInd = {};
  const void *ellValue{};
  ellValue = {};
  cusparseIndexType_t ellIdxType{};
  ellIdxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t, int64_t *, int64_t *, int64_t *, int64_t *,
      const void **, const void **, cusparseIndexType_t *,
      cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstBlockedEllGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, &rows, &cols, &ellBlockSize,
                                    &ellCols, &ellColInd, &ellValue,
                                    &ellIdxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellBlockSize, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellCols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ellColInd, sizeof(const void *)) < 0 ||
      rpc_write(conn, &ellValue, sizeof(const void *)) < 0 ||
      rpc_write(conn, &ellIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12100
int handle_cusparseCreateSlicedEll(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  int64_t sellValuesSize;
  int64_t sliceSize;
  void *sellSliceOffsets;
  void *sellColInd;
  void *sellValues;
  cusparseIndexType_t sellSliceOffsetsType;
  cusparseIndexType_t sellColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t *, int64_t, int64_t, int64_t, int64_t, int64_t,
      void *, void *, void *, cusparseIndexType_t, cusparseIndexType_t,
      cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sellValuesSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sliceSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sellSliceOffsets, sizeof(void *)) < 0 ||
      rpc_read(conn, &sellColInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &sellValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &sellSliceOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &sellColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateSlicedEll");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&spMatDescr, rows, cols, nnz, sellValuesSize, sliceSize,
               sellSliceOffsets, sellColInd, sellValues, sellSliceOffsetsType,
               sellColIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12102
int handle_cusparseCreateConstSlicedEll(conn_t *conn) {
  cusparseConstSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  int64_t sellValuesSize;
  int64_t sliceSize;
  const void *sellSliceOffsets;
  const void *sellColInd;
  const void *sellValues;
  cusparseIndexType_t sellSliceOffsetsType;
  cusparseIndexType_t sellColIndType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseConstSpMatDescr_t *, int64_t, int64_t, int64_t, int64_t, int64_t,
      const void *, const void *, const void *, cusparseIndexType_t,
      cusparseIndexType_t, cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sellValuesSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sliceSize, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &sellSliceOffsets, sizeof(const void *)) < 0 ||
      rpc_read(conn, &sellColInd, sizeof(const void *)) < 0 ||
      rpc_read(conn, &sellValues, sizeof(const void *)) < 0 ||
      rpc_read(conn, &sellSliceOffsetsType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &sellColIndType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstSlicedEll");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&spMatDescr, rows, cols, nnz, sellValuesSize, sliceSize,
               sellSliceOffsets, sellColInd, sellValues, sellSliceOffsetsType,
               sellColIndType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseCreateDnMat(conn_t *conn) {
  cusparseDnMatDescr_t dnMatDescr{};
  dnMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t ld;
  void *values;
  cudaDataType valueType;
  cusparseOrder_t order;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseDnMatDescr_t *, int64_t, int64_t, int64_t,
                           void *, cudaDataType, cusparseOrder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateDnMat");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(&dnMatDescr, rows, cols, ld, values, valueType, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dnMatDescr, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseCreateConstDnMat(conn_t *conn) {
  cusparseConstDnMatDescr_t dnMatDescr{};
  dnMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t ld;
  const void *values;
  cudaDataType valueType;
  cusparseOrder_t order;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnMatDescr_t *, int64_t,
                                    int64_t, int64_t, const void *,
                                    cudaDataType, cusparseOrder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &values, sizeof(const void *)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateConstDnMat");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(&dnMatDescr, rows, cols, ld, values, valueType, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dnMatDescr, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDestroyDnMat(conn_t *conn) {
  cusparseConstDnMatDescr_t dnMatDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnMatDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyDnMat");
  return_value = fn == nullptr ? function_not_found() : fn(dnMatDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnMatGet(conn_t *conn) {
  cusparseDnMatDescr_t dnMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t ld{};
  ld = {};
  void *values{};
  values = {};
  cudaDataType type{};
  type = {};
  cusparseOrder_t order{};
  order = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnMatDescr_t, int64_t *, int64_t *,
                                    int64_t *, void **, cudaDataType *,
                                    cusparseOrder_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseDnMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnMatGet");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(dnMatDescr, &rows, &cols, &ld, &values, &type, &order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &type, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &order, sizeof(cusparseOrder_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstDnMatGet(conn_t *conn) {
  cusparseConstDnMatDescr_t dnMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t ld{};
  ld = {};
  const void *values{};
  values = {};
  cudaDataType type{};
  type = {};
  cusparseOrder_t order{};
  order = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnMatDescr_t, int64_t *,
                                    int64_t *, int64_t *, const void **,
                                    cudaDataType *, cusparseOrder_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstDnMatGet");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(dnMatDescr, &rows, &cols, &ld, &values, &type, &order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &type, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &order, sizeof(cusparseOrder_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDnMatGetValues(conn_t *conn) {
  cusparseDnMatDescr_t dnMatDescr;
  void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnMatDescr_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseDnMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnMatGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnMatDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseConstDnMatGetValues(conn_t *conn) {
  cusparseConstDnMatDescr_t dnMatDescr;
  const void *values{};
  values = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseConstDnMatDescr_t, const void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstDnMatGetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnMatDescr, &values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &values, sizeof(const void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseDnMatSetValues(conn_t *conn) {
  cusparseDnMatDescr_t dnMatDescr;
  void *values;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnMatDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &values, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnMatSetValues");
  return_value = fn == nullptr ? function_not_found() : fn(dnMatDescr, values);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnMatSetStridedBatch(conn_t *conn) {
  cusparseDnMatDescr_t dnMatDescr;
  int batchCount;
  int64_t batchStride;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseDnMatDescr_t, int, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_read(conn, &batchStride, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnMatSetStridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dnMatDescr, batchCount, batchStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDnMatGetStridedBatch(conn_t *conn) {
  cusparseConstDnMatDescr_t dnMatDescr;
  int batchCount{};
  batchCount = {};
  int64_t batchStride{};
  batchStride = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseConstDnMatDescr_t, int *, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dnMatDescr, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDnMatGetStridedBatch");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dnMatDescr, &batchCount, &batchStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &batchCount, sizeof(int)) < 0 ||
      rpc_write(conn, &batchStride, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseAxpby(conn_t *conn) {
  cusparseHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpVecDescr_t vecX;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnVecDescr_t vecY;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, const void *,
                                    cusparseConstSpVecDescr_t, const void *,
                                    cusparseDnVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &vecX, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseAxpby");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               vecX,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               vecY);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseGather(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstDnVecDescr_t vecY;
  cusparseSpVecDescr_t vecX;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstDnVecDescr_t,
                                    cusparseSpVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseSpVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseGather");
  return_value = fn == nullptr ? function_not_found() : fn(handle, vecY, vecX);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseScatter(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstSpVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstSpVecDescr_t,
                                    cusparseDnVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScatter");
  return_value = fn == nullptr ? function_not_found() : fn(handle, vecX, vecY);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseRot(conn_t *conn) {
  cusparseHandle_t handle;
  uint32_t c_coeff_width;
  alignas(16) unsigned char c_coeff_value[64];
  std::vector<unsigned char> c_coeff_heap;
  const void *c_coeff = nullptr;
  uint32_t s_coeff_width;
  alignas(16) unsigned char s_coeff_value[64];
  std::vector<unsigned char> s_coeff_heap;
  const void *s_coeff = nullptr;
  cusparseSpVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, const void *, const void *,
                           cusparseSpVecDescr_t, cusparseDnVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &c_coeff_width, sizeof(c_coeff_width)) < 0 ||
      (c_coeff_width == 0 && rpc_read(conn, &c_coeff, sizeof(c_coeff)) < 0) ||
      (c_coeff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_coeff_heap, c_coeff_value,
                                   sizeof(c_coeff_value), c_coeff_width),
                c_coeff_width) < 0) ||
      rpc_read(conn, &s_coeff_width, sizeof(s_coeff_width)) < 0 ||
      (s_coeff_width == 0 && rpc_read(conn, &s_coeff, sizeof(s_coeff)) < 0) ||
      (s_coeff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_coeff_heap, s_coeff_value,
                                   sizeof(s_coeff_value), s_coeff_width),
                s_coeff_width) < 0) ||
      rpc_read(conn, &vecX, sizeof(cusparseSpVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseRot");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               c_coeff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         c_coeff_heap, c_coeff_value, sizeof(c_coeff_value),
                         c_coeff_width))
                   : c_coeff,
               s_coeff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         s_coeff_heap, s_coeff_value, sizeof(s_coeff_value),
                         s_coeff_width))
                   : s_coeff,
               vecX, vecY);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpVV_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opX;
  cusparseConstSpVecDescr_t vecX;
  cusparseConstDnVecDescr_t vecY;
  const void *result;
  cudaDataType computeType;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseConstSpVecDescr_t,
      cusparseConstDnVecDescr_t, const void *, cudaDataType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opX, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &result, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVV_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, opX, vecX, vecY, result,
                                    computeType, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpVV(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opX;
  cusparseConstSpVecDescr_t vecX;
  cusparseConstDnVecDescr_t vecY;
  uint32_t result_width;
  alignas(16) unsigned char result_value[64];
  std::vector<unsigned char> result_heap;
  void *result = nullptr;
  cudaDataType computeType;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseConstSpVecDescr_t,
      cusparseConstDnVecDescr_t, void *, cudaDataType, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opX, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstSpVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &result_width, sizeof(result_width)) < 0 ||
      (result_width == 0 && rpc_read(conn, &result, sizeof(result)) < 0) ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpVV");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opX, vecX, vecY,
               result_width != 0 ? reinterpret_cast<void *>(lupine_scalar_slot(
                                       result_heap, result_value,
                                       sizeof(result_value), result_width))
                                 : result,
               computeType, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (result_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(result_heap, result_value,
                                    sizeof(result_value), result_width),
                 result_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSparseToDense_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstSpMatDescr_t matA;
  cusparseDnMatDescr_t matB;
  cusparseSparseToDenseAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstSpMatDescr_t,
                                    cusparseDnMatDescr_t,
                                    cusparseSparseToDenseAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSparseToDenseAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSparseToDense_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matA, matB, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSparseToDense(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstSpMatDescr_t matA;
  cusparseDnMatDescr_t matB;
  cusparseSparseToDenseAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstSpMatDescr_t,
                                    cusparseDnMatDescr_t,
                                    cusparseSparseToDenseAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSparseToDenseAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSparseToDense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matA, matB, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDenseToSparse_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstDnMatDescr_t matA;
  cusparseSpMatDescr_t matB;
  cusparseDenseToSparseAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstDnMatDescr_t,
                                    cusparseSpMatDescr_t,
                                    cusparseDenseToSparseAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseDenseToSparseAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDenseToSparse_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matA, matB, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDenseToSparse_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstDnMatDescr_t matA;
  cusparseSpMatDescr_t matB;
  cusparseDenseToSparseAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstDnMatDescr_t,
                                    cusparseSpMatDescr_t,
                                    cusparseDenseToSparseAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseDenseToSparseAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDenseToSparse_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matA, matB, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseDenseToSparse_convert(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseConstDnMatDescr_t matA;
  cusparseSpMatDescr_t matB;
  cusparseDenseToSparseAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseConstDnMatDescr_t,
                                    cusparseSpMatDescr_t,
                                    cusparseDenseToSparseAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseDenseToSparseAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDenseToSparse_convert");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matA, matB, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMV(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpMVAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t, const void *,
      cusparseDnVecDescr_t, cudaDataType, cusparseSpMVAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMVAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMV");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               vecY, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMV_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpMVAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t, const void *,
      cusparseDnVecDescr_t, cudaDataType, cusparseSpMVAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMVAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMV_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               vecY, computeType, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12300
int handle_cusparseSpMV_preprocess(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpMVAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t, const void *,
      cusparseDnVecDescr_t, cudaDataType, cusparseSpMVAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMVAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMV_preprocess");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               vecY, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpMVOp_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  cusparseDnVecDescr_t vecZ;
  cudaDataType computeType;
  cusparseSpMVOpAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseConstSpMatDescr_t,
      cusparseConstDnVecDescr_t, cusparseDnVecDescr_t, cusparseDnVecDescr_t,
      cudaDataType, cusparseSpMVOpAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecZ, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMVOpAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, opA, matA, vecX, vecY, vecZ,
                                    computeType, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpMVOp_createDescr(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpMVOpDescr_t desc{};
  desc = {};
  cusparseOperation_t opA;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  cusparseDnVecDescr_t vecZ;
  cudaDataType computeType;
  cusparseSpMVOpAlg_t alg;
  void *buffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseSpMVOpDescr_t *, cusparseOperation_t,
      cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t,
      cusparseDnVecDescr_t, cusparseDnVecDescr_t, cudaDataType,
      cusparseSpMVOpAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecZ, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMVOpAlg_t)) < 0 ||
      rpc_read(conn, &buffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_createDescr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, &desc, opA, matA, vecX, vecY, vecZ,
                                    computeType, alg, buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &desc, sizeof(cusparseSpMVOpDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12702
int handle_cusparseSpMVOp_destroyDescr(conn_t *conn) {
  cusparseSpMVOpDescr_t desc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMVOpDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(cusparseSpMVOpDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_destroyDescr");
  return_value = fn == nullptr ? function_not_found() : fn(desc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12702
int handle_cusparseSpMVOp_createPlan(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpMVOpDescr_t desc;
  cusparseSpMVOpPlan_t plan{};
  plan = {};
  size_t codeSize;
  void *code = nullptr;
  size_t code_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseSpMVOpDescr_t,
                           cusparseSpMVOpPlan_t *, const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &desc, sizeof(cusparseSpMVOpDescr_t)) < 0 ||
      rpc_read(conn, &codeSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  code_size = codeSize;
  code = (void *)malloc(code_size);
  if (code_size != 0 && code == nullptr)
    goto ERROR_0;
  if ((code_size != 0 && rpc_read(conn, code, code_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_createPlan");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, desc, &plan,
                                    (codeSize == 0 ? nullptr : code), codeSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cusparseSpMVOpPlan_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)code);
  return 0;
ERROR_0:
  free((void *)code);
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12702
int handle_cusparseSpMVOp_destroyPlan(conn_t *conn) {
  cusparseSpMVOpPlan_t plan;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMVOpPlan_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cusparseSpMVOpPlan_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_destroyPlan");
  return_value = fn == nullptr ? function_not_found() : fn(plan);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12702
int handle_cusparseSpMVOp_setGlobalUserData(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpMVOpPlan_t plan;
  const char *global_data_name = nullptr;
  std::size_t global_data_name_len;
  size_t data_size;
  void *input_data = nullptr;
  size_t input_data_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseSpMVOpPlan_t,
                                    const char *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &plan, sizeof(cusparseSpMVOpPlan_t)) < 0 ||
      rpc_read(conn, &global_data_name_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  global_data_name = (const char *)malloc(global_data_name_len);
  if ((global_data_name_len != 0 && global_data_name == nullptr) ||
      rpc_read(conn, (void *)global_data_name, global_data_name_len) < 0 ||
      rpc_read(conn, &data_size, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  input_data_size = data_size;
  input_data = (void *)malloc(input_data_size);
  if (input_data_size != 0 && input_data == nullptr)
    goto ERROR_0;
  if ((input_data_size != 0 &&
       rpc_read(conn, input_data, input_data_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp_setGlobalUserData");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, plan, global_data_name,
                          (data_size == 0 ? nullptr : input_data), data_size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)input_data);
  free((void *)global_data_name);
  return 0;
ERROR_0:
  free((void *)input_data);
  free((void *)global_data_name);
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12702
int handle_cusparseSpMVOp(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpMVOpPlan_t plan;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseConstDnVecDescr_t vecX;
  cusparseConstDnVecDescr_t vecY;
  cusparseDnVecDescr_t vecZ;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseSpMVOpPlan_t, const void *,
                           const void *, cusparseConstDnVecDescr_t,
                           cusparseConstDnVecDescr_t, cusparseDnVecDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &plan, sizeof(cusparseSpMVOpPlan_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecZ, sizeof(cusparseDnVecDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMVOp");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, plan,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               vecX, vecY, vecZ);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpSV_createDescr(conn_t *conn) {
  cusparseSpSVDescr_t descr{};
  descr = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpSVDescr_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_createDescr");
  return_value = fn == nullptr ? function_not_found() : fn(&descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descr, sizeof(cusparseSpSVDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSV_destroyDescr(conn_t *conn) {
  cusparseSpSVDescr_t descr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpSVDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descr, sizeof(cusparseSpSVDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_destroyDescr");
  return_value = fn == nullptr ? function_not_found() : fn(descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSV_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpSVAlg_t alg;
  cusparseSpSVDescr_t spsvDescr;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, const void *,
                           cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t,
                           cusparseDnVecDescr_t, cudaDataType,
                           cusparseSpSVAlg_t, cusparseSpSVDescr_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSVAlg_t)) < 0 ||
      rpc_read(conn, &spsvDescr, sizeof(cusparseSpSVDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX, vecY, computeType, alg, spsvDescr, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSV_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpSVAlg_t alg;
  cusparseSpSVDescr_t spsvDescr;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, const void *,
                           cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t,
                           cusparseDnVecDescr_t, cudaDataType,
                           cusparseSpSVAlg_t, cusparseSpSVDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSVAlg_t)) < 0 ||
      rpc_read(conn, &spsvDescr, sizeof(cusparseSpSVDescr_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX, vecY, computeType, alg, spsvDescr, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSV_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnVecDescr_t vecX;
  cusparseDnVecDescr_t vecY;
  cudaDataType computeType;
  cusparseSpSVAlg_t alg;
  cusparseSpSVDescr_t spsvDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, cusparseOperation_t, const void *,
                           cusparseConstSpMatDescr_t, cusparseConstDnVecDescr_t,
                           cusparseDnVecDescr_t, cudaDataType,
                           cusparseSpSVAlg_t, cusparseSpSVDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &vecX, sizeof(cusparseConstDnVecDescr_t)) < 0 ||
      rpc_read(conn, &vecY, sizeof(cusparseDnVecDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSVAlg_t)) < 0 ||
      rpc_read(conn, &spsvDescr, sizeof(cusparseSpSVDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, vecX, vecY, computeType, alg, spsvDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12100
int handle_cusparseSpSV_updateMatrix(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpSVDescr_t spsvDescr;
  void *newValues;
  cusparseSpSVUpdate_t updatePart;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseSpSVDescr_t,
                                    void *, cusparseSpSVUpdate_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &spsvDescr, sizeof(cusparseSpSVDescr_t)) < 0 ||
      rpc_read(conn, &newValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &updatePart, sizeof(cusparseSpSVUpdate_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSV_updateMatrix");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, spsvDescr, newValues, updatePart);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpSM_createDescr(conn_t *conn) {
  cusparseSpSMDescr_t descr{};
  descr = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpSMDescr_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_createDescr");
  return_value = fn == nullptr ? function_not_found() : fn(&descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descr, sizeof(cusparseSpSMDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSM_destroyDescr(conn_t *conn) {
  cusparseSpSMDescr_t descr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpSMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descr, sizeof(cusparseSpSMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_destroyDescr");
  return_value = fn == nullptr ? function_not_found() : fn(descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSM_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpSMAlg_t alg;
  cusparseSpSMDescr_t spsmDescr;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpSMAlg_t,
      cusparseSpSMDescr_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSMAlg_t)) < 0 ||
      rpc_read(conn, &spsmDescr, sizeof(cusparseSpSMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB, matC, computeType, alg, spsmDescr, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSM_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpSMAlg_t alg;
  cusparseSpSMDescr_t spsmDescr;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpSMAlg_t,
      cusparseSpSMDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSMAlg_t)) < 0 ||
      rpc_read(conn, &spsmDescr, sizeof(cusparseSpSMDescr_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB, matC, computeType, alg, spsmDescr, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpSM_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpSMAlg_t alg;
  cusparseSpSMDescr_t spsmDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpSMAlg_t,
      cusparseSpSMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpSMAlg_t)) < 0 ||
      rpc_read(conn, &spsmDescr, sizeof(cusparseSpSMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB, matC, computeType, alg, spsmDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12300
int handle_cusparseSpSM_updateMatrix(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpSMDescr_t spsmDescr;
  void *newValues;
  cusparseSpSMUpdate_t updatePart;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, cusparseSpSMDescr_t,
                                    void *, cusparseSpSMUpdate_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &spsmDescr, sizeof(cusparseSpSMDescr_t)) < 0 ||
      rpc_read(conn, &newValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &updatePart, sizeof(cusparseSpSMUpdate_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpSM_updateMatrix");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, spsmDescr, newValues, updatePart);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpMM_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpMMAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpMMAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMMAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMM_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMM_preprocess(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpMMAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpMMAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMMAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMM_preprocess");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMM(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpMMAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpMMAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMMAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMM");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMM_createDescr(conn_t *conn) {
  cusparseSpGEMMDescr_t descr{};
  descr = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpGEMMDescr_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_createDescr");
  return_value = fn == nullptr ? function_not_found() : fn(&descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMM_destroyDescr(conn_t *conn) {
  cusparseSpGEMMDescr_t descr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpGEMMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descr, sizeof(cusparseSpGEMMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_destroyDescr");
  return_value = fn == nullptr ? function_not_found() : fn(descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMM_workEstimation(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  size_t bufferSize1{};
  void *externalBuffer1;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEMMAlg_t,
      cusparseSpGEMMDescr_t, size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &bufferSize1, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer1, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_workEstimation");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, spgemmDescr, &bufferSize1,
               externalBuffer1);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize1, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12000
int handle_cusparseSpGEMM_getNumProducts(conn_t *conn) {
  cusparseSpGEMMDescr_t spgemmDescr;
  int64_t num_prods{};
  num_prods = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpGEMMDescr_t, int64_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_getNumProducts");
  return_value =
      fn == nullptr ? function_not_found() : fn(spgemmDescr, &num_prods);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_prods, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12000
int handle_cusparseSpGEMM_estimateMemory(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  float chunk_fraction;
  size_t bufferSize3{};
  void *externalBuffer3;
  size_t *bufferSize2_null_check;
  size_t bufferSize2;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEMMAlg_t,
      cusparseSpGEMMDescr_t, float, size_t *, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &chunk_fraction, sizeof(float)) < 0 ||
      rpc_read(conn, &bufferSize3, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer3, sizeof(void *)) < 0 ||
      rpc_read(conn, &bufferSize2_null_check, sizeof(size_t *)) < 0 ||
      (bufferSize2_null_check &&
       rpc_read(conn, &bufferSize2, sizeof(size_t)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_estimateMemory");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, spgemmDescr, chunk_fraction,
               &bufferSize3, externalBuffer3,
               bufferSize2_null_check ? &bufferSize2 : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize3, sizeof(size_t)) < 0 ||
      rpc_write(conn, &bufferSize2_null_check, sizeof(size_t *)) < 0 ||
      (bufferSize2_null_check &&
       rpc_write(conn, &bufferSize2, sizeof(size_t)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSpGEMM_compute(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  size_t bufferSize2{};
  void *externalBuffer2;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEMMAlg_t,
      cusparseSpGEMMDescr_t, size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &bufferSize2, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer2, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_compute");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, spgemmDescr, &bufferSize2,
               externalBuffer2);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize2, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMM_copy(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEMMAlg_t,
      cusparseSpGEMMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMM_copy");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, spgemmDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMMreuse_workEstimation(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  size_t bufferSize1{};
  void *externalBuffer1;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t,
      cusparseSpMatDescr_t, cusparseSpGEMMAlg_t, cusparseSpGEMMDescr_t,
      size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &bufferSize1, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer1, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMMreuse_workEstimation");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, opA, opB, matA, matB, matC, alg,
                                    spgemmDescr, &bufferSize1, externalBuffer1);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize1, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMMreuse_nnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  size_t bufferSize2{};
  void *externalBuffer2;
  size_t bufferSize3{};
  void *externalBuffer3;
  size_t bufferSize4{};
  void *externalBuffer4;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t,
      cusparseSpMatDescr_t, cusparseSpGEMMAlg_t, cusparseSpGEMMDescr_t,
      size_t *, void *, size_t *, void *, size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &bufferSize2, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer2, sizeof(void *)) < 0 ||
      rpc_read(conn, &bufferSize3, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer3, sizeof(void *)) < 0 ||
      rpc_read(conn, &bufferSize4, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer4, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMMreuse_nnz");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, opA, opB, matA, matB, matC, alg, spgemmDescr,
                          &bufferSize2, externalBuffer2, &bufferSize3,
                          externalBuffer3, &bufferSize4, externalBuffer4);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize2, sizeof(size_t)) < 0 ||
      rpc_write(conn, &bufferSize3, sizeof(size_t)) < 0 ||
      rpc_write(conn, &bufferSize4, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMMreuse_copy(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  size_t bufferSize5{};
  void *externalBuffer5;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t,
      cusparseSpMatDescr_t, cusparseSpGEMMAlg_t, cusparseSpGEMMDescr_t,
      size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 ||
      rpc_read(conn, &bufferSize5, sizeof(size_t)) < 0 ||
      rpc_read(conn, &externalBuffer5, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMMreuse_copy");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, opA, opB, matA, matB, matC, alg,
                                    spgemmDescr, &bufferSize5, externalBuffer5);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize5, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpGEMMreuse_compute(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstSpMatDescr_t matA;
  cusparseConstSpMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEMMAlg_t alg;
  cusparseSpGEMMDescr_t spgemmDescr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstSpMatDescr_t, cusparseConstSpMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEMMAlg_t,
      cusparseSpGEMMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEMMAlg_t)) < 0 ||
      rpc_read(conn, &spgemmDescr, sizeof(cusparseSpGEMMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEMMreuse_compute");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, spgemmDescr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpGEAM_createDescr(conn_t *conn) {
  cusparseSpGEAMDescr_t descr{};
  descr = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpGEAMDescr_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEAM_createDescr");
  return_value = fn == nullptr ? function_not_found() : fn(&descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descr, sizeof(cusparseSpGEAMDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpGEAM_destroyDescr(conn_t *conn) {
  cusparseSpGEAMDescr_t descr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpGEAMDescr_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descr, sizeof(cusparseSpGEAMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEAM_destroyDescr");
  return_value = fn == nullptr ? function_not_found() : fn(descr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpGEAM_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseSpMatDescr_t matA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEAMAlg_t alg;
  cusparseSpGEAMDescr_t spgeamDescr;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseSpMatDescr_t, const void *, cusparseSpMatDescr_t,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEAMAlg_t,
      cusparseSpGEAMDescr_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEAMAlg_t)) < 0 ||
      rpc_read(conn, &spgeamDescr, sizeof(cusparseSpGEAMDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEAM_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matB, matC, computeType, alg, spgeamDescr, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpGEAM_nnz(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseSpMatDescr_t matA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEAMAlg_t alg;
  cusparseSpGEAMDescr_t spgeamDescr;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseSpMatDescr_t, const void *, cusparseSpMatDescr_t,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEAMAlg_t,
      cusparseSpGEAMDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEAMAlg_t)) < 0 ||
      rpc_read(conn, &spgeamDescr, sizeof(cusparseSpGEAMDescr_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEAM_nnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matB, matC, computeType, alg, spgeamDescr, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION >= 12801
int handle_cusparseSpGEAM(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseSpMatDescr_t matA;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matB;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpGEAMAlg_t alg;
  cusparseSpGEAMDescr_t spgeamDescr;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseSpMatDescr_t, const void *, cusparseSpMatDescr_t,
      cusparseSpMatDescr_t, cudaDataType, cusparseSpGEAMAlg_t,
      cusparseSpGEAMDescr_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matB, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpGEAMAlg_t)) < 0 ||
      rpc_read(conn, &spgeamDescr, sizeof(cusparseSpGEAMDescr_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpGEAM");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matB, matC, computeType, alg, spgeamDescr, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusparseSDDMM_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstDnMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSDDMMAlg_t alg;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstDnMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSDDMMAlg_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSDDMMAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSDDMM_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSDDMM_preprocess(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstDnMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSDDMMAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstDnMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSDDMMAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSDDMMAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSDDMM_preprocess");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSDDMM(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseConstDnMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  cusparseSDDMMAlg_t alg;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseConstDnMatDescr_t, cusparseConstDnMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, cusparseSDDMMAlg_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSDDMMAlg_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSDDMM");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, alg, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMMOp_createPlan(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseSpMMOpPlan_t plan{};
  plan = {};
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  cusparseConstSpMatDescr_t matA;
  cusparseConstDnMatDescr_t matB;
  cusparseDnMatDescr_t matC;
  cudaDataType computeType;
  cusparseSpMMOpAlg_t alg;
  size_t addOperationBufferSize;
  void *addOperationLtoirBuffer = nullptr;
  size_t addOperationLtoirBuffer_size;
  uint8_t addOperationLtoirBuffer_null = 0;
  size_t mulOperationBufferSize;
  void *mulOperationLtoirBuffer = nullptr;
  size_t mulOperationLtoirBuffer_size;
  uint8_t mulOperationLtoirBuffer_null = 0;
  size_t epilogueBufferSize;
  void *epilogueLtoirBuffer = nullptr;
  size_t epilogueLtoirBuffer_size;
  uint8_t epilogueLtoirBuffer_null = 0;
  size_t SpMMWorkspaceSize{};
  SpMMWorkspaceSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseSpMMOpPlan_t *, cusparseOperation_t,
      cusparseOperation_t, cusparseConstSpMatDescr_t, cusparseConstDnMatDescr_t,
      cusparseDnMatDescr_t, cudaDataType, cusparseSpMMOpAlg_t, const void *,
      size_t, const void *, size_t, const void *, size_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(cusparseConstSpMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseConstDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matC, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseSpMMOpAlg_t)) < 0 ||
      rpc_read(conn, &addOperationBufferSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &addOperationLtoirBuffer_null, sizeof(uint8_t)) < 0 ||
      false)
    goto ERROR_0;
  addOperationLtoirBuffer_size = addOperationBufferSize;
  if (!addOperationLtoirBuffer_null) {
    addOperationLtoirBuffer = (void *)malloc(addOperationLtoirBuffer_size);
    if (addOperationLtoirBuffer_size != 0 && addOperationLtoirBuffer == nullptr)
      goto ERROR_0;
  }
  if ((!addOperationLtoirBuffer_null && addOperationLtoirBuffer_size != 0 &&
       rpc_read(conn, addOperationLtoirBuffer, addOperationLtoirBuffer_size) <
           0) ||
      rpc_read(conn, &mulOperationBufferSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &mulOperationLtoirBuffer_null, sizeof(uint8_t)) < 0 ||
      false)
    goto ERROR_0;
  mulOperationLtoirBuffer_size = mulOperationBufferSize;
  if (!mulOperationLtoirBuffer_null) {
    mulOperationLtoirBuffer = (void *)malloc(mulOperationLtoirBuffer_size);
    if (mulOperationLtoirBuffer_size != 0 && mulOperationLtoirBuffer == nullptr)
      goto ERROR_0;
  }
  if ((!mulOperationLtoirBuffer_null && mulOperationLtoirBuffer_size != 0 &&
       rpc_read(conn, mulOperationLtoirBuffer, mulOperationLtoirBuffer_size) <
           0) ||
      rpc_read(conn, &epilogueBufferSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &epilogueLtoirBuffer_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  epilogueLtoirBuffer_size = epilogueBufferSize;
  if (!epilogueLtoirBuffer_null) {
    epilogueLtoirBuffer = (void *)malloc(epilogueLtoirBuffer_size);
    if (epilogueLtoirBuffer_size != 0 && epilogueLtoirBuffer == nullptr)
      goto ERROR_0;
  }
  if ((!epilogueLtoirBuffer_null && epilogueLtoirBuffer_size != 0 &&
       rpc_read(conn, epilogueLtoirBuffer, epilogueLtoirBuffer_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMMOp_createPlan");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, &plan, opA, opB, matA, matB, matC, computeType, alg,
               (addOperationLtoirBuffer_null ? nullptr
                                             : addOperationLtoirBuffer),
               addOperationBufferSize,
               (mulOperationLtoirBuffer_null ? nullptr
                                             : mulOperationLtoirBuffer),
               mulOperationBufferSize,
               (epilogueLtoirBuffer_null ? nullptr : epilogueLtoirBuffer),
               epilogueBufferSize, &SpMMWorkspaceSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cusparseSpMMOpPlan_t)) < 0 ||
      rpc_write(conn, &SpMMWorkspaceSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)epilogueLtoirBuffer);
  free((void *)mulOperationLtoirBuffer);
  free((void *)addOperationLtoirBuffer);
  return 0;
ERROR_0:
  free((void *)epilogueLtoirBuffer);
  free((void *)mulOperationLtoirBuffer);
  free((void *)addOperationLtoirBuffer);
  return -1;
}

int handle_cusparseSpMMOp(conn_t *conn) {
  cusparseSpMMOpPlan_t plan;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMMOpPlan_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cusparseSpMMOpPlan_t)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMMOp");
  return_value =
      fn == nullptr ? function_not_found() : fn(plan, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseSpMMOp_destroyPlan(conn_t *conn) {
  cusparseSpMMOpPlan_t plan;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMMOpPlan_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cusparseSpMMOpPlan_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMMOp_destroyPlan");
  return_value = fn == nullptr ? function_not_found() : fn(plan);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSPARSE_VERSION < 12000
int handle_cusparseCaxpyi(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *xVal;
  const int *xInd;
  cuComplex *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const cuComplex *,
                                    const cuComplex *, const int *, cuComplex *,
                                    cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCaxpyi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsc2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *cscSortedValA;
  const int *cscSortedRowIndA;
  const int *cscSortedColPtrA;
  cuComplex *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsc2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, cscSortedValA,
                                    cscSortedRowIndA, cscSortedColPtrA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsr2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cuComplex *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsr2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrgemm2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const cuComplex *csrSortedValD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  cusparseMatDescr_t descrC;
  cuComplex *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  csrgemm2Info_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuComplex *,
      const cusparseMatDescr_t, int, const cuComplex *, const int *,
      const int *, const cusparseMatDescr_t, int, const cuComplex *,
      const int *, const int *, const cuComplex *, const cusparseMatDescr_t,
      int, const cuComplex *, const int *, const int *,
      const cusparseMatDescr_t, cuComplex *, const int *, int *,
      const csrgemm2Info_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValD, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const csrgemm2Info_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrgemm2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedValD, csrSortedRowPtrD, csrSortedColIndD,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC, info,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrgemm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  csrgemm2Info_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuComplex *,
      const cusparseMatDescr_t, int, const int *, const int *,
      const cusparseMatDescr_t, int, const int *, const int *,
      const cuComplex *, const cusparseMatDescr_t, int, const int *,
      const int *, csrgemm2Info_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrgemm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrgemm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedRowPtrA, csrSortedColIndA, descrB, nnzB,
               csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedRowPtrD, csrSortedColIndD, info,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const cuComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, const cuComplex *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsm2_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const cuComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  size_t pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, const cuComplex *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cuComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuComplex *, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, cuComplex *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, csrsv2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsv2_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, nnz, descrA,
                                    csrSortedValA, csrSortedRowPtrA,
                                    csrSortedColIndA, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      cuComplex *, const int *, const int *, csrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsv2_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCcsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  const cuComplex *f;
  cuComplex *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cuComplex *,
      const cusparseMatDescr_t, const cuComplex *, const int *, const int *,
      csrsv2Info_t, const cuComplex *, cuComplex *, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCcsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, info,
               f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCdense2csc(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *A;
  int lda;
  const int *nnzPerCol;
  cuComplex *cscSortedValA;
  int *cscSortedRowIndA;
  int *cscSortedColPtrA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      int, const int *, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerCol, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCdense2csc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerCol,
                          cscSortedValA, cscSortedRowIndA, cscSortedColPtrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCdense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuComplex *A;
  int lda;
  const int *nnzPerRow;
  cuComplex *csrSortedValA;
  int *csrSortedRowPtrA;
  int *csrSortedColIndA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      int, const int *, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCdense2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerRow,
                          csrSortedValA, csrSortedRowPtrA, csrSortedColIndA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCgemmi(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuComplex *alpha = nullptr;
  const cuComplex *A;
  int lda;
  const cuComplex *cscValB;
  const int *cscColPtrB;
  const int *cscRowIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuComplex *beta = nullptr;
  cuComplex *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int, int, int,
                                    const cuComplex *, const cuComplex *, int,
                                    const cuComplex *, const int *, const int *,
                                    const cuComplex *, cuComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &cscValB, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &cscColPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgemmi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, cscValB, cscColPtrB, cscRowIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuComplex *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCgthr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const cuComplex *y;
  cuComplex *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cuComplex *,
                           cuComplex *, const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgthr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCgthrz(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  cuComplex *y;
  cuComplex *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, cuComplex *, cuComplex *,
                           const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCgthrz");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseConstrainedGeMM(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseDnMatDescr_t matA;
  cusparseDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  void *externalBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseDnMatDescr_t, cusparseDnMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &externalBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstrainedGeMM");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, externalBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseConstrainedGeMM_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cusparseDnMatDescr_t matA;
  cusparseDnMatDescr_t matB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cusparseSpMatDescr_t matC;
  cudaDataType computeType;
  size_t bufferSize{};
  bufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, cusparseOperation_t, const void *,
      cusparseDnMatDescr_t, cusparseDnMatDescr_t, const void *,
      cusparseSpMatDescr_t, cudaDataType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &matA, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &matB, sizeof(cusparseDnMatDescr_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &matC, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseConstrainedGeMM_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opA, opB,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               matA, matB,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               matC, computeType, &bufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCooAoSGet(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int64_t rows{};
  rows = {};
  int64_t cols{};
  cols = {};
  int64_t nnz{};
  nnz = {};
  void *cooInd{};
  cooInd = {};
  void *cooValues{};
  cooValues = {};
  cusparseIndexType_t idxType{};
  idxType = {};
  cusparseIndexBase_t idxBase{};
  idxBase = {};
  cudaDataType valueType{};
  valueType = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t, int64_t *, int64_t *, int64_t *, void **, void **,
      cusparseIndexType_t *, cusparseIndexBase_t *, cudaDataType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCooAoSGet");
  return_value = fn == nullptr ? function_not_found()
                               : fn(spMatDescr, &rows, &cols, &nnz, &cooInd,
                                    &cooValues, &idxType, &idxBase, &valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &cooInd, sizeof(void *)) < 0 ||
      rpc_write(conn, &cooValues, sizeof(void *)) < 0 ||
      rpc_write(conn, &idxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_write(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_write(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCreateCooAoS(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr{};
  spMatDescr = {};
  int64_t rows;
  int64_t cols;
  int64_t nnz;
  void *cooInd;
  void *cooValues;
  cusparseIndexType_t cooIdxType;
  cusparseIndexBase_t idxBase;
  cudaDataType valueType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseSpMatDescr_t *, int64_t, int64_t, int64_t, void *, void *,
      cusparseIndexType_t, cusparseIndexBase_t, cudaDataType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cooInd, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooValues, sizeof(void *)) < 0 ||
      rpc_read(conn, &cooIdxType, sizeof(cusparseIndexType_t)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCooAoS");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&spMatDescr, rows, cols, nnz, cooInd,
                                    cooValues, cooIdxType, idxBase, valueType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCreateCsrgemm2Info(conn_t *conn) {
  csrgemm2Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrgemm2Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsrgemm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrgemm2Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCreateCsrsm2Info(conn_t *conn) {
  csrsm2Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrsm2Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsrsm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCreateCsrsv2Info(conn_t *conn) {
  csrsv2Info_t info{};
  info = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrsv2Info_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCreateCsrsv2Info");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCsctr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const cuComplex *xVal;
  const int *xInd;
  cuComplex *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cuComplex *,
                           const int *, cuComplex *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsctr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCsrmvEx(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseAlgMode_t alg;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphatype;
  cusparseMatDescr_t descrA;
  const void *csrValA;
  cudaDataType csrValAtype;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const void *x;
  cudaDataType xtype;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudaDataType betatype;
  void *y;
  cudaDataType ytype;
  cudaDataType executiontype;
  void *buffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseAlgMode_t, cusparseOperation_t, int, int, int,
      const void *, cudaDataType, const cusparseMatDescr_t, const void *,
      cudaDataType, const int *, const int *, const void *, cudaDataType,
      const void *, cudaDataType, void *, cudaDataType, cudaDataType, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseAlgMode_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphatype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrValAtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &betatype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &ytype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &buffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsrmvEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, alg, transA, m, n, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphatype, descrA, csrValA, csrValAtype, csrRowPtrA, csrColIndA,
               x, xtype,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               betatype, y, ytype, executiontype, buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseCsrmvEx_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseAlgMode_t alg;
  cusparseOperation_t transA;
  int m;
  int n;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudaDataType alphatype;
  cusparseMatDescr_t descrA;
  const void *csrValA;
  cudaDataType csrValAtype;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const void *x;
  cudaDataType xtype;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudaDataType betatype;
  void *y;
  cudaDataType ytype;
  cudaDataType executiontype;
  size_t bufferSizeInBytes{};
  bufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseAlgMode_t, cusparseOperation_t, int, int, int,
      const void *, cudaDataType, const cusparseMatDescr_t, const void *,
      cudaDataType, const int *, const int *, const void *, cudaDataType,
      const void *, cudaDataType, void *, cudaDataType, cudaDataType, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseAlgMode_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &alphatype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const void *)) < 0 ||
      rpc_read(conn, &csrValAtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xtype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &betatype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &ytype, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &executiontype, sizeof(cudaDataType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseCsrmvEx_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, alg, transA, m, n, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               alphatype, descrA, csrValA, csrValAtype, csrRowPtrA, csrColIndA,
               x, xtype,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               betatype, y, ytype, executiontype, &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDaxpyi(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *xVal;
  const int *xInd;
  double *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const double *,
                                    const double *, const int *, double *,
                                    cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDaxpyi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsc2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *cscSortedValA;
  const int *cscSortedRowIndA;
  const int *cscSortedColPtrA;
  double *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsc2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, cscSortedValA,
                                    cscSortedRowIndA, cscSortedColPtrA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsr2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  double *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsr2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrgemm2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const double *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const double *csrSortedValD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  cusparseMatDescr_t descrC;
  double *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  csrgemm2Info_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const double *, const cusparseMatDescr_t,
      int, const double *, const int *, const int *, const cusparseMatDescr_t,
      int, const double *, const int *, const int *, const double *,
      const cusparseMatDescr_t, int, const double *, const int *, const int *,
      const cusparseMatDescr_t, double *, const int *, int *,
      const csrgemm2Info_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValD, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const csrgemm2Info_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrgemm2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedValD, csrSortedRowPtrD, csrSortedColIndD,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC, info,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrgemm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  csrgemm2Info_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const double *, const cusparseMatDescr_t,
      int, const int *, const int *, const cusparseMatDescr_t, int, const int *,
      const int *, const double *, const cusparseMatDescr_t, int, const int *,
      const int *, csrgemm2Info_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrgemm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrgemm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedRowPtrA, csrSortedColIndA, descrB, nnzB,
               csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedRowPtrD, csrSortedColIndD, info,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const double *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const double *, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsm2_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const double *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  size_t pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const double *, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  double *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const double *, const cusparseMatDescr_t, const double *,
      const int *, const int *, double *, int, csrsm2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, csrsv2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsv2_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, nnz, descrA,
                                    csrSortedValA, csrSortedRowPtrA,
                                    csrSortedColIndA, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      double *, const int *, const int *, csrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsv2_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDcsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const double *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  const double *f;
  double *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const double *,
      const cusparseMatDescr_t, const double *, const int *, const int *,
      csrsv2Info_t, const double *, double *, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDcsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, info,
               f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDdense2csc(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *A;
  int lda;
  const int *nnzPerCol;
  double *cscSortedValA;
  int *cscSortedRowIndA;
  int *cscSortedColPtrA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    int, const int *, double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerCol, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDdense2csc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerCol,
                          cscSortedValA, cscSortedRowIndA, cscSortedColPtrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDdense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const double *A;
  int lda;
  const int *nnzPerRow;
  double *csrSortedValA;
  int *csrSortedRowPtrA;
  int *csrSortedColIndA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    int, const int *, double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDdense2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerRow,
                          csrSortedValA, csrSortedRowPtrA, csrSortedColIndA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDestroyCsrgemm2Info(conn_t *conn) {
  csrgemm2Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrgemm2Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrgemm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsrgemm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDestroyCsrsm2Info(conn_t *conn) {
  csrsm2Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrsm2Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsrsm2Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDestroyCsrsv2Info(conn_t *conn) {
  csrsv2Info_t info;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(csrsv2Info_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDestroyCsrsv2Info");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDgemmi(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const double *alpha = nullptr;
  const double *A;
  int lda;
  const double *cscValB;
  const int *cscColPtrB;
  const int *cscRowIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const double *beta = nullptr;
  double *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, int, const double *, const double *, int,
      const double *, const int *, const int *, const double *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &cscValB, sizeof(const double *)) < 0 ||
      rpc_read(conn, &cscColPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgemmi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const double *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, cscValB, cscColPtrB, cscRowIndB,
               beta_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDgthr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const double *y;
  double *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const double *,
                                    double *, const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const double *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgthr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDgthrz(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  double *y;
  double *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, double *, double *,
                                    const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDgthrz");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDroti(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  double *xVal;
  const int *xInd;
  double *y;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const double *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const double *s = nullptr;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, double *,
                                    const int *, double *, const double *,
                                    const double *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDroti");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz, xVal, xInd, y,
               c_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const double *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s,
               idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseDsctr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const double *xVal;
  const int *xInd;
  double *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const double *,
                                    const int *, double *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(double *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseDsctr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSaxpyi(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *xVal;
  const int *xInd;
  float *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const float *, const float *,
                           const int *, float *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSaxpyi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsc2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *cscSortedValA;
  const int *cscSortedRowIndA;
  const int *cscSortedColPtrA;
  float *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    const int *, const int *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsc2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, cscSortedValA,
                                    cscSortedRowIndA, cscSortedColPtrA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsr2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    const int *, const int *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsr2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrgemm2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const float *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const float *csrSortedValD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  cusparseMatDescr_t descrC;
  float *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  csrgemm2Info_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const float *, const cusparseMatDescr_t,
      int, const float *, const int *, const int *, const cusparseMatDescr_t,
      int, const float *, const int *, const int *, const float *,
      const cusparseMatDescr_t, int, const float *, const int *, const int *,
      const cusparseMatDescr_t, float *, const int *, int *,
      const csrgemm2Info_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValD, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const csrgemm2Info_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrgemm2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedValD, csrSortedRowPtrD, csrSortedColIndD,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC, info,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrgemm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  csrgemm2Info_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const float *, const cusparseMatDescr_t,
      int, const int *, const int *, const cusparseMatDescr_t, int, const int *,
      const int *, const float *, const cusparseMatDescr_t, int, const int *,
      const int *, csrgemm2Info_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrgemm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrgemm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedRowPtrA, csrSortedColIndA, descrB, nnzB,
               csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedRowPtrD, csrSortedColIndD, info,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const float *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, const float *, int, csrsm2Info_t, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsm2_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const float *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  size_t pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, const float *, int, csrsm2Info_t, cusparseSolvePolicy_t,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  float *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const float *, const cusparseMatDescr_t, const float *, const int *,
      const int *, float *, int, csrsm2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, csrsv2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsv2_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, nnz, descrA,
                                    csrSortedValA, csrSortedRowPtrA,
                                    csrSortedColIndA, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      float *, const int *, const int *, csrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsv2_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseScsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const float *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  const float *f;
  float *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const float *,
      const cusparseMatDescr_t, const float *, const int *, const int *,
      csrsv2Info_t, const float *, float *, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseScsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, info,
               f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSdense2csc(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *A;
  int lda;
  const int *nnzPerCol;
  float *cscSortedValA;
  int *cscSortedRowIndA;
  int *cscSortedColPtrA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    int, const int *, float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerCol, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSdense2csc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerCol,
                          cscSortedValA, cscSortedRowIndA, cscSortedColPtrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSdense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const float *A;
  int lda;
  const int *nnzPerRow;
  float *csrSortedValA;
  int *csrSortedRowPtrA;
  int *csrSortedColIndA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    int, const int *, float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(float *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSdense2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerRow,
                          csrSortedValA, csrSortedRowPtrA, csrSortedColIndA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSgemmi(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const float *alpha = nullptr;
  const float *A;
  int lda;
  const float *cscValB;
  const int *cscColPtrB;
  const int *cscRowIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const float *beta = nullptr;
  float *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, int, const float *, const float *, int,
      const float *, const int *, const int *, const float *, float *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &cscValB, sizeof(const float *)) < 0 ||
      rpc_read(conn, &cscColPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgemmi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const float *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, cscValB, cscColPtrB, cscRowIndB,
               beta_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSgthr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const float *y;
  float *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const float *,
                                    float *, const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const float *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgthr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSgthrz(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  float *y;
  float *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, float *, float *,
                                    const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSgthrz");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSpMatSetStridedBatch(conn_t *conn) {
  cusparseSpMatDescr_t spMatDescr;
  int batchCount;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseSpMatDescr_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &spMatDescr, sizeof(cusparseSpMatDescr_t)) < 0 ||
      rpc_read(conn, &batchCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSpMatSetStridedBatch");
  return_value =
      fn == nullptr ? function_not_found() : fn(spMatDescr, batchCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSroti(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  float *xVal;
  const int *xInd;
  float *y;
  uint32_t c_width;
  alignas(16) unsigned char c_value[64];
  std::vector<unsigned char> c_heap;
  const float *c = nullptr;
  uint32_t s_width;
  alignas(16) unsigned char s_value[64];
  std::vector<unsigned char> s_heap;
  const float *s = nullptr;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, float *, const int *, float *,
                           const float *, const float *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &c_width, sizeof(c_width)) < 0 ||
      (c_width == 0 && rpc_read(conn, &c, sizeof(c)) < 0) ||
      (c_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(c_heap, c_value, sizeof(c_value), c_width),
                c_width) < 0) ||
      rpc_read(conn, &s_width, sizeof(s_width)) < 0 ||
      (s_width == 0 && rpc_read(conn, &s, sizeof(s)) < 0) ||
      (s_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(s_heap, s_value, sizeof(s_value), s_width),
                s_width) < 0) ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSroti");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz, xVal, xInd, y,
               c_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         c_heap, c_value, sizeof(c_value), c_width))
                   : c,
               s_width != 0
                   ? reinterpret_cast<const float *>(lupine_scalar_slot(
                         s_heap, s_value, sizeof(s_value), s_width))
                   : s,
               idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseSsctr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const float *xVal;
  const int *xInd;
  float *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, int, const float *,
                                    const int *, float *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(float *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseSsctr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseXcsrgemm2Nnz(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  cusparseMatDescr_t descrD;
  int nnzD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  cusparseMatDescr_t descrC;
  int *csrSortedRowPtrC;
  uint32_t nnzTotalDevHostPtr_width;
  alignas(16) unsigned char nnzTotalDevHostPtr_value[64];
  std::vector<unsigned char> nnzTotalDevHostPtr_heap;
  int *nnzTotalDevHostPtr = nullptr;
  csrgemm2Info_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cusparseMatDescr_t, int,
      const int *, const int *, const cusparseMatDescr_t, int, const int *,
      const int *, const cusparseMatDescr_t, int, const int *, const int *,
      const cusparseMatDescr_t, int *, int *, const csrgemm2Info_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(int *)) < 0 ||
      rpc_read(conn, &nnzTotalDevHostPtr_width,
               sizeof(nnzTotalDevHostPtr_width)) < 0 ||
      (nnzTotalDevHostPtr_width == 0 &&
       rpc_read(conn, &nnzTotalDevHostPtr, sizeof(nnzTotalDevHostPtr)) < 0) ||
      rpc_read(conn, &info, sizeof(const csrgemm2Info_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrgemm2Nnz");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k, descrA, nnzA, csrSortedRowPtrA,
               csrSortedColIndA, descrB, nnzB, csrSortedRowPtrB,
               csrSortedColIndB, descrD, nnzD, csrSortedRowPtrD,
               csrSortedColIndD, descrC, csrSortedRowPtrC,
               nnzTotalDevHostPtr_width != 0
                   ? reinterpret_cast<int *>(lupine_scalar_slot(
                         nnzTotalDevHostPtr_heap, nnzTotalDevHostPtr_value,
                         sizeof(nnzTotalDevHostPtr_value),
                         nnzTotalDevHostPtr_width))
                   : nnzTotalDevHostPtr,
               info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (nnzTotalDevHostPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(nnzTotalDevHostPtr_heap,
                                    nnzTotalDevHostPtr_value,
                                    sizeof(nnzTotalDevHostPtr_value),
                                    nnzTotalDevHostPtr_width),
                 nnzTotalDevHostPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseXcsrsm2_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  csrsm2Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrsm2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrsm2_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseXcsrsv2_zeroPivot(conn_t *conn) {
  cusparseHandle_t handle;
  csrsv2Info_t info;
  uint32_t position_width;
  alignas(16) unsigned char position_value[64];
  std::vector<unsigned char> position_heap;
  int *position = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseHandle_t, csrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &position_width, sizeof(position_width)) < 0 ||
      (position_width == 0 &&
       rpc_read(conn, &position, sizeof(position)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseXcsrsv2_zeroPivot");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, info,
                          position_width != 0
                              ? reinterpret_cast<int *>(lupine_scalar_slot(
                                    position_heap, position_value,
                                    sizeof(position_value), position_width))
                              : position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (position_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(position_heap, position_value,
                                    sizeof(position_value), position_width),
                 position_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZaxpyi(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *xVal;
  const int *xInd;
  cuDoubleComplex *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, const cuDoubleComplex *, const cuDoubleComplex *,
      const int *, cuDoubleComplex *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZaxpyi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsc2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *cscSortedValA;
  const int *cscSortedRowIndA;
  const int *cscSortedColPtrA;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, const int *, const int *,
                           cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsc2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, cscSortedValA,
                                    cscSortedRowIndA, cscSortedColPtrA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsr2dense(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cuDoubleComplex *A;
  int lda;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, const int *, const int *,
                           cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsr2dense");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, descrA, csrSortedValA,
                                    csrSortedRowPtrA, csrSortedColIndA, A, lda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrgemm2(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const cuDoubleComplex *csrSortedValB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const cuDoubleComplex *csrSortedValD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  cusparseMatDescr_t descrC;
  cuDoubleComplex *csrSortedValC;
  const int *csrSortedRowPtrC;
  int *csrSortedColIndC;
  csrgemm2Info_t info;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, int, const cuDoubleComplex *, const int *,
      const int *, const cusparseMatDescr_t, int, const cuDoubleComplex *,
      const int *, const int *, const cuDoubleComplex *,
      const cusparseMatDescr_t, int, const cuDoubleComplex *, const int *,
      const int *, const cusparseMatDescr_t, cuDoubleComplex *, const int *,
      int *, const csrgemm2Info_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValB, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedValD, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrC, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValC, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrC, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndC, sizeof(int *)) < 0 ||
      rpc_read(conn, &info, sizeof(const csrgemm2Info_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrgemm2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA,
               descrB, nnzB, csrSortedValB, csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedValD, csrSortedRowPtrD, csrSortedColIndD,
               descrC, csrSortedValC, csrSortedRowPtrC, csrSortedColIndC, info,
               pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrgemm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  int nnzA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cusparseMatDescr_t descrB;
  int nnzB;
  const int *csrSortedRowPtrB;
  const int *csrSortedColIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cusparseMatDescr_t descrD;
  int nnzD;
  const int *csrSortedRowPtrD;
  const int *csrSortedColIndD;
  csrgemm2Info_t info;
  size_t pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, int, const int *, const int *,
      const cusparseMatDescr_t, int, const int *, const int *,
      const cuDoubleComplex *, const cusparseMatDescr_t, int, const int *,
      const int *, csrgemm2Info_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &descrB, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzB, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &descrD, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &nnzD, sizeof(int)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndD, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrgemm2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrgemm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, nnzA, csrSortedRowPtrA, csrSortedColIndA, descrB, nnzB,
               csrSortedRowPtrB, csrSortedColIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               descrD, nnzD, csrSortedRowPtrD, csrSortedColIndD, info,
               &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsm2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const cuDoubleComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, int, csrsm2Info_t, cusparseSolvePolicy_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsm2_analysis");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsm2_bufferSizeExt(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  const cuDoubleComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  size_t pBufferSize{};
  pBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, int, csrsm2Info_t, cusparseSolvePolicy_t,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsm2_bufferSizeExt");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, &pBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsm2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  int algo;
  cusparseOperation_t transA;
  cusparseOperation_t transB;
  int m;
  int nrhs;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  cuDoubleComplex *B;
  int ldb;
  csrsm2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, cusparseOperation_t, cusparseOperation_t, int, int,
      int, const cuDoubleComplex *, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, cuDoubleComplex *, int,
      csrsm2Info_t, cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(int)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &transB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsm2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsm2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, transA, transB, m, nrhs, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, B,
               ldb, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsv2_analysis(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrsv2Info_t,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsv2_analysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, transA, m, nnz, descrA,
                                    csrSortedValA, csrSortedRowPtrA,
                                    csrSortedColIndA, info, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsv2_bufferSize(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  int pBufferSizeInBytes{};
  pBufferSizeInBytes = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cusparseMatDescr_t,
      cuDoubleComplex *, const int *, const int *, csrsv2Info_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsv2_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz, descrA, csrSortedValA, csrSortedRowPtrA,
               csrSortedColIndA, info, &pBufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pBufferSizeInBytes, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZcsrsv2_solve(conn_t *conn) {
  cusparseHandle_t handle;
  cusparseOperation_t transA;
  int m;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrSortedValA;
  const int *csrSortedRowPtrA;
  const int *csrSortedColIndA;
  csrsv2Info_t info;
  const cuDoubleComplex *f;
  cuDoubleComplex *x;
  cusparseSolvePolicy_t policy;
  void *pBuffer;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, cusparseOperation_t, int, int, const cuDoubleComplex *,
      const cusparseMatDescr_t, const cuDoubleComplex *, const int *,
      const int *, csrsv2Info_t, const cuDoubleComplex *, cuDoubleComplex *,
      cusparseSolvePolicy_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &transA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrsv2Info_t)) < 0 ||
      rpc_read(conn, &f, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &policy, sizeof(cusparseSolvePolicy_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZcsrsv2_solve");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transA, m, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               descrA, csrSortedValA, csrSortedRowPtrA, csrSortedColIndA, info,
               f, x, policy, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZdense2csc(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *A;
  int lda;
  const int *nnzPerCol;
  cuDoubleComplex *cscSortedValA;
  int *cscSortedRowIndA;
  int *cscSortedColPtrA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, int, const int *,
                           cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerCol, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &cscSortedRowIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &cscSortedColPtrA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZdense2csc");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerCol,
                          cscSortedValA, cscSortedRowIndA, cscSortedColPtrA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZdense2csr(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *A;
  int lda;
  const int *nnzPerRow;
  cuDoubleComplex *csrSortedValA;
  int *csrSortedRowPtrA;
  int *csrSortedColIndA;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, int, const cusparseMatDescr_t,
                           const cuDoubleComplex *, int, const int *,
                           cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzPerRow, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrSortedValA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrSortedRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrSortedColIndA, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZdense2csr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, descrA, A, lda, nnzPerRow,
                          csrSortedValA, csrSortedRowPtrA, csrSortedColIndA);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZgemmi(conn_t *conn) {
  cusparseHandle_t handle;
  int m;
  int n;
  int k;
  int nnz;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const cuDoubleComplex *alpha = nullptr;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *cscValB;
  const int *cscColPtrB;
  const int *cscRowIndB;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const cuDoubleComplex *beta = nullptr;
  cuDoubleComplex *C;
  int ldc;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      cusparseHandle_t, int, int, int, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, const cuDoubleComplex *, const int *,
      const int *, const cuDoubleComplex *, cuDoubleComplex *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &cscValB, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &cscColPtrB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &cscRowIndB, sizeof(const int *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgemmi");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, k, nnz,
               alpha_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               A, lda, cscValB, cscColPtrB, cscRowIndB,
               beta_width != 0
                   ? reinterpret_cast<const cuDoubleComplex *>(
                         lupine_scalar_slot(beta_heap, beta_value,
                                            sizeof(beta_value), beta_width))
                   : beta,
               C, ldc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZgthr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const cuDoubleComplex *y;
  cuDoubleComplex *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cuDoubleComplex *,
                           cuDoubleComplex *, const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgthr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZgthrz(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  cuDoubleComplex *y;
  cuDoubleComplex *xVal;
  const int *xInd;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, cuDoubleComplex *,
                           cuDoubleComplex *, const int *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xVal, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZgthrz");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, y, xVal, xInd, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSPARSE_VERSION < 12000
int handle_cusparseZsctr(conn_t *conn) {
  cusparseHandle_t handle;
  int nnz;
  const cuDoubleComplex *xVal;
  const int *xInd;
  cuDoubleComplex *y;
  cusparseIndexBase_t idxBase;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(cusparseHandle_t, int, const cuDoubleComplex *,
                           const int *, cuDoubleComplex *, cusparseIndexBase_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusparseHandle_t)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &xVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &xInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &y, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &idxBase, sizeof(cusparseIndexBase_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparse_symbol<fn_t>("cusparseZsctr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, nnz, xVal, xInd, y, idxBase);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif
