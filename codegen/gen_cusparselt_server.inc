// Generated by codegen.py. Do not edit by hand.

// A pointer-mode scalar's value lands in the handler's inline slot,
// or on the heap when it is wider.
static unsigned char *lupine_scalar_slot(std::vector<unsigned char> &heap,
                                         unsigned char *slot, size_t size,
                                         uint32_t width) {
  if (width <= size) {
    return slot;
  }
  heap.resize(width);
  return heap.data();
}

int handle_cusparseLtInit(conn_t *conn) {
  cusparseLtHandle_t *handle = new cusparseLtHandle_t();
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(cusparseLtHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtInit");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtGetVersion(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  int version{};
  version = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtGetProperty(conn_t *conn) {
  libraryPropertyType propertyType;
  int value{};
  value = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &propertyType, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtGetProperty");
  return_value =
      fn == nullptr ? function_not_found() : fn(propertyType, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtDenseDescriptorInit(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatDescriptor_t *matDescr = new cusparseLtMatDescriptor_t();
  int64_t rows;
  int64_t cols;
  int64_t ld;
  uint32_t alignment;
  cudaDataType valueType;
  cusparseOrder_t order;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatDescriptor_t *, int64_t, int64_t,
      int64_t, uint32_t, cudaDataType, cusparseOrder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alignment, sizeof(uint32_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtDenseDescriptorInit");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matDescr, rows, cols, ld, alignment,
                                    valueType, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &matDescr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtStructuredDescriptorInit(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatDescriptor_t *matDescr = new cusparseLtMatDescriptor_t();
  int64_t rows;
  int64_t cols;
  int64_t ld;
  uint32_t alignment;
  cudaDataType valueType;
  cusparseOrder_t order;
  cusparseLtSparsity_t sparsity;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatDescriptor_t *, int64_t, int64_t,
      int64_t, uint32_t, cudaDataType, cusparseOrder_t, cusparseLtSparsity_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &rows, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &cols, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ld, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &alignment, sizeof(uint32_t)) < 0 ||
      rpc_read(conn, &valueType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &order, sizeof(cusparseOrder_t)) < 0 ||
      rpc_read(conn, &sparsity, sizeof(cusparseLtSparsity_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtStructuredDescriptorInit");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matDescr, rows, cols, ld, alignment,
                                    valueType, order, sparsity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &matDescr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtMatDescSetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatDescriptor_t *matmulDescr;
  cusparseLtMatDescAttribute_t matAttribute;
  size_t dataSize;
  void *data = nullptr;
  size_t data_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatDescriptor_t *,
      cusparseLtMatDescAttribute_t, const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &matAttribute, sizeof(cusparseLtMatDescAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = dataSize;
  data = (void *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatDescSetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, matAttribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatDescGetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatDescriptor_t *matmulDescr;
  cusparseLtMatDescAttribute_t matAttribute;
  size_t dataSize;
  void *data = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatDescriptor_t *,
      cusparseLtMatDescAttribute_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &matAttribute, sizeof(cusparseLtMatDescAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data = (void *)calloc(dataSize, 1);
  if ((dataSize != 0 && data == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatDescGetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, matAttribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, data, dataSize) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatmulDescriptorInit(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulDescriptor_t *matmulDescr =
      new cusparseLtMatmulDescriptor_t();
  cusparseOperation_t opA;
  cusparseOperation_t opB;
  const cusparseLtMatDescriptor_t *matA;
  const cusparseLtMatDescriptor_t *matB;
  const cusparseLtMatDescriptor_t *matC;
  const cusparseLtMatDescriptor_t *matD;
  cusparseComputeType computeType;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatmulDescriptor_t *,
      cusparseOperation_t, cusparseOperation_t,
      const cusparseLtMatDescriptor_t *, const cusparseLtMatDescriptor_t *,
      const cusparseLtMatDescriptor_t *, const cusparseLtMatDescriptor_t *,
      cusparseComputeType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &opA, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &opB, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &matA, sizeof(void *)) < 0 ||
      rpc_read(conn, &matB, sizeof(void *)) < 0 ||
      rpc_read(conn, &matC, sizeof(void *)) < 0 ||
      rpc_read(conn, &matD, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cusparseComputeType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulDescriptorInit");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, opA, opB, matA, matB,
                                    matC, matD, computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtMatmulDescSetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulDescriptor_t *matmulDescr;
  cusparseLtMatmulDescAttribute_t matmulAttribute;
  size_t dataSize;
  void *data = nullptr;
  size_t data_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatmulDescriptor_t *,
      cusparseLtMatmulDescAttribute_t, const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulAttribute,
               sizeof(cusparseLtMatmulDescAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = dataSize;
  data = (void *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulDescSetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, matmulAttribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatmulDescGetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulDescriptor_t *matmulDescr;
  cusparseLtMatmulDescAttribute_t matmulAttribute;
  size_t dataSize;
  void *data = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatmulDescriptor_t *,
      cusparseLtMatmulDescAttribute_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulAttribute,
               sizeof(cusparseLtMatmulDescAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data = (void *)calloc(dataSize, 1);
  if ((dataSize != 0 && data == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulDescGetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, matmulAttribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, data, dataSize) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatmulAlgSelectionInit(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulAlgSelection_t *algSelection =
      new cusparseLtMatmulAlgSelection_t();
  const cusparseLtMatmulDescriptor_t *matmulDescr;
  cusparseLtMatmulAlg_t alg;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatmulAlgSelection_t *,
      const cusparseLtMatmulDescriptor_t *, cusparseLtMatmulAlg_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &alg, sizeof(cusparseLtMatmulAlg_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulAlgSelectionInit");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algSelection, matmulDescr, alg);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &algSelection, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtMatmulAlgSetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulAlgSelection_t *algSelection;
  cusparseLtMatmulAlgAttribute_t attribute;
  size_t dataSize;
  void *data = nullptr;
  size_t data_size;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatmulAlgSelection_t *,
      cusparseLtMatmulAlgAttribute_t, const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &algSelection, sizeof(void *)) < 0 ||
      rpc_read(conn, &attribute, sizeof(cusparseLtMatmulAlgAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = dataSize;
  data = (void *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulAlgSetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algSelection, attribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatmulAlgGetAttribute(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulAlgSelection_t *algSelection;
  cusparseLtMatmulAlgAttribute_t attribute;
  size_t dataSize;
  void *data = nullptr;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatmulAlgSelection_t *,
      cusparseLtMatmulAlgAttribute_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &algSelection, sizeof(void *)) < 0 ||
      rpc_read(conn, &attribute, sizeof(cusparseLtMatmulAlgAttribute_t)) < 0 ||
      rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data = (void *)calloc(dataSize, 1);
  if ((dataSize != 0 && data == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulAlgGetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algSelection, attribute,
                                    (dataSize == 0 ? nullptr : data), dataSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, data, dataSize) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cusparseLtMatmulGetWorkspace(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulPlan_t *plan;
  size_t workspaceSize{};
  workspaceSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *,
                                    const cusparseLtMatmulPlan_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &plan, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulGetWorkspace");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, plan, &workspaceSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtMatmulPlanInit(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulPlan_t *plan = new cusparseLtMatmulPlan_t();
  const cusparseLtMatmulDescriptor_t *matmulDescr;
  const cusparseLtMatmulAlgSelection_t *algSelection;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(const cusparseLtHandle_t *, cusparseLtMatmulPlan_t *,
                           const cusparseLtMatmulDescriptor_t *,
                           const cusparseLtMatmulAlgSelection_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &algSelection, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulPlanInit");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, plan, matmulDescr, algSelection);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtMatmul(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulPlan_t *plan;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *d_A;
  const void *d_B;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  const void *d_C;
  void *d_D;
  void *workspace;
  int32_t numStreams;
  cudaStream_t *streams = nullptr;
  size_t streams_size;
  uint8_t streams_null = 0;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatmulPlan_t *, const void *,
      const void *, const void *, const void *, const void *, void *, void *,
      cudaStream_t *, int32_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &plan, sizeof(void *)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &d_A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &d_C, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_D, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &numStreams, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &streams_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  streams_size = numStreams * sizeof(cudaStream_t);
  if (!streams_null) {
    streams = (cudaStream_t *)malloc(streams_size);
    if (streams_size != 0 && streams == nullptr)
      goto ERROR_0;
  }
  if ((!streams_null && streams_size != 0 &&
       rpc_read(conn, streams, streams_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmul");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, plan,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               d_A, d_B,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               d_C, d_D, workspace, (streams_null ? nullptr : streams),
               numStreams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)streams);
  return 0;
ERROR_0:
  free((void *)streams);
  return -1;
}

int handle_cusparseLtMatmulSearch(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  cusparseLtMatmulPlan_t *plan;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  const void *d_A;
  const void *d_B;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  const void *d_C;
  void *d_D;
  void *workspace;
  int32_t numStreams;
  cudaStream_t *streams = nullptr;
  size_t streams_size;
  uint8_t streams_null = 0;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, cusparseLtMatmulPlan_t *, const void *,
      const void *, const void *, const void *, const void *, void *, void *,
      cudaStream_t *, int32_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &plan, sizeof(void *)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &d_A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &d_C, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_D, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &numStreams, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &streams_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  streams_size = numStreams * sizeof(cudaStream_t);
  if (!streams_null) {
    streams = (cudaStream_t *)malloc(streams_size);
    if (streams_size != 0 && streams == nullptr)
      goto ERROR_0;
  }
  if ((!streams_null && streams_size != 0 &&
       rpc_read(conn, streams, streams_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtMatmulSearch");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, plan,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               d_A, d_B,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               d_C, d_D, workspace, (streams_null ? nullptr : streams),
               numStreams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)streams);
  return 0;
ERROR_0:
  free((void *)streams);
  return -1;
}

int handle_cusparseLtSpMMAPrune(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulDescriptor_t *matmulDescr;
  const void *d_in;
  void *d_out;
  cusparseLtPruneAlg_t pruneAlg;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatmulDescriptor_t *,
      const void *, void *, cusparseLtPruneAlg_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &d_in, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_out, sizeof(void *)) < 0 ||
      rpc_read(conn, &pruneAlg, sizeof(cusparseLtPruneAlg_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMAPrune");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, matmulDescr, d_in, d_out, pruneAlg, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMAPruneCheck(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulDescriptor_t *matmulDescr;
  const void *d_in;
  int *valid;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *,
                                    const cusparseLtMatmulDescriptor_t *,
                                    const void *, int *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &matmulDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &d_in, sizeof(const void *)) < 0 ||
      rpc_read(conn, &valid, sizeof(int *)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMAPruneCheck");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, matmulDescr, d_in, valid, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMAPrune2(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatDescriptor_t *sparseMatDescr;
  int isSparseA;
  cusparseOperation_t op;
  const void *d_in;
  void *d_out;
  cusparseLtPruneAlg_t pruneAlg;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *,
                                    const cusparseLtMatDescriptor_t *, int,
                                    cusparseOperation_t, const void *, void *,
                                    cusparseLtPruneAlg_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &sparseMatDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &isSparseA, sizeof(int)) < 0 ||
      rpc_read(conn, &op, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &d_in, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_out, sizeof(void *)) < 0 ||
      rpc_read(conn, &pruneAlg, sizeof(cusparseLtPruneAlg_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMAPrune2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, sparseMatDescr, isSparseA, op, d_in,
                                    d_out, pruneAlg, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMAPruneCheck2(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatDescriptor_t *sparseMatDescr;
  int isSparseA;
  cusparseOperation_t op;
  const void *d_in;
  int *d_valid;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatDescriptor_t *, int,
      cusparseOperation_t, const void *, int *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &sparseMatDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &isSparseA, sizeof(int)) < 0 ||
      rpc_read(conn, &op, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &d_in, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_valid, sizeof(int *)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMAPruneCheck2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, sparseMatDescr, isSparseA, op, d_in,
                                    d_valid, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMACompressedSize(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulPlan_t *plan;
  size_t compressedSize{};
  compressedSize = {};
  size_t compressedBufferSize{};
  compressedBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t =
      cusparseStatus_t (*)(const cusparseLtHandle_t *,
                           const cusparseLtMatmulPlan_t *, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &plan, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMACompressedSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, plan, &compressedSize, &compressedBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compressedSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &compressedBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMACompress(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatmulPlan_t *plan;
  const void *d_dense;
  void *d_compressed;
  void *d_compressed_buffer;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *,
                                    const cusparseLtMatmulPlan_t *,
                                    const void *, void *, void *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &plan, sizeof(void *)) < 0 ||
      rpc_read(conn, &d_dense, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_compressed, sizeof(void *)) < 0 ||
      rpc_read(conn, &d_compressed_buffer, sizeof(void *)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMACompress");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, plan, d_dense, d_compressed,
                                    d_compressed_buffer, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMACompressedSize2(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatDescriptor_t *sparseMatDescr;
  size_t compressedSize{};
  compressedSize = {};
  size_t compressedBufferSize{};
  compressedBufferSize = {};
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(const cusparseLtHandle_t *,
                                    const cusparseLtMatDescriptor_t *, size_t *,
                                    size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &sparseMatDescr, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMACompressedSize2");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, sparseMatDescr, &compressedSize,
                                    &compressedBufferSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compressedSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &compressedBufferSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusparseLtSpMMACompress2(conn_t *conn) {
  const cusparseLtHandle_t *handle;
  const cusparseLtMatDescriptor_t *sparseMatDescr;
  int isSparseA;
  cusparseOperation_t op;
  const void *d_dense;
  void *d_compressed;
  void *d_compressed_buffer;
  cudaStream_t stream;
  int request_id;
  cusparseStatus_t return_value;
  using fn_t = cusparseStatus_t (*)(
      const cusparseLtHandle_t *, const cusparseLtMatDescriptor_t *, int,
      cusparseOperation_t, const void *, void *, void *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(void *)) < 0 ||
      rpc_read(conn, &sparseMatDescr, sizeof(void *)) < 0 ||
      rpc_read(conn, &isSparseA, sizeof(int)) < 0 ||
      rpc_read(conn, &op, sizeof(cusparseOperation_t)) < 0 ||
      rpc_read(conn, &d_dense, sizeof(const void *)) < 0 ||
      rpc_read(conn, &d_compressed, sizeof(void *)) < 0 ||
      rpc_read(conn, &d_compressed_buffer, sizeof(void *)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusparselt_symbol<fn_t>("cusparseLtSpMMACompress2");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, sparseMatDescr, isSparseA, op, d_dense,
                          d_compressed, d_compressed_buffer, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusparseStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
