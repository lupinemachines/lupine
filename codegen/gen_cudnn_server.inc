// Generated by codegen.py. Do not edit by hand.

// A pointer-mode scalar's value lands in the handler's inline slot,
// or on the heap when it is wider.
static unsigned char *lupine_scalar_slot(std::vector<unsigned char> &heap,
                                         unsigned char *slot, size_t size,
                                         uint32_t width) {
  if (width <= size) {
    return slot;
  }
  heap.resize(width);
  return heap.data();
}

int handle_cudnnGetVersion(conn_t *conn) {
  int request_id;
  size_t return_value;
  using fn_t = size_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetVersion");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(size_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetMaxDeviceVersion(conn_t *conn) {
  int request_id;
  size_t return_value;
  using fn_t = size_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetMaxDeviceVersion");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(size_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCudartVersion(conn_t *conn) {
  int request_id;
  size_t return_value;
  using fn_t = size_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCudartVersion");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(size_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetLastErrorString(conn_t *conn) {
  size_t max_size;
  char *message = nullptr;
  int request_id;
  using fn_t = void (*)(char *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &max_size, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  message = (char *)malloc(max_size * sizeof(char));
  if ((max_size * sizeof(char) != 0 && message == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetLastErrorString");
  if (fn == nullptr)
    goto ERROR_0;
  fn((max_size * sizeof(char) == 0 ? nullptr : message), max_size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, message, max_size * sizeof(char)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)message);
  return 0;
ERROR_0:
  free((void *)message);
  return -1;
}

int handle_cudnnQueryRuntimeError(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnStatus_t rstatus{};
  rstatus = {};
  cudnnErrQueryMode_t mode;
  cudnnRuntimeTag_t *tag;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnStatus_t *,
                                 cudnnErrQueryMode_t, cudnnRuntimeTag_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnErrQueryMode_t)) < 0 ||
      rpc_read(conn, &tag, sizeof(cudnnRuntimeTag_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnQueryRuntimeError");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &rstatus, mode, tag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rstatus, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  value = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreate(conn_t *conn) {
  cudnnHandle_t handle{};
  handle = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroy(conn_t *conn) {
  cudnnHandle_t handle;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetStream(conn_t *conn) {
  cudnnHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetStream(conn_t *conn) {
  cudnnHandle_t handle;
  cudaStream_t streamId{};
  streamId = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGraphVersionCheck(conn_t *conn) {
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGraphVersionCheck");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBackendCreateDescriptor(conn_t *conn) {
  cudnnBackendDescriptorType_t descriptorType;
  cudnnBackendDescriptor_t descriptor{};
  descriptor = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnBackendDescriptorType_t,
                                 cudnnBackendDescriptor_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descriptorType, sizeof(cudnnBackendDescriptorType_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendCreateDescriptor");
  return_value =
      fn == nullptr ? function_not_found() : fn(descriptorType, &descriptor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &descriptor, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBackendDestroyDescriptor(conn_t *conn) {
  cudnnBackendDescriptor_t descriptor;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnBackendDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descriptor, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendDestroyDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(descriptor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBackendInitialize(conn_t *conn) {
  cudnnBackendDescriptor_t descriptor;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnBackendDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descriptor, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendInitialize");
  return_value = fn == nullptr ? function_not_found() : fn(descriptor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBackendFinalize(conn_t *conn) {
  cudnnBackendDescriptor_t descriptor;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnBackendDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &descriptor, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendFinalize");
  return_value = fn == nullptr ? function_not_found() : fn(descriptor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDNN_VERSION >= 92300
int handle_cudnnGetExecutionPlanWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBackendDescriptor_t executionPlan;
  cudnnBackendDescriptor_t variantPack;
  size_t workspaceSizeInBytes{};
  workspaceSizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnBackendDescriptor_t,
                                 cudnnBackendDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &executionPlan, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &variantPack, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetExecutionPlanWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, executionPlan, variantPack,
                                    &workspaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudnnBackendExecute(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBackendDescriptor_t executionPlan;
  cudnnBackendDescriptor_t variantPack;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnBackendDescriptor_t,
                                 cudnnBackendDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &executionPlan, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &variantPack, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendExecute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, executionPlan, variantPack);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDNN_VERSION >= 90500
int handle_cudnnBackendPopulateCudaGraph(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBackendDescriptor_t executionPlan;
  cudnnBackendDescriptor_t variantPack;
  cudaGraph_t graph;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnBackendDescriptor_t,
                                 cudnnBackendDescriptor_t, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &executionPlan, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &variantPack, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendPopulateCudaGraph");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, executionPlan, variantPack, graph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 90500
int handle_cudnnBackendUpdateCudaGraph(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBackendDescriptor_t executionPlan;
  cudnnBackendDescriptor_t variantPack;
  cudaGraph_t graph;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnBackendDescriptor_t,
                                 cudnnBackendDescriptor_t, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &executionPlan, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &variantPack, sizeof(cudnnBackendDescriptor_t)) < 0 ||
      rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBackendUpdateCudaGraph");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, executionPlan, variantPack, graph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudnnCreateTensorDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc{};
  tensorDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&tensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetTensor4dDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  cudnnTensorFormat_t format;
  cudnnDataType_t dataType;
  int n;
  int c;
  int h;
  int w;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t, cudnnTensorFormat_t,
                                 cudnnDataType_t, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(int)) < 0 ||
      rpc_read(conn, &h, sizeof(int)) < 0 ||
      rpc_read(conn, &w, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensor4dDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(tensorDesc, format, dataType, n, c, h, w);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetTensor4dDescriptorEx(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  cudnnDataType_t dataType;
  int n;
  int c;
  int h;
  int w;
  int nStride;
  int cStride;
  int hStride;
  int wStride;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t, cudnnDataType_t, int,
                                 int, int, int, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(int)) < 0 ||
      rpc_read(conn, &h, sizeof(int)) < 0 ||
      rpc_read(conn, &w, sizeof(int)) < 0 ||
      rpc_read(conn, &nStride, sizeof(int)) < 0 ||
      rpc_read(conn, &cStride, sizeof(int)) < 0 ||
      rpc_read(conn, &hStride, sizeof(int)) < 0 ||
      rpc_read(conn, &wStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensor4dDescriptorEx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(tensorDesc, dataType, n, c, h, w, nStride,
                                    cStride, hStride, wStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetTensor4dDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  cudnnDataType_t dataType{};
  dataType = {};
  int n{};
  n = {};
  int c{};
  c = {};
  int h{};
  h = {};
  int w{};
  w = {};
  int nStride{};
  nStride = {};
  int cStride{};
  cStride = {};
  int hStride{};
  hStride = {};
  int wStride{};
  wStride = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(const cudnnTensorDescriptor_t, cudnnDataType_t *, int *,
                        int *, int *, int *, int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetTensor4dDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(tensorDesc, &dataType, &n, &c, &h, &w,
                                    &nStride, &cStride, &hStride, &wStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &n, sizeof(int)) < 0 ||
      rpc_write(conn, &c, sizeof(int)) < 0 ||
      rpc_write(conn, &h, sizeof(int)) < 0 ||
      rpc_write(conn, &w, sizeof(int)) < 0 ||
      rpc_write(conn, &nStride, sizeof(int)) < 0 ||
      rpc_write(conn, &cStride, sizeof(int)) < 0 ||
      rpc_write(conn, &hStride, sizeof(int)) < 0 ||
      rpc_write(conn, &wStride, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetTensorNdDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  cudnnDataType_t dataType;
  int nbDims;
  int *dimA = nullptr;
  size_t dimA_size;
  int *strideA = nullptr;
  size_t strideA_size;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t, cudnnDataType_t, int,
                                 const int *, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  dimA_size = nbDims * sizeof(const int);
  dimA = (int *)malloc(dimA_size);
  if (dimA_size != 0 && dimA == nullptr)
    goto ERROR_0;
  if ((dimA_size != 0 && rpc_read(conn, dimA, dimA_size) < 0) || nbDims < 0 ||
      false)
    goto ERROR_0;
  strideA_size = nbDims * sizeof(const int);
  strideA = (int *)malloc(strideA_size);
  if (strideA_size != 0 && strideA == nullptr)
    goto ERROR_0;
  if ((strideA_size != 0 && rpc_read(conn, strideA, strideA_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensorNdDescriptor");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(tensorDesc, dataType, nbDims,
                         (nbDims * sizeof(const int) == 0 ? nullptr : dimA),
                         (nbDims * sizeof(const int) == 0 ? nullptr : strideA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)strideA);
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)strideA);
  free((void *)dimA);
  return -1;
}

int handle_cudnnSetTensorNdDescriptorEx(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  cudnnTensorFormat_t format;
  cudnnDataType_t dataType;
  int nbDims;
  int *dimA = nullptr;
  size_t dimA_size;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t, cudnnTensorFormat_t,
                                 cudnnDataType_t, int, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  dimA_size = nbDims * sizeof(const int);
  dimA = (int *)malloc(dimA_size);
  if (dimA_size != 0 && dimA == nullptr)
    goto ERROR_0;
  if ((dimA_size != 0 && rpc_read(conn, dimA, dimA_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensorNdDescriptorEx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(tensorDesc, format, dataType, nbDims,
                          (nbDims * sizeof(const int) == 0 ? nullptr : dimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)dimA);
  return -1;
}

int handle_cudnnGetTensorNdDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  int nbDimsRequested;
  cudnnDataType_t dataType{};
  dataType = {};
  int nbDims{};
  nbDims = {};
  int *dimA = nullptr;
  int *strideA = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnTensorDescriptor_t, int,
                                 cudnnDataType_t *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &nbDimsRequested, sizeof(int)) < 0 ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  dimA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && dimA == nullptr) ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  strideA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && strideA == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetTensorNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(tensorDesc, nbDimsRequested, &dataType, &nbDims,
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : dimA),
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : strideA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &nbDims, sizeof(int)) < 0 ||
      rpc_write(conn, dimA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, strideA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)strideA);
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)strideA);
  free((void *)dimA);
  return -1;
}

int handle_cudnnGetTensorSizeInBytes(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  size_t size{};
  size = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetTensorSizeInBytes");
  return_value = fn == nullptr ? function_not_found() : fn(tensorDesc, &size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyTensorDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t tensorDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &tensorDesc, sizeof(cudnnTensorDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(tensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnInitTransformDest(conn_t *conn) {
  cudnnTensorTransformDescriptor_t transformDesc;
  cudnnTensorDescriptor_t srcDesc;
  cudnnTensorDescriptor_t destDesc;
  size_t destSizeInBytes{};
  destSizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnTensorTransformDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &transformDesc,
               sizeof(const cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &srcDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &destDesc, sizeof(cudnnTensorDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnInitTransformDest");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(transformDesc, srcDesc, destDesc, &destSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &destSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateTensorTransformDescriptor(conn_t *conn) {
  cudnnTensorTransformDescriptor_t transformDesc{};
  transformDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorTransformDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateTensorTransformDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&transformDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &transformDesc,
                sizeof(cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetTensorTransformDescriptor(conn_t *conn) {
  cudnnTensorTransformDescriptor_t transformDesc;
  uint32_t nbDims;
  cudnnTensorFormat_t destFormat;
  int32_t *padBeforeA = nullptr;
  size_t padBeforeA_size;
  uint8_t padBeforeA_null = 0;
  int32_t *padAfterA = nullptr;
  size_t padAfterA_size;
  uint8_t padAfterA_null = 0;
  uint32_t *foldA = nullptr;
  size_t foldA_size;
  uint8_t foldA_null = 0;
  cudnnFoldingDirection_t direction;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnTensorTransformDescriptor_t, const uint32_t,
      const cudnnTensorFormat_t, const int32_t *, const int32_t *,
      const uint32_t *, const cudnnFoldingDirection_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &transformDesc, sizeof(cudnnTensorTransformDescriptor_t)) <
          0 ||
      rpc_read(conn, &nbDims, sizeof(const uint32_t)) < 0 ||
      rpc_read(conn, &destFormat, sizeof(const cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &padBeforeA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  padBeforeA_size = nbDims * sizeof(const int32_t);
  if (!padBeforeA_null) {
    padBeforeA = (int32_t *)malloc(padBeforeA_size);
    if (padBeforeA_size != 0 && padBeforeA == nullptr)
      goto ERROR_0;
  }
  if ((!padBeforeA_null && padBeforeA_size != 0 &&
       rpc_read(conn, padBeforeA, padBeforeA_size) < 0) ||
      rpc_read(conn, &padAfterA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  padAfterA_size = nbDims * sizeof(const int32_t);
  if (!padAfterA_null) {
    padAfterA = (int32_t *)malloc(padAfterA_size);
    if (padAfterA_size != 0 && padAfterA == nullptr)
      goto ERROR_0;
  }
  if ((!padAfterA_null && padAfterA_size != 0 &&
       rpc_read(conn, padAfterA, padAfterA_size) < 0) ||
      rpc_read(conn, &foldA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  foldA_size = nbDims * sizeof(const uint32_t);
  if (!foldA_null) {
    foldA = (uint32_t *)malloc(foldA_size);
    if (foldA_size != 0 && foldA == nullptr)
      goto ERROR_0;
  }
  if ((!foldA_null && foldA_size != 0 &&
       rpc_read(conn, foldA, foldA_size) < 0) ||
      rpc_read(conn, &direction, sizeof(const cudnnFoldingDirection_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensorTransformDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(transformDesc, nbDims, destFormat,
                                    (padBeforeA_null ? nullptr : padBeforeA),
                                    (padAfterA_null ? nullptr : padAfterA),
                                    (foldA_null ? nullptr : foldA), direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)foldA);
  free((void *)padAfterA);
  free((void *)padBeforeA);
  return 0;
ERROR_0:
  free((void *)foldA);
  free((void *)padAfterA);
  free((void *)padBeforeA);
  return -1;
}

int handle_cudnnGetTensorTransformDescriptor(conn_t *conn) {
  cudnnTensorTransformDescriptor_t transformDesc;
  uint32_t nbDimsRequested;
  cudnnTensorFormat_t destFormat{};
  destFormat = {};
  int32_t *padBeforeA = nullptr;
  uint8_t padBeforeA_null = 0;
  int32_t *padAfterA = nullptr;
  uint8_t padAfterA_null = 0;
  uint32_t *foldA = nullptr;
  uint8_t foldA_null = 0;
  cudnnFoldingDirection_t direction{};
  direction = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorTransformDescriptor_t, uint32_t,
                                 cudnnTensorFormat_t *, int32_t *, int32_t *,
                                 uint32_t *, cudnnFoldingDirection_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &transformDesc, sizeof(cudnnTensorTransformDescriptor_t)) <
          0 ||
      rpc_read(conn, &nbDimsRequested, sizeof(uint32_t)) < 0 ||
      rpc_read(conn, &padBeforeA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!padBeforeA_null) {
    padBeforeA = (int32_t *)malloc(
        (nbDimsRequested != 0 ? nbDimsRequested : 1) * sizeof(int32_t));
    if (padBeforeA == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &padAfterA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!padAfterA_null) {
    padAfterA = (int32_t *)malloc((nbDimsRequested != 0 ? nbDimsRequested : 1) *
                                  sizeof(int32_t));
    if (padAfterA == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &foldA_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!foldA_null) {
    foldA = (uint32_t *)malloc((nbDimsRequested != 0 ? nbDimsRequested : 1) *
                               sizeof(uint32_t));
    if (foldA == nullptr)
      goto ERROR_0;
  }
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetTensorTransformDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(transformDesc, nbDimsRequested, &destFormat,
                                    padBeforeA, padAfterA, foldA, &direction);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &destFormat, sizeof(cudnnTensorFormat_t)) < 0 ||
      (!padBeforeA_null &&
       rpc_write(conn, padBeforeA, nbDimsRequested * sizeof(int32_t)) < 0) ||
      (!padAfterA_null &&
       rpc_write(conn, padAfterA, nbDimsRequested * sizeof(int32_t)) < 0) ||
      (!foldA_null &&
       rpc_write(conn, foldA, nbDimsRequested * sizeof(uint32_t)) < 0) ||
      rpc_write(conn, &direction, sizeof(cudnnFoldingDirection_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)foldA);
  free((void *)padAfterA);
  free((void *)padBeforeA);
  return 0;
ERROR_0:
  free((void *)foldA);
  free((void *)padAfterA);
  free((void *)padBeforeA);
  return -1;
}

int handle_cudnnDestroyTensorTransformDescriptor(conn_t *conn) {
  cudnnTensorTransformDescriptor_t transformDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorTransformDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &transformDesc, sizeof(cudnnTensorTransformDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyTensorTransformDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(transformDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnTransformTensor(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnTransformTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnTransformTensorEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorTransformDescriptor_t transDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t srcDesc;
  const void *srcData;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t destDesc;
  void *destData;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorTransformDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &transDesc,
               sizeof(const cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &srcDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &srcData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &destDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &destData, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnTransformTensorEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               srcDesc, srcData,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               destDesc, destData);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnAddTensor(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t aDesc;
  const void *A;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t cDesc;
  void *C;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &aDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &cDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnAddTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               aDesc, A,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               cDesc, C);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateOpTensorDescriptor(conn_t *conn) {
  cudnnOpTensorDescriptor_t opTensorDesc{};
  opTensorDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnOpTensorDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateOpTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&opTensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &opTensorDesc, sizeof(cudnnOpTensorDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetOpTensorDescriptor(conn_t *conn) {
  cudnnOpTensorDescriptor_t opTensorDesc;
  cudnnOpTensorOp_t opTensorOp;
  cudnnDataType_t opTensorCompType;
  cudnnNanPropagation_t opTensorNanOpt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnOpTensorDescriptor_t, cudnnOpTensorOp_t,
                                 cudnnDataType_t, cudnnNanPropagation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &opTensorDesc, sizeof(cudnnOpTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &opTensorOp, sizeof(cudnnOpTensorOp_t)) < 0 ||
      rpc_read(conn, &opTensorCompType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &opTensorNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetOpTensorDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(opTensorDesc, opTensorOp, opTensorCompType,
                                    opTensorNanOpt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetOpTensorDescriptor(conn_t *conn) {
  cudnnOpTensorDescriptor_t opTensorDesc;
  cudnnOpTensorOp_t opTensorOp{};
  opTensorOp = {};
  cudnnDataType_t opTensorCompType{};
  opTensorCompType = {};
  cudnnNanPropagation_t opTensorNanOpt{};
  opTensorNanOpt = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(const cudnnOpTensorDescriptor_t, cudnnOpTensorOp_t *,
                        cudnnDataType_t *, cudnnNanPropagation_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &opTensorDesc, sizeof(const cudnnOpTensorDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetOpTensorDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(opTensorDesc, &opTensorOp,
                                    &opTensorCompType, &opTensorNanOpt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &opTensorOp, sizeof(cudnnOpTensorOp_t)) < 0 ||
      rpc_write(conn, &opTensorCompType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &opTensorNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyOpTensorDescriptor(conn_t *conn) {
  cudnnOpTensorDescriptor_t opTensorDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnOpTensorDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &opTensorDesc, sizeof(cudnnOpTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyOpTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(opTensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnOpTensor(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnOpTensorDescriptor_t opTensorDesc;
  uint32_t alpha1_width;
  alignas(16) unsigned char alpha1_value[64];
  std::vector<unsigned char> alpha1_heap;
  const void *alpha1 = nullptr;
  cudnnTensorDescriptor_t aDesc;
  const void *A;
  uint32_t alpha2_width;
  alignas(16) unsigned char alpha2_value[64];
  std::vector<unsigned char> alpha2_heap;
  const void *alpha2 = nullptr;
  cudnnTensorDescriptor_t bDesc;
  const void *B;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t cDesc;
  void *C;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnOpTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &opTensorDesc, sizeof(const cudnnOpTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha1_width, sizeof(alpha1_width)) < 0 ||
      (alpha1_width == 0 && rpc_read(conn, &alpha1, sizeof(alpha1)) < 0) ||
      (alpha1_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha1_heap, alpha1_value,
                                   sizeof(alpha1_value), alpha1_width),
                alpha1_width) < 0) ||
      rpc_read(conn, &aDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &alpha2_width, sizeof(alpha2_width)) < 0 ||
      (alpha2_width == 0 && rpc_read(conn, &alpha2, sizeof(alpha2)) < 0) ||
      (alpha2_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha2_heap, alpha2_value,
                                   sizeof(alpha2_value), alpha2_width),
                alpha2_width) < 0) ||
      rpc_read(conn, &bDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &cDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnOpTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, opTensorDesc,
               alpha1_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha1_heap, alpha1_value,
                                            sizeof(alpha1_value), alpha1_width))
                   : alpha1,
               aDesc, A,
               alpha2_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha2_heap, alpha2_value,
                                            sizeof(alpha2_value), alpha2_width))
                   : alpha2,
               bDesc, B,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               cDesc, C);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateReduceTensorDescriptor(conn_t *conn) {
  cudnnReduceTensorDescriptor_t reduceTensorDesc{};
  reduceTensorDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnReduceTensorDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateReduceTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&reduceTensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &reduceTensorDesc,
                sizeof(cudnnReduceTensorDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetReduceTensorDescriptor(conn_t *conn) {
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  cudnnReduceTensorOp_t reduceTensorOp;
  cudnnDataType_t reduceTensorCompType;
  cudnnNanPropagation_t reduceTensorNanOpt;
  cudnnReduceTensorIndices_t reduceTensorIndices;
  cudnnIndicesType_t reduceTensorIndicesType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnReduceTensorDescriptor_t, cudnnReduceTensorOp_t, cudnnDataType_t,
      cudnnNanPropagation_t, cudnnReduceTensorIndices_t, cudnnIndicesType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &reduceTensorDesc, sizeof(cudnnReduceTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &reduceTensorOp, sizeof(cudnnReduceTensorOp_t)) < 0 ||
      rpc_read(conn, &reduceTensorCompType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &reduceTensorNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_read(conn, &reduceTensorIndices, sizeof(cudnnReduceTensorIndices_t)) <
          0 ||
      rpc_read(conn, &reduceTensorIndicesType, sizeof(cudnnIndicesType_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetReduceTensorDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(reduceTensorDesc, reduceTensorOp,
                          reduceTensorCompType, reduceTensorNanOpt,
                          reduceTensorIndices, reduceTensorIndicesType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetReduceTensorDescriptor(conn_t *conn) {
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  cudnnReduceTensorOp_t reduceTensorOp{};
  reduceTensorOp = {};
  cudnnDataType_t reduceTensorCompType{};
  reduceTensorCompType = {};
  cudnnNanPropagation_t reduceTensorNanOpt{};
  reduceTensorNanOpt = {};
  cudnnReduceTensorIndices_t reduceTensorIndices{};
  reduceTensorIndices = {};
  cudnnIndicesType_t reduceTensorIndicesType{};
  reduceTensorIndicesType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      const cudnnReduceTensorDescriptor_t, cudnnReduceTensorOp_t *,
      cudnnDataType_t *, cudnnNanPropagation_t *, cudnnReduceTensorIndices_t *,
      cudnnIndicesType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &reduceTensorDesc,
               sizeof(const cudnnReduceTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetReduceTensorDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(reduceTensorDesc, &reduceTensorOp,
                          &reduceTensorCompType, &reduceTensorNanOpt,
                          &reduceTensorIndices, &reduceTensorIndicesType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &reduceTensorOp, sizeof(cudnnReduceTensorOp_t)) < 0 ||
      rpc_write(conn, &reduceTensorCompType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &reduceTensorNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &reduceTensorIndices,
                sizeof(cudnnReduceTensorIndices_t)) < 0 ||
      rpc_write(conn, &reduceTensorIndicesType, sizeof(cudnnIndicesType_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyReduceTensorDescriptor(conn_t *conn) {
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnReduceTensorDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &reduceTensorDesc, sizeof(cudnnReduceTensorDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyReduceTensorDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(reduceTensorDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetReductionIndicesSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  cudnnTensorDescriptor_t aDesc;
  cudnnTensorDescriptor_t cDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnReduceTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &reduceTensorDesc,
               sizeof(const cudnnReduceTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &aDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &cDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetReductionIndicesSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, reduceTensorDesc, aDesc, cDesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetReductionWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  cudnnTensorDescriptor_t aDesc;
  cudnnTensorDescriptor_t cDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnReduceTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &reduceTensorDesc,
               sizeof(const cudnnReduceTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &aDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &cDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetReductionWorkspaceSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, reduceTensorDesc, aDesc, cDesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnReduceTensor(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnReduceTensorDescriptor_t reduceTensorDesc;
  void *indices;
  size_t indicesSizeInBytes;
  void *workspace;
  size_t workspaceSizeInBytes;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t aDesc;
  const void *A;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t cDesc;
  void *C;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnReduceTensorDescriptor_t, void *, size_t,
      void *, size_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &reduceTensorDesc,
               sizeof(const cudnnReduceTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &indices, sizeof(void *)) < 0 ||
      rpc_read(conn, &indicesSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &aDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &cDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &C, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnReduceTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, reduceTensorDesc, indices, indicesSizeInBytes, workspace,
               workspaceSizeInBytes,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               aDesc, A,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               cDesc, C);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetTensor(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  uint32_t valuePtr_width;
  alignas(16) unsigned char valuePtr_value[64];
  std::vector<unsigned char> valuePtr_heap;
  const void *valuePtr = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 void *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &valuePtr_width, sizeof(valuePtr_width)) < 0 ||
      (valuePtr_width == 0 &&
       rpc_read(conn, &valuePtr, sizeof(valuePtr)) < 0) ||
      (valuePtr_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(valuePtr_heap, valuePtr_value,
                                   sizeof(valuePtr_value), valuePtr_width),
                valuePtr_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, yDesc, y,
               valuePtr_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         valuePtr_heap, valuePtr_value, sizeof(valuePtr_value),
                         valuePtr_width))
                   : valuePtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnScaleTensor(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 void *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnScaleTensor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, yDesc, y,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateFilterDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc{};
  filterDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFilterDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateFilterDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&filterDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &filterDesc, sizeof(cudnnFilterDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetFilter4dDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  cudnnDataType_t dataType;
  cudnnTensorFormat_t format;
  int k;
  int c;
  int h;
  int w;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFilterDescriptor_t, cudnnDataType_t,
                                 cudnnTensorFormat_t, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &c, sizeof(int)) < 0 ||
      rpc_read(conn, &h, sizeof(int)) < 0 ||
      rpc_read(conn, &w, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetFilter4dDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(filterDesc, dataType, format, k, c, h, w);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetFilter4dDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  cudnnDataType_t dataType{};
  dataType = {};
  cudnnTensorFormat_t format{};
  format = {};
  int k{};
  k = {};
  int c{};
  c = {};
  int h{};
  h = {};
  int w{};
  w = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(const cudnnFilterDescriptor_t, cudnnDataType_t *,
                        cudnnTensorFormat_t *, int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetFilter4dDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(filterDesc, &dataType, &format, &k, &c, &h, &w);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_write(conn, &k, sizeof(int)) < 0 ||
      rpc_write(conn, &c, sizeof(int)) < 0 ||
      rpc_write(conn, &h, sizeof(int)) < 0 ||
      rpc_write(conn, &w, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetFilterNdDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  cudnnDataType_t dataType;
  cudnnTensorFormat_t format;
  int nbDims;
  int *filterDimA = nullptr;
  size_t filterDimA_size;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFilterDescriptor_t, cudnnDataType_t,
                                 cudnnTensorFormat_t, int, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  filterDimA_size = nbDims * sizeof(const int);
  filterDimA = (int *)malloc(filterDimA_size);
  if (filterDimA_size != 0 && filterDimA == nullptr)
    goto ERROR_0;
  if ((filterDimA_size != 0 &&
       rpc_read(conn, filterDimA, filterDimA_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetFilterNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(filterDesc, dataType, format, nbDims,
               (nbDims * sizeof(const int) == 0 ? nullptr : filterDimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)filterDimA);
  return 0;
ERROR_0:
  free((void *)filterDimA);
  return -1;
}

int handle_cudnnGetFilterNdDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  int nbDimsRequested;
  cudnnDataType_t dataType{};
  dataType = {};
  cudnnTensorFormat_t format{};
  format = {};
  int nbDims{};
  nbDims = {};
  int *filterDimA = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(const cudnnFilterDescriptor_t, int, cudnnDataType_t *,
                        cudnnTensorFormat_t *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &nbDimsRequested, sizeof(int)) < 0 ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  filterDimA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && filterDimA == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetFilterNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(filterDesc, nbDimsRequested, &dataType, &format, &nbDims,
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : filterDimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &format, sizeof(cudnnTensorFormat_t)) < 0 ||
      rpc_write(conn, &nbDims, sizeof(int)) < 0 ||
      rpc_write(conn, filterDimA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)filterDimA);
  return 0;
ERROR_0:
  free((void *)filterDimA);
  return -1;
}

int handle_cudnnGetFilterSizeInBytes(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  size_t size{};
  size = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnFilterDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetFilterSizeInBytes");
  return_value = fn == nullptr ? function_not_found() : fn(filterDesc, &size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnTransformFilter(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorTransformDescriptor_t transDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnFilterDescriptor_t srcDesc;
  const void *srcData;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnFilterDescriptor_t destDesc;
  void *destData;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorTransformDescriptor_t, const void *,
      const cudnnFilterDescriptor_t, const void *, const void *,
      const cudnnFilterDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &transDesc,
               sizeof(const cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &srcDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &srcData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &destDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &destData, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnTransformFilter");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, transDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               srcDesc, srcData,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               destDesc, destData);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyFilterDescriptor(conn_t *conn) {
  cudnnFilterDescriptor_t filterDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFilterDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &filterDesc, sizeof(cudnnFilterDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyFilterDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(filterDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSoftmaxForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSoftmaxAlgorithm_t algo;
  cudnnSoftmaxMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnSoftmaxAlgorithm_t, cudnnSoftmaxMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnSoftmaxAlgorithm_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnSoftmaxMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSoftmaxForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreatePoolingDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc{};
  poolingDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnPoolingDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreatePoolingDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&poolingDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &poolingDesc, sizeof(cudnnPoolingDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetPooling2dDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  cudnnPoolingMode_t mode;
  cudnnNanPropagation_t maxpoolingNanOpt;
  int windowHeight;
  int windowWidth;
  int verticalPadding;
  int horizontalPadding;
  int verticalStride;
  int horizontalStride;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnPoolingDescriptor_t, cudnnPoolingMode_t,
                        cudnnNanPropagation_t, int, int, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(cudnnPoolingDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnPoolingMode_t)) < 0 ||
      rpc_read(conn, &maxpoolingNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_read(conn, &windowHeight, sizeof(int)) < 0 ||
      rpc_read(conn, &windowWidth, sizeof(int)) < 0 ||
      rpc_read(conn, &verticalPadding, sizeof(int)) < 0 ||
      rpc_read(conn, &horizontalPadding, sizeof(int)) < 0 ||
      rpc_read(conn, &verticalStride, sizeof(int)) < 0 ||
      rpc_read(conn, &horizontalStride, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetPooling2dDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(poolingDesc, mode, maxpoolingNanOpt, windowHeight,
                          windowWidth, verticalPadding, horizontalPadding,
                          verticalStride, horizontalStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetPooling2dDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  cudnnPoolingMode_t mode{};
  mode = {};
  cudnnNanPropagation_t maxpoolingNanOpt{};
  maxpoolingNanOpt = {};
  int windowHeight{};
  windowHeight = {};
  int windowWidth{};
  windowWidth = {};
  int verticalPadding{};
  verticalPadding = {};
  int horizontalPadding{};
  horizontalPadding = {};
  int verticalStride{};
  verticalStride = {};
  int horizontalStride{};
  horizontalStride = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnPoolingDescriptor_t,
                                 cudnnPoolingMode_t *, cudnnNanPropagation_t *,
                                 int *, int *, int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetPooling2dDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(poolingDesc, &mode, &maxpoolingNanOpt, &windowHeight,
                          &windowWidth, &verticalPadding, &horizontalPadding,
                          &verticalStride, &horizontalStride);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cudnnPoolingMode_t)) < 0 ||
      rpc_write(conn, &maxpoolingNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &windowHeight, sizeof(int)) < 0 ||
      rpc_write(conn, &windowWidth, sizeof(int)) < 0 ||
      rpc_write(conn, &verticalPadding, sizeof(int)) < 0 ||
      rpc_write(conn, &horizontalPadding, sizeof(int)) < 0 ||
      rpc_write(conn, &verticalStride, sizeof(int)) < 0 ||
      rpc_write(conn, &horizontalStride, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetPoolingNdDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  cudnnPoolingMode_t mode;
  cudnnNanPropagation_t maxpoolingNanOpt;
  int nbDims;
  int *windowDimA = nullptr;
  size_t windowDimA_size;
  int *paddingA = nullptr;
  size_t paddingA_size;
  int *strideA = nullptr;
  size_t strideA_size;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnPoolingDescriptor_t, const cudnnPoolingMode_t,
      const cudnnNanPropagation_t, int, const int *, const int *, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(cudnnPoolingDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(const cudnnPoolingMode_t)) < 0 ||
      rpc_read(conn, &maxpoolingNanOpt, sizeof(const cudnnNanPropagation_t)) <
          0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  windowDimA_size = nbDims * sizeof(const int);
  windowDimA = (int *)malloc(windowDimA_size);
  if (windowDimA_size != 0 && windowDimA == nullptr)
    goto ERROR_0;
  if ((windowDimA_size != 0 &&
       rpc_read(conn, windowDimA, windowDimA_size) < 0) ||
      nbDims < 0 || false)
    goto ERROR_0;
  paddingA_size = nbDims * sizeof(const int);
  paddingA = (int *)malloc(paddingA_size);
  if (paddingA_size != 0 && paddingA == nullptr)
    goto ERROR_0;
  if ((paddingA_size != 0 && rpc_read(conn, paddingA, paddingA_size) < 0) ||
      nbDims < 0 || false)
    goto ERROR_0;
  strideA_size = nbDims * sizeof(const int);
  strideA = (int *)malloc(strideA_size);
  if (strideA_size != 0 && strideA == nullptr)
    goto ERROR_0;
  if ((strideA_size != 0 && rpc_read(conn, strideA, strideA_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetPoolingNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(poolingDesc, mode, maxpoolingNanOpt, nbDims,
               (nbDims * sizeof(const int) == 0 ? nullptr : windowDimA),
               (nbDims * sizeof(const int) == 0 ? nullptr : paddingA),
               (nbDims * sizeof(const int) == 0 ? nullptr : strideA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)strideA);
  free((void *)paddingA);
  free((void *)windowDimA);
  return 0;
ERROR_0:
  free((void *)strideA);
  free((void *)paddingA);
  free((void *)windowDimA);
  return -1;
}

int handle_cudnnGetPoolingNdDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  int nbDimsRequested;
  cudnnPoolingMode_t mode{};
  mode = {};
  cudnnNanPropagation_t maxpoolingNanOpt{};
  maxpoolingNanOpt = {};
  int nbDims{};
  nbDims = {};
  int *windowDimA = nullptr;
  int *paddingA = nullptr;
  int *strideA = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnPoolingDescriptor_t, int,
                                 cudnnPoolingMode_t *, cudnnNanPropagation_t *,
                                 int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      rpc_read(conn, &nbDimsRequested, sizeof(int)) < 0 ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  windowDimA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && windowDimA == nullptr) ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  paddingA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && paddingA == nullptr) ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  strideA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && strideA == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetPoolingNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(poolingDesc, nbDimsRequested, &mode, &maxpoolingNanOpt, &nbDims,
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : windowDimA),
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : paddingA),
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : strideA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cudnnPoolingMode_t)) < 0 ||
      rpc_write(conn, &maxpoolingNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &nbDims, sizeof(int)) < 0 ||
      rpc_write(conn, windowDimA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, paddingA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, strideA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)strideA);
  free((void *)paddingA);
  free((void *)windowDimA);
  return 0;
ERROR_0:
  free((void *)strideA);
  free((void *)paddingA);
  free((void *)windowDimA);
  return -1;
}

int handle_cudnnGetPoolingNdForwardOutputDim(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  cudnnTensorDescriptor_t inputTensorDesc;
  int nbDims;
  int *outputTensorDimA = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnPoolingDescriptor_t,
                                 const cudnnTensorDescriptor_t, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      rpc_read(conn, &inputTensorDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  outputTensorDimA = (int *)malloc(nbDims * sizeof(int));
  if ((nbDims * sizeof(int) != 0 && outputTensorDimA == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetPoolingNdForwardOutputDim");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(poolingDesc, inputTensorDesc, nbDims,
               (nbDims * sizeof(int) == 0 ? nullptr : outputTensorDimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, outputTensorDimA, nbDims * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)outputTensorDimA);
  return 0;
ERROR_0:
  free((void *)outputTensorDimA);
  return -1;
}

int handle_cudnnGetPooling2dForwardOutputDim(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  cudnnTensorDescriptor_t inputTensorDesc;
  int n{};
  n = {};
  int c{};
  c = {};
  int h{};
  h = {};
  int w{};
  w = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnPoolingDescriptor_t,
                                 const cudnnTensorDescriptor_t, int *, int *,
                                 int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      rpc_read(conn, &inputTensorDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetPooling2dForwardOutputDim");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(poolingDesc, inputTensorDesc, &n, &c, &h, &w);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &n, sizeof(int)) < 0 ||
      rpc_write(conn, &c, sizeof(int)) < 0 ||
      rpc_write(conn, &h, sizeof(int)) < 0 ||
      rpc_write(conn, &w, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyPoolingDescriptor(conn_t *conn) {
  cudnnPoolingDescriptor_t poolingDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnPoolingDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolingDesc, sizeof(cudnnPoolingDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyPoolingDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(poolingDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnPoolingForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnPoolingDescriptor_t poolingDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnPoolingDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const void *,
                                 const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnPoolingForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, poolingDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateActivationDescriptor(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc{};
  activationDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnActivationDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateActivationDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&activationDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetActivationDescriptor(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc;
  cudnnActivationMode_t mode;
  cudnnNanPropagation_t reluNanOpt;
  double coef;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnActivationDescriptor_t, cudnnActivationMode_t,
                        cudnnNanPropagation_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &mode, sizeof(cudnnActivationMode_t)) < 0 ||
      rpc_read(conn, &reluNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_read(conn, &coef, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetActivationDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(activationDesc, mode, reluNanOpt, coef);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetActivationDescriptor(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc;
  cudnnActivationMode_t mode{};
  mode = {};
  cudnnNanPropagation_t reluNanOpt{};
  reluNanOpt = {};
  double coef{};
  coef = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnActivationDescriptor_t,
                                 cudnnActivationMode_t *,
                                 cudnnNanPropagation_t *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetActivationDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(activationDesc, &mode, &reluNanOpt, &coef);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cudnnActivationMode_t)) < 0 ||
      rpc_write(conn, &reluNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &coef, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetActivationDescriptorSwishBeta(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc;
  double swish_beta;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnActivationDescriptor_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &swish_beta, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetActivationDescriptorSwishBeta");
  return_value =
      fn == nullptr ? function_not_found() : fn(activationDesc, swish_beta);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetActivationDescriptorSwishBeta(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc;
  double swish_beta{};
  swish_beta = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnActivationDescriptor_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetActivationDescriptorSwishBeta");
  return_value =
      fn == nullptr ? function_not_found() : fn(activationDesc, &swish_beta);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &swish_beta, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyActivationDescriptor(conn_t *conn) {
  cudnnActivationDescriptor_t activationDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnActivationDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyActivationDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(activationDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnActivationForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnActivationDescriptor_t activationDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnActivationDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const void *,
                                 const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnActivationForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, activationDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateLRNDescriptor(conn_t *conn) {
  cudnnLRNDescriptor_t normDesc{};
  normDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnLRNDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateLRNDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&normDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetLRNDescriptor(conn_t *conn) {
  cudnnLRNDescriptor_t normDesc;
  unsigned lrnN;
  double lrnAlpha;
  double lrnBeta;
  double lrnK;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnLRNDescriptor_t, unsigned, double, double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_read(conn, &lrnN, sizeof(unsigned)) < 0 ||
      rpc_read(conn, &lrnAlpha, sizeof(double)) < 0 ||
      rpc_read(conn, &lrnBeta, sizeof(double)) < 0 ||
      rpc_read(conn, &lrnK, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetLRNDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(normDesc, lrnN, lrnAlpha, lrnBeta, lrnK);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetLRNDescriptor(conn_t *conn) {
  cudnnLRNDescriptor_t normDesc;
  unsigned lrnN{};
  lrnN = {};
  double lrnAlpha{};
  lrnAlpha = {};
  double lrnBeta{};
  lrnBeta = {};
  double lrnK{};
  lrnK = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnLRNDescriptor_t, unsigned *, double *,
                                 double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetLRNDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(normDesc, &lrnN, &lrnAlpha, &lrnBeta, &lrnK);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lrnN, sizeof(unsigned)) < 0 ||
      rpc_write(conn, &lrnAlpha, sizeof(double)) < 0 ||
      rpc_write(conn, &lrnBeta, sizeof(double)) < 0 ||
      rpc_write(conn, &lrnK, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyLRNDescriptor(conn_t *conn) {
  cudnnLRNDescriptor_t lrnDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnLRNDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &lrnDesc, sizeof(cudnnLRNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyLRNDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(lrnDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnLRNCrossChannelForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnLRNDescriptor_t normDesc;
  cudnnLRNMode_t lrnMode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnLRNDescriptor_t, cudnnLRNMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_read(conn, &lrnMode, sizeof(cudnnLRNMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnLRNCrossChannelForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, normDesc, lrnMode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDivisiveNormalizationForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnLRNDescriptor_t normDesc;
  cudnnDivNormMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  const void *means;
  void *temp;
  void *temp2;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnLRNDescriptor_t, cudnnDivNormMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *, void *, void *,
      const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnDivNormMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &means, sizeof(const void *)) < 0 ||
      rpc_read(conn, &temp, sizeof(void *)) < 0 ||
      rpc_read(conn, &temp2, sizeof(void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDivisiveNormalizationForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, normDesc, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x, means, temp, temp2,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDeriveBNTensorDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t derivedBnDesc;
  cudnnTensorDescriptor_t xDesc;
  cudnnBatchNormMode_t mode;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
                        cudnnBatchNormMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &derivedBnDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDeriveBNTensorDescriptor");
  return_value =
      fn == nullptr ? function_not_found() : fn(derivedBnDesc, xDesc, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBatchNormalizationForwardInference(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  cudnnTensorDescriptor_t bnScaleBiasMeanVarDesc;
  const void *bnScale;
  const void *bnBias;
  const void *estimatedMean;
  const void *estimatedVariance;
  double epsilon;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      const void *, const void *, const void *, const void *, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &bnScaleBiasMeanVarDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bnScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bnBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &estimatedMean, sizeof(const void *)) < 0 ||
      rpc_read(conn, &estimatedVariance, sizeof(const void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBatchNormalizationForwardInference");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               xDesc, x, yDesc, y, bnScaleBiasMeanVarDesc, bnScale, bnBias,
               estimatedMean, estimatedVariance, epsilon);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDeriveNormTensorDescriptor(conn_t *conn) {
  cudnnTensorDescriptor_t derivedNormScaleBiasDesc;
  cudnnTensorDescriptor_t derivedNormMeanVarDesc;
  cudnnTensorDescriptor_t xDesc;
  cudnnNormMode_t mode;
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnTensorDescriptor_t, cudnnTensorDescriptor_t,
                        const cudnnTensorDescriptor_t, cudnnNormMode_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &derivedNormScaleBiasDesc,
               sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &derivedNormMeanVarDesc, sizeof(cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDeriveNormTensorDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(derivedNormScaleBiasDesc, derivedNormMeanVarDesc,
                          xDesc, mode, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnNormalizationForwardInference(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t normScaleBiasDesc;
  const void *normScale;
  const void *normBias;
  cudnnTensorDescriptor_t normMeanVarDesc;
  const void *estimatedMean;
  const void *estimatedVariance;
  cudnnTensorDescriptor_t zDesc;
  const void *z;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  double epsilon;
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t, cudnnNormAlgo_t,
      const void *, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *, cudnnActivationDescriptor_t,
      const cudnnTensorDescriptor_t, void *, double, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normScaleBiasDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &normScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normMeanVarDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &estimatedMean, sizeof(const void *)) < 0 ||
      rpc_read(conn, &estimatedVariance, sizeof(const void *)) < 0 ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &z, sizeof(const void *)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnNormalizationForwardInference");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode, normOps, algo,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               xDesc, x, normScaleBiasDesc, normScale, normBias,
               normMeanVarDesc, estimatedMean, estimatedVariance, zDesc, z,
               activationDesc, yDesc, y, epsilon, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateSpatialTransformerDescriptor(conn_t *conn) {
  cudnnSpatialTransformerDescriptor_t stDesc{};
  stDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnSpatialTransformerDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateSpatialTransformerDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&stDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &stDesc, sizeof(cudnnSpatialTransformerDescriptor_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetSpatialTransformerNdDescriptor(conn_t *conn) {
  cudnnSpatialTransformerDescriptor_t stDesc;
  cudnnSamplerType_t samplerType;
  cudnnDataType_t dataType;
  int nbDims;
  int *dimA = nullptr;
  size_t dimA_size;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnSpatialTransformerDescriptor_t, cudnnSamplerType_t,
                        cudnnDataType_t, const int, const int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stDesc, sizeof(cudnnSpatialTransformerDescriptor_t)) <
          0 ||
      rpc_read(conn, &samplerType, sizeof(cudnnSamplerType_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  dimA_size = nbDims * sizeof(const int);
  dimA = (int *)malloc(dimA_size);
  if (dimA_size != 0 && dimA == nullptr)
    goto ERROR_0;
  if ((dimA_size != 0 && rpc_read(conn, dimA, dimA_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetSpatialTransformerNdDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(stDesc, samplerType, dataType, nbDims,
                          (nbDims * sizeof(const int) == 0 ? nullptr : dimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)dimA);
  return -1;
}

int handle_cudnnDestroySpatialTransformerDescriptor(conn_t *conn) {
  cudnnSpatialTransformerDescriptor_t stDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnSpatialTransformerDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stDesc, sizeof(cudnnSpatialTransformerDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroySpatialTransformerDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(stDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSpatialTfGridGeneratorForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSpatialTransformerDescriptor_t stDesc;
  const void *theta;
  void *grid;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t,
                                 const cudnnSpatialTransformerDescriptor_t,
                                 const void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &stDesc,
               sizeof(const cudnnSpatialTransformerDescriptor_t)) < 0 ||
      rpc_read(conn, &theta, sizeof(const void *)) < 0 ||
      rpc_read(conn, &grid, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSpatialTfGridGeneratorForward");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, stDesc, theta, grid);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSpatialTfSamplerForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSpatialTransformerDescriptor_t stDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  const void *grid;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnSpatialTransformerDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *, const void *,
      cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &stDesc, sizeof(cudnnSpatialTransformerDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &grid, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSpatialTfSamplerForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, stDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x, grid,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateDropoutDescriptor(conn_t *conn) {
  cudnnDropoutDescriptor_t dropoutDesc{};
  dropoutDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnDropoutDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateDropoutDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&dropoutDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyDropoutDescriptor(conn_t *conn) {
  cudnnDropoutDescriptor_t dropoutDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnDropoutDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyDropoutDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(dropoutDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDropoutGetStatesSize(conn_t *conn) {
  cudnnHandle_t handle;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDropoutGetStatesSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDropoutGetReserveSpaceSize(conn_t *conn) {
  cudnnTensorDescriptor_t xdesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &xdesc, sizeof(cudnnTensorDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDropoutGetReserveSpaceSize");
  return_value = fn == nullptr ? function_not_found() : fn(xdesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetDropoutDescriptor(conn_t *conn) {
  cudnnDropoutDescriptor_t dropoutDesc;
  cudnnHandle_t handle;
  float dropout;
  void *states;
  size_t stateSizeInBytes;
  unsigned long long seed;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnDropoutDescriptor_t, cudnnHandle_t, float,
                                 void *, size_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &dropout, sizeof(float)) < 0 ||
      rpc_read(conn, &states, sizeof(void *)) < 0 ||
      rpc_read(conn, &stateSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &seed, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetDropoutDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dropoutDesc, handle, dropout, states,
                                    stateSizeInBytes, seed);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRestoreDropoutDescriptor(conn_t *conn) {
  cudnnDropoutDescriptor_t dropoutDesc;
  cudnnHandle_t handle;
  float dropout;
  void *states;
  size_t stateSizeInBytes;
  unsigned long long seed;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnDropoutDescriptor_t, cudnnHandle_t, float,
                                 void *, size_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &dropout, sizeof(float)) < 0 ||
      rpc_read(conn, &states, sizeof(void *)) < 0 ||
      rpc_read(conn, &stateSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &seed, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRestoreDropoutDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(dropoutDesc, handle, dropout, states,
                                    stateSizeInBytes, seed);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetDropoutDescriptor(conn_t *conn) {
  cudnnDropoutDescriptor_t dropoutDesc;
  cudnnHandle_t handle;
  float dropout{};
  dropout = {};
  void *states{};
  states = {};
  unsigned long long seed{};
  seed = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnDropoutDescriptor_t, cudnnHandle_t,
                                 float *, void **, unsigned long long *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetDropoutDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(dropoutDesc, handle, &dropout, &states, &seed);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dropout, sizeof(float)) < 0 ||
      rpc_write(conn, &states, sizeof(void *)) < 0 ||
      rpc_write(conn, &seed, sizeof(unsigned long long)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDropoutForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnDropoutDescriptor_t dropoutDesc;
  cudnnTensorDescriptor_t xdesc;
  const void *x;
  cudnnTensorDescriptor_t ydesc;
  void *y;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, const cudnnDropoutDescriptor_t,
                        const cudnnTensorDescriptor_t, const void *,
                        const cudnnTensorDescriptor_t, void *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &dropoutDesc, sizeof(const cudnnDropoutDescriptor_t)) <
          0 ||
      rpc_read(conn, &xdesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ydesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDropoutForward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, dropoutDesc, xdesc, x, ydesc, y,
                                    reserveSpace, reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnOpsVersionCheck(conn_t *conn) {
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnOpsVersionCheck");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSoftmaxBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSoftmaxAlgorithm_t algo;
  cudnnSoftmaxMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t yDesc;
  const void *y;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnSoftmaxAlgorithm_t, cudnnSoftmaxMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnSoftmaxAlgorithm_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnSoftmaxMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSoftmaxBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, algo, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               yDesc, y, dyDesc, dy,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnPoolingBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnPoolingDescriptor_t poolingDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t yDesc;
  const void *y;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnPoolingDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const void *,
                                 const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &poolingDesc, sizeof(const cudnnPoolingDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnPoolingBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, poolingDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               yDesc, y, dyDesc, dy, xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnActivationBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnActivationDescriptor_t activationDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t yDesc;
  const void *y;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnActivationDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const cudnnTensorDescriptor_t,
                                 const void *, const void *,
                                 const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnActivationBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, activationDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               yDesc, y, dyDesc, dy, xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnLRNCrossChannelBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnLRNDescriptor_t normDesc;
  cudnnLRNMode_t lrnMode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t yDesc;
  const void *y;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnLRNDescriptor_t, cudnnLRNMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_read(conn, &lrnMode, sizeof(cudnnLRNMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnLRNCrossChannelBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, normDesc, lrnMode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               yDesc, y, dyDesc, dy, xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDivisiveNormalizationBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnLRNDescriptor_t normDesc;
  cudnnDivNormMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  const void *means;
  const void *dy;
  void *temp;
  void *temp2;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dXdMeansDesc;
  void *dx;
  void *dMeans;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnLRNDescriptor_t, cudnnDivNormMode_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *, const void *,
      void *, void *, const void *, const cudnnTensorDescriptor_t, void *,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &normDesc, sizeof(cudnnLRNDescriptor_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnDivNormMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &means, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &temp, sizeof(void *)) < 0 ||
      rpc_read(conn, &temp2, sizeof(void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dXdMeansDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dMeans, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDivisiveNormalizationBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, normDesc, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x, means, dy, temp, temp2,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dXdMeansDesc, dx, dMeans);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize(
    conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  cudnnBatchNormOps_t bnOps;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t zDesc;
  cudnnTensorDescriptor_t yDesc;
  cudnnTensorDescriptor_t bnScaleBiasMeanVarDesc;
  cudnnActivationDescriptor_t activationDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, cudnnBatchNormOps_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnActivationDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &bnOps, sizeof(cudnnBatchNormOps_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bnScaleBiasMeanVarDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>(
      "cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, bnOps, xDesc, zDesc, yDesc,
                          bnScaleBiasMeanVarDesc, activationDesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetBatchNormalizationBackwardExWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  cudnnBatchNormOps_t bnOps;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t yDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnTensorDescriptor_t dzDesc;
  cudnnTensorDescriptor_t dxDesc;
  cudnnTensorDescriptor_t dBnScaleBiasDesc;
  cudnnActivationDescriptor_t activationDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, cudnnBatchNormOps_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnActivationDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &bnOps, sizeof(cudnnBatchNormOps_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dzDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dBnScaleBiasDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetBatchNormalizationBackwardExWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, mode, bnOps, xDesc, yDesc, dyDesc,
                                    dzDesc, dxDesc, dBnScaleBiasDesc,
                                    activationDesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetBatchNormalizationTrainingExReserveSpaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  cudnnBatchNormOps_t bnOps;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t xDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, cudnnBatchNormMode_t,
                        cudnnBatchNormOps_t, const cudnnActivationDescriptor_t,
                        const cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &bnOps, sizeof(cudnnBatchNormOps_t)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>(
      "cudnnGetBatchNormalizationTrainingExReserveSpaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, mode, bnOps, activationDesc, xDesc,
                                    &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBatchNormalizationForwardTraining(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  cudnnTensorDescriptor_t bnScaleBiasMeanVarDesc;
  const void *bnScale;
  const void *bnBias;
  double exponentialAverageFactor;
  void *resultRunningMean;
  void *resultRunningVariance;
  double epsilon;
  void *resultSaveMean;
  void *resultSaveInvVariance;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      const void *, const void *, double, void *, void *, double, void *,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &bnScaleBiasMeanVarDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bnScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bnBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &exponentialAverageFactor, sizeof(double)) < 0 ||
      rpc_read(conn, &resultRunningMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultRunningVariance, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &resultSaveMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultSaveInvVariance, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBatchNormalizationForwardTraining");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               xDesc, x, yDesc, y, bnScaleBiasMeanVarDesc, bnScale, bnBias,
               exponentialAverageFactor, resultRunningMean,
               resultRunningVariance, epsilon, resultSaveMean,
               resultSaveInvVariance);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBatchNormalizationForwardTrainingEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  cudnnBatchNormOps_t bnOps;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *xData;
  cudnnTensorDescriptor_t zDesc;
  const void *zData;
  cudnnTensorDescriptor_t yDesc;
  void *yData;
  cudnnTensorDescriptor_t bnScaleBiasMeanVarDesc;
  const void *bnScale;
  const void *bnBias;
  double exponentialAverageFactor;
  void *resultRunningMean;
  void *resultRunningVariance;
  double epsilon;
  void *resultSaveMean;
  void *resultSaveInvVariance;
  cudnnActivationDescriptor_t activationDesc;
  void *workspace;
  size_t workSpaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, cudnnBatchNormOps_t, const void *,
      const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      const void *, const void *, double, void *, void *, double, void *,
      void *, cudnnActivationDescriptor_t, void *, size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &bnOps, sizeof(cudnnBatchNormOps_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &xData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &zData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yData, sizeof(void *)) < 0 ||
      rpc_read(conn, &bnScaleBiasMeanVarDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bnScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bnBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &exponentialAverageFactor, sizeof(double)) < 0 ||
      rpc_read(conn, &resultRunningMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultRunningVariance, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &resultSaveMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultSaveInvVariance, sizeof(void *)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBatchNormalizationForwardTrainingEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode, bnOps,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               xDesc, xData, zDesc, zData, yDesc, yData, bnScaleBiasMeanVarDesc,
               bnScale, bnBias, exponentialAverageFactor, resultRunningMean,
               resultRunningVariance, epsilon, resultSaveMean,
               resultSaveInvVariance, activationDesc, workspace,
               workSpaceSizeInBytes, reserveSpace, reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBatchNormalizationBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  uint32_t alphaDataDiff_width;
  alignas(16) unsigned char alphaDataDiff_value[64];
  std::vector<unsigned char> alphaDataDiff_heap;
  const void *alphaDataDiff = nullptr;
  uint32_t betaDataDiff_width;
  alignas(16) unsigned char betaDataDiff_value[64];
  std::vector<unsigned char> betaDataDiff_heap;
  const void *betaDataDiff = nullptr;
  uint32_t alphaParamDiff_width;
  alignas(16) unsigned char alphaParamDiff_value[64];
  std::vector<unsigned char> alphaParamDiff_heap;
  const void *alphaParamDiff = nullptr;
  uint32_t betaParamDiff_width;
  alignas(16) unsigned char betaParamDiff_value[64];
  std::vector<unsigned char> betaParamDiff_heap;
  const void *betaParamDiff = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  cudnnTensorDescriptor_t dBnScaleBiasDesc;
  const void *bnScale;
  void *dBnScaleResult;
  void *dBnBiasResult;
  double epsilon;
  const void *savedMean;
  const void *savedInvVariance;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, const void *, const void *,
      const void *, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      const void *, void *, void *, double, const void *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &alphaDataDiff_width, sizeof(alphaDataDiff_width)) < 0 ||
      (alphaDataDiff_width == 0 &&
       rpc_read(conn, &alphaDataDiff, sizeof(alphaDataDiff)) < 0) ||
      (alphaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaDataDiff_heap, alphaDataDiff_value,
                                   sizeof(alphaDataDiff_value),
                                   alphaDataDiff_width),
                alphaDataDiff_width) < 0) ||
      rpc_read(conn, &betaDataDiff_width, sizeof(betaDataDiff_width)) < 0 ||
      (betaDataDiff_width == 0 &&
       rpc_read(conn, &betaDataDiff, sizeof(betaDataDiff)) < 0) ||
      (betaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaDataDiff_heap, betaDataDiff_value,
                                   sizeof(betaDataDiff_value),
                                   betaDataDiff_width),
                betaDataDiff_width) < 0) ||
      rpc_read(conn, &alphaParamDiff_width, sizeof(alphaParamDiff_width)) < 0 ||
      (alphaParamDiff_width == 0 &&
       rpc_read(conn, &alphaParamDiff, sizeof(alphaParamDiff)) < 0) ||
      (alphaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaParamDiff_heap, alphaParamDiff_value,
                                   sizeof(alphaParamDiff_value),
                                   alphaParamDiff_width),
                alphaParamDiff_width) < 0) ||
      rpc_read(conn, &betaParamDiff_width, sizeof(betaParamDiff_width)) < 0 ||
      (betaParamDiff_width == 0 &&
       rpc_read(conn, &betaParamDiff, sizeof(betaParamDiff)) < 0) ||
      (betaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaParamDiff_heap, betaParamDiff_value,
                                   sizeof(betaParamDiff_value),
                                   betaParamDiff_width),
                betaParamDiff_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dBnScaleBiasDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &bnScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dBnScaleResult, sizeof(void *)) < 0 ||
      rpc_read(conn, &dBnBiasResult, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &savedMean, sizeof(const void *)) < 0 ||
      rpc_read(conn, &savedInvVariance, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBatchNormalizationBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode,
               alphaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaDataDiff_heap, alphaDataDiff_value,
                         sizeof(alphaDataDiff_value), alphaDataDiff_width))
                   : alphaDataDiff,
               betaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaDataDiff_heap, betaDataDiff_value,
                         sizeof(betaDataDiff_value), betaDataDiff_width))
                   : betaDataDiff,
               alphaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaParamDiff_heap, alphaParamDiff_value,
                         sizeof(alphaParamDiff_value), alphaParamDiff_width))
                   : alphaParamDiff,
               betaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaParamDiff_heap, betaParamDiff_value,
                         sizeof(betaParamDiff_value), betaParamDiff_width))
                   : betaParamDiff,
               xDesc, x, dyDesc, dy, dxDesc, dx, dBnScaleBiasDesc, bnScale,
               dBnScaleResult, dBnBiasResult, epsilon, savedMean,
               savedInvVariance);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBatchNormalizationBackwardEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnBatchNormMode_t mode;
  cudnnBatchNormOps_t bnOps;
  uint32_t alphaDataDiff_width;
  alignas(16) unsigned char alphaDataDiff_value[64];
  std::vector<unsigned char> alphaDataDiff_heap;
  const void *alphaDataDiff = nullptr;
  uint32_t betaDataDiff_width;
  alignas(16) unsigned char betaDataDiff_value[64];
  std::vector<unsigned char> betaDataDiff_heap;
  const void *betaDataDiff = nullptr;
  uint32_t alphaParamDiff_width;
  alignas(16) unsigned char alphaParamDiff_value[64];
  std::vector<unsigned char> alphaParamDiff_heap;
  const void *alphaParamDiff = nullptr;
  uint32_t betaParamDiff_width;
  alignas(16) unsigned char betaParamDiff_value[64];
  std::vector<unsigned char> betaParamDiff_heap;
  const void *betaParamDiff = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *xData;
  cudnnTensorDescriptor_t yDesc;
  const void *yData;
  cudnnTensorDescriptor_t dyDesc;
  const void *dyData;
  cudnnTensorDescriptor_t dzDesc;
  void *dzData;
  cudnnTensorDescriptor_t dxDesc;
  void *dxData;
  cudnnTensorDescriptor_t dBnScaleBiasDesc;
  const void *bnScaleData;
  const void *bnBiasData;
  void *dBnScaleData;
  void *dBnBiasData;
  double epsilon;
  const void *savedMean;
  const void *savedInvVariance;
  cudnnActivationDescriptor_t activationDesc;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnBatchNormMode_t, cudnnBatchNormOps_t, const void *,
      const void *, const void *, const void *, const cudnnTensorDescriptor_t,
      const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      void *, const cudnnTensorDescriptor_t, const void *, const void *, void *,
      void *, double, const void *, const void *, cudnnActivationDescriptor_t,
      void *, size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnBatchNormMode_t)) < 0 ||
      rpc_read(conn, &bnOps, sizeof(cudnnBatchNormOps_t)) < 0 ||
      rpc_read(conn, &alphaDataDiff_width, sizeof(alphaDataDiff_width)) < 0 ||
      (alphaDataDiff_width == 0 &&
       rpc_read(conn, &alphaDataDiff, sizeof(alphaDataDiff)) < 0) ||
      (alphaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaDataDiff_heap, alphaDataDiff_value,
                                   sizeof(alphaDataDiff_value),
                                   alphaDataDiff_width),
                alphaDataDiff_width) < 0) ||
      rpc_read(conn, &betaDataDiff_width, sizeof(betaDataDiff_width)) < 0 ||
      (betaDataDiff_width == 0 &&
       rpc_read(conn, &betaDataDiff, sizeof(betaDataDiff)) < 0) ||
      (betaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaDataDiff_heap, betaDataDiff_value,
                                   sizeof(betaDataDiff_value),
                                   betaDataDiff_width),
                betaDataDiff_width) < 0) ||
      rpc_read(conn, &alphaParamDiff_width, sizeof(alphaParamDiff_width)) < 0 ||
      (alphaParamDiff_width == 0 &&
       rpc_read(conn, &alphaParamDiff, sizeof(alphaParamDiff)) < 0) ||
      (alphaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaParamDiff_heap, alphaParamDiff_value,
                                   sizeof(alphaParamDiff_value),
                                   alphaParamDiff_width),
                alphaParamDiff_width) < 0) ||
      rpc_read(conn, &betaParamDiff_width, sizeof(betaParamDiff_width)) < 0 ||
      (betaParamDiff_width == 0 &&
       rpc_read(conn, &betaParamDiff, sizeof(betaParamDiff)) < 0) ||
      (betaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaParamDiff_heap, betaParamDiff_value,
                                   sizeof(betaParamDiff_value),
                                   betaParamDiff_width),
                betaParamDiff_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &xData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dzDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dzData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dxData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dBnScaleBiasDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &bnScaleData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bnBiasData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dBnScaleData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dBnBiasData, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &savedMean, sizeof(const void *)) < 0 ||
      rpc_read(conn, &savedInvVariance, sizeof(const void *)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBatchNormalizationBackwardEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode, bnOps,
               alphaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaDataDiff_heap, alphaDataDiff_value,
                         sizeof(alphaDataDiff_value), alphaDataDiff_width))
                   : alphaDataDiff,
               betaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaDataDiff_heap, betaDataDiff_value,
                         sizeof(betaDataDiff_value), betaDataDiff_width))
                   : betaDataDiff,
               alphaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaParamDiff_heap, alphaParamDiff_value,
                         sizeof(alphaParamDiff_value), alphaParamDiff_width))
                   : alphaParamDiff,
               betaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaParamDiff_heap, betaParamDiff_value,
                         sizeof(betaParamDiff_value), betaParamDiff_width))
                   : betaParamDiff,
               xDesc, xData, yDesc, yData, dyDesc, dyData, dzDesc, dzData,
               dxDesc, dxData, dBnScaleBiasDesc, bnScaleData, bnBiasData,
               dBnScaleData, dBnBiasData, epsilon, savedMean, savedInvVariance,
               activationDesc, workSpace, workSpaceSizeInBytes, reserveSpace,
               reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetNormalizationForwardTrainingWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t zDesc;
  cudnnTensorDescriptor_t yDesc;
  cudnnTensorDescriptor_t normScaleBiasDesc;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t normMeanVarDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t, cudnnNormAlgo_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnActivationDescriptor_t, const cudnnTensorDescriptor_t,
      size_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &normScaleBiasDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      rpc_read(conn, &normMeanVarDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetNormalizationForwardTrainingWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, mode, normOps, algo, xDesc, zDesc,
                                    yDesc, normScaleBiasDesc, activationDesc,
                                    normMeanVarDesc, &sizeInBytes, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetNormalizationBackwardWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t yDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnTensorDescriptor_t dzDesc;
  cudnnTensorDescriptor_t dxDesc;
  cudnnTensorDescriptor_t dNormScaleBiasDesc;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t normMeanVarDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t, cudnnNormAlgo_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnActivationDescriptor_t, const cudnnTensorDescriptor_t,
      size_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dzDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dNormScaleBiasDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      rpc_read(conn, &normMeanVarDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetNormalizationBackwardWorkspaceSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, mode, normOps, algo, xDesc, yDesc, dyDesc,
                          dzDesc, dxDesc, dNormScaleBiasDesc, activationDesc,
                          normMeanVarDesc, &sizeInBytes, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetNormalizationTrainingReserveSpaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t xDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t,
                        cudnnNormAlgo_t, const cudnnActivationDescriptor_t,
                        const cudnnTensorDescriptor_t, size_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetNormalizationTrainingReserveSpaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, mode, normOps, algo, activationDesc,
                                    xDesc, &sizeInBytes, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnNormalizationForwardTraining(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *xData;
  cudnnTensorDescriptor_t normScaleBiasDesc;
  const void *normScale;
  const void *normBias;
  double exponentialAverageFactor;
  cudnnTensorDescriptor_t normMeanVarDesc;
  void *resultRunningMean;
  void *resultRunningVariance;
  double epsilon;
  void *resultSaveMean;
  void *resultSaveInvVariance;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t zDesc;
  const void *zData;
  cudnnTensorDescriptor_t yDesc;
  void *yData;
  void *workspace;
  size_t workSpaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t, cudnnNormAlgo_t,
      const void *, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *, double,
      const cudnnTensorDescriptor_t, void *, void *, double, void *, void *,
      cudnnActivationDescriptor_t, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, void *, size_t, void *, size_t,
      int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &xData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normScaleBiasDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &normScale, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &exponentialAverageFactor, sizeof(double)) < 0 ||
      rpc_read(conn, &normMeanVarDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &resultRunningMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultRunningVariance, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &resultSaveMean, sizeof(void *)) < 0 ||
      rpc_read(conn, &resultSaveInvVariance, sizeof(void *)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &zData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yData, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnNormalizationForwardTraining");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode, normOps, algo,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               xDesc, xData, normScaleBiasDesc, normScale, normBias,
               exponentialAverageFactor, normMeanVarDesc, resultRunningMean,
               resultRunningVariance, epsilon, resultSaveMean,
               resultSaveInvVariance, activationDesc, zDesc, zData, yDesc,
               yData, workspace, workSpaceSizeInBytes, reserveSpace,
               reserveSpaceSizeInBytes, groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnNormalizationBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnNormMode_t mode;
  cudnnNormOps_t normOps;
  cudnnNormAlgo_t algo;
  uint32_t alphaDataDiff_width;
  alignas(16) unsigned char alphaDataDiff_value[64];
  std::vector<unsigned char> alphaDataDiff_heap;
  const void *alphaDataDiff = nullptr;
  uint32_t betaDataDiff_width;
  alignas(16) unsigned char betaDataDiff_value[64];
  std::vector<unsigned char> betaDataDiff_heap;
  const void *betaDataDiff = nullptr;
  uint32_t alphaParamDiff_width;
  alignas(16) unsigned char alphaParamDiff_value[64];
  std::vector<unsigned char> alphaParamDiff_heap;
  const void *alphaParamDiff = nullptr;
  uint32_t betaParamDiff_width;
  alignas(16) unsigned char betaParamDiff_value[64];
  std::vector<unsigned char> betaParamDiff_heap;
  const void *betaParamDiff = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *xData;
  cudnnTensorDescriptor_t yDesc;
  const void *yData;
  cudnnTensorDescriptor_t dyDesc;
  const void *dyData;
  cudnnTensorDescriptor_t dzDesc;
  void *dzData;
  cudnnTensorDescriptor_t dxDesc;
  void *dxData;
  cudnnTensorDescriptor_t dNormScaleBiasDesc;
  const void *normScaleData;
  const void *normBiasData;
  void *dNormScaleData;
  void *dNormBiasData;
  double epsilon;
  cudnnTensorDescriptor_t normMeanVarDesc;
  const void *savedMean;
  const void *savedInvVariance;
  cudnnActivationDescriptor_t activationDesc;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int groupCnt;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnNormMode_t, cudnnNormOps_t, cudnnNormAlgo_t,
      const void *, const void *, const void *, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, void *, const cudnnTensorDescriptor_t,
      void *, const cudnnTensorDescriptor_t, const void *, const void *, void *,
      void *, double, const cudnnTensorDescriptor_t, const void *, const void *,
      cudnnActivationDescriptor_t, void *, size_t, void *, size_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnNormMode_t)) < 0 ||
      rpc_read(conn, &normOps, sizeof(cudnnNormOps_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnNormAlgo_t)) < 0 ||
      rpc_read(conn, &alphaDataDiff_width, sizeof(alphaDataDiff_width)) < 0 ||
      (alphaDataDiff_width == 0 &&
       rpc_read(conn, &alphaDataDiff, sizeof(alphaDataDiff)) < 0) ||
      (alphaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaDataDiff_heap, alphaDataDiff_value,
                                   sizeof(alphaDataDiff_value),
                                   alphaDataDiff_width),
                alphaDataDiff_width) < 0) ||
      rpc_read(conn, &betaDataDiff_width, sizeof(betaDataDiff_width)) < 0 ||
      (betaDataDiff_width == 0 &&
       rpc_read(conn, &betaDataDiff, sizeof(betaDataDiff)) < 0) ||
      (betaDataDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaDataDiff_heap, betaDataDiff_value,
                                   sizeof(betaDataDiff_value),
                                   betaDataDiff_width),
                betaDataDiff_width) < 0) ||
      rpc_read(conn, &alphaParamDiff_width, sizeof(alphaParamDiff_width)) < 0 ||
      (alphaParamDiff_width == 0 &&
       rpc_read(conn, &alphaParamDiff, sizeof(alphaParamDiff)) < 0) ||
      (alphaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaParamDiff_heap, alphaParamDiff_value,
                                   sizeof(alphaParamDiff_value),
                                   alphaParamDiff_width),
                alphaParamDiff_width) < 0) ||
      rpc_read(conn, &betaParamDiff_width, sizeof(betaParamDiff_width)) < 0 ||
      (betaParamDiff_width == 0 &&
       rpc_read(conn, &betaParamDiff, sizeof(betaParamDiff)) < 0) ||
      (betaParamDiff_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaParamDiff_heap, betaParamDiff_value,
                                   sizeof(betaParamDiff_value),
                                   betaParamDiff_width),
                betaParamDiff_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &xData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &yData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dzDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dzData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dxData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dNormScaleBiasDesc,
               sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &normScaleData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &normBiasData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dNormScaleData, sizeof(void *)) < 0 ||
      rpc_read(conn, &dNormBiasData, sizeof(void *)) < 0 ||
      rpc_read(conn, &epsilon, sizeof(double)) < 0 ||
      rpc_read(conn, &normMeanVarDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &savedMean, sizeof(const void *)) < 0 ||
      rpc_read(conn, &savedInvVariance, sizeof(const void *)) < 0 ||
      rpc_read(conn, &activationDesc, sizeof(cudnnActivationDescriptor_t)) <
          0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &groupCnt, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnNormalizationBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, mode, normOps, algo,
               alphaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaDataDiff_heap, alphaDataDiff_value,
                         sizeof(alphaDataDiff_value), alphaDataDiff_width))
                   : alphaDataDiff,
               betaDataDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaDataDiff_heap, betaDataDiff_value,
                         sizeof(betaDataDiff_value), betaDataDiff_width))
                   : betaDataDiff,
               alphaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaParamDiff_heap, alphaParamDiff_value,
                         sizeof(alphaParamDiff_value), alphaParamDiff_width))
                   : alphaParamDiff,
               betaParamDiff_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaParamDiff_heap, betaParamDiff_value,
                         sizeof(betaParamDiff_value), betaParamDiff_width))
                   : betaParamDiff,
               xDesc, xData, yDesc, yData, dyDesc, dyData, dzDesc, dzData,
               dxDesc, dxData, dNormScaleBiasDesc, normScaleData, normBiasData,
               dNormScaleData, dNormBiasData, epsilon, normMeanVarDesc,
               savedMean, savedInvVariance, activationDesc, workSpace,
               workSpaceSizeInBytes, reserveSpace, reserveSpaceSizeInBytes,
               groupCnt);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSpatialTfGridGeneratorBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSpatialTransformerDescriptor_t stDesc;
  const void *dgrid;
  void *dtheta;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t,
                                 const cudnnSpatialTransformerDescriptor_t,
                                 const void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &stDesc,
               sizeof(const cudnnSpatialTransformerDescriptor_t)) < 0 ||
      rpc_read(conn, &dgrid, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dtheta, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSpatialTfGridGeneratorBackward");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, stDesc, dgrid, dtheta);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSpatialTfSamplerBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnSpatialTransformerDescriptor_t stDesc;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  uint32_t alphaDgrid_width;
  alignas(16) unsigned char alphaDgrid_value[64];
  std::vector<unsigned char> alphaDgrid_heap;
  const void *alphaDgrid = nullptr;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  const void *grid;
  uint32_t betaDgrid_width;
  alignas(16) unsigned char betaDgrid_value[64];
  std::vector<unsigned char> betaDgrid_heap;
  const void *betaDgrid = nullptr;
  void *dgrid;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnSpatialTransformerDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *,
      const cudnnTensorDescriptor_t, void *, const void *,
      const cudnnTensorDescriptor_t, const void *, const void *, const void *,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &stDesc, sizeof(cudnnSpatialTransformerDescriptor_t)) <
          0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &alphaDgrid_width, sizeof(alphaDgrid_width)) < 0 ||
      (alphaDgrid_width == 0 &&
       rpc_read(conn, &alphaDgrid, sizeof(alphaDgrid)) < 0) ||
      (alphaDgrid_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alphaDgrid_heap, alphaDgrid_value,
                                   sizeof(alphaDgrid_value), alphaDgrid_width),
                alphaDgrid_width) < 0) ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &grid, sizeof(const void *)) < 0 ||
      rpc_read(conn, &betaDgrid_width, sizeof(betaDgrid_width)) < 0 ||
      (betaDgrid_width == 0 &&
       rpc_read(conn, &betaDgrid, sizeof(betaDgrid)) < 0) ||
      (betaDgrid_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(betaDgrid_heap, betaDgrid_value,
                                   sizeof(betaDgrid_value), betaDgrid_width),
                betaDgrid_width) < 0) ||
      rpc_read(conn, &dgrid, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSpatialTfSamplerBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, stDesc,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx,
               alphaDgrid_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         alphaDgrid_heap, alphaDgrid_value,
                         sizeof(alphaDgrid_value), alphaDgrid_width))
                   : alphaDgrid,
               dyDesc, dy, grid,
               betaDgrid_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         betaDgrid_heap, betaDgrid_value,
                         sizeof(betaDgrid_value), betaDgrid_width))
                   : betaDgrid,
               dgrid);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDropoutBackward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnDropoutDescriptor_t dropoutDesc;
  cudnnTensorDescriptor_t dydesc;
  const void *dy;
  cudnnTensorDescriptor_t dxdesc;
  void *dx;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, const cudnnDropoutDescriptor_t,
                        const cudnnTensorDescriptor_t, const void *,
                        const cudnnTensorDescriptor_t, void *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &dropoutDesc, sizeof(const cudnnDropoutDescriptor_t)) <
          0 ||
      rpc_read(conn, &dydesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dxdesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDropoutBackward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, dropoutDesc, dydesc, dy, dxdesc, dx,
                                    reserveSpace, reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateConvolutionDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc{};
  convDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateConvolutionDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&convDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyConvolutionDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyConvolutionDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(convDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetConvolutionMathType(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnMathType_t mathType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, cudnnMathType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_read(conn, &mathType, sizeof(cudnnMathType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetConvolutionMathType");
  return_value = fn == nullptr ? function_not_found() : fn(convDesc, mathType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionMathType(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnMathType_t mathType{};
  mathType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, cudnnMathType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionMathType");
  return_value = fn == nullptr ? function_not_found() : fn(convDesc, &mathType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mathType, sizeof(cudnnMathType_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetConvolutionGroupCount(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int groupCount;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_read(conn, &groupCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetConvolutionGroupCount");
  return_value =
      fn == nullptr ? function_not_found() : fn(convDesc, groupCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionGroupCount(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int groupCount{};
  groupCount = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionGroupCount");
  return_value =
      fn == nullptr ? function_not_found() : fn(convDesc, &groupCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &groupCount, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetConvolutionReorderType(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnReorderType_t reorderType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, cudnnReorderType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_read(conn, &reorderType, sizeof(cudnnReorderType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetConvolutionReorderType");
  return_value =
      fn == nullptr ? function_not_found() : fn(convDesc, reorderType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionReorderType(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnReorderType_t reorderType{};
  reorderType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, cudnnReorderType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionReorderType");
  return_value =
      fn == nullptr ? function_not_found() : fn(convDesc, &reorderType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &reorderType, sizeof(cudnnReorderType_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetConvolution2dDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int pad_h;
  int pad_w;
  int u;
  int v;
  int dilation_h;
  int dilation_w;
  cudnnConvolutionMode_t mode;
  cudnnDataType_t computeType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, int, int, int, int, int,
                        int, cudnnConvolutionMode_t, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_read(conn, &pad_h, sizeof(int)) < 0 ||
      rpc_read(conn, &pad_w, sizeof(int)) < 0 ||
      rpc_read(conn, &u, sizeof(int)) < 0 ||
      rpc_read(conn, &v, sizeof(int)) < 0 ||
      rpc_read(conn, &dilation_h, sizeof(int)) < 0 ||
      rpc_read(conn, &dilation_w, sizeof(int)) < 0 ||
      rpc_read(conn, &mode, sizeof(cudnnConvolutionMode_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetConvolution2dDescriptor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(convDesc, pad_h, pad_w, u, v, dilation_h,
                                    dilation_w, mode, computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolution2dDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int pad_h{};
  pad_h = {};
  int pad_w{};
  pad_w = {};
  int u{};
  u = {};
  int v{};
  v = {};
  int dilation_h{};
  dilation_h = {};
  int dilation_w{};
  dilation_w = {};
  cudnnConvolutionMode_t mode{};
  mode = {};
  cudnnDataType_t computeType{};
  computeType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnConvolutionDescriptor_t, int *,
                                 int *, int *, int *, int *, int *,
                                 cudnnConvolutionMode_t *, cudnnDataType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolution2dDescriptor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(convDesc, &pad_h, &pad_w, &u, &v, &dilation_h,
                          &dilation_w, &mode, &computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pad_h, sizeof(int)) < 0 ||
      rpc_write(conn, &pad_w, sizeof(int)) < 0 ||
      rpc_write(conn, &u, sizeof(int)) < 0 ||
      rpc_write(conn, &v, sizeof(int)) < 0 ||
      rpc_write(conn, &dilation_h, sizeof(int)) < 0 ||
      rpc_write(conn, &dilation_w, sizeof(int)) < 0 ||
      rpc_write(conn, &mode, sizeof(cudnnConvolutionMode_t)) < 0 ||
      rpc_write(conn, &computeType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetConvolutionNdDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int arrayLength;
  int *padA = nullptr;
  size_t padA_size;
  int *filterStrideA = nullptr;
  size_t filterStrideA_size;
  int *dilationA = nullptr;
  size_t dilationA_size;
  cudnnConvolutionMode_t mode;
  cudnnDataType_t computeType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnConvolutionDescriptor_t, int, const int *,
                                 const int *, const int *,
                                 cudnnConvolutionMode_t, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(cudnnConvolutionDescriptor_t)) < 0 ||
      rpc_read(conn, &arrayLength, sizeof(int)) < 0 || arrayLength < 0 || false)
    goto ERROR_0;
  padA_size = arrayLength * sizeof(const int);
  padA = (int *)malloc(padA_size);
  if (padA_size != 0 && padA == nullptr)
    goto ERROR_0;
  if ((padA_size != 0 && rpc_read(conn, padA, padA_size) < 0) ||
      arrayLength < 0 || false)
    goto ERROR_0;
  filterStrideA_size = arrayLength * sizeof(const int);
  filterStrideA = (int *)malloc(filterStrideA_size);
  if (filterStrideA_size != 0 && filterStrideA == nullptr)
    goto ERROR_0;
  if ((filterStrideA_size != 0 &&
       rpc_read(conn, filterStrideA, filterStrideA_size) < 0) ||
      arrayLength < 0 || false)
    goto ERROR_0;
  dilationA_size = arrayLength * sizeof(const int);
  dilationA = (int *)malloc(dilationA_size);
  if (dilationA_size != 0 && dilationA == nullptr)
    goto ERROR_0;
  if ((dilationA_size != 0 && rpc_read(conn, dilationA, dilationA_size) < 0) ||
      rpc_read(conn, &mode, sizeof(cudnnConvolutionMode_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetConvolutionNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(convDesc, arrayLength,
               (arrayLength * sizeof(const int) == 0 ? nullptr : padA),
               (arrayLength * sizeof(const int) == 0 ? nullptr : filterStrideA),
               (arrayLength * sizeof(const int) == 0 ? nullptr : dilationA),
               mode, computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dilationA);
  free((void *)filterStrideA);
  free((void *)padA);
  return 0;
ERROR_0:
  free((void *)dilationA);
  free((void *)filterStrideA);
  free((void *)padA);
  return -1;
}

int handle_cudnnGetConvolutionNdDescriptor(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  int arrayLengthRequested;
  int arrayLength{};
  arrayLength = {};
  int *padA = nullptr;
  int *strideA = nullptr;
  int *dilationA = nullptr;
  cudnnConvolutionMode_t mode{};
  mode = {};
  cudnnDataType_t computeType{};
  computeType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnConvolutionDescriptor_t, int, int *,
                                 int *, int *, int *, cudnnConvolutionMode_t *,
                                 cudnnDataType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &arrayLengthRequested, sizeof(int)) < 0 ||
      arrayLengthRequested < 0 || false)
    goto ERROR_0;
  padA = (int *)malloc(arrayLengthRequested * sizeof(int));
  if ((arrayLengthRequested * sizeof(int) != 0 && padA == nullptr) ||
      arrayLengthRequested < 0 || false)
    goto ERROR_0;
  strideA = (int *)malloc(arrayLengthRequested * sizeof(int));
  if ((arrayLengthRequested * sizeof(int) != 0 && strideA == nullptr) ||
      arrayLengthRequested < 0 || false)
    goto ERROR_0;
  dilationA = (int *)malloc(arrayLengthRequested * sizeof(int));
  if ((arrayLengthRequested * sizeof(int) != 0 && dilationA == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionNdDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(convDesc, arrayLengthRequested, &arrayLength,
               (arrayLengthRequested * sizeof(int) == 0 ? nullptr : padA),
               (arrayLengthRequested * sizeof(int) == 0 ? nullptr : strideA),
               (arrayLengthRequested * sizeof(int) == 0 ? nullptr : dilationA),
               &mode, &computeType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &arrayLength, sizeof(int)) < 0 ||
      rpc_write(conn, padA, arrayLengthRequested * sizeof(int)) < 0 ||
      rpc_write(conn, strideA, arrayLengthRequested * sizeof(int)) < 0 ||
      rpc_write(conn, dilationA, arrayLengthRequested * sizeof(int)) < 0 ||
      rpc_write(conn, &mode, sizeof(cudnnConvolutionMode_t)) < 0 ||
      rpc_write(conn, &computeType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dilationA);
  free((void *)strideA);
  free((void *)padA);
  return 0;
ERROR_0:
  free((void *)dilationA);
  free((void *)strideA);
  free((void *)padA);
  return -1;
}

int handle_cudnnGetConvolution2dForwardOutputDim(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t inputTensorDesc;
  cudnnFilterDescriptor_t filterDesc;
  int n{};
  n = {};
  int c{};
  c = {};
  int h{};
  h = {};
  int w{};
  w = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      const cudnnConvolutionDescriptor_t, const cudnnTensorDescriptor_t,
      const cudnnFilterDescriptor_t, int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &inputTensorDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolution2dForwardOutputDim");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(convDesc, inputTensorDesc, filterDesc, &n, &c, &h, &w);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &n, sizeof(int)) < 0 ||
      rpc_write(conn, &c, sizeof(int)) < 0 ||
      rpc_write(conn, &h, sizeof(int)) < 0 ||
      rpc_write(conn, &w, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionNdForwardOutputDim(conn_t *conn) {
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t inputTensorDesc;
  cudnnFilterDescriptor_t filterDesc;
  int nbDims;
  int *tensorOuputDimA = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(const cudnnConvolutionDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 const cudnnFilterDescriptor_t, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &inputTensorDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  tensorOuputDimA = (int *)malloc(nbDims * sizeof(int));
  if ((nbDims * sizeof(int) != 0 && tensorOuputDimA == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionNdForwardOutputDim");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(convDesc, inputTensorDesc, filterDesc, nbDims,
               (nbDims * sizeof(int) == 0 ? nullptr : tensorOuputDimA));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, tensorOuputDimA, nbDims * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)tensorOuputDimA);
  return 0;
ERROR_0:
  free((void *)tensorOuputDimA);
  return -1;
}

int handle_cudnnGetConvolutionForwardAlgorithmMaxCount(conn_t *conn) {
  cudnnHandle_t handle;
  int count{};
  count = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionForwardAlgorithmMaxCount");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionForwardAlgorithm_v7(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t srcDesc;
  cudnnFilterDescriptor_t filterDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t destDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionFwdAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 const cudnnFilterDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnTensorDescriptor_t, const int,
                                 int *, cudnnConvolutionFwdAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &srcDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &destDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionFwdAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionForwardAlgorithm_v7");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, srcDesc, filterDesc, convDesc, destDesc,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) == 0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnFindConvolutionForwardAlgorithm(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  cudnnFilterDescriptor_t wDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t yDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionFwdAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 const cudnnFilterDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnTensorDescriptor_t, const int,
                                 int *, cudnnConvolutionFwdAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionFwdAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionForwardAlgorithm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, xDesc, wDesc, convDesc, yDesc, requestedAlgoCount,
               &returnedAlgoCount,
               (requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) == 0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnFindConvolutionForwardAlgorithmEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnFilterDescriptor_t wDesc;
  const void *w;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionFwdAlgoPerf_t *perfResults = nullptr;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorDescriptor_t, const void *,
      const cudnnFilterDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, const cudnnTensorDescriptor_t, void *,
      const int, int *, cudnnConvolutionFwdAlgoPerf_t *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &w, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionFwdAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionForwardAlgorithmEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, xDesc, x, wDesc, w, convDesc, yDesc, y,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t) == 0
                    ? nullptr
                    : perfResults),
               workSpace, workSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount * sizeof(cudnnConvolutionFwdAlgoPerf_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnIm2Col(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnFilterDescriptor_t wDesc;
  cudnnConvolutionDescriptor_t convDesc;
  void *colBuffer;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 const void *, const cudnnFilterDescriptor_t,
                                 const cudnnConvolutionDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &colBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnIm2Col");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, xDesc, x, wDesc, convDesc, colBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnReorderFilterAndBias(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t filterDesc;
  cudnnReorderType_t reorderType;
  const void *filterData;
  void *reorderedFilterData;
  int reorderBias;
  const void *biasData;
  void *reorderedBiasData;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnFilterDescriptor_t,
                                 cudnnReorderType_t, const void *, void *, int,
                                 const void *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &reorderType, sizeof(cudnnReorderType_t)) < 0 ||
      rpc_read(conn, &filterData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &reorderedFilterData, sizeof(void *)) < 0 ||
      rpc_read(conn, &reorderBias, sizeof(int)) < 0 ||
      rpc_read(conn, &biasData, sizeof(const void *)) < 0 ||
      rpc_read(conn, &reorderedBiasData, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnReorderFilterAndBias");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, filterDesc, reorderType, filterData,
                                    reorderedFilterData, reorderBias, biasData,
                                    reorderedBiasData);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionForwardWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  cudnnFilterDescriptor_t wDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t yDesc;
  cudnnConvolutionFwdAlgo_t algo;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorDescriptor_t,
      const cudnnFilterDescriptor_t, const cudnnConvolutionDescriptor_t,
      const cudnnTensorDescriptor_t, cudnnConvolutionFwdAlgo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionFwdAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionForwardWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, xDesc, wDesc, convDesc, yDesc, algo,
                                    &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnConvolutionForward(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnFilterDescriptor_t wDesc;
  const void *w;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnConvolutionFwdAlgo_t algo;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnFilterDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, cudnnConvolutionFwdAlgo_t, void *,
      size_t, const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &w, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionFwdAlgo_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnConvolutionForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x, wDesc, w, convDesc, algo, workSpace,
               workSpaceSizeInBytes,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnConvolutionBiasActivationForward(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha1_width;
  alignas(16) unsigned char alpha1_value[64];
  std::vector<unsigned char> alpha1_heap;
  const void *alpha1 = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnFilterDescriptor_t wDesc;
  const void *w;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnConvolutionFwdAlgo_t algo;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  uint32_t alpha2_width;
  alignas(16) unsigned char alpha2_value[64];
  std::vector<unsigned char> alpha2_heap;
  const void *alpha2 = nullptr;
  cudnnTensorDescriptor_t zDesc;
  const void *z;
  cudnnTensorDescriptor_t biasDesc;
  const void *bias;
  cudnnActivationDescriptor_t activationDesc;
  cudnnTensorDescriptor_t yDesc;
  void *y;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnFilterDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, cudnnConvolutionFwdAlgo_t, void *,
      size_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnActivationDescriptor_t, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha1_width, sizeof(alpha1_width)) < 0 ||
      (alpha1_width == 0 && rpc_read(conn, &alpha1, sizeof(alpha1)) < 0) ||
      (alpha1_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha1_heap, alpha1_value,
                                   sizeof(alpha1_value), alpha1_width),
                alpha1_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &w, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionFwdAlgo_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &alpha2_width, sizeof(alpha2_width)) < 0 ||
      (alpha2_width == 0 && rpc_read(conn, &alpha2, sizeof(alpha2)) < 0) ||
      (alpha2_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha2_heap, alpha2_value,
                                   sizeof(alpha2_value), alpha2_width),
                alpha2_width) < 0) ||
      rpc_read(conn, &zDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &z, sizeof(const void *)) < 0 ||
      rpc_read(conn, &biasDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &activationDesc,
               sizeof(const cudnnActivationDescriptor_t)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnConvolutionBiasActivationForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha1_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha1_heap, alpha1_value,
                                            sizeof(alpha1_value), alpha1_width))
                   : alpha1,
               xDesc, x, wDesc, w, convDesc, algo, workSpace,
               workSpaceSizeInBytes,
               alpha2_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha2_heap, alpha2_value,
                                            sizeof(alpha2_value), alpha2_width))
                   : alpha2,
               zDesc, z, biasDesc, bias, activationDesc, yDesc, y);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionBackwardDataAlgorithmMaxCount(conn_t *conn) {
  cudnnHandle_t handle;
  int count{};
  count = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardDataAlgorithmMaxCount");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnFindConvolutionBackwardDataAlgorithm(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t wDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t dxDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdDataAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnFilterDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnTensorDescriptor_t, const int,
                                 int *, cudnnConvolutionBwdDataAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdDataAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionBackwardDataAlgorithm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, wDesc, dyDesc, convDesc, dxDesc, requestedAlgoCount,
               &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdDataAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdDataAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnFindConvolutionBackwardDataAlgorithmEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t wDesc;
  const void *w;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdDataAlgoPerf_t *perfResults = nullptr;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnFilterDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, const cudnnTensorDescriptor_t, void *,
      const int, int *, cudnnConvolutionBwdDataAlgoPerf_t *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &w, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdDataAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionBackwardDataAlgorithmEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, wDesc, w, dyDesc, dy, convDesc, dxDesc, dx,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdDataAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults),
               workSpace, workSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdDataAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnGetConvolutionBackwardDataAlgorithm_v7(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t filterDesc;
  cudnnTensorDescriptor_t diffDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t gradDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdDataAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnFilterDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnTensorDescriptor_t, const int,
                                 int *, cudnnConvolutionBwdDataAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &diffDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &gradDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdDataAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdDataAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardDataAlgorithm_v7");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, filterDesc, diffDesc, convDesc, gradDesc,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdDataAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdDataAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnGetConvolutionBackwardDataWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t wDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t dxDesc;
  cudnnConvolutionBwdDataAlgo_t algo;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnFilterDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnConvolutionDescriptor_t,
      const cudnnTensorDescriptor_t, cudnnConvolutionBwdDataAlgo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionBwdDataAlgo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardDataWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, wDesc, dyDesc, convDesc, dxDesc,
                                    algo, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnConvolutionBackwardData(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnFilterDescriptor_t wDesc;
  const void *w;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnConvolutionBwdDataAlgo_t algo;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dxDesc;
  void *dx;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnFilterDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, cudnnConvolutionBwdDataAlgo_t, void *,
      size_t, const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &wDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &w, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionBwdDataAlgo_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dxDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnConvolutionBackwardData");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               wDesc, w, dyDesc, dy, convDesc, algo, workSpace,
               workSpaceSizeInBytes,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dxDesc, dx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetFoldedConvBackwardDataDescriptors(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFilterDescriptor_t filterDesc;
  cudnnTensorDescriptor_t diffDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnTensorDescriptor_t gradDesc;
  cudnnTensorFormat_t transformFormat;
  cudnnFilterDescriptor_t foldedFilterDesc;
  cudnnTensorDescriptor_t paddedDiffDesc;
  cudnnConvolutionDescriptor_t foldedConvDesc;
  cudnnTensorDescriptor_t foldedGradDesc;
  cudnnTensorTransformDescriptor_t filterFoldTransDesc;
  cudnnTensorTransformDescriptor_t diffPadTransDesc;
  cudnnTensorTransformDescriptor_t gradFoldTransDesc;
  cudnnTensorTransformDescriptor_t gradUnfoldTransDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      const cudnnHandle_t, const cudnnFilterDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnConvolutionDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorFormat_t,
      cudnnFilterDescriptor_t, cudnnTensorDescriptor_t,
      cudnnConvolutionDescriptor_t, cudnnTensorDescriptor_t,
      cudnnTensorTransformDescriptor_t, cudnnTensorTransformDescriptor_t,
      cudnnTensorTransformDescriptor_t, cudnnTensorTransformDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(const cudnnHandle_t)) < 0 ||
      rpc_read(conn, &filterDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &diffDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &gradDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &transformFormat, sizeof(const cudnnTensorFormat_t)) < 0 ||
      rpc_read(conn, &foldedFilterDesc, sizeof(cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &paddedDiffDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &foldedConvDesc, sizeof(cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &foldedGradDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &filterFoldTransDesc,
               sizeof(cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &diffPadTransDesc,
               sizeof(cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &gradFoldTransDesc,
               sizeof(cudnnTensorTransformDescriptor_t)) < 0 ||
      rpc_read(conn, &gradUnfoldTransDesc,
               sizeof(cudnnTensorTransformDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetFoldedConvBackwardDataDescriptors");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, filterDesc, diffDesc, convDesc, gradDesc,
               transformFormat, foldedFilterDesc, paddedDiffDesc,
               foldedConvDesc, foldedGradDesc, filterFoldTransDesc,
               diffPadTransDesc, gradFoldTransDesc, gradUnfoldTransDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCnnVersionCheck(conn_t *conn) {
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCnnVersionCheck");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetConvolutionBackwardFilterAlgorithmMaxCount(conn_t *conn) {
  cudnnHandle_t handle;
  int count{};
  count = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardFilterAlgorithmMaxCount");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnFindConvolutionBackwardFilterAlgorithm(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnFilterDescriptor_t dwDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdFilterAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnFilterDescriptor_t, const int,
                                 int *, cudnnConvolutionBwdFilterAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &dwDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdFilterAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionBackwardFilterAlgorithm");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, xDesc, dyDesc, convDesc, dwDesc, requestedAlgoCount,
               &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdFilterAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnFindConvolutionBackwardFilterAlgorithmEx(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t dyDesc;
  const void *y;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnFilterDescriptor_t dwDesc;
  void *dw;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdFilterAlgoPerf_t *perfResults = nullptr;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, const cudnnFilterDescriptor_t, void *,
      const int, int *, cudnnConvolutionBwdFilterAlgoPerf_t *, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &dwDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dw, sizeof(void *)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdFilterAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFindConvolutionBackwardFilterAlgorithmEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, xDesc, x, dyDesc, y, convDesc, dwDesc, dw,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults),
               workSpace, workSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdFilterAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnGetConvolutionBackwardFilterAlgorithm_v7(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t srcDesc;
  cudnnTensorDescriptor_t diffDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnFilterDescriptor_t gradDesc;
  int requestedAlgoCount;
  int returnedAlgoCount{};
  returnedAlgoCount = {};
  cudnnConvolutionBwdFilterAlgoPerf_t *perfResults = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnTensorDescriptor_t,
                                 const cudnnTensorDescriptor_t,
                                 const cudnnConvolutionDescriptor_t,
                                 const cudnnFilterDescriptor_t, const int,
                                 int *, cudnnConvolutionBwdFilterAlgoPerf_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &srcDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &diffDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &gradDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &requestedAlgoCount, sizeof(const int)) < 0 || false)
    goto ERROR_0;
  perfResults = (cudnnConvolutionBwdFilterAlgoPerf_t *)malloc(
      requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t));
  if ((requestedAlgoCount * sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) != 0 &&
       perfResults == nullptr) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardFilterAlgorithm_v7");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, srcDesc, diffDesc, convDesc, gradDesc,
               requestedAlgoCount, &returnedAlgoCount,
               (requestedAlgoCount *
                            sizeof(cudnnConvolutionBwdFilterAlgoPerf_t) ==
                        0
                    ? nullptr
                    : perfResults));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &returnedAlgoCount, sizeof(int)) < 0 ||
      rpc_write(conn, perfResults,
                requestedAlgoCount *
                    sizeof(cudnnConvolutionBwdFilterAlgoPerf_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)perfResults);
  return 0;
ERROR_0:
  free((void *)perfResults);
  return -1;
}

int handle_cudnnGetConvolutionBackwardFilterWorkspaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnTensorDescriptor_t xDesc;
  cudnnTensorDescriptor_t dyDesc;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnFilterDescriptor_t gradDesc;
  cudnnConvolutionBwdFilterAlgo_t algo;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnTensorDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnConvolutionDescriptor_t,
      const cudnnFilterDescriptor_t, cudnnConvolutionBwdFilterAlgo_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &gradDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionBwdFilterAlgo_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetConvolutionBackwardFilterWorkspaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, xDesc, dyDesc, convDesc, gradDesc,
                                    algo, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnConvolutionBackwardFilter(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  cudnnConvolutionDescriptor_t convDesc;
  cudnnConvolutionBwdFilterAlgo_t algo;
  void *workSpace;
  size_t workSpaceSizeInBytes;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnFilterDescriptor_t dwDesc;
  void *dw;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const cudnnTensorDescriptor_t, const void *,
      const cudnnConvolutionDescriptor_t, cudnnConvolutionBwdFilterAlgo_t,
      void *, size_t, const void *, const cudnnFilterDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &xDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &convDesc, sizeof(const cudnnConvolutionDescriptor_t)) <
          0 ||
      rpc_read(conn, &algo, sizeof(cudnnConvolutionBwdFilterAlgo_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dwDesc, sizeof(const cudnnFilterDescriptor_t)) < 0 ||
      rpc_read(conn, &dw, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnConvolutionBackwardFilter");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               xDesc, x, dyDesc, dy, convDesc, algo, workSpace,
               workSpaceSizeInBytes,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dwDesc, dw);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnConvolutionBackwardBias(conn_t *conn) {
  cudnnHandle_t handle;
  uint32_t alpha_width;
  alignas(16) unsigned char alpha_value[64];
  std::vector<unsigned char> alpha_heap;
  const void *alpha = nullptr;
  cudnnTensorDescriptor_t dyDesc;
  const void *dy;
  uint32_t beta_width;
  alignas(16) unsigned char beta_value[64];
  std::vector<unsigned char> beta_heap;
  const void *beta = nullptr;
  cudnnTensorDescriptor_t dbDesc;
  void *db;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const void *, const cudnnTensorDescriptor_t, const void *,
      const void *, const cudnnTensorDescriptor_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &alpha_width, sizeof(alpha_width)) < 0 ||
      (alpha_width == 0 && rpc_read(conn, &alpha, sizeof(alpha)) < 0) ||
      (alpha_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(alpha_heap, alpha_value, sizeof(alpha_value),
                                   alpha_width),
                alpha_width) < 0) ||
      rpc_read(conn, &dyDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &beta_width, sizeof(beta_width)) < 0 ||
      (beta_width == 0 && rpc_read(conn, &beta, sizeof(beta)) < 0) ||
      (beta_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(beta_heap, beta_value, sizeof(beta_value),
                                   beta_width),
                beta_width) < 0) ||
      rpc_read(conn, &dbDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &db, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnConvolutionBackwardBias");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               alpha_width != 0
                   ? reinterpret_cast<const void *>(
                         lupine_scalar_slot(alpha_heap, alpha_value,
                                            sizeof(alpha_value), alpha_width))
                   : alpha,
               dyDesc, dy,
               beta_width != 0
                   ? reinterpret_cast<const void *>(lupine_scalar_slot(
                         beta_heap, beta_value, sizeof(beta_value), beta_width))
                   : beta,
               dbDesc, db);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateFusedOpsConstParamPack(conn_t *conn) {
  cudnnFusedOpsConstParamPack_t constPack{};
  constPack = {};
  cudnnFusedOps_t ops;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnFusedOpsConstParamPack_t *, cudnnFusedOps_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ops, sizeof(cudnnFusedOps_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateFusedOpsConstParamPack");
  return_value = fn == nullptr ? function_not_found() : fn(&constPack, ops);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &constPack, sizeof(cudnnFusedOpsConstParamPack_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyFusedOpsConstParamPack(conn_t *conn) {
  cudnnFusedOpsConstParamPack_t constPack;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFusedOpsConstParamPack_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &constPack, sizeof(cudnnFusedOpsConstParamPack_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyFusedOpsConstParamPack");
  return_value = fn == nullptr ? function_not_found() : fn(constPack);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateFusedOpsVariantParamPack(conn_t *conn) {
  cudnnFusedOpsVariantParamPack_t varPack{};
  varPack = {};
  cudnnFusedOps_t ops;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnFusedOpsVariantParamPack_t *, cudnnFusedOps_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ops, sizeof(cudnnFusedOps_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateFusedOpsVariantParamPack");
  return_value = fn == nullptr ? function_not_found() : fn(&varPack, ops);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &varPack, sizeof(cudnnFusedOpsVariantParamPack_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyFusedOpsVariantParamPack(conn_t *conn) {
  cudnnFusedOpsVariantParamPack_t varPack;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFusedOpsVariantParamPack_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &varPack, sizeof(cudnnFusedOpsVariantParamPack_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyFusedOpsVariantParamPack");
  return_value = fn == nullptr ? function_not_found() : fn(varPack);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateFusedOpsPlan(conn_t *conn) {
  cudnnFusedOpsPlan_t plan{};
  plan = {};
  cudnnFusedOps_t ops;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFusedOpsPlan_t *, cudnnFusedOps_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ops, sizeof(cudnnFusedOps_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateFusedOpsPlan");
  return_value = fn == nullptr ? function_not_found() : fn(&plan, ops);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &plan, sizeof(cudnnFusedOpsPlan_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyFusedOpsPlan(conn_t *conn) {
  cudnnFusedOpsPlan_t plan;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnFusedOpsPlan_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &plan, sizeof(cudnnFusedOpsPlan_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyFusedOpsPlan");
  return_value = fn == nullptr ? function_not_found() : fn(plan);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnMakeFusedOpsPlan(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFusedOpsPlan_t plan;
  cudnnFusedOpsConstParamPack_t constPack;
  size_t workspaceSizeInBytes{};
  workspaceSizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnFusedOpsPlan_t,
                                 const cudnnFusedOpsConstParamPack_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &plan, sizeof(cudnnFusedOpsPlan_t)) < 0 ||
      rpc_read(conn, &constPack, sizeof(const cudnnFusedOpsConstParamPack_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnMakeFusedOpsPlan");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, plan, constPack, &workspaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnFusedOpsExecute(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnFusedOpsPlan_t plan;
  cudnnFusedOpsVariantParamPack_t varPack;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnFusedOpsPlan_t,
                                 cudnnFusedOpsVariantParamPack_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &plan, sizeof(const cudnnFusedOpsPlan_t)) < 0 ||
      rpc_read(conn, &varPack, sizeof(cudnnFusedOpsVariantParamPack_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFusedOpsExecute");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, plan, varPack);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateRNNDescriptor(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc{};
  rnnDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateRNNDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&rnnDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyRNNDescriptor(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyRNNDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(rnnDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetRNNDescriptor_v8(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNAlgo_t algo;
  cudnnRNNMode_t cellMode;
  cudnnRNNBiasMode_t biasMode;
  cudnnDirectionMode_t dirMode;
  cudnnRNNInputMode_t inputMode;
  cudnnDataType_t dataType;
  cudnnDataType_t mathPrec;
  cudnnMathType_t mathType;
  int32_t inputSize;
  int32_t hiddenSize;
  int32_t projSize;
  int32_t numLayers;
  cudnnDropoutDescriptor_t dropoutDesc;
  uint32_t auxFlags;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnRNNDescriptor_t, cudnnRNNAlgo_t, cudnnRNNMode_t, cudnnRNNBiasMode_t,
      cudnnDirectionMode_t, cudnnRNNInputMode_t, cudnnDataType_t,
      cudnnDataType_t, cudnnMathType_t, int32_t, int32_t, int32_t, int32_t,
      cudnnDropoutDescriptor_t, uint32_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnRNNAlgo_t)) < 0 ||
      rpc_read(conn, &cellMode, sizeof(cudnnRNNMode_t)) < 0 ||
      rpc_read(conn, &biasMode, sizeof(cudnnRNNBiasMode_t)) < 0 ||
      rpc_read(conn, &dirMode, sizeof(cudnnDirectionMode_t)) < 0 ||
      rpc_read(conn, &inputMode, sizeof(cudnnRNNInputMode_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &mathPrec, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &mathType, sizeof(cudnnMathType_t)) < 0 ||
      rpc_read(conn, &inputSize, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &hiddenSize, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &projSize, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &numLayers, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &auxFlags, sizeof(uint32_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetRNNDescriptor_v8");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(rnnDesc, algo, cellMode, biasMode, dirMode, inputMode,
                          dataType, mathPrec, mathType, inputSize, hiddenSize,
                          projSize, numLayers, dropoutDesc, auxFlags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetRNNDescriptor_v8(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNAlgo_t algo{};
  algo = {};
  cudnnRNNMode_t cellMode{};
  cellMode = {};
  cudnnRNNBiasMode_t biasMode{};
  biasMode = {};
  cudnnDirectionMode_t dirMode{};
  dirMode = {};
  cudnnRNNInputMode_t inputMode{};
  inputMode = {};
  cudnnDataType_t dataType{};
  dataType = {};
  cudnnDataType_t mathPrec{};
  mathPrec = {};
  cudnnMathType_t mathType{};
  mathType = {};
  int32_t inputSize{};
  inputSize = {};
  int32_t hiddenSize{};
  hiddenSize = {};
  int32_t projSize{};
  projSize = {};
  int32_t numLayers{};
  numLayers = {};
  cudnnDropoutDescriptor_t dropoutDesc{};
  dropoutDesc = {};
  uint32_t auxFlags{};
  auxFlags = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnRNNDescriptor_t, cudnnRNNAlgo_t *, cudnnRNNMode_t *,
      cudnnRNNBiasMode_t *, cudnnDirectionMode_t *, cudnnRNNInputMode_t *,
      cudnnDataType_t *, cudnnDataType_t *, cudnnMathType_t *, int32_t *,
      int32_t *, int32_t *, int32_t *, cudnnDropoutDescriptor_t *, uint32_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetRNNDescriptor_v8");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(rnnDesc, &algo, &cellMode, &biasMode, &dirMode, &inputMode,
               &dataType, &mathPrec, &mathType, &inputSize, &hiddenSize,
               &projSize, &numLayers, &dropoutDesc, &auxFlags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &algo, sizeof(cudnnRNNAlgo_t)) < 0 ||
      rpc_write(conn, &cellMode, sizeof(cudnnRNNMode_t)) < 0 ||
      rpc_write(conn, &biasMode, sizeof(cudnnRNNBiasMode_t)) < 0 ||
      rpc_write(conn, &dirMode, sizeof(cudnnDirectionMode_t)) < 0 ||
      rpc_write(conn, &inputMode, sizeof(cudnnRNNInputMode_t)) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &mathPrec, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &mathType, sizeof(cudnnMathType_t)) < 0 ||
      rpc_write(conn, &inputSize, sizeof(int32_t)) < 0 ||
      rpc_write(conn, &hiddenSize, sizeof(int32_t)) < 0 ||
      rpc_write(conn, &projSize, sizeof(int32_t)) < 0 ||
      rpc_write(conn, &numLayers, sizeof(int32_t)) < 0 ||
      rpc_write(conn, &dropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_write(conn, &auxFlags, sizeof(uint32_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNSetClip_v8(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNClipMode_t clipMode;
  cudnnNanPropagation_t clipNanOpt;
  double lclip;
  double rclip;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t, cudnnRNNClipMode_t,
                                 cudnnNanPropagation_t, double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &clipMode, sizeof(cudnnRNNClipMode_t)) < 0 ||
      rpc_read(conn, &clipNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_read(conn, &lclip, sizeof(double)) < 0 ||
      rpc_read(conn, &rclip, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNSetClip_v8");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(rnnDesc, clipMode, clipNanOpt, lclip, rclip);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNSetClip_v9(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNClipMode_t clipMode;
  double lclip;
  double rclip;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t, cudnnRNNClipMode_t,
                                 double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &clipMode, sizeof(cudnnRNNClipMode_t)) < 0 ||
      rpc_read(conn, &lclip, sizeof(double)) < 0 ||
      rpc_read(conn, &rclip, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNSetClip_v9");
  return_value = fn == nullptr ? function_not_found()
                               : fn(rnnDesc, clipMode, lclip, rclip);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNGetClip_v8(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNClipMode_t clipMode{};
  clipMode = {};
  cudnnNanPropagation_t clipNanOpt{};
  clipNanOpt = {};
  double lclip{};
  lclip = {};
  double rclip{};
  rclip = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t, cudnnRNNClipMode_t *,
                                 cudnnNanPropagation_t *, double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNGetClip_v8");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(rnnDesc, &clipMode, &clipNanOpt, &lclip, &rclip);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &clipMode, sizeof(cudnnRNNClipMode_t)) < 0 ||
      rpc_write(conn, &clipNanOpt, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &lclip, sizeof(double)) < 0 ||
      rpc_write(conn, &rclip, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNGetClip_v9(conn_t *conn) {
  cudnnRNNDescriptor_t rnnDesc;
  cudnnRNNClipMode_t clipMode{};
  clipMode = {};
  double lclip{};
  lclip = {};
  double rclip{};
  rclip = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDescriptor_t, cudnnRNNClipMode_t *,
                                 double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNGetClip_v9");
  return_value = fn == nullptr ? function_not_found()
                               : fn(rnnDesc, &clipMode, &lclip, &rclip);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &clipMode, sizeof(cudnnRNNClipMode_t)) < 0 ||
      rpc_write(conn, &lclip, sizeof(double)) < 0 ||
      rpc_write(conn, &rclip, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnBuildRNNDynamic(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  int miniBatch;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnRNNDescriptor_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &miniBatch, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnBuildRNNDynamic");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, rnnDesc, miniBatch);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetRNNTempSpaceSizes(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  cudnnForwardMode_t fwdMode;
  cudnnRNNDataDescriptor_t xDesc;
  size_t *workSpaceSize_null_check;
  size_t workSpaceSize;
  size_t *reserveSpaceSize_null_check;
  size_t reserveSpaceSize;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, cudnnRNNDescriptor_t, cudnnForwardMode_t,
                        cudnnRNNDataDescriptor_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &fwdMode, sizeof(cudnnForwardMode_t)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &workSpaceSize_null_check, sizeof(size_t *)) < 0 ||
      rpc_read(conn, &reserveSpaceSize_null_check, sizeof(size_t *)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetRNNTempSpaceSizes");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, rnnDesc, fwdMode, xDesc,
               workSpaceSize_null_check ? &workSpaceSize : nullptr,
               reserveSpaceSize_null_check ? &reserveSpaceSize : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workSpaceSize_null_check, sizeof(size_t *)) < 0 ||
      (workSpaceSize_null_check &&
       rpc_write(conn, &workSpaceSize, sizeof(size_t)) < 0) ||
      rpc_write(conn, &reserveSpaceSize_null_check, sizeof(size_t *)) < 0 ||
      (reserveSpaceSize_null_check &&
       rpc_write(conn, &reserveSpaceSize, sizeof(size_t)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetRNNWeightSpaceSize(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  size_t weightSpaceSize{};
  weightSpaceSize = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, cudnnRNNDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetRNNWeightSpaceSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, rnnDesc, &weightSpaceSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &weightSpaceSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetRNNWeightParams(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  int32_t pseudoLayer;
  size_t weightSpaceSize;
  const void *weightSpace;
  int32_t linLayerID;
  cudnnTensorDescriptor_t mDesc;
  void **mAddr_null_check;
  void *mAddr;
  cudnnTensorDescriptor_t bDesc;
  void **bAddr_null_check;
  void *bAddr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, cudnnRNNDescriptor_t, int32_t, size_t,
                        const void *, int32_t, cudnnTensorDescriptor_t, void **,
                        cudnnTensorDescriptor_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &pseudoLayer, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &weightSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &weightSpace, sizeof(const void *)) < 0 ||
      rpc_read(conn, &linLayerID, sizeof(int32_t)) < 0 ||
      rpc_read(conn, &mDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &mAddr_null_check, sizeof(void **)) < 0 ||
      rpc_read(conn, &bDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &bAddr_null_check, sizeof(void **)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetRNNWeightParams");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, rnnDesc, pseudoLayer,
                                    weightSpaceSize, weightSpace, linLayerID,
                                    mDesc, mAddr_null_check ? &mAddr : nullptr,
                                    bDesc, bAddr_null_check ? &bAddr : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mAddr_null_check, sizeof(void **)) < 0 ||
      (mAddr_null_check && rpc_write(conn, &mAddr, sizeof(void *)) < 0) ||
      rpc_write(conn, &bAddr_null_check, sizeof(void **)) < 0 ||
      (bAddr_null_check && rpc_write(conn, &bAddr, sizeof(void *)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateRNNDataDescriptor(conn_t *conn) {
  cudnnRNNDataDescriptor_t rnnDataDesc{};
  rnnDataDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDataDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateRNNDataDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&rnnDataDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rnnDataDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyRNNDataDescriptor(conn_t *conn) {
  cudnnRNNDataDescriptor_t rnnDataDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDataDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDataDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyRNNDataDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(rnnDataDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetRNNDataDescriptor(conn_t *conn) {
  cudnnRNNDataDescriptor_t rnnDataDesc;
  cudnnDataType_t dataType;
  cudnnRNNDataLayout_t layout;
  int maxSeqLength;
  int batchSize;
  int vectorSize;
  int *seqLengthArray = nullptr;
  size_t seqLengthArray_size;
  uint32_t paddingFill_width;
  alignas(16) unsigned char paddingFill_value[64];
  std::vector<unsigned char> paddingFill_heap;
  void *paddingFill = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDataDescriptor_t, cudnnDataType_t,
                                 cudnnRNNDataLayout_t, int, int, int,
                                 const int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDataDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &layout, sizeof(cudnnRNNDataLayout_t)) < 0 ||
      rpc_read(conn, &maxSeqLength, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &vectorSize, sizeof(int)) < 0 || batchSize < 0 || false)
    goto ERROR_0;
  seqLengthArray_size = batchSize * sizeof(const int);
  seqLengthArray = (int *)malloc(seqLengthArray_size);
  if (seqLengthArray_size != 0 && seqLengthArray == nullptr)
    goto ERROR_0;
  if ((seqLengthArray_size != 0 &&
       rpc_read(conn, seqLengthArray, seqLengthArray_size) < 0) ||
      rpc_read(conn, &paddingFill_width, sizeof(paddingFill_width)) < 0 ||
      (paddingFill_width == 0 &&
       rpc_read(conn, &paddingFill, sizeof(paddingFill)) < 0) ||
      (paddingFill_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(paddingFill_heap, paddingFill_value,
                                   sizeof(paddingFill_value),
                                   paddingFill_width),
                paddingFill_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetRNNDataDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(rnnDataDesc, dataType, layout, maxSeqLength, batchSize,
               vectorSize,
               (batchSize * sizeof(const int) == 0 ? nullptr : seqLengthArray),
               paddingFill_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         paddingFill_heap, paddingFill_value,
                         sizeof(paddingFill_value), paddingFill_width))
                   : paddingFill);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)seqLengthArray);
  return 0;
ERROR_0:
  free((void *)seqLengthArray);
  return -1;
}

int handle_cudnnGetRNNDataDescriptor(conn_t *conn) {
  cudnnRNNDataDescriptor_t rnnDataDesc;
  cudnnDataType_t dataType{};
  dataType = {};
  cudnnRNNDataLayout_t layout{};
  layout = {};
  int maxSeqLength{};
  maxSeqLength = {};
  int batchSize{};
  batchSize = {};
  int vectorSize{};
  vectorSize = {};
  int arrayLengthRequested;
  int *seqLengthArray = nullptr;
  uint32_t paddingFill_width;
  alignas(16) unsigned char paddingFill_value[64];
  std::vector<unsigned char> paddingFill_heap;
  void *paddingFill = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnRNNDataDescriptor_t, cudnnDataType_t *,
                                 cudnnRNNDataLayout_t *, int *, int *, int *,
                                 int, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rnnDataDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &arrayLengthRequested, sizeof(int)) < 0 ||
      arrayLengthRequested < 0 || false)
    goto ERROR_0;
  seqLengthArray = (int *)malloc(arrayLengthRequested * sizeof(int));
  if ((arrayLengthRequested * sizeof(int) != 0 && seqLengthArray == nullptr) ||
      rpc_read(conn, &paddingFill_width, sizeof(paddingFill_width)) < 0 ||
      (paddingFill_width == 0 &&
       rpc_read(conn, &paddingFill, sizeof(paddingFill)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetRNNDataDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(rnnDataDesc, &dataType, &layout, &maxSeqLength, &batchSize,
               &vectorSize, arrayLengthRequested,
               (arrayLengthRequested * sizeof(int) == 0 ? nullptr
                                                        : seqLengthArray),
               paddingFill_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         paddingFill_heap, paddingFill_value,
                         sizeof(paddingFill_value), paddingFill_width))
                   : paddingFill);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &layout, sizeof(cudnnRNNDataLayout_t)) < 0 ||
      rpc_write(conn, &maxSeqLength, sizeof(int)) < 0 ||
      rpc_write(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_write(conn, &vectorSize, sizeof(int)) < 0 ||
      rpc_write(conn, seqLengthArray, arrayLengthRequested * sizeof(int)) < 0 ||
      (paddingFill_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(paddingFill_heap, paddingFill_value,
                                    sizeof(paddingFill_value),
                                    paddingFill_width),
                 paddingFill_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)seqLengthArray);
  return 0;
ERROR_0:
  free((void *)seqLengthArray);
  return -1;
}

int handle_cudnnRNNForward(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  cudnnForwardMode_t fwdMode;
  const int32_t *devSeqLengths;
  cudnnRNNDataDescriptor_t xDesc;
  const void *x;
  cudnnRNNDataDescriptor_t yDesc;
  void *y;
  cudnnTensorDescriptor_t hDesc;
  const void *hx;
  void *hy;
  cudnnTensorDescriptor_t cDesc;
  const void *cx;
  void *cy;
  size_t weightSpaceSize;
  const void *weightSpace;
  size_t workSpaceSize;
  void *workSpace;
  size_t reserveSpaceSize;
  void *reserveSpace;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnRNNDescriptor_t, cudnnForwardMode_t, const int32_t *,
      cudnnRNNDataDescriptor_t, const void *, cudnnRNNDataDescriptor_t, void *,
      cudnnTensorDescriptor_t, const void *, void *, cudnnTensorDescriptor_t,
      const void *, void *, size_t, const void *, size_t, void *, size_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &fwdMode, sizeof(cudnnForwardMode_t)) < 0 ||
      rpc_read(conn, &devSeqLengths, sizeof(const int32_t *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &hDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &hx, sizeof(const void *)) < 0 ||
      rpc_read(conn, &hy, sizeof(void *)) < 0 ||
      rpc_read(conn, &cDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &cx, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cy, sizeof(void *)) < 0 ||
      rpc_read(conn, &weightSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &weightSpace, sizeof(const void *)) < 0 ||
      rpc_read(conn, &workSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNForward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, rnnDesc, fwdMode, devSeqLengths, xDesc, x, yDesc, y,
               hDesc, hx, hy, cDesc, cx, cy, weightSpaceSize, weightSpace,
               workSpaceSize, workSpace, reserveSpaceSize, reserveSpace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateSeqDataDescriptor(conn_t *conn) {
  cudnnSeqDataDescriptor_t seqDataDesc{};
  seqDataDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnSeqDataDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateSeqDataDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&seqDataDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &seqDataDesc, sizeof(cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroySeqDataDescriptor(conn_t *conn) {
  cudnnSeqDataDescriptor_t seqDataDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnSeqDataDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &seqDataDesc, sizeof(cudnnSeqDataDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroySeqDataDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(seqDataDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetSeqDataDescriptor(conn_t *conn) {
  cudnnSeqDataDescriptor_t seqDataDesc;
  cudnnDataType_t dataType;
  int nbDims;
  int *dimA = nullptr;
  size_t dimA_size;
  cudnnSeqDataAxis_t *axes = nullptr;
  size_t axes_size;
  size_t seqLengthArraySize;
  int *seqLengthArray = nullptr;
  size_t seqLengthArray_size;
  uint32_t paddingFill_width;
  alignas(16) unsigned char paddingFill_value[64];
  std::vector<unsigned char> paddingFill_heap;
  void *paddingFill = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnSeqDataDescriptor_t, cudnnDataType_t, int,
                                 const int *, const cudnnSeqDataAxis_t *,
                                 size_t, const int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &seqDataDesc, sizeof(cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &nbDims, sizeof(int)) < 0 || nbDims < 0 || false)
    goto ERROR_0;
  dimA_size = nbDims * sizeof(const int);
  dimA = (int *)malloc(dimA_size);
  if (dimA_size != 0 && dimA == nullptr)
    goto ERROR_0;
  if ((dimA_size != 0 && rpc_read(conn, dimA, dimA_size) < 0) || nbDims < 0 ||
      false)
    goto ERROR_0;
  axes_size = nbDims * sizeof(const cudnnSeqDataAxis_t);
  axes = (cudnnSeqDataAxis_t *)malloc(axes_size);
  if (axes_size != 0 && axes == nullptr)
    goto ERROR_0;
  if ((axes_size != 0 && rpc_read(conn, axes, axes_size) < 0) ||
      rpc_read(conn, &seqLengthArraySize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  seqLengthArray_size = seqLengthArraySize * sizeof(const int);
  seqLengthArray = (int *)malloc(seqLengthArray_size);
  if (seqLengthArray_size != 0 && seqLengthArray == nullptr)
    goto ERROR_0;
  if ((seqLengthArray_size != 0 &&
       rpc_read(conn, seqLengthArray, seqLengthArray_size) < 0) ||
      rpc_read(conn, &paddingFill_width, sizeof(paddingFill_width)) < 0 ||
      (paddingFill_width == 0 &&
       rpc_read(conn, &paddingFill, sizeof(paddingFill)) < 0) ||
      (paddingFill_width != 0 &&
       rpc_read(conn,
                lupine_scalar_slot(paddingFill_heap, paddingFill_value,
                                   sizeof(paddingFill_value),
                                   paddingFill_width),
                paddingFill_width) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetSeqDataDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(seqDataDesc, dataType, nbDims,
               (nbDims * sizeof(const int) == 0 ? nullptr : dimA),
               (nbDims * sizeof(const cudnnSeqDataAxis_t) == 0 ? nullptr
                                                               : axes),
               seqLengthArraySize,
               (seqLengthArraySize * sizeof(const int) == 0 ? nullptr
                                                            : seqLengthArray),
               paddingFill_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         paddingFill_heap, paddingFill_value,
                         sizeof(paddingFill_value), paddingFill_width))
                   : paddingFill);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)seqLengthArray);
  free((void *)axes);
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)seqLengthArray);
  free((void *)axes);
  free((void *)dimA);
  return -1;
}

int handle_cudnnGetSeqDataDescriptor(conn_t *conn) {
  cudnnSeqDataDescriptor_t seqDataDesc;
  cudnnDataType_t dataType{};
  dataType = {};
  int nbDims{};
  nbDims = {};
  int nbDimsRequested;
  int *dimA = nullptr;
  cudnnSeqDataAxis_t *axes = nullptr;
  size_t seqLengthArraySize{};
  seqLengthArraySize = {};
  size_t seqLengthSizeRequested;
  int *seqLengthArray = nullptr;
  uint32_t paddingFill_width;
  alignas(16) unsigned char paddingFill_value[64];
  std::vector<unsigned char> paddingFill_heap;
  void *paddingFill = nullptr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      const cudnnSeqDataDescriptor_t, cudnnDataType_t *, int *, int, int *,
      cudnnSeqDataAxis_t *, size_t *, size_t, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &seqDataDesc, sizeof(const cudnnSeqDataDescriptor_t)) <
          0 ||
      rpc_read(conn, &nbDimsRequested, sizeof(int)) < 0 ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  dimA = (int *)malloc(nbDimsRequested * sizeof(int));
  if ((nbDimsRequested * sizeof(int) != 0 && dimA == nullptr) ||
      nbDimsRequested < 0 || false)
    goto ERROR_0;
  axes = (cudnnSeqDataAxis_t *)malloc(nbDimsRequested *
                                      sizeof(cudnnSeqDataAxis_t));
  if ((nbDimsRequested * sizeof(cudnnSeqDataAxis_t) != 0 && axes == nullptr) ||
      rpc_read(conn, &seqLengthSizeRequested, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  seqLengthArray = (int *)malloc(seqLengthSizeRequested * sizeof(int));
  if ((seqLengthSizeRequested * sizeof(int) != 0 &&
       seqLengthArray == nullptr) ||
      rpc_read(conn, &paddingFill_width, sizeof(paddingFill_width)) < 0 ||
      (paddingFill_width == 0 &&
       rpc_read(conn, &paddingFill, sizeof(paddingFill)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetSeqDataDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(seqDataDesc, &dataType, &nbDims, nbDimsRequested,
               (nbDimsRequested * sizeof(int) == 0 ? nullptr : dimA),
               (nbDimsRequested * sizeof(cudnnSeqDataAxis_t) == 0 ? nullptr
                                                                  : axes),
               &seqLengthArraySize, seqLengthSizeRequested,
               (seqLengthSizeRequested * sizeof(int) == 0 ? nullptr
                                                          : seqLengthArray),
               paddingFill_width != 0
                   ? reinterpret_cast<void *>(lupine_scalar_slot(
                         paddingFill_heap, paddingFill_value,
                         sizeof(paddingFill_value), paddingFill_width))
                   : paddingFill);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &nbDims, sizeof(int)) < 0 ||
      rpc_write(conn, dimA, nbDimsRequested * sizeof(int)) < 0 ||
      rpc_write(conn, axes, nbDimsRequested * sizeof(cudnnSeqDataAxis_t)) < 0 ||
      rpc_write(conn, &seqLengthArraySize, sizeof(size_t)) < 0 ||
      rpc_write(conn, seqLengthArray, seqLengthSizeRequested * sizeof(int)) <
          0 ||
      (paddingFill_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(paddingFill_heap, paddingFill_value,
                                    sizeof(paddingFill_value),
                                    paddingFill_width),
                 paddingFill_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)seqLengthArray);
  free((void *)axes);
  free((void *)dimA);
  return 0;
ERROR_0:
  free((void *)seqLengthArray);
  free((void *)axes);
  free((void *)dimA);
  return -1;
}

int handle_cudnnCreateAttnDescriptor(conn_t *conn) {
  cudnnAttnDescriptor_t attnDesc{};
  attnDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnAttnDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateAttnDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&attnDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &attnDesc, sizeof(cudnnAttnDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyAttnDescriptor(conn_t *conn) {
  cudnnAttnDescriptor_t attnDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnAttnDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attnDesc, sizeof(cudnnAttnDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyAttnDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(attnDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetAttnDescriptor(conn_t *conn) {
  cudnnAttnDescriptor_t attnDesc;
  unsigned attnMode;
  int nHeads;
  double smScaler;
  cudnnDataType_t dataType;
  cudnnDataType_t computePrec;
  cudnnMathType_t mathType;
  cudnnDropoutDescriptor_t attnDropoutDesc;
  cudnnDropoutDescriptor_t postDropoutDesc;
  int qSize;
  int kSize;
  int vSize;
  int qProjSize;
  int kProjSize;
  int vProjSize;
  int oProjSize;
  int qoMaxSeqLength;
  int kvMaxSeqLength;
  int maxBatchSize;
  int maxBeamSize;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnAttnDescriptor_t, unsigned, int, double,
                        cudnnDataType_t, cudnnDataType_t, cudnnMathType_t,
                        cudnnDropoutDescriptor_t, cudnnDropoutDescriptor_t, int,
                        int, int, int, int, int, int, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attnDesc, sizeof(cudnnAttnDescriptor_t)) < 0 ||
      rpc_read(conn, &attnMode, sizeof(unsigned)) < 0 ||
      rpc_read(conn, &nHeads, sizeof(int)) < 0 ||
      rpc_read(conn, &smScaler, sizeof(double)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &computePrec, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &mathType, sizeof(cudnnMathType_t)) < 0 ||
      rpc_read(conn, &attnDropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &postDropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_read(conn, &qSize, sizeof(int)) < 0 ||
      rpc_read(conn, &kSize, sizeof(int)) < 0 ||
      rpc_read(conn, &vSize, sizeof(int)) < 0 ||
      rpc_read(conn, &qProjSize, sizeof(int)) < 0 ||
      rpc_read(conn, &kProjSize, sizeof(int)) < 0 ||
      rpc_read(conn, &vProjSize, sizeof(int)) < 0 ||
      rpc_read(conn, &oProjSize, sizeof(int)) < 0 ||
      rpc_read(conn, &qoMaxSeqLength, sizeof(int)) < 0 ||
      rpc_read(conn, &kvMaxSeqLength, sizeof(int)) < 0 ||
      rpc_read(conn, &maxBatchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &maxBeamSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetAttnDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(attnDesc, attnMode, nHeads, smScaler, dataType, computePrec,
               mathType, attnDropoutDesc, postDropoutDesc, qSize, kSize, vSize,
               qProjSize, kProjSize, vProjSize, oProjSize, qoMaxSeqLength,
               kvMaxSeqLength, maxBatchSize, maxBeamSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetAttnDescriptor(conn_t *conn) {
  cudnnAttnDescriptor_t attnDesc;
  unsigned attnMode{};
  attnMode = {};
  int nHeads{};
  nHeads = {};
  double smScaler{};
  smScaler = {};
  cudnnDataType_t dataType{};
  dataType = {};
  cudnnDataType_t computePrec{};
  computePrec = {};
  cudnnMathType_t mathType{};
  mathType = {};
  cudnnDropoutDescriptor_t attnDropoutDesc{};
  attnDropoutDesc = {};
  cudnnDropoutDescriptor_t postDropoutDesc{};
  postDropoutDesc = {};
  int qSize{};
  qSize = {};
  int kSize{};
  kSize = {};
  int vSize{};
  vSize = {};
  int qProjSize{};
  qProjSize = {};
  int kProjSize{};
  kProjSize = {};
  int vProjSize{};
  vProjSize = {};
  int oProjSize{};
  oProjSize = {};
  int qoMaxSeqLength{};
  qoMaxSeqLength = {};
  int kvMaxSeqLength{};
  kvMaxSeqLength = {};
  int maxBatchSize{};
  maxBatchSize = {};
  int maxBeamSize{};
  maxBeamSize = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnAttnDescriptor_t, unsigned *, int *, double *, cudnnDataType_t *,
      cudnnDataType_t *, cudnnMathType_t *, cudnnDropoutDescriptor_t *,
      cudnnDropoutDescriptor_t *, int *, int *, int *, int *, int *, int *,
      int *, int *, int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attnDesc, sizeof(cudnnAttnDescriptor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetAttnDescriptor");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(attnDesc, &attnMode, &nHeads, &smScaler, &dataType, &computePrec,
               &mathType, &attnDropoutDesc, &postDropoutDesc, &qSize, &kSize,
               &vSize, &qProjSize, &kProjSize, &vProjSize, &oProjSize,
               &qoMaxSeqLength, &kvMaxSeqLength, &maxBatchSize, &maxBeamSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &attnMode, sizeof(unsigned)) < 0 ||
      rpc_write(conn, &nHeads, sizeof(int)) < 0 ||
      rpc_write(conn, &smScaler, sizeof(double)) < 0 ||
      rpc_write(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &computePrec, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &mathType, sizeof(cudnnMathType_t)) < 0 ||
      rpc_write(conn, &attnDropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_write(conn, &postDropoutDesc, sizeof(cudnnDropoutDescriptor_t)) < 0 ||
      rpc_write(conn, &qSize, sizeof(int)) < 0 ||
      rpc_write(conn, &kSize, sizeof(int)) < 0 ||
      rpc_write(conn, &vSize, sizeof(int)) < 0 ||
      rpc_write(conn, &qProjSize, sizeof(int)) < 0 ||
      rpc_write(conn, &kProjSize, sizeof(int)) < 0 ||
      rpc_write(conn, &vProjSize, sizeof(int)) < 0 ||
      rpc_write(conn, &oProjSize, sizeof(int)) < 0 ||
      rpc_write(conn, &qoMaxSeqLength, sizeof(int)) < 0 ||
      rpc_write(conn, &kvMaxSeqLength, sizeof(int)) < 0 ||
      rpc_write(conn, &maxBatchSize, sizeof(int)) < 0 ||
      rpc_write(conn, &maxBeamSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetMultiHeadAttnBuffers(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnAttnDescriptor_t attnDesc;
  size_t *weightSizeInBytes_null_check;
  size_t weightSizeInBytes;
  size_t *workSpaceSizeInBytes_null_check;
  size_t workSpaceSizeInBytes;
  size_t *reserveSpaceSizeInBytes_null_check;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnHandle_t, const cudnnAttnDescriptor_t,
                                 size_t *, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &attnDesc, sizeof(const cudnnAttnDescriptor_t)) < 0 ||
      rpc_read(conn, &weightSizeInBytes_null_check, sizeof(size_t *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes_null_check, sizeof(size_t *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes_null_check, sizeof(size_t *)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetMultiHeadAttnBuffers");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, attnDesc,
               weightSizeInBytes_null_check ? &weightSizeInBytes : nullptr,
               workSpaceSizeInBytes_null_check ? &workSpaceSizeInBytes
                                               : nullptr,
               reserveSpaceSizeInBytes_null_check ? &reserveSpaceSizeInBytes
                                                  : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &weightSizeInBytes_null_check, sizeof(size_t *)) < 0 ||
      (weightSizeInBytes_null_check &&
       rpc_write(conn, &weightSizeInBytes, sizeof(size_t)) < 0) ||
      rpc_write(conn, &workSpaceSizeInBytes_null_check, sizeof(size_t *)) < 0 ||
      (workSpaceSizeInBytes_null_check &&
       rpc_write(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0) ||
      rpc_write(conn, &reserveSpaceSizeInBytes_null_check, sizeof(size_t *)) <
          0 ||
      (reserveSpaceSizeInBytes_null_check &&
       rpc_write(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetMultiHeadAttnWeights(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnAttnDescriptor_t attnDesc;
  cudnnMultiHeadAttnWeightKind_t wKind;
  size_t weightSizeInBytes;
  const void *weights;
  cudnnTensorDescriptor_t wDesc;
  void **wAddr_null_check;
  void *wAddr;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnHandle_t, const cudnnAttnDescriptor_t,
                        cudnnMultiHeadAttnWeightKind_t, size_t, const void *,
                        cudnnTensorDescriptor_t, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &attnDesc, sizeof(const cudnnAttnDescriptor_t)) < 0 ||
      rpc_read(conn, &wKind, sizeof(cudnnMultiHeadAttnWeightKind_t)) < 0 ||
      rpc_read(conn, &weightSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &weights, sizeof(const void *)) < 0 ||
      rpc_read(conn, &wDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &wAddr_null_check, sizeof(void **)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetMultiHeadAttnWeights");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, attnDesc, wKind, weightSizeInBytes, weights,
                          wDesc, wAddr_null_check ? &wAddr : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &wAddr_null_check, sizeof(void **)) < 0 ||
      (wAddr_null_check && rpc_write(conn, &wAddr, sizeof(void *)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnAdvVersionCheck(conn_t *conn) {
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnAdvVersionCheck");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNBackwardData_v8(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  const int32_t *devSeqLengths;
  cudnnRNNDataDescriptor_t yDesc;
  const void *y;
  const void *dy;
  cudnnRNNDataDescriptor_t xDesc;
  void *dx;
  cudnnTensorDescriptor_t hDesc;
  const void *hx;
  const void *dhy;
  void *dhx;
  cudnnTensorDescriptor_t cDesc;
  const void *cx;
  const void *dcy;
  void *dcx;
  size_t weightSpaceSize;
  const void *weightSpace;
  size_t workSpaceSize;
  void *workSpace;
  size_t reserveSpaceSize;
  void *reserveSpace;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnRNNDescriptor_t, const int32_t *,
      cudnnRNNDataDescriptor_t, const void *, const void *,
      cudnnRNNDataDescriptor_t, void *, cudnnTensorDescriptor_t, const void *,
      const void *, void *, cudnnTensorDescriptor_t, const void *, const void *,
      void *, size_t, const void *, size_t, void *, size_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &devSeqLengths, sizeof(const int32_t *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &hDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &hx, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dhy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dhx, sizeof(void *)) < 0 ||
      rpc_read(conn, &cDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &cx, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dcy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dcx, sizeof(void *)) < 0 ||
      rpc_read(conn, &weightSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &weightSpace, sizeof(const void *)) < 0 ||
      rpc_read(conn, &workSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNBackwardData_v8");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, rnnDesc, devSeqLengths, yDesc, y, dy, xDesc, dx, hDesc,
               hx, dhy, dhx, cDesc, cx, dcy, dcx, weightSpaceSize, weightSpace,
               workSpaceSize, workSpace, reserveSpaceSize, reserveSpace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnRNNBackwardWeights_v8(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnRNNDescriptor_t rnnDesc;
  cudnnWgradMode_t addGrad;
  const int32_t *devSeqLengths;
  cudnnRNNDataDescriptor_t xDesc;
  const void *x;
  cudnnTensorDescriptor_t hDesc;
  const void *hx;
  cudnnRNNDataDescriptor_t yDesc;
  const void *y;
  size_t weightSpaceSize;
  void *dweightSpace;
  size_t workSpaceSize;
  void *workSpace;
  size_t reserveSpaceSize;
  void *reserveSpace;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnRNNDescriptor_t, cudnnWgradMode_t, const int32_t *,
      cudnnRNNDataDescriptor_t, const void *, cudnnTensorDescriptor_t,
      const void *, cudnnRNNDataDescriptor_t, const void *, size_t, void *,
      size_t, void *, size_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &rnnDesc, sizeof(cudnnRNNDescriptor_t)) < 0 ||
      rpc_read(conn, &addGrad, sizeof(cudnnWgradMode_t)) < 0 ||
      rpc_read(conn, &devSeqLengths, sizeof(const int32_t *)) < 0 ||
      rpc_read(conn, &xDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &hDesc, sizeof(cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &hx, sizeof(const void *)) < 0 ||
      rpc_read(conn, &yDesc, sizeof(cudnnRNNDataDescriptor_t)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &dweightSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnRNNBackwardWeights_v8");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, rnnDesc, addGrad, devSeqLengths, xDesc, x, hDesc, hx,
               yDesc, y, weightSpaceSize, dweightSpace, workSpaceSize,
               workSpace, reserveSpaceSize, reserveSpace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnMultiHeadAttnBackwardWeights(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnAttnDescriptor_t attnDesc;
  cudnnWgradMode_t addGrad;
  cudnnSeqDataDescriptor_t qDesc;
  const void *queries;
  cudnnSeqDataDescriptor_t kDesc;
  const void *keys;
  cudnnSeqDataDescriptor_t vDesc;
  const void *values;
  cudnnSeqDataDescriptor_t doDesc;
  const void *dout;
  size_t weightSizeInBytes;
  const void *weights;
  void *dweights;
  size_t workSpaceSizeInBytes;
  void *workSpace;
  size_t reserveSpaceSizeInBytes;
  void *reserveSpace;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, const cudnnAttnDescriptor_t, cudnnWgradMode_t,
      const cudnnSeqDataDescriptor_t, const void *,
      const cudnnSeqDataDescriptor_t, const void *,
      const cudnnSeqDataDescriptor_t, const void *,
      const cudnnSeqDataDescriptor_t, const void *, size_t, const void *,
      void *, size_t, void *, size_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &attnDesc, sizeof(const cudnnAttnDescriptor_t)) < 0 ||
      rpc_read(conn, &addGrad, sizeof(cudnnWgradMode_t)) < 0 ||
      rpc_read(conn, &qDesc, sizeof(const cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_read(conn, &queries, sizeof(const void *)) < 0 ||
      rpc_read(conn, &kDesc, sizeof(const cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_read(conn, &keys, sizeof(const void *)) < 0 ||
      rpc_read(conn, &vDesc, sizeof(const cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_read(conn, &values, sizeof(const void *)) < 0 ||
      rpc_read(conn, &doDesc, sizeof(const cudnnSeqDataDescriptor_t)) < 0 ||
      rpc_read(conn, &dout, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &weights, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dweights, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnMultiHeadAttnBackwardWeights");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, attnDesc, addGrad, qDesc, queries, kDesc,
                          keys, vDesc, values, doDesc, dout, weightSizeInBytes,
                          weights, dweights, workSpaceSizeInBytes, workSpace,
                          reserveSpaceSizeInBytes, reserveSpace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCreateCTCLossDescriptor(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc{};
  ctcLossDesc = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCreateCTCLossDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(&ctcLossDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetCTCLossDescriptor(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &compType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetCTCLossDescriptor");
  return_value =
      fn == nullptr ? function_not_found() : fn(ctcLossDesc, compType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetCTCLossDescriptorEx(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType;
  cudnnLossNormalizationMode_t normMode;
  cudnnNanPropagation_t gradMode;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t,
                        cudnnLossNormalizationMode_t, cudnnNanPropagation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_read(conn, &gradMode, sizeof(cudnnNanPropagation_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetCTCLossDescriptorEx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(ctcLossDesc, compType, normMode, gradMode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetCTCLossDescriptor_v8(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType;
  cudnnLossNormalizationMode_t normMode;
  cudnnNanPropagation_t gradMode;
  int maxLabelLength;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t,
                                 cudnnLossNormalizationMode_t,
                                 cudnnNanPropagation_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_read(conn, &gradMode, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_read(conn, &maxLabelLength, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetCTCLossDescriptor_v8");
  return_value = fn == nullptr ? function_not_found()
                               : fn(ctcLossDesc, compType, normMode, gradMode,
                                    maxLabelLength);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnSetCTCLossDescriptor_v9(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType;
  cudnnLossNormalizationMode_t normMode;
  cudnnCTCGradMode_t ctcGradMode;
  int maxLabelLength;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t,
                        cudnnLossNormalizationMode_t, cudnnCTCGradMode_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_read(conn, &ctcGradMode, sizeof(cudnnCTCGradMode_t)) < 0 ||
      rpc_read(conn, &maxLabelLength, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSetCTCLossDescriptor_v9");
  return_value = fn == nullptr ? function_not_found()
                               : fn(ctcLossDesc, compType, normMode,
                                    ctcGradMode, maxLabelLength);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCTCLossDescriptor(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType{};
  compType = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCTCLossDescriptor");
  return_value =
      fn == nullptr ? function_not_found() : fn(ctcLossDesc, &compType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCTCLossDescriptorEx(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType{};
  compType = {};
  cudnnLossNormalizationMode_t normMode{};
  normMode = {};
  cudnnNanPropagation_t gradMode{};
  gradMode = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t *,
                                 cudnnLossNormalizationMode_t *,
                                 cudnnNanPropagation_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCTCLossDescriptorEx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(ctcLossDesc, &compType, &normMode, &gradMode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_write(conn, &gradMode, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCTCLossDescriptor_v8(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType{};
  compType = {};
  cudnnLossNormalizationMode_t normMode{};
  normMode = {};
  cudnnNanPropagation_t gradMode{};
  gradMode = {};
  int maxLabelLength{};
  maxLabelLength = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t *,
                                 cudnnLossNormalizationMode_t *,
                                 cudnnNanPropagation_t *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCTCLossDescriptor_v8");
  return_value = fn == nullptr ? function_not_found()
                               : fn(ctcLossDesc, &compType, &normMode,
                                    &gradMode, &maxLabelLength);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_write(conn, &gradMode, sizeof(cudnnNanPropagation_t)) < 0 ||
      rpc_write(conn, &maxLabelLength, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCTCLossDescriptor_v9(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnDataType_t compType{};
  compType = {};
  cudnnLossNormalizationMode_t normMode{};
  normMode = {};
  cudnnCTCGradMode_t ctcGradMode{};
  ctcGradMode = {};
  int maxLabelLength{};
  maxLabelLength = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t, cudnnDataType_t *,
                                 cudnnLossNormalizationMode_t *,
                                 cudnnCTCGradMode_t *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCTCLossDescriptor_v9");
  return_value = fn == nullptr ? function_not_found()
                               : fn(ctcLossDesc, &compType, &normMode,
                                    &ctcGradMode, &maxLabelLength);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &compType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_write(conn, &normMode, sizeof(cudnnLossNormalizationMode_t)) < 0 ||
      rpc_write(conn, &ctcGradMode, sizeof(cudnnCTCGradMode_t)) < 0 ||
      rpc_write(conn, &maxLabelLength, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnDestroyCTCLossDescriptor(conn_t *conn) {
  cudnnCTCLossDescriptor_t ctcLossDesc;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudnnCTCLossDescriptor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnDestroyCTCLossDescriptor");
  return_value = fn == nullptr ? function_not_found() : fn(ctcLossDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnCTCLoss_v8(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnCTCLossAlgo_t algo;
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnTensorDescriptor_t probsDesc;
  const void *probs;
  const int *labels;
  const int *labelLengths;
  const int *inputLengths;
  void *costs;
  cudnnTensorDescriptor_t gradientsDesc;
  void *gradients;
  size_t workSpaceSizeInBytes;
  void *workspace;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnCTCLossAlgo_t, cudnnCTCLossDescriptor_t,
      const cudnnTensorDescriptor_t, const void *, const int *, const int *,
      const int *, void *, const cudnnTensorDescriptor_t, void *, size_t,
      void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnCTCLossAlgo_t)) < 0 ||
      rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &probsDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &probs, sizeof(const void *)) < 0 ||
      rpc_read(conn, &labels, sizeof(const int *)) < 0 ||
      rpc_read(conn, &labelLengths, sizeof(const int *)) < 0 ||
      rpc_read(conn, &inputLengths, sizeof(const int *)) < 0 ||
      rpc_read(conn, &costs, sizeof(void *)) < 0 ||
      rpc_read(conn, &gradientsDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      rpc_read(conn, &gradients, sizeof(void *)) < 0 ||
      rpc_read(conn, &workSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCTCLoss_v8");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, algo, ctcLossDesc, probsDesc, probs, labels,
                          labelLengths, inputLengths, costs, gradientsDesc,
                          gradients, workSpaceSizeInBytes, workspace);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudnnGetCTCLossWorkspaceSize_v8(conn_t *conn) {
  cudnnHandle_t handle;
  cudnnCTCLossAlgo_t algo;
  cudnnCTCLossDescriptor_t ctcLossDesc;
  cudnnTensorDescriptor_t probsDesc;
  cudnnTensorDescriptor_t gradientsDesc;
  size_t sizeInBytes{};
  sizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudnnHandle_t, cudnnCTCLossAlgo_t, cudnnCTCLossDescriptor_t,
      const cudnnTensorDescriptor_t, const cudnnTensorDescriptor_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudnnHandle_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cudnnCTCLossAlgo_t)) < 0 ||
      rpc_read(conn, &ctcLossDesc, sizeof(cudnnCTCLossDescriptor_t)) < 0 ||
      rpc_read(conn, &probsDesc, sizeof(const cudnnTensorDescriptor_t)) < 0 ||
      rpc_read(conn, &gradientsDesc, sizeof(const cudnnTensorDescriptor_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGetCTCLossWorkspaceSize_v8");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, algo, ctcLossDesc, probsDesc,
                                    gradientsDesc, &sizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDNN_VERSION >= 92200
int handle_cudnnSubquadraticOpsVersionCheck(conn_t *conn) {
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnSubquadraticOpsVersionCheck");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92200
int handle_cudnnCausalConv1dForward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  const void *bias;
  void *y;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  cudnnCausalConv1dActivation_t activation;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const void *, const void *, const void *, void *, int, int,
      int, int, cudnnDataType_t, cudnnCausalConv1dActivation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &activation, sizeof(cudnnCausalConv1dActivation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCausalConv1dForward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, bias, y, batch, dim,
                                    seqLen, kernelSize, dataType, activation);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92200
int handle_cudnnCausalConv1dBackward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  const void *bias;
  const void *dy;
  void *dx;
  void *dweight;
  void *dbias;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  cudnnDataType_t dwDataType;
  cudnnCausalConv1dActivation_t activation;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const void *, const void *, const void *, const void *,
      void *, void *, void *, int, int, int, int, cudnnDataType_t,
      cudnnDataType_t, cudnnCausalConv1dActivation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweight, sizeof(void *)) < 0 ||
      rpc_read(conn, &dbias, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &dwDataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &activation, sizeof(cudnnCausalConv1dActivation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCausalConv1dBackward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, bias, dy, dx, dweight,
                                    dbias, batch, dim, seqLen, kernelSize,
                                    dataType, dwDataType, activation);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92400
int handle_cudnnCausalConv1dNwhForward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  const void *bias;
  void *y;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  cudnnCausalConv1dActivation_t activation;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const void *, const void *, const void *, void *, int, int,
      int, int, cudnnDataType_t, cudnnCausalConv1dActivation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &activation, sizeof(cudnnCausalConv1dActivation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCausalConv1dNwhForward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, bias, y, batch, dim,
                                    seqLen, kernelSize, dataType, activation);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92400
int handle_cudnnCausalConv1dNwhBackward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  const void *bias;
  const void *dy;
  void *dx;
  void *dweight;
  void *dbias;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  cudnnDataType_t dwDataType;
  cudnnCausalConv1dActivation_t activation;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const void *, const void *, const void *, const void *,
      void *, void *, void *, int, int, int, int, cudnnDataType_t,
      cudnnDataType_t, cudnnCausalConv1dActivation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &bias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweight, sizeof(void *)) < 0 ||
      rpc_read(conn, &dbias, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &dwDataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &activation, sizeof(cudnnCausalConv1dActivation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnCausalConv1dNwhBackward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, bias, dy, dx, dweight,
                                    dbias, batch, dim, seqLen, kernelSize,
                                    dataType, dwDataType, activation);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92400
int handle_cudnnB2BCausalConv1dForward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weightsProj;
  const void *weightsMixer;
  const void *skipBias;
  void *y;
  void *yGated;
  int batch;
  int dim;
  int seqLen;
  int kernelSizeProj;
  int kernelSizeMixer;
  cudnnDataType_t dataType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudaStream_t, const void *, const void *,
                                 const void *, const void *, void *, void *,
                                 int, int, int, int, int, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightsProj, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightsMixer, sizeof(const void *)) < 0 ||
      rpc_read(conn, &skipBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &yGated, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSizeProj, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSizeMixer, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnB2BCausalConv1dForward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weightsProj, weightsMixer,
                                    skipBias, y, yGated, batch, dim, seqLen,
                                    kernelSizeProj, kernelSizeMixer, dataType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92400
int handle_cudnnB2BCausalConv1dBackward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weightsProj;
  const void *weightsMixer;
  const void *skipBias;
  const void *y;
  const void *dy;
  void *dx;
  void *dweightsProj;
  void *dweightsMixer;
  void *dskipBias;
  int batch;
  int dim;
  int seqLen;
  int kernelSizeProj;
  int kernelSizeMixer;
  cudnnDataType_t dataType;
  cudnnDataType_t dwDataType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const void *, const void *, const void *, const void *,
      const void *, const void *, void *, void *, void *, void *, int, int, int,
      int, int, cudnnDataType_t, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightsProj, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weightsMixer, sizeof(const void *)) < 0 ||
      rpc_read(conn, &skipBias, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweightsProj, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweightsMixer, sizeof(void *)) < 0 ||
      rpc_read(conn, &dskipBias, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSizeProj, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSizeMixer, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &dwDataType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnB2BCausalConv1dBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(stream, x, weightsProj, weightsMixer, skipBias, y, dy, dx,
               dweightsProj, dweightsMixer, dskipBias, batch, dim, seqLen,
               kernelSizeProj, kernelSizeMixer, dataType, dwDataType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnFFTCausalConv1dForward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  void *y;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudaStream_t, const void *, const void *,
                                 void *, int, int, int, int, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFFTCausalConv1dForward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, y, batch, dim, seqLen,
                                    kernelSize, dataType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnFFTCausalConv1dBackward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  const void *dy;
  void *dx;
  void *dweight;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudaStream_t, const void *, const void *, const void *,
                        void *, void *, int, int, int, int, cudnnDataType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweight, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnFFTCausalConv1dBackward");
  return_value = fn == nullptr ? function_not_found()
                               : fn(stream, x, weight, dy, dx, dweight, batch,
                                    dim, seqLen, kernelSize, dataType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnLongFFTCausalConv1dGetBufferSizes(conn_t *conn) {
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  size_t workspaceSizeInBytes{};
  workspaceSizeInBytes = {};
  size_t reserveSpaceSizeInBytes{};
  reserveSpaceSizeInBytes = {};
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(int, int, int, int, cudnnDataType_t, size_t *,
                                 size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnLongFFTCausalConv1dGetBufferSizes");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(batch, dim, seqLen, kernelSize, dataType,
                          &workspaceSizeInBytes, &reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnLongFFTCausalConv1dForward(conn_t *conn) {
  cudaStream_t stream;
  const void *x;
  const void *weight;
  void *y;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  void *workspace;
  size_t workspaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudaStream_t, const void *, const void *,
                                 void *, int, int, int, int, cudnnDataType_t,
                                 void *, size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &x, sizeof(const void *)) < 0 ||
      rpc_read(conn, &weight, sizeof(const void *)) < 0 ||
      rpc_read(conn, &y, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnLongFFTCausalConv1dForward");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(stream, x, weight, y, batch, dim, seqLen, kernelSize,
                          dataType, workspace, workspaceSizeInBytes,
                          reserveSpace, reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnLongFFTCausalConv1dBackward(conn_t *conn) {
  cudaStream_t stream;
  const void *dy;
  void *dx;
  void *dweight;
  int batch;
  int dim;
  int seqLen;
  int kernelSize;
  cudnnDataType_t dataType;
  void *workspace;
  size_t workspaceSizeInBytes;
  void *reserveSpace;
  size_t reserveSpaceSizeInBytes;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(cudaStream_t, const void *, void *, void *,
                                 int, int, int, int, cudnnDataType_t, void *,
                                 size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &dy, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dx, sizeof(void *)) < 0 ||
      rpc_read(conn, &dweight, sizeof(void *)) < 0 ||
      rpc_read(conn, &batch, sizeof(int)) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &seqLen, sizeof(int)) < 0 ||
      rpc_read(conn, &kernelSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &workspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &reserveSpace, sizeof(void *)) < 0 ||
      rpc_read(conn, &reserveSpaceSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnLongFFTCausalConv1dBackward");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(stream, dy, dx, dweight, batch, dim, seqLen,
                          kernelSize, dataType, workspace, workspaceSizeInBytes,
                          reserveSpace, reserveSpaceSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnGnnAggSimpleForward(conn_t *conn) {
  cudaStream_t stream;
  cudnnGnnCscGraph_t graph{};
  const void *nodeFeatures;
  const void *edgeFeatures;
  const void *concatFeatures;
  void *output;
  void *outPositions;
  int nodeFeatDim;
  int edgeFeatDim;
  int concatFeatDim;
  cudnnDataType_t dataType;
  cudnnGnnAggOp_t aggOp;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t =
      cudnnStatus_t (*)(cudaStream_t, const cudnnGnnCscGraph_t *, const void *,
                        const void *, const void *, void *, void *, int, int,
                        int, cudnnDataType_t, cudnnGnnAggOp_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &graph, sizeof(const cudnnGnnCscGraph_t)) < 0 ||
      rpc_read(conn, &nodeFeatures, sizeof(const void *)) < 0 ||
      rpc_read(conn, &edgeFeatures, sizeof(const void *)) < 0 ||
      rpc_read(conn, &concatFeatures, sizeof(const void *)) < 0 ||
      rpc_read(conn, &output, sizeof(void *)) < 0 ||
      rpc_read(conn, &outPositions, sizeof(void *)) < 0 ||
      rpc_read(conn, &nodeFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &edgeFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &concatFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &aggOp, sizeof(cudnnGnnAggOp_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGnnAggSimpleForward");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(stream, &graph, nodeFeatures, edgeFeatures,
                          concatFeatures, output, outPositions, nodeFeatDim,
                          edgeFeatDim, concatFeatDim, dataType, aggOp);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDNN_VERSION >= 92600
int handle_cudnnGnnAggSimpleBackward(conn_t *conn) {
  cudaStream_t stream;
  cudnnGnnCscGraph_t graph{};
  const void *gradOutput;
  const void *outPositions;
  void *gradNodeFeatures;
  void *gradEdgeFeatures;
  void *gradConcatFeatures;
  int nodeFeatDim;
  int edgeFeatDim;
  int concatFeatDim;
  cudnnDataType_t dataType;
  cudnnGnnAggOp_t aggOp;
  int request_id;
  cudnnStatus_t return_value;
  using fn_t = cudnnStatus_t (*)(
      cudaStream_t, const cudnnGnnCscGraph_t *, const void *, const void *,
      void *, void *, void *, int, int, int, cudnnDataType_t, cudnnGnnAggOp_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &graph, sizeof(const cudnnGnnCscGraph_t)) < 0 ||
      rpc_read(conn, &gradOutput, sizeof(const void *)) < 0 ||
      rpc_read(conn, &outPositions, sizeof(const void *)) < 0 ||
      rpc_read(conn, &gradNodeFeatures, sizeof(void *)) < 0 ||
      rpc_read(conn, &gradEdgeFeatures, sizeof(void *)) < 0 ||
      rpc_read(conn, &gradConcatFeatures, sizeof(void *)) < 0 ||
      rpc_read(conn, &nodeFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &edgeFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &concatFeatDim, sizeof(int)) < 0 ||
      rpc_read(conn, &dataType, sizeof(cudnnDataType_t)) < 0 ||
      rpc_read(conn, &aggOp, sizeof(cudnnGnnAggOp_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudnn_symbol<fn_t>("cudnnGnnAggSimpleBackward");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(stream, &graph, gradOutput, outPositions, gradNodeFeatures,
               gradEdgeFeatures, gradConcatFeatures, nodeFeatDim, edgeFeatDim,
               concatFeatDim, dataType, aggOp);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudnnStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif
