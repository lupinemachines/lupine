// Generated by codegen.py. Do not edit by hand.

int handle_nppGetGpuNumSMs(conn_t *conn) {
  int request_id;
  int return_value;
  using fn_t = int (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetGpuNumSMs");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetMaxThreadsPerBlock(conn_t *conn) {
  int request_id;
  int return_value;
  using fn_t = int (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetMaxThreadsPerBlock");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetMaxThreadsPerSM(conn_t *conn) {
  int request_id;
  int return_value;
  using fn_t = int (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetMaxThreadsPerSM");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetGpuDeviceProperties(conn_t *conn) {
  int pMaxThreadsPerSM{};
  int pMaxThreadsPerBlock{};
  int pNumberOfSMs{};
  int request_id;
  int return_value;
  using fn_t = int (*)(int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pMaxThreadsPerSM, sizeof(int)) < 0 ||
      rpc_read(conn, &pMaxThreadsPerBlock, sizeof(int)) < 0 ||
      rpc_read(conn, &pNumberOfSMs, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetGpuDeviceProperties");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(&pMaxThreadsPerSM, &pMaxThreadsPerBlock, &pNumberOfSMs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pMaxThreadsPerSM, sizeof(int)) < 0 ||
      rpc_write(conn, &pMaxThreadsPerBlock, sizeof(int)) < 0 ||
      rpc_write(conn, &pNumberOfSMs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetStream(conn_t *conn) {
  int request_id;
  cudaStream_t return_value;
  using fn_t = cudaStream_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetStream");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaStream_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetStreamContext(conn_t *conn) {
  NppStreamContext pNppStreamContext{};
  int request_id;
  NppStatus return_value;
  using fn_t = NppStatus (*)(NppStreamContext *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pNppStreamContext, sizeof(NppStreamContext)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetStreamContext");
  return_value = fn == nullptr ? function_not_found() : fn(&pNppStreamContext);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pNppStreamContext, sizeof(NppStreamContext)) < 0 ||
      rpc_write(conn, &return_value, sizeof(NppStatus)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetStreamNumSMs(conn_t *conn) {
  int request_id;
  unsigned int return_value;
  using fn_t = unsigned int (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetStreamNumSMs");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(unsigned int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppGetStreamMaxThreadsPerSM(conn_t *conn) {
  int request_id;
  unsigned int return_value;
  using fn_t = unsigned int (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppGetStreamMaxThreadsPerSM");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(unsigned int)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nppSetStream(conn_t *conn) {
  cudaStream_t hStream;
  int request_id;
  NppStatus return_value;
  using fn_t = NppStatus (*)(cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = npp_symbol<fn_t>("nppSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(hStream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(NppStatus)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
