// Generated by codegen.py. Do not edit by hand.

int handle_nvjpegGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  value = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegGetCudartProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  value = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetCudartProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegCreate(conn_t *conn) {
  nvjpegBackend_t backend;
  nvjpegDevAllocator_t *dev_allocator_null_check;
  nvjpegDevAllocator_t dev_allocator;
  nvjpegHandle_t handle{};
  handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBackend_t, nvjpegDevAllocator_t *,
                                  nvjpegHandle_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &backend, sizeof(nvjpegBackend_t)) < 0 ||
      rpc_read(conn, &dev_allocator_null_check,
               sizeof(nvjpegDevAllocator_t *)) < 0 ||
      (dev_allocator_null_check &&
       rpc_read(conn, &dev_allocator, sizeof(nvjpegDevAllocator_t)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegCreate");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(backend, dev_allocator_null_check ? &dev_allocator : nullptr,
               &handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegCreateSimple(conn_t *conn) {
  nvjpegHandle_t handle{};
  handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegCreateSimple");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegCreateEx(conn_t *conn) {
  nvjpegBackend_t backend;
  nvjpegDevAllocator_t *dev_allocator_null_check;
  nvjpegDevAllocator_t dev_allocator;
  nvjpegPinnedAllocator_t *pinned_allocator_null_check;
  nvjpegPinnedAllocator_t pinned_allocator;
  unsigned int flags;
  nvjpegHandle_t handle{};
  handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBackend_t, nvjpegDevAllocator_t *,
                                  nvjpegPinnedAllocator_t *, unsigned int,
                                  nvjpegHandle_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &backend, sizeof(nvjpegBackend_t)) < 0 ||
      rpc_read(conn, &dev_allocator_null_check,
               sizeof(nvjpegDevAllocator_t *)) < 0 ||
      (dev_allocator_null_check &&
       rpc_read(conn, &dev_allocator, sizeof(nvjpegDevAllocator_t)) < 0) ||
      rpc_read(conn, &pinned_allocator_null_check,
               sizeof(nvjpegPinnedAllocator_t *)) < 0 ||
      (pinned_allocator_null_check &&
       rpc_read(conn, &pinned_allocator, sizeof(nvjpegPinnedAllocator_t)) <
           0) ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegCreateEx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(backend, dev_allocator_null_check ? &dev_allocator : nullptr,
               pinned_allocator_null_check ? &pinned_allocator : nullptr, flags,
               &handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegCreateExV2(conn_t *conn) {
  nvjpegBackend_t backend;
  nvjpegDevAllocatorV2_t *dev_allocator_null_check;
  nvjpegDevAllocatorV2_t dev_allocator;
  nvjpegPinnedAllocatorV2_t *pinned_allocator_null_check;
  nvjpegPinnedAllocatorV2_t pinned_allocator;
  unsigned int flags;
  nvjpegHandle_t handle{};
  handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBackend_t, nvjpegDevAllocatorV2_t *,
                                  nvjpegPinnedAllocatorV2_t *, unsigned int,
                                  nvjpegHandle_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &backend, sizeof(nvjpegBackend_t)) < 0 ||
      rpc_read(conn, &dev_allocator_null_check,
               sizeof(nvjpegDevAllocatorV2_t *)) < 0 ||
      (dev_allocator_null_check &&
       rpc_read(conn, &dev_allocator, sizeof(nvjpegDevAllocatorV2_t)) < 0) ||
      rpc_read(conn, &pinned_allocator_null_check,
               sizeof(nvjpegPinnedAllocatorV2_t *)) < 0 ||
      (pinned_allocator_null_check &&
       rpc_read(conn, &pinned_allocator, sizeof(nvjpegPinnedAllocatorV2_t)) <
           0) ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegCreateExV2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(backend, dev_allocator_null_check ? &dev_allocator : nullptr,
               pinned_allocator_null_check ? &pinned_allocator : nullptr, flags,
               &handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDestroy(conn_t *conn) {
  nvjpegHandle_t handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegSetDeviceMemoryPadding(conn_t *conn) {
  size_t padding;
  nvjpegHandle_t handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(size_t, nvjpegHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &padding, sizeof(size_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegSetDeviceMemoryPadding");
  return_value = fn == nullptr ? function_not_found() : fn(padding, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegGetDeviceMemoryPadding(conn_t *conn) {
  size_t padding{};
  padding = {};
  nvjpegHandle_t handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(size_t *, nvjpegHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetDeviceMemoryPadding");
  return_value = fn == nullptr ? function_not_found() : fn(&padding, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &padding, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegSetPinnedMemoryPadding(conn_t *conn) {
  size_t padding;
  nvjpegHandle_t handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(size_t, nvjpegHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &padding, sizeof(size_t)) < 0 ||
      rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegSetPinnedMemoryPadding");
  return_value = fn == nullptr ? function_not_found() : fn(padding, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegGetPinnedMemoryPadding(conn_t *conn) {
  size_t padding{};
  padding = {};
  nvjpegHandle_t handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(size_t *, nvjpegHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetPinnedMemoryPadding");
  return_value = fn == nullptr ? function_not_found() : fn(&padding, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &padding, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegGetHardwareDecoderInfo(conn_t *conn) {
  nvjpegHandle_t handle;
  unsigned int num_engines{};
  num_engines = {};
  unsigned int num_cores_per_engine{};
  num_cores_per_engine = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, unsigned int *, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetHardwareDecoderInfo");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, &num_engines, &num_cores_per_engine);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_engines, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &num_cores_per_engine, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1204
int handle_nvjpegGetHardwareEncoderInfo(conn_t *conn) {
  nvjpegHandle_t handle;
  unsigned int num_engines{};
  num_engines = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetHardwareEncoderInfo");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &num_engines);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_engines, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegJpegStateCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegState_t jpeg_handle{};
  jpeg_handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegState_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStateCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &jpeg_handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &jpeg_handle, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegGetImageInfo(conn_t *conn) {
  nvjpegHandle_t handle;
  size_t length;
  unsigned char *data = nullptr;
  size_t data_size;
  int nComponents{};
  nComponents = {};
  nvjpegChromaSubsampling_t subsampling{};
  subsampling = {};
  int *widths = nullptr;
  size_t widths_size;
  uint64_t widths_count = 0;
  int *heights = nullptr;
  size_t heights_size;
  uint64_t heights_count = 0;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, const unsigned char *, size_t, int *,
                         nvjpegChromaSubsampling_t *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &length, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = length * sizeof(const unsigned char);
  data = (unsigned char *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) ||
      rpc_read(conn, &widths_count, sizeof(widths_count)) < 0 || false)
    goto ERROR_0;
  widths_size = widths_count * sizeof(int);
  widths = (int *)malloc(widths_size);
  if (widths_size != 0 && widths == nullptr)
    goto ERROR_0;
  if ((widths_size != 0 && rpc_read(conn, widths, widths_size) < 0) ||
      rpc_read(conn, &heights_count, sizeof(heights_count)) < 0 || false)
    goto ERROR_0;
  heights_size = heights_count * sizeof(int);
  heights = (int *)malloc(heights_size);
  if (heights_size != 0 && heights == nullptr)
    goto ERROR_0;
  if ((heights_size != 0 && rpc_read(conn, heights, heights_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegGetImageInfo");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (length * sizeof(const unsigned char) == 0 ? nullptr : data),
               length, &nComponents, &subsampling,
               (widths_count * sizeof(int) == 0 ? nullptr : widths),
               (heights_count * sizeof(int) == 0 ? nullptr : heights));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nComponents, sizeof(int)) < 0 ||
      rpc_write(conn, &subsampling, sizeof(nvjpegChromaSubsampling_t)) < 0 ||
      rpc_write(conn, widths, widths_count * sizeof(int)) < 0 ||
      rpc_write(conn, heights, heights_count * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)heights);
  free((void *)widths);
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)heights);
  free((void *)widths);
  free((void *)data);
  return -1;
}

int handle_nvjpegDecode(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegState_t jpeg_handle;
  size_t length;
  unsigned char *data = nullptr;
  size_t data_size;
  nvjpegOutputFormat_t output_format;
  nvjpegImage_t destination{};
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegHandle_t, nvjpegJpegState_t, const unsigned char *, size_t,
      nvjpegOutputFormat_t, nvjpegImage_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &jpeg_handle, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &length, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = length * sizeof(const unsigned char);
  data = (unsigned char *)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) ||
      rpc_read(conn, &output_format, sizeof(nvjpegOutputFormat_t)) < 0 ||
      rpc_read(conn, &destination, sizeof(nvjpegImage_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecode");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jpeg_handle,
               (length * sizeof(const unsigned char) == 0 ? nullptr : data),
               length, output_format, &destination, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_nvjpegDecodeBatchedInitialize(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegState_t jpeg_handle;
  int batch_size;
  int max_cpu_threads;
  nvjpegOutputFormat_t output_format;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegState_t, int, int,
                                  nvjpegOutputFormat_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &jpeg_handle, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &batch_size, sizeof(int)) < 0 ||
      rpc_read(conn, &max_cpu_threads, sizeof(int)) < 0 ||
      rpc_read(conn, &output_format, sizeof(nvjpegOutputFormat_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeBatchedInitialize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jpeg_handle, batch_size,
                                    max_cpu_threads, output_format);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeBatchedPreAllocate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegState_t jpeg_handle;
  int batch_size;
  int width;
  int height;
  nvjpegChromaSubsampling_t chroma_subsampling;
  nvjpegOutputFormat_t output_format;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegState_t, int, int, int,
                         nvjpegChromaSubsampling_t, nvjpegOutputFormat_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &jpeg_handle, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &batch_size, sizeof(int)) < 0 ||
      rpc_read(conn, &width, sizeof(int)) < 0 ||
      rpc_read(conn, &height, sizeof(int)) < 0 ||
      rpc_read(conn, &chroma_subsampling, sizeof(nvjpegChromaSubsampling_t)) <
          0 ||
      rpc_read(conn, &output_format, sizeof(nvjpegOutputFormat_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeBatchedPreAllocate");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jpeg_handle, batch_size, width,
                                    height, chroma_subsampling, output_format);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderStateCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state{};
  encoder_state = {};
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegEncoderState_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderStateCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &encoder_state, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1204
int handle_nvjpegEncoderStateCreateWithBackend(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state{};
  encoder_state = {};
  nvjpegEncBackend_t backend;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegEncoderState_t *,
                                  nvjpegEncBackend_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &backend, sizeof(nvjpegEncBackend_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderStateCreateWithBackend");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, &encoder_state, backend, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegEncoderStateDestroy(conn_t *conn) {
  nvjpegEncoderState_t encoder_state;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderState_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderStateDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(encoder_state);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderParams_t encoder_params{};
  encoder_params = {};
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegEncoderParams_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsCreate");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, &encoder_params, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsDestroy(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(encoder_params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsSetQuality(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  int quality;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegEncoderParams_t, const int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &quality, sizeof(const int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsSetQuality");
  return_value = fn == nullptr ? function_not_found()
                               : fn(encoder_params, quality, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsSetEncoding(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  nvjpegJpegEncoding_t etype;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderParams_t, nvjpegJpegEncoding_t,
                                  cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &etype, sizeof(nvjpegJpegEncoding_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsSetEncoding");
  return_value =
      fn == nullptr ? function_not_found() : fn(encoder_params, etype, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsSetOptimizedHuffman(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  int optimized;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegEncoderParams_t, const int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &optimized, sizeof(const int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsSetOptimizedHuffman");
  return_value = fn == nullptr ? function_not_found()
                               : fn(encoder_params, optimized, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsSetSamplingFactors(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  nvjpegChromaSubsampling_t chroma_subsampling;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegEncoderParams_t, const nvjpegChromaSubsampling_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &chroma_subsampling,
               sizeof(const nvjpegChromaSubsampling_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsSetSamplingFactors");
  return_value = fn == nullptr ? function_not_found()
                               : fn(encoder_params, chroma_subsampling, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1204
int handle_nvjpegEncoderParamsSetRestartInterval(conn_t *conn) {
  nvjpegEncoderParams_t encoder_params;
  unsigned int restart_interval;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegEncoderParams_t, unsigned int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &restart_interval, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsSetRestartInterval");
  return_value = fn == nullptr ? function_not_found()
                               : fn(encoder_params, restart_interval, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegEncodeGetBufferSize(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderParams_t encoder_params;
  int image_width;
  int image_height;
  size_t max_stream_length{};
  max_stream_length = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, const nvjpegEncoderParams_t,
                                  int, int, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_params, sizeof(const nvjpegEncoderParams_t)) <
          0 ||
      rpc_read(conn, &image_width, sizeof(int)) < 0 ||
      rpc_read(conn, &image_height, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncodeGetBufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, encoder_params, image_width,
                                    image_height, &max_stream_length);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &max_stream_length, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncodeYUV(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state;
  nvjpegEncoderParams_t encoder_params;
  nvjpegImage_t source{};
  nvjpegChromaSubsampling_t chroma_subsampling;
  int image_width;
  int image_height;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegHandle_t, nvjpegEncoderState_t, const nvjpegEncoderParams_t,
      const nvjpegImage_t *, nvjpegChromaSubsampling_t, int, int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &encoder_params, sizeof(const nvjpegEncoderParams_t)) <
          0 ||
      rpc_read(conn, &source, sizeof(const nvjpegImage_t)) < 0 ||
      rpc_read(conn, &chroma_subsampling, sizeof(nvjpegChromaSubsampling_t)) <
          0 ||
      rpc_read(conn, &image_width, sizeof(int)) < 0 ||
      rpc_read(conn, &image_height, sizeof(int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncodeYUV");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, encoder_state, encoder_params, &source,
                         chroma_subsampling, image_width, image_height, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncodeImage(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state;
  nvjpegEncoderParams_t encoder_params;
  nvjpegImage_t source{};
  nvjpegInputFormat_t input_format;
  int image_width;
  int image_height;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegHandle_t, nvjpegEncoderState_t, const nvjpegEncoderParams_t,
      const nvjpegImage_t *, nvjpegInputFormat_t, int, int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &encoder_params, sizeof(const nvjpegEncoderParams_t)) <
          0 ||
      rpc_read(conn, &source, sizeof(const nvjpegImage_t)) < 0 ||
      rpc_read(conn, &input_format, sizeof(nvjpegInputFormat_t)) < 0 ||
      rpc_read(conn, &image_width, sizeof(int)) < 0 ||
      rpc_read(conn, &image_height, sizeof(int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncodeImage");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, encoder_state, encoder_params, &source,
                          input_format, image_width, image_height, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1204
int handle_nvjpegEncode(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state;
  nvjpegEncoderParams_t encoder_params;
  nvjpegImage_t source{};
  nvjpegChromaSubsampling_t input_subsampling;
  nvjpegInputFormat_t input_format;
  int image_width;
  int image_height;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegHandle_t, nvjpegEncoderState_t, const nvjpegEncoderParams_t,
      const nvjpegImage_t *, nvjpegChromaSubsampling_t, nvjpegInputFormat_t,
      int, int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &encoder_params, sizeof(const nvjpegEncoderParams_t)) <
          0 ||
      rpc_read(conn, &source, sizeof(const nvjpegImage_t)) < 0 ||
      rpc_read(conn, &input_subsampling, sizeof(nvjpegChromaSubsampling_t)) <
          0 ||
      rpc_read(conn, &input_format, sizeof(nvjpegInputFormat_t)) < 0 ||
      rpc_read(conn, &image_width, sizeof(int)) < 0 ||
      rpc_read(conn, &image_height, sizeof(int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncode");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, encoder_state, encoder_params,
                                    &source, input_subsampling, input_format,
                                    image_width, image_height, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegEncodeRetrieveBitstreamDevice(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state;
  unsigned char *data;
  size_t length{};
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegEncoderState_t,
                                  unsigned char *, size_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &data, sizeof(unsigned char *)) < 0 ||
      rpc_read(conn, &length, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncodeRetrieveBitstreamDevice");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, encoder_state, data, &length, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &length, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncodeRetrieveBitstream(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegEncoderState_t encoder_state;
  size_t length = 0;
  size_t length_requested = 0;
  unsigned char *data = nullptr;
  uint8_t data_null = 0;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegEncoderState_t,
                                  unsigned char *, size_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &length_requested, sizeof(size_t)) < 0 ||
      ((length = length_requested), false) ||
      rpc_read(conn, &data_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!data_null) {
    data = (unsigned char *)calloc(
        (length_requested != 0 ? length_requested : 1), sizeof(unsigned char));
    if (data == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncodeRetrieveBitstream");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, encoder_state, data, &length, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &length, sizeof(size_t)) < 0 ||
      (!data_null &&
       rpc_write(conn, data,
                 (length < length_requested ? length : length_requested) *
                     sizeof(unsigned char)) < 0) ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_nvjpegBufferPinnedCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegPinnedAllocator_t *pinned_allocator_null_check;
  nvjpegPinnedAllocator_t pinned_allocator;
  nvjpegBufferPinned_t buffer{};
  buffer = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegPinnedAllocator_t *,
                                  nvjpegBufferPinned_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &pinned_allocator_null_check,
               sizeof(nvjpegPinnedAllocator_t *)) < 0 ||
      (pinned_allocator_null_check &&
       rpc_read(conn, &pinned_allocator, sizeof(nvjpegPinnedAllocator_t)) <
           0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferPinnedCreate");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               pinned_allocator_null_check ? &pinned_allocator : nullptr,
               &buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &buffer, sizeof(nvjpegBufferPinned_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegBufferPinnedCreateV2(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegPinnedAllocatorV2_t *pinned_allocator_null_check;
  nvjpegPinnedAllocatorV2_t pinned_allocator;
  nvjpegBufferPinned_t buffer{};
  buffer = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegPinnedAllocatorV2_t *,
                                  nvjpegBufferPinned_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &pinned_allocator_null_check,
               sizeof(nvjpegPinnedAllocatorV2_t *)) < 0 ||
      (pinned_allocator_null_check &&
       rpc_read(conn, &pinned_allocator, sizeof(nvjpegPinnedAllocatorV2_t)) <
           0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferPinnedCreateV2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               pinned_allocator_null_check ? &pinned_allocator : nullptr,
               &buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &buffer, sizeof(nvjpegBufferPinned_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1203
int handle_nvjpegBufferPinnedResize(conn_t *conn) {
  nvjpegBufferPinned_t buffer;
  size_t size;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferPinned_t, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferPinned_t)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferPinnedResize");
  return_value =
      fn == nullptr ? function_not_found() : fn(buffer, size, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegBufferPinnedDestroy(conn_t *conn) {
  nvjpegBufferPinned_t buffer;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferPinned_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferPinned_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferPinnedDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegBufferDeviceCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegDevAllocator_t *device_allocator_null_check;
  nvjpegDevAllocator_t device_allocator;
  nvjpegBufferDevice_t buffer{};
  buffer = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegDevAllocator_t *,
                                  nvjpegBufferDevice_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &device_allocator_null_check,
               sizeof(nvjpegDevAllocator_t *)) < 0 ||
      (device_allocator_null_check &&
       rpc_read(conn, &device_allocator, sizeof(nvjpegDevAllocator_t)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferDeviceCreate");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               device_allocator_null_check ? &device_allocator : nullptr,
               &buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &buffer, sizeof(nvjpegBufferDevice_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegBufferDeviceCreateV2(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegDevAllocatorV2_t *device_allocator_null_check;
  nvjpegDevAllocatorV2_t device_allocator;
  nvjpegBufferDevice_t buffer{};
  buffer = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegDevAllocatorV2_t *,
                                  nvjpegBufferDevice_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &device_allocator_null_check,
               sizeof(nvjpegDevAllocatorV2_t *)) < 0 ||
      (device_allocator_null_check &&
       rpc_read(conn, &device_allocator, sizeof(nvjpegDevAllocatorV2_t)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferDeviceCreateV2");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               device_allocator_null_check ? &device_allocator : nullptr,
               &buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &buffer, sizeof(nvjpegBufferDevice_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1203
int handle_nvjpegBufferDeviceResize(conn_t *conn) {
  nvjpegBufferDevice_t buffer;
  size_t size;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferDevice_t, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferDevice_t)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferDeviceResize");
  return_value =
      fn == nullptr ? function_not_found() : fn(buffer, size, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegBufferDeviceDestroy(conn_t *conn) {
  nvjpegBufferDevice_t buffer;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferDevice_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferDevice_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferDeviceDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegBufferPinnedRetrieve(conn_t *conn) {
  nvjpegBufferPinned_t buffer;
  size_t size{};
  size = {};
  void *ptr{};
  ptr = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferPinned_t, size_t *, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferPinned_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferPinnedRetrieve");
  return_value = fn == nullptr ? function_not_found() : fn(buffer, &size, &ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &ptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegBufferDeviceRetrieve(conn_t *conn) {
  nvjpegBufferDevice_t buffer;
  size_t size{};
  size = {};
  void *ptr{};
  ptr = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegBufferDevice_t, size_t *, void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(nvjpegBufferDevice_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegBufferDeviceRetrieve");
  return_value = fn == nullptr ? function_not_found() : fn(buffer, &size, &ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &ptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegStateAttachPinnedBuffer(conn_t *conn) {
  nvjpegJpegState_t decoder_state;
  nvjpegBufferPinned_t pinned_buffer;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegState_t, nvjpegBufferPinned_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &pinned_buffer, sizeof(nvjpegBufferPinned_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegStateAttachPinnedBuffer");
  return_value =
      fn == nullptr ? function_not_found() : fn(decoder_state, pinned_buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegStateAttachDeviceBuffer(conn_t *conn) {
  nvjpegJpegState_t decoder_state;
  nvjpegBufferDevice_t device_buffer;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegState_t, nvjpegBufferDevice_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &device_buffer, sizeof(nvjpegBufferDevice_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegStateAttachDeviceBuffer");
  return_value =
      fn == nullptr ? function_not_found() : fn(decoder_state, device_buffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegStream_t jpeg_stream{};
  jpeg_stream = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &jpeg_stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamGetJpegEncoding(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  nvjpegJpegEncoding_t jpeg_encoding{};
  jpeg_encoding = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegStream_t, nvjpegJpegEncoding_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetJpegEncoding");
  return_value =
      fn == nullptr ? function_not_found() : fn(jpeg_stream, &jpeg_encoding);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &jpeg_encoding, sizeof(nvjpegJpegEncoding_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamGetFrameDimensions(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  unsigned int width{};
  width = {};
  unsigned int height{};
  height = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegJpegStream_t, unsigned int *, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetFrameDimensions");
  return_value =
      fn == nullptr ? function_not_found() : fn(jpeg_stream, &width, &height);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &width, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &height, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamGetComponentsNum(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  unsigned int components_num{};
  components_num = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegStream_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetComponentsNum");
  return_value =
      fn == nullptr ? function_not_found() : fn(jpeg_stream, &components_num);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &components_num, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamGetComponentDimensions(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  unsigned int component;
  unsigned int width{};
  width = {};
  unsigned int height{};
  height = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegStream_t, unsigned int,
                                  unsigned int *, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &component, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetComponentDimensions");
  return_value = fn == nullptr ? function_not_found()
                               : fn(jpeg_stream, component, &width, &height);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &width, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &height, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegJpegStreamGetExifOrientation(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  nvjpegExifOrientation_t orientation_flag{};
  orientation_flag = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegJpegStream_t, nvjpegExifOrientation_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetExifOrientation");
  return_value =
      fn == nullptr ? function_not_found() : fn(jpeg_stream, &orientation_flag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &orientation_flag, sizeof(nvjpegExifOrientation_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR * 100 + NVJPEG_VER_MINOR >= 1201
int handle_nvjpegJpegStreamGetSamplePrecision(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  unsigned int precision{};
  precision = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegStream_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetSamplePrecision");
  return_value =
      fn == nullptr ? function_not_found() : fn(jpeg_stream, &precision);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &precision, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegJpegStreamGetChromaSubsampling(conn_t *conn) {
  nvjpegJpegStream_t jpeg_stream;
  nvjpegChromaSubsampling_t chroma_subsampling{};
  chroma_subsampling = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegJpegStream_t, nvjpegChromaSubsampling_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegJpegStreamGetChromaSubsampling");
  return_value = fn == nullptr ? function_not_found()
                               : fn(jpeg_stream, &chroma_subsampling);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &chroma_subsampling, sizeof(nvjpegChromaSubsampling_t)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsCreate(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegDecodeParams_t decode_params{};
  decode_params = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegDecodeParams_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &decode_params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsDestroy(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegDecodeParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(decode_params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsSetOutputFormat(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  nvjpegOutputFormat_t output_format;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegDecodeParams_t, nvjpegOutputFormat_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &output_format, sizeof(nvjpegOutputFormat_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsSetOutputFormat");
  return_value =
      fn == nullptr ? function_not_found() : fn(decode_params, output_format);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsSetROI(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  int offset_x;
  int offset_y;
  int roi_width;
  int roi_height;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegDecodeParams_t, int, int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &offset_x, sizeof(int)) < 0 ||
      rpc_read(conn, &offset_y, sizeof(int)) < 0 ||
      rpc_read(conn, &roi_width, sizeof(int)) < 0 ||
      rpc_read(conn, &roi_height, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsSetROI");
  return_value = fn == nullptr ? function_not_found()
                               : fn(decode_params, offset_x, offset_y,
                                    roi_width, roi_height);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsSetAllowCMYK(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  int allow_cmyk;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegDecodeParams_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &allow_cmyk, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsSetAllowCMYK");
  return_value =
      fn == nullptr ? function_not_found() : fn(decode_params, allow_cmyk);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsSetScaleFactor(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  nvjpegScaleFactor_t scale_factor;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegDecodeParams_t, nvjpegScaleFactor_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &scale_factor, sizeof(nvjpegScaleFactor_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsSetScaleFactor");
  return_value =
      fn == nullptr ? function_not_found() : fn(decode_params, scale_factor);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeParamsSetExifOrientation(conn_t *conn) {
  nvjpegDecodeParams_t decode_params;
  nvjpegExifOrientation_t orientation;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegDecodeParams_t, nvjpegExifOrientation_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &orientation, sizeof(nvjpegExifOrientation_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeParamsSetExifOrientation");
  return_value =
      fn == nullptr ? function_not_found() : fn(decode_params, orientation);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecoderCreate(conn_t *conn) {
  nvjpegHandle_t nvjpeg_handle;
  nvjpegBackend_t implementation;
  nvjpegJpegDecoder_t decoder_handle{};
  decoder_handle = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegBackend_t,
                                  nvjpegJpegDecoder_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nvjpeg_handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &implementation, sizeof(nvjpegBackend_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecoderCreate");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(nvjpeg_handle, implementation, &decoder_handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &decoder_handle, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecoderDestroy(conn_t *conn) {
  nvjpegJpegDecoder_t decoder_handle;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegDecoder_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decoder_handle, sizeof(nvjpegJpegDecoder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecoderDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(decoder_handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecoderJpegSupported(conn_t *conn) {
  nvjpegJpegDecoder_t decoder_handle;
  nvjpegJpegStream_t jpeg_stream;
  nvjpegDecodeParams_t decode_params;
  int is_supported{};
  is_supported = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegJpegDecoder_t, nvjpegJpegStream_t,
                                  nvjpegDecodeParams_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &decoder_handle, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecoderJpegSupported");
  return_value = fn == nullptr ? function_not_found()
                               : fn(decoder_handle, jpeg_stream, decode_params,
                                    &is_supported);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &is_supported, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeBatchedSupported(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegStream_t jpeg_stream;
  int is_supported{};
  is_supported = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegStream_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeBatchedSupported");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jpeg_stream, &is_supported);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &is_supported, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeBatchedSupportedEx(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegStream_t jpeg_stream;
  nvjpegDecodeParams_t decode_params;
  int is_supported{};
  is_supported = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegStream_t,
                                  nvjpegDecodeParams_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeBatchedSupportedEx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jpeg_stream, decode_params, &is_supported);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &is_supported, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecoderStateCreate(conn_t *conn) {
  nvjpegHandle_t nvjpeg_handle;
  nvjpegJpegDecoder_t decoder_handle;
  nvjpegJpegState_t decoder_state{};
  decoder_state = {};
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegDecoder_t,
                                  nvjpegJpegState_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nvjpeg_handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &decoder_handle, sizeof(nvjpegJpegDecoder_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecoderStateCreate");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(nvjpeg_handle, decoder_handle, &decoder_state);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeJpeg(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegDecoder_t decoder;
  nvjpegJpegState_t decoder_state;
  nvjpegJpegStream_t jpeg_bitstream;
  nvjpegImage_t destination{};
  nvjpegDecodeParams_t decode_params;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(
      nvjpegHandle_t, nvjpegJpegDecoder_t, nvjpegJpegState_t,
      nvjpegJpegStream_t, nvjpegImage_t *, nvjpegDecodeParams_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &decoder, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &jpeg_bitstream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &destination, sizeof(nvjpegImage_t)) < 0 ||
      rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeJpeg");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, decoder, decoder_state, jpeg_bitstream,
                          &destination, decode_params, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeJpegHost(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegDecoder_t decoder;
  nvjpegJpegState_t decoder_state;
  nvjpegDecodeParams_t decode_params;
  nvjpegJpegStream_t jpeg_stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegDecoder_t, nvjpegJpegState_t,
                         nvjpegDecodeParams_t, nvjpegJpegStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &decoder, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &decode_params, sizeof(nvjpegDecodeParams_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeJpegHost");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, decoder, decoder_state,
                                    decode_params, jpeg_stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeJpegTransferToDevice(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegDecoder_t decoder;
  nvjpegJpegState_t decoder_state;
  nvjpegJpegStream_t jpeg_stream;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegDecoder_t, nvjpegJpegState_t,
                         nvjpegJpegStream_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &decoder, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeJpegTransferToDevice");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, decoder, decoder_state, jpeg_stream, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegDecodeJpegDevice(conn_t *conn) {
  nvjpegHandle_t handle;
  nvjpegJpegDecoder_t decoder;
  nvjpegJpegState_t decoder_state;
  nvjpegImage_t destination{};
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t =
      nvjpegStatus_t (*)(nvjpegHandle_t, nvjpegJpegDecoder_t, nvjpegJpegState_t,
                         nvjpegImage_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(nvjpegHandle_t)) < 0 ||
      rpc_read(conn, &decoder, sizeof(nvjpegJpegDecoder_t)) < 0 ||
      rpc_read(conn, &decoder_state, sizeof(nvjpegJpegState_t)) < 0 ||
      rpc_read(conn, &destination, sizeof(nvjpegImage_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegDecodeJpegDevice");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, decoder, decoder_state, &destination, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_nvjpegEncoderParamsCopyMetadata(conn_t *conn) {
  nvjpegEncoderState_t encoder_state;
  nvjpegEncoderParams_t encode_params;
  nvjpegJpegStream_t jpeg_stream;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderState_t, nvjpegEncoderParams_t,
                                  nvjpegJpegStream_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &encode_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsCopyMetadata");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(encoder_state, encode_params, jpeg_stream, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if NVJPEG_VER_MAJOR < 13
int handle_nvjpegEncoderParamsCopyHuffmanTables(conn_t *conn) {
  nvjpegEncoderState_t encoder_state;
  nvjpegEncoderParams_t encode_params;
  nvjpegJpegStream_t jpeg_stream;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderState_t, nvjpegEncoderParams_t,
                                  nvjpegJpegStream_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encoder_state, sizeof(nvjpegEncoderState_t)) < 0 ||
      rpc_read(conn, &encode_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsCopyHuffmanTables");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(encoder_state, encode_params, jpeg_stream, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_nvjpegEncoderParamsCopyQuantizationTables(conn_t *conn) {
  nvjpegEncoderParams_t encode_params;
  nvjpegJpegStream_t jpeg_stream;
  cudaStream_t stream;
  int request_id;
  nvjpegStatus_t return_value;
  using fn_t = nvjpegStatus_t (*)(nvjpegEncoderParams_t, nvjpegJpegStream_t,
                                  cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &encode_params, sizeof(nvjpegEncoderParams_t)) < 0 ||
      rpc_read(conn, &jpeg_stream, sizeof(nvjpegJpegStream_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = nvjpeg_symbol<fn_t>("nvjpegEncoderParamsCopyQuantizationTables");
  return_value = fn == nullptr ? function_not_found()
                               : fn(encode_params, jpeg_stream, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(nvjpegStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
