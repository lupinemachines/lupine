// Generated by codegen.py. Do not edit by hand.

static nvrtcResult lupine_rpc_nvrtcVersion(conn_t *conn, int *major,
                                           int *minor) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr || rpc_write_start_request(conn, RPC_nvrtcVersion) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, major, sizeof(int)) < 0 ||
      rpc_read(conn, minor, sizeof(int)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcVersion(int *major, int *minor) {
  if (major == nullptr || minor == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection();
  return lupine_rpc_nvrtcVersion(conn, major, minor);
}

static nvrtcResult lupine_rpc_nvrtcGetNumSupportedArchs(conn_t *conn,
                                                        int *numArchs) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetNumSupportedArchs) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, numArchs, sizeof(int)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetNumSupportedArchs(int *numArchs) {
  if (numArchs == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection();
  return lupine_rpc_nvrtcGetNumSupportedArchs(conn, numArchs);
}

static nvrtcResult lupine_rpc_nvrtcDestroyProgram(conn_t *conn,
                                                  nvrtcProgram *prog) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcDestroyProgram) < 0 ||
      rpc_write(conn, prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

static nvrtcResult lupine_rpc_nvrtcGetPTXSize(conn_t *conn, nvrtcProgram prog,
                                              size_t *ptxSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetPTXSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, ptxSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetPTXSize(nvrtcProgram prog, size_t *ptxSizeRet) {
  if (ptxSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value = lupine_rpc_nvrtcGetPTXSize(conn, prog, ptxSizeRet);
  return return_value;
}

static nvrtcResult lupine_rpc_nvrtcGetCUBINSize(conn_t *conn, nvrtcProgram prog,
                                                size_t *cubinSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetCUBINSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, cubinSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetCUBINSize(nvrtcProgram prog,
                                         size_t *cubinSizeRet) {
  if (cubinSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetCUBINSize(conn, prog, cubinSizeRet);
  return return_value;
}

#if CUDA_VERSION >= 12000
static nvrtcResult lupine_rpc_nvrtcGetLTOIRSize(conn_t *conn, nvrtcProgram prog,
                                                size_t *LTOIRSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetLTOIRSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, LTOIRSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetLTOIRSize(nvrtcProgram prog,
                                         size_t *LTOIRSizeRet) {
  if (LTOIRSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetLTOIRSize(conn, prog, LTOIRSizeRet);
  return return_value;
}

#endif

#if CUDA_VERSION >= 12000
static nvrtcResult lupine_rpc_nvrtcGetOptiXIRSize(conn_t *conn,
                                                  nvrtcProgram prog,
                                                  size_t *optixirSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetOptiXIRSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, optixirSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetOptiXIRSize(nvrtcProgram prog,
                                           size_t *optixirSizeRet) {
  if (optixirSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetOptiXIRSize(conn, prog, optixirSizeRet);
  return return_value;
}

#endif

#if CUDA_VERSION < 13000
static nvrtcResult lupine_rpc_nvrtcGetNVVMSize(conn_t *conn, nvrtcProgram prog,
                                               size_t *nvvmSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetNVVMSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, nvvmSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetNVVMSize(nvrtcProgram prog,
                                        size_t *nvvmSizeRet) {
  if (nvvmSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetNVVMSize(conn, prog, nvvmSizeRet);
  return return_value;
}

#endif

static nvrtcResult lupine_rpc_nvrtcGetProgramLogSize(conn_t *conn,
                                                     nvrtcProgram prog,
                                                     size_t *logSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetProgramLogSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, logSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetProgramLogSize(nvrtcProgram prog,
                                              size_t *logSizeRet) {
  if (logSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetProgramLogSize(conn, prog, logSizeRet);
  return return_value;
}

static nvrtcResult
lupine_rpc_nvrtcAddNameExpression(conn_t *conn, nvrtcProgram prog,
                                  const char *name_expression) {
  nvrtcResult return_value = rpc_error();
  std::size_t name_expression_len =
      static_cast<std::size_t>(std::strlen(name_expression) + 1);
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcAddNameExpression) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_write(conn, &name_expression_len, sizeof(std::size_t)) < 0 ||
      rpc_write(conn, name_expression, name_expression_len) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcAddNameExpression(nvrtcProgram prog,
                                              const char *name_expression) {
  if (name_expression == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  const nvrtcProgram program = prog;
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcAddNameExpression(conn, prog, name_expression);
  if (return_value == NVRTC_SUCCESS) {
    note_name_expression(program, name_expression);
  }
  return return_value;
}

#if CUDA_VERSION >= 12080
static nvrtcResult lupine_rpc_nvrtcGetPCHHeapSize(conn_t *conn, size_t *ret) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetPCHHeapSize) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, ret, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetPCHHeapSize(size_t *ret) {
  if (ret == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection();
  return lupine_rpc_nvrtcGetPCHHeapSize(conn, ret);
}

#endif

#if CUDA_VERSION >= 12080
static nvrtcResult lupine_rpc_nvrtcSetPCHHeapSize(conn_t *conn, size_t size) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcSetPCHHeapSize) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcSetPCHHeapSize(size_t size) {
  conn_t *conn = connection();
  return lupine_rpc_nvrtcSetPCHHeapSize(conn, size);
}

#endif

#if CUDA_VERSION >= 12080
static nvrtcResult lupine_rpc_nvrtcGetPCHCreateStatus(conn_t *conn,
                                                      nvrtcProgram prog) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetPCHCreateStatus) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetPCHCreateStatus(nvrtcProgram prog) {
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value = lupine_rpc_nvrtcGetPCHCreateStatus(conn, prog);
  return return_value;
}

#endif

#if CUDA_VERSION >= 12080
static nvrtcResult lupine_rpc_nvrtcGetPCHHeapSizeRequired(conn_t *conn,
                                                          nvrtcProgram prog,
                                                          size_t *size) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetPCHHeapSizeRequired) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetPCHHeapSizeRequired(nvrtcProgram prog,
                                                   size_t *size) {
  if (size == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetPCHHeapSizeRequired(conn, prog, size);
  return return_value;
}

#endif

#if CUDA_VERSION >= 12080
extern "C" nvrtcResult nvrtcSetFlowCallback(nvrtcProgram prog,
                                            int (*callback)(void *, void *),
                                            void *payload) {
  (void)prog;
  (void)callback;
  (void)payload;
  return NVRTC_ERROR_INVALID_INPUT;
}

#endif

#if CUDA_VERSION >= 13020
static nvrtcResult lupine_rpc_nvrtcGetTileIRSize(conn_t *conn,
                                                 nvrtcProgram prog,
                                                 size_t *TileIRSizeRet) {
  nvrtcResult return_value = rpc_error();
  if (conn == nullptr ||
      rpc_write_start_request(conn, RPC_nvrtcGetTileIRSize) < 0 ||
      rpc_write(conn, &prog, sizeof(nvrtcProgram)) < 0 ||
      rpc_wait_for_response(conn) < 0 ||
      rpc_read(conn, TileIRSizeRet, sizeof(size_t)) < 0 ||
      rpc_read(conn, &return_value, sizeof(return_value)) < 0 ||
      rpc_read_end(conn) < 0) {
    return rpc_error();
  }
  return return_value;
}

extern "C" nvrtcResult nvrtcGetTileIRSize(nvrtcProgram prog,
                                          size_t *TileIRSizeRet) {
  if (TileIRSizeRet == nullptr) {
    return NVRTC_ERROR_INVALID_INPUT;
  }
  conn_t *conn = connection_for_handle(prog);
  prog = server_program(prog);
  nvrtcResult return_value =
      lupine_rpc_nvrtcGetTileIRSize(conn, prog, TileIRSizeRet);
  return return_value;
}

#endif
