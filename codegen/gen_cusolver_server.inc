// Generated by codegen.py. Do not edit by hand.

int handle_cusolverGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverGetProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverGetVersion(conn_t *conn) {
  int version{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &version, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(&version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCreate(conn_t *conn) {
  cusolverDnHandle_t handle{};
  handle = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDestroy(conn_t *conn) {
  cusolverDnHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSetStream(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnGetStream(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaStream_t streamId{};
  streamId = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSOLVER_VERSION >= 11500
int handle_cusolverDnSetDeterministicMode(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDeterministicMode_t mode;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDeterministicMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cusolverDeterministicMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetDeterministicMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 11500
int handle_cusolverDnGetDeterministicMode(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDeterministicMode_t mode{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDeterministicMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cusolverDeterministicMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetDeterministicMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cusolverDeterministicMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12003
int handle_cusolverDnSetMathMode(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverMathMode_t mode;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverMathMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cusolverMathMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetMathMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12003
int handle_cusolverDnGetMathMode(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverMathMode_t mode{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverMathMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(cusolverMathMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetMathMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(cusolverMathMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12003
int handle_cusolverDnSetEmulationStrategy(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationStrategy_t strategy;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cudaEmulationStrategy_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &strategy, sizeof(cudaEmulationStrategy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetEmulationStrategy");
  return_value = fn == nullptr ? function_not_found() : fn(handle, strategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12003
int handle_cusolverDnGetEmulationStrategy(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationStrategy_t strategy{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cudaEmulationStrategy_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &strategy, sizeof(cudaEmulationStrategy_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetEmulationStrategy");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &strategy);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &strategy, sizeof(cudaEmulationStrategy_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnSetFixedPointEmulationMantissaControl(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationMantissaControl_t control;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cudaEmulationMantissaControl_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &control, sizeof(cudaEmulationMantissaControl_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetFixedPointEmulationMantissaControl");
  return_value = fn == nullptr ? function_not_found() : fn(handle, control);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnGetFixedPointEmulationMantissaControl(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationMantissaControl_t control{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t,
                                    cudaEmulationMantissaControl_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &control, sizeof(cudaEmulationMantissaControl_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetFixedPointEmulationMantissaControl");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &control);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &control, sizeof(cudaEmulationMantissaControl_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnSetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cusolverDnHandle_t handle;
  int mantissaBitCount;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>(
      "cusolverDnSetFixedPointEmulationMaxMantissaBitCount");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnGetFixedPointEmulationMaxMantissaBitCount(conn_t *conn) {
  cusolverDnHandle_t handle;
  int mantissaBitCount{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitCount, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>(
      "cusolverDnGetFixedPointEmulationMaxMantissaBitCount");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &mantissaBitCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitCount, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnSetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cusolverDnHandle_t handle;
  int mantissaBitOffset;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitOffset, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>(
      "cusolverDnSetFixedPointEmulationMantissaBitOffset");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnGetFixedPointEmulationMantissaBitOffset(conn_t *conn) {
  cusolverDnHandle_t handle;
  int mantissaBitOffset{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mantissaBitOffset, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>(
      "cusolverDnGetFixedPointEmulationMantissaBitOffset");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &mantissaBitOffset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mantissaBitOffset, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnSetEmulationSpecialValuesSupport(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationSpecialValuesSupport_t mask;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t,
                                    cudaEmulationSpecialValuesSupport_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetEmulationSpecialValuesSupport");
  return_value = fn == nullptr ? function_not_found() : fn(handle, mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnGetEmulationSpecialValuesSupport(conn_t *conn) {
  cusolverDnHandle_t handle;
  cudaEmulationSpecialValuesSupport_t mask{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t,
                                    cudaEmulationSpecialValuesSupport_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnGetEmulationSpecialValuesSupport");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &mask);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mask, sizeof(cudaEmulationSpecialValuesSupport_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cusolverDnIRSParamsCreate(conn_t *conn) {
  cusolverDnIRSParams_t params_ptr{};
  params_ptr = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&params_ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &params_ptr, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsDestroy(conn_t *conn) {
  cusolverDnIRSParams_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetRefinementSolver(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolverIRSRefinement_t refinement_solver;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolverIRSRefinement_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &refinement_solver, sizeof(cusolverIRSRefinement_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetRefinementSolver");
  return_value =
      fn == nullptr ? function_not_found() : fn(params, refinement_solver);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetSolverMainPrecision(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolverPrecType_t solver_main_precision;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolverPrecType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &solver_main_precision, sizeof(cusolverPrecType_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetSolverMainPrecision");
  return_value =
      fn == nullptr ? function_not_found() : fn(params, solver_main_precision);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetSolverLowestPrecision(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolverPrecType_t solver_lowest_precision;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolverPrecType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &solver_lowest_precision, sizeof(cusolverPrecType_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetSolverLowestPrecision");
  return_value = fn == nullptr ? function_not_found()
                               : fn(params, solver_lowest_precision);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetSolverPrecisions(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolverPrecType_t solver_main_precision;
  cusolverPrecType_t solver_lowest_precision;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolverPrecType_t,
                                    cusolverPrecType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &solver_main_precision, sizeof(cusolverPrecType_t)) < 0 ||
      rpc_read(conn, &solver_lowest_precision, sizeof(cusolverPrecType_t)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetSolverPrecisions");
  return_value = fn == nullptr ? function_not_found()
                               : fn(params, solver_main_precision,
                                    solver_lowest_precision);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetTol(conn_t *conn) {
  cusolverDnIRSParams_t params;
  double val;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &val, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetTol");
  return_value = fn == nullptr ? function_not_found() : fn(params, val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetTolInner(conn_t *conn) {
  cusolverDnIRSParams_t params;
  double val;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &val, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetTolInner");
  return_value = fn == nullptr ? function_not_found() : fn(params, val);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetMaxIters(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolver_int_t maxiters;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolver_int_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &maxiters, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetMaxIters");
  return_value = fn == nullptr ? function_not_found() : fn(params, maxiters);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsSetMaxItersInner(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolver_int_t maxiters_inner;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolver_int_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &maxiters_inner, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsSetMaxItersInner");
  return_value =
      fn == nullptr ? function_not_found() : fn(params, maxiters_inner);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsGetMaxIters(conn_t *conn) {
  cusolverDnIRSParams_t params;
  cusolver_int_t maxiters{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &maxiters, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsGetMaxIters");
  return_value = fn == nullptr ? function_not_found() : fn(params, &maxiters);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &maxiters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsEnableFallback(conn_t *conn) {
  cusolverDnIRSParams_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsEnableFallback");
  return_value = fn == nullptr ? function_not_found() : fn(params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSParamsDisableFallback(conn_t *conn) {
  cusolverDnIRSParams_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSParamsDisableFallback");
  return_value = fn == nullptr ? function_not_found() : fn(params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosDestroy(conn_t *conn) {
  cusolverDnIRSInfos_t infos;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &infos, sizeof(cusolverDnIRSInfos_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(infos);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosCreate(conn_t *conn) {
  cusolverDnIRSInfos_t infos_ptr{};
  infos_ptr = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&infos_ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &infos_ptr, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosGetNiters(conn_t *conn) {
  cusolverDnIRSInfos_t infos;
  cusolver_int_t niters{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &infos, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_read(conn, &niters, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosGetNiters");
  return_value = fn == nullptr ? function_not_found() : fn(infos, &niters);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosGetOuterNiters(conn_t *conn) {
  cusolverDnIRSInfos_t infos;
  cusolver_int_t outer_niters{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &infos, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_read(conn, &outer_niters, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosGetOuterNiters");
  return_value =
      fn == nullptr ? function_not_found() : fn(infos, &outer_niters);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &outer_niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosRequestResidual(conn_t *conn) {
  cusolverDnIRSInfos_t infos;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &infos, sizeof(cusolverDnIRSInfos_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosRequestResidual");
  return_value = fn == nullptr ? function_not_found() : fn(infos);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSInfosGetMaxIters(conn_t *conn) {
  cusolverDnIRSInfos_t infos;
  cusolver_int_t maxiters{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnIRSInfos_t, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &infos, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_read(conn, &maxiters, sizeof(cusolver_int_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSInfosGetMaxIters");
  return_value = fn == nullptr ? function_not_found() : fn(infos, &maxiters);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &maxiters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZZgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZZgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZCgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZCgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZKgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZKgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZEgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZEgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZYgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZYgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCCgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCCgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCEgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCEgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCKgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCKgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCYgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCYgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDDgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, double *,
      cusolver_int_t, cusolver_int_t *, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDDgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDSgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, double *,
      cusolver_int_t, cusolver_int_t *, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDSgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDHgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, double *,
      cusolver_int_t, cusolver_int_t *, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDHgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDBgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, double *,
      cusolver_int_t, cusolver_int_t *, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDBgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDXgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, double *,
      cusolver_int_t, cusolver_int_t *, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDXgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSSgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, float *,
      cusolver_int_t, cusolver_int_t *, float *, cusolver_int_t, float *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSSgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSHgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, float *,
      cusolver_int_t, cusolver_int_t *, float *, cusolver_int_t, float *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSHgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSBgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, float *,
      cusolver_int_t, cusolver_int_t *, float *, cusolver_int_t, float *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSBgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSXgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, float *,
      cusolver_int_t, cusolver_int_t *, float *, cusolver_int_t, float *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSXgesv");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZZgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZZgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZCgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZCgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZKgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZKgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZEgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZEgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZYgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuDoubleComplex *,
      cusolver_int_t, cusolver_int_t *, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZYgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCCgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCCgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCKgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCKgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCEgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCEgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCYgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cuComplex *,
      cusolver_int_t, cusolver_int_t *, cuComplex *, cusolver_int_t,
      cuComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCYgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDDgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    cusolver_int_t *, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDDgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDSgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    cusolver_int_t *, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDSgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDHgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    cusolver_int_t *, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDHgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDBgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    cusolver_int_t *, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDBgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDXgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    cusolver_int_t *, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDXgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSSgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    cusolver_int_t *, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSSgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSHgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    cusolver_int_t *, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSHgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSBgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    cusolver_int_t *, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSBgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSXgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  cusolver_int_t *dipiv;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    cusolver_int_t *, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dipiv, sizeof(cusolver_int_t *)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSXgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nrhs, dA, ldda, dipiv, dB, lddb,
                                    dX, lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZZgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZZgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZCgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZCgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZKgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZKgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZEgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZEgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZYgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZYgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCCgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCCgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCKgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCKgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCEgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCEgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCYgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCYgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDDgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      double *, cusolver_int_t, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDDgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDSgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      double *, cusolver_int_t, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDSgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDHgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      double *, cusolver_int_t, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDHgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDBgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      double *, cusolver_int_t, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDBgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDXgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      double *, cusolver_int_t, double *, cusolver_int_t, double *,
      cusolver_int_t, void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDXgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSSgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      float *, cusolver_int_t, float *, cusolver_int_t, float *, cusolver_int_t,
      void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSSgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSHgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      float *, cusolver_int_t, float *, cusolver_int_t, float *, cusolver_int_t,
      void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSHgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSBgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      float *, cusolver_int_t, float *, cusolver_int_t, float *, cusolver_int_t,
      void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSBgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSXgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t iter{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      float *, cusolver_int_t, float *, cusolver_int_t, float *, cusolver_int_t,
      void *, size_t, cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSXgels");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                          dWorkspace, lwork_bytes, &iter, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iter, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZZgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZZgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZCgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZCgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZKgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZKgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZEgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZEgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZYgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuDoubleComplex *dA;
  cusolver_int_t ldda;
  cuDoubleComplex *dB;
  cusolver_int_t lddb;
  cuDoubleComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, cuDoubleComplex *, cusolver_int_t,
      cuDoubleComplex *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZYgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCCgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCCgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCKgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCKgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCEgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCEgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCYgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  cuComplex *dA;
  cusolver_int_t ldda;
  cuComplex *dB;
  cusolver_int_t lddb;
  cuComplex *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolver_int_t, cusolver_int_t, cusolver_int_t,
      cuComplex *, cusolver_int_t, cuComplex *, cusolver_int_t, cuComplex *,
      cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCYgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDDgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, double *,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDDgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDSgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, double *,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDSgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDHgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, double *,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDHgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDBgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, double *,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDBgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDXgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  double *dA;
  cusolver_int_t ldda;
  double *dB;
  cusolver_int_t lddb;
  double *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, double *,
                                    cusolver_int_t, double *, cusolver_int_t,
                                    double *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(double *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDXgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSSgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, float *,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSSgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSHgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, float *,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSHgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSBgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, float *,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSBgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSXgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  float *dA;
  cusolver_int_t ldda;
  float *dB;
  cusolver_int_t lddb;
  float *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolver_int_t,
                                    cusolver_int_t, cusolver_int_t, float *,
                                    cusolver_int_t, float *, cusolver_int_t,
                                    float *, cusolver_int_t, void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(float *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSXgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nrhs, dA, ldda, dB, lddb, dX,
                                    lddx, dWorkspace, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSXgesv(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnIRSParams_t gesv_irs_params;
  cusolverDnIRSInfos_t gesv_irs_infos;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  void *dA;
  cusolver_int_t ldda;
  void *dB;
  cusolver_int_t lddb;
  void *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t niters{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnIRSParams_t, cusolverDnIRSInfos_t,
      cusolver_int_t, cusolver_int_t, void *, cusolver_int_t, void *,
      cusolver_int_t, void *, cusolver_int_t, void *, size_t, cusolver_int_t *,
      cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &gesv_irs_params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &gesv_irs_infos, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(void *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(void *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSXgesv");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, gesv_irs_params, gesv_irs_infos, n,
                                    nrhs, dA, ldda, dB, lddb, dX, lddx,
                                    dWorkspace, lwork_bytes, &niters, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSXgesv_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnIRSParams_t params;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnIRSParams_t,
                                    cusolver_int_t, cusolver_int_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSXgesv_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, params, n, nrhs, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSXgels(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnIRSParams_t gels_irs_params;
  cusolverDnIRSInfos_t gels_irs_infos;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  void *dA;
  cusolver_int_t ldda;
  void *dB;
  cusolver_int_t lddb;
  void *dX;
  cusolver_int_t lddx;
  void *dWorkspace;
  size_t lwork_bytes;
  cusolver_int_t niters{};
  cusolver_int_t *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnIRSParams_t, cusolverDnIRSInfos_t,
      cusolver_int_t, cusolver_int_t, cusolver_int_t, void *, cusolver_int_t,
      void *, cusolver_int_t, void *, cusolver_int_t, void *, size_t,
      cusolver_int_t *, cusolver_int_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &gels_irs_params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &gels_irs_infos, sizeof(cusolverDnIRSInfos_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dA, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldda, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dB, sizeof(void *)) < 0 ||
      rpc_read(conn, &lddb, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dX, sizeof(void *)) < 0 ||
      rpc_read(conn, &lddx, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &dWorkspace, sizeof(void *)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &d_info, sizeof(cusolver_int_t *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSXgels");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, gels_irs_params, gels_irs_infos, m,
                                    n, nrhs, dA, ldda, dB, lddb, dX, lddx,
                                    dWorkspace, lwork_bytes, &niters, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &niters, sizeof(cusolver_int_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnIRSXgels_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnIRSParams_t params;
  cusolver_int_t m;
  cusolver_int_t n;
  cusolver_int_t nrhs;
  size_t lwork_bytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnIRSParams_t,
                                    cusolver_int_t, cusolver_int_t,
                                    cusolver_int_t, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnIRSParams_t)) < 0 ||
      rpc_read(conn, &m, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &n, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(cusolver_int_t)) < 0 ||
      rpc_read(conn, &lwork_bytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnIRSXgels_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, params, m, n, nrhs, &lwork_bytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork_bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotrf_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotrf_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotrf_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotrf_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(float *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(double *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, cuDoubleComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  const float *A;
  int lda;
  float *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           const float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotrs");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, nrhs, A, lda, B, ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  const double *A;
  int lda;
  double *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           const double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotrs");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, nrhs, A, lda, B, ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  const cuComplex *A;
  int lda;
  cuComplex *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           const cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotrs");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, nrhs, A, lda, B, ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  const cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    int, const cuDoubleComplex *, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotrs");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, nrhs, A, lda, B, ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotrfBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float **Aarray;
  int lda;
  int *infoArray;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(float **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &infoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotrfBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, Aarray, lda, infoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotrfBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double **Aarray;
  int lda;
  int *infoArray;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(double **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &infoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotrfBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, Aarray, lda, infoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotrfBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex **Aarray;
  int lda;
  int *infoArray;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuComplex **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &infoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotrfBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, Aarray, lda, infoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotrfBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex **Aarray;
  int lda;
  int *infoArray;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Aarray, sizeof(cuDoubleComplex **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &infoArray, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotrfBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, Aarray, lda, infoArray, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotrsBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  float **A;
  int lda;
  float **B;
  int ldb;
  int *d_info;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           float **, int, float **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float **)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotrsBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, nrhs, A, lda, B, ldb,
                                    d_info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotrsBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  double **A;
  int lda;
  double **B;
  int ldb;
  int *d_info;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           double **, int, double **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double **)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotrsBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, nrhs, A, lda, B, ldb,
                                    d_info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotrsBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  cuComplex **A;
  int lda;
  cuComplex **B;
  int ldb;
  int *d_info;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, int,
                           cuComplex **, int, cuComplex **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex **)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotrsBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, nrhs, A, lda, B, ldb,
                                    d_info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotrsBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  int nrhs;
  cuDoubleComplex **A;
  int lda;
  cuDoubleComplex **B;
  int ldb;
  int *d_info;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    int, cuDoubleComplex **, int,
                                    cuDoubleComplex **, int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex **)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex **)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotrsBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, nrhs, A, lda, B, ldb,
                                    d_info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSpotri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSpotri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDpotri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDpotri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCpotri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCpotri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZpotri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, cuDoubleComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZpotri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXtrtri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  cublasDiagType_t diag;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t,
                                    cublasDiagType_t, int64_t, cudaDataType,
                                    void *, int64_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXtrtri_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, diag, n, dataTypeA, A, lda,
                          &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXtrtri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  cublasDiagType_t diag;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, cublasDiagType_t, int64_t,
      cudaDataType, void *, int64_t, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cublasDiagType_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXtrtri");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, uplo, diag, n, dataTypeA, A, lda,
                         bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnSlauum_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSlauum_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDlauum_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDlauum_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnClauum_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnClauum_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZlauum_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZlauum_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSlauum(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSlauum");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDlauum(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDlauum");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnClauum(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnClauum");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZlauum(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, cuDoubleComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZlauum");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgetrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  float *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgetrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgetrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  double *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgetrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgetrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuComplex *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgetrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgetrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgetrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgetrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  float *A;
  int lda;
  float *Workspace;
  int *devIpiv;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, float *, int,
                                    float *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(float *)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgetrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, Workspace, devIpiv, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgetrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  double *A;
  int lda;
  double *Workspace;
  int *devIpiv;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, double *, int,
                                    double *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(double *)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgetrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, Workspace, devIpiv, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgetrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *Workspace;
  int *devIpiv;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuComplex *,
                                    int, cuComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgetrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, Workspace, devIpiv, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgetrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *Workspace;
  int *devIpiv;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuDoubleComplex *, int,
                           cuDoubleComplex *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgetrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, Workspace, devIpiv, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSlaswp(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  float *A;
  int lda;
  int k1;
  int k2;
  const int *devIpiv;
  int incx;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, float *, int, int,
                                    int, const int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &k1, sizeof(int)) < 0 ||
      rpc_read(conn, &k2, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSlaswp");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, k1, k2, devIpiv, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDlaswp(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  double *A;
  int lda;
  int k1;
  int k2;
  const int *devIpiv;
  int incx;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, double *, int, int,
                                    int, const int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &k1, sizeof(int)) < 0 ||
      rpc_read(conn, &k2, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDlaswp");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, k1, k2, devIpiv, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnClaswp(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  cuComplex *A;
  int lda;
  int k1;
  int k2;
  const int *devIpiv;
  int incx;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, cuComplex *, int,
                                    int, int, const int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &k1, sizeof(int)) < 0 ||
      rpc_read(conn, &k2, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnClaswp");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, k1, k2, devIpiv, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZlaswp(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  cuDoubleComplex *A;
  int lda;
  int k1;
  int k2;
  const int *devIpiv;
  int incx;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, cuDoubleComplex *,
                                    int, int, int, const int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &k1, sizeof(int)) < 0 ||
      rpc_read(conn, &k2, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &incx, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZlaswp");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, A, lda, k1, k2, devIpiv, incx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgetrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const float *A;
  int lda;
  const int *devIpiv;
  float *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasOperation_t, int,
                                    int, const float *, int, const int *,
                                    float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgetrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, A, lda, devIpiv, B,
                                    ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgetrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const double *A;
  int lda;
  const int *devIpiv;
  double *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasOperation_t, int,
                                    int, const double *, int, const int *,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgetrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, A, lda, devIpiv, B,
                                    ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgetrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuComplex *A;
  int lda;
  const int *devIpiv;
  cuComplex *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasOperation_t, int,
                                    int, const cuComplex *, int, const int *,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgetrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, A, lda, devIpiv, B,
                                    ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgetrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasOperation_t trans;
  int n;
  int nrhs;
  const cuDoubleComplex *A;
  int lda;
  const int *devIpiv;
  cuDoubleComplex *B;
  int ldb;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasOperation_t, int,
                                    int, const cuDoubleComplex *, int,
                                    const int *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &devIpiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgetrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, trans, n, nrhs, A, lda, devIpiv, B,
                                    ldb, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgeqrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  float *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgeqrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgeqrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  double *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgeqrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgeqrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgeqrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgeqrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgeqrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgeqrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  float *A;
  int lda;
  float *TAU;
  float *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, float *, int,
                                    float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TAU, sizeof(float *)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(float *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgeqrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, TAU, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgeqrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  double *A;
  int lda;
  double *TAU;
  double *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, double *, int,
                                    double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TAU, sizeof(double *)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(double *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgeqrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, TAU, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgeqrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *TAU;
  cuComplex *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuComplex *,
                                    int, cuComplex *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TAU, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgeqrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, TAU, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgeqrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *TAU;
  cuDoubleComplex *Workspace;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuDoubleComplex *, int,
                           cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &TAU, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &Workspace, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgeqrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, A, lda, TAU, Workspace, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  const float *A;
  int lda;
  const float *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int,
                                    const float *, int, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  const double *A;
  int lda;
  const double *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int,
                                    const double *, int, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int, const cuComplex *,
                           int, const cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int,
                                    const cuDoubleComplex *, int,
                                    const cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  float *A;
  int lda;
  const float *tau;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int, float *,
                                    int, const float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  double *A;
  int lda;
  const double *tau;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int, double *,
                                    int, const double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  cuComplex *A;
  int lda;
  const cuComplex *tau;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int, cuComplex *, int,
                           const cuComplex *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int k;
  cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, int, int, int, cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSormqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const float *A;
  int lda;
  const float *tau;
  const float *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const float *, int, const float *, const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSormqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDormqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const double *A;
  int lda;
  const double *tau;
  const double *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const double *, int, const double *, const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDormqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCunmqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  const cuComplex *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const cuComplex *, int, const cuComplex *, const cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCunmqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZunmqr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  const cuDoubleComplex *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZunmqr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSormqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const float *A;
  int lda;
  const float *tau;
  float *C;
  int ldc;
  float *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const float *, int, const float *, float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSormqr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDormqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const double *A;
  int lda;
  const double *tau;
  double *C;
  int ldc;
  double *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const double *, int, const double *, double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDormqr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCunmqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  cuComplex *C;
  int ldc;
  cuComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t,
                                    cublasOperation_t, int, int, int,
                                    const cuComplex *, int, const cuComplex *,
                                    cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCunmqr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZunmqr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasOperation_t trans;
  int m;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  cuDoubleComplex *C;
  int ldc;
  cuDoubleComplex *work;
  int lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasOperation_t, int, int, int,
      const cuDoubleComplex *, int, const cuDoubleComplex *, cuDoubleComplex *,
      int, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZunmqr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, trans, m, n, k, A, lda, tau,
                                    C, ldc, work, lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsytrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  float *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  double *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCsytrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  cuComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCsytrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZsytrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int n;
  cuDoubleComplex *A;
  int lda;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, cuDoubleComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZsytrf_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, A, lda, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsytrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  int *ipiv;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, int *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  int *ipiv;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, int *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCsytrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  int *ipiv;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                           cuComplex *, int, int *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCsytrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZsytrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  int *ipiv;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, int *,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(int *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZsytrf");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsytrs_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t nrhs;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  const int64_t *ipiv;
  cudaDataType dataTypeB;
  void *B;
  int64_t ldb;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, int64_t, int64_t, cudaDataType,
      const void *, int64_t, const int64_t *, cudaDataType, void *, int64_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int64_t *)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsytrs_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, nrhs, dataTypeA, A, lda, ipiv, dataTypeB, B,
               ldb, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsytrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t nrhs;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  const int64_t *ipiv;
  cudaDataType dataTypeB;
  void *B;
  int64_t ldb;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, int64_t, int64_t, cudaDataType,
      const void *, int64_t, const int64_t *, cudaDataType, void *, int64_t,
      void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int64_t *)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsytrs");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, uplo, n, nrhs, dataTypeA, A, lda, ipiv, dataTypeB, B,
               ldb, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnSsytri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  const int *ipiv;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    float *, int, const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, ipiv, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  const int *ipiv;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, ipiv, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCsytri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  const int *ipiv;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCsytri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, ipiv, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZsytri_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  const int *ipiv;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZsytri_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, ipiv, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsytri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  const int *ipiv;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, float *,
                           int, const int *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  const int *ipiv;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, double *,
                           int, const int *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCsytri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  const int *ipiv;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, const int *, cuComplex *,
                                    int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCsytri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZsytri(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  const int *ipiv;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuDoubleComplex *, int, const int *,
                                    cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZsytri");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, ipiv, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgebrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgebrd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgebrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgebrd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgebrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgebrd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgebrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int Lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgebrd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &Lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgebrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  float *A;
  int lda;
  float *D;
  float *E;
  float *TAUQ;
  float *TAUP;
  float *Work;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, int, int, float *, int, float *,
                           float *, float *, float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &D, sizeof(float *)) < 0 ||
      rpc_read(conn, &E, sizeof(float *)) < 0 ||
      rpc_read(conn, &TAUQ, sizeof(float *)) < 0 ||
      rpc_read(conn, &TAUP, sizeof(float *)) < 0 ||
      rpc_read(conn, &Work, sizeof(float *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgebrd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, D, E, TAUQ, TAUP,
                                    Work, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgebrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  double *A;
  int lda;
  double *D;
  double *E;
  double *TAUQ;
  double *TAUP;
  double *Work;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, double *, int,
                                    double *, double *, double *, double *,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &D, sizeof(double *)) < 0 ||
      rpc_read(conn, &E, sizeof(double *)) < 0 ||
      rpc_read(conn, &TAUQ, sizeof(double *)) < 0 ||
      rpc_read(conn, &TAUP, sizeof(double *)) < 0 ||
      rpc_read(conn, &Work, sizeof(double *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgebrd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, D, E, TAUQ, TAUP,
                                    Work, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgebrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuComplex *A;
  int lda;
  float *D;
  float *E;
  cuComplex *TAUQ;
  cuComplex *TAUP;
  cuComplex *Work;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, cuComplex *,
                                    int, float *, float *, cuComplex *,
                                    cuComplex *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &D, sizeof(float *)) < 0 ||
      rpc_read(conn, &E, sizeof(float *)) < 0 ||
      rpc_read(conn, &TAUQ, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &TAUP, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &Work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgebrd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, D, E, TAUQ, TAUP,
                                    Work, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgebrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *D;
  double *E;
  cuDoubleComplex *TAUQ;
  cuDoubleComplex *TAUP;
  cuDoubleComplex *Work;
  int Lwork;
  int *devInfo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, int, int, cuDoubleComplex *, int, double *, double *,
      cuDoubleComplex *, cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &D, sizeof(double *)) < 0 ||
      rpc_read(conn, &E, sizeof(double *)) < 0 ||
      rpc_read(conn, &TAUQ, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &TAUP, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &Work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &Lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &devInfo, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgebrd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, A, lda, D, E, TAUQ, TAUP,
                                    Work, Lwork, devInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgbr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  const float *A;
  int lda;
  const float *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int, int, int,
                           const float *, int, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgbr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgbr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  const double *A;
  int lda;
  const double *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int, int, int,
                           const double *, int, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgbr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungbr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int, int, int,
                           const cuComplex *, int, const cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungbr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungbr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int,
                                    int, int, const cuDoubleComplex *, int,
                                    const cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungbr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, m, n, k, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgbr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  float *A;
  int lda;
  const float *tau;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int, int, int,
                           float *, int, const float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgbr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, side, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgbr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  double *A;
  int lda;
  const double *tau;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int, int, int,
                           double *, int, const double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgbr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, side, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungbr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  cuComplex *A;
  int lda;
  const cuComplex *tau;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t, int,
                                    int, int, cuComplex *, int,
                                    const cuComplex *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungbr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, side, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungbr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  int m;
  int n;
  int k;
  cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, int, int, int, cuDoubleComplex *,
      int, const cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &k, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungbr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, side, m, n, k, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsytrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *d;
  const float *e;
  const float *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const float *, int, const float *,
                                    const float *, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &e, sizeof(const float *)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytrd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, d, e, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *d;
  const double *e;
  const double *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const double *, int, const double *,
                                    const double *, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &e, sizeof(const double *)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytrd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, d, e, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChetrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const float *d;
  const float *e;
  const cuComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const cuComplex *, int, const float *,
                                    const float *, const cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(const float *)) < 0 ||
      rpc_read(conn, &e, sizeof(const float *)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChetrd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, d, e, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhetrd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *d;
  const double *e;
  const cuDoubleComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, int, const cuDoubleComplex *, int,
      const double *, const double *, const cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(const double *)) < 0 ||
      rpc_read(conn, &e, sizeof(const double *)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhetrd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, d, e, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsytrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *d;
  float *e;
  float *tau;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, float *,
                           int, float *, float *, float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(float *)) < 0 ||
      rpc_read(conn, &e, sizeof(float *)) < 0 ||
      rpc_read(conn, &tau, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsytrd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, uplo, n, A, lda, d, e, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsytrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *d;
  double *e;
  double *tau;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    double *, int, double *, double *, double *,
                                    double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(double *)) < 0 ||
      rpc_read(conn, &e, sizeof(double *)) < 0 ||
      rpc_read(conn, &tau, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsytrd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, uplo, n, A, lda, d, e, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChetrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  float *d;
  float *e;
  cuComplex *tau;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, float *, float *,
                                    cuComplex *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(float *)) < 0 ||
      rpc_read(conn, &e, sizeof(float *)) < 0 ||
      rpc_read(conn, &tau, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChetrd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, uplo, n, A, lda, d, e, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhetrd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *d;
  double *e;
  cuDoubleComplex *tau;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, int, cuDoubleComplex *, int,
      double *, double *, cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &d, sizeof(double *)) < 0 ||
      rpc_read(conn, &e, sizeof(double *)) < 0 ||
      rpc_read(conn, &tau, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhetrd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, uplo, n, A, lda, d, e, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const float *, int, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const double *, int, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                           const cuComplex *, int, const cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    const cuDoubleComplex *, int,
                                    const cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, uplo, n, A, lda, tau, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSorgtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  const float *tau;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, float *,
                           int, const float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSorgtr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDorgtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  const double *tau;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int, double *,
                           int, const double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDorgtr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCungtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  const cuComplex *tau;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasFillMode_t, int,
                                    cuComplex *, int, const cuComplex *,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCungtr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZungtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasFillMode_t, int, cuDoubleComplex *, int,
      const cuDoubleComplex *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZungtr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, uplo, n, A, lda, tau, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSormtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  const float *A;
  int lda;
  const float *tau;
  const float *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, const float *, int, const float *, const float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const float *)) < 0 ||
      rpc_read(conn, &C, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSormtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDormtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  const double *A;
  int lda;
  const double *tau;
  const double *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t,
                                    cublasFillMode_t, cublasOperation_t, int,
                                    int, const double *, int, const double *,
                                    const double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const double *)) < 0 ||
      rpc_read(conn, &C, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDormtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCunmtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *tau;
  const cuComplex *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, const cuComplex *, int, const cuComplex *, const cuComplex *,
      int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCunmtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZunmtr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *tau;
  const cuDoubleComplex *C;
  int ldc;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, const cuDoubleComplex *, int, const cuDoubleComplex *,
      const cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZunmtr_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSormtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  float *A;
  int lda;
  float *tau;
  float *C;
  int ldc;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, float *, int, float *, float *, int, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(float *)) < 0 ||
      rpc_read(conn, &C, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSormtr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDormtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  double *A;
  int lda;
  double *tau;
  double *C;
  int ldc;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, double *, int, double *, double *, int, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(double *)) < 0 ||
      rpc_read(conn, &C, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDormtr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCunmtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *tau;
  cuComplex *C;
  int ldc;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cublasSideMode_t,
                                    cublasFillMode_t, cublasOperation_t, int,
                                    int, cuComplex *, int, cuComplex *,
                                    cuComplex *, int, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCunmtr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZunmtr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cublasSideMode_t side;
  cublasFillMode_t uplo;
  cublasOperation_t trans;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *tau;
  cuDoubleComplex *C;
  int ldc;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cublasSideMode_t, cublasFillMode_t, cublasOperation_t,
      int, int, cuDoubleComplex *, int, cuDoubleComplex *, cuDoubleComplex *,
      int, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &side, sizeof(cublasSideMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &tau, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &C, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldc, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZunmtr");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, side, uplo, trans, m, n, A, lda,
                                    tau, C, ldc, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  int m;
  int n;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, int, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvd_bufferSize");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, m, n, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  signed char jobu;
  signed char jobvt;
  int m;
  int n;
  float *A;
  int lda;
  float *S;
  float *U;
  int ldu;
  float *VT;
  int ldvt;
  float *work;
  int lwork;
  float *rwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, signed char, signed char, int, int, float *, int,
      float *, float *, int, float *, int, float *, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &VT, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &rwork, sizeof(float *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobu, jobvt, m, n, A, lda, S, U,
                                    ldu, VT, ldvt, work, lwork, rwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  signed char jobu;
  signed char jobvt;
  int m;
  int n;
  double *A;
  int lda;
  double *S;
  double *U;
  int ldu;
  double *VT;
  int ldvt;
  double *work;
  int lwork;
  double *rwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, signed char, signed char, int, int, double *, int,
      double *, double *, int, double *, int, double *, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &VT, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &rwork, sizeof(double *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobu, jobvt, m, n, A, lda, S, U,
                                    ldu, VT, ldvt, work, lwork, rwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  signed char jobu;
  signed char jobvt;
  int m;
  int n;
  cuComplex *A;
  int lda;
  float *S;
  cuComplex *U;
  int ldu;
  cuComplex *VT;
  int ldvt;
  cuComplex *work;
  int lwork;
  float *rwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, signed char, signed char, int,
                           int, cuComplex *, int, float *, cuComplex *, int,
                           cuComplex *, int, cuComplex *, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &VT, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &rwork, sizeof(float *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobu, jobvt, m, n, A, lda, S, U,
                                    ldu, VT, ldvt, work, lwork, rwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  signed char jobu;
  signed char jobvt;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *S;
  cuDoubleComplex *U;
  int ldu;
  cuDoubleComplex *VT;
  int ldvt;
  cuDoubleComplex *work;
  int lwork;
  double *rwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, signed char, signed char, int, int, cuDoubleComplex *,
      int, double *, cuDoubleComplex *, int, cuDoubleComplex *, int,
      cuDoubleComplex *, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &VT, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &rwork, sizeof(double *)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobu, jobvt, m, n, A, lda, S, U,
                                    ldu, VT, ldvt, work, lwork, rwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const float *, int,
                                    const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const double *, int,
                                    const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const cuComplex *,
                                    int, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int,
      const cuDoubleComplex *, int, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *W;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, float *, int,
                                    float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevd");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *W;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, double *, int,
                                    double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevd");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, cuComplex *, int,
                                    float *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevd");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int,
      cuDoubleComplex *, int, double *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevd");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    const float *, int, float, float, int, int,
                                    int *, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    const double *, int, double, double, int,
                                    int, int *, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    const cuComplex *, int, float, float, int,
                                    int, int *, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cusolverEigRange_t,
      cublasFillMode_t, int, const cuDoubleComplex *, int, double, double, int,
      int, int *, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  float *W;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    float *, int, float, float, int, int, int *,
                                    float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevdx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  double *W;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    double *, int, double, double, int, int,
                                    int *, double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevdx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cusolverEigRange_t, cublasFillMode_t, int,
                                    cuComplex *, int, float, float, int, int,
                                    int *, float *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevdx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cusolverEigRange_t,
      cublasFillMode_t, int, cuDoubleComplex *, int, double, double, int, int,
      int *, double *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevdx");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, range, uplo, n, A, lda, vl,
                                    vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, const float *, int,
      const float *, int, float, float, int, int, int *, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, range, uplo, n, A, lda,
                                    B, ldb, vl, vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cusolverEigRange_t,
                                    cublasFillMode_t, int, const double *, int,
                                    const double *, int, double, double, int,
                                    int, int *, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, range, uplo, n, A, lda,
                                    B, ldb, vl, vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cusolverEigRange_t,
                                    cublasFillMode_t, int, const cuComplex *,
                                    int, const cuComplex *, int, float, float,
                                    int, int, int *, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, range, uplo, n, A, lda,
                                    B, ldb, vl, vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, double, double, int, int, int *,
      const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvdx_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, range, uplo, n, A, lda,
                                    B, ldb, vl, vu, il, iu, &meig, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *B;
  int ldb;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  float *W;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, float *, int, float *, int,
      float, float, int, int, int *, float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvdx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, itype, jobz, range, uplo, n, A, lda, B, ldb,
                          vl, vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *B;
  int ldb;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  double *W;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, double *, int, double *, int,
      double, double, int, int, int *, double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvdx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, itype, jobz, range, uplo, n, A, lda, B, ldb,
                          vl, vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *B;
  int ldb;
  float vl;
  float vu;
  int il;
  int iu;
  int meig{};
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, cuComplex *, int, cuComplex *,
      int, float, float, int, int, int *, float *, cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(float)) < 0 ||
      rpc_read(conn, &vu, sizeof(float)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvdx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, itype, jobz, range, uplo, n, A, lda, B, ldb,
                          vl, vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *B;
  int ldb;
  double vl;
  double vu;
  int il;
  int iu;
  int meig{};
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int, cuDoubleComplex *, int,
      cuDoubleComplex *, int, double, double, int, int, int *, double *,
      cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &vl, sizeof(double)) < 0 ||
      rpc_read(conn, &vu, sizeof(double)) < 0 ||
      rpc_read(conn, &il, sizeof(int)) < 0 ||
      rpc_read(conn, &iu, sizeof(int)) < 0 ||
      rpc_read(conn, &meig, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvdx");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, itype, jobz, range, uplo, n, A, lda, B, ldb,
                          vl, vu, il, iu, &meig, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const float *, int, const float *, int,
                                    const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const double *, int, const double *, int,
                                    const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const float *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const cuComplex *, int, const cuComplex *,
                                    int, const float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const double *W;
  int lwork{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cublasFillMode_t, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvd_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *B;
  int ldb;
  float *W;
  float *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                           cusolverEigMode_t, cublasFillMode_t, int, float *,
                           int, float *, int, float *, float *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *B;
  int ldb;
  double *W;
  double *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                           cusolverEigMode_t, cublasFillMode_t, int, double *,
                           int, double *, int, double *, double *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *B;
  int ldb;
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    cuComplex *, int, cuComplex *, int, float *,
                                    cuComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *B;
  int ldb;
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cublasFillMode_t, int, cuDoubleComplex *, int, cuDoubleComplex *, int,
      double *, cuDoubleComplex *, int, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvd");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnXsygvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *d_A;
  int64_t lda;
  cudaDataType dataTypeB;
  const void *d_B;
  int64_t ldb;
  cudaDataType dataTypeW;
  const void *d_W;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigType_t,
      cusolverEigMode_t, cublasFillMode_t, int64_t, cudaDataType, const void *,
      int64_t, cudaDataType, const void *, int64_t, cudaDataType, const void *,
      cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsygvd_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, params, itype, jobz, uplo, n, dataTypeA, d_A,
                          lda, dataTypeB, d_B, ldb, dataTypeW, d_W, computeType,
                          &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnXsygvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *d_A;
  int64_t lda;
  cudaDataType dataTypeB;
  void *d_B;
  int64_t ldb;
  cudaDataType dataTypeW;
  void *d_W;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigType_t,
      cusolverEigMode_t, cublasFillMode_t, int64_t, cudaDataType, void *,
      int64_t, cudaDataType, void *, int64_t, cudaDataType, void *,
      cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_W, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsygvd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, itype, jobz, uplo, n, dataTypeA, d_A,
                         lda, dataTypeB, d_B, ldb, dataTypeW, d_W, computeType,
                         bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnXsygvdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *d_A;
  int64_t lda;
  cudaDataType dataTypeB;
  const void *d_B;
  int64_t ldb;
  void *vl = nullptr;
  size_t vl_size;
  uint64_t vl_count = 0;
  void *vu = nullptr;
  size_t vu_size;
  uint64_t vu_count = 0;
  int64_t il;
  int64_t iu;
  int64_t meig{};
  cudaDataType dataTypeW;
  const void *d_W;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigType_t,
      cusolverEigMode_t, cublasFillMode_t, int64_t, cudaDataType, const void *,
      int64_t, cudaDataType, const void *, int64_t, void *, void *, int64_t,
      int64_t, int64_t *, cudaDataType, const void *, cudaDataType, size_t *,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_B, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &vl_count, sizeof(vl_count)) < 0 || false)
    goto ERROR_0;
  vl_size = vl_count;
  vl = (void *)malloc(vl_size);
  if (vl_size != 0 && vl == nullptr)
    goto ERROR_0;
  if ((vl_size != 0 && rpc_read(conn, vl, vl_size) < 0) ||
      rpc_read(conn, &vu_count, sizeof(vu_count)) < 0 || false)
    goto ERROR_0;
  vu_size = vu_count;
  vu = (void *)malloc(vu_size);
  if (vu_size != 0 && vu == nullptr)
    goto ERROR_0;
  if ((vu_size != 0 && rpc_read(conn, vu, vu_size) < 0) ||
      rpc_read(conn, &il, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &iu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &meig, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsygvdx_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, itype, jobz, uplo, n, dataTypeA, d_A, lda,
               dataTypeB, d_B, ldb, (vl_count == 0 ? nullptr : vl),
               (vu_count == 0 ? nullptr : vu), il, iu, &meig, dataTypeW, d_W,
               computeType, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)vu);
  free((void *)vl);
  return 0;
ERROR_0:
  free((void *)vu);
  free((void *)vl);
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12100
int handle_cusolverDnXsygvdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *d_A;
  int64_t lda;
  cudaDataType dataTypeB;
  void *d_B;
  int64_t ldb;
  void *vl = nullptr;
  size_t vl_size;
  uint64_t vl_count = 0;
  void *vu = nullptr;
  size_t vu_size;
  uint64_t vu_count = 0;
  int64_t il;
  int64_t iu;
  int64_t meig{};
  cudaDataType dataTypeW;
  void *d_W;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigType_t,
      cusolverEigMode_t, cusolverEigRange_t, cublasFillMode_t, int64_t,
      cudaDataType, void *, int64_t, cudaDataType, void *, int64_t, void *,
      void *, int64_t, int64_t, int64_t *, cudaDataType, void *, cudaDataType,
      void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &vl_count, sizeof(vl_count)) < 0 || false)
    goto ERROR_0;
  vl_size = vl_count;
  vl = (void *)malloc(vl_size);
  if (vl_size != 0 && vl == nullptr)
    goto ERROR_0;
  if ((vl_size != 0 && rpc_read(conn, vl, vl_size) < 0) ||
      rpc_read(conn, &vu_count, sizeof(vu_count)) < 0 || false)
    goto ERROR_0;
  vu_size = vu_count;
  vu = (void *)malloc(vu_size);
  if (vu_size != 0 && vu == nullptr)
    goto ERROR_0;
  if ((vu_size != 0 && rpc_read(conn, vu, vu_size) < 0) ||
      rpc_read(conn, &il, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &iu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &meig, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &d_W, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsygvdx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, itype, jobz, range, uplo, n, dataTypeA, d_A, lda,
               dataTypeB, d_B, ldb, (vl_count == 0 ? nullptr : vl),
               (vu_count == 0 ? nullptr : vu), il, iu, &meig, dataTypeW, d_W,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig, sizeof(int64_t)) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  free((void *)vu);
  free((void *)vl);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  free((void *)vu);
  free((void *)vl);
  return -1;
}

#endif

int handle_cusolverDnCreateSyevjInfo(conn_t *conn) {
  syevjInfo_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(syevjInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCreateSyevjInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDestroySyevjInfo(conn_t *conn) {
  syevjInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDestroySyevjInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevjSetTolerance(conn_t *conn) {
  syevjInfo_t info;
  double tolerance;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(syevjInfo_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &tolerance, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevjSetTolerance");
  return_value = fn == nullptr ? function_not_found() : fn(info, tolerance);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevjSetMaxSweeps(conn_t *conn) {
  syevjInfo_t info;
  int max_sweeps;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &max_sweeps, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevjSetMaxSweeps");
  return_value = fn == nullptr ? function_not_found() : fn(info, max_sweeps);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevjSetSortEig(conn_t *conn) {
  syevjInfo_t info;
  int sort_eig;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &sort_eig, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevjSetSortEig");
  return_value = fn == nullptr ? function_not_found() : fn(info, sort_eig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevjGetResidual(conn_t *conn) {
  cusolverDnHandle_t handle;
  syevjInfo_t info;
  double residual{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, syevjInfo_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &residual, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevjGetResidual");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, &residual);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &residual, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevjGetSweeps(conn_t *conn) {
  cusolverDnHandle_t handle;
  syevjInfo_t info;
  int executed_sweeps{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, syevjInfo_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &executed_sweeps, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevjGetSweeps");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, &executed_sweeps);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &executed_sweeps, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const float *, int,
                                    const float *, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork,
                                    params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const double *, int,
                                    const double *, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork,
                                    params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int,
      const cuComplex *, int, const float *, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork,
                                    params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int,
      const cuDoubleComplex *, int, const double *, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, &lwork,
                                    params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *W;
  float *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int, float *,
      int, float *, float *, int, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *W;
  double *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int, double *,
      int, double *, double *, int, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int, cuComplex *,
      int, float *, cuComplex *, int, int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, cuDoubleComplex *,
                                    int, double *, cuDoubleComplex *, int,
                                    int *, syevjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const float *, int,
                                    const float *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevj_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const double *, int,
                                    const double *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevj_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, const cuComplex *,
                                    int, const float *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevj_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int,
      const cuDoubleComplex *, int, const double *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevj_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, jobz, uplo, n, A, lda, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsyevj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *W;
  float *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, float *, int,
                                    float *, float *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsyevj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsyevj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *W;
  double *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int, double *,
      int, double *, double *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsyevj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCheevj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, cublasFillMode_t, int, cuComplex *,
      int, float *, cuComplex *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCheevj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZheevj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t,
                                    cublasFillMode_t, int, cuDoubleComplex *,
                                    int, double *, cuDoubleComplex *, int,
                                    int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZheevj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, uplo, n, A, lda, W, work,
                                    lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const float *A;
  int lda;
  const float *B;
  int ldb;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const float *, int, const float *, int,
                                    const float *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const double *A;
  int lda;
  const double *B;
  int ldb;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const double *, int, const double *, int,
                                    const double *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuComplex *A;
  int lda;
  const cuComplex *B;
  int ldb;
  const float *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    const cuComplex *, int, const cuComplex *,
                                    int, const float *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const cuDoubleComplex *B;
  int ldb;
  const double *W;
  int lwork{};
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cublasFillMode_t, int, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, const double *, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSsygvj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  float *A;
  int lda;
  float *B;
  int ldb;
  float *W;
  float *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    float *, int, float *, int, float *,
                                    float *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSsygvj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDsygvj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  double *A;
  int lda;
  double *B;
  int ldb;
  double *W;
  double *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    double *, int, double *, int, double *,
                                    double *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDsygvj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnChegvj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuComplex *A;
  int lda;
  cuComplex *B;
  int ldb;
  float *W;
  cuComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigType_t,
                                    cusolverEigMode_t, cublasFillMode_t, int,
                                    cuComplex *, int, cuComplex *, int, float *,
                                    cuComplex *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(float *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnChegvj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZhegvj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigType_t itype;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int n;
  cuDoubleComplex *A;
  int lda;
  cuDoubleComplex *B;
  int ldb;
  double *W;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  syevjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigType_t, cusolverEigMode_t,
      cublasFillMode_t, int, cuDoubleComplex *, int, cuDoubleComplex *, int,
      double *, cuDoubleComplex *, int, int *, syevjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &itype, sizeof(cusolverEigType_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &B, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int)) < 0 ||
      rpc_read(conn, &W, sizeof(double *)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(syevjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZhegvj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, itype, jobz, uplo, n, A, lda, B,
                                    ldb, W, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCreateGesvdjInfo(conn_t *conn) {
  gesvdjInfo_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(gesvdjInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCreateGesvdjInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDestroyGesvdjInfo(conn_t *conn) {
  gesvdjInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDestroyGesvdjInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdjSetTolerance(conn_t *conn) {
  gesvdjInfo_t info;
  double tolerance;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(gesvdjInfo_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &tolerance, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdjSetTolerance");
  return_value = fn == nullptr ? function_not_found() : fn(info, tolerance);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdjSetMaxSweeps(conn_t *conn) {
  gesvdjInfo_t info;
  int max_sweeps;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &max_sweeps, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdjSetMaxSweeps");
  return_value = fn == nullptr ? function_not_found() : fn(info, max_sweeps);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdjSetSortEig(conn_t *conn) {
  gesvdjInfo_t info;
  int sort_svd;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &sort_svd, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdjSetSortEig");
  return_value = fn == nullptr ? function_not_found() : fn(info, sort_svd);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdjGetResidual(conn_t *conn) {
  cusolverDnHandle_t handle;
  gesvdjInfo_t info;
  double residual{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, gesvdjInfo_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &residual, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdjGetResidual");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, &residual);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &residual, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdjGetSweeps(conn_t *conn) {
  cusolverDnHandle_t handle;
  gesvdjInfo_t info;
  int executed_sweeps{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, gesvdjInfo_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &executed_sweeps, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdjGetSweeps");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, &executed_sweeps);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &executed_sweeps, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  const float *A;
  int lda;
  const float *S;
  const float *U;
  int ldu;
  const float *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int, int,
                           const float *, int, const float *, const float *,
                           int, const float *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &U, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, &lwork, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvdjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  const double *A;
  int lda;
  const double *S;
  const double *U;
  int ldu;
  const double *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int, int,
                           const double *, int, const double *, const double *,
                           int, const double *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &U, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, &lwork, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvdjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  const cuComplex *A;
  int lda;
  const float *S;
  const cuComplex *U;
  int ldu;
  const cuComplex *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int,
                                    int, const cuComplex *, int, const float *,
                                    const cuComplex *, int, const cuComplex *,
                                    int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &U, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, &lwork, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvdjBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *S;
  const cuDoubleComplex *U;
  int ldu;
  const cuDoubleComplex *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, const cuDoubleComplex *,
      int, const double *, const cuDoubleComplex *, int,
      const cuDoubleComplex *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &U, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdjBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, &lwork, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  float *A;
  int lda;
  float *S;
  float *U;
  int ldu;
  float *V;
  int ldv;
  float *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, float *, int, float *,
      float *, int, float *, int, float *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, work, lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvdjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  double *A;
  int lda;
  double *S;
  double *U;
  int ldu;
  double *V;
  int ldv;
  double *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, double *, int, double *,
      double *, int, double *, int, double *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, work, lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvdjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  cuComplex *A;
  int lda;
  float *S;
  cuComplex *U;
  int ldu;
  cuComplex *V;
  int ldv;
  cuComplex *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int,
                                    int, cuComplex *, int, float *, cuComplex *,
                                    int, cuComplex *, int, cuComplex *, int,
                                    int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, work, lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvdjBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *S;
  cuDoubleComplex *U;
  int ldu;
  cuDoubleComplex *V;
  int ldv;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, cuDoubleComplex *, int,
      double *, cuDoubleComplex *, int, cuDoubleComplex *, int,
      cuDoubleComplex *, int, int *, gesvdjInfo_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdjBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, m, n, A, lda, S, U, ldu, V,
                                    ldv, work, lwork, info, params, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  const float *A;
  int lda;
  const float *S;
  const float *U;
  int ldu;
  const float *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
                           const float *, int, const float *, const float *,
                           int, const float *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &U, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvdj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  const double *A;
  int lda;
  const double *S;
  const double *U;
  int ldu;
  const double *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
                           const double *, int, const double *, const double *,
                           int, const double *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &U, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvdj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  const cuComplex *A;
  int lda;
  const float *S;
  const cuComplex *U;
  int ldu;
  const cuComplex *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const cuComplex *,
      int, const float *, const cuComplex *, int, const cuComplex *, int, int *,
      gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &U, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvdj_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  const cuDoubleComplex *A;
  int lda;
  const double *S;
  const cuDoubleComplex *U;
  int ldu;
  const cuDoubleComplex *V;
  int ldv;
  int lwork{};
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
      const cuDoubleComplex *, int, const double *, const cuDoubleComplex *,
      int, const cuDoubleComplex *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &U, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdj_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, &lwork, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  float *A;
  int lda;
  float *S;
  float *U;
  int ldu;
  float *V;
  int ldv;
  float *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, float *, int,
      float *, float *, int, float *, int, float *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvdj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  double *A;
  int lda;
  double *S;
  double *U;
  int ldu;
  double *V;
  int ldv;
  double *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
                           double *, int, double *, double *, int, double *,
                           int, double *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvdj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  cuComplex *A;
  int lda;
  float *S;
  cuComplex *U;
  int ldu;
  cuComplex *V;
  int ldv;
  cuComplex *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverEigMode_t, int,
                                    int, int, cuComplex *, int, float *,
                                    cuComplex *, int, cuComplex *, int,
                                    cuComplex *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(float *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvdj(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int econ;
  int m;
  int n;
  cuDoubleComplex *A;
  int lda;
  double *S;
  cuDoubleComplex *U;
  int ldu;
  cuDoubleComplex *V;
  int ldv;
  cuDoubleComplex *work;
  int lwork;
  int *info;
  gesvdjInfo_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, cuDoubleComplex *,
      int, double *, cuDoubleComplex *, int, cuDoubleComplex *, int,
      cuDoubleComplex *, int, int *, gesvdjInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &A, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &S, sizeof(double *)) < 0 ||
      rpc_read(conn, &U, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &V, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &params, sizeof(gesvdjInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdj");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, econ, m, n, A, lda, S, U, ldu,
                                    V, ldv, work, lwork, info, params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdaStridedBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const float *d_A;
  int lda;
  long long int strideA;
  const float *d_S;
  long long int strideS;
  const float *d_U;
  int ldu;
  long long int strideU;
  const float *d_V;
  int ldv;
  long long int strideV;
  int lwork{};
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const float *, int,
      long long int, const float *, long long int, const float *, int,
      long long int, const float *, int, long long int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(const float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdaStridedBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, rank, m, n, d_A, lda, strideA,
                                    d_S, strideS, d_U, ldu, strideU, d_V, ldv,
                                    strideV, &lwork, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDgesvdaStridedBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const double *d_A;
  int lda;
  long long int strideA;
  const double *d_S;
  long long int strideS;
  const double *d_U;
  int ldu;
  long long int strideU;
  const double *d_V;
  int ldv;
  long long int strideV;
  int lwork{};
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const double *, int,
      long long int, const double *, long long int, const double *, int,
      long long int, const double *, int, long long int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(const double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdaStridedBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, rank, m, n, d_A, lda, strideA,
                                    d_S, strideS, d_U, ldu, strideU, d_V, ldv,
                                    strideV, &lwork, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnCgesvdaStridedBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const cuComplex *d_A;
  int lda;
  long long int strideA;
  const float *d_S;
  long long int strideS;
  const cuComplex *d_U;
  int ldu;
  long long int strideU;
  const cuComplex *d_V;
  int ldv;
  long long int strideV;
  int lwork{};
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const cuComplex *,
      int, long long int, const float *, long long int, const cuComplex *, int,
      long long int, const cuComplex *, int, long long int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(const float *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdaStridedBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, rank, m, n, d_A, lda, strideA,
                                    d_S, strideS, d_U, ldu, strideU, d_V, ldv,
                                    strideV, &lwork, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnZgesvdaStridedBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const cuDoubleComplex *d_A;
  int lda;
  long long int strideA;
  const double *d_S;
  long long int strideS;
  const cuDoubleComplex *d_U;
  int ldu;
  long long int strideU;
  const cuDoubleComplex *d_V;
  int ldv;
  long long int strideV;
  int lwork{};
  int batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
      const cuDoubleComplex *, int, long long int, const double *,
      long long int, const cuDoubleComplex *, int, long long int,
      const cuDoubleComplex *, int, long long int, int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(const double *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdaStridedBatched_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, jobz, rank, m, n, d_A, lda, strideA,
                                    d_S, strideS, d_U, ldu, strideU, d_V, ldv,
                                    strideV, &lwork, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &lwork, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSgesvdaStridedBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const float *d_A;
  int lda;
  long long int strideA;
  float *d_S;
  long long int strideS;
  float *d_U;
  int ldu;
  long long int strideU;
  float *d_V;
  int ldv;
  long long int strideV;
  float *d_work;
  int lwork;
  int *d_info;
  int batchSize;
  double *h_R_nrmF = nullptr;
  size_t h_R_nrmF_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const float *, int,
      long long int, float *, long long int, float *, int, long long int,
      float *, int, long long int, float *, int, int *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const float *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(float *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(float *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_work, sizeof(float *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || batchSize < 0 || false)
    goto ERROR_0;
  h_R_nrmF_size = batchSize * sizeof(double);
  h_R_nrmF = (double *)malloc(h_R_nrmF_size);
  if (h_R_nrmF_size != 0 && h_R_nrmF == nullptr)
    goto ERROR_0;
  if ((h_R_nrmF_size != 0 && rpc_read(conn, h_R_nrmF, h_R_nrmF_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSgesvdaStridedBatched");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, rank, m, n, d_A, lda, strideA, d_S, strideS, d_U,
               ldu, strideU, d_V, ldv, strideV, d_work, lwork, d_info,
               (batchSize * sizeof(double) == 0 ? nullptr : h_R_nrmF),
               batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, h_R_nrmF, batchSize * sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)h_R_nrmF);
  return 0;
ERROR_0:
  free((void *)h_R_nrmF);
  return -1;
}

int handle_cusolverDnDgesvdaStridedBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const double *d_A;
  int lda;
  long long int strideA;
  double *d_S;
  long long int strideS;
  double *d_U;
  int ldu;
  long long int strideU;
  double *d_V;
  int ldv;
  long long int strideV;
  double *d_work;
  int lwork;
  int *d_info;
  int batchSize;
  double *h_R_nrmF = nullptr;
  size_t h_R_nrmF_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const double *, int,
      long long int, double *, long long int, double *, int, long long int,
      double *, int, long long int, double *, int, int *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const double *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(double *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_work, sizeof(double *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || batchSize < 0 || false)
    goto ERROR_0;
  h_R_nrmF_size = batchSize * sizeof(double);
  h_R_nrmF = (double *)malloc(h_R_nrmF_size);
  if (h_R_nrmF_size != 0 && h_R_nrmF == nullptr)
    goto ERROR_0;
  if ((h_R_nrmF_size != 0 && rpc_read(conn, h_R_nrmF, h_R_nrmF_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDgesvdaStridedBatched");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, rank, m, n, d_A, lda, strideA, d_S, strideS, d_U,
               ldu, strideU, d_V, ldv, strideV, d_work, lwork, d_info,
               (batchSize * sizeof(double) == 0 ? nullptr : h_R_nrmF),
               batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, h_R_nrmF, batchSize * sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)h_R_nrmF);
  return 0;
ERROR_0:
  free((void *)h_R_nrmF);
  return -1;
}

int handle_cusolverDnCgesvdaStridedBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const cuComplex *d_A;
  int lda;
  long long int strideA;
  float *d_S;
  long long int strideS;
  cuComplex *d_U;
  int ldu;
  long long int strideU;
  cuComplex *d_V;
  int ldv;
  long long int strideV;
  cuComplex *d_work;
  int lwork;
  int *d_info;
  int batchSize;
  double *h_R_nrmF = nullptr;
  size_t h_R_nrmF_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int, const cuComplex *,
      int, long long int, float *, long long int, cuComplex *, int,
      long long int, cuComplex *, int, long long int, cuComplex *, int, int *,
      double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(float *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_work, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || batchSize < 0 || false)
    goto ERROR_0;
  h_R_nrmF_size = batchSize * sizeof(double);
  h_R_nrmF = (double *)malloc(h_R_nrmF_size);
  if (h_R_nrmF_size != 0 && h_R_nrmF == nullptr)
    goto ERROR_0;
  if ((h_R_nrmF_size != 0 && rpc_read(conn, h_R_nrmF, h_R_nrmF_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCgesvdaStridedBatched");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, rank, m, n, d_A, lda, strideA, d_S, strideS, d_U,
               ldu, strideU, d_V, ldv, strideV, d_work, lwork, d_info,
               (batchSize * sizeof(double) == 0 ? nullptr : h_R_nrmF),
               batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, h_R_nrmF, batchSize * sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)h_R_nrmF);
  return 0;
ERROR_0:
  free((void *)h_R_nrmF);
  return -1;
}

int handle_cusolverDnZgesvdaStridedBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverEigMode_t jobz;
  int rank;
  int m;
  int n;
  const cuDoubleComplex *d_A;
  int lda;
  long long int strideA;
  double *d_S;
  long long int strideS;
  cuDoubleComplex *d_U;
  int ldu;
  long long int strideU;
  cuDoubleComplex *d_V;
  int ldv;
  long long int strideV;
  cuDoubleComplex *d_work;
  int lwork;
  int *d_info;
  int batchSize;
  double *h_R_nrmF = nullptr;
  size_t h_R_nrmF_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverEigMode_t, int, int, int,
      const cuDoubleComplex *, int, long long int, double *, long long int,
      cuDoubleComplex *, int, long long int, cuDoubleComplex *, int,
      long long int, cuDoubleComplex *, int, int *, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &rank, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &d_A, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int)) < 0 ||
      rpc_read(conn, &strideA, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_S, sizeof(double *)) < 0 ||
      rpc_read(conn, &strideS, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_U, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int)) < 0 ||
      rpc_read(conn, &strideU, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_V, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int)) < 0 ||
      rpc_read(conn, &strideV, sizeof(long long int)) < 0 ||
      rpc_read(conn, &d_work, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &lwork, sizeof(int)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 || batchSize < 0 || false)
    goto ERROR_0;
  h_R_nrmF_size = batchSize * sizeof(double);
  h_R_nrmF = (double *)malloc(h_R_nrmF_size);
  if (h_R_nrmF_size != 0 && h_R_nrmF == nullptr)
    goto ERROR_0;
  if ((h_R_nrmF_size != 0 && rpc_read(conn, h_R_nrmF, h_R_nrmF_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnZgesvdaStridedBatched");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, jobz, rank, m, n, d_A, lda, strideA, d_S, strideS, d_U,
               ldu, strideU, d_V, ldv, strideV, d_work, lwork, d_info,
               (batchSize * sizeof(double) == 0 ? nullptr : h_R_nrmF),
               batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, h_R_nrmF, batchSize * sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)h_R_nrmF);
  return 0;
ERROR_0:
  free((void *)h_R_nrmF);
  return -1;
}

int handle_cusolverDnCreateParams(conn_t *conn) {
  cusolverDnParams_t params{};
  params = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnParams_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnCreateParams");
  return_value = fn == nullptr ? function_not_found() : fn(&params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnDestroyParams(conn_t *conn) {
  cusolverDnParams_t params;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnParams_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnDestroyParams");
  return_value = fn == nullptr ? function_not_found() : fn(params);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnSetAdvOptions(conn_t *conn) {
  cusolverDnParams_t params;
  cusolverDnFunction_t function;
  cusolverAlgMode_t algo;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnParams_t, cusolverDnFunction_t,
                                    cusolverAlgMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &function, sizeof(cusolverDnFunction_t)) < 0 ||
      rpc_read(conn, &algo, sizeof(cusolverAlgMode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnSetAdvOptions");
  return_value =
      fn == nullptr ? function_not_found() : fn(params, function, algo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXpotrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cublasFillMode_t, int64_t,
      cudaDataType, const void *, int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXpotrf_bufferSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, params, uplo, n, dataTypeA, A, lda,
                                    computeType, &workspaceInBytesOnDevice,
                                    &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXpotrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnParams_t,
                                    cublasFillMode_t, int64_t, cudaDataType,
                                    void *, int64_t, cudaDataType, void *,
                                    size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXpotrf");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, uplo, n, dataTypeA, A, lda,
                         computeType, bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnXpotrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasFillMode_t uplo;
  int64_t n;
  int64_t nrhs;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeB;
  void *B;
  int64_t ldb;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnParams_t,
                                    cublasFillMode_t, int64_t, int64_t,
                                    cudaDataType, const void *, int64_t,
                                    cudaDataType, void *, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXpotrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, params, uplo, n, nrhs, dataTypeA, A,
                                    lda, dataTypeB, B, ldb, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgeqrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeTau;
  const void *tau;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, int64_t, int64_t, cudaDataType,
      const void *, int64_t, cudaDataType, const void *, cudaDataType, size_t *,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeTau, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &tau, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgeqrf_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, m, n, dataTypeA, A, lda, dataTypeTau, tau,
               computeType, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgeqrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeTau;
  void *tau;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnParams_t,
                                    int64_t, int64_t, cudaDataType, void *,
                                    int64_t, cudaDataType, void *, cudaDataType,
                                    void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeTau, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &tau, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgeqrf");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, m, n, dataTypeA, A, lda, dataTypeTau, tau,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnXgetrf_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, int64_t, int64_t, cudaDataType,
      const void *, int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgetrf_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, params, m, n, dataTypeA, A, lda, computeType,
                          &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgetrf(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  int64_t *ipiv;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverDnHandle_t, cusolverDnParams_t, int64_t,
                           int64_t, cudaDataType, void *, int64_t, int64_t *,
                           cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(int64_t *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgetrf");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, m, n, dataTypeA, A, lda, ipiv,
                         computeType, bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnXgetrs(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasOperation_t trans;
  int64_t n;
  int64_t nrhs;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  const int64_t *ipiv;
  cudaDataType dataTypeB;
  void *B;
  int64_t ldb;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cublasOperation_t, int64_t,
      int64_t, cudaDataType, const void *, int64_t, const int64_t *,
      cudaDataType, void *, int64_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &trans, sizeof(cublasOperation_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &ipiv, sizeof(const int64_t *)) < 0 ||
      rpc_read(conn, &dataTypeB, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &B, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldb, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgetrs");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, params, trans, n, nrhs, dataTypeA,
                                    A, lda, ipiv, dataTypeB, B, ldb, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  const void *W;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cublasFillMode_t, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevd_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, uplo, n, dataTypeA, A, lda, dataTypeW, W,
               computeType, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXsyevd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  void *W;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cublasFillMode_t, int64_t, cudaDataType, void *, int64_t, cudaDataType,
      void *, cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevd");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, uplo, n, dataTypeA, A, lda, dataTypeW, W,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#if CUSOLVER_VERSION >= 12200
int handle_cusolverDnXstedc_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigComp_t compz;
  int64_t n;
  cudaDataType dataTypeDE;
  const void *D;
  const void *E;
  cudaDataType dataTypeZ;
  const void *Z;
  int64_t ldz;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigComp_t, int64_t,
      cudaDataType, const void *, const void *, cudaDataType, const void *,
      int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &compz, sizeof(cusolverEigComp_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeDE, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &D, sizeof(const void *)) < 0 ||
      rpc_read(conn, &E, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeZ, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Z, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXstedc_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, compz, n, dataTypeDE, D, E, dataTypeZ, Z, ldz,
               computeType, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12200
int handle_cusolverDnXstedc(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigComp_t compz;
  int64_t n;
  cudaDataType dataTypeDE;
  void *D;
  void *E;
  cudaDataType dataTypeZ;
  void *Z;
  int64_t ldz;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigComp_t, int64_t,
      cudaDataType, void *, void *, cudaDataType, void *, int64_t, cudaDataType,
      void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &compz, sizeof(cusolverEigComp_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeDE, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &D, sizeof(void *)) < 0 ||
      rpc_read(conn, &E, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataTypeZ, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Z, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldz, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXstedc");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, compz, n, dataTypeDE, D, E, dataTypeZ, Z, ldz,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 11701
int handle_cusolverDnXsyevBatched_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  const void *W;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int64_t batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cublasFillMode_t, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, cudaDataType, size_t *, size_t *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevBatched_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, params, jobz, uplo, n, dataTypeA, A, lda,
                          dataTypeW, W, computeType, &workspaceInBytesOnDevice,
                          &workspaceInBytesOnHost, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 11701
int handle_cusolverDnXsyevBatched(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  void *W;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int64_t batchSize;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cublasFillMode_t, int64_t, cudaDataType, void *, int64_t, cudaDataType,
      void *, cudaDataType, void *, size_t, void *, size_t, int *, int64_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int64_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevBatched");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, uplo, n, dataTypeA, A, lda, dataTypeW, W,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info, batchSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

int handle_cusolverDnXsyevdx_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  void *vl = nullptr;
  size_t vl_size;
  uint64_t vl_count = 0;
  void *vu = nullptr;
  size_t vu_size;
  uint64_t vu_count = 0;
  int64_t il;
  int64_t iu;
  int64_t h_meig{};
  cudaDataType dataTypeW;
  const void *W;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int64_t, cudaDataType, const void *,
      int64_t, void *, void *, int64_t, int64_t, int64_t *, cudaDataType,
      const void *, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &vl_count, sizeof(vl_count)) < 0 || false)
    goto ERROR_0;
  vl_size = vl_count;
  vl = (void *)malloc(vl_size);
  if (vl_size != 0 && vl == nullptr)
    goto ERROR_0;
  if ((vl_size != 0 && rpc_read(conn, vl, vl_size) < 0) ||
      rpc_read(conn, &vu_count, sizeof(vu_count)) < 0 || false)
    goto ERROR_0;
  vu_size = vu_count;
  vu = (void *)malloc(vu_size);
  if (vu_size != 0 && vu == nullptr)
    goto ERROR_0;
  if ((vu_size != 0 && rpc_read(conn, vu, vu_size) < 0) ||
      rpc_read(conn, &il, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &iu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &h_meig, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevdx_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, range, uplo, n, dataTypeA, A, lda,
               (vl_count == 0 ? nullptr : vl), (vu_count == 0 ? nullptr : vu),
               il, iu, &h_meig, dataTypeW, W, computeType,
               &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &h_meig, sizeof(int64_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)vu);
  free((void *)vl);
  return 0;
ERROR_0:
  free((void *)vu);
  free((void *)vl);
  return -1;
}

int handle_cusolverDnXsyevdx(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  cusolverEigRange_t range;
  cublasFillMode_t uplo;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  void *vl = nullptr;
  size_t vl_size;
  uint64_t vl_count = 0;
  void *vu = nullptr;
  size_t vu_size;
  uint64_t vu_count = 0;
  int64_t il;
  int64_t iu;
  int64_t meig64{};
  cudaDataType dataTypeW;
  void *W;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cusolverEigRange_t, cublasFillMode_t, int64_t, cudaDataType, void *,
      int64_t, void *, void *, int64_t, int64_t, int64_t *, cudaDataType,
      void *, cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &range, sizeof(cusolverEigRange_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &vl_count, sizeof(vl_count)) < 0 || false)
    goto ERROR_0;
  vl_size = vl_count;
  vl = (void *)malloc(vl_size);
  if (vl_size != 0 && vl == nullptr)
    goto ERROR_0;
  if ((vl_size != 0 && rpc_read(conn, vl, vl_size) < 0) ||
      rpc_read(conn, &vu_count, sizeof(vu_count)) < 0 || false)
    goto ERROR_0;
  vu_size = vu_count;
  vu = (void *)malloc(vu_size);
  if (vu_size != 0 && vu == nullptr)
    goto ERROR_0;
  if ((vu_size != 0 && rpc_read(conn, vu, vu_size) < 0) ||
      rpc_read(conn, &il, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &iu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &meig64, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(void *)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXsyevdx");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, range, uplo, n, dataTypeA, A, lda,
               (vl_count == 0 ? nullptr : vl), (vu_count == 0 ? nullptr : vu),
               il, iu, &meig64, dataTypeW, W, computeType, bufferOnDevice,
               workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &meig64, sizeof(int64_t)) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  free((void *)vu);
  free((void *)vl);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  free((void *)vu);
  free((void *)vl);
  return -1;
}

#if CUSOLVER_VERSION >= 11701
int handle_cusolverDnXgeev_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobvl;
  cusolverEigMode_t jobvr;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  const void *W;
  cudaDataType dataTypeVL;
  const void *VL;
  int64_t ldvl;
  cudaDataType dataTypeVR;
  const void *VR;
  int64_t ldvr;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cusolverEigMode_t, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobvl, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &jobvr, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeVL, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VL, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldvl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVR, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VR, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldvr, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgeev_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobvl, jobvr, n, dataTypeA, A, lda, dataTypeW, W,
               dataTypeVL, VL, ldvl, dataTypeVR, VR, ldvr, computeType,
               &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 11701
int handle_cusolverDnXgeev(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobvl;
  cusolverEigMode_t jobvr;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeW;
  void *W;
  cudaDataType dataTypeVL;
  void *VL;
  int64_t ldvl;
  cudaDataType dataTypeVR;
  void *VR;
  int64_t ldvr;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t,
      cusolverEigMode_t, int64_t, cudaDataType, void *, int64_t, cudaDataType,
      void *, cudaDataType, void *, int64_t, cudaDataType, void *, int64_t,
      cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobvl, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &jobvr, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeW, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &W, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataTypeVL, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VL, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldvl, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVR, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VR, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldvr, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgeev");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobvl, jobvr, n, dataTypeA, A, lda, dataTypeW, W,
               dataTypeVL, VL, ldvl, dataTypeVR, VR, ldvr, computeType,
               bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

int handle_cusolverDnXgesvd_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  signed char jobu;
  signed char jobvt;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeS;
  const void *S;
  cudaDataType dataTypeU;
  const void *U;
  int64_t ldu;
  cudaDataType dataTypeVT;
  const void *VT;
  int64_t ldvt;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, signed char, signed char, int64_t,
      int64_t, cudaDataType, const void *, int64_t, cudaDataType, const void *,
      cudaDataType, const void *, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeS, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &S, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeU, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &U, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVT, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VT, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvd_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobu, jobvt, m, n, dataTypeA, A, lda, dataTypeS,
               S, dataTypeU, U, ldu, dataTypeVT, VT, ldvt, computeType,
               &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvd(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  signed char jobu;
  signed char jobvt;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeS;
  void *S;
  cudaDataType dataTypeU;
  void *U;
  int64_t ldu;
  cudaDataType dataTypeVT;
  void *VT;
  int64_t ldvt;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, signed char, signed char, int64_t,
      int64_t, cudaDataType, void *, int64_t, cudaDataType, void *,
      cudaDataType, void *, int64_t, cudaDataType, void *, int64_t,
      cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobvt, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeS, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &S, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataTypeU, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &U, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVT, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &VT, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldvt, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvd");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, jobu, jobvt, m, n, dataTypeA, A, lda,
                         dataTypeS, S, dataTypeU, U, ldu, dataTypeVT, VT, ldvt,
                         computeType, bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnXgesvdp_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  int econ;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeS;
  const void *S;
  cudaDataType dataTypeU;
  const void *U;
  int64_t ldu;
  cudaDataType dataTypeV;
  const void *V;
  int64_t ldv;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t, int, int64_t,
      int64_t, cudaDataType, const void *, int64_t, cudaDataType, const void *,
      cudaDataType, const void *, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeS, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &S, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeU, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &U, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeV, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &V, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdp_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobz, econ, m, n, dataTypeA, A, lda, dataTypeS,
               S, dataTypeU, U, ldu, dataTypeV, V, ldv, computeType,
               &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdp(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverEigMode_t jobz;
  int econ;
  int64_t m;
  int64_t n;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeS;
  void *S;
  cudaDataType dataTypeU;
  void *U;
  int64_t ldu;
  cudaDataType dataTypeV;
  void *V;
  int64_t ldv;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *d_info;
  double h_err_sigma{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverEigMode_t, int, int64_t,
      int64_t, cudaDataType, void *, int64_t, cudaDataType, void *,
      cudaDataType, void *, int64_t, cudaDataType, void *, int64_t,
      cudaDataType, void *, size_t, void *, size_t, int *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobz, sizeof(cusolverEigMode_t)) < 0 ||
      rpc_read(conn, &econ, sizeof(int)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeS, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &S, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataTypeU, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &U, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldu, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeV, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &V, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 ||
      rpc_read(conn, &h_err_sigma, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdp");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, jobz, econ, m, n, dataTypeA, A, lda,
                         dataTypeS, S, dataTypeU, U, ldu, dataTypeV, V, ldv,
                         computeType, bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, d_info, &h_err_sigma);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &h_err_sigma, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

int handle_cusolverDnXgesvdr_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  signed char jobu;
  signed char jobv;
  int64_t m;
  int64_t n;
  int64_t k;
  int64_t p;
  int64_t niters;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeSrand;
  const void *Srand;
  cudaDataType dataTypeUrand;
  const void *Urand;
  int64_t ldUrand;
  cudaDataType dataTypeVrand;
  const void *Vrand;
  int64_t ldVrand;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, signed char, signed char, int64_t,
      int64_t, int64_t, int64_t, int64_t, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, cudaDataType, const void *, int64_t,
      cudaDataType, const void *, int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobv, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &p, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &niters, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeSrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Srand, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeUrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Urand, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldUrand, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Vrand, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldVrand, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdr_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, jobu, jobv, m, n, k, p, niters, dataTypeA, A,
               lda, dataTypeSrand, Srand, dataTypeUrand, Urand, ldUrand,
               dataTypeVrand, Vrand, ldVrand, computeType,
               &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverDnXgesvdr(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  signed char jobu;
  signed char jobv;
  int64_t m;
  int64_t n;
  int64_t k;
  int64_t p;
  int64_t niters;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeSrand;
  void *Srand;
  cudaDataType dataTypeUrand;
  void *Urand;
  int64_t ldUrand;
  cudaDataType dataTypeVrand;
  void *Vrand;
  int64_t ldVrand;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, signed char, signed char, int64_t,
      int64_t, int64_t, int64_t, int64_t, cudaDataType, void *, int64_t,
      cudaDataType, void *, cudaDataType, void *, int64_t, cudaDataType, void *,
      int64_t, cudaDataType, void *, size_t, void *, size_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &jobu, sizeof(signed char)) < 0 ||
      rpc_read(conn, &jobv, sizeof(signed char)) < 0 ||
      rpc_read(conn, &m, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &p, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &niters, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeSrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Srand, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataTypeUrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Urand, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldUrand, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeVrand, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &Vrand, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldVrand, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXgesvdr");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, jobu, jobv, m, n, k, p, niters,
                         dataTypeA, A, lda, dataTypeSrand, Srand, dataTypeUrand,
                         Urand, ldUrand, dataTypeVrand, Vrand, ldVrand,
                         computeType, bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#if CUSOLVER_VERSION >= 11600
int handle_cusolverDnXlarft_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverDirectMode_t direct;
  cusolverStorevMode_t storev;
  int64_t n;
  int64_t k;
  cudaDataType dataTypeV;
  const void *V;
  int64_t ldv;
  cudaDataType dataTypeTau;
  const void *tau;
  cudaDataType dataTypeT;
  void *T;
  int64_t ldt;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverDirectMode_t,
      cusolverStorevMode_t, int64_t, int64_t, cudaDataType, const void *,
      int64_t, cudaDataType, const void *, cudaDataType, void *, int64_t,
      cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &direct, sizeof(cusolverDirectMode_t)) < 0 ||
      rpc_read(conn, &storev, sizeof(cusolverStorevMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeV, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &V, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeTau, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &tau, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeT, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &T, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldt, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXlarft_bufferSize");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, params, direct, storev, n, k, dataTypeV, V,
                          ldv, dataTypeTau, tau, dataTypeT, T, ldt, computeType,
                          &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 11600
int handle_cusolverDnXlarft(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cusolverDirectMode_t direct;
  cusolverStorevMode_t storev;
  int64_t n;
  int64_t k;
  cudaDataType dataTypeV;
  const void *V;
  int64_t ldv;
  cudaDataType dataTypeTau;
  const void *tau;
  cudaDataType dataTypeT;
  void *T;
  int64_t ldt;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cusolverDirectMode_t,
      cusolverStorevMode_t, int64_t, int64_t, cudaDataType, const void *,
      int64_t, cudaDataType, const void *, cudaDataType, void *, int64_t,
      cudaDataType, void *, size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &direct, sizeof(cusolverDirectMode_t)) < 0 ||
      rpc_read(conn, &storev, sizeof(cusolverStorevMode_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &k, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeV, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &V, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldv, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeTau, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &tau, sizeof(const void *)) < 0 ||
      rpc_read(conn, &dataTypeT, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &T, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldt, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXlarft");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, params, direct, storev, n, k, dataTypeV, V,
                         ldv, dataTypeTau, tau, dataTypeT, T, ldt, computeType,
                         bufferOnDevice, workspaceInBytesOnDevice,
                         (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
                         workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12200
int handle_cusolverDnXpolar_bufferSize(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasFillMode_t uplo;
  int64_t M;
  int64_t N;
  cudaDataType dataTypeA;
  const void *A;
  int64_t lda;
  cudaDataType dataTypeH;
  const void *H;
  int64_t ldh;
  cudaDataType computeType;
  size_t workspaceInBytesOnDevice{};
  size_t workspaceInBytesOnHost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cublasFillMode_t, int64_t,
      int64_t, cudaDataType, const void *, int64_t, cudaDataType, const void *,
      int64_t, cudaDataType, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &M, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(const void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeH, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &H, sizeof(const void *)) < 0 ||
      rpc_read(conn, &ldh, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXpolar_bufferSize");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, uplo, M, N, dataTypeA, A, lda, dataTypeH, H, ldh,
               computeType, &workspaceInBytesOnDevice, &workspaceInBytesOnHost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUSOLVER_VERSION >= 12200
int handle_cusolverDnXpolar(conn_t *conn) {
  cusolverDnHandle_t handle;
  cusolverDnParams_t params;
  cublasFillMode_t uplo;
  int64_t M;
  int64_t N;
  cudaDataType dataTypeA;
  void *A;
  int64_t lda;
  cudaDataType dataTypeH;
  void *H;
  int64_t ldh;
  cudaDataType computeType;
  void *bufferOnDevice;
  size_t workspaceInBytesOnDevice;
  size_t workspaceInBytesOnHost;
  void *bufferOnHost = nullptr;
  size_t bufferOnHost_size;
  double *d_res_nrm;
  double *d_A_nrmF;
  double *d_rcond;
  int *d_info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverDnHandle_t, cusolverDnParams_t, cublasFillMode_t, int64_t,
      int64_t, cudaDataType, void *, int64_t, cudaDataType, void *, int64_t,
      cudaDataType, void *, size_t, void *, size_t, double *, double *,
      double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverDnHandle_t)) < 0 ||
      rpc_read(conn, &params, sizeof(cusolverDnParams_t)) < 0 ||
      rpc_read(conn, &uplo, sizeof(cublasFillMode_t)) < 0 ||
      rpc_read(conn, &M, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &N, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeA, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &A, sizeof(void *)) < 0 ||
      rpc_read(conn, &lda, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &dataTypeH, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &H, sizeof(void *)) < 0 ||
      rpc_read(conn, &ldh, sizeof(int64_t)) < 0 ||
      rpc_read(conn, &computeType, sizeof(cudaDataType)) < 0 ||
      rpc_read(conn, &bufferOnDevice, sizeof(void *)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnDevice, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytesOnHost, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  bufferOnHost_size = workspaceInBytesOnHost;
  bufferOnHost = (void *)malloc(bufferOnHost_size);
  if (bufferOnHost_size != 0 && bufferOnHost == nullptr)
    goto ERROR_0;
  if ((bufferOnHost_size != 0 &&
       rpc_read(conn, bufferOnHost, bufferOnHost_size) < 0) ||
      rpc_read(conn, &d_res_nrm, sizeof(double *)) < 0 ||
      rpc_read(conn, &d_A_nrmF, sizeof(double *)) < 0 ||
      rpc_read(conn, &d_rcond, sizeof(double *)) < 0 ||
      rpc_read(conn, &d_info, sizeof(int *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverDnXpolar");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, params, uplo, M, N, dataTypeA, A, lda, dataTypeH, H, ldh,
               computeType, bufferOnDevice, workspaceInBytesOnDevice,
               (workspaceInBytesOnHost == 0 ? nullptr : bufferOnHost),
               workspaceInBytesOnHost, d_res_nrm, d_A_nrmF, d_rcond, d_info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, bufferOnHost, workspaceInBytesOnHost) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)bufferOnHost);
  return 0;
ERROR_0:
  free((void *)bufferOnHost);
  return -1;
}

#endif

int handle_cusolverSpCreate(conn_t *conn) {
  cusolverSpHandle_t handle{};
  handle = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroy(conn_t *conn) {
  cusolverSpHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpSetStream(conn_t *conn) {
  cusolverSpHandle_t handle;
  cudaStream_t streamId;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &streamId, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpGetStream(conn_t *conn) {
  cusolverSpHandle_t handle;
  cudaStream_t streamId{};
  streamId = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, cudaStream_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpGetStream");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrissymHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  int *csrEndPtrA = nullptr;
  size_t csrEndPtrA_size;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int issym{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 || m < 0 ||
      false)
    goto ERROR_0;
  csrRowPtrA_size = m * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  csrEndPtrA_size = m * sizeof(const int);
  csrEndPtrA = (int *)malloc(csrEndPtrA_size);
  if (csrEndPtrA_size != 0 && csrEndPtrA == nullptr)
    goto ERROR_0;
  if ((csrEndPtrA_size != 0 &&
       rpc_read(conn, csrEndPtrA, csrEndPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &issym, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrissymHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnzA, descrA,
               (m * sizeof(const int) == 0 ? nullptr : csrRowPtrA),
               (m * sizeof(const int) == 0 ? nullptr : csrEndPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), &issym);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &issym, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrEndPtrA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrEndPtrA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpScsrlsvluHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  float *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  float *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, float, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsvluHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(const float) == 0 ? nullptr : b), tol, reorder,
               (n * sizeof(float) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(float)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrlsvluHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  double *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  double *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, double, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsvluHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(const double) == 0 ? nullptr : b), tol, reorder,
               (n * sizeof(double) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(double)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrlsvluHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  cuComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int, cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsvluHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(const cuComplex) == 0 ? nullptr : b), tol, reorder,
               (n * sizeof(cuComplex) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrlsvluHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, double, int, cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsvluHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(const cuDoubleComplex) == 0 ? nullptr : b), tol,
               reorder, (n * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
               &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrlsvqr(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const float *b;
  float tol;
  int reorder;
  float *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, float, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const float *)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsvqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrlsvqr(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const double *b;
  double tol;
  int reorder;
  double *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, double, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const double *)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsvqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrlsvqr(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const cuComplex *b;
  float tol;
  int reorder;
  cuComplex *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int, cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsvqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrlsvqr(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const cuDoubleComplex *b;
  double tol;
  int reorder;
  cuDoubleComplex *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, double, int, cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsvqr");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrlsvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  float *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  float *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, float, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const float) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(float) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(float)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrlsvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  double *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  double *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, double, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const double) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(double) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(double)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrlsvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  cuComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int, cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const cuComplex) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(cuComplex) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrlsvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, double, int, cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const cuDoubleComplex) == 0 ? nullptr : b), tol,
               reorder, (m * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
               &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrlsvcholHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrVal = nullptr;
  size_t csrVal_size;
  int *csrRowPtr = nullptr;
  size_t csrRowPtr_size;
  uint64_t csrRowPtr_count = 0;
  int *csrColInd = nullptr;
  size_t csrColInd_size;
  float *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  float *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, float, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrVal_size = nnz * sizeof(const float);
  csrVal = (float *)malloc(csrVal_size);
  if (csrVal_size != 0 && csrVal == nullptr)
    goto ERROR_0;
  if ((csrVal_size != 0 && rpc_read(conn, csrVal, csrVal_size) < 0) ||
      rpc_read(conn, &csrRowPtr_count, sizeof(csrRowPtr_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtr_size = csrRowPtr_count * sizeof(const int);
  csrRowPtr = (int *)malloc(csrRowPtr_size);
  if (csrRowPtr_size != 0 && csrRowPtr == nullptr)
    goto ERROR_0;
  if ((csrRowPtr_size != 0 && rpc_read(conn, csrRowPtr, csrRowPtr_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColInd_size = nnz * sizeof(const int);
  csrColInd = (int *)malloc(csrColInd_size);
  if (csrColInd_size != 0 && csrColInd == nullptr)
    goto ERROR_0;
  if ((csrColInd_size != 0 && rpc_read(conn, csrColInd, csrColInd_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsvcholHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrVal),
               (csrRowPtr_count * sizeof(const int) == 0 ? nullptr : csrRowPtr),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColInd),
               (m * sizeof(const float) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(float) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(float)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return -1;
}

int handle_cusolverSpDcsrlsvcholHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrVal = nullptr;
  size_t csrVal_size;
  int *csrRowPtr = nullptr;
  size_t csrRowPtr_size;
  uint64_t csrRowPtr_count = 0;
  int *csrColInd = nullptr;
  size_t csrColInd_size;
  double *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  double *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, double, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrVal_size = nnz * sizeof(const double);
  csrVal = (double *)malloc(csrVal_size);
  if (csrVal_size != 0 && csrVal == nullptr)
    goto ERROR_0;
  if ((csrVal_size != 0 && rpc_read(conn, csrVal, csrVal_size) < 0) ||
      rpc_read(conn, &csrRowPtr_count, sizeof(csrRowPtr_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtr_size = csrRowPtr_count * sizeof(const int);
  csrRowPtr = (int *)malloc(csrRowPtr_size);
  if (csrRowPtr_size != 0 && csrRowPtr == nullptr)
    goto ERROR_0;
  if ((csrRowPtr_size != 0 && rpc_read(conn, csrRowPtr, csrRowPtr_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColInd_size = nnz * sizeof(const int);
  csrColInd = (int *)malloc(csrColInd_size);
  if (csrColInd_size != 0 && csrColInd == nullptr)
    goto ERROR_0;
  if ((csrColInd_size != 0 && rpc_read(conn, csrColInd, csrColInd_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsvcholHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrVal),
               (csrRowPtr_count * sizeof(const int) == 0 ? nullptr : csrRowPtr),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColInd),
               (m * sizeof(const double) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(double) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(double)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return -1;
}

int handle_cusolverSpCcsrlsvcholHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrVal = nullptr;
  size_t csrVal_size;
  int *csrRowPtr = nullptr;
  size_t csrRowPtr_size;
  uint64_t csrRowPtr_count = 0;
  int *csrColInd = nullptr;
  size_t csrColInd_size;
  cuComplex *b = nullptr;
  size_t b_size;
  float tol;
  int reorder;
  cuComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int, cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrVal_size = nnz * sizeof(const cuComplex);
  csrVal = (cuComplex *)malloc(csrVal_size);
  if (csrVal_size != 0 && csrVal == nullptr)
    goto ERROR_0;
  if ((csrVal_size != 0 && rpc_read(conn, csrVal, csrVal_size) < 0) ||
      rpc_read(conn, &csrRowPtr_count, sizeof(csrRowPtr_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtr_size = csrRowPtr_count * sizeof(const int);
  csrRowPtr = (int *)malloc(csrRowPtr_size);
  if (csrRowPtr_size != 0 && csrRowPtr == nullptr)
    goto ERROR_0;
  if ((csrRowPtr_size != 0 && rpc_read(conn, csrRowPtr, csrRowPtr_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColInd_size = nnz * sizeof(const int);
  csrColInd = (int *)malloc(csrColInd_size);
  if (csrColInd_size != 0 && csrColInd == nullptr)
    goto ERROR_0;
  if ((csrColInd_size != 0 && rpc_read(conn, csrColInd, csrColInd_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsvcholHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrVal),
               (csrRowPtr_count * sizeof(const int) == 0 ? nullptr : csrRowPtr),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColInd),
               (m * sizeof(const cuComplex) == 0 ? nullptr : b), tol, reorder,
               (m * sizeof(cuComplex) == 0 ? nullptr : x), &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return -1;
}

int handle_cusolverSpZcsrlsvcholHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrVal = nullptr;
  size_t csrVal_size;
  int *csrRowPtr = nullptr;
  size_t csrRowPtr_size;
  uint64_t csrRowPtr_count = 0;
  int *csrColInd = nullptr;
  size_t csrColInd_size;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  double tol;
  int reorder;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, double, int, cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrVal_size = nnz * sizeof(const cuDoubleComplex);
  csrVal = (cuDoubleComplex *)malloc(csrVal_size);
  if (csrVal_size != 0 && csrVal == nullptr)
    goto ERROR_0;
  if ((csrVal_size != 0 && rpc_read(conn, csrVal, csrVal_size) < 0) ||
      rpc_read(conn, &csrRowPtr_count, sizeof(csrRowPtr_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtr_size = csrRowPtr_count * sizeof(const int);
  csrRowPtr = (int *)malloc(csrRowPtr_size);
  if (csrRowPtr_size != 0 && csrRowPtr == nullptr)
    goto ERROR_0;
  if ((csrRowPtr_size != 0 && rpc_read(conn, csrRowPtr, csrRowPtr_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColInd_size = nnz * sizeof(const int);
  csrColInd = (int *)malloc(csrColInd_size);
  if (csrColInd_size != 0 && csrColInd == nullptr)
    goto ERROR_0;
  if ((csrColInd_size != 0 && rpc_read(conn, csrColInd, csrColInd_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsvcholHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrVal),
               (csrRowPtr_count * sizeof(const int) == 0 ? nullptr : csrRowPtr),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColInd),
               (m * sizeof(const cuDoubleComplex) == 0 ? nullptr : b), tol,
               reorder, (m * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
               &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, m * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)b);
  free((void *)csrColInd);
  free((void *)csrRowPtr);
  free((void *)csrVal);
  return -1;
}

int handle_cusolverSpScsrlsvchol(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const float *b;
  float tol;
  int reorder;
  float *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, const float *, float, int, float *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const float *)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsvchol");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrlsvchol(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const double *b;
  double tol;
  int reorder;
  double *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, const double *, double, int, double *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const double *)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsvchol");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrlsvchol(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const cuComplex *b;
  float tol;
  int reorder;
  cuComplex *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int, cuComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsvchol");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrlsvchol(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  const cuDoubleComplex *b;
  double tol;
  int reorder;
  cuDoubleComplex *x;
  int singularity{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, double, int, cuDoubleComplex *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &reorder, sizeof(int)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &singularity, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsvchol");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, nnz, descrA, csrVal, csrRowPtr, csrColInd,
                          b, tol, reorder, x, &singularity);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &singularity, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrlsqvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  float *b = nullptr;
  size_t b_size;
  float tol;
  int rankA{};
  float *x = nullptr;
  size_t x_size;
  int *p = nullptr;
  size_t p_size;
  float min_norm{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    const int *, const int *, const float *,
                                    float, int *, float *, int *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &rankA, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) ||
      rpc_read(conn, &min_norm, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrlsqvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const float) == 0 ? nullptr : b), tol, &rankA,
               (n * sizeof(float) == 0 ? nullptr : x),
               (n * sizeof(int) == 0 ? nullptr : p), &min_norm);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rankA, sizeof(int)) < 0 ||
      rpc_write(conn, x, n * sizeof(float)) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &min_norm, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrlsqvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  double *b = nullptr;
  size_t b_size;
  double tol;
  int rankA{};
  double *x = nullptr;
  size_t x_size;
  int *p = nullptr;
  size_t p_size;
  double min_norm{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, const double *,
                                    double, int *, double *, int *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &rankA, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) ||
      rpc_read(conn, &min_norm, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrlsqvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const double) == 0 ? nullptr : b), tol, &rankA,
               (n * sizeof(double) == 0 ? nullptr : x),
               (n * sizeof(int) == 0 ? nullptr : p), &min_norm);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rankA, sizeof(int)) < 0 ||
      rpc_write(conn, x, n * sizeof(double)) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &min_norm, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrlsqvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex *b = nullptr;
  size_t b_size;
  float tol;
  int rankA{};
  cuComplex *x = nullptr;
  size_t x_size;
  int *p = nullptr;
  size_t p_size;
  float min_norm{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    float, int *, cuComplex *, int *, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &rankA, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) ||
      rpc_read(conn, &min_norm, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrlsqvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const cuComplex) == 0 ? nullptr : b), tol, &rankA,
               (n * sizeof(cuComplex) == 0 ? nullptr : x),
               (n * sizeof(int) == 0 ? nullptr : p), &min_norm);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rankA, sizeof(int)) < 0 ||
      rpc_write(conn, x, n * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &min_norm, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrlsqvqrHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  double tol;
  int rankA{};
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  int *p = nullptr;
  size_t p_size;
  double min_norm{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                           const cusparseMatDescr_t, const cuDoubleComplex *,
                           const int *, const int *, const cuDoubleComplex *,
                           double, int *, cuDoubleComplex *, int *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  b_size = m * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &rankA, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) ||
      rpc_read(conn, &min_norm, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrlsqvqrHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const cuDoubleComplex) == 0 ? nullptr : b), tol,
               &rankA, (n * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
               (n * sizeof(int) == 0 ? nullptr : p), &min_norm);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &rankA, sizeof(int)) < 0 ||
      rpc_write(conn, x, n * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &min_norm, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)x);
  free((void *)b);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsreigvsiHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  float mu0;
  float *x0 = nullptr;
  size_t x0_size;
  int maxite;
  float tol;
  float mu{};
  float *x = nullptr;
  size_t x_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, float, const float *, int, float, float *,
      float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu0, sizeof(float)) < 0 || m < 0 || false)
    goto ERROR_0;
  x0_size = m * sizeof(const float);
  x0 = (float *)malloc(x0_size);
  if (x0_size != 0 && x0 == nullptr)
    goto ERROR_0;
  if ((x0_size != 0 && rpc_read(conn, x0, x0_size) < 0) ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &mu, sizeof(float)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsreigvsiHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA), mu0,
               (m * sizeof(const float) == 0 ? nullptr : x0), maxite, tol, &mu,
               (m * sizeof(float) == 0 ? nullptr : x));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mu, sizeof(float)) < 0 ||
      rpc_write(conn, x, m * sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsreigvsiHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  double mu0;
  double *x0 = nullptr;
  size_t x0_size;
  int maxite;
  double tol;
  double mu{};
  double *x = nullptr;
  size_t x_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, double, const double *, int, double, double *,
      double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu0, sizeof(double)) < 0 || m < 0 || false)
    goto ERROR_0;
  x0_size = m * sizeof(const double);
  x0 = (double *)malloc(x0_size);
  if (x0_size != 0 && x0 == nullptr)
    goto ERROR_0;
  if ((x0_size != 0 && rpc_read(conn, x0, x0_size) < 0) ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &mu, sizeof(double)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsreigvsiHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA), mu0,
               (m * sizeof(const double) == 0 ? nullptr : x0), maxite, tol, &mu,
               (m * sizeof(double) == 0 ? nullptr : x));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mu, sizeof(double)) < 0 ||
      rpc_write(conn, x, m * sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsreigvsiHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex mu0;
  cuComplex *x0 = nullptr;
  size_t x0_size;
  int maxite;
  float tol;
  cuComplex mu{};
  cuComplex *x = nullptr;
  size_t x_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, cuComplex, const cuComplex *, int, float,
      cuComplex *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu0, sizeof(cuComplex)) < 0 || m < 0 || false)
    goto ERROR_0;
  x0_size = m * sizeof(const cuComplex);
  x0 = (cuComplex *)malloc(x0_size);
  if (x0_size != 0 && x0 == nullptr)
    goto ERROR_0;
  if ((x0_size != 0 && rpc_read(conn, x0, x0_size) < 0) ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuComplex)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsreigvsiHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA), mu0,
               (m * sizeof(const cuComplex) == 0 ? nullptr : x0), maxite, tol,
               &mu, (m * sizeof(cuComplex) == 0 ? nullptr : x));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mu, sizeof(cuComplex)) < 0 ||
      rpc_write(conn, x, m * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsreigvsiHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex mu0;
  cuDoubleComplex *x0 = nullptr;
  size_t x0_size;
  int maxite;
  double tol;
  cuDoubleComplex mu{};
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, cuDoubleComplex,
      const cuDoubleComplex *, int, double, cuDoubleComplex *,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu0, sizeof(cuDoubleComplex)) < 0 || m < 0 || false)
    goto ERROR_0;
  x0_size = m * sizeof(const cuDoubleComplex);
  x0 = (cuDoubleComplex *)malloc(x0_size);
  if (x0_size != 0 && x0 == nullptr)
    goto ERROR_0;
  if ((x0_size != 0 && rpc_read(conn, x0, x0_size) < 0) ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuDoubleComplex)) < 0 || m < 0 || false)
    goto ERROR_0;
  x_size = m * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsreigvsiHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA), mu0,
               (m * sizeof(const cuDoubleComplex) == 0 ? nullptr : x0), maxite,
               tol, &mu, (m * sizeof(cuDoubleComplex) == 0 ? nullptr : x));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mu, sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, x, m * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)x);
  free((void *)x0);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsreigvsi(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  float mu0;
  const float *x0;
  int maxite;
  float eps;
  float *mu;
  float *x;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, float, const float *, int, float, float *,
      float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu0, sizeof(float)) < 0 ||
      rpc_read(conn, &x0, sizeof(const float *)) < 0 ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &eps, sizeof(float)) < 0 ||
      rpc_read(conn, &mu, sizeof(float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsreigvsi");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrValA, csrRowPtrA,
                                    csrColIndA, mu0, x0, maxite, eps, mu, x);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsreigvsi(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  double mu0;
  const double *x0;
  int maxite;
  double eps;
  double *mu;
  double *x;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, double, const double *, int, double, double *,
      double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu0, sizeof(double)) < 0 ||
      rpc_read(conn, &x0, sizeof(const double *)) < 0 ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &eps, sizeof(double)) < 0 ||
      rpc_read(conn, &mu, sizeof(double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsreigvsi");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrValA, csrRowPtrA,
                                    csrColIndA, mu0, x0, maxite, eps, mu, x);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsreigvsi(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  cuComplex mu0;
  const cuComplex *x0;
  int maxite;
  float eps;
  cuComplex *mu;
  cuComplex *x;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, cuComplex, const cuComplex *, int, float,
      cuComplex *, cuComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu0, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &x0, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &eps, sizeof(float)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsreigvsi");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrValA, csrRowPtrA,
                                    csrColIndA, mu0, x0, maxite, eps, mu, x);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsreigvsi(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  cuDoubleComplex mu0;
  const cuDoubleComplex *x0;
  int maxite;
  double eps;
  cuDoubleComplex *mu;
  cuDoubleComplex *x;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, cuDoubleComplex,
      const cuDoubleComplex *, int, double, cuDoubleComplex *,
      cuDoubleComplex *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu0, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &x0, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &maxite, sizeof(int)) < 0 ||
      rpc_read(conn, &eps, sizeof(double)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsreigvsi");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, nnz, descrA, csrValA, csrRowPtrA,
                                    csrColIndA, mu0, x0, maxite, eps, mu, x);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsreigsHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex left_bottom_corner;
  cuComplex right_upper_corner;
  int num_eigs{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, cuComplex, cuComplex, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &left_bottom_corner, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &right_upper_corner, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &num_eigs, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsreigsHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               left_bottom_corner, right_upper_corner, &num_eigs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_eigs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsreigsHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex left_bottom_corner;
  cuDoubleComplex right_upper_corner;
  int num_eigs{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, cuDoubleComplex, cuDoubleComplex, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &left_bottom_corner, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &right_upper_corner, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &num_eigs, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsreigsHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               left_bottom_corner, right_upper_corner, &num_eigs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_eigs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsreigsHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex left_bottom_corner;
  cuComplex right_upper_corner;
  int num_eigs{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, cuComplex, cuComplex, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &left_bottom_corner, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &right_upper_corner, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &num_eigs, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsreigsHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               left_bottom_corner, right_upper_corner, &num_eigs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_eigs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsreigsHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex left_bottom_corner;
  cuDoubleComplex right_upper_corner;
  int num_eigs{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, cuDoubleComplex,
      cuDoubleComplex, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &left_bottom_corner, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &right_upper_corner, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &num_eigs, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsreigsHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               left_bottom_corner, right_upper_corner, &num_eigs);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &num_eigs, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpXcsrsymrcmHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *p = nullptr;
  size_t p_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrsymrcmHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : p));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpXcsrsymmdqHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *p = nullptr;
  size_t p_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrsymmdqHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : p));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpXcsrsymamdHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *p = nullptr;
  size_t p_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrsymamdHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : p));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpXcsrmetisndHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int64_t *options = nullptr;
  size_t options_size;
  uint64_t options_count = 0;
  int *p = nullptr;
  size_t p_size;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, const int64_t *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &options_count, sizeof(options_count)) < 0 || false)
    goto ERROR_0;
  options_size = options_count * sizeof(const int64_t);
  options = (int64_t *)malloc(options_size);
  if (options_size != 0 && options == nullptr)
    goto ERROR_0;
  if ((options_size != 0 && rpc_read(conn, options, options_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  p_size = n * sizeof(int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrmetisndHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (options_count * sizeof(const int64_t) == 0 ? nullptr : options),
               (n * sizeof(int) == 0 ? nullptr : p));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, p, n * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)p);
  free((void *)options);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)p);
  free((void *)options);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpScsrzfdHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *P = nullptr;
  size_t P_size;
  int numnz{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    const int *, const int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  P_size = n * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &numnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrzfdHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnz, descrA,
               (nnz * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : P), &numnz);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, n * sizeof(int)) < 0 ||
      rpc_write(conn, &numnz, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrzfdHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *P = nullptr;
  size_t P_size;
  int numnz{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  P_size = n * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &numnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrzfdHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnz, descrA,
               (nnz * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : P), &numnz);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, n * sizeof(int)) < 0 ||
      rpc_write(conn, &numnz, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrzfdHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *P = nullptr;
  size_t P_size;
  int numnz{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  P_size = n * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &numnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrzfdHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnz, descrA,
               (nnz * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : P), &numnz);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, n * sizeof(int)) < 0 ||
      rpc_write(conn, &numnz, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrzfdHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *P = nullptr;
  size_t P_size;
  int numnz{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int *, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnz < 0 || false)
    goto ERROR_0;
  csrValA_size = nnz * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnz < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnz * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  P_size = n * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &numnz, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrzfdHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnz, descrA,
               (nnz * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnz * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (n * sizeof(int) == 0 ? nullptr : P), &numnz);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, n * sizeof(int)) < 0 ||
      rpc_write(conn, &numnz, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)P);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpXcsrperm_bufferSizeHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *p = nullptr;
  size_t p_size;
  int *q = nullptr;
  size_t q_size;
  size_t bufferSizeInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t, const int *,
      const int *, const int *, const int *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  p_size = m * sizeof(const int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || n < 0 || false)
    goto ERROR_0;
  q_size = n * sizeof(const int);
  q = (int *)malloc(q_size);
  if (q_size != 0 && q == nullptr)
    goto ERROR_0;
  if ((q_size != 0 && rpc_read(conn, q, q_size) < 0) ||
      rpc_read(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrperm_bufferSizeHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const int) == 0 ? nullptr : p),
               (n * sizeof(const int) == 0 ? nullptr : q), &bufferSizeInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &bufferSizeInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)q);
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)q);
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpXcsrpermHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  int *p = nullptr;
  size_t p_size;
  int *q = nullptr;
  size_t q_size;
  int *map = nullptr;
  size_t map_size;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, int *, int *,
                                    const int *, const int *, int *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      m < 0 || false)
    goto ERROR_0;
  p_size = m * sizeof(const int);
  p = (int *)malloc(p_size);
  if (p_size != 0 && p == nullptr)
    goto ERROR_0;
  if ((p_size != 0 && rpc_read(conn, p, p_size) < 0) || n < 0 || false)
    goto ERROR_0;
  q_size = n * sizeof(const int);
  q = (int *)malloc(q_size);
  if (q_size != 0 && q == nullptr)
    goto ERROR_0;
  if ((q_size != 0 && rpc_read(conn, q, q_size) < 0) || nnzA < 0 || false)
    goto ERROR_0;
  map_size = nnzA * sizeof(int);
  map = (int *)malloc(map_size);
  if (map_size != 0 && map == nullptr)
    goto ERROR_0;
  if ((map_size != 0 && rpc_read(conn, map, map_size) < 0) ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrpermHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(int) == 0 ? nullptr : csrRowPtrA),
               (nnzA * sizeof(int) == 0 ? nullptr : csrColIndA),
               (m * sizeof(const int) == 0 ? nullptr : p),
               (n * sizeof(const int) == 0 ? nullptr : q),
               (nnzA * sizeof(int) == 0 ? nullptr : map),
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, csrRowPtrA, csrRowPtrA_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndA, nnzA * sizeof(int)) < 0 ||
      rpc_write(conn, map, nnzA * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)map);
  free((void *)q);
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)map);
  free((void *)q);
  free((void *)p);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpCreateCsrqrInfo(conn_t *conn) {
  csrqrInfo_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrqrInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreateCsrqrInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroyCsrqrInfo(conn_t *conn) {
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroyCsrqrInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrqrAnalysisBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrqrAnalysisBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrRowPtrA,
                                    csrColIndA, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrBufferInfoBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  int batchSize;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                           const cusparseMatDescr_t, const float *, const int *,
                           const int *, int, csrqrInfo_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrBufferInfoBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, batchSize, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrBufferInfoBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  int batchSize;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, int, csrqrInfo_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrBufferInfoBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, batchSize, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrBufferInfoBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  int batchSize;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, int, csrqrInfo_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrBufferInfoBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, batchSize, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrBufferInfoBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrVal;
  const int *csrRowPtr;
  const int *csrColInd;
  int batchSize;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, int, csrqrInfo_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrVal, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtr, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColInd, sizeof(const int *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrBufferInfoBatched");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnz, descrA, csrVal,
                                    csrRowPtr, csrColInd, batchSize, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrsvBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const float *b;
  float *x;
  int batchSize;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const float *,
                                    const int *, const int *, const float *,
                                    float *, int, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrsvBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, descrA, csrValA, csrRowPtrA,
                          csrColIndA, b, x, batchSize, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrsvBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const double *b;
  double *x;
  int batchSize;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, const double *,
                                    double *, int, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrsvBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, descrA, csrValA, csrRowPtrA,
                          csrColIndA, b, x, batchSize, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrsvBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const cuComplex *b;
  cuComplex *x;
  int batchSize;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, const cuComplex *,
                                    cuComplex *, int, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrsvBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, descrA, csrValA, csrRowPtrA,
                          csrColIndA, b, x, batchSize, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrsvBatched(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnz;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  const cuDoubleComplex *b;
  cuDoubleComplex *x;
  int batchSize;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *,
      const cuDoubleComplex *, cuDoubleComplex *, int, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnz, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrsvBatched");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnz, descrA, csrValA, csrRowPtrA,
                          csrColIndA, b, x, batchSize, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCreateCsrluInfoHost(conn_t *conn) {
  csrluInfoHost_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrluInfoHost_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreateCsrluInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroyCsrluInfoHost(conn_t *conn) {
  csrluInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrluInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroyCsrluInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrluAnalysisHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrluInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrluAnalysisHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpScsrluBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrluInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrluBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrluBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrluInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrluBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrluBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrluInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrluBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrluBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrluInfoHost_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrluBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrluFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  float pivot_threshold;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrluInfoHost_t, float, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pivot_threshold, sizeof(float)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrluFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               pivot_threshold, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrluFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  double pivot_threshold;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrluInfoHost_t, double, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pivot_threshold, sizeof(double)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrluFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               pivot_threshold, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrluFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  float pivot_threshold;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrluInfoHost_t, float, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pivot_threshold, sizeof(float)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrluFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               pivot_threshold, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrluFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrluInfoHost_t info;
  double pivot_threshold;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrluInfoHost_t,
      double, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pivot_threshold, sizeof(double)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrluFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               pivot_threshold, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrluZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrluInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrluInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrluZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrluZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrluInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrluInfoHost_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrluZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrluZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrluInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrluInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrluZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrluZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrluInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrluInfoHost_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrluZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrluSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  float *b = nullptr;
  size_t b_size;
  float *x = nullptr;
  size_t x_size;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const float *,
                                    float *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrluSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const float) == 0 ? nullptr : b),
               (n * sizeof(float) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(float)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpDcsrluSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  double *b = nullptr;
  size_t b_size;
  double *x = nullptr;
  size_t x_size;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const double *,
                                    double *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrluSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const double) == 0 ? nullptr : b),
               (n * sizeof(double) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(double)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpCcsrluSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  cuComplex *b = nullptr;
  size_t b_size;
  cuComplex *x = nullptr;
  size_t x_size;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuComplex *,
                                    cuComplex *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrluSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const cuComplex) == 0 ? nullptr : b),
               (n * sizeof(cuComplex) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpZcsrluSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuDoubleComplex *,
                           cuDoubleComplex *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrluSolveHost");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n,
                         (n * sizeof(const cuDoubleComplex) == 0 ? nullptr : b),
                         (n * sizeof(cuDoubleComplex) == 0 ? nullptr : x), info,
                         (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpXcsrluNnzHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int nnzLRef{};
  int nnzURef{};
  csrluInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int *, int *, csrluInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &nnzLRef, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzURef, sizeof(int)) < 0 ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrluNnzHost");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, &nnzLRef, &nnzURef, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nnzLRef, sizeof(int)) < 0 ||
      rpc_write(conn, &nnzURef, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrluExtractHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int *P = nullptr;
  size_t P_size;
  uint64_t P_count = 0;
  int *Q = nullptr;
  size_t Q_size;
  uint64_t Q_count = 0;
  cusparseMatDescr_t descrL;
  float *csrValL = nullptr;
  size_t csrValL_size;
  uint64_t csrValL_count = 0;
  int *csrRowPtrL = nullptr;
  size_t csrRowPtrL_size;
  uint64_t csrRowPtrL_count = 0;
  int *csrColIndL = nullptr;
  size_t csrColIndL_size;
  uint64_t csrColIndL_count = 0;
  cusparseMatDescr_t descrU;
  float *csrValU = nullptr;
  size_t csrValU_size;
  uint64_t csrValU_count = 0;
  int *csrRowPtrU = nullptr;
  size_t csrRowPtrU_size;
  uint64_t csrRowPtrU_count = 0;
  int *csrColIndU = nullptr;
  size_t csrColIndU_size;
  uint64_t csrColIndU_count = 0;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int *, int *,
                                    const cusparseMatDescr_t, float *, int *,
                                    int *, const cusparseMatDescr_t, float *,
                                    int *, int *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &P_count, sizeof(P_count)) < 0 || false)
    goto ERROR_0;
  P_size = P_count * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &Q_count, sizeof(Q_count)) < 0 || false)
    goto ERROR_0;
  Q_size = Q_count * sizeof(int);
  Q = (int *)malloc(Q_size);
  if (Q_size != 0 && Q == nullptr)
    goto ERROR_0;
  if ((Q_size != 0 && rpc_read(conn, Q, Q_size) < 0) ||
      rpc_read(conn, &descrL, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValL_count, sizeof(csrValL_count)) < 0 || false)
    goto ERROR_0;
  csrValL_size = csrValL_count * sizeof(float);
  csrValL = (float *)malloc(csrValL_size);
  if (csrValL_size != 0 && csrValL == nullptr)
    goto ERROR_0;
  if ((csrValL_size != 0 && rpc_read(conn, csrValL, csrValL_size) < 0) ||
      rpc_read(conn, &csrRowPtrL_count, sizeof(csrRowPtrL_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrL_size = csrRowPtrL_count * sizeof(int);
  csrRowPtrL = (int *)malloc(csrRowPtrL_size);
  if (csrRowPtrL_size != 0 && csrRowPtrL == nullptr)
    goto ERROR_0;
  if ((csrRowPtrL_size != 0 &&
       rpc_read(conn, csrRowPtrL, csrRowPtrL_size) < 0) ||
      rpc_read(conn, &csrColIndL_count, sizeof(csrColIndL_count)) < 0 || false)
    goto ERROR_0;
  csrColIndL_size = csrColIndL_count * sizeof(int);
  csrColIndL = (int *)malloc(csrColIndL_size);
  if (csrColIndL_size != 0 && csrColIndL == nullptr)
    goto ERROR_0;
  if ((csrColIndL_size != 0 &&
       rpc_read(conn, csrColIndL, csrColIndL_size) < 0) ||
      rpc_read(conn, &descrU, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValU_count, sizeof(csrValU_count)) < 0 || false)
    goto ERROR_0;
  csrValU_size = csrValU_count * sizeof(float);
  csrValU = (float *)malloc(csrValU_size);
  if (csrValU_size != 0 && csrValU == nullptr)
    goto ERROR_0;
  if ((csrValU_size != 0 && rpc_read(conn, csrValU, csrValU_size) < 0) ||
      rpc_read(conn, &csrRowPtrU_count, sizeof(csrRowPtrU_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrU_size = csrRowPtrU_count * sizeof(int);
  csrRowPtrU = (int *)malloc(csrRowPtrU_size);
  if (csrRowPtrU_size != 0 && csrRowPtrU == nullptr)
    goto ERROR_0;
  if ((csrRowPtrU_size != 0 &&
       rpc_read(conn, csrRowPtrU, csrRowPtrU_size) < 0) ||
      rpc_read(conn, &csrColIndU_count, sizeof(csrColIndU_count)) < 0 || false)
    goto ERROR_0;
  csrColIndU_size = csrColIndU_count * sizeof(int);
  csrColIndU = (int *)malloc(csrColIndU_size);
  if (csrColIndU_size != 0 && csrColIndU == nullptr)
    goto ERROR_0;
  if ((csrColIndU_size != 0 &&
       rpc_read(conn, csrColIndU, csrColIndU_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrluExtractHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, (P_count * sizeof(int) == 0 ? nullptr : P),
               (Q_count * sizeof(int) == 0 ? nullptr : Q), descrL,
               (csrValL_count * sizeof(float) == 0 ? nullptr : csrValL),
               (csrRowPtrL_count * sizeof(int) == 0 ? nullptr : csrRowPtrL),
               (csrColIndL_count * sizeof(int) == 0 ? nullptr : csrColIndL),
               descrU, (csrValU_count * sizeof(float) == 0 ? nullptr : csrValU),
               (csrRowPtrU_count * sizeof(int) == 0 ? nullptr : csrRowPtrU),
               (csrColIndU_count * sizeof(int) == 0 ? nullptr : csrColIndU),
               info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, P_count * sizeof(int)) < 0 ||
      rpc_write(conn, Q, Q_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValL, csrValL_count * sizeof(float)) < 0 ||
      rpc_write(conn, csrRowPtrL, csrRowPtrL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndL, csrColIndL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValU, csrValU_count * sizeof(float)) < 0 ||
      rpc_write(conn, csrRowPtrU, csrRowPtrU_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndU, csrColIndU_count * sizeof(int)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return -1;
}

int handle_cusolverSpDcsrluExtractHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int *P = nullptr;
  size_t P_size;
  uint64_t P_count = 0;
  int *Q = nullptr;
  size_t Q_size;
  uint64_t Q_count = 0;
  cusparseMatDescr_t descrL;
  double *csrValL = nullptr;
  size_t csrValL_size;
  uint64_t csrValL_count = 0;
  int *csrRowPtrL = nullptr;
  size_t csrRowPtrL_size;
  uint64_t csrRowPtrL_count = 0;
  int *csrColIndL = nullptr;
  size_t csrColIndL_size;
  uint64_t csrColIndL_count = 0;
  cusparseMatDescr_t descrU;
  double *csrValU = nullptr;
  size_t csrValU_size;
  uint64_t csrValU_count = 0;
  int *csrRowPtrU = nullptr;
  size_t csrRowPtrU_size;
  uint64_t csrRowPtrU_count = 0;
  int *csrColIndU = nullptr;
  size_t csrColIndU_size;
  uint64_t csrColIndU_count = 0;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int *, int *,
                                    const cusparseMatDescr_t, double *, int *,
                                    int *, const cusparseMatDescr_t, double *,
                                    int *, int *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &P_count, sizeof(P_count)) < 0 || false)
    goto ERROR_0;
  P_size = P_count * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &Q_count, sizeof(Q_count)) < 0 || false)
    goto ERROR_0;
  Q_size = Q_count * sizeof(int);
  Q = (int *)malloc(Q_size);
  if (Q_size != 0 && Q == nullptr)
    goto ERROR_0;
  if ((Q_size != 0 && rpc_read(conn, Q, Q_size) < 0) ||
      rpc_read(conn, &descrL, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValL_count, sizeof(csrValL_count)) < 0 || false)
    goto ERROR_0;
  csrValL_size = csrValL_count * sizeof(double);
  csrValL = (double *)malloc(csrValL_size);
  if (csrValL_size != 0 && csrValL == nullptr)
    goto ERROR_0;
  if ((csrValL_size != 0 && rpc_read(conn, csrValL, csrValL_size) < 0) ||
      rpc_read(conn, &csrRowPtrL_count, sizeof(csrRowPtrL_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrL_size = csrRowPtrL_count * sizeof(int);
  csrRowPtrL = (int *)malloc(csrRowPtrL_size);
  if (csrRowPtrL_size != 0 && csrRowPtrL == nullptr)
    goto ERROR_0;
  if ((csrRowPtrL_size != 0 &&
       rpc_read(conn, csrRowPtrL, csrRowPtrL_size) < 0) ||
      rpc_read(conn, &csrColIndL_count, sizeof(csrColIndL_count)) < 0 || false)
    goto ERROR_0;
  csrColIndL_size = csrColIndL_count * sizeof(int);
  csrColIndL = (int *)malloc(csrColIndL_size);
  if (csrColIndL_size != 0 && csrColIndL == nullptr)
    goto ERROR_0;
  if ((csrColIndL_size != 0 &&
       rpc_read(conn, csrColIndL, csrColIndL_size) < 0) ||
      rpc_read(conn, &descrU, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValU_count, sizeof(csrValU_count)) < 0 || false)
    goto ERROR_0;
  csrValU_size = csrValU_count * sizeof(double);
  csrValU = (double *)malloc(csrValU_size);
  if (csrValU_size != 0 && csrValU == nullptr)
    goto ERROR_0;
  if ((csrValU_size != 0 && rpc_read(conn, csrValU, csrValU_size) < 0) ||
      rpc_read(conn, &csrRowPtrU_count, sizeof(csrRowPtrU_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrU_size = csrRowPtrU_count * sizeof(int);
  csrRowPtrU = (int *)malloc(csrRowPtrU_size);
  if (csrRowPtrU_size != 0 && csrRowPtrU == nullptr)
    goto ERROR_0;
  if ((csrRowPtrU_size != 0 &&
       rpc_read(conn, csrRowPtrU, csrRowPtrU_size) < 0) ||
      rpc_read(conn, &csrColIndU_count, sizeof(csrColIndU_count)) < 0 || false)
    goto ERROR_0;
  csrColIndU_size = csrColIndU_count * sizeof(int);
  csrColIndU = (int *)malloc(csrColIndU_size);
  if (csrColIndU_size != 0 && csrColIndU == nullptr)
    goto ERROR_0;
  if ((csrColIndU_size != 0 &&
       rpc_read(conn, csrColIndU, csrColIndU_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrluExtractHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, (P_count * sizeof(int) == 0 ? nullptr : P),
               (Q_count * sizeof(int) == 0 ? nullptr : Q), descrL,
               (csrValL_count * sizeof(double) == 0 ? nullptr : csrValL),
               (csrRowPtrL_count * sizeof(int) == 0 ? nullptr : csrRowPtrL),
               (csrColIndL_count * sizeof(int) == 0 ? nullptr : csrColIndL),
               descrU,
               (csrValU_count * sizeof(double) == 0 ? nullptr : csrValU),
               (csrRowPtrU_count * sizeof(int) == 0 ? nullptr : csrRowPtrU),
               (csrColIndU_count * sizeof(int) == 0 ? nullptr : csrColIndU),
               info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, P_count * sizeof(int)) < 0 ||
      rpc_write(conn, Q, Q_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValL, csrValL_count * sizeof(double)) < 0 ||
      rpc_write(conn, csrRowPtrL, csrRowPtrL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndL, csrColIndL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValU, csrValU_count * sizeof(double)) < 0 ||
      rpc_write(conn, csrRowPtrU, csrRowPtrU_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndU, csrColIndU_count * sizeof(int)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return -1;
}

int handle_cusolverSpCcsrluExtractHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int *P = nullptr;
  size_t P_size;
  uint64_t P_count = 0;
  int *Q = nullptr;
  size_t Q_size;
  uint64_t Q_count = 0;
  cusparseMatDescr_t descrL;
  cuComplex *csrValL = nullptr;
  size_t csrValL_size;
  uint64_t csrValL_count = 0;
  int *csrRowPtrL = nullptr;
  size_t csrRowPtrL_size;
  uint64_t csrRowPtrL_count = 0;
  int *csrColIndL = nullptr;
  size_t csrColIndL_size;
  uint64_t csrColIndL_count = 0;
  cusparseMatDescr_t descrU;
  cuComplex *csrValU = nullptr;
  size_t csrValU_size;
  uint64_t csrValU_count = 0;
  int *csrRowPtrU = nullptr;
  size_t csrRowPtrU_size;
  uint64_t csrRowPtrU_count = 0;
  int *csrColIndU = nullptr;
  size_t csrColIndU_size;
  uint64_t csrColIndU_count = 0;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int *, int *, const cusparseMatDescr_t, cuComplex *,
      int *, int *, const cusparseMatDescr_t, cuComplex *, int *, int *,
      csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &P_count, sizeof(P_count)) < 0 || false)
    goto ERROR_0;
  P_size = P_count * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &Q_count, sizeof(Q_count)) < 0 || false)
    goto ERROR_0;
  Q_size = Q_count * sizeof(int);
  Q = (int *)malloc(Q_size);
  if (Q_size != 0 && Q == nullptr)
    goto ERROR_0;
  if ((Q_size != 0 && rpc_read(conn, Q, Q_size) < 0) ||
      rpc_read(conn, &descrL, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValL_count, sizeof(csrValL_count)) < 0 || false)
    goto ERROR_0;
  csrValL_size = csrValL_count * sizeof(cuComplex);
  csrValL = (cuComplex *)malloc(csrValL_size);
  if (csrValL_size != 0 && csrValL == nullptr)
    goto ERROR_0;
  if ((csrValL_size != 0 && rpc_read(conn, csrValL, csrValL_size) < 0) ||
      rpc_read(conn, &csrRowPtrL_count, sizeof(csrRowPtrL_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrL_size = csrRowPtrL_count * sizeof(int);
  csrRowPtrL = (int *)malloc(csrRowPtrL_size);
  if (csrRowPtrL_size != 0 && csrRowPtrL == nullptr)
    goto ERROR_0;
  if ((csrRowPtrL_size != 0 &&
       rpc_read(conn, csrRowPtrL, csrRowPtrL_size) < 0) ||
      rpc_read(conn, &csrColIndL_count, sizeof(csrColIndL_count)) < 0 || false)
    goto ERROR_0;
  csrColIndL_size = csrColIndL_count * sizeof(int);
  csrColIndL = (int *)malloc(csrColIndL_size);
  if (csrColIndL_size != 0 && csrColIndL == nullptr)
    goto ERROR_0;
  if ((csrColIndL_size != 0 &&
       rpc_read(conn, csrColIndL, csrColIndL_size) < 0) ||
      rpc_read(conn, &descrU, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValU_count, sizeof(csrValU_count)) < 0 || false)
    goto ERROR_0;
  csrValU_size = csrValU_count * sizeof(cuComplex);
  csrValU = (cuComplex *)malloc(csrValU_size);
  if (csrValU_size != 0 && csrValU == nullptr)
    goto ERROR_0;
  if ((csrValU_size != 0 && rpc_read(conn, csrValU, csrValU_size) < 0) ||
      rpc_read(conn, &csrRowPtrU_count, sizeof(csrRowPtrU_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrU_size = csrRowPtrU_count * sizeof(int);
  csrRowPtrU = (int *)malloc(csrRowPtrU_size);
  if (csrRowPtrU_size != 0 && csrRowPtrU == nullptr)
    goto ERROR_0;
  if ((csrRowPtrU_size != 0 &&
       rpc_read(conn, csrRowPtrU, csrRowPtrU_size) < 0) ||
      rpc_read(conn, &csrColIndU_count, sizeof(csrColIndU_count)) < 0 || false)
    goto ERROR_0;
  csrColIndU_size = csrColIndU_count * sizeof(int);
  csrColIndU = (int *)malloc(csrColIndU_size);
  if (csrColIndU_size != 0 && csrColIndU == nullptr)
    goto ERROR_0;
  if ((csrColIndU_size != 0 &&
       rpc_read(conn, csrColIndU, csrColIndU_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrluExtractHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, (P_count * sizeof(int) == 0 ? nullptr : P),
               (Q_count * sizeof(int) == 0 ? nullptr : Q), descrL,
               (csrValL_count * sizeof(cuComplex) == 0 ? nullptr : csrValL),
               (csrRowPtrL_count * sizeof(int) == 0 ? nullptr : csrRowPtrL),
               (csrColIndL_count * sizeof(int) == 0 ? nullptr : csrColIndL),
               descrU,
               (csrValU_count * sizeof(cuComplex) == 0 ? nullptr : csrValU),
               (csrRowPtrU_count * sizeof(int) == 0 ? nullptr : csrRowPtrU),
               (csrColIndU_count * sizeof(int) == 0 ? nullptr : csrColIndU),
               info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, P_count * sizeof(int)) < 0 ||
      rpc_write(conn, Q, Q_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValL, csrValL_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, csrRowPtrL, csrRowPtrL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndL, csrColIndL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValU, csrValU_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, csrRowPtrU, csrRowPtrU_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndU, csrColIndU_count * sizeof(int)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return -1;
}

int handle_cusolverSpZcsrluExtractHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int *P = nullptr;
  size_t P_size;
  uint64_t P_count = 0;
  int *Q = nullptr;
  size_t Q_size;
  uint64_t Q_count = 0;
  cusparseMatDescr_t descrL;
  cuDoubleComplex *csrValL = nullptr;
  size_t csrValL_size;
  uint64_t csrValL_count = 0;
  int *csrRowPtrL = nullptr;
  size_t csrRowPtrL_size;
  uint64_t csrRowPtrL_count = 0;
  int *csrColIndL = nullptr;
  size_t csrColIndL_size;
  uint64_t csrColIndL_count = 0;
  cusparseMatDescr_t descrU;
  cuDoubleComplex *csrValU = nullptr;
  size_t csrValU_size;
  uint64_t csrValU_count = 0;
  int *csrRowPtrU = nullptr;
  size_t csrRowPtrU_size;
  uint64_t csrRowPtrU_count = 0;
  int *csrColIndU = nullptr;
  size_t csrColIndU_size;
  uint64_t csrColIndU_count = 0;
  csrluInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int *, int *, const cusparseMatDescr_t,
      cuDoubleComplex *, int *, int *, const cusparseMatDescr_t,
      cuDoubleComplex *, int *, int *, csrluInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &P_count, sizeof(P_count)) < 0 || false)
    goto ERROR_0;
  P_size = P_count * sizeof(int);
  P = (int *)malloc(P_size);
  if (P_size != 0 && P == nullptr)
    goto ERROR_0;
  if ((P_size != 0 && rpc_read(conn, P, P_size) < 0) ||
      rpc_read(conn, &Q_count, sizeof(Q_count)) < 0 || false)
    goto ERROR_0;
  Q_size = Q_count * sizeof(int);
  Q = (int *)malloc(Q_size);
  if (Q_size != 0 && Q == nullptr)
    goto ERROR_0;
  if ((Q_size != 0 && rpc_read(conn, Q, Q_size) < 0) ||
      rpc_read(conn, &descrL, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValL_count, sizeof(csrValL_count)) < 0 || false)
    goto ERROR_0;
  csrValL_size = csrValL_count * sizeof(cuDoubleComplex);
  csrValL = (cuDoubleComplex *)malloc(csrValL_size);
  if (csrValL_size != 0 && csrValL == nullptr)
    goto ERROR_0;
  if ((csrValL_size != 0 && rpc_read(conn, csrValL, csrValL_size) < 0) ||
      rpc_read(conn, &csrRowPtrL_count, sizeof(csrRowPtrL_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrL_size = csrRowPtrL_count * sizeof(int);
  csrRowPtrL = (int *)malloc(csrRowPtrL_size);
  if (csrRowPtrL_size != 0 && csrRowPtrL == nullptr)
    goto ERROR_0;
  if ((csrRowPtrL_size != 0 &&
       rpc_read(conn, csrRowPtrL, csrRowPtrL_size) < 0) ||
      rpc_read(conn, &csrColIndL_count, sizeof(csrColIndL_count)) < 0 || false)
    goto ERROR_0;
  csrColIndL_size = csrColIndL_count * sizeof(int);
  csrColIndL = (int *)malloc(csrColIndL_size);
  if (csrColIndL_size != 0 && csrColIndL == nullptr)
    goto ERROR_0;
  if ((csrColIndL_size != 0 &&
       rpc_read(conn, csrColIndL, csrColIndL_size) < 0) ||
      rpc_read(conn, &descrU, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValU_count, sizeof(csrValU_count)) < 0 || false)
    goto ERROR_0;
  csrValU_size = csrValU_count * sizeof(cuDoubleComplex);
  csrValU = (cuDoubleComplex *)malloc(csrValU_size);
  if (csrValU_size != 0 && csrValU == nullptr)
    goto ERROR_0;
  if ((csrValU_size != 0 && rpc_read(conn, csrValU, csrValU_size) < 0) ||
      rpc_read(conn, &csrRowPtrU_count, sizeof(csrRowPtrU_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrU_size = csrRowPtrU_count * sizeof(int);
  csrRowPtrU = (int *)malloc(csrRowPtrU_size);
  if (csrRowPtrU_size != 0 && csrRowPtrU == nullptr)
    goto ERROR_0;
  if ((csrRowPtrU_size != 0 &&
       rpc_read(conn, csrRowPtrU, csrRowPtrU_size) < 0) ||
      rpc_read(conn, &csrColIndU_count, sizeof(csrColIndU_count)) < 0 || false)
    goto ERROR_0;
  csrColIndU_size = csrColIndU_count * sizeof(int);
  csrColIndU = (int *)malloc(csrColIndU_size);
  if (csrColIndU_size != 0 && csrColIndU == nullptr)
    goto ERROR_0;
  if ((csrColIndU_size != 0 &&
       rpc_read(conn, csrColIndU, csrColIndU_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrluInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrluExtractHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, (P_count * sizeof(int) == 0 ? nullptr : P),
               (Q_count * sizeof(int) == 0 ? nullptr : Q), descrL,
               (csrValL_count * sizeof(cuDoubleComplex) == 0 ? nullptr
                                                             : csrValL),
               (csrRowPtrL_count * sizeof(int) == 0 ? nullptr : csrRowPtrL),
               (csrColIndL_count * sizeof(int) == 0 ? nullptr : csrColIndL),
               descrU,
               (csrValU_count * sizeof(cuDoubleComplex) == 0 ? nullptr
                                                             : csrValU),
               (csrRowPtrU_count * sizeof(int) == 0 ? nullptr : csrRowPtrU),
               (csrColIndU_count * sizeof(int) == 0 ? nullptr : csrColIndU),
               info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, P, P_count * sizeof(int)) < 0 ||
      rpc_write(conn, Q, Q_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValL, csrValL_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, csrRowPtrL, csrRowPtrL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndL, csrColIndL_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrValU, csrValU_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, csrRowPtrU, csrRowPtrU_count * sizeof(int)) < 0 ||
      rpc_write(conn, csrColIndU, csrColIndU_count * sizeof(int)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndU);
  free((void *)csrRowPtrU);
  free((void *)csrValU);
  free((void *)csrColIndL);
  free((void *)csrRowPtrL);
  free((void *)csrValL);
  free((void *)Q);
  free((void *)P);
  return -1;
}

int handle_cusolverSpCreateCsrqrInfoHost(conn_t *conn) {
  csrqrInfoHost_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrqrInfoHost_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreateCsrqrInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroyCsrqrInfoHost(conn_t *conn) {
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroyCsrqrInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrqrAnalysisHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrqrAnalysisHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpScsrqrBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrqrInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                           const cusparseMatDescr_t, const float *, const int *,
                           const int *, csrqrInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrqrBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrqrInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, csrqrInfoHost_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrqrBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrqrInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, csrqrInfoHost_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrqrBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrqrInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrqrInfoHost_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrqrSetupHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  float mu;
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, float, csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu, sizeof(float)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrSetupHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), mu,
               info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrqrSetupHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  double mu;
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, double, csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu, sizeof(double)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrSetupHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), mu,
               info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrqrSetupHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuComplex mu;
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, cuComplex, csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrSetupHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), mu,
               info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrqrSetupHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  cuDoubleComplex mu;
  csrqrInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, cuDoubleComplex,
      csrqrInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &mu, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrSetupHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), mu,
               info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrqrFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  float *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  float *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, float *,
                                    float *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, nnzA, (b_count * sizeof(float) == 0 ? nullptr : b),
               (x_count * sizeof(float) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(float)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(float)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpDcsrqrFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  double *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  double *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, double *,
                                    double *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrFactorHost");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnzA,
                          (b_count * sizeof(double) == 0 ? nullptr : b),
                          (x_count * sizeof(double) == 0 ? nullptr : x), info,
                          (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(double)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(double)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpCcsrqrFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cuComplex *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  cuComplex *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, cuComplex *,
                           cuComplex *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrFactorHost");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, m, n, nnzA,
                          (b_count * sizeof(cuComplex) == 0 ? nullptr : b),
                          (x_count * sizeof(cuComplex) == 0 ? nullptr : x),
                          info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpZcsrqrFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, cuDoubleComplex *,
                           cuDoubleComplex *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrFactorHost");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n, nnzA,
                         (b_count * sizeof(cuDoubleComplex) == 0 ? nullptr : b),
                         (x_count * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
                         info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpScsrqrZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfoHost_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfoHost_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  float *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  float *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, float *,
                                    float *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, (b_count * sizeof(float) == 0 ? nullptr : b),
               (x_count * sizeof(float) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(float)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(float)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpDcsrqrSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  double *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  double *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, double *,
                                    double *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, (b_count * sizeof(double) == 0 ? nullptr : b),
               (x_count * sizeof(double) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(double)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(double)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpCcsrqrSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  cuComplex *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  cuComplex *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, cuComplex *,
                                    cuComplex *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, m, n, (b_count * sizeof(cuComplex) == 0 ? nullptr : b),
               (x_count * sizeof(cuComplex) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpZcsrqrSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  uint64_t b_count = 0;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  uint64_t x_count = 0;
  csrqrInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, cuDoubleComplex *,
                           cuDoubleComplex *, csrqrInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b_count, sizeof(b_count)) < 0 || false)
    goto ERROR_0;
  b_size = b_count * sizeof(cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) ||
      rpc_read(conn, &x_count, sizeof(x_count)) < 0 || false)
    goto ERROR_0;
  x_size = x_count * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrqrInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrSolveHost");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, m, n,
                         (b_count * sizeof(cuDoubleComplex) == 0 ? nullptr : b),
                         (x_count * sizeof(cuDoubleComplex) == 0 ? nullptr : x),
                         info, (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, b, b_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, x, x_count * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpXcsrqrAnalysis(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrqrAnalysis");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrRowPtrA,
                                    csrColIndA, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, csrqrInfo_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const double *,
                                    const int *, const int *, csrqrInfo_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t, const cuComplex *,
                                    const int *, const int *, csrqrInfo_t,
                                    size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrqrInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrqrInfo_t, size_t *,
      size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrSetup(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  float mu;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const float *, const int *, const int *, float, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu, sizeof(float)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrSetup");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, mu, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrSetup(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  double mu;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const double *, const int *, const int *, double, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu, sizeof(double)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrSetup");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, mu, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrSetup(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  cuComplex mu;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, int, const cusparseMatDescr_t,
      const cuComplex *, const int *, const int *, cuComplex, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuComplex)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrSetup");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, mu, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrSetup(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  cuDoubleComplex mu;
  csrqrInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int,
                                    const cusparseMatDescr_t,
                                    const cuDoubleComplex *, const int *,
                                    const int *, cuDoubleComplex, csrqrInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &mu, sizeof(cuDoubleComplex)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrSetup");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, mu, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  float *b;
  float *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, float *,
                                    float *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  double *b;
  double *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, double *,
                                    double *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cuComplex *b;
  cuComplex *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, cuComplex *,
                           cuComplex *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  int nnzA;
  cuDoubleComplex *b;
  cuDoubleComplex *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, int, cuDoubleComplex *,
                           cuDoubleComplex *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, nnzA, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfo_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfo_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfo_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfo_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfo_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfo_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrqrInfo_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrqrInfo_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrqrSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  float *b;
  float *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, float *,
                                    float *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrqrSolve");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrqrSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  double *b;
  double *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, double *,
                                    double *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrqrSolve");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrqrSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  cuComplex *b;
  cuComplex *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int, cuComplex *,
                                    cuComplex *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrqrSolve");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrqrSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int m;
  int n;
  cuDoubleComplex *b;
  cuDoubleComplex *x;
  csrqrInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int, cuDoubleComplex *,
                           cuDoubleComplex *, csrqrInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &m, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrqrInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrqrSolve");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, m, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCreateCsrcholInfoHost(conn_t *conn) {
  csrcholInfoHost_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrcholInfoHost_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreateCsrcholInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroyCsrcholInfoHost(conn_t *conn) {
  csrcholInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrcholInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroyCsrcholInfoHost");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrcholAnalysisHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrcholInfoHost_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrcholAnalysisHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  return -1;
}

int handle_cusolverSpScsrcholBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrcholInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrcholBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrcholInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrcholBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrcholInfoHost_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrcholBufferInfoHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrcholInfoHost_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholBufferInfoHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrcholFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  float *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const float);
  csrValA = (float *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const float) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpDcsrcholFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  double *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const double);
  csrValA = (double *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const double) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpCcsrcholFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuComplex);
  csrValA = (cuComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpZcsrcholFactorHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  cuDoubleComplex *csrValA = nullptr;
  size_t csrValA_size;
  int *csrRowPtrA = nullptr;
  size_t csrRowPtrA_size;
  uint64_t csrRowPtrA_count = 0;
  int *csrColIndA = nullptr;
  size_t csrColIndA_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                           const cusparseMatDescr_t, const cuDoubleComplex *,
                           const int *, const int *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrValA_size = nnzA * sizeof(const cuDoubleComplex);
  csrValA = (cuDoubleComplex *)malloc(csrValA_size);
  if (csrValA_size != 0 && csrValA == nullptr)
    goto ERROR_0;
  if ((csrValA_size != 0 && rpc_read(conn, csrValA, csrValA_size) < 0) ||
      rpc_read(conn, &csrRowPtrA_count, sizeof(csrRowPtrA_count)) < 0 || false)
    goto ERROR_0;
  csrRowPtrA_size = csrRowPtrA_count * sizeof(const int);
  csrRowPtrA = (int *)malloc(csrRowPtrA_size);
  if (csrRowPtrA_size != 0 && csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((csrRowPtrA_size != 0 &&
       rpc_read(conn, csrRowPtrA, csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  csrColIndA_size = nnzA * sizeof(const int);
  csrColIndA = (int *)malloc(csrColIndA_size);
  if (csrColIndA_size != 0 && csrColIndA == nullptr)
    goto ERROR_0;
  if ((csrColIndA_size != 0 &&
       rpc_read(conn, csrColIndA, csrColIndA_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholFactorHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, nnzA, descrA,
               (nnzA * sizeof(const cuDoubleComplex) == 0 ? nullptr : csrValA),
               (csrRowPtrA_count * sizeof(const int) == 0 ? nullptr
                                                          : csrRowPtrA),
               (nnzA * sizeof(const int) == 0 ? nullptr : csrColIndA), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)csrColIndA);
  free((void *)csrRowPtrA);
  free((void *)csrValA);
  return -1;
}

int handle_cusolverSpScsrcholZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfoHost_t,
                                    double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfoHost_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfoHost_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholZeroPivotHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfoHost_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfoHost_t,
                                    double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholZeroPivotHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  float *b = nullptr;
  size_t b_size;
  float *x = nullptr;
  size_t x_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const float *,
                                    float *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const float);
  b = (float *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(float);
  x = (float *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const float) == 0 ? nullptr : b),
               (n * sizeof(float) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(float)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpDcsrcholSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  double *b = nullptr;
  size_t b_size;
  double *x = nullptr;
  size_t x_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const double *,
                                    double *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const double);
  b = (double *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(double);
  x = (double *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const double) == 0 ? nullptr : b),
               (n * sizeof(double) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(double)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpCcsrcholSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  cuComplex *b = nullptr;
  size_t b_size;
  cuComplex *x = nullptr;
  size_t x_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuComplex *,
                                    cuComplex *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuComplex);
  b = (cuComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuComplex);
  x = (cuComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholSolveHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle, n, (n * sizeof(const cuComplex) == 0 ? nullptr : b),
               (n * sizeof(cuComplex) == 0 ? nullptr : x), info,
               (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpZcsrcholSolveHost(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  cuDoubleComplex *b = nullptr;
  size_t b_size;
  cuDoubleComplex *x = nullptr;
  size_t x_size;
  csrcholInfoHost_t info;
  void *pBuffer = nullptr;
  size_t pBuffer_size;
  uint64_t pBuffer_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuDoubleComplex *,
                           cuDoubleComplex *, csrcholInfoHost_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 || n < 0 || false)
    goto ERROR_0;
  b_size = n * sizeof(const cuDoubleComplex);
  b = (cuDoubleComplex *)malloc(b_size);
  if (b_size != 0 && b == nullptr)
    goto ERROR_0;
  if ((b_size != 0 && rpc_read(conn, b, b_size) < 0) || n < 0 || false)
    goto ERROR_0;
  x_size = n * sizeof(cuDoubleComplex);
  x = (cuDoubleComplex *)malloc(x_size);
  if (x_size != 0 && x == nullptr)
    goto ERROR_0;
  if ((x_size != 0 && rpc_read(conn, x, x_size) < 0) ||
      rpc_read(conn, &info, sizeof(csrcholInfoHost_t)) < 0 ||
      rpc_read(conn, &pBuffer_count, sizeof(pBuffer_count)) < 0 || false)
    goto ERROR_0;
  pBuffer_size = pBuffer_count;
  pBuffer = (void *)malloc(pBuffer_size);
  if (pBuffer_size != 0 && pBuffer == nullptr)
    goto ERROR_0;
  if ((pBuffer_size != 0 && rpc_read(conn, pBuffer, pBuffer_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholSolveHost");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n,
                         (n * sizeof(const cuDoubleComplex) == 0 ? nullptr : b),
                         (n * sizeof(cuDoubleComplex) == 0 ? nullptr : x), info,
                         (pBuffer_count == 0 ? nullptr : pBuffer));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, x, n * sizeof(cuDoubleComplex)) < 0 ||
      rpc_write(conn, pBuffer, pBuffer_count) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return 0;
ERROR_0:
  free((void *)pBuffer);
  free((void *)x);
  free((void *)b);
  return -1;
}

int handle_cusolverSpCreateCsrcholInfo(conn_t *conn) {
  csrcholInfo_t info{};
  info = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrcholInfo_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCreateCsrcholInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDestroyCsrcholInfo(conn_t *conn) {
  csrcholInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(csrcholInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDestroyCsrcholInfo");
  return_value = fn == nullptr ? function_not_found() : fn(info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpXcsrcholAnalysis(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, int,
                                    const cusparseMatDescr_t, const int *,
                                    const int *, csrcholInfo_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpXcsrcholAnalysis");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(handle, n, nnzA, descrA, csrRowPtrA, csrColIndA, info);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrcholInfo_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrcholInfo_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrcholInfo_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholBufferInfo(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  size_t internalDataInBytes{};
  size_t workspaceInBytes{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrcholInfo_t,
      size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_read(conn, &workspaceInBytes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholBufferInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info,
                                    &internalDataInBytes, &workspaceInBytes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &internalDataInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &workspaceInBytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const float *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const float *,
      const int *, const int *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const float *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const double *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const double *,
      const int *, const int *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const double *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t, const cuComplex *,
      const int *, const int *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholFactor(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  int nnzA;
  cusparseMatDescr_t descrA;
  const cuDoubleComplex *csrValA;
  const int *csrRowPtrA;
  const int *csrColIndA;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverSpHandle_t, int, int, const cusparseMatDescr_t,
      const cuDoubleComplex *, const int *, const int *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &descrA, sizeof(const cusparseMatDescr_t)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(const int *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholFactor");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, n, nnzA, descrA, csrValA,
                                    csrRowPtrA, csrColIndA, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  float tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, float, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(float)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholZeroPivot(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  double tol;
  int position{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, double, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &tol, sizeof(double)) < 0 ||
      rpc_read(conn, &position, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholZeroPivot");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, info, tol, &position);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &position, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  const float *b;
  float *x;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const float *,
                                    float *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(const float *)) < 0 ||
      rpc_read(conn, &x, sizeof(float *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholSolve");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  const double *b;
  double *x;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const double *,
                                    double *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(const double *)) < 0 ||
      rpc_read(conn, &x, sizeof(double *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholSolve");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  const cuComplex *b;
  cuComplex *x;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuComplex *,
                                    cuComplex *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholSolve");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholSolve(conn_t *conn) {
  cusolverSpHandle_t handle;
  int n;
  const cuDoubleComplex *b;
  cuDoubleComplex *x;
  csrcholInfo_t info;
  void *pBuffer;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, int, const cuDoubleComplex *,
                           cuDoubleComplex *, csrcholInfo_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &b, sizeof(const cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &x, sizeof(cuDoubleComplex *)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &pBuffer, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholSolve");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, n, b, x, info, pBuffer);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpScsrcholDiag(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  float *diag;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpScsrcholDiag");
  return_value = fn == nullptr ? function_not_found() : fn(handle, info, diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpDcsrcholDiag(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  double *diag;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpDcsrcholDiag");
  return_value = fn == nullptr ? function_not_found() : fn(handle, info, diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpCcsrcholDiag(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  float *diag;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, float *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(float *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpCcsrcholDiag");
  return_value = fn == nullptr ? function_not_found() : fn(handle, info, diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverSpZcsrcholDiag(conn_t *conn) {
  cusolverSpHandle_t handle;
  csrcholInfo_t info;
  double *diag;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverSpHandle_t, csrcholInfo_t, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverSpHandle_t)) < 0 ||
      rpc_read(conn, &info, sizeof(csrcholInfo_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(double *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverSpZcsrcholDiag");
  return_value = fn == nullptr ? function_not_found() : fn(handle, info, diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfCreate(conn_t *conn) {
  cusolverRfHandle_t handle{};
  handle = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfDestroy(conn_t *conn) {
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfGetMatrixFormat(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfMatrixFormat_t format{};
  cusolverRfUnitDiagonal_t diag{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverRfHandle_t, cusolverRfMatrixFormat_t *,
                           cusolverRfUnitDiagonal_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cusolverRfMatrixFormat_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cusolverRfUnitDiagonal_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfGetMatrixFormat");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &format, &diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &format, sizeof(cusolverRfMatrixFormat_t)) < 0 ||
      rpc_write(conn, &diag, sizeof(cusolverRfUnitDiagonal_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSetMatrixFormat(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfMatrixFormat_t format;
  cusolverRfUnitDiagonal_t diag;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(
      cusolverRfHandle_t, cusolverRfMatrixFormat_t, cusolverRfUnitDiagonal_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &format, sizeof(cusolverRfMatrixFormat_t)) < 0 ||
      rpc_read(conn, &diag, sizeof(cusolverRfUnitDiagonal_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetMatrixFormat");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, format, diag);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSetNumericProperties(conn_t *conn) {
  cusolverRfHandle_t handle;
  double zero;
  double boost;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &zero, sizeof(double)) < 0 ||
      rpc_read(conn, &boost, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetNumericProperties");
  return_value = fn == nullptr ? function_not_found() : fn(handle, zero, boost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfGetNumericProperties(conn_t *conn) {
  cusolverRfHandle_t handle;
  double zero{};
  double boost{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, double *, double *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &zero, sizeof(double)) < 0 ||
      rpc_read(conn, &boost, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfGetNumericProperties");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &zero, &boost);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &zero, sizeof(double)) < 0 ||
      rpc_write(conn, &boost, sizeof(double)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfGetNumericBoostReport(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfNumericBoostReport_t report{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t,
                                    cusolverRfNumericBoostReport_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &report, sizeof(cusolverRfNumericBoostReport_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfGetNumericBoostReport");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &report);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &report, sizeof(cusolverRfNumericBoostReport_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSetAlgs(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfFactorization_t factAlg;
  cusolverRfTriangularSolve_t solveAlg;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverRfHandle_t, cusolverRfFactorization_t,
                           cusolverRfTriangularSolve_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &factAlg, sizeof(cusolverRfFactorization_t)) < 0 ||
      rpc_read(conn, &solveAlg, sizeof(cusolverRfTriangularSolve_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetAlgs");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, factAlg, solveAlg);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfGetAlgs(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfFactorization_t factAlg{};
  cusolverRfTriangularSolve_t solveAlg{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverRfHandle_t, cusolverRfFactorization_t *,
                           cusolverRfTriangularSolve_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &factAlg, sizeof(cusolverRfFactorization_t)) < 0 ||
      rpc_read(conn, &solveAlg, sizeof(cusolverRfTriangularSolve_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfGetAlgs");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &factAlg, &solveAlg);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &factAlg, sizeof(cusolverRfFactorization_t)) < 0 ||
      rpc_write(conn, &solveAlg, sizeof(cusolverRfTriangularSolve_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfGetResetValuesFastMode(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfResetValuesFastMode_t fastMode{};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t,
                                    cusolverRfResetValuesFastMode_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &fastMode, sizeof(cusolverRfResetValuesFastMode_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfGetResetValuesFastMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, &fastMode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &fastMode, sizeof(cusolverRfResetValuesFastMode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSetResetValuesFastMode(conn_t *conn) {
  cusolverRfHandle_t handle;
  cusolverRfResetValuesFastMode_t fastMode;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t =
      cusolverStatus_t (*)(cusolverRfHandle_t, cusolverRfResetValuesFastMode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &fastMode, sizeof(cusolverRfResetValuesFastMode_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetResetValuesFastMode");
  return_value = fn == nullptr ? function_not_found() : fn(handle, fastMode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSetupHost(conn_t *conn) {
  int n;
  int nnzA;
  int *h_csrRowPtrA = nullptr;
  size_t h_csrRowPtrA_size;
  uint64_t h_csrRowPtrA_count = 0;
  int *h_csrColIndA = nullptr;
  size_t h_csrColIndA_size;
  double *h_csrValA = nullptr;
  size_t h_csrValA_size;
  int nnzL;
  int *h_csrRowPtrL = nullptr;
  size_t h_csrRowPtrL_size;
  uint64_t h_csrRowPtrL_count = 0;
  int *h_csrColIndL = nullptr;
  size_t h_csrColIndL_size;
  double *h_csrValL = nullptr;
  size_t h_csrValL_size;
  int nnzU;
  int *h_csrRowPtrU = nullptr;
  size_t h_csrRowPtrU_size;
  uint64_t h_csrRowPtrU_count = 0;
  int *h_csrColIndU = nullptr;
  size_t h_csrColIndU_size;
  double *h_csrValU = nullptr;
  size_t h_csrValU_size;
  int *h_P = nullptr;
  size_t h_P_size;
  int *h_Q = nullptr;
  size_t h_Q_size;
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(int, int, int *, int *, double *, int,
                                    int *, int *, double *, int, int *, int *,
                                    double *, int *, int *, cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &h_csrRowPtrA_count, sizeof(h_csrRowPtrA_count)) < 0 ||
      false)
    goto ERROR_0;
  h_csrRowPtrA_size = h_csrRowPtrA_count * sizeof(int);
  h_csrRowPtrA = (int *)malloc(h_csrRowPtrA_size);
  if (h_csrRowPtrA_size != 0 && h_csrRowPtrA == nullptr)
    goto ERROR_0;
  if ((h_csrRowPtrA_size != 0 &&
       rpc_read(conn, h_csrRowPtrA, h_csrRowPtrA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  h_csrColIndA_size = nnzA * sizeof(int);
  h_csrColIndA = (int *)malloc(h_csrColIndA_size);
  if (h_csrColIndA_size != 0 && h_csrColIndA == nullptr)
    goto ERROR_0;
  if ((h_csrColIndA_size != 0 &&
       rpc_read(conn, h_csrColIndA, h_csrColIndA_size) < 0) ||
      nnzA < 0 || false)
    goto ERROR_0;
  h_csrValA_size = nnzA * sizeof(double);
  h_csrValA = (double *)malloc(h_csrValA_size);
  if (h_csrValA_size != 0 && h_csrValA == nullptr)
    goto ERROR_0;
  if ((h_csrValA_size != 0 && rpc_read(conn, h_csrValA, h_csrValA_size) < 0) ||
      rpc_read(conn, &nnzL, sizeof(int)) < 0 ||
      rpc_read(conn, &h_csrRowPtrL_count, sizeof(h_csrRowPtrL_count)) < 0 ||
      false)
    goto ERROR_0;
  h_csrRowPtrL_size = h_csrRowPtrL_count * sizeof(int);
  h_csrRowPtrL = (int *)malloc(h_csrRowPtrL_size);
  if (h_csrRowPtrL_size != 0 && h_csrRowPtrL == nullptr)
    goto ERROR_0;
  if ((h_csrRowPtrL_size != 0 &&
       rpc_read(conn, h_csrRowPtrL, h_csrRowPtrL_size) < 0) ||
      nnzL < 0 || false)
    goto ERROR_0;
  h_csrColIndL_size = nnzL * sizeof(int);
  h_csrColIndL = (int *)malloc(h_csrColIndL_size);
  if (h_csrColIndL_size != 0 && h_csrColIndL == nullptr)
    goto ERROR_0;
  if ((h_csrColIndL_size != 0 &&
       rpc_read(conn, h_csrColIndL, h_csrColIndL_size) < 0) ||
      nnzL < 0 || false)
    goto ERROR_0;
  h_csrValL_size = nnzL * sizeof(double);
  h_csrValL = (double *)malloc(h_csrValL_size);
  if (h_csrValL_size != 0 && h_csrValL == nullptr)
    goto ERROR_0;
  if ((h_csrValL_size != 0 && rpc_read(conn, h_csrValL, h_csrValL_size) < 0) ||
      rpc_read(conn, &nnzU, sizeof(int)) < 0 ||
      rpc_read(conn, &h_csrRowPtrU_count, sizeof(h_csrRowPtrU_count)) < 0 ||
      false)
    goto ERROR_0;
  h_csrRowPtrU_size = h_csrRowPtrU_count * sizeof(int);
  h_csrRowPtrU = (int *)malloc(h_csrRowPtrU_size);
  if (h_csrRowPtrU_size != 0 && h_csrRowPtrU == nullptr)
    goto ERROR_0;
  if ((h_csrRowPtrU_size != 0 &&
       rpc_read(conn, h_csrRowPtrU, h_csrRowPtrU_size) < 0) ||
      nnzU < 0 || false)
    goto ERROR_0;
  h_csrColIndU_size = nnzU * sizeof(int);
  h_csrColIndU = (int *)malloc(h_csrColIndU_size);
  if (h_csrColIndU_size != 0 && h_csrColIndU == nullptr)
    goto ERROR_0;
  if ((h_csrColIndU_size != 0 &&
       rpc_read(conn, h_csrColIndU, h_csrColIndU_size) < 0) ||
      nnzU < 0 || false)
    goto ERROR_0;
  h_csrValU_size = nnzU * sizeof(double);
  h_csrValU = (double *)malloc(h_csrValU_size);
  if (h_csrValU_size != 0 && h_csrValU == nullptr)
    goto ERROR_0;
  if ((h_csrValU_size != 0 && rpc_read(conn, h_csrValU, h_csrValU_size) < 0) ||
      n < 0 || false)
    goto ERROR_0;
  h_P_size = n * sizeof(int);
  h_P = (int *)malloc(h_P_size);
  if (h_P_size != 0 && h_P == nullptr)
    goto ERROR_0;
  if ((h_P_size != 0 && rpc_read(conn, h_P, h_P_size) < 0) || n < 0 || false)
    goto ERROR_0;
  h_Q_size = n * sizeof(int);
  h_Q = (int *)malloc(h_Q_size);
  if (h_Q_size != 0 && h_Q == nullptr)
    goto ERROR_0;
  if ((h_Q_size != 0 && rpc_read(conn, h_Q, h_Q_size) < 0) ||
      rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetupHost");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(n, nnzA,
               (h_csrRowPtrA_count * sizeof(int) == 0 ? nullptr : h_csrRowPtrA),
               (nnzA * sizeof(int) == 0 ? nullptr : h_csrColIndA),
               (nnzA * sizeof(double) == 0 ? nullptr : h_csrValA), nnzL,
               (h_csrRowPtrL_count * sizeof(int) == 0 ? nullptr : h_csrRowPtrL),
               (nnzL * sizeof(int) == 0 ? nullptr : h_csrColIndL),
               (nnzL * sizeof(double) == 0 ? nullptr : h_csrValL), nnzU,
               (h_csrRowPtrU_count * sizeof(int) == 0 ? nullptr : h_csrRowPtrU),
               (nnzU * sizeof(int) == 0 ? nullptr : h_csrColIndU),
               (nnzU * sizeof(double) == 0 ? nullptr : h_csrValU),
               (n * sizeof(int) == 0 ? nullptr : h_P),
               (n * sizeof(int) == 0 ? nullptr : h_Q), handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)h_Q);
  free((void *)h_P);
  free((void *)h_csrValU);
  free((void *)h_csrColIndU);
  free((void *)h_csrRowPtrU);
  free((void *)h_csrValL);
  free((void *)h_csrColIndL);
  free((void *)h_csrRowPtrL);
  free((void *)h_csrValA);
  free((void *)h_csrColIndA);
  free((void *)h_csrRowPtrA);
  return 0;
ERROR_0:
  free((void *)h_Q);
  free((void *)h_P);
  free((void *)h_csrValU);
  free((void *)h_csrColIndU);
  free((void *)h_csrRowPtrU);
  free((void *)h_csrValL);
  free((void *)h_csrColIndL);
  free((void *)h_csrRowPtrL);
  free((void *)h_csrValA);
  free((void *)h_csrColIndA);
  free((void *)h_csrRowPtrA);
  return -1;
}

int handle_cusolverRfSetupDevice(conn_t *conn) {
  int n;
  int nnzA;
  int *csrRowPtrA;
  int *csrColIndA;
  double *csrValA;
  int nnzL;
  int *csrRowPtrL;
  int *csrColIndL;
  double *csrValL;
  int nnzU;
  int *csrRowPtrU;
  int *csrColIndU;
  double *csrValU;
  int *P;
  int *Q;
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(int, int, int *, int *, double *, int,
                                    int *, int *, double *, int, int *, int *,
                                    double *, int *, int *, cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &nnzL, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrL, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrColIndL, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrValL, sizeof(double *)) < 0 ||
      rpc_read(conn, &nnzU, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrU, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrColIndU, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrValU, sizeof(double *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &Q, sizeof(int *)) < 0 ||
      rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSetupDevice");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(n, nnzA, csrRowPtrA, csrColIndA, csrValA, nnzL,
                          csrRowPtrL, csrColIndL, csrValL, nnzU, csrRowPtrU,
                          csrColIndU, csrValU, P, Q, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfResetValues(conn_t *conn) {
  int n;
  int nnzA;
  int *csrRowPtrA;
  int *csrColIndA;
  double *csrValA;
  int *P;
  int *Q;
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(int, int, int *, int *, double *, int *,
                                    int *, cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrValA, sizeof(double *)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &Q, sizeof(int *)) < 0 ||
      rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfResetValues");
  return_value = fn == nullptr ? function_not_found()
                               : fn(n, nnzA, csrRowPtrA, csrColIndA, csrValA, P,
                                    Q, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfAnalyze(conn_t *conn) {
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfAnalyze");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfRefactor(conn_t *conn) {
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfRefactor");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfAccessBundledFactorsDevice(conn_t *conn) {
  cusolverRfHandle_t handle;
  int nnzM{};
  int *Mp{};
  Mp = {};
  int *Mi{};
  Mi = {};
  double *Mx{};
  Mx = {};
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, int *, int **, int **,
                                    double **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &nnzM, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfAccessBundledFactorsDevice");
  return_value =
      fn == nullptr ? function_not_found() : fn(handle, &nnzM, &Mp, &Mi, &Mx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nnzM, sizeof(int)) < 0 ||
      rpc_write(conn, &Mp, sizeof(int *)) < 0 ||
      rpc_write(conn, &Mi, sizeof(int *)) < 0 ||
      rpc_write(conn, &Mx, sizeof(double *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfSolve(conn_t *conn) {
  cusolverRfHandle_t handle;
  int *P;
  int *Q;
  int nrhs;
  double *Temp;
  int ldt;
  double *XF;
  int ldxf;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, int *, int *, int,
                                    double *, int, double *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &Q, sizeof(int *)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Temp, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldt, sizeof(int)) < 0 ||
      rpc_read(conn, &XF, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldxf, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfSolve");
  return_value = fn == nullptr ? function_not_found()
                               : fn(handle, P, Q, nrhs, Temp, ldt, XF, ldxf);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfBatchResetValues(conn_t *conn) {
  int batchSize;
  int n;
  int nnzA;
  int *csrRowPtrA;
  int *csrColIndA;
  double **csrValA_array;
  int *P;
  int *Q;
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(int, int, int, int *, int *, double **,
                                    int *, int *, cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &batchSize, sizeof(int)) < 0 ||
      rpc_read(conn, &n, sizeof(int)) < 0 ||
      rpc_read(conn, &nnzA, sizeof(int)) < 0 ||
      rpc_read(conn, &csrRowPtrA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrColIndA, sizeof(int *)) < 0 ||
      rpc_read(conn, &csrValA_array, sizeof(double **)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &Q, sizeof(int *)) < 0 ||
      rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfBatchResetValues");
  return_value = fn == nullptr ? function_not_found()
                               : fn(batchSize, n, nnzA, csrRowPtrA, csrColIndA,
                                    csrValA_array, P, Q, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfBatchAnalyze(conn_t *conn) {
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfBatchAnalyze");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfBatchRefactor(conn_t *conn) {
  cusolverRfHandle_t handle;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfBatchRefactor");
  return_value = fn == nullptr ? function_not_found() : fn(handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfBatchSolve(conn_t *conn) {
  cusolverRfHandle_t handle;
  int *P;
  int *Q;
  int nrhs;
  double *Temp;
  int ldt;
  double **XF_array;
  int ldxf;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, int *, int *, int,
                                    double *, int, double **, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &P, sizeof(int *)) < 0 ||
      rpc_read(conn, &Q, sizeof(int *)) < 0 ||
      rpc_read(conn, &nrhs, sizeof(int)) < 0 ||
      rpc_read(conn, &Temp, sizeof(double *)) < 0 ||
      rpc_read(conn, &ldt, sizeof(int)) < 0 ||
      rpc_read(conn, &XF_array, sizeof(double **)) < 0 ||
      rpc_read(conn, &ldxf, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfBatchSolve");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(handle, P, Q, nrhs, Temp, ldt, XF_array, ldxf);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cusolverRfBatchZeroPivot(conn_t *conn) {
  cusolverRfHandle_t handle;
  int *position = nullptr;
  size_t position_size;
  uint64_t position_count = 0;
  int request_id;
  cusolverStatus_t return_value;
  using fn_t = cusolverStatus_t (*)(cusolverRfHandle_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cusolverRfHandle_t)) < 0 ||
      rpc_read(conn, &position_count, sizeof(position_count)) < 0 || false)
    goto ERROR_0;
  position_size = position_count * sizeof(int);
  position = (int *)malloc(position_size);
  if (position_size != 0 && position == nullptr)
    goto ERROR_0;
  if ((position_size != 0 && rpc_read(conn, position, position_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cusolver_symbol<fn_t>("cusolverRfBatchZeroPivot");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(handle,
               (position_count * sizeof(int) == 0 ? nullptr : position));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, position, position_count * sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cusolverStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)position);
  return 0;
ERROR_0:
  free((void *)position);
  return -1;
}
