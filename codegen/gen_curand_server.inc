// Generated by codegen.py. Do not edit by hand.

// A pointer-mode scalar's value lands in the handler's inline slot,
// or on the heap when it is wider.
static unsigned char *lupine_scalar_slot(std::vector<unsigned char> &heap,
                                         unsigned char *slot, size_t size,
                                         uint32_t width) {
  if (width <= size) {
    return slot;
  }
  heap.resize(width);
  return heap.data();
}

int handle_curandCreateGenerator(conn_t *conn) {
  curandGenerator_t generator{};
  generator = {};
  curandRngType_t rng_type;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t *, curandRngType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rng_type, sizeof(curandRngType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandCreateGenerator");
  return_value =
      fn == nullptr ? function_not_found() : fn(&generator, rng_type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandCreateGeneratorHost(conn_t *conn) {
  curandGenerator_t generator{};
  generator = {};
  curandRngType_t rng_type;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t *, curandRngType_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &rng_type, sizeof(curandRngType_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandCreateGeneratorHost");
  return_value =
      fn == nullptr ? function_not_found() : fn(&generator, rng_type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandDestroyGenerator(conn_t *conn) {
  curandGenerator_t generator;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandDestroyGenerator");
  return_value = fn == nullptr ? function_not_found() : fn(generator);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGetVersion(conn_t *conn) {
  int version{};
  version = {};
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(&version);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &version, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGetProperty(conn_t *conn) {
  libraryPropertyType type;
  int value{};
  value = {};
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(libraryPropertyType, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &type, sizeof(libraryPropertyType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGetProperty");
  return_value = fn == nullptr ? function_not_found() : fn(type, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandSetStream(conn_t *conn) {
  curandGenerator_t generator;
  cudaStream_t stream;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandSetStream");
  return_value = fn == nullptr ? function_not_found() : fn(generator, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandSetPseudoRandomGeneratorSeed(conn_t *conn) {
  curandGenerator_t generator;
  unsigned long long seed;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &seed, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandSetPseudoRandomGeneratorSeed");
  return_value = fn == nullptr ? function_not_found() : fn(generator, seed);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandSetGeneratorOffset(conn_t *conn) {
  curandGenerator_t generator;
  unsigned long long offset;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &offset, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandSetGeneratorOffset");
  return_value = fn == nullptr ? function_not_found() : fn(generator, offset);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandSetGeneratorOrdering(conn_t *conn) {
  curandGenerator_t generator;
  curandOrdering_t order;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, curandOrdering_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &order, sizeof(curandOrdering_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandSetGeneratorOrdering");
  return_value = fn == nullptr ? function_not_found() : fn(generator, order);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandSetQuasiRandomGeneratorDimensions(conn_t *conn) {
  curandGenerator_t generator;
  unsigned int num_dimensions;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &num_dimensions, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandSetQuasiRandomGeneratorDimensions");
  return_value =
      fn == nullptr ? function_not_found() : fn(generator, num_dimensions);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerate(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  unsigned int *outputPtr = nullptr;
  size_t num;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, unsigned int *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &num, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerate");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(generator,
               outputPtr_width != 0
                   ? reinterpret_cast<unsigned int *>(lupine_scalar_slot(
                         outputPtr_heap, outputPtr_value,
                         sizeof(outputPtr_value), outputPtr_width))
                   : outputPtr,
               num);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateLongLong(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  unsigned long long *outputPtr = nullptr;
  size_t num;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, unsigned long long *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &num, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateLongLong");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(generator,
               outputPtr_width != 0
                   ? reinterpret_cast<unsigned long long *>(lupine_scalar_slot(
                         outputPtr_heap, outputPtr_value,
                         sizeof(outputPtr_value), outputPtr_width))
                   : outputPtr,
               num);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateUniform(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  float *outputPtr = nullptr;
  size_t num;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, float *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &num, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateUniform");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<float *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          num);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateUniformDouble(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  double *outputPtr = nullptr;
  size_t num;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, double *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &num, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateUniformDouble");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          num);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateNormal(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  float *outputPtr = nullptr;
  size_t n;
  float mean;
  float stddev;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, float *, size_t, float, float);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &mean, sizeof(float)) < 0 ||
      rpc_read(conn, &stddev, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateNormal");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<float *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          n, mean, stddev);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateNormalDouble(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  double *outputPtr = nullptr;
  size_t n;
  double mean;
  double stddev;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, double *, size_t, double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &mean, sizeof(double)) < 0 ||
      rpc_read(conn, &stddev, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateNormalDouble");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          n, mean, stddev);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateLogNormal(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  float *outputPtr = nullptr;
  size_t n;
  float mean;
  float stddev;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, float *, size_t, float, float);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &mean, sizeof(float)) < 0 ||
      rpc_read(conn, &stddev, sizeof(float)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateLogNormal");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<float *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          n, mean, stddev);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateLogNormalDouble(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  double *outputPtr = nullptr;
  size_t n;
  double mean;
  double stddev;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, double *, size_t, double, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &mean, sizeof(double)) < 0 ||
      rpc_read(conn, &stddev, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateLogNormalDouble");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(generator,
                          outputPtr_width != 0
                              ? reinterpret_cast<double *>(lupine_scalar_slot(
                                    outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width))
                              : outputPtr,
                          n, mean, stddev);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandCreatePoissonDistribution(conn_t *conn) {
  double lambda;
  curandDiscreteDistribution_t discrete_distribution{};
  discrete_distribution = {};
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(double, curandDiscreteDistribution_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &lambda, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandCreatePoissonDistribution");
  return_value =
      fn == nullptr ? function_not_found() : fn(lambda, &discrete_distribution);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &discrete_distribution,
                sizeof(curandDiscreteDistribution_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandDestroyDistribution(conn_t *conn) {
  curandDiscreteDistribution_t discrete_distribution;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandDiscreteDistribution_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &discrete_distribution,
               sizeof(curandDiscreteDistribution_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandDestroyDistribution");
  return_value =
      fn == nullptr ? function_not_found() : fn(discrete_distribution);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGeneratePoisson(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  unsigned int *outputPtr = nullptr;
  size_t n;
  double lambda;
  int request_id;
  curandStatus_t return_value;
  using fn_t =
      curandStatus_t (*)(curandGenerator_t, unsigned int *, size_t, double);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &lambda, sizeof(double)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGeneratePoisson");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(generator,
               outputPtr_width != 0
                   ? reinterpret_cast<unsigned int *>(lupine_scalar_slot(
                         outputPtr_heap, outputPtr_value,
                         sizeof(outputPtr_value), outputPtr_width))
                   : outputPtr,
               n, lambda);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGeneratePoissonMethod(conn_t *conn) {
  curandGenerator_t generator;
  uint32_t outputPtr_width;
  alignas(16) unsigned char outputPtr_value[64];
  std::vector<unsigned char> outputPtr_heap;
  unsigned int *outputPtr = nullptr;
  size_t n;
  double lambda;
  curandMethod_t method;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t, unsigned int *, size_t,
                                  double, curandMethod_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 ||
      rpc_read(conn, &outputPtr_width, sizeof(outputPtr_width)) < 0 ||
      (outputPtr_width == 0 &&
       rpc_read(conn, &outputPtr, sizeof(outputPtr)) < 0) ||
      rpc_read(conn, &n, sizeof(size_t)) < 0 ||
      rpc_read(conn, &lambda, sizeof(double)) < 0 ||
      rpc_read(conn, &method, sizeof(curandMethod_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGeneratePoissonMethod");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(generator,
               outputPtr_width != 0
                   ? reinterpret_cast<unsigned int *>(lupine_scalar_slot(
                         outputPtr_heap, outputPtr_value,
                         sizeof(outputPtr_value), outputPtr_width))
                   : outputPtr,
               n, lambda, method);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (outputPtr_width != 0 &&
       rpc_write(conn,
                 lupine_scalar_slot(outputPtr_heap, outputPtr_value,
                                    sizeof(outputPtr_value), outputPtr_width),
                 outputPtr_width) < 0) ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_curandGenerateSeeds(conn_t *conn) {
  curandGenerator_t generator;
  int request_id;
  curandStatus_t return_value;
  using fn_t = curandStatus_t (*)(curandGenerator_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &generator, sizeof(curandGenerator_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = curand_symbol<fn_t>("curandGenerateSeeds");
  return_value = fn == nullptr ? function_not_found() : fn(generator);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(curandStatus_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}
