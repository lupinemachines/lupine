// Generated by codegen.py. Do not edit by hand.

int handle_cudaArrayGetInfo(conn_t *conn) {
  struct cudaChannelFormatDesc desc {};
  desc = {};
  struct cudaExtent extent {};
  extent = {};
  unsigned int flags{};
  flags = {};
  cudaArray_t array;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(struct cudaChannelFormatDesc *, struct cudaExtent *,
                      unsigned int *, cudaArray_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &array, sizeof(cudaArray_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaArrayGetInfo");
  return_value =
      fn == nullptr ? function_not_found() : fn(&desc, &extent, &flags, array);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &desc, sizeof(struct cudaChannelFormatDesc)) < 0 ||
      rpc_write(conn, &extent, sizeof(struct cudaExtent)) < 0 ||
      rpc_write(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaArrayGetMemoryRequirements(conn_t *conn) {
  struct cudaArrayMemoryRequirements memoryRequirements {};
  memoryRequirements = {};
  cudaArray_t array;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(struct cudaArrayMemoryRequirements *, cudaArray_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &array, sizeof(cudaArray_t)) < 0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaArrayGetMemoryRequirements");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&memoryRequirements, array, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memoryRequirements,
                sizeof(struct cudaArrayMemoryRequirements)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaArrayGetPlane(conn_t *conn) {
  cudaArray_t pPlaneArray{};
  pPlaneArray = {};
  cudaArray_t hArray;
  unsigned int planeIdx;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaArray_t *, cudaArray_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hArray, sizeof(cudaArray_t)) < 0 ||
      rpc_read(conn, &planeIdx, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaArrayGetPlane");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pPlaneArray, hArray, planeIdx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pPlaneArray, sizeof(cudaArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaArrayGetSparseProperties(conn_t *conn) {
  struct cudaArraySparseProperties sparseProperties {};
  sparseProperties = {};
  cudaArray_t array;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaArraySparseProperties *, cudaArray_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &array, sizeof(cudaArray_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaArrayGetSparseProperties");
  return_value =
      fn == nullptr ? function_not_found() : fn(&sparseProperties, array);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sparseProperties,
                sizeof(struct cudaArraySparseProperties)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaChooseDevice(conn_t *conn) {
  int device{};
  device = {};
  struct cudaDeviceProp prop {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const struct cudaDeviceProp *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &prop, sizeof(const struct cudaDeviceProp)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaChooseDevice");
  return_value = fn == nullptr ? function_not_found() : fn(&device, &prop);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaCreateChannelDesc(conn_t *conn) {
  int x;
  int y;
  int z;
  int w;
  enum cudaChannelFormatKind f;
  int request_id;
  struct cudaChannelFormatDesc return_value;
  using fn_t = struct cudaChannelFormatDesc (*)(int, int, int, int,
                                                enum cudaChannelFormatKind);
  fn_t fn = nullptr;
  if (rpc_read(conn, &x, sizeof(int)) < 0 ||
      rpc_read(conn, &y, sizeof(int)) < 0 ||
      rpc_read(conn, &z, sizeof(int)) < 0 ||
      rpc_read(conn, &w, sizeof(int)) < 0 ||
      rpc_read(conn, &f, sizeof(enum cudaChannelFormatKind)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaCreateChannelDesc");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(x, y, z, w, f);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(struct cudaChannelFormatDesc)) <
          0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaCreateSurfaceObject(conn_t *conn) {
  cudaSurfaceObject_t pSurfObject{};
  pSurfObject = {};
  struct cudaResourceDesc pResDesc {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaSurfaceObject_t *, const struct cudaResourceDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pResDesc, sizeof(const struct cudaResourceDesc)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaCreateSurfaceObject");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pSurfObject, &pResDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pSurfObject, sizeof(cudaSurfaceObject_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaCreateTextureObject(conn_t *conn) {
  cudaTextureObject_t pTexObject{};
  pTexObject = {};
  struct cudaResourceDesc pResDesc {};
  struct cudaTextureDesc *pTexDesc_null_check;
  struct cudaTextureDesc pTexDesc;
  struct cudaResourceViewDesc *pResViewDesc_null_check;
  struct cudaResourceViewDesc pResViewDesc;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaTextureObject_t *, const struct cudaResourceDesc *,
      const struct cudaTextureDesc *, const struct cudaResourceViewDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pResDesc, sizeof(const struct cudaResourceDesc)) < 0 ||
      rpc_read(conn, &pTexDesc_null_check,
               sizeof(const struct cudaTextureDesc *)) < 0 ||
      (pTexDesc_null_check &&
       rpc_read(conn, &pTexDesc, sizeof(const struct cudaTextureDesc)) < 0) ||
      rpc_read(conn, &pResViewDesc_null_check,
               sizeof(const struct cudaResourceViewDesc *)) < 0 ||
      (pResViewDesc_null_check &&
       rpc_read(conn, &pResViewDesc,
                sizeof(const struct cudaResourceViewDesc)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaCreateTextureObject");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pTexObject, &pResDesc,
                          pTexDesc_null_check ? &pTexDesc : nullptr,
                          pResViewDesc_null_check ? &pResViewDesc : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pTexObject, sizeof(cudaTextureObject_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaCtxResetPersistingL2Cache(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaCtxResetPersistingL2Cache");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDestroyExternalMemory(conn_t *conn) {
  cudaExternalMemory_t extMem;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExternalMemory_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &extMem, sizeof(cudaExternalMemory_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDestroyExternalMemory");
  return_value = fn == nullptr ? function_not_found() : fn(extMem);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDestroyExternalSemaphore(conn_t *conn) {
  cudaExternalSemaphore_t extSem;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExternalSemaphore_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &extSem, sizeof(cudaExternalSemaphore_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDestroyExternalSemaphore");
  return_value = fn == nullptr ? function_not_found() : fn(extSem);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDestroySurfaceObject(conn_t *conn) {
  cudaSurfaceObject_t surfObject;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaSurfaceObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &surfObject, sizeof(cudaSurfaceObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDestroySurfaceObject");
  return_value = fn == nullptr ? function_not_found() : fn(surfObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDestroyTextureObject(conn_t *conn) {
  cudaTextureObject_t texObject;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaTextureObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &texObject, sizeof(cudaTextureObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDestroyTextureObject");
  return_value = fn == nullptr ? function_not_found() : fn(texObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaDevResourceGenerateDesc(conn_t *conn) {
  unsigned int nbResources;
  cudaDevResource *resources = nullptr;
  size_t resources_size;
  cudaDevResourceDesc_t phDesc{};
  phDesc = {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaDevResourceDesc_t *, cudaDevResource *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nbResources, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  resources_size = nbResources * sizeof(cudaDevResource);
  resources = (cudaDevResource *)malloc(resources_size);
  if (resources_size != 0 && resources == nullptr)
    goto ERROR_0;
  if ((resources_size != 0 && rpc_read(conn, resources, resources_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDevResourceGenerateDesc");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&phDesc,
               (nbResources * sizeof(cudaDevResource) == 0 ? nullptr
                                                           : resources),
               nbResources);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &phDesc, sizeof(cudaDevResourceDesc_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)resources);
  return 0;
ERROR_0:
  free((void *)resources);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaDevSmResourceSplit(conn_t *conn) {
  unsigned int nbGroups;
  cudaDevResource *result = nullptr;
  uint8_t result_null = 0;
  cudaDevResource input{};
  cudaDevResource *remainder_null_check;
  cudaDevResource remainder;
  unsigned int flags;
  cudaDevSmResourceGroupParams *groupParams = nullptr;
  size_t groupParams_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaDevResource *, unsigned int,
                               const cudaDevResource *, cudaDevResource *,
                               unsigned int, cudaDevSmResourceGroupParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nbGroups, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &result_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!result_null) {
    result = (cudaDevResource *)malloc((nbGroups != 0 ? nbGroups : 1) *
                                       sizeof(cudaDevResource));
    if (result == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &input, sizeof(const cudaDevResource)) < 0 ||
      rpc_read(conn, &remainder_null_check, sizeof(cudaDevResource *)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  groupParams_size = nbGroups * sizeof(cudaDevSmResourceGroupParams);
  groupParams = (cudaDevSmResourceGroupParams *)malloc(groupParams_size);
  if (groupParams_size != 0 && groupParams == nullptr)
    goto ERROR_0;
  if ((groupParams_size != 0 &&
       rpc_read(conn, groupParams, groupParams_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDevSmResourceSplit");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(result, nbGroups, &input,
                          remainder_null_check ? &remainder : nullptr, flags,
                          (nbGroups * sizeof(cudaDevSmResourceGroupParams) == 0
                               ? nullptr
                               : groupParams));

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!result_null &&
       rpc_write(conn, result, nbGroups * sizeof(cudaDevResource)) < 0) ||
      rpc_write(conn, &remainder_null_check, sizeof(cudaDevResource *)) < 0 ||
      (remainder_null_check &&
       rpc_write(conn, &remainder, sizeof(cudaDevResource)) < 0) ||
      rpc_write(conn, groupParams,
                nbGroups * sizeof(cudaDevSmResourceGroupParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)groupParams);
  free((void *)result);
  return 0;
ERROR_0:
  free((void *)groupParams);
  free((void *)result);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaDevSmResourceSplitByCount(conn_t *conn) {
  unsigned int nbGroups = 0;
  unsigned int nbGroups_requested = 0;
  cudaDevResource *result = nullptr;
  uint8_t result_null = 0;
  cudaDevResource input{};
  cudaDevResource *remaining_null_check;
  cudaDevResource remaining;
  unsigned int flags;
  unsigned int minCount;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaDevResource *, unsigned int *,
                               const cudaDevResource *, cudaDevResource *,
                               unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &nbGroups_requested, sizeof(unsigned int)) < 0 ||
      ((nbGroups = nbGroups_requested), false) ||
      rpc_read(conn, &result_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!result_null) {
    result = (cudaDevResource *)malloc(
        (nbGroups_requested != 0 ? nbGroups_requested : 1) *
        sizeof(cudaDevResource));
    if (result == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &input, sizeof(const cudaDevResource)) < 0 ||
      rpc_read(conn, &remaining_null_check, sizeof(cudaDevResource *)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &minCount, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDevSmResourceSplitByCount");
  return_value = fn == nullptr ? function_not_found()
                               : fn(result, &nbGroups, &input,
                                    remaining_null_check ? &remaining : nullptr,
                                    flags, minCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nbGroups, sizeof(unsigned int)) < 0 ||
      (!result_null &&
       rpc_write(
           conn, result,
           (nbGroups < nbGroups_requested ? nbGroups : nbGroups_requested) *
               sizeof(cudaDevResource)) < 0) ||
      rpc_write(conn, &remaining_null_check, sizeof(cudaDevResource *)) < 0 ||
      (remaining_null_check &&
       rpc_write(conn, &remaining, sizeof(cudaDevResource)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)result);
  return 0;
ERROR_0:
  free((void *)result);
  return -1;
}

#endif

int handle_cudaDeviceCanAccessPeer(conn_t *conn) {
  int canAccessPeer{};
  canAccessPeer = {};
  int device;
  int peerDevice;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &peerDevice, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceCanAccessPeer");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&canAccessPeer, device, peerDevice);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &canAccessPeer, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceDisablePeerAccess(conn_t *conn) {
  int peerDevice;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &peerDevice, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceDisablePeerAccess");
  return_value = fn == nullptr ? function_not_found() : fn(peerDevice);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceEnablePeerAccess(conn_t *conn) {
  int peerDevice;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &peerDevice, sizeof(int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceEnablePeerAccess");
  return_value = fn == nullptr ? function_not_found() : fn(peerDevice, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceFlushGPUDirectRDMAWrites(conn_t *conn) {
  enum cudaFlushGPUDirectRDMAWritesTarget target;
  enum cudaFlushGPUDirectRDMAWritesScope scope;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaFlushGPUDirectRDMAWritesTarget,
                               enum cudaFlushGPUDirectRDMAWritesScope);
  fn_t fn = nullptr;
  if (rpc_read(conn, &target, sizeof(enum cudaFlushGPUDirectRDMAWritesTarget)) <
          0 ||
      rpc_read(conn, &scope, sizeof(enum cudaFlushGPUDirectRDMAWritesScope)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceFlushGPUDirectRDMAWrites");
  return_value = fn == nullptr ? function_not_found() : fn(target, scope);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetAttribute(conn_t *conn) {
  int value{};
  value = {};
  enum cudaDeviceAttr attr;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, enum cudaDeviceAttr, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attr, sizeof(enum cudaDeviceAttr)) < 0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(&value, attr, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetByPCIBusId(conn_t *conn) {
  int device{};
  device = {};
  const char *pciBusId = nullptr;
  std::size_t pciBusId_len;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pciBusId_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  pciBusId = (const char *)malloc(pciBusId_len);
  if ((pciBusId_len != 0 && pciBusId == nullptr) ||
      rpc_read(conn, (void *)pciBusId, pciBusId_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetByPCIBusId");
  return_value = fn == nullptr ? function_not_found() : fn(&device, pciBusId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pciBusId);
  return 0;
ERROR_0:
  free((void *)pciBusId);
  return -1;
}

int handle_cudaDeviceGetCacheConfig(conn_t *conn) {
  enum cudaFuncCache pCacheConfig {};
  pCacheConfig = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaFuncCache *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetCacheConfig");
  return_value = fn == nullptr ? function_not_found() : fn(&pCacheConfig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pCacheConfig, sizeof(enum cudaFuncCache)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetDefaultMemPool(conn_t *conn) {
  cudaMemPool_t memPool{};
  memPool = {};
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetDefaultMemPool");
  return_value = fn == nullptr ? function_not_found() : fn(&memPool, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaDeviceGetDevResource(conn_t *conn) {
  int device;
  cudaDevResource resource{};
  resource = {};
  enum cudaDevResourceType type;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(int, cudaDevResource *, enum cudaDevResourceType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaDevResourceType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetDevResource");
  return_value =
      fn == nullptr ? function_not_found() : fn(device, &resource, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resource, sizeof(cudaDevResource)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaDeviceGetExecutionCtx(conn_t *conn) {
  cudaExecutionContext_t ctx{};
  ctx = {};
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetExecutionCtx");
  return_value = fn == nullptr ? function_not_found() : fn(&ctx, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaDeviceGetGraphMemAttribute(conn_t *conn) {
  int device;
  enum cudaGraphMemAttributeType attr;
  void *value = nullptr;
  size_t value_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, enum cudaGraphMemAttributeType, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaGraphMemAttributeType)) < 0 ||
      false)
    goto ERROR_0;
  value_size = 8;
  value = (void *)malloc(value_size);
  if (value_size != 0 && value == nullptr)
    goto ERROR_0;
  if ((value_size != 0 && rpc_read(conn, value, value_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetGraphMemAttribute");
  return_value = fn == nullptr ? function_not_found() : fn(device, attr, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, value, 8) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)value);
  return 0;
ERROR_0:
  free((void *)value);
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaDeviceGetHostAtomicCapabilities(conn_t *conn) {
  unsigned int count;
  unsigned int *capabilities = nullptr;
  enum cudaAtomicOperation *operations = nullptr;
  size_t operations_size;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(unsigned int *, const enum cudaAtomicOperation *,
                               unsigned int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  capabilities = (unsigned int *)malloc(count * sizeof(unsigned int));
  if ((count * sizeof(unsigned int) != 0 && capabilities == nullptr) || false)
    goto ERROR_0;
  operations_size = count * sizeof(const enum cudaAtomicOperation);
  operations = (enum cudaAtomicOperation *)malloc(operations_size);
  if (operations_size != 0 && operations == nullptr)
    goto ERROR_0;
  if ((operations_size != 0 &&
       rpc_read(conn, operations, operations_size) < 0) ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetHostAtomicCapabilities");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((count * sizeof(unsigned int) == 0 ? nullptr : capabilities),
               (count * sizeof(const enum cudaAtomicOperation) == 0
                    ? nullptr
                    : operations),
               count, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, capabilities, count * sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)operations);
  free((void *)capabilities);
  return 0;
ERROR_0:
  free((void *)operations);
  free((void *)capabilities);
  return -1;
}

#endif

int handle_cudaDeviceGetLimit(conn_t *conn) {
  size_t pValue{};
  pValue = {};
  enum cudaLimit limit;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(size_t *, enum cudaLimit);
  fn_t fn = nullptr;
  if (rpc_read(conn, &limit, sizeof(enum cudaLimit)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetLimit");
  return_value = fn == nullptr ? function_not_found() : fn(&pValue, limit);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pValue, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetMemPool(conn_t *conn) {
  cudaMemPool_t memPool{};
  memPool = {};
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetMemPool");
  return_value = fn == nullptr ? function_not_found() : fn(&memPool, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaDeviceGetP2PAtomicCapabilities(conn_t *conn) {
  unsigned int count;
  unsigned int *capabilities = nullptr;
  enum cudaAtomicOperation *operations = nullptr;
  size_t operations_size;
  int srcDevice;
  int dstDevice;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(unsigned int *, const enum cudaAtomicOperation *,
                               unsigned int, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  capabilities = (unsigned int *)malloc(count * sizeof(unsigned int));
  if ((count * sizeof(unsigned int) != 0 && capabilities == nullptr) || false)
    goto ERROR_0;
  operations_size = count * sizeof(const enum cudaAtomicOperation);
  operations = (enum cudaAtomicOperation *)malloc(operations_size);
  if (operations_size != 0 && operations == nullptr)
    goto ERROR_0;
  if ((operations_size != 0 &&
       rpc_read(conn, operations, operations_size) < 0) ||
      rpc_read(conn, &srcDevice, sizeof(int)) < 0 ||
      rpc_read(conn, &dstDevice, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetP2PAtomicCapabilities");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((count * sizeof(unsigned int) == 0 ? nullptr : capabilities),
               (count * sizeof(const enum cudaAtomicOperation) == 0
                    ? nullptr
                    : operations),
               count, srcDevice, dstDevice);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, capabilities, count * sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)operations);
  free((void *)capabilities);
  return 0;
ERROR_0:
  free((void *)operations);
  free((void *)capabilities);
  return -1;
}

#endif

int handle_cudaDeviceGetP2PAttribute(conn_t *conn) {
  int value{};
  value = {};
  enum cudaDeviceP2PAttr attr;
  int srcDevice;
  int dstDevice;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, enum cudaDeviceP2PAttr, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &attr, sizeof(enum cudaDeviceP2PAttr)) < 0 ||
      rpc_read(conn, &srcDevice, sizeof(int)) < 0 ||
      rpc_read(conn, &dstDevice, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetP2PAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&value, attr, srcDevice, dstDevice);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetPCIBusId(conn_t *conn) {
  int len;
  char *pciBusId = nullptr;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(char *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &len, sizeof(int)) < 0 || len < 0 || false)
    goto ERROR_0;
  pciBusId = (char *)malloc(len * sizeof(char));
  if ((len * sizeof(char) != 0 && pciBusId == nullptr) ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetPCIBusId");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((len * sizeof(char) == 0 ? nullptr : pciBusId), len, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, pciBusId, len * sizeof(char)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pciBusId);
  return 0;
ERROR_0:
  free((void *)pciBusId);
  return -1;
}

int handle_cudaDeviceGetStreamPriorityRange(conn_t *conn) {
  int leastPriority{};
  leastPriority = {};
  int greatestPriority{};
  greatestPriority = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetStreamPriorityRange");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&leastPriority, &greatestPriority);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &leastPriority, sizeof(int)) < 0 ||
      rpc_write(conn, &greatestPriority, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGetTexture1DLinearMaxWidth(conn_t *conn) {
  size_t maxWidthInElements{};
  maxWidthInElements = {};
  struct cudaChannelFormatDesc fmtDesc {};
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(size_t *, const struct cudaChannelFormatDesc *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fmtDesc, sizeof(const struct cudaChannelFormatDesc)) <
          0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGetTexture1DLinearMaxWidth");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&maxWidthInElements, &fmtDesc, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &maxWidthInElements, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceGraphMemTrim(conn_t *conn) {
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceGraphMemTrim");
  return_value = fn == nullptr ? function_not_found() : fn(device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaDeviceRegisterAsyncNotification(conn_t *conn) {
  int device;
  cudaAsyncCallback callbackFunc;
  void *userData;
  cudaAsyncCallbackHandle_t callback{};
  callback = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, cudaAsyncCallback, void *,
                               cudaAsyncCallbackHandle_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &callbackFunc, sizeof(cudaAsyncCallback)) < 0 ||
      rpc_read(conn, &userData, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceRegisterAsyncNotification");
  return_value = fn == nullptr ? function_not_found()
                               : fn(device, callbackFunc, userData, &callback);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &callback, sizeof(cudaAsyncCallbackHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaDeviceReset(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceReset");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceSetCacheConfig(conn_t *conn) {
  enum cudaFuncCache cacheConfig;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaFuncCache);
  fn_t fn = nullptr;
  if (rpc_read(conn, &cacheConfig, sizeof(enum cudaFuncCache)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceSetCacheConfig");
  return_value = fn == nullptr ? function_not_found() : fn(cacheConfig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceSetGraphMemAttribute(conn_t *conn) {
  int device;
  enum cudaGraphMemAttributeType attr;
  void *value = nullptr;
  size_t value_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, enum cudaGraphMemAttributeType, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaGraphMemAttributeType)) < 0 ||
      false)
    goto ERROR_0;
  value_size = 8;
  value = (void *)malloc(value_size);
  if (value_size != 0 && value == nullptr)
    goto ERROR_0;
  if ((value_size != 0 && rpc_read(conn, value, value_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceSetGraphMemAttribute");
  return_value = fn == nullptr ? function_not_found() : fn(device, attr, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)value);
  return 0;
ERROR_0:
  free((void *)value);
  return -1;
}

int handle_cudaDeviceSetLimit(conn_t *conn) {
  enum cudaLimit limit;
  size_t value;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaLimit, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &limit, sizeof(enum cudaLimit)) < 0 ||
      rpc_read(conn, &value, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceSetLimit");
  return_value = fn == nullptr ? function_not_found() : fn(limit, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaDeviceSetMemPool(conn_t *conn) {
  int device;
  cudaMemPool_t memPool;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, cudaMemPool_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceSetMemPool");
  return_value = fn == nullptr ? function_not_found() : fn(device, memPool);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaDeviceUnregisterAsyncNotification(conn_t *conn) {
  int device;
  cudaAsyncCallbackHandle_t callback;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, cudaAsyncCallbackHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &callback, sizeof(cudaAsyncCallbackHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDeviceUnregisterAsyncNotification");
  return_value = fn == nullptr ? function_not_found() : fn(device, callback);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaDriverGetVersion(conn_t *conn) {
  int driverVersion{};
  driverVersion = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaDriverGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(&driverVersion);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &driverVersion, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaEventCreate(conn_t *conn) {
  cudaEvent_t event{};
  event = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaEvent_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaEventCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &event, sizeof(cudaEvent_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaEventCreateWithFlags(conn_t *conn) {
  cudaEvent_t event{};
  event = {};
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaEvent_t *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaEventCreateWithFlags");
  return_value = fn == nullptr ? function_not_found() : fn(&event, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &event, sizeof(cudaEvent_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaEventElapsedTime(conn_t *conn) {
  float ms{};
  ms = {};
  cudaEvent_t start;
  cudaEvent_t end;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(float *, cudaEvent_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &start, sizeof(cudaEvent_t)) < 0 ||
      rpc_read(conn, &end, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaEventElapsedTime");
  return_value = fn == nullptr ? function_not_found() : fn(&ms, start, end);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ms, sizeof(float)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxDestroy(conn_t *conn) {
  cudaExecutionContext_t ctx;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(ctx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxGetDevResource(conn_t *conn) {
  cudaExecutionContext_t ctx;
  cudaDevResource resource{};
  resource = {};
  enum cudaDevResourceType type;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t, cudaDevResource *,
                               enum cudaDevResourceType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaDevResourceType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxGetDevResource");
  return_value =
      fn == nullptr ? function_not_found() : fn(ctx, &resource, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resource, sizeof(cudaDevResource)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxGetDevice(conn_t *conn) {
  int device{};
  device = {};
  cudaExecutionContext_t ctx;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, cudaExecutionContext_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxGetDevice");
  return_value = fn == nullptr ? function_not_found() : fn(&device, ctx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxGetId(conn_t *conn) {
  cudaExecutionContext_t ctx;
  unsigned long long ctxId{};
  ctxId = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t, unsigned long long *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxGetId");
  return_value = fn == nullptr ? function_not_found() : fn(ctx, &ctxId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ctxId, sizeof(unsigned long long)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxRecordEvent(conn_t *conn) {
  cudaExecutionContext_t ctx;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxRecordEvent");
  return_value = fn == nullptr ? function_not_found() : fn(ctx, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxStreamCreate(conn_t *conn) {
  cudaStream_t phStream{};
  phStream = {};
  cudaExecutionContext_t ctx;
  unsigned int flags;
  int priority;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t *, cudaExecutionContext_t,
                               unsigned int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &priority, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxStreamCreate");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&phStream, ctx, flags, priority);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &phStream, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxSynchronize(conn_t *conn) {
  cudaExecutionContext_t ctx;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxSynchronize");
  return_value = fn == nullptr ? function_not_found() : fn(ctx);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaExecutionCtxWaitEvent(conn_t *conn) {
  cudaExecutionContext_t ctx;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExecutionCtxWaitEvent");
  return_value = fn == nullptr ? function_not_found() : fn(ctx, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaExternalMemoryGetMappedBuffer(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  cudaExternalMemory_t extMem;
  struct cudaExternalMemoryBufferDesc bufferDesc {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, cudaExternalMemory_t,
                               const struct cudaExternalMemoryBufferDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &extMem, sizeof(cudaExternalMemory_t)) < 0 ||
      rpc_read(conn, &bufferDesc,
               sizeof(const struct cudaExternalMemoryBufferDesc)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExternalMemoryGetMappedBuffer");
  return_value =
      fn == nullptr ? function_not_found() : fn(&devPtr, extMem, &bufferDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaExternalMemoryGetMappedMipmappedArray(conn_t *conn) {
  cudaMipmappedArray_t mipmap{};
  mipmap = {};
  cudaExternalMemory_t extMem;
  struct cudaExternalMemoryMipmappedArrayDesc mipmapDesc {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaMipmappedArray_t *, cudaExternalMemory_t,
                      const struct cudaExternalMemoryMipmappedArrayDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &extMem, sizeof(cudaExternalMemory_t)) < 0 ||
      rpc_read(conn, &mipmapDesc,
               sizeof(const struct cudaExternalMemoryMipmappedArrayDesc)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaExternalMemoryGetMappedMipmappedArray");
  return_value =
      fn == nullptr ? function_not_found() : fn(&mipmap, extMem, &mipmapDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mipmap, sizeof(cudaMipmappedArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaFreeArray(conn_t *conn) {
  cudaArray_t array;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaArray_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &array, sizeof(cudaArray_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFreeArray");
  return_value = fn == nullptr ? function_not_found() : fn(array);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaFreeAsync(conn_t *conn) {
  void *devPtr;
  cudaStream_t hStream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFreeAsync");
  return_value = fn == nullptr ? function_not_found() : fn(devPtr, hStream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaFreeMipmappedArray(conn_t *conn) {
  cudaMipmappedArray_t mipmappedArray;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMipmappedArray_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mipmappedArray, sizeof(cudaMipmappedArray_t)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFreeMipmappedArray");
  return_value = fn == nullptr ? function_not_found() : fn(mipmappedArray);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaFuncGetAttributes(conn_t *conn) {
  struct cudaFuncAttributes attr {};
  attr = {};
  const void *func;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaFuncAttributes *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFuncGetAttributes");
  return_value = fn == nullptr ? function_not_found() : fn(&attr, func);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &attr, sizeof(struct cudaFuncAttributes)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaFuncGetParamCount(conn_t *conn) {
  const void *func;
  size_t paramCount{};
  paramCount = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFuncGetParamCount");
  return_value = fn == nullptr ? function_not_found() : fn(func, &paramCount);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &paramCount, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 12000
int handle_cudaFuncGetParamInfo(conn_t *conn) {
  const void *func;
  size_t paramIndex;
  size_t paramOffset{};
  paramOffset = {};
  size_t paramSize{};
  paramSize = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, size_t, size_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &paramIndex, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFuncGetParamInfo");
  return_value = fn == nullptr ? function_not_found()
                               : fn(func, paramIndex, &paramOffset, &paramSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &paramOffset, sizeof(size_t)) < 0 ||
      rpc_write(conn, &paramSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaFuncSetAttribute(conn_t *conn) {
  const void *func;
  enum cudaFuncAttribute attr;
  int value;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, enum cudaFuncAttribute, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaFuncAttribute)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFuncSetAttribute");
  return_value = fn == nullptr ? function_not_found() : fn(func, attr, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaFuncSetCacheConfig(conn_t *conn) {
  const void *func;
  enum cudaFuncCache cacheConfig;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, enum cudaFuncCache);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &cacheConfig, sizeof(enum cudaFuncCache)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaFuncSetCacheConfig");
  return_value = fn == nullptr ? function_not_found() : fn(func, cacheConfig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetChannelDesc(conn_t *conn) {
  struct cudaChannelFormatDesc desc {};
  desc = {};
  cudaArray_const_t array;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(struct cudaChannelFormatDesc *, cudaArray_const_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &array, sizeof(cudaArray_const_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetChannelDesc");
  return_value = fn == nullptr ? function_not_found() : fn(&desc, array);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &desc, sizeof(struct cudaChannelFormatDesc)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetDevice(conn_t *conn) {
  int device{};
  device = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDevice");
  return_value = fn == nullptr ? function_not_found() : fn(&device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetDeviceCount(conn_t *conn) {
  int count{};
  count = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDeviceCount");
  return_value = fn == nullptr ? function_not_found() : fn(&count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetDeviceFlags(conn_t *conn) {
  unsigned int flags{};
  flags = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(unsigned int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDeviceFlags");
  return_value = fn == nullptr ? function_not_found() : fn(&flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsGLRegisterBuffer(conn_t *conn) {
  struct cudaGraphicsResource *resource{};
  resource = {};
  unsigned int buffer;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaGraphicsResource **, unsigned int,
                               unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &buffer, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsGLRegisterBuffer");
  return_value =
      fn == nullptr ? function_not_found() : fn(&resource, buffer, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resource, sizeof(struct cudaGraphicsResource *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsGLRegisterImage(conn_t *conn) {
  struct cudaGraphicsResource *resource{};
  resource = {};
  unsigned int image;
  unsigned int target;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaGraphicsResource **, unsigned int,
                               unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &image, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &target, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsGLRegisterImage");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&resource, image, target, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resource, sizeof(struct cudaGraphicsResource *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetDeviceProperties(conn_t *conn) {
  struct cudaDeviceProp prop {};
  prop = {};
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaDeviceProp *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDeviceProperties");
  return_value = fn == nullptr ? function_not_found() : fn(&prop, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &prop, sizeof(struct cudaDeviceProp)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGetDriverEntryPoint(conn_t *conn) {
  const char *symbol = nullptr;
  std::size_t symbol_len;
  void *funcPtr{};
  funcPtr = {};
  unsigned long long flags;
  enum cudaDriverEntryPointQueryResult *driverStatus_null_check;
  enum cudaDriverEntryPointQueryResult driverStatus;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const char *, void **, unsigned long long,
                               enum cudaDriverEntryPointQueryResult *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &symbol_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  symbol = (const char *)malloc(symbol_len);
  if ((symbol_len != 0 && symbol == nullptr) ||
      rpc_read(conn, (void *)symbol, symbol_len) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &driverStatus_null_check,
               sizeof(enum cudaDriverEntryPointQueryResult *)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDriverEntryPoint");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(symbol, &funcPtr, flags,
                          driverStatus_null_check ? &driverStatus : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &funcPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &driverStatus_null_check,
                sizeof(enum cudaDriverEntryPointQueryResult *)) < 0 ||
      (driverStatus_null_check &&
       rpc_write(conn, &driverStatus,
                 sizeof(enum cudaDriverEntryPointQueryResult)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)symbol);
  return 0;
ERROR_0:
  free((void *)symbol);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGetDriverEntryPointByVersion(conn_t *conn) {
  const char *symbol = nullptr;
  std::size_t symbol_len;
  void *funcPtr{};
  funcPtr = {};
  unsigned int cudaVersion;
  unsigned long long flags;
  enum cudaDriverEntryPointQueryResult *driverStatus_null_check;
  enum cudaDriverEntryPointQueryResult driverStatus;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(const char *, void **, unsigned int, unsigned long long,
                      enum cudaDriverEntryPointQueryResult *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &symbol_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  symbol = (const char *)malloc(symbol_len);
  if ((symbol_len != 0 && symbol == nullptr) ||
      rpc_read(conn, (void *)symbol, symbol_len) < 0 ||
      rpc_read(conn, &cudaVersion, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &driverStatus_null_check,
               sizeof(enum cudaDriverEntryPointQueryResult *)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetDriverEntryPointByVersion");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(symbol, &funcPtr, cudaVersion, flags,
                          driverStatus_null_check ? &driverStatus : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &funcPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &driverStatus_null_check,
                sizeof(enum cudaDriverEntryPointQueryResult *)) < 0 ||
      (driverStatus_null_check &&
       rpc_write(conn, &driverStatus,
                 sizeof(enum cudaDriverEntryPointQueryResult)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)symbol);
  return 0;
ERROR_0:
  free((void *)symbol);
  return -1;
}

#endif

int handle_cudaGetExportTable(conn_t *conn) {
  const void *ppExportTable{};
  ppExportTable = {};
  cudaUUID_t pExportTableId{};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void **, const cudaUUID_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pExportTableId, sizeof(const cudaUUID_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetExportTable");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&ppExportTable, &pExportTableId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ppExportTable, sizeof(const void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetFuncBySymbol(conn_t *conn) {
  cudaFunction_t functionPtr{};
  functionPtr = {};
  const void *symbolPtr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaFunction_t *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &symbolPtr, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetFuncBySymbol");
  return_value =
      fn == nullptr ? function_not_found() : fn(&functionPtr, symbolPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &functionPtr, sizeof(cudaFunction_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGetKernel(conn_t *conn) {
  cudaKernel_t kernelPtr{};
  kernelPtr = {};
  const void *entryFuncAddr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaKernel_t *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &entryFuncAddr, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetKernel");
  return_value =
      fn == nullptr ? function_not_found() : fn(&kernelPtr, entryFuncAddr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &kernelPtr, sizeof(cudaKernel_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGetLastError(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetLastError");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetMipmappedArrayLevel(conn_t *conn) {
  cudaArray_t levelArray{};
  levelArray = {};
  cudaMipmappedArray_const_t mipmappedArray;
  unsigned int level;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaArray_t *, cudaMipmappedArray_const_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mipmappedArray, sizeof(cudaMipmappedArray_const_t)) < 0 ||
      rpc_read(conn, &level, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetMipmappedArrayLevel");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&levelArray, mipmappedArray, level);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &levelArray, sizeof(cudaArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetSurfaceObjectResourceDesc(conn_t *conn) {
  struct cudaResourceDesc pResDesc {};
  pResDesc = {};
  cudaSurfaceObject_t surfObject;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaResourceDesc *, cudaSurfaceObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &surfObject, sizeof(cudaSurfaceObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetSurfaceObjectResourceDesc");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pResDesc, surfObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pResDesc, sizeof(struct cudaResourceDesc)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetSymbolAddress(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  const void *symbol;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &symbol, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetSymbolAddress");
  return_value = fn == nullptr ? function_not_found() : fn(&devPtr, symbol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetSymbolSize(conn_t *conn) {
  size_t size{};
  size = {};
  const void *symbol;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(size_t *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &symbol, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetSymbolSize");
  return_value = fn == nullptr ? function_not_found() : fn(&size, symbol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetTextureObjectResourceDesc(conn_t *conn) {
  struct cudaResourceDesc pResDesc {};
  pResDesc = {};
  cudaTextureObject_t texObject;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaResourceDesc *, cudaTextureObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &texObject, sizeof(cudaTextureObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetTextureObjectResourceDesc");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pResDesc, texObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pResDesc, sizeof(struct cudaResourceDesc)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetTextureObjectResourceViewDesc(conn_t *conn) {
  struct cudaResourceViewDesc pResViewDesc {};
  pResViewDesc = {};
  cudaTextureObject_t texObject;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(struct cudaResourceViewDesc *, cudaTextureObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &texObject, sizeof(cudaTextureObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetTextureObjectResourceViewDesc");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pResViewDesc, texObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pResViewDesc, sizeof(struct cudaResourceViewDesc)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGetTextureObjectTextureDesc(conn_t *conn) {
  struct cudaTextureDesc pTexDesc {};
  pTexDesc = {};
  cudaTextureObject_t texObject;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaTextureDesc *, cudaTextureObject_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &texObject, sizeof(cudaTextureObject_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGetTextureObjectTextureDesc");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pTexDesc, texObject);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pTexDesc, sizeof(struct cudaTextureDesc)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGreenCtxCreate(conn_t *conn) {
  cudaExecutionContext_t phCtx{};
  phCtx = {};
  cudaDevResourceDesc_t desc;
  int device;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExecutionContext_t *, cudaDevResourceDesc_t,
                               int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(cudaDevResourceDesc_t)) < 0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGreenCtxCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(&phCtx, desc, device, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &phCtx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaHostGetDevicePointer(conn_t *conn) {
  void *pDevice{};
  pDevice = {};
  void *pHost;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, void *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pHost, sizeof(void *)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaHostGetDevicePointer");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pDevice, pHost, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pDevice, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaImportExternalMemory(conn_t *conn) {
  cudaExternalMemory_t extMem_out{};
  extMem_out = {};
  struct cudaExternalMemoryHandleDesc memHandleDesc {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExternalMemory_t *,
                               const struct cudaExternalMemoryHandleDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memHandleDesc,
               sizeof(const struct cudaExternalMemoryHandleDesc)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaImportExternalMemory");
  return_value =
      fn == nullptr ? function_not_found() : fn(&extMem_out, &memHandleDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &extMem_out, sizeof(cudaExternalMemory_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaImportExternalSemaphore(conn_t *conn) {
  cudaExternalSemaphore_t extSem_out{};
  extSem_out = {};
  struct cudaExternalSemaphoreHandleDesc semHandleDesc {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaExternalSemaphore_t *,
                               const struct cudaExternalSemaphoreHandleDesc *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &semHandleDesc,
               sizeof(const struct cudaExternalSemaphoreHandleDesc)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaImportExternalSemaphore");
  return_value =
      fn == nullptr ? function_not_found() : fn(&extSem_out, &semHandleDesc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &extSem_out, sizeof(cudaExternalSemaphore_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaInitDevice(conn_t *conn) {
  int device;
  unsigned int deviceFlags;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 ||
      rpc_read(conn, &deviceFlags, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaInitDevice");
  return_value =
      fn == nullptr ? function_not_found() : fn(device, deviceFlags, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaIpcCloseMemHandle(conn_t *conn) {
  void *devPtr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaIpcCloseMemHandle");
  return_value = fn == nullptr ? function_not_found() : fn(devPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaIpcGetEventHandle(conn_t *conn) {
  cudaIpcEventHandle_t handle{};
  handle = {};
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaIpcEventHandle_t *, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaIpcGetEventHandle");
  return_value = fn == nullptr ? function_not_found() : fn(&handle, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cudaIpcEventHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaIpcGetMemHandle(conn_t *conn) {
  cudaIpcMemHandle_t handle{};
  handle = {};
  void *devPtr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaIpcMemHandle_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaIpcGetMemHandle");
  return_value = fn == nullptr ? function_not_found() : fn(&handle, devPtr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &handle, sizeof(cudaIpcMemHandle_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaIpcOpenEventHandle(conn_t *conn) {
  cudaEvent_t event{};
  event = {};
  cudaIpcEventHandle_t handle;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaEvent_t *, cudaIpcEventHandle_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudaIpcEventHandle_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaIpcOpenEventHandle");
  return_value = fn == nullptr ? function_not_found() : fn(&event, handle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &event, sizeof(cudaEvent_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaIpcOpenMemHandle(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  cudaIpcMemHandle_t handle;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, cudaIpcMemHandle_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &handle, sizeof(cudaIpcMemHandle_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaIpcOpenMemHandle");
  return_value =
      fn == nullptr ? function_not_found() : fn(&devPtr, handle, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaKernelSetAttributeForDevice(conn_t *conn) {
  cudaKernel_t kernel;
  enum cudaFuncAttribute attr;
  int value;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaKernel_t, enum cudaFuncAttribute, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &kernel, sizeof(cudaKernel_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaFuncAttribute)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaKernelSetAttributeForDevice");
  return_value =
      fn == nullptr ? function_not_found() : fn(kernel, attr, value, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryEnumerateKernels(conn_t *conn) {
  unsigned int numKernels;
  cudaKernel_t *kernels = nullptr;
  cudaLibrary_t lib;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaKernel_t *, unsigned int, cudaLibrary_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numKernels, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  kernels = (cudaKernel_t *)malloc(numKernels * sizeof(cudaKernel_t));
  if ((numKernels * sizeof(cudaKernel_t) != 0 && kernels == nullptr) ||
      rpc_read(conn, &lib, sizeof(cudaLibrary_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryEnumerateKernels");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((numKernels * sizeof(cudaKernel_t) == 0 ? nullptr : kernels),
               numKernels, lib);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, kernels, numKernels * sizeof(cudaKernel_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)kernels);
  return 0;
ERROR_0:
  free((void *)kernels);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryGetGlobal(conn_t *conn) {
  void *dptr{};
  dptr = {};
  size_t bytes{};
  bytes = {};
  cudaLibrary_t library;
  const char *name = nullptr;
  std::size_t name_len;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, cudaLibrary_t, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_read(conn, &name_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  name = (const char *)malloc(name_len);
  if ((name_len != 0 && name == nullptr) ||
      rpc_read(conn, (void *)name, name_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryGetGlobal");
  return_value =
      fn == nullptr ? function_not_found() : fn(&dptr, &bytes, library, name);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)name);
  return 0;
ERROR_0:
  free((void *)name);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryGetKernel(conn_t *conn) {
  cudaKernel_t pKernel{};
  pKernel = {};
  cudaLibrary_t library;
  const char *name = nullptr;
  std::size_t name_len;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaKernel_t *, cudaLibrary_t, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_read(conn, &name_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  name = (const char *)malloc(name_len);
  if ((name_len != 0 && name == nullptr) ||
      rpc_read(conn, (void *)name, name_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryGetKernel");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pKernel, library, name);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pKernel, sizeof(cudaKernel_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)name);
  return 0;
ERROR_0:
  free((void *)name);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryGetKernelCount(conn_t *conn) {
  unsigned int count{};
  count = {};
  cudaLibrary_t lib;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(unsigned int *, cudaLibrary_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &lib, sizeof(cudaLibrary_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryGetKernelCount");
  return_value = fn == nullptr ? function_not_found() : fn(&count, lib);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &count, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryGetManaged(conn_t *conn) {
  void *dptr{};
  dptr = {};
  size_t bytes{};
  bytes = {};
  cudaLibrary_t library;
  const char *name = nullptr;
  std::size_t name_len;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, cudaLibrary_t, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_read(conn, &name_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  name = (const char *)malloc(name_len);
  if ((name_len != 0 && name == nullptr) ||
      rpc_read(conn, (void *)name, name_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryGetManaged");
  return_value =
      fn == nullptr ? function_not_found() : fn(&dptr, &bytes, library, name);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &bytes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)name);
  return 0;
ERROR_0:
  free((void *)name);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryGetUnifiedFunction(conn_t *conn) {
  void *fptr{};
  fptr = {};
  cudaLibrary_t library;
  const char *symbol = nullptr;
  std::size_t symbol_len;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, cudaLibrary_t, const char *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_read(conn, &symbol_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  symbol = (const char *)malloc(symbol_len);
  if ((symbol_len != 0 && symbol == nullptr) ||
      rpc_read(conn, (void *)symbol, symbol_len) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryGetUnifiedFunction");
  return_value =
      fn == nullptr ? function_not_found() : fn(&fptr, library, symbol);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &fptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)symbol);
  return 0;
ERROR_0:
  free((void *)symbol);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryLoadData(conn_t *conn) {
  cudaLibrary_t library{};
  library = {};
  const void *code;
  unsigned int numJitOptions;
  enum cudaJitOption *jitOptions = nullptr;
  size_t jitOptions_size;
  void **jitOptionsValues = nullptr;
  size_t jitOptionsValues_size;
  unsigned int numLibraryOptions;
  enum cudaLibraryOption *libraryOptions = nullptr;
  size_t libraryOptions_size;
  void **libraryOptionValues = nullptr;
  size_t libraryOptionValues_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLibrary_t *, const void *,
                               enum cudaJitOption *, void **, unsigned int,
                               enum cudaLibraryOption *, void **, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &code, sizeof(const void *)) < 0 ||
      rpc_read(conn, &numJitOptions, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  jitOptions_size = numJitOptions * sizeof(enum cudaJitOption);
  jitOptions = (enum cudaJitOption *)malloc(jitOptions_size);
  if (jitOptions_size != 0 && jitOptions == nullptr)
    goto ERROR_0;
  if ((jitOptions_size != 0 &&
       rpc_read(conn, jitOptions, jitOptions_size) < 0) ||
      false)
    goto ERROR_0;
  jitOptionsValues_size = numJitOptions * sizeof(void *);
  jitOptionsValues = (void **)malloc(jitOptionsValues_size);
  if (jitOptionsValues_size != 0 && jitOptionsValues == nullptr)
    goto ERROR_0;
  if ((jitOptionsValues_size != 0 &&
       rpc_read(conn, jitOptionsValues, jitOptionsValues_size) < 0) ||
      rpc_read(conn, &numLibraryOptions, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  libraryOptions_size = numLibraryOptions * sizeof(enum cudaLibraryOption);
  libraryOptions = (enum cudaLibraryOption *)malloc(libraryOptions_size);
  if (libraryOptions_size != 0 && libraryOptions == nullptr)
    goto ERROR_0;
  if ((libraryOptions_size != 0 &&
       rpc_read(conn, libraryOptions, libraryOptions_size) < 0) ||
      false)
    goto ERROR_0;
  libraryOptionValues_size = numLibraryOptions * sizeof(void *);
  libraryOptionValues = (void **)malloc(libraryOptionValues_size);
  if (libraryOptionValues_size != 0 && libraryOptionValues == nullptr)
    goto ERROR_0;
  if ((libraryOptionValues_size != 0 &&
       rpc_read(conn, libraryOptionValues, libraryOptionValues_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryLoadData");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&library, code,
               (numJitOptions * sizeof(enum cudaJitOption) == 0 ? nullptr
                                                                : jitOptions),
               (numJitOptions * sizeof(void *) == 0 ? nullptr
                                                    : jitOptionsValues),
               numJitOptions,
               (numLibraryOptions * sizeof(enum cudaLibraryOption) == 0
                    ? nullptr
                    : libraryOptions),
               (numLibraryOptions * sizeof(void *) == 0 ? nullptr
                                                        : libraryOptionValues),
               numLibraryOptions);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)libraryOptionValues);
  free((void *)libraryOptions);
  free((void *)jitOptionsValues);
  free((void *)jitOptions);
  return 0;
ERROR_0:
  free((void *)libraryOptionValues);
  free((void *)libraryOptions);
  free((void *)jitOptionsValues);
  free((void *)jitOptions);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryLoadFromFile(conn_t *conn) {
  cudaLibrary_t library{};
  library = {};
  const char *fileName = nullptr;
  std::size_t fileName_len;
  unsigned int numJitOptions;
  enum cudaJitOption *jitOptions = nullptr;
  size_t jitOptions_size;
  void **jitOptionsValues = nullptr;
  size_t jitOptionsValues_size;
  unsigned int numLibraryOptions;
  enum cudaLibraryOption *libraryOptions = nullptr;
  size_t libraryOptions_size;
  void **libraryOptionValues = nullptr;
  size_t libraryOptionValues_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLibrary_t *, const char *,
                               enum cudaJitOption *, void **, unsigned int,
                               enum cudaLibraryOption *, void **, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fileName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  fileName = (const char *)malloc(fileName_len);
  if ((fileName_len != 0 && fileName == nullptr) ||
      rpc_read(conn, (void *)fileName, fileName_len) < 0 ||
      rpc_read(conn, &numJitOptions, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  jitOptions_size = numJitOptions * sizeof(enum cudaJitOption);
  jitOptions = (enum cudaJitOption *)malloc(jitOptions_size);
  if (jitOptions_size != 0 && jitOptions == nullptr)
    goto ERROR_0;
  if ((jitOptions_size != 0 &&
       rpc_read(conn, jitOptions, jitOptions_size) < 0) ||
      false)
    goto ERROR_0;
  jitOptionsValues_size = numJitOptions * sizeof(void *);
  jitOptionsValues = (void **)malloc(jitOptionsValues_size);
  if (jitOptionsValues_size != 0 && jitOptionsValues == nullptr)
    goto ERROR_0;
  if ((jitOptionsValues_size != 0 &&
       rpc_read(conn, jitOptionsValues, jitOptionsValues_size) < 0) ||
      rpc_read(conn, &numLibraryOptions, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  libraryOptions_size = numLibraryOptions * sizeof(enum cudaLibraryOption);
  libraryOptions = (enum cudaLibraryOption *)malloc(libraryOptions_size);
  if (libraryOptions_size != 0 && libraryOptions == nullptr)
    goto ERROR_0;
  if ((libraryOptions_size != 0 &&
       rpc_read(conn, libraryOptions, libraryOptions_size) < 0) ||
      false)
    goto ERROR_0;
  libraryOptionValues_size = numLibraryOptions * sizeof(void *);
  libraryOptionValues = (void **)malloc(libraryOptionValues_size);
  if (libraryOptionValues_size != 0 && libraryOptionValues == nullptr)
    goto ERROR_0;
  if ((libraryOptionValues_size != 0 &&
       rpc_read(conn, libraryOptionValues, libraryOptionValues_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryLoadFromFile");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(&library, fileName,
               (numJitOptions * sizeof(enum cudaJitOption) == 0 ? nullptr
                                                                : jitOptions),
               (numJitOptions * sizeof(void *) == 0 ? nullptr
                                                    : jitOptionsValues),
               numJitOptions,
               (numLibraryOptions * sizeof(enum cudaLibraryOption) == 0
                    ? nullptr
                    : libraryOptions),
               (numLibraryOptions * sizeof(void *) == 0 ? nullptr
                                                        : libraryOptionValues),
               numLibraryOptions);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &library, sizeof(cudaLibrary_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)libraryOptionValues);
  free((void *)libraryOptions);
  free((void *)jitOptionsValues);
  free((void *)jitOptions);
  free((void *)fileName);
  return 0;
ERROR_0:
  free((void *)libraryOptionValues);
  free((void *)libraryOptions);
  free((void *)jitOptionsValues);
  free((void *)jitOptions);
  free((void *)fileName);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLibraryUnload(conn_t *conn) {
  cudaLibrary_t library;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLibrary_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &library, sizeof(cudaLibrary_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLibraryUnload");
  return_value = fn == nullptr ? function_not_found() : fn(library);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLogsCurrent(conn_t *conn) {
  cudaLogIterator iterator_out{};
  iterator_out = {};
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLogIterator *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLogsCurrent");
  return_value =
      fn == nullptr ? function_not_found() : fn(&iterator_out, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iterator_out, sizeof(cudaLogIterator)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLogsDumpToFile(conn_t *conn) {
  cudaLogIterator iterator{};
  const char *pathToFile = nullptr;
  std::size_t pathToFile_len;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLogIterator *, const char *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &iterator, sizeof(cudaLogIterator)) < 0 ||
      rpc_read(conn, &pathToFile_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  pathToFile = (const char *)malloc(pathToFile_len);
  if ((pathToFile_len != 0 && pathToFile == nullptr) ||
      rpc_read(conn, (void *)pathToFile, pathToFile_len) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLogsDumpToFile");
  return_value =
      fn == nullptr ? function_not_found() : fn(&iterator, pathToFile, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iterator, sizeof(cudaLogIterator)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pathToFile);
  return 0;
ERROR_0:
  free((void *)pathToFile);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLogsDumpToMemory(conn_t *conn) {
  cudaLogIterator iterator{};
  size_t size = 0;
  size_t size_requested = 0;
  char *buffer = nullptr;
  uint8_t buffer_null = 0;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaLogIterator *, char *, size_t *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &iterator, sizeof(cudaLogIterator)) < 0 ||
      rpc_read(conn, &size_requested, sizeof(size_t)) < 0 ||
      ((size = size_requested), false) ||
      rpc_read(conn, &buffer_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!buffer_null) {
    buffer = (char *)malloc((size_requested != 0 ? size_requested : 1) *
                            sizeof(char));
    if (buffer == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLogsDumpToMemory");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&iterator, buffer, &size, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &iterator, sizeof(cudaLogIterator)) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      (!buffer_null &&
       rpc_write(conn, buffer,
                 (size < size_requested ? size : size_requested) *
                     sizeof(char)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)buffer);
  return 0;
ERROR_0:
  free((void *)buffer);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLogsRegisterCallback(conn_t *conn) {
  cudaLogsCallback_t callbackFunc;
  void *userData;
  cudaLogsCallbackHandle callback_out{};
  callback_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaLogsCallback_t, void *, cudaLogsCallbackHandle *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &callbackFunc, sizeof(cudaLogsCallback_t)) < 0 ||
      rpc_read(conn, &userData, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLogsRegisterCallback");
  return_value = fn == nullptr ? function_not_found()
                               : fn(callbackFunc, userData, &callback_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &callback_out, sizeof(cudaLogsCallbackHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaLogsUnregisterCallback(conn_t *conn) {
  cudaLogsCallbackHandle callback;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaLogsCallbackHandle);
  fn_t fn = nullptr;
  if (rpc_read(conn, &callback, sizeof(cudaLogsCallbackHandle)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaLogsUnregisterCallback");
  return_value = fn == nullptr ? function_not_found() : fn(callback);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaMalloc(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  size_t size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMalloc");
  return_value = fn == nullptr ? function_not_found() : fn(&devPtr, size);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMalloc3D(conn_t *conn) {
  struct cudaPitchedPtr pitchedDevPtr {};
  pitchedDevPtr = {};
  struct cudaExtent extent;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaPitchedPtr *, struct cudaExtent);
  fn_t fn = nullptr;
  if (rpc_read(conn, &extent, sizeof(struct cudaExtent)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMalloc3D");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pitchedDevPtr, extent);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pitchedDevPtr, sizeof(struct cudaPitchedPtr)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMalloc3DArray(conn_t *conn) {
  cudaArray_t array{};
  array = {};
  struct cudaChannelFormatDesc desc {};
  struct cudaExtent extent;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaArray_t *, const struct cudaChannelFormatDesc *,
                      struct cudaExtent, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(const struct cudaChannelFormatDesc)) < 0 ||
      rpc_read(conn, &extent, sizeof(struct cudaExtent)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMalloc3DArray");
  return_value =
      fn == nullptr ? function_not_found() : fn(&array, &desc, extent, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &array, sizeof(cudaArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMallocArray(conn_t *conn) {
  cudaArray_t array{};
  array = {};
  struct cudaChannelFormatDesc desc {};
  size_t width;
  size_t height;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaArray_t *, const struct cudaChannelFormatDesc *,
                      size_t, size_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(const struct cudaChannelFormatDesc)) < 0 ||
      rpc_read(conn, &width, sizeof(size_t)) < 0 ||
      rpc_read(conn, &height, sizeof(size_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMallocArray");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&array, &desc, width, height, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &array, sizeof(cudaArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMallocAsync(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  size_t size;
  cudaStream_t hStream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMallocAsync");
  return_value =
      fn == nullptr ? function_not_found() : fn(&devPtr, size, hStream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMallocFromPoolAsync(conn_t *conn) {
  void *ptr{};
  ptr = {};
  size_t size;
  cudaMemPool_t memPool;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t, cudaMemPool_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMallocFromPoolAsync");
  return_value =
      fn == nullptr ? function_not_found() : fn(&ptr, size, memPool, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMallocMipmappedArray(conn_t *conn) {
  cudaMipmappedArray_t mipmappedArray{};
  mipmappedArray = {};
  struct cudaChannelFormatDesc desc {};
  struct cudaExtent extent;
  unsigned int numLevels;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMipmappedArray_t *,
                               const struct cudaChannelFormatDesc *,
                               struct cudaExtent, unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &desc, sizeof(const struct cudaChannelFormatDesc)) < 0 ||
      rpc_read(conn, &extent, sizeof(struct cudaExtent)) < 0 ||
      rpc_read(conn, &numLevels, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMallocMipmappedArray");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&mipmappedArray, &desc, extent, numLevels, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mipmappedArray, sizeof(cudaMipmappedArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMallocPitch(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  size_t pitch{};
  pitch = {};
  size_t width;
  size_t height;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, size_t, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &width, sizeof(size_t)) < 0 ||
      rpc_read(conn, &height, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMallocPitch");
  return_value =
      fn == nullptr ? function_not_found() : fn(&devPtr, &pitch, width, height);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &pitch, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemAdvise(conn_t *conn) {
  const void *devPtr;
  size_t count;
  enum cudaMemoryAdvise advice;
  struct cudaMemLocation location;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, size_t, enum cudaMemoryAdvise,
                               struct cudaMemLocation);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(const void *)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 ||
      rpc_read(conn, &advice, sizeof(enum cudaMemoryAdvise)) < 0 ||
      rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemAdvise");
  return_value = fn == nullptr ? function_not_found()
                               : fn(devPtr, count, advice, location);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemDiscardAndPrefetchBatchAsync(conn_t *conn) {
  size_t count;
  void **dptrs = nullptr;
  size_t dptrs_size;
  size_t *sizes = nullptr;
  size_t sizes_size;
  size_t numPrefetchLocs;
  struct cudaMemLocation *prefetchLocs = nullptr;
  size_t prefetchLocs_size;
  size_t *prefetchLocIdxs = nullptr;
  size_t prefetchLocIdxs_size;
  unsigned long long flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(void **, size_t *, size_t, struct cudaMemLocation *,
                      size_t *, size_t, unsigned long long, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dptrs_size = count * sizeof(void *);
  dptrs = (void **)malloc(dptrs_size);
  if (dptrs_size != 0 && dptrs == nullptr)
    goto ERROR_0;
  if ((dptrs_size != 0 && rpc_read(conn, dptrs, dptrs_size) < 0) || false)
    goto ERROR_0;
  sizes_size = count * sizeof(size_t);
  sizes = (size_t *)malloc(sizes_size);
  if (sizes_size != 0 && sizes == nullptr)
    goto ERROR_0;
  if ((sizes_size != 0 && rpc_read(conn, sizes, sizes_size) < 0) ||
      rpc_read(conn, &numPrefetchLocs, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  prefetchLocs_size = numPrefetchLocs * sizeof(struct cudaMemLocation);
  prefetchLocs = (struct cudaMemLocation *)malloc(prefetchLocs_size);
  if (prefetchLocs_size != 0 && prefetchLocs == nullptr)
    goto ERROR_0;
  if ((prefetchLocs_size != 0 &&
       rpc_read(conn, prefetchLocs, prefetchLocs_size) < 0) ||
      false)
    goto ERROR_0;
  prefetchLocIdxs_size = numPrefetchLocs * sizeof(size_t);
  prefetchLocIdxs = (size_t *)malloc(prefetchLocIdxs_size);
  if (prefetchLocIdxs_size != 0 && prefetchLocIdxs == nullptr)
    goto ERROR_0;
  if ((prefetchLocIdxs_size != 0 &&
       rpc_read(conn, prefetchLocIdxs, prefetchLocIdxs_size) < 0) ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemDiscardAndPrefetchBatchAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((count * sizeof(void *) == 0 ? nullptr : dptrs),
               (count * sizeof(size_t) == 0 ? nullptr : sizes), count,
               (numPrefetchLocs * sizeof(struct cudaMemLocation) == 0
                    ? nullptr
                    : prefetchLocs),
               (numPrefetchLocs * sizeof(size_t) == 0 ? nullptr
                                                      : prefetchLocIdxs),
               numPrefetchLocs, flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)prefetchLocIdxs);
  free((void *)prefetchLocs);
  free((void *)sizes);
  free((void *)dptrs);
  return 0;
ERROR_0:
  free((void *)prefetchLocIdxs);
  free((void *)prefetchLocs);
  free((void *)sizes);
  free((void *)dptrs);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemDiscardBatchAsync(conn_t *conn) {
  size_t count;
  void **dptrs = nullptr;
  size_t dptrs_size;
  size_t *sizes = nullptr;
  size_t sizes_size;
  unsigned long long flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, size_t, unsigned long long,
                               cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dptrs_size = count * sizeof(void *);
  dptrs = (void **)malloc(dptrs_size);
  if (dptrs_size != 0 && dptrs == nullptr)
    goto ERROR_0;
  if ((dptrs_size != 0 && rpc_read(conn, dptrs, dptrs_size) < 0) || false)
    goto ERROR_0;
  sizes_size = count * sizeof(size_t);
  sizes = (size_t *)malloc(sizes_size);
  if (sizes_size != 0 && sizes == nullptr)
    goto ERROR_0;
  if ((sizes_size != 0 && rpc_read(conn, sizes, sizes_size) < 0) ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemDiscardBatchAsync");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn((count * sizeof(void *) == 0 ? nullptr : dptrs),
                          (count * sizeof(size_t) == 0 ? nullptr : sizes),
                          count, flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)sizes);
  free((void *)dptrs);
  return 0;
ERROR_0:
  free((void *)sizes);
  free((void *)dptrs);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemGetDefaultMemPool(conn_t *conn) {
  cudaMemPool_t memPool{};
  memPool = {};
  struct cudaMemLocation location {};
  enum cudaMemAllocationType type;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t *, struct cudaMemLocation *,
                               enum cudaMemAllocationType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaMemAllocationType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemGetDefaultMemPool");
  return_value =
      fn == nullptr ? function_not_found() : fn(&memPool, &location, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaMemGetInfo(conn_t *conn) {
  size_t free{};
  free = {};
  size_t total{};
  total = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(size_t *, size_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemGetInfo");
  return_value = fn == nullptr ? function_not_found() : fn(&free, &total);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &free, sizeof(size_t)) < 0 ||
      rpc_write(conn, &total, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemGetMemPool(conn_t *conn) {
  cudaMemPool_t memPool{};
  memPool = {};
  struct cudaMemLocation location {};
  enum cudaMemAllocationType type;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t *, struct cudaMemLocation *,
                               enum cudaMemAllocationType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaMemAllocationType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemGetMemPool");
  return_value =
      fn == nullptr ? function_not_found() : fn(&memPool, &location, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaMemPoolCreate(conn_t *conn) {
  cudaMemPool_t memPool{};
  memPool = {};
  struct cudaMemPoolProps poolProps {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaMemPool_t *, const struct cudaMemPoolProps *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &poolProps, sizeof(const struct cudaMemPoolProps)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolCreate");
  return_value =
      fn == nullptr ? function_not_found() : fn(&memPool, &poolProps);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemPoolDestroy(conn_t *conn) {
  cudaMemPool_t memPool;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(memPool);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemPoolExportPointer(conn_t *conn) {
  struct cudaMemPoolPtrExportData exportData {};
  exportData = {};
  void *ptr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaMemPoolPtrExportData *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ptr, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolExportPointer");
  return_value = fn == nullptr ? function_not_found() : fn(&exportData, ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &exportData, sizeof(struct cudaMemPoolPtrExportData)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemPoolGetAccess(conn_t *conn) {
  enum cudaMemAccessFlags flags {};
  flags = {};
  cudaMemPool_t memPool;
  struct cudaMemLocation location {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaMemAccessFlags *, cudaMemPool_t,
                               struct cudaMemLocation *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolGetAccess");
  return_value =
      fn == nullptr ? function_not_found() : fn(&flags, memPool, &location);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &flags, sizeof(enum cudaMemAccessFlags)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemPoolGetAttribute(conn_t *conn) {
  cudaMemPool_t memPool;
  enum cudaMemPoolAttr attr;
  void *value = nullptr;
  size_t value_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t, enum cudaMemPoolAttr, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaMemPoolAttr)) < 0 || false)
    goto ERROR_0;
  value_size = 8;
  value = (void *)malloc(value_size);
  if (value_size != 0 && value == nullptr)
    goto ERROR_0;
  if ((value_size != 0 && rpc_read(conn, value, value_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolGetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(memPool, attr, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, value, 8) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)value);
  return 0;
ERROR_0:
  free((void *)value);
  return -1;
}

int handle_cudaMemPoolImportPointer(conn_t *conn) {
  void *ptr{};
  ptr = {};
  cudaMemPool_t memPool;
  struct cudaMemPoolPtrExportData exportData {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, cudaMemPool_t,
                               struct cudaMemPoolPtrExportData *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &exportData, sizeof(struct cudaMemPoolPtrExportData)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolImportPointer");
  return_value =
      fn == nullptr ? function_not_found() : fn(&ptr, memPool, &exportData);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &ptr, sizeof(void *)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemPoolSetAccess(conn_t *conn) {
  cudaMemPool_t memPool;
  size_t count;
  struct cudaMemAccessDesc *descList = nullptr;
  size_t descList_size;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaMemPool_t, const struct cudaMemAccessDesc *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  descList_size = count * sizeof(const struct cudaMemAccessDesc);
  descList = (struct cudaMemAccessDesc *)malloc(descList_size);
  if (descList_size != 0 && descList == nullptr)
    goto ERROR_0;
  if ((descList_size != 0 && rpc_read(conn, descList, descList_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolSetAccess");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(memPool,
               (count * sizeof(const struct cudaMemAccessDesc) == 0 ? nullptr
                                                                    : descList),
               count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)descList);
  return 0;
ERROR_0:
  free((void *)descList);
  return -1;
}

int handle_cudaMemPoolSetAttribute(conn_t *conn) {
  cudaMemPool_t memPool;
  enum cudaMemPoolAttr attr;
  void *value = nullptr;
  size_t value_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t, enum cudaMemPoolAttr, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(enum cudaMemPoolAttr)) < 0 || false)
    goto ERROR_0;
  value_size = 8;
  value = (void *)malloc(value_size);
  if (value_size != 0 && value == nullptr)
    goto ERROR_0;
  if ((value_size != 0 && rpc_read(conn, value, value_size) < 0) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolSetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(memPool, attr, value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)value);
  return 0;
ERROR_0:
  free((void *)value);
  return -1;
}

int handle_cudaMemPoolTrimTo(conn_t *conn) {
  cudaMemPool_t memPool;
  size_t minBytesToKeep;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMemPool_t, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 ||
      rpc_read(conn, &minBytesToKeep, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPoolTrimTo");
  return_value =
      fn == nullptr ? function_not_found() : fn(memPool, minBytesToKeep);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemPrefetchAsync(conn_t *conn) {
  const void *devPtr;
  size_t count;
  struct cudaMemLocation location;
  unsigned int flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const void *, size_t, struct cudaMemLocation,
                               unsigned int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(const void *)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 ||
      rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPrefetchAsync");
  return_value = fn == nullptr ? function_not_found()
                               : fn(devPtr, count, location, flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemPrefetchBatchAsync(conn_t *conn) {
  size_t count;
  void **dptrs = nullptr;
  size_t dptrs_size;
  size_t *sizes = nullptr;
  size_t sizes_size;
  size_t numPrefetchLocs;
  struct cudaMemLocation *prefetchLocs = nullptr;
  size_t prefetchLocs_size;
  size_t *prefetchLocIdxs = nullptr;
  size_t prefetchLocIdxs_size;
  unsigned long long flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(void **, size_t *, size_t, struct cudaMemLocation *,
                      size_t *, size_t, unsigned long long, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dptrs_size = count * sizeof(void *);
  dptrs = (void **)malloc(dptrs_size);
  if (dptrs_size != 0 && dptrs == nullptr)
    goto ERROR_0;
  if ((dptrs_size != 0 && rpc_read(conn, dptrs, dptrs_size) < 0) || false)
    goto ERROR_0;
  sizes_size = count * sizeof(size_t);
  sizes = (size_t *)malloc(sizes_size);
  if (sizes_size != 0 && sizes == nullptr)
    goto ERROR_0;
  if ((sizes_size != 0 && rpc_read(conn, sizes, sizes_size) < 0) ||
      rpc_read(conn, &numPrefetchLocs, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  prefetchLocs_size = numPrefetchLocs * sizeof(struct cudaMemLocation);
  prefetchLocs = (struct cudaMemLocation *)malloc(prefetchLocs_size);
  if (prefetchLocs_size != 0 && prefetchLocs == nullptr)
    goto ERROR_0;
  if ((prefetchLocs_size != 0 &&
       rpc_read(conn, prefetchLocs, prefetchLocs_size) < 0) ||
      false)
    goto ERROR_0;
  prefetchLocIdxs_size = numPrefetchLocs * sizeof(size_t);
  prefetchLocIdxs = (size_t *)malloc(prefetchLocIdxs_size);
  if (prefetchLocIdxs_size != 0 && prefetchLocIdxs == nullptr)
    goto ERROR_0;
  if ((prefetchLocIdxs_size != 0 &&
       rpc_read(conn, prefetchLocIdxs, prefetchLocIdxs_size) < 0) ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemPrefetchBatchAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((count * sizeof(void *) == 0 ? nullptr : dptrs),
               (count * sizeof(size_t) == 0 ? nullptr : sizes), count,
               (numPrefetchLocs * sizeof(struct cudaMemLocation) == 0
                    ? nullptr
                    : prefetchLocs),
               (numPrefetchLocs * sizeof(size_t) == 0 ? nullptr
                                                      : prefetchLocIdxs),
               numPrefetchLocs, flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)prefetchLocIdxs);
  free((void *)prefetchLocs);
  free((void *)sizes);
  free((void *)dptrs);
  return 0;
ERROR_0:
  free((void *)prefetchLocIdxs);
  free((void *)prefetchLocs);
  free((void *)sizes);
  free((void *)dptrs);
  return -1;
}

#endif

int handle_cudaMemRangeGetAttribute(conn_t *conn) {
  size_t dataSize;
  void *data = nullptr;
  enum cudaMemRangeAttribute attribute;
  const void *devPtr;
  size_t count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, size_t, enum cudaMemRangeAttribute,
                               const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dataSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data = (void *)malloc(dataSize);
  if ((dataSize != 0 && data == nullptr) ||
      rpc_read(conn, &attribute, sizeof(enum cudaMemRangeAttribute)) < 0 ||
      rpc_read(conn, &devPtr, sizeof(const void *)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemRangeGetAttribute");
  return_value = fn == nullptr ? function_not_found()
                               : fn((dataSize == 0 ? nullptr : data), dataSize,
                                    attribute, devPtr, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, data, dataSize) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)data);
  return -1;
}

int handle_cudaMemRangeGetAttributes(conn_t *conn) {
  size_t numAttributes;
  void **data = nullptr;
  size_t data_size;
  size_t *dataSizes = nullptr;
  size_t dataSizes_size;
  enum cudaMemRangeAttribute *attributes = nullptr;
  size_t attributes_size;
  const void *devPtr;
  size_t count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, enum cudaMemRangeAttribute *,
                               size_t, const void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numAttributes, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  data_size = numAttributes * sizeof(void *);
  data = (void **)malloc(data_size);
  if (data_size != 0 && data == nullptr)
    goto ERROR_0;
  if ((data_size != 0 && rpc_read(conn, data, data_size) < 0) || false)
    goto ERROR_0;
  dataSizes_size = numAttributes * sizeof(size_t);
  dataSizes = (size_t *)malloc(dataSizes_size);
  if (dataSizes_size != 0 && dataSizes == nullptr)
    goto ERROR_0;
  if ((dataSizes_size != 0 && rpc_read(conn, dataSizes, dataSizes_size) < 0) ||
      false)
    goto ERROR_0;
  attributes_size = numAttributes * sizeof(enum cudaMemRangeAttribute);
  attributes = (enum cudaMemRangeAttribute *)malloc(attributes_size);
  if (attributes_size != 0 && attributes == nullptr)
    goto ERROR_0;
  if ((attributes_size != 0 &&
       rpc_read(conn, attributes, attributes_size) < 0) ||
      rpc_read(conn, &devPtr, sizeof(const void *)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemRangeGetAttributes");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((numAttributes * sizeof(void *) == 0 ? nullptr : data),
               (numAttributes * sizeof(size_t) == 0 ? nullptr : dataSizes),
               (numAttributes * sizeof(enum cudaMemRangeAttribute) == 0
                    ? nullptr
                    : attributes),
               numAttributes, devPtr, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)attributes);
  free((void *)dataSizes);
  free((void *)data);
  return 0;
ERROR_0:
  free((void *)attributes);
  free((void *)dataSizes);
  free((void *)data);
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemSetMemPool(conn_t *conn) {
  struct cudaMemLocation location {};
  enum cudaMemAllocationType type;
  cudaMemPool_t memPool;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaMemLocation *,
                               enum cudaMemAllocationType, cudaMemPool_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &location, sizeof(struct cudaMemLocation)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaMemAllocationType)) < 0 ||
      rpc_read(conn, &memPool, sizeof(cudaMemPool_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemSetMemPool");
  return_value =
      fn == nullptr ? function_not_found() : fn(&location, type, memPool);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaMemcpy3D(conn_t *conn) {
  struct cudaMemcpy3DParms p {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const struct cudaMemcpy3DParms *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &p, sizeof(const struct cudaMemcpy3DParms)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3D");
  return_value = fn == nullptr ? function_not_found() : fn(&p);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemcpy3DAsync(conn_t *conn) {
  struct cudaMemcpy3DParms p {};
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const struct cudaMemcpy3DParms *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &p, sizeof(const struct cudaMemcpy3DParms)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3DAsync");
  return_value = fn == nullptr ? function_not_found() : fn(&p, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemcpy3DBatchAsync(conn_t *conn) {
  size_t numOps;
  struct cudaMemcpy3DBatchOp *opList = nullptr;
  size_t opList_size;
  unsigned long long flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(size_t, struct cudaMemcpy3DBatchOp *,
                               unsigned long long, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numOps, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  opList_size = numOps * sizeof(struct cudaMemcpy3DBatchOp);
  opList = (struct cudaMemcpy3DBatchOp *)malloc(opList_size);
  if (opList_size != 0 && opList == nullptr)
    goto ERROR_0;
  if ((opList_size != 0 && rpc_read(conn, opList, opList_size) < 0) ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3DBatchAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(numOps,
               (numOps * sizeof(struct cudaMemcpy3DBatchOp) == 0 ? nullptr
                                                                 : opList),
               flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)opList);
  return 0;
ERROR_0:
  free((void *)opList);
  return -1;
}

#endif

int handle_cudaMemcpy3DPeer(conn_t *conn) {
  struct cudaMemcpy3DPeerParms p {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const struct cudaMemcpy3DPeerParms *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &p, sizeof(const struct cudaMemcpy3DPeerParms)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3DPeer");
  return_value = fn == nullptr ? function_not_found() : fn(&p);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemcpy3DPeerAsync(conn_t *conn) {
  struct cudaMemcpy3DPeerParms p {};
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(const struct cudaMemcpy3DPeerParms *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &p, sizeof(const struct cudaMemcpy3DPeerParms)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3DPeerAsync");
  return_value = fn == nullptr ? function_not_found() : fn(&p, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaMemcpy3DWithAttributesAsync(conn_t *conn) {
  struct cudaMemcpy3DBatchOp op {};
  unsigned long long flags;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaMemcpy3DBatchOp *, unsigned long long,
                               cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &op, sizeof(struct cudaMemcpy3DBatchOp)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpy3DWithAttributesAsync");
  return_value = fn == nullptr ? function_not_found() : fn(&op, flags, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemcpyBatchAsync(conn_t *conn) {
  size_t count;
  void **dsts = nullptr;
  size_t dsts_size;
  const void **srcs = nullptr;
  size_t srcs_size;
  size_t *sizes = nullptr;
  size_t sizes_size;
  size_t numAttrs;
  struct cudaMemcpyAttributes *attrs = nullptr;
  size_t attrs_size;
  size_t *attrsIdxs = nullptr;
  size_t attrsIdxs_size;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      void *const *, const void *const *, const size_t *, size_t,
      struct cudaMemcpyAttributes *, size_t *, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dsts_size = count * sizeof(void *const);
  dsts = (void **)malloc(dsts_size);
  if (dsts_size != 0 && dsts == nullptr)
    goto ERROR_0;
  if ((dsts_size != 0 && rpc_read(conn, dsts, dsts_size) < 0) || false)
    goto ERROR_0;
  srcs_size = count * sizeof(const void *const);
  srcs = (const void **)malloc(srcs_size);
  if (srcs_size != 0 && srcs == nullptr)
    goto ERROR_0;
  if ((srcs_size != 0 && rpc_read(conn, srcs, srcs_size) < 0) || false)
    goto ERROR_0;
  sizes_size = count * sizeof(const size_t);
  sizes = (size_t *)malloc(sizes_size);
  if (sizes_size != 0 && sizes == nullptr)
    goto ERROR_0;
  if ((sizes_size != 0 && rpc_read(conn, sizes, sizes_size) < 0) ||
      rpc_read(conn, &numAttrs, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  attrs_size = numAttrs * sizeof(struct cudaMemcpyAttributes);
  attrs = (struct cudaMemcpyAttributes *)malloc(attrs_size);
  if (attrs_size != 0 && attrs == nullptr)
    goto ERROR_0;
  if ((attrs_size != 0 && rpc_read(conn, attrs, attrs_size) < 0) || false)
    goto ERROR_0;
  attrsIdxs_size = numAttrs * sizeof(size_t);
  attrsIdxs = (size_t *)malloc(attrsIdxs_size);
  if (attrsIdxs_size != 0 && attrsIdxs == nullptr)
    goto ERROR_0;
  if ((attrsIdxs_size != 0 && rpc_read(conn, attrsIdxs, attrsIdxs_size) < 0) ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpyBatchAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((count * sizeof(void *const) == 0 ? nullptr : dsts),
               (count * sizeof(const void *const) == 0 ? nullptr : srcs),
               (count * sizeof(const size_t) == 0 ? nullptr : sizes), count,
               (numAttrs * sizeof(struct cudaMemcpyAttributes) == 0 ? nullptr
                                                                    : attrs),
               (numAttrs * sizeof(size_t) == 0 ? nullptr : attrsIdxs), numAttrs,
               stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)attrsIdxs);
  free((void *)attrs);
  free((void *)sizes);
  free((void *)srcs);
  free((void *)dsts);
  return 0;
ERROR_0:
  free((void *)attrsIdxs);
  free((void *)attrs);
  free((void *)sizes);
  free((void *)srcs);
  free((void *)dsts);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaMemcpyWithAttributesAsync(conn_t *conn) {
  void *dst;
  const void *src;
  size_t size;
  struct cudaMemcpyAttributes attr {};
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, const void *, size_t,
                               struct cudaMemcpyAttributes *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dst, sizeof(void *)) < 0 ||
      rpc_read(conn, &src, sizeof(const void *)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(struct cudaMemcpyAttributes)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemcpyWithAttributesAsync");
  return_value =
      fn == nullptr ? function_not_found() : fn(dst, src, size, &attr, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaMemset(conn_t *conn) {
  void *devPtr;
  int value;
  size_t count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, int, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemset");
  return_value =
      fn == nullptr ? function_not_found() : fn(devPtr, value, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemset2D(conn_t *conn) {
  void *devPtr;
  size_t pitch;
  int value;
  size_t width;
  size_t height;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, size_t, int, size_t, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &pitch, sizeof(size_t)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &width, sizeof(size_t)) < 0 ||
      rpc_read(conn, &height, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemset2D");
  return_value = fn == nullptr ? function_not_found()
                               : fn(devPtr, pitch, value, width, height);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemset2DAsync(conn_t *conn) {
  void *devPtr;
  size_t pitch;
  int value;
  size_t width;
  size_t height;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(void *, size_t, int, size_t, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &pitch, sizeof(size_t)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &width, sizeof(size_t)) < 0 ||
      rpc_read(conn, &height, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemset2DAsync");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(devPtr, pitch, value, width, height, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemset3D(conn_t *conn) {
  struct cudaPitchedPtr pitchedDevPtr;
  int value;
  struct cudaExtent extent;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaPitchedPtr, int, struct cudaExtent);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pitchedDevPtr, sizeof(struct cudaPitchedPtr)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &extent, sizeof(struct cudaExtent)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemset3D");
  return_value =
      fn == nullptr ? function_not_found() : fn(pitchedDevPtr, value, extent);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemset3DAsync(conn_t *conn) {
  struct cudaPitchedPtr pitchedDevPtr;
  int value;
  struct cudaExtent extent;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaPitchedPtr, int, struct cudaExtent,
                               cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &pitchedDevPtr, sizeof(struct cudaPitchedPtr)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &extent, sizeof(struct cudaExtent)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemset3DAsync");
  return_value = fn == nullptr ? function_not_found()
                               : fn(pitchedDevPtr, value, extent, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMemsetAsync(conn_t *conn) {
  void *devPtr;
  int value;
  size_t count;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void *, int, size_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &value, sizeof(int)) < 0 ||
      rpc_read(conn, &count, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMemsetAsync");
  return_value =
      fn == nullptr ? function_not_found() : fn(devPtr, value, count, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMipmappedArrayGetMemoryRequirements(conn_t *conn) {
  struct cudaArrayMemoryRequirements memoryRequirements {};
  memoryRequirements = {};
  cudaMipmappedArray_t mipmap;
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaArrayMemoryRequirements *,
                               cudaMipmappedArray_t, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mipmap, sizeof(cudaMipmappedArray_t)) < 0 ||
      rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMipmappedArrayGetMemoryRequirements");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&memoryRequirements, mipmap, device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &memoryRequirements,
                sizeof(struct cudaArrayMemoryRequirements)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaMipmappedArrayGetSparseProperties(conn_t *conn) {
  struct cudaArraySparseProperties sparseProperties {};
  sparseProperties = {};
  cudaMipmappedArray_t mipmap;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(struct cudaArraySparseProperties *, cudaMipmappedArray_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mipmap, sizeof(cudaMipmappedArray_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaMipmappedArrayGetSparseProperties");
  return_value =
      fn == nullptr ? function_not_found() : fn(&sparseProperties, mipmap);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &sparseProperties,
                sizeof(struct cudaArraySparseProperties)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaOccupancyAvailableDynamicSMemPerBlock(conn_t *conn) {
  size_t dynamicSmemSize{};
  dynamicSmemSize = {};
  const void *func;
  int numBlocks;
  int blockSize;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(size_t *, const void *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &numBlocks, sizeof(int)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaOccupancyAvailableDynamicSMemPerBlock");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&dynamicSmemSize, func, numBlocks, blockSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &dynamicSmemSize, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaOccupancyMaxActiveBlocksPerMultiprocessor(conn_t *conn) {
  int numBlocks{};
  numBlocks = {};
  const void *func;
  int blockSize;
  size_t dynamicSMemSize;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const void *, int, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dynamicSMemSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaOccupancyMaxActiveBlocksPerMultiprocessor");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&numBlocks, func, blockSize, dynamicSMemSize);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &numBlocks, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaOccupancyMaxActiveBlocksPerMultiprocessorWithFlags(
    conn_t *conn) {
  int numBlocks{};
  numBlocks = {};
  const void *func;
  int blockSize;
  size_t dynamicSMemSize;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const void *, int, size_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &blockSize, sizeof(int)) < 0 ||
      rpc_read(conn, &dynamicSMemSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>(
      "cudaOccupancyMaxActiveBlocksPerMultiprocessorWithFlags");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&numBlocks, func, blockSize, dynamicSMemSize, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &numBlocks, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaOccupancyMaxActiveClusters(conn_t *conn) {
  int numClusters{};
  numClusters = {};
  const void *func;
  cudaLaunchConfig_t launchConfig = {};
  std::vector<unsigned char> launchConfig_attrs_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const void *, const cudaLaunchConfig_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &launchConfig, sizeof(launchConfig)) < 0 ||
      ((launchConfig_attrs_buf.resize(launchConfig.numAttrs *
                                      sizeof(*launchConfig.attrs)),
        false)) ||
      (launchConfig.numAttrs != 0 &&
       rpc_read(conn, launchConfig_attrs_buf.data(),
                launchConfig_attrs_buf.size()) < 0) ||
      ((launchConfig.attrs =
            (decltype(launchConfig.attrs))launchConfig_attrs_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaOccupancyMaxActiveClusters");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&numClusters, func, &launchConfig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &numClusters, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaOccupancyMaxPotentialClusterSize(conn_t *conn) {
  int clusterSize{};
  clusterSize = {};
  const void *func;
  cudaLaunchConfig_t launchConfig = {};
  std::vector<unsigned char> launchConfig_attrs_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, const void *, const cudaLaunchConfig_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &func, sizeof(const void *)) < 0 ||
      rpc_read(conn, &launchConfig, sizeof(launchConfig)) < 0 ||
      ((launchConfig_attrs_buf.resize(launchConfig.numAttrs *
                                      sizeof(*launchConfig.attrs)),
        false)) ||
      (launchConfig.numAttrs != 0 &&
       rpc_read(conn, launchConfig_attrs_buf.data(),
                launchConfig_attrs_buf.size()) < 0) ||
      ((launchConfig.attrs =
            (decltype(launchConfig.attrs))launchConfig_attrs_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaOccupancyMaxPotentialClusterSize");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&clusterSize, func, &launchConfig);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &clusterSize, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaPeekAtLastError(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaPeekAtLastError");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaPointerGetAttributes(conn_t *conn) {
  struct cudaPointerAttributes attributes {};
  attributes = {};
  const void *ptr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(struct cudaPointerAttributes *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ptr, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaPointerGetAttributes");
  return_value = fn == nullptr ? function_not_found() : fn(&attributes, ptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &attributes, sizeof(struct cudaPointerAttributes)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaProfilerStart(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaProfilerStart");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaProfilerStop(conn_t *conn) {
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)();
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaProfilerStop");
  return_value = fn == nullptr ? function_not_found() : fn();

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaRuntimeGetVersion(conn_t *conn) {
  int runtimeVersion{};
  runtimeVersion = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaRuntimeGetVersion");
  return_value = fn == nullptr ? function_not_found() : fn(&runtimeVersion);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &runtimeVersion, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaSetDevice(conn_t *conn) {
  int device;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &device, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaSetDevice");
  return_value = fn == nullptr ? function_not_found() : fn(device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaSetDeviceFlags(conn_t *conn) {
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaSetDeviceFlags");
  return_value = fn == nullptr ? function_not_found() : fn(flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaSetValidDevices(conn_t *conn) {
  int len;
  int *device_arr = nullptr;
  size_t device_arr_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int *, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &len, sizeof(int)) < 0 || len < 0 || false)
    goto ERROR_0;
  device_arr_size = len * sizeof(int);
  device_arr = (int *)malloc(device_arr_size);
  if (device_arr_size != 0 && device_arr == nullptr)
    goto ERROR_0;
  if ((device_arr_size != 0 &&
       rpc_read(conn, device_arr, device_arr_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaSetValidDevices");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn((len * sizeof(int) == 0 ? nullptr : device_arr), len);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)device_arr);
  return 0;
ERROR_0:
  free((void *)device_arr);
  return -1;
}

int handle_cudaSignalExternalSemaphoresAsync(conn_t *conn) {
  unsigned int numExtSems;
  cudaExternalSemaphore_t *extSemArray = nullptr;
  size_t extSemArray_size;
  struct cudaExternalSemaphoreSignalParams *paramsArray = nullptr;
  size_t paramsArray_size;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const cudaExternalSemaphore_t *,
                               const struct cudaExternalSemaphoreSignalParams *,
                               unsigned int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numExtSems, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  extSemArray_size = numExtSems * sizeof(const cudaExternalSemaphore_t);
  extSemArray = (cudaExternalSemaphore_t *)malloc(extSemArray_size);
  if (extSemArray_size != 0 && extSemArray == nullptr)
    goto ERROR_0;
  if ((extSemArray_size != 0 &&
       rpc_read(conn, extSemArray, extSemArray_size) < 0) ||
      false)
    goto ERROR_0;
  paramsArray_size =
      numExtSems * sizeof(const struct cudaExternalSemaphoreSignalParams);
  paramsArray =
      (struct cudaExternalSemaphoreSignalParams *)malloc(paramsArray_size);
  if (paramsArray_size != 0 && paramsArray == nullptr)
    goto ERROR_0;
  if ((paramsArray_size != 0 &&
       rpc_read(conn, paramsArray, paramsArray_size) < 0) ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaSignalExternalSemaphoresAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((numExtSems * sizeof(const cudaExternalSemaphore_t) == 0
                    ? nullptr
                    : extSemArray),
               (numExtSems * sizeof(const struct
                                    cudaExternalSemaphoreSignalParams) ==
                        0
                    ? nullptr
                    : paramsArray),
               numExtSems, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)paramsArray);
  free((void *)extSemArray);
  return 0;
ERROR_0:
  free((void *)paramsArray);
  free((void *)extSemArray);
  return -1;
}

int handle_cudaStreamAttachMemAsync(conn_t *conn) {
  cudaStream_t stream;
  void *devPtr;
  size_t length;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, void *, size_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_read(conn, &length, sizeof(size_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamAttachMemAsync");
  return_value =
      fn == nullptr ? function_not_found() : fn(stream, devPtr, length, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaStreamBeginCaptureToGraph(conn_t *conn) {
  cudaStream_t stream;
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *dependencies = nullptr;
  size_t dependencies_size;
  cudaGraphEdgeData *dependencyData = nullptr;
  size_t dependencyData_size;
  enum cudaStreamCaptureMode mode;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaStream_t, cudaGraph_t, const cudaGraphNode_t *,
      const cudaGraphEdgeData *, size_t, enum cudaStreamCaptureMode);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  dependencies = (cudaGraphNode_t *)malloc(dependencies_size);
  if (dependencies_size != 0 && dependencies == nullptr)
    goto ERROR_0;
  if ((dependencies_size != 0 &&
       rpc_read(conn, dependencies, dependencies_size) < 0) ||
      false)
    goto ERROR_0;
  dependencyData_size = numDependencies * sizeof(const cudaGraphEdgeData);
  dependencyData = (cudaGraphEdgeData *)malloc(dependencyData_size);
  if (dependencyData_size != 0 && dependencyData == nullptr)
    goto ERROR_0;
  if ((dependencyData_size != 0 &&
       rpc_read(conn, dependencyData, dependencyData_size) < 0) ||
      rpc_read(conn, &mode, sizeof(enum cudaStreamCaptureMode)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamBeginCaptureToGraph");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(stream, graph,
                         (numDependencies * sizeof(const cudaGraphNode_t) == 0
                              ? nullptr
                              : dependencies),
                         (numDependencies * sizeof(const cudaGraphEdgeData) == 0
                              ? nullptr
                              : dependencyData),
                         numDependencies, mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dependencyData);
  free((void *)dependencies);
  return 0;
ERROR_0:
  free((void *)dependencyData);
  free((void *)dependencies);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaStreamBeginRecaptureToGraph(conn_t *conn) {
  cudaStream_t stream;
  enum cudaStreamCaptureMode mode;
  cudaGraph_t graph;
  struct cudaGraphRecaptureCallbackData *callbackData_null_check;
  struct cudaGraphRecaptureCallbackData callbackData;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaStream_t, enum cudaStreamCaptureMode, cudaGraph_t,
                      struct cudaGraphRecaptureCallbackData *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &mode, sizeof(enum cudaStreamCaptureMode)) < 0 ||
      rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &callbackData_null_check,
               sizeof(struct cudaGraphRecaptureCallbackData *)) < 0 ||
      (callbackData_null_check &&
       rpc_read(conn, &callbackData,
                sizeof(struct cudaGraphRecaptureCallbackData)) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamBeginRecaptureToGraph");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(stream, mode, graph,
                          callbackData_null_check ? &callbackData : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaStreamCopyAttributes(conn_t *conn) {
  cudaStream_t dst;
  cudaStream_t src;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &dst, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &src, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamCopyAttributes");
  return_value = fn == nullptr ? function_not_found() : fn(dst, src);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamCreate(conn_t *conn) {
  cudaStream_t pStream{};
  pStream = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t *);
  fn_t fn = nullptr;
  if (false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&pStream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pStream, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamCreateWithFlags(conn_t *conn) {
  cudaStream_t pStream{};
  pStream = {};
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamCreateWithFlags");
  return_value = fn == nullptr ? function_not_found() : fn(&pStream, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pStream, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamCreateWithPriority(conn_t *conn) {
  cudaStream_t pStream{};
  pStream = {};
  unsigned int flags;
  int priority;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t *, unsigned int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &priority, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamCreateWithPriority");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pStream, flags, priority);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pStream, sizeof(cudaStream_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamDestroy(conn_t *conn) {
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamGetAttribute(conn_t *conn) {
  cudaStream_t hStream;
  cudaLaunchAttributeID attr;
  cudaLaunchAttributeValue value_out{};
  value_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaLaunchAttributeID,
                               cudaLaunchAttributeValue *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(cudaLaunchAttributeID)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(hStream, attr, &value_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value_out, sizeof(cudaLaunchAttributeValue)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaStreamGetCaptureInfo(conn_t *conn) {
  cudaStream_t stream;
  enum cudaStreamCaptureStatus captureStatus_out {};
  captureStatus_out = {};
  unsigned long long *id_out_null_check;
  unsigned long long id_out;
  cudaGraph_t *graph_out_null_check;
  cudaGraph_t graph_out;
  const cudaGraphNode_t **dependencies_out_null_check;
  const cudaGraphNode_t *dependencies_out;
  const cudaGraphEdgeData **edgeData_out_null_check;
  const cudaGraphEdgeData *edgeData_out;
  size_t *numDependencies_out_null_check;
  size_t numDependencies_out;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, enum cudaStreamCaptureStatus *,
                               unsigned long long *, cudaGraph_t *,
                               const cudaGraphNode_t **,
                               const cudaGraphEdgeData **, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &id_out_null_check, sizeof(unsigned long long *)) < 0 ||
      rpc_read(conn, &graph_out_null_check, sizeof(cudaGraph_t *)) < 0 ||
      rpc_read(conn, &dependencies_out_null_check,
               sizeof(const cudaGraphNode_t **)) < 0 ||
      rpc_read(conn, &edgeData_out_null_check,
               sizeof(const cudaGraphEdgeData **)) < 0 ||
      rpc_read(conn, &numDependencies_out_null_check, sizeof(size_t *)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetCaptureInfo");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(stream, &captureStatus_out,
               id_out_null_check ? &id_out : nullptr,
               graph_out_null_check ? &graph_out : nullptr,
               dependencies_out_null_check ? &dependencies_out : nullptr,
               edgeData_out_null_check ? &edgeData_out : nullptr,
               numDependencies_out_null_check ? &numDependencies_out : nullptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &captureStatus_out,
                sizeof(enum cudaStreamCaptureStatus)) < 0 ||
      rpc_write(conn, &id_out_null_check, sizeof(unsigned long long *)) < 0 ||
      (id_out_null_check &&
       rpc_write(conn, &id_out, sizeof(unsigned long long)) < 0) ||
      rpc_write(conn, &graph_out_null_check, sizeof(cudaGraph_t *)) < 0 ||
      (graph_out_null_check &&
       rpc_write(conn, &graph_out, sizeof(cudaGraph_t)) < 0) ||
      rpc_write(conn, &dependencies_out_null_check,
                sizeof(const cudaGraphNode_t **)) < 0 ||
      (dependencies_out_null_check &&
       rpc_write(conn, &dependencies_out, sizeof(const cudaGraphNode_t *)) <
           0) ||
      rpc_write(conn, &edgeData_out_null_check,
                sizeof(const cudaGraphEdgeData **)) < 0 ||
      (edgeData_out_null_check &&
       rpc_write(conn, &edgeData_out, sizeof(const cudaGraphEdgeData *)) < 0) ||
      rpc_write(conn, &numDependencies_out_null_check, sizeof(size_t *)) < 0 ||
      (numDependencies_out_null_check &&
       rpc_write(conn, &numDependencies_out, sizeof(size_t)) < 0) ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaStreamGetDevResource(conn_t *conn) {
  cudaStream_t hStream;
  cudaDevResource resource{};
  resource = {};
  enum cudaDevResourceType type;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaDevResource *,
                               enum cudaDevResourceType);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &type, sizeof(enum cudaDevResourceType)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetDevResource");
  return_value =
      fn == nullptr ? function_not_found() : fn(hStream, &resource, type);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resource, sizeof(cudaDevResource)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaStreamGetDevice(conn_t *conn) {
  cudaStream_t hStream;
  int device{};
  device = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetDevice");
  return_value = fn == nullptr ? function_not_found() : fn(hStream, &device);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &device, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaStreamGetFlags(conn_t *conn) {
  cudaStream_t hStream;
  unsigned int flags{};
  flags = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetFlags");
  return_value = fn == nullptr ? function_not_found() : fn(hStream, &flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &flags, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaStreamGetId(conn_t *conn) {
  cudaStream_t hStream;
  unsigned long long streamId{};
  streamId = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, unsigned long long *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetId");
  return_value = fn == nullptr ? function_not_found() : fn(hStream, &streamId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &streamId, sizeof(unsigned long long)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaStreamGetPriority(conn_t *conn) {
  cudaStream_t hStream;
  int priority{};
  priority = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamGetPriority");
  return_value = fn == nullptr ? function_not_found() : fn(hStream, &priority);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &priority, sizeof(int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamIsCapturing(conn_t *conn) {
  cudaStream_t stream;
  enum cudaStreamCaptureStatus pCaptureStatus {};
  pCaptureStatus = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, enum cudaStreamCaptureStatus *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamIsCapturing");
  return_value =
      fn == nullptr ? function_not_found() : fn(stream, &pCaptureStatus);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pCaptureStatus, sizeof(enum cudaStreamCaptureStatus)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaStreamSetAttribute(conn_t *conn) {
  cudaStream_t hStream;
  cudaLaunchAttributeID attr;
  cudaLaunchAttributeValue value{};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaLaunchAttributeID,
                               const cudaLaunchAttributeValue *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hStream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(cudaLaunchAttributeID)) < 0 ||
      rpc_read(conn, &value, sizeof(const cudaLaunchAttributeValue)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamSetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(hStream, attr, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaStreamUpdateCaptureDependencies(conn_t *conn) {
  cudaStream_t stream;
  size_t numDependencies;
  cudaGraphNode_t *dependencies = nullptr;
  size_t dependencies_size;
  cudaGraphEdgeData *dependencyData = nullptr;
  size_t dependencyData_size;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaGraphNode_t *,
                               const cudaGraphEdgeData *, size_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  dependencies_size = numDependencies * sizeof(cudaGraphNode_t);
  dependencies = (cudaGraphNode_t *)malloc(dependencies_size);
  if (dependencies_size != 0 && dependencies == nullptr)
    goto ERROR_0;
  if ((dependencies_size != 0 &&
       rpc_read(conn, dependencies, dependencies_size) < 0) ||
      false)
    goto ERROR_0;
  dependencyData_size = numDependencies * sizeof(const cudaGraphEdgeData);
  dependencyData = (cudaGraphEdgeData *)malloc(dependencyData_size);
  if (dependencyData_size != 0 && dependencyData == nullptr)
    goto ERROR_0;
  if ((dependencyData_size != 0 &&
       rpc_read(conn, dependencyData, dependencyData_size) < 0) ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamUpdateCaptureDependencies");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(stream,
               (numDependencies * sizeof(cudaGraphNode_t) == 0 ? nullptr
                                                               : dependencies),
               (numDependencies * sizeof(const cudaGraphEdgeData) == 0
                    ? nullptr
                    : dependencyData),
               numDependencies, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dependencyData);
  free((void *)dependencies);
  return 0;
ERROR_0:
  free((void *)dependencyData);
  free((void *)dependencies);
  return -1;
}

#endif

int handle_cudaStreamWaitEvent(conn_t *conn) {
  cudaStream_t stream;
  cudaEvent_t event;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaStream_t, cudaEvent_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaStreamWaitEvent");
  return_value =
      fn == nullptr ? function_not_found() : fn(stream, event, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaThreadExchangeStreamCaptureMode(conn_t *conn) {
  enum cudaStreamCaptureMode mode {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(enum cudaStreamCaptureMode *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &mode, sizeof(enum cudaStreamCaptureMode)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaThreadExchangeStreamCaptureMode");
  return_value = fn == nullptr ? function_not_found() : fn(&mode);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mode, sizeof(enum cudaStreamCaptureMode)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaUserObjectCreate(conn_t *conn) {
  cudaUserObject_t object_out{};
  object_out = {};
  void *ptr;
  cudaHostFn_t destroy;
  unsigned int initialRefcount;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaUserObject_t *, void *, cudaHostFn_t,
                               unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &ptr, sizeof(void *)) < 0 ||
      rpc_read(conn, &destroy, sizeof(cudaHostFn_t)) < 0 ||
      rpc_read(conn, &initialRefcount, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaUserObjectCreate");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&object_out, ptr, destroy, initialRefcount, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &object_out, sizeof(cudaUserObject_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaUserObjectRelease(conn_t *conn) {
  cudaUserObject_t object;
  unsigned int count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaUserObject_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &object, sizeof(cudaUserObject_t)) < 0 ||
      rpc_read(conn, &count, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaUserObjectRelease");
  return_value = fn == nullptr ? function_not_found() : fn(object, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaUserObjectRetain(conn_t *conn) {
  cudaUserObject_t object;
  unsigned int count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaUserObject_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &object, sizeof(cudaUserObject_t)) < 0 ||
      rpc_read(conn, &count, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaUserObjectRetain");
  return_value = fn == nullptr ? function_not_found() : fn(object, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaWaitExternalSemaphoresAsync(conn_t *conn) {
  unsigned int numExtSems;
  cudaExternalSemaphore_t *extSemArray = nullptr;
  size_t extSemArray_size;
  struct cudaExternalSemaphoreWaitParams *paramsArray = nullptr;
  size_t paramsArray_size;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(const cudaExternalSemaphore_t *,
                               const struct cudaExternalSemaphoreWaitParams *,
                               unsigned int, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &numExtSems, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;
  extSemArray_size = numExtSems * sizeof(const cudaExternalSemaphore_t);
  extSemArray = (cudaExternalSemaphore_t *)malloc(extSemArray_size);
  if (extSemArray_size != 0 && extSemArray == nullptr)
    goto ERROR_0;
  if ((extSemArray_size != 0 &&
       rpc_read(conn, extSemArray, extSemArray_size) < 0) ||
      false)
    goto ERROR_0;
  paramsArray_size =
      numExtSems * sizeof(const struct cudaExternalSemaphoreWaitParams);
  paramsArray =
      (struct cudaExternalSemaphoreWaitParams *)malloc(paramsArray_size);
  if (paramsArray_size != 0 && paramsArray == nullptr)
    goto ERROR_0;
  if ((paramsArray_size != 0 &&
       rpc_read(conn, paramsArray, paramsArray_size) < 0) ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaWaitExternalSemaphoresAsync");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn((numExtSems * sizeof(const cudaExternalSemaphore_t) == 0
                    ? nullptr
                    : extSemArray),
               (numExtSems *
                            sizeof(
                                const struct cudaExternalSemaphoreWaitParams) ==
                        0
                    ? nullptr
                    : paramsArray),
               numExtSems, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)paramsArray);
  free((void *)extSemArray);
  return 0;
ERROR_0:
  free((void *)paramsArray);
  free((void *)extSemArray);
  return -1;
}

int handle_cudaGraphAddChildGraphNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  cudaGraph_t childGraph;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t,
                               const cudaGraphNode_t *, size_t, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &childGraph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddChildGraphNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, childGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphAddDependencies(conn_t *conn) {
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *from = nullptr;
  size_t from_size;
  cudaGraphNode_t *to = nullptr;
  size_t to_size;
  cudaGraphEdgeData *edgeData = nullptr;
  size_t edgeData_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, const cudaGraphNode_t *,
                               const cudaGraphNode_t *,
                               const cudaGraphEdgeData *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  from_size = numDependencies * sizeof(const cudaGraphNode_t);
  from = (cudaGraphNode_t *)malloc(from_size);
  if (from_size != 0 && from == nullptr)
    goto ERROR_0;
  if ((from_size != 0 && rpc_read(conn, from, from_size) < 0) || false)
    goto ERROR_0;
  to_size = numDependencies * sizeof(const cudaGraphNode_t);
  to = (cudaGraphNode_t *)malloc(to_size);
  if (to_size != 0 && to == nullptr)
    goto ERROR_0;
  if ((to_size != 0 && rpc_read(conn, to, to_size) < 0) || false)
    goto ERROR_0;
  edgeData_size = numDependencies * sizeof(const cudaGraphEdgeData);
  edgeData = (cudaGraphEdgeData *)malloc(edgeData_size);
  if (edgeData_size != 0 && edgeData == nullptr)
    goto ERROR_0;
  if ((edgeData_size != 0 && rpc_read(conn, edgeData, edgeData_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddDependencies");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(graph,
               (numDependencies * sizeof(const cudaGraphNode_t) == 0 ? nullptr
                                                                     : from),
               (numDependencies * sizeof(const cudaGraphNode_t) == 0 ? nullptr
                                                                     : to),
               (numDependencies * sizeof(const cudaGraphEdgeData) == 0
                    ? nullptr
                    : edgeData),
               numDependencies);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return 0;
ERROR_0:
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return -1;
}

#endif

int handle_cudaGraphAddEmptyNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t,
                               const cudaGraphNode_t *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddEmptyNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddEventRecordNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t,
                               const cudaGraphNode_t *, size_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddEventRecordNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddEventWaitNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t,
                               const cudaGraphNode_t *, size_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddEventWaitNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddExternalSemaphoresSignalNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaExternalSemaphoreSignalNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *, size_t,
      const struct cudaExternalSemaphoreSignalNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddExternalSemaphoresSignalNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddExternalSemaphoresWaitNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaExternalSemaphoreWaitNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *, size_t,
      const struct cudaExternalSemaphoreWaitNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddExternalSemaphoresWaitNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddHostNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaHostNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *,
                      size_t, const struct cudaHostNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaHostNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddHostNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddKernelNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaKernelNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *,
                      size_t, const struct cudaKernelNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaKernelNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddKernelNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddMemAllocNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaMemAllocNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_accessDescs_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *,
                      size_t, struct cudaMemAllocNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_accessDescs_buf.resize(nodeParams.accessDescCount *
                                          sizeof(*nodeParams.accessDescs)),
        false)) ||
      (nodeParams.accessDescCount != 0 &&
       rpc_read(conn, nodeParams_accessDescs_buf.data(),
                nodeParams_accessDescs_buf.size()) < 0) ||
      ((nodeParams.accessDescs = (decltype(nodeParams.accessDescs))
                                     nodeParams_accessDescs_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddMemAllocNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      rpc_write(conn, nodeParams.accessDescs,
                nodeParams.accessDescCount * sizeof(*nodeParams.accessDescs)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddMemFreeNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  void *dptr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t,
                               const cudaGraphNode_t *, size_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &dptr, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddMemFreeNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, dptr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

int handle_cudaGraphAddMemsetNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  struct cudaMemsetParams pMemsetParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *,
                      size_t, const struct cudaMemsetParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      rpc_read(conn, &pMemsetParams, sizeof(const struct cudaMemsetParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddMemsetNode");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pGraphNode, graph,
                          (numDependencies * sizeof(const cudaGraphNode_t) == 0
                               ? nullptr
                               : pDependencies),
                          numDependencies, &pMemsetParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)pDependencies);
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphAddNode(conn_t *conn) {
  cudaGraphNode_t pGraphNode{};
  pGraphNode = {};
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *pDependencies = nullptr;
  size_t pDependencies_size;
  cudaGraphEdgeData *dependencyData = nullptr;
  size_t dependencyData_size;
  struct cudaGraphNodeParams nodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaGraphNode_t *, cudaGraph_t, const cudaGraphNode_t *,
      const cudaGraphEdgeData *, size_t, struct cudaGraphNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  pDependencies_size = numDependencies * sizeof(const cudaGraphNode_t);
  pDependencies = (cudaGraphNode_t *)malloc(pDependencies_size);
  if (pDependencies_size != 0 && pDependencies == nullptr)
    goto ERROR_0;
  if ((pDependencies_size != 0 &&
       rpc_read(conn, pDependencies, pDependencies_size) < 0) ||
      false)
    goto ERROR_0;
  dependencyData_size = numDependencies * sizeof(const cudaGraphEdgeData);
  dependencyData = (cudaGraphEdgeData *)malloc(dependencyData_size);
  if (dependencyData_size != 0 && dependencyData == nullptr)
    goto ERROR_0;
  if ((dependencyData_size != 0 &&
       rpc_read(conn, dependencyData, dependencyData_size) < 0) ||
      rpc_read(conn, &nodeParams, sizeof(struct cudaGraphNodeParams)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphAddNode");
  return_value =
      fn == nullptr ? function_not_found()
                    : fn(&pGraphNode, graph,
                         (numDependencies * sizeof(const cudaGraphNode_t) == 0
                              ? nullptr
                              : pDependencies),
                         (numDependencies * sizeof(const cudaGraphEdgeData) == 0
                              ? nullptr
                              : dependencyData),
                         numDependencies, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &nodeParams, sizeof(struct cudaGraphNodeParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dependencyData);
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)dependencyData);
  free((void *)pDependencies);
  return -1;
}

#endif

int handle_cudaGraphChildGraphNodeGetGraph(conn_t *conn) {
  cudaGraphNode_t node;
  cudaGraph_t pGraph{};
  pGraph = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaGraph_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphChildGraphNodeGetGraph");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraph, sizeof(cudaGraph_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphClone(conn_t *conn) {
  cudaGraph_t pGraphClone{};
  pGraphClone = {};
  cudaGraph_t originalGraph;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t *, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &originalGraph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphClone");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pGraphClone, originalGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphClone, sizeof(cudaGraph_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphConditionalHandleCreate(conn_t *conn) {
  cudaGraphConditionalHandle pHandle_out{};
  pHandle_out = {};
  cudaGraph_t graph;
  unsigned int defaultLaunchValue;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphConditionalHandle *, cudaGraph_t,
                               unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &defaultLaunchValue, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphConditionalHandleCreate");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pHandle_out, graph, defaultLaunchValue, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pHandle_out, sizeof(cudaGraphConditionalHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphConditionalHandleCreate_v2(conn_t *conn) {
  cudaGraphConditionalHandle pHandle_out{};
  pHandle_out = {};
  cudaGraph_t graph;
  cudaExecutionContext_t ctx;
  unsigned int defaultLaunchValue;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphConditionalHandle *, cudaGraph_t,
                      cudaExecutionContext_t, unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &ctx, sizeof(cudaExecutionContext_t)) < 0 ||
      rpc_read(conn, &defaultLaunchValue, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphConditionalHandleCreate_v2");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(&pHandle_out, graph, ctx, defaultLaunchValue, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pHandle_out, sizeof(cudaGraphConditionalHandle)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphCreate(conn_t *conn) {
  cudaGraph_t pGraph{};
  pGraph = {};
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphCreate");
  return_value = fn == nullptr ? function_not_found() : fn(&pGraph, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraph, sizeof(cudaGraph_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphDebugDotPrint(conn_t *conn) {
  cudaGraph_t graph;
  const char *path = nullptr;
  std::size_t path_len;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, const char *, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &path_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  path = (const char *)malloc(path_len);
  if ((path_len != 0 && path == nullptr) ||
      rpc_read(conn, (void *)path, path_len) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphDebugDotPrint");
  return_value = fn == nullptr ? function_not_found() : fn(graph, path, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)path);
  return 0;
ERROR_0:
  free((void *)path);
  return -1;
}

int handle_cudaGraphDestroy(conn_t *conn) {
  cudaGraph_t graph;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(graph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphDestroyNode(conn_t *conn) {
  cudaGraphNode_t node;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphDestroyNode");
  return_value = fn == nullptr ? function_not_found() : fn(node);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphEventRecordNodeGetEvent(conn_t *conn) {
  cudaGraphNode_t node;
  cudaEvent_t event_out{};
  event_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaEvent_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphEventRecordNodeGetEvent");
  return_value = fn == nullptr ? function_not_found() : fn(node, &event_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &event_out, sizeof(cudaEvent_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphEventRecordNodeSetEvent(conn_t *conn) {
  cudaGraphNode_t node;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphEventRecordNodeSetEvent");
  return_value = fn == nullptr ? function_not_found() : fn(node, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphEventWaitNodeGetEvent(conn_t *conn) {
  cudaGraphNode_t node;
  cudaEvent_t event_out{};
  event_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaEvent_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphEventWaitNodeGetEvent");
  return_value = fn == nullptr ? function_not_found() : fn(node, &event_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &event_out, sizeof(cudaEvent_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphEventWaitNodeSetEvent(conn_t *conn) {
  cudaGraphNode_t node;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphEventWaitNodeSetEvent");
  return_value = fn == nullptr ? function_not_found() : fn(node, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecChildGraphNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t node;
  cudaGraph_t childGraph;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &childGraph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecChildGraphNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, node, childGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecDestroy(conn_t *conn) {
  cudaGraphExec_t graphExec;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graphExec, sizeof(cudaGraphExec_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecDestroy");
  return_value = fn == nullptr ? function_not_found() : fn(graphExec);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecEventRecordNodeSetEvent(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecEventRecordNodeSetEvent");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecEventWaitNodeSetEvent(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  cudaEvent_t event;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t, cudaEvent_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &event, sizeof(cudaEvent_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecEventWaitNodeSetEvent");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, event);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecExternalSemaphoresSignalNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreSignalNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                      const struct cudaExternalSemaphoreSignalNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn =
      cudart_symbol<fn_t>("cudaGraphExecExternalSemaphoresSignalNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecExternalSemaphoresWaitNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreWaitNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                      const struct cudaExternalSemaphoreWaitNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecExternalSemaphoresWaitNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphExecGetFlags(conn_t *conn) {
  cudaGraphExec_t graphExec;
  unsigned long long flags{};
  flags = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, unsigned long long *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graphExec, sizeof(cudaGraphExec_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecGetFlags");
  return_value = fn == nullptr ? function_not_found() : fn(graphExec, &flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &flags, sizeof(unsigned long long)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphExecGetId(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  unsigned int graphID{};
  graphID = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecGetId");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, &graphID);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &graphID, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphExecHostNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t node;
  struct cudaHostNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                               const struct cudaHostNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaHostNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecHostNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecKernelNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t node;
  struct cudaKernelNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                               const struct cudaKernelNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaKernelNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecKernelNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExecMemsetNodeSetParams(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t node;
  struct cudaMemsetParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                               const struct cudaMemsetParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaMemsetParams)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecMemsetNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphExecNodeSetParams(conn_t *conn) {
  cudaGraphExec_t graphExec;
  cudaGraphNode_t node;
  struct cudaGraphNodeParams nodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t,
                               struct cudaGraphNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(struct cudaGraphNodeParams)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecNodeSetParams");
  return_value =
      fn == nullptr ? function_not_found() : fn(graphExec, node, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 12000
int handle_cudaGraphExecUpdate(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraph_t hGraph;
  cudaGraphExecUpdateResultInfo resultInfo{};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraph_t,
                               cudaGraphExecUpdateResultInfo *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hGraph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &resultInfo, sizeof(cudaGraphExecUpdateResultInfo)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExecUpdate");
  return_value = fn == nullptr ? function_not_found()
                               : fn(hGraphExec, hGraph, &resultInfo);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &resultInfo, sizeof(cudaGraphExecUpdateResultInfo)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphExternalSemaphoresSignalNodeGetParams(conn_t *conn) {
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreSignalNodeParams params_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t,
                               struct cudaExternalSemaphoreSignalNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExternalSemaphoresSignalNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &params_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &params_out, sizeof(params_out)) < 0 ||
      rpc_write(conn, params_out.extSemArray,
                params_out.numExtSems * sizeof(*params_out.extSemArray)) < 0 ||
      rpc_write(conn, params_out.paramsArray,
                params_out.numExtSems * sizeof(*params_out.paramsArray)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExternalSemaphoresSignalNodeSetParams(conn_t *conn) {
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreSignalNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaGraphNode_t, const struct cudaExternalSemaphoreSignalNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExternalSemaphoresSignalNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExternalSemaphoresWaitNodeGetParams(conn_t *conn) {
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreWaitNodeParams params_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t,
                               struct cudaExternalSemaphoreWaitNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExternalSemaphoresWaitNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &params_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &params_out, sizeof(params_out)) < 0 ||
      rpc_write(conn, params_out.extSemArray,
                params_out.numExtSems * sizeof(*params_out.extSemArray)) < 0 ||
      rpc_write(conn, params_out.paramsArray,
                params_out.numExtSems * sizeof(*params_out.paramsArray)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphExternalSemaphoresWaitNodeSetParams(conn_t *conn) {
  cudaGraphNode_t hNode;
  struct cudaExternalSemaphoreWaitNodeParams nodeParams = {};
  std::vector<unsigned char> nodeParams_extSemArray_buf;
  std::vector<unsigned char> nodeParams_paramsArray_buf;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(
      cudaGraphNode_t, const struct cudaExternalSemaphoreWaitNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(nodeParams)) < 0 ||
      ((nodeParams_extSemArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.extSemArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_extSemArray_buf.data(),
                nodeParams_extSemArray_buf.size()) < 0) ||
      ((nodeParams.extSemArray = (decltype(nodeParams.extSemArray))
                                     nodeParams_extSemArray_buf.data()),
       false) ||
      ((nodeParams_paramsArray_buf.resize(nodeParams.numExtSems *
                                          sizeof(*nodeParams.paramsArray)),
        false)) ||
      (nodeParams.numExtSems != 0 &&
       rpc_read(conn, nodeParams_paramsArray_buf.data(),
                nodeParams_paramsArray_buf.size()) < 0) ||
      ((nodeParams.paramsArray = (decltype(nodeParams.paramsArray))
                                     nodeParams_paramsArray_buf.data()),
       false) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphExternalSemaphoresWaitNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphGetEdges(conn_t *conn) {
  cudaGraph_t graph;
  cudaGraphNode_t *from = nullptr;
  uint8_t from_null = 0;
  cudaGraphNode_t *to = nullptr;
  uint8_t to_null = 0;
  cudaGraphEdgeData *edgeData = nullptr;
  uint8_t edgeData_null = 0;
  size_t numEdges = 0;
  size_t numEdges_requested = 0;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraph_t, cudaGraphNode_t *, cudaGraphNode_t *,
                      cudaGraphEdgeData *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &from_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!from_null) {
    from = (cudaGraphNode_t *)malloc(
        (numEdges_requested != 0 ? numEdges_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (from == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &to_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!to_null) {
    to = (cudaGraphNode_t *)malloc(
        (numEdges_requested != 0 ? numEdges_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (to == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &edgeData_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!edgeData_null) {
    edgeData = (cudaGraphEdgeData *)malloc(
        (numEdges_requested != 0 ? numEdges_requested : 1) *
        sizeof(cudaGraphEdgeData));
    if (edgeData == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &numEdges_requested, sizeof(size_t)) < 0 ||
      ((numEdges = numEdges_requested), false) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphGetEdges");
  return_value = fn == nullptr ? function_not_found()
                               : fn(graph, from, to, edgeData, &numEdges);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!from_null &&
       rpc_write(
           conn, from,
           (numEdges < numEdges_requested ? numEdges : numEdges_requested) *
               sizeof(cudaGraphNode_t)) < 0) ||
      (!to_null &&
       rpc_write(
           conn, to,
           (numEdges < numEdges_requested ? numEdges : numEdges_requested) *
               sizeof(cudaGraphNode_t)) < 0) ||
      (!edgeData_null &&
       rpc_write(
           conn, edgeData,
           (numEdges < numEdges_requested ? numEdges : numEdges_requested) *
               sizeof(cudaGraphEdgeData)) < 0) ||
      rpc_write(conn, &numEdges, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return 0;
ERROR_0:
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphGetId(conn_t *conn) {
  cudaGraph_t hGraph;
  unsigned int graphID{};
  graphID = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphGetId");
  return_value = fn == nullptr ? function_not_found() : fn(hGraph, &graphID);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &graphID, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphGetNodes(conn_t *conn) {
  cudaGraph_t graph;
  cudaGraphNode_t *nodes = nullptr;
  uint8_t nodes_null = 0;
  size_t numNodes = 0;
  size_t numNodes_requested = 0;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, cudaGraphNode_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &nodes_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!nodes_null) {
    nodes = (cudaGraphNode_t *)malloc(
        (numNodes_requested != 0 ? numNodes_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (nodes == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &numNodes_requested, sizeof(size_t)) < 0 ||
      ((numNodes = numNodes_requested), false) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphGetNodes");
  return_value =
      fn == nullptr ? function_not_found() : fn(graph, nodes, &numNodes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!nodes_null &&
       rpc_write(
           conn, nodes,
           (numNodes < numNodes_requested ? numNodes : numNodes_requested) *
               sizeof(cudaGraphNode_t)) < 0) ||
      rpc_write(conn, &numNodes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)nodes);
  return 0;
ERROR_0:
  free((void *)nodes);
  return -1;
}

int handle_cudaGraphGetRootNodes(conn_t *conn) {
  cudaGraph_t graph;
  cudaGraphNode_t *pRootNodes = nullptr;
  uint8_t pRootNodes_null = 0;
  size_t pNumRootNodes = 0;
  size_t pNumRootNodes_requested = 0;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, cudaGraphNode_t *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &pRootNodes_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!pRootNodes_null) {
    pRootNodes = (cudaGraphNode_t *)malloc(
        (pNumRootNodes_requested != 0 ? pNumRootNodes_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (pRootNodes == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &pNumRootNodes_requested, sizeof(size_t)) < 0 ||
      ((pNumRootNodes = pNumRootNodes_requested), false) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphGetRootNodes");
  return_value = fn == nullptr ? function_not_found()
                               : fn(graph, pRootNodes, &pNumRootNodes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!pRootNodes_null && rpc_write(conn, pRootNodes,
                                     (pNumRootNodes < pNumRootNodes_requested
                                          ? pNumRootNodes
                                          : pNumRootNodes_requested) *
                                         sizeof(cudaGraphNode_t)) < 0) ||
      rpc_write(conn, &pNumRootNodes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)pRootNodes);
  return 0;
ERROR_0:
  free((void *)pRootNodes);
  return -1;
}

int handle_cudaGraphHostNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaHostNodeParams pNodeParams {};
  pNodeParams = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, struct cudaHostNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphHostNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pNodeParams, sizeof(struct cudaHostNodeParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphHostNodeSetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaHostNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t, const struct cudaHostNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaHostNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphHostNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphInstantiate(conn_t *conn) {
  cudaGraphExec_t pGraphExec{};
  pGraphExec = {};
  cudaGraph_t graph;
  unsigned long long flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphExec_t *, cudaGraph_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphInstantiate");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pGraphExec, graph, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphInstantiateWithFlags(conn_t *conn) {
  cudaGraphExec_t pGraphExec{};
  pGraphExec = {};
  cudaGraph_t graph;
  unsigned long long flags;
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphExec_t *, cudaGraph_t, unsigned long long);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned long long)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphInstantiateWithFlags");
  return_value =
      fn == nullptr ? function_not_found() : fn(&pGraphExec, graph, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphInstantiateWithParams(conn_t *conn) {
  cudaGraphExec_t pGraphExec{};
  pGraphExec = {};
  cudaGraph_t graph;
  cudaGraphInstantiateParams instantiateParams{};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t *, cudaGraph_t,
                               cudaGraphInstantiateParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &instantiateParams, sizeof(cudaGraphInstantiateParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphInstantiateWithParams");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&pGraphExec, graph, &instantiateParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_write(conn, &instantiateParams, sizeof(cudaGraphInstantiateParams)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphKernelNodeCopyAttributes(conn_t *conn) {
  cudaGraphNode_t hDst;
  cudaGraphNode_t hSrc;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaGraphNode_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hDst, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &hSrc, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphKernelNodeCopyAttributes");
  return_value = fn == nullptr ? function_not_found() : fn(hDst, hSrc);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphKernelNodeGetAttribute(conn_t *conn) {
  cudaGraphNode_t hNode;
  cudaLaunchAttributeID attr;
  cudaLaunchAttributeValue value_out{};
  value_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaLaunchAttributeID,
                               cudaLaunchAttributeValue *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(cudaLaunchAttributeID)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphKernelNodeGetAttribute");
  return_value =
      fn == nullptr ? function_not_found() : fn(hNode, attr, &value_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &value_out, sizeof(cudaLaunchAttributeValue)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphKernelNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaKernelNodeParams pNodeParams {};
  pNodeParams = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, struct cudaKernelNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphKernelNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pNodeParams, sizeof(struct cudaKernelNodeParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphKernelNodeSetAttribute(conn_t *conn) {
  cudaGraphNode_t hNode;
  cudaLaunchAttributeID attr;
  cudaLaunchAttributeValue value{};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaLaunchAttributeID,
                               const cudaLaunchAttributeValue *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &attr, sizeof(cudaLaunchAttributeID)) < 0 ||
      rpc_read(conn, &value, sizeof(const cudaLaunchAttributeValue)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphKernelNodeSetAttribute");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, attr, &value);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphKernelNodeSetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaKernelNodeParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t, const struct cudaKernelNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaKernelNodeParams)) <
          0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphKernelNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphLaunch(conn_t *conn) {
  cudaGraphExec_t graphExec;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphLaunch");
  return_value = fn == nullptr ? function_not_found() : fn(graphExec, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphMemAllocNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaMemAllocNodeParams params_out = {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t, struct cudaMemAllocNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphMemAllocNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &params_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &params_out, sizeof(params_out)) < 0 ||
      rpc_write(conn, params_out.accessDescs,
                params_out.accessDescCount * sizeof(*params_out.accessDescs)) <
          0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphMemFreeNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  void *dptr_out = nullptr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;
  dptr_out = (void *)malloc(8);
  if ((8 != 0 && dptr_out == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphMemFreeNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, dptr_out);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, dptr_out, 8) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)dptr_out);
  return 0;
ERROR_0:
  free((void *)dptr_out);
  return -1;
}

int handle_cudaGraphMemsetNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaMemsetParams pNodeParams {};
  pNodeParams = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, struct cudaMemsetParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphMemsetNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pNodeParams, sizeof(struct cudaMemsetParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphMemsetNodeSetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaMemsetParams pNodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphNode_t, const struct cudaMemsetParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pNodeParams, sizeof(const struct cudaMemsetParams)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphMemsetNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pNodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphNodeFindInClone(conn_t *conn) {
  cudaGraphNode_t pNode{};
  pNode = {};
  cudaGraphNode_t originalNode;
  cudaGraph_t clonedGraph;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t *, cudaGraphNode_t, cudaGraph_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &originalNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &clonedGraph, sizeof(cudaGraph_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeFindInClone");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&pNode, originalNode, clonedGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetContainingGraph(conn_t *conn) {
  cudaGraphNode_t hNode;
  cudaGraph_t phGraph{};
  phGraph = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaGraph_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetContainingGraph");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &phGraph);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &phGraph, sizeof(cudaGraph_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetDependencies(conn_t *conn) {
  cudaGraphNode_t node;
  cudaGraphNode_t *pDependencies = nullptr;
  uint8_t pDependencies_null = 0;
  cudaGraphEdgeData *edgeData = nullptr;
  uint8_t edgeData_null = 0;
  size_t pNumDependencies = 0;
  size_t pNumDependencies_requested = 0;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaGraphNode_t *,
                               cudaGraphEdgeData *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pDependencies_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!pDependencies_null) {
    pDependencies = (cudaGraphNode_t *)malloc(
        (pNumDependencies_requested != 0 ? pNumDependencies_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (pDependencies == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &edgeData_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!edgeData_null) {
    edgeData = (cudaGraphEdgeData *)malloc(
        (pNumDependencies_requested != 0 ? pNumDependencies_requested : 1) *
        sizeof(cudaGraphEdgeData));
    if (edgeData == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &pNumDependencies_requested, sizeof(size_t)) < 0 ||
      ((pNumDependencies = pNumDependencies_requested), false) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetDependencies");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(node, pDependencies, edgeData, &pNumDependencies);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!pDependencies_null &&
       rpc_write(conn, pDependencies,
                 (pNumDependencies < pNumDependencies_requested
                      ? pNumDependencies
                      : pNumDependencies_requested) *
                     sizeof(cudaGraphNode_t)) < 0) ||
      (!edgeData_null &&
       rpc_write(conn, edgeData,
                 (pNumDependencies < pNumDependencies_requested
                      ? pNumDependencies
                      : pNumDependencies_requested) *
                     sizeof(cudaGraphEdgeData)) < 0) ||
      rpc_write(conn, &pNumDependencies, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)edgeData);
  free((void *)pDependencies);
  return 0;
ERROR_0:
  free((void *)edgeData);
  free((void *)pDependencies);
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetDependentNodes(conn_t *conn) {
  cudaGraphNode_t node;
  cudaGraphNode_t *pDependentNodes = nullptr;
  uint8_t pDependentNodes_null = 0;
  cudaGraphEdgeData *edgeData = nullptr;
  uint8_t edgeData_null = 0;
  size_t pNumDependentNodes = 0;
  size_t pNumDependentNodes_requested = 0;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, cudaGraphNode_t *,
                               cudaGraphEdgeData *, size_t *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &pDependentNodes_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!pDependentNodes_null) {
    pDependentNodes = (cudaGraphNode_t *)malloc(
        (pNumDependentNodes_requested != 0 ? pNumDependentNodes_requested : 1) *
        sizeof(cudaGraphNode_t));
    if (pDependentNodes == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &edgeData_null, sizeof(uint8_t)) < 0 || false)
    goto ERROR_0;
  if (!edgeData_null) {
    edgeData = (cudaGraphEdgeData *)malloc(
        (pNumDependentNodes_requested != 0 ? pNumDependentNodes_requested : 1) *
        sizeof(cudaGraphEdgeData));
    if (edgeData == nullptr)
      goto ERROR_0;
  }
  if (rpc_read(conn, &pNumDependentNodes_requested, sizeof(size_t)) < 0 ||
      ((pNumDependentNodes = pNumDependentNodes_requested), false) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetDependentNodes");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(node, pDependentNodes, edgeData, &pNumDependentNodes);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      (!pDependentNodes_null &&
       rpc_write(conn, pDependentNodes,
                 (pNumDependentNodes < pNumDependentNodes_requested
                      ? pNumDependentNodes
                      : pNumDependentNodes_requested) *
                     sizeof(cudaGraphNode_t)) < 0) ||
      (!edgeData_null &&
       rpc_write(conn, edgeData,
                 (pNumDependentNodes < pNumDependentNodes_requested
                      ? pNumDependentNodes
                      : pNumDependentNodes_requested) *
                     sizeof(cudaGraphEdgeData)) < 0) ||
      rpc_write(conn, &pNumDependentNodes, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)edgeData);
  free((void *)pDependentNodes);
  return 0;
ERROR_0:
  free((void *)edgeData);
  free((void *)pDependentNodes);
  return -1;
}

#endif

int handle_cudaGraphNodeGetEnabled(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  unsigned int isEnabled{};
  isEnabled = {};
  int request_id;
  cudaError_t return_value;
  using fn_t =
      cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetEnabled");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, &isEnabled);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &isEnabled, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetLocalId(conn_t *conn) {
  cudaGraphNode_t hNode;
  unsigned int nodeId{};
  nodeId = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, unsigned int *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetLocalId");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &nodeId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nodeId, sizeof(unsigned int)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaGraphNodeParams nodeParams {};
  nodeParams = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, struct cudaGraphNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &nodeParams, sizeof(struct cudaGraphNodeParams)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

#if CUDART_VERSION >= 13000
int handle_cudaGraphNodeGetToolsId(conn_t *conn) {
  cudaGraphNode_t hNode;
  unsigned long long toolsNodeId{};
  toolsNodeId = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, unsigned long long *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetToolsId");
  return_value = fn == nullptr ? function_not_found() : fn(hNode, &toolsNodeId);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &toolsNodeId, sizeof(unsigned long long)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphNodeGetType(conn_t *conn) {
  cudaGraphNode_t node;
  enum cudaGraphNodeType pType {};
  pType = {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, enum cudaGraphNodeType *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeGetType");
  return_value = fn == nullptr ? function_not_found() : fn(node, &pType);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &pType, sizeof(enum cudaGraphNodeType)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphNodeSetEnabled(conn_t *conn) {
  cudaGraphExec_t hGraphExec;
  cudaGraphNode_t hNode;
  unsigned int isEnabled;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaGraphNode_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &hGraphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &hNode, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &isEnabled, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeSetEnabled");
  return_value =
      fn == nullptr ? function_not_found() : fn(hGraphExec, hNode, isEnabled);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 12000
int handle_cudaGraphNodeSetParams(conn_t *conn) {
  cudaGraphNode_t node;
  struct cudaGraphNodeParams nodeParams {};
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphNode_t, struct cudaGraphNodeParams *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &node, sizeof(cudaGraphNode_t)) < 0 ||
      rpc_read(conn, &nodeParams, sizeof(struct cudaGraphNodeParams)) < 0 ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphNodeSetParams");
  return_value = fn == nullptr ? function_not_found() : fn(node, &nodeParams);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif

int handle_cudaGraphReleaseUserObject(conn_t *conn) {
  cudaGraph_t graph;
  cudaUserObject_t object;
  unsigned int count;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, cudaUserObject_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &object, sizeof(cudaUserObject_t)) < 0 ||
      rpc_read(conn, &count, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphReleaseUserObject");
  return_value =
      fn == nullptr ? function_not_found() : fn(graph, object, count);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle_cudaGraphRemoveDependencies(conn_t *conn) {
  cudaGraph_t graph;
  size_t numDependencies;
  cudaGraphNode_t *from = nullptr;
  size_t from_size;
  cudaGraphNode_t *to = nullptr;
  size_t to_size;
  cudaGraphEdgeData *edgeData = nullptr;
  size_t edgeData_size;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, const cudaGraphNode_t *,
                               const cudaGraphNode_t *,
                               const cudaGraphEdgeData *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &numDependencies, sizeof(size_t)) < 0 || false)
    goto ERROR_0;
  from_size = numDependencies * sizeof(const cudaGraphNode_t);
  from = (cudaGraphNode_t *)malloc(from_size);
  if (from_size != 0 && from == nullptr)
    goto ERROR_0;
  if ((from_size != 0 && rpc_read(conn, from, from_size) < 0) || false)
    goto ERROR_0;
  to_size = numDependencies * sizeof(const cudaGraphNode_t);
  to = (cudaGraphNode_t *)malloc(to_size);
  if (to_size != 0 && to == nullptr)
    goto ERROR_0;
  if ((to_size != 0 && rpc_read(conn, to, to_size) < 0) || false)
    goto ERROR_0;
  edgeData_size = numDependencies * sizeof(const cudaGraphEdgeData);
  edgeData = (cudaGraphEdgeData *)malloc(edgeData_size);
  if (edgeData_size != 0 && edgeData == nullptr)
    goto ERROR_0;
  if ((edgeData_size != 0 && rpc_read(conn, edgeData, edgeData_size) < 0) ||
      false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphRemoveDependencies");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(graph,
               (numDependencies * sizeof(const cudaGraphNode_t) == 0 ? nullptr
                                                                     : from),
               (numDependencies * sizeof(const cudaGraphNode_t) == 0 ? nullptr
                                                                     : to),
               (numDependencies * sizeof(const cudaGraphEdgeData) == 0
                    ? nullptr
                    : edgeData),
               numDependencies);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return 0;
ERROR_0:
  free((void *)edgeData);
  free((void *)to);
  free((void *)from);
  return -1;
}

#endif

int handle_cudaGraphRetainUserObject(conn_t *conn) {
  cudaGraph_t graph;
  cudaUserObject_t object;
  unsigned int count;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraph_t, cudaUserObject_t, unsigned int,
                               unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graph, sizeof(cudaGraph_t)) < 0 ||
      rpc_read(conn, &object, sizeof(cudaUserObject_t)) < 0 ||
      rpc_read(conn, &count, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphRetainUserObject");
  return_value =
      fn == nullptr ? function_not_found() : fn(graph, object, count, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphUpload(conn_t *conn) {
  cudaGraphExec_t graphExec;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphExec_t, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &graphExec, sizeof(cudaGraphExec_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphUpload");
  return_value = fn == nullptr ? function_not_found() : fn(graphExec, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsMapResources(conn_t *conn) {
  int count;
  cudaGraphicsResource_t *resources = nullptr;
  size_t resources_size;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, cudaGraphicsResource_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(int)) < 0 || count < 0 || false)
    goto ERROR_0;
  resources_size = count * sizeof(cudaGraphicsResource_t);
  resources = (cudaGraphicsResource_t *)malloc(resources_size);
  if (resources_size != 0 && resources == nullptr)
    goto ERROR_0;
  if ((resources_size != 0 && rpc_read(conn, resources, resources_size) < 0) ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsMapResources");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(count,
               (count * sizeof(cudaGraphicsResource_t) == 0 ? nullptr
                                                            : resources),
               stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)resources);
  return 0;
ERROR_0:
  free((void *)resources);
  return -1;
}

int handle_cudaGraphicsResourceGetMappedMipmappedArray(conn_t *conn) {
  cudaMipmappedArray_t mipmappedArray{};
  mipmappedArray = {};
  cudaGraphicsResource_t resource;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaMipmappedArray_t *, cudaGraphicsResource_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &resource, sizeof(cudaGraphicsResource_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsResourceGetMappedMipmappedArray");
  return_value =
      fn == nullptr ? function_not_found() : fn(&mipmappedArray, resource);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &mipmappedArray, sizeof(cudaMipmappedArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsResourceGetMappedPointer(conn_t *conn) {
  void *devPtr{};
  devPtr = {};
  size_t size{};
  size = {};
  cudaGraphicsResource_t resource;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(void **, size_t *, cudaGraphicsResource_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &resource, sizeof(cudaGraphicsResource_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsResourceGetMappedPointer");
  return_value =
      fn == nullptr ? function_not_found() : fn(&devPtr, &size, resource);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &devPtr, sizeof(void *)) < 0 ||
      rpc_write(conn, &size, sizeof(size_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsResourceSetMapFlags(conn_t *conn) {
  cudaGraphicsResource_t resource;
  unsigned int flags;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphicsResource_t, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &resource, sizeof(cudaGraphicsResource_t)) < 0 ||
      rpc_read(conn, &flags, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsResourceSetMapFlags");
  return_value = fn == nullptr ? function_not_found() : fn(resource, flags);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsSubResourceGetMappedArray(conn_t *conn) {
  cudaArray_t array{};
  array = {};
  cudaGraphicsResource_t resource;
  unsigned int arrayIndex;
  unsigned int mipLevel;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaArray_t *, cudaGraphicsResource_t,
                               unsigned int, unsigned int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &resource, sizeof(cudaGraphicsResource_t)) < 0 ||
      rpc_read(conn, &arrayIndex, sizeof(unsigned int)) < 0 ||
      rpc_read(conn, &mipLevel, sizeof(unsigned int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsSubResourceGetMappedArray");
  return_value = fn == nullptr ? function_not_found()
                               : fn(&array, resource, arrayIndex, mipLevel);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &array, sizeof(cudaArray_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle_cudaGraphicsUnmapResources(conn_t *conn) {
  int count;
  cudaGraphicsResource_t *resources = nullptr;
  size_t resources_size;
  cudaStream_t stream;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(int, cudaGraphicsResource_t *, cudaStream_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &count, sizeof(int)) < 0 || count < 0 || false)
    goto ERROR_0;
  resources_size = count * sizeof(cudaGraphicsResource_t);
  resources = (cudaGraphicsResource_t *)malloc(resources_size);
  if (resources_size != 0 && resources == nullptr)
    goto ERROR_0;
  if ((resources_size != 0 && rpc_read(conn, resources, resources_size) < 0) ||
      rpc_read(conn, &stream, sizeof(cudaStream_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsUnmapResources");
  return_value =
      fn == nullptr
          ? function_not_found()
          : fn(count,
               (count * sizeof(cudaGraphicsResource_t) == 0 ? nullptr
                                                            : resources),
               stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)resources);
  return 0;
ERROR_0:
  free((void *)resources);
  return -1;
}

int handle_cudaGraphicsUnregisterResource(conn_t *conn) {
  cudaGraphicsResource_t resource;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaGraphicsResource_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &resource, sizeof(cudaGraphicsResource_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("cudaGraphicsUnregisterResource");
  return_value = fn == nullptr ? function_not_found() : fn(resource);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle___cudaRegisterVar(conn_t *conn) {
  void **fatCubinHandle;
  char *hostVar;
  char *deviceAddress = nullptr;
  std::size_t deviceAddress_len;
  const char *deviceName = nullptr;
  std::size_t deviceName_len;
  int ext;
  size_t size;
  int constant;
  int global;
  int request_id;
  using fn_t =
      void (*)(void **, char *, char *, const char *, int, size_t, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &hostVar, sizeof(char *)) < 0 ||
      rpc_read(conn, &deviceAddress_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceAddress = (char *)malloc(deviceAddress_len);
  if ((deviceAddress_len != 0 && deviceAddress == nullptr) ||
      rpc_read(conn, (void *)deviceAddress, deviceAddress_len) < 0 ||
      rpc_read(conn, &deviceName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceName = (const char *)malloc(deviceName_len);
  if ((deviceName_len != 0 && deviceName == nullptr) ||
      rpc_read(conn, (void *)deviceName, deviceName_len) < 0 ||
      rpc_read(conn, &ext, sizeof(int)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &constant, sizeof(int)) < 0 ||
      rpc_read(conn, &global, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterVar");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, hostVar, deviceAddress, deviceName, ext, size, constant,
     global);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceName);
  free((void *)deviceAddress);
  return 0;
ERROR_0:
  free((void *)deviceName);
  free((void *)deviceAddress);
  return -1;
}

int handle___cudaRegisterManagedVar(conn_t *conn) {
  void **fatCubinHandle;
  void **hostVarPtrAddress;
  char *deviceAddress = nullptr;
  std::size_t deviceAddress_len;
  const char *deviceName = nullptr;
  std::size_t deviceName_len;
  int ext;
  size_t size;
  int constant;
  int global;
  int request_id;
  using fn_t =
      void (*)(void **, void **, char *, const char *, int, size_t, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &hostVarPtrAddress, sizeof(void **)) < 0 ||
      rpc_read(conn, &deviceAddress_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceAddress = (char *)malloc(deviceAddress_len);
  if ((deviceAddress_len != 0 && deviceAddress == nullptr) ||
      rpc_read(conn, (void *)deviceAddress, deviceAddress_len) < 0 ||
      rpc_read(conn, &deviceName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceName = (const char *)malloc(deviceName_len);
  if ((deviceName_len != 0 && deviceName == nullptr) ||
      rpc_read(conn, (void *)deviceName, deviceName_len) < 0 ||
      rpc_read(conn, &ext, sizeof(int)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 ||
      rpc_read(conn, &constant, sizeof(int)) < 0 ||
      rpc_read(conn, &global, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterManagedVar");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, hostVarPtrAddress, deviceAddress, deviceName, ext, size,
     constant, global);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceName);
  free((void *)deviceAddress);
  return 0;
ERROR_0:
  free((void *)deviceName);
  free((void *)deviceAddress);
  return -1;
}

int handle___cudaRegisterTexture(conn_t *conn) {
  void **fatCubinHandle;
  const void *hostVar;
  const void **deviceAddress;
  const char *deviceName = nullptr;
  std::size_t deviceName_len;
  int dim;
  int norm;
  int ext;
  int request_id;
  using fn_t = void (*)(void **, const void *, const void **, const char *, int,
                        int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &hostVar, sizeof(const void *)) < 0 ||
      rpc_read(conn, &deviceAddress, sizeof(const void **)) < 0 ||
      rpc_read(conn, &deviceName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceName = (const char *)malloc(deviceName_len);
  if ((deviceName_len != 0 && deviceName == nullptr) ||
      rpc_read(conn, (void *)deviceName, deviceName_len) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &norm, sizeof(int)) < 0 ||
      rpc_read(conn, &ext, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterTexture");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, hostVar, deviceAddress, deviceName, dim, norm, ext);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceName);
  return 0;
ERROR_0:
  free((void *)deviceName);
  return -1;
}

int handle___cudaRegisterSurface(conn_t *conn) {
  void **fatCubinHandle;
  const void *hostVar;
  const void **deviceAddress;
  const char *deviceName = nullptr;
  std::size_t deviceName_len;
  int dim;
  int ext;
  int request_id;
  using fn_t =
      void (*)(void **, const void *, const void **, const char *, int, int);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &hostVar, sizeof(const void *)) < 0 ||
      rpc_read(conn, &deviceAddress, sizeof(const void **)) < 0 ||
      rpc_read(conn, &deviceName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceName = (const char *)malloc(deviceName_len);
  if ((deviceName_len != 0 && deviceName == nullptr) ||
      rpc_read(conn, (void *)deviceName, deviceName_len) < 0 ||
      rpc_read(conn, &dim, sizeof(int)) < 0 ||
      rpc_read(conn, &ext, sizeof(int)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterSurface");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, hostVar, deviceAddress, deviceName, dim, ext);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceName);
  return 0;
ERROR_0:
  free((void *)deviceName);
  return -1;
}

int handle___cudaRegisterHostVar(conn_t *conn) {
  void **fatCubinHandle;
  const char *deviceName = nullptr;
  std::size_t deviceName_len;
  char *hostVar;
  size_t size;
  int request_id;
  using fn_t = void (*)(void **, const char *, char *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &deviceName_len, sizeof(std::size_t)) < 0)
    goto ERROR_0;
  deviceName = (const char *)malloc(deviceName_len);
  if ((deviceName_len != 0 && deviceName == nullptr) ||
      rpc_read(conn, (void *)deviceName, deviceName_len) < 0 ||
      rpc_read(conn, &hostVar, sizeof(char *)) < 0 ||
      rpc_read(conn, &size, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterHostVar");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, deviceName, hostVar, size);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)deviceName);
  return 0;
ERROR_0:
  free((void *)deviceName);
  return -1;
}

int handle___cudaRegisterUnifiedTable(conn_t *conn) {
  void **fatCubinHandle;
  void *functionTable;
  size_t functionWindowSize;
  void *dataTable;
  size_t dataWindowSize;
  int request_id;
  using fn_t = void (*)(void **, void *, size_t, void *, size_t);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 ||
      rpc_read(conn, &functionTable, sizeof(void *)) < 0 ||
      rpc_read(conn, &functionWindowSize, sizeof(size_t)) < 0 ||
      rpc_read(conn, &dataTable, sizeof(void *)) < 0 ||
      rpc_read(conn, &dataWindowSize, sizeof(size_t)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterUnifiedTable");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle, functionTable, functionWindowSize, dataTable,
     dataWindowSize);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle___cudaPushCallConfiguration(conn_t *conn) {
  dim3 gridDim;
  dim3 blockDim;
  size_t sharedMem;
  void *stream;
  int request_id;
  unsigned return_value;
  using fn_t = unsigned (*)(dim3, dim3, size_t, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &gridDim, sizeof(dim3)) < 0 ||
      rpc_read(conn, &blockDim, sizeof(dim3)) < 0 ||
      rpc_read(conn, &sharedMem, sizeof(size_t)) < 0 ||
      rpc_read(conn, &stream, sizeof(void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaPushCallConfiguration");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(gridDim, blockDim, sharedMem, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(unsigned)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle___cudaPopCallConfiguration(conn_t *conn) {
  dim3 *gridDim_null_check;
  dim3 gridDim;
  dim3 *blockDim_null_check;
  dim3 blockDim;
  size_t *sharedMem_null_check;
  size_t sharedMem;
  void *stream = nullptr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(dim3 *, dim3 *, size_t *, void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &gridDim_null_check, sizeof(dim3 *)) < 0 ||
      rpc_read(conn, &blockDim_null_check, sizeof(dim3 *)) < 0 ||
      rpc_read(conn, &sharedMem_null_check, sizeof(size_t *)) < 0 || false)
    goto ERROR_0;
  stream = (void *)malloc(8);
  if ((8 != 0 && stream == nullptr) || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaPopCallConfiguration");
  return_value = fn == nullptr
                     ? function_not_found()
                     : fn(gridDim_null_check ? &gridDim : nullptr,
                          blockDim_null_check ? &blockDim : nullptr,
                          sharedMem_null_check ? &sharedMem : nullptr, stream);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &gridDim_null_check, sizeof(dim3 *)) < 0 ||
      (gridDim_null_check && rpc_write(conn, &gridDim, sizeof(dim3)) < 0) ||
      rpc_write(conn, &blockDim_null_check, sizeof(dim3 *)) < 0 ||
      (blockDim_null_check && rpc_write(conn, &blockDim, sizeof(dim3)) < 0) ||
      rpc_write(conn, &sharedMem_null_check, sizeof(size_t *)) < 0 ||
      (sharedMem_null_check &&
       rpc_write(conn, &sharedMem, sizeof(size_t)) < 0) ||
      rpc_write(conn, stream, 8) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  free((void *)stream);
  return 0;
ERROR_0:
  free((void *)stream);
  return -1;
}

int handle___cudaRegisterFatBinaryEnd(conn_t *conn) {
  void **fatCubinHandle;
  int request_id;
  using fn_t = void (*)(void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaRegisterFatBinaryEnd");
  if (fn == nullptr)
    goto ERROR_0;
  fn(fatCubinHandle);

  if (rpc_write_start_response(conn, request_id) < 0 || rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

int handle___cudaInitModule(conn_t *conn) {
  void **fatCubinHandle;
  int request_id;
  char return_value;
  using fn_t = char (*)(void **);
  fn_t fn = nullptr;
  if (rpc_read(conn, &fatCubinHandle, sizeof(void **)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaInitModule");
  if (fn == nullptr)
    goto ERROR_0;
  return_value = fn(fatCubinHandle);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &return_value, sizeof(char)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#if CUDART_VERSION >= 13000
int handle___cudaGetKernel(conn_t *conn) {
  cudaKernel_t kernel{};
  kernel = {};
  const void *entryFuncAddr;
  int request_id;
  cudaError_t return_value;
  using fn_t = cudaError_t (*)(cudaKernel_t *, const void *);
  fn_t fn = nullptr;
  if (rpc_read(conn, &entryFuncAddr, sizeof(const void *)) < 0 || false)
    goto ERROR_0;

  request_id = rpc_read_end(conn);
  if (request_id < 0)
    goto ERROR_0;

  fn = cudart_symbol<fn_t>("__cudaGetKernel");
  return_value =
      fn == nullptr ? function_not_found() : fn(&kernel, entryFuncAddr);

  if (rpc_write_start_response(conn, request_id) < 0 ||
      rpc_write(conn, &kernel, sizeof(cudaKernel_t)) < 0 ||
      rpc_write(conn, &return_value, sizeof(cudaError_t)) < 0 ||
      rpc_write_end(conn) < 0)
    goto ERROR_0;
  return 0;
ERROR_0:
  return -1;
}

#endif
