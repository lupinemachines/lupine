[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lupine-auto"
version = "2.0.0"
description = "Automatic Python startup bootstrap for LUPINE"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.hatch.build.targets.wheel]
packages = ["lupine_auto"]

[tool.hatch.build.targets.wheel.force-include]
"lupine_auto.pth" = "lupine_auto.pth"
