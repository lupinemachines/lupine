# lupine-autocloud

This companion package is installed by `lupine[autocloud]`. It activates
LUPINE's bundled native shims at Python startup and defaults `LUPINE_SERVER`
to the hosted demo when no server was explicitly configured.

Set `LUPINE_AUTOCLOUD=0` to disable the startup hook for a process.
