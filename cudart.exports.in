/* The version node is literally NVIDIA's SONAME: consumers such as PyTorch
   reference runtime symbols as name@libcudart.so.<major>. */
libcudart.so.@LUPINE_CUDART_SOVERSION@ {
  global:
    cuda*;
    __cuda*;
  local:
    *;
};
